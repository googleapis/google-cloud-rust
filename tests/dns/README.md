# Integration tests for the DNS library

This library is discovery-based and uses `{+resource}` syntax in the request
paths. At the time of its onboarding, no other integration test covered that
particular combination.
