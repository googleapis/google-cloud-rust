# Integration tests for `google-cloud-bigquery-v2`

The BigQuery v2 API has a few quirks. This directory contains integration test
to verify the generator produces code that takes those quirks into account.
