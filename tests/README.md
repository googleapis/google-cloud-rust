# Complex tests for more than one client libraries

Use this directory for integration tests using generated clients, complex tests
using more than one client library, or any other test that is too large or
complex to include in a single crate.
