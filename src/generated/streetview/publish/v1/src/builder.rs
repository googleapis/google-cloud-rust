// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// Code generated by sidekick. DO NOT EDIT.

/// Request and client builders for [StreetViewPublishService][crate::client::StreetViewPublishService].
pub mod street_view_publish_service {
    use crate::Result;

    /// A builder for [StreetViewPublishService][crate::client::StreetViewPublishService].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_streetview_publish_v1::*;
    /// # use builder::street_view_publish_service::ClientBuilder;
    /// # use client::StreetViewPublishService;
    /// let builder : ClientBuilder = StreetViewPublishService::builder();
    /// let client = builder
    ///     .with_endpoint("https://streetviewpublish.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::StreetViewPublishService;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = StreetViewPublishService;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::StreetViewPublishService] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::StreetViewPublishService>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StreetViewPublishService>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [StreetViewPublishService::start_upload][crate::client::StreetViewPublishService::start_upload] calls.
    ///
    /// # Example
    /// ```
    /// # use google_streetview_publish_v1::builder::street_view_publish_service::StartUpload;
    /// # async fn sample() -> google_streetview_publish_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> StartUpload {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct StartUpload(RequestBuilder<wkt::Empty>);

    impl StartUpload {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StreetViewPublishService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<wkt::Empty>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::UploadRef> {
            (*self.0.stub)
                .start_upload(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for StartUpload {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StreetViewPublishService::create_photo][crate::client::StreetViewPublishService::create_photo] calls.
    ///
    /// # Example
    /// ```
    /// # use google_streetview_publish_v1::builder::street_view_publish_service::CreatePhoto;
    /// # async fn sample() -> google_streetview_publish_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CreatePhoto {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CreatePhoto(RequestBuilder<crate::model::CreatePhotoRequest>);

    impl CreatePhoto {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StreetViewPublishService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::CreatePhotoRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Photo> {
            (*self.0.stub)
                .create_photo(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [photo][crate::model::CreatePhotoRequest::photo].
        ///
        /// This is a **required** field for requests.
        pub fn set_photo<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Photo>,
        {
            self.0.request.photo = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [photo][crate::model::CreatePhotoRequest::photo].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_photo<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Photo>,
        {
            self.0.request.photo = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CreatePhoto {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StreetViewPublishService::get_photo][crate::client::StreetViewPublishService::get_photo] calls.
    ///
    /// # Example
    /// ```
    /// # use google_streetview_publish_v1::builder::street_view_publish_service::GetPhoto;
    /// # async fn sample() -> google_streetview_publish_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetPhoto {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetPhoto(RequestBuilder<crate::model::GetPhotoRequest>);

    impl GetPhoto {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StreetViewPublishService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetPhotoRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Photo> {
            (*self.0.stub)
                .get_photo(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [photo_id][crate::model::GetPhotoRequest::photo_id].
        ///
        /// This is a **required** field for requests.
        pub fn set_photo_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.photo_id = v.into();
            self
        }

        /// Sets the value of [view][crate::model::GetPhotoRequest::view].
        ///
        /// This is a **required** field for requests.
        pub fn set_view<T: Into<crate::model::PhotoView>>(mut self, v: T) -> Self {
            self.0.request.view = v.into();
            self
        }

        /// Sets the value of [language_code][crate::model::GetPhotoRequest::language_code].
        pub fn set_language_code<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.language_code = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetPhoto {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StreetViewPublishService::batch_get_photos][crate::client::StreetViewPublishService::batch_get_photos] calls.
    ///
    /// # Example
    /// ```
    /// # use google_streetview_publish_v1::builder::street_view_publish_service::BatchGetPhotos;
    /// # async fn sample() -> google_streetview_publish_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> BatchGetPhotos {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct BatchGetPhotos(RequestBuilder<crate::model::BatchGetPhotosRequest>);

    impl BatchGetPhotos {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StreetViewPublishService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::BatchGetPhotosRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::BatchGetPhotosResponse> {
            (*self.0.stub)
                .batch_get_photos(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [photo_ids][crate::model::BatchGetPhotosRequest::photo_ids].
        ///
        /// This is a **required** field for requests.
        pub fn set_photo_ids<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<std::string::String>,
        {
            use std::iter::Iterator;
            self.0.request.photo_ids = v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [view][crate::model::BatchGetPhotosRequest::view].
        ///
        /// This is a **required** field for requests.
        pub fn set_view<T: Into<crate::model::PhotoView>>(mut self, v: T) -> Self {
            self.0.request.view = v.into();
            self
        }

        /// Sets the value of [language_code][crate::model::BatchGetPhotosRequest::language_code].
        pub fn set_language_code<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.language_code = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for BatchGetPhotos {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StreetViewPublishService::list_photos][crate::client::StreetViewPublishService::list_photos] calls.
    ///
    /// # Example
    /// ```
    /// # use google_streetview_publish_v1::builder::street_view_publish_service::ListPhotos;
    /// # async fn sample() -> google_streetview_publish_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListPhotos {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListPhotos(RequestBuilder<crate::model::ListPhotosRequest>);

    impl ListPhotos {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StreetViewPublishService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ListPhotosRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ListPhotosResponse> {
            (*self.0.stub)
                .list_photos(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::ListPhotosResponse, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ListPhotosResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [view][crate::model::ListPhotosRequest::view].
        ///
        /// This is a **required** field for requests.
        pub fn set_view<T: Into<crate::model::PhotoView>>(mut self, v: T) -> Self {
            self.0.request.view = v.into();
            self
        }

        /// Sets the value of [page_size][crate::model::ListPhotosRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::ListPhotosRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }

        /// Sets the value of [filter][crate::model::ListPhotosRequest::filter].
        pub fn set_filter<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.filter = v.into();
            self
        }

        /// Sets the value of [language_code][crate::model::ListPhotosRequest::language_code].
        pub fn set_language_code<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.language_code = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListPhotos {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StreetViewPublishService::update_photo][crate::client::StreetViewPublishService::update_photo] calls.
    ///
    /// # Example
    /// ```
    /// # use google_streetview_publish_v1::builder::street_view_publish_service::UpdatePhoto;
    /// # async fn sample() -> google_streetview_publish_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdatePhoto {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdatePhoto(RequestBuilder<crate::model::UpdatePhotoRequest>);

    impl UpdatePhoto {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StreetViewPublishService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::UpdatePhotoRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Photo> {
            (*self.0.stub)
                .update_photo(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [photo][crate::model::UpdatePhotoRequest::photo].
        ///
        /// This is a **required** field for requests.
        pub fn set_photo<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Photo>,
        {
            self.0.request.photo = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [photo][crate::model::UpdatePhotoRequest::photo].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_photo<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Photo>,
        {
            self.0.request.photo = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::UpdatePhotoRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::UpdatePhotoRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdatePhoto {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StreetViewPublishService::batch_update_photos][crate::client::StreetViewPublishService::batch_update_photos] calls.
    ///
    /// # Example
    /// ```
    /// # use google_streetview_publish_v1::builder::street_view_publish_service::BatchUpdatePhotos;
    /// # async fn sample() -> google_streetview_publish_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> BatchUpdatePhotos {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct BatchUpdatePhotos(RequestBuilder<crate::model::BatchUpdatePhotosRequest>);

    impl BatchUpdatePhotos {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StreetViewPublishService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::BatchUpdatePhotosRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::BatchUpdatePhotosResponse> {
            (*self.0.stub)
                .batch_update_photos(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [update_photo_requests][crate::model::BatchUpdatePhotosRequest::update_photo_requests].
        ///
        /// This is a **required** field for requests.
        pub fn set_update_photo_requests<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<crate::model::UpdatePhotoRequest>,
        {
            use std::iter::Iterator;
            self.0.request.update_photo_requests = v.into_iter().map(|i| i.into()).collect();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for BatchUpdatePhotos {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StreetViewPublishService::delete_photo][crate::client::StreetViewPublishService::delete_photo] calls.
    ///
    /// # Example
    /// ```
    /// # use google_streetview_publish_v1::builder::street_view_publish_service::DeletePhoto;
    /// # async fn sample() -> google_streetview_publish_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeletePhoto {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeletePhoto(RequestBuilder<crate::model::DeletePhotoRequest>);

    impl DeletePhoto {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StreetViewPublishService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::DeletePhotoRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<()> {
            (*self.0.stub)
                .delete_photo(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [photo_id][crate::model::DeletePhotoRequest::photo_id].
        ///
        /// This is a **required** field for requests.
        pub fn set_photo_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.photo_id = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeletePhoto {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StreetViewPublishService::batch_delete_photos][crate::client::StreetViewPublishService::batch_delete_photos] calls.
    ///
    /// # Example
    /// ```
    /// # use google_streetview_publish_v1::builder::street_view_publish_service::BatchDeletePhotos;
    /// # async fn sample() -> google_streetview_publish_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> BatchDeletePhotos {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct BatchDeletePhotos(RequestBuilder<crate::model::BatchDeletePhotosRequest>);

    impl BatchDeletePhotos {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StreetViewPublishService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::BatchDeletePhotosRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::BatchDeletePhotosResponse> {
            (*self.0.stub)
                .batch_delete_photos(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [photo_ids][crate::model::BatchDeletePhotosRequest::photo_ids].
        ///
        /// This is a **required** field for requests.
        pub fn set_photo_ids<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<std::string::String>,
        {
            use std::iter::Iterator;
            self.0.request.photo_ids = v.into_iter().map(|i| i.into()).collect();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for BatchDeletePhotos {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StreetViewPublishService::start_photo_sequence_upload][crate::client::StreetViewPublishService::start_photo_sequence_upload] calls.
    ///
    /// # Example
    /// ```
    /// # use google_streetview_publish_v1::builder::street_view_publish_service::StartPhotoSequenceUpload;
    /// # async fn sample() -> google_streetview_publish_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> StartPhotoSequenceUpload {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct StartPhotoSequenceUpload(RequestBuilder<wkt::Empty>);

    impl StartPhotoSequenceUpload {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StreetViewPublishService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<wkt::Empty>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::UploadRef> {
            (*self.0.stub)
                .start_photo_sequence_upload(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for StartPhotoSequenceUpload {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StreetViewPublishService::create_photo_sequence][crate::client::StreetViewPublishService::create_photo_sequence] calls.
    ///
    /// # Example
    /// ```
    /// # use google_streetview_publish_v1::builder::street_view_publish_service::CreatePhotoSequence;
    /// # async fn sample() -> google_streetview_publish_v1::Result<()> {
    /// use google_cloud_lro::Poller;
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.poller().until_done().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CreatePhotoSequence {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CreatePhotoSequence(RequestBuilder<crate::model::CreatePhotoSequenceRequest>);

    impl CreatePhotoSequence {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StreetViewPublishService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::CreatePhotoSequenceRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        ///
        /// # Long running operations
        ///
        /// This starts, but does not poll, a longrunning operation. More information
        /// on [create_photo_sequence][crate::client::StreetViewPublishService::create_photo_sequence].
        pub async fn send(self) -> Result<google_cloud_longrunning::model::Operation> {
            (*self.0.stub)
                .create_photo_sequence(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `create_photo_sequence`.
        pub fn poller(self) -> impl google_cloud_lro::Poller<crate::model::PhotoSequence, ()> {
            type Operation =
                google_cloud_lro::internal::Operation<crate::model::PhotoSequence, wkt::Empty>;
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_streetview_publish_v1::client::StreetViewPublishService::create_photo_sequence::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    let op = GetOperation::new(stub)
                        .set_name(name)
                        .with_options(options)
                        .send()
                        .await?;
                    Ok(Operation::new(op))
                }
            };

            let start = move || async {
                let op = self.send().await?;
                Ok(Operation::new(op))
            };

            use google_cloud_lro::internal::PollerExt;
            {
                google_cloud_lro::internal::new_unit_metadata_poller(
                    polling_error_policy,
                    polling_backoff_policy,
                    start,
                    query,
                )
            }
            .with_options(poller_options)
        }

        /// Sets the value of [photo_sequence][crate::model::CreatePhotoSequenceRequest::photo_sequence].
        ///
        /// This is a **required** field for requests.
        pub fn set_photo_sequence<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::PhotoSequence>,
        {
            self.0.request.photo_sequence = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [photo_sequence][crate::model::CreatePhotoSequenceRequest::photo_sequence].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_photo_sequence<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::PhotoSequence>,
        {
            self.0.request.photo_sequence = v.map(|x| x.into());
            self
        }

        /// Sets the value of [input_type][crate::model::CreatePhotoSequenceRequest::input_type].
        ///
        /// This is a **required** field for requests.
        pub fn set_input_type<T: Into<crate::model::create_photo_sequence_request::InputType>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.input_type = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CreatePhotoSequence {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StreetViewPublishService::get_photo_sequence][crate::client::StreetViewPublishService::get_photo_sequence] calls.
    ///
    /// # Example
    /// ```
    /// # use google_streetview_publish_v1::builder::street_view_publish_service::GetPhotoSequence;
    /// # async fn sample() -> google_streetview_publish_v1::Result<()> {
    /// use google_cloud_lro::Poller;
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.poller().until_done().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetPhotoSequence {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetPhotoSequence(RequestBuilder<crate::model::GetPhotoSequenceRequest>);

    impl GetPhotoSequence {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StreetViewPublishService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetPhotoSequenceRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        ///
        /// # Long running operations
        ///
        /// This starts, but does not poll, a longrunning operation. More information
        /// on [get_photo_sequence][crate::client::StreetViewPublishService::get_photo_sequence].
        pub async fn send(self) -> Result<google_cloud_longrunning::model::Operation> {
            (*self.0.stub)
                .get_photo_sequence(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `get_photo_sequence`.
        pub fn poller(self) -> impl google_cloud_lro::Poller<crate::model::PhotoSequence, ()> {
            type Operation =
                google_cloud_lro::internal::Operation<crate::model::PhotoSequence, wkt::Empty>;
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_streetview_publish_v1::client::StreetViewPublishService::get_photo_sequence::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    let op = GetOperation::new(stub)
                        .set_name(name)
                        .with_options(options)
                        .send()
                        .await?;
                    Ok(Operation::new(op))
                }
            };

            let start = move || async {
                let op = self.send().await?;
                Ok(Operation::new(op))
            };

            use google_cloud_lro::internal::PollerExt;
            {
                google_cloud_lro::internal::new_unit_metadata_poller(
                    polling_error_policy,
                    polling_backoff_policy,
                    start,
                    query,
                )
            }
            .with_options(poller_options)
        }

        /// Sets the value of [sequence_id][crate::model::GetPhotoSequenceRequest::sequence_id].
        ///
        /// This is a **required** field for requests.
        pub fn set_sequence_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.sequence_id = v.into();
            self
        }

        /// Sets the value of [view][crate::model::GetPhotoSequenceRequest::view].
        #[deprecated]
        pub fn set_view<T: Into<crate::model::PhotoView>>(mut self, v: T) -> Self {
            self.0.request.view = v.into();
            self
        }

        /// Sets the value of [filter][crate::model::GetPhotoSequenceRequest::filter].
        pub fn set_filter<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.filter = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetPhotoSequence {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StreetViewPublishService::list_photo_sequences][crate::client::StreetViewPublishService::list_photo_sequences] calls.
    ///
    /// # Example
    /// ```
    /// # use google_streetview_publish_v1::builder::street_view_publish_service::ListPhotoSequences;
    /// # async fn sample() -> google_streetview_publish_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListPhotoSequences {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListPhotoSequences(RequestBuilder<crate::model::ListPhotoSequencesRequest>);

    impl ListPhotoSequences {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StreetViewPublishService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ListPhotoSequencesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ListPhotoSequencesResponse> {
            (*self.0.stub)
                .list_photo_sequences(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::ListPhotoSequencesResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ListPhotoSequencesResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [page_size][crate::model::ListPhotoSequencesRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::ListPhotoSequencesRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }

        /// Sets the value of [filter][crate::model::ListPhotoSequencesRequest::filter].
        pub fn set_filter<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.filter = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListPhotoSequences {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StreetViewPublishService::delete_photo_sequence][crate::client::StreetViewPublishService::delete_photo_sequence] calls.
    ///
    /// # Example
    /// ```
    /// # use google_streetview_publish_v1::builder::street_view_publish_service::DeletePhotoSequence;
    /// # async fn sample() -> google_streetview_publish_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeletePhotoSequence {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeletePhotoSequence(RequestBuilder<crate::model::DeletePhotoSequenceRequest>);

    impl DeletePhotoSequence {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StreetViewPublishService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::DeletePhotoSequenceRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<()> {
            (*self.0.stub)
                .delete_photo_sequence(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [sequence_id][crate::model::DeletePhotoSequenceRequest::sequence_id].
        ///
        /// This is a **required** field for requests.
        pub fn set_sequence_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.sequence_id = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeletePhotoSequence {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StreetViewPublishService::get_operation][crate::client::StreetViewPublishService::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_streetview_publish_v1::builder::street_view_publish_service::GetOperation;
    /// # async fn sample() -> google_streetview_publish_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<google_cloud_longrunning::model::GetOperationRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StreetViewPublishService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<google_cloud_longrunning::model::GetOperationRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<google_cloud_longrunning::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][google_cloud_longrunning::model::GetOperationRequest::name].
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}
