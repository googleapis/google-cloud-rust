// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// Code generated by sidekick. DO NOT EDIT.

/// Request and client builders for [DataPointsService][crate::client::DataPointsService].
pub mod data_points_service {
    use crate::Result;

    /// A builder for [DataPointsService][crate::client::DataPointsService].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_devicesandservices_health_v4::*;
    /// # use builder::data_points_service::ClientBuilder;
    /// # use client::DataPointsService;
    /// let builder : ClientBuilder = DataPointsService::builder();
    /// let client = builder
    ///     .with_endpoint("https://health.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::DataPointsService;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = DataPointsService;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::DataPointsService] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::DataPointsService>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DataPointsService>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [DataPointsService::get_data_point][crate::client::DataPointsService::get_data_point] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::data_points_service::GetDataPoint;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetDataPoint {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetDataPoint(RequestBuilder<crate::model::GetDataPointRequest>);

    impl GetDataPoint {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DataPointsService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetDataPointRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::DataPoint> {
            (*self.0.stub)
                .get_data_point(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::GetDataPointRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetDataPoint {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DataPointsService::list_data_points][crate::client::DataPointsService::list_data_points] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::data_points_service::ListDataPoints;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListDataPoints {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListDataPoints(RequestBuilder<crate::model::ListDataPointsRequest>);

    impl ListDataPoints {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DataPointsService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ListDataPointsRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ListDataPointsResponse> {
            (*self.0.stub)
                .list_data_points(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::ListDataPointsResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ListDataPointsResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [parent][crate::model::ListDataPointsRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [page_size][crate::model::ListDataPointsRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::ListDataPointsRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }

        /// Sets the value of [filter][crate::model::ListDataPointsRequest::filter].
        pub fn set_filter<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.filter = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListDataPoints {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DataPointsService::create_data_point][crate::client::DataPointsService::create_data_point] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::data_points_service::CreateDataPoint;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    /// use google_cloud_lro::Poller;
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.poller().until_done().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CreateDataPoint {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CreateDataPoint(RequestBuilder<crate::model::CreateDataPointRequest>);

    impl CreateDataPoint {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DataPointsService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::CreateDataPointRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        ///
        /// # Long running operations
        ///
        /// This starts, but does not poll, a longrunning operation. More information
        /// on [create_data_point][crate::client::DataPointsService::create_data_point].
        pub async fn send(self) -> Result<google_cloud_longrunning::model::Operation> {
            (*self.0.stub)
                .create_data_point(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `create_data_point`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<
            crate::model::DataPoint,
            crate::model::CreateDataPointOperationMetadata,
        > {
            type Operation = google_cloud_lro::internal::Operation<
                crate::model::DataPoint,
                crate::model::CreateDataPointOperationMetadata,
            >;
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            #[cfg(google_cloud_unstable_tracing)]
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            #[cfg(google_cloud_unstable_tracing)]
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_devicesandservices_health_v4::client::DataPointsService::create_data_point::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    let op = GetOperation::new(stub)
                        .set_name(name)
                        .with_options(options)
                        .send()
                        .await?;
                    Ok(Operation::new(op))
                }
            };

            let start = move || async {
                let op = self.send().await?;
                Ok(Operation::new(op))
            };

            #[cfg(google_cloud_unstable_tracing)]
            {
                use google_cloud_lro::internal::PollerExt;
                {
                    google_cloud_lro::internal::new_poller(
                        polling_error_policy,
                        polling_backoff_policy,
                        start,
                        query,
                    )
                }
                .with_options(poller_options)
            }

            #[cfg(not(google_cloud_unstable_tracing))]
            {
                google_cloud_lro::internal::new_poller(
                    polling_error_policy,
                    polling_backoff_policy,
                    start,
                    query,
                )
            }
        }

        /// Sets the value of [parent][crate::model::CreateDataPointRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [data_point][crate::model::CreateDataPointRequest::data_point].
        ///
        /// This is a **required** field for requests.
        pub fn set_data_point<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DataPoint>,
        {
            self.0.request.data_point = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [data_point][crate::model::CreateDataPointRequest::data_point].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_data_point<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DataPoint>,
        {
            self.0.request.data_point = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CreateDataPoint {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DataPointsService::update_data_point][crate::client::DataPointsService::update_data_point] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::data_points_service::UpdateDataPoint;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    /// use google_cloud_lro::Poller;
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.poller().until_done().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateDataPoint {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateDataPoint(RequestBuilder<crate::model::UpdateDataPointRequest>);

    impl UpdateDataPoint {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DataPointsService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::UpdateDataPointRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        ///
        /// # Long running operations
        ///
        /// This starts, but does not poll, a longrunning operation. More information
        /// on [update_data_point][crate::client::DataPointsService::update_data_point].
        pub async fn send(self) -> Result<google_cloud_longrunning::model::Operation> {
            (*self.0.stub)
                .update_data_point(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update_data_point`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<
            crate::model::DataPoint,
            crate::model::UpdateDataPointOperationMetadata,
        > {
            type Operation = google_cloud_lro::internal::Operation<
                crate::model::DataPoint,
                crate::model::UpdateDataPointOperationMetadata,
            >;
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            #[cfg(google_cloud_unstable_tracing)]
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            #[cfg(google_cloud_unstable_tracing)]
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_devicesandservices_health_v4::client::DataPointsService::update_data_point::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    let op = GetOperation::new(stub)
                        .set_name(name)
                        .with_options(options)
                        .send()
                        .await?;
                    Ok(Operation::new(op))
                }
            };

            let start = move || async {
                let op = self.send().await?;
                Ok(Operation::new(op))
            };

            #[cfg(google_cloud_unstable_tracing)]
            {
                use google_cloud_lro::internal::PollerExt;
                {
                    google_cloud_lro::internal::new_poller(
                        polling_error_policy,
                        polling_backoff_policy,
                        start,
                        query,
                    )
                }
                .with_options(poller_options)
            }

            #[cfg(not(google_cloud_unstable_tracing))]
            {
                google_cloud_lro::internal::new_poller(
                    polling_error_policy,
                    polling_backoff_policy,
                    start,
                    query,
                )
            }
        }

        /// Sets the value of [data_point][crate::model::UpdateDataPointRequest::data_point].
        ///
        /// This is a **required** field for requests.
        pub fn set_data_point<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DataPoint>,
        {
            self.0.request.data_point = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [data_point][crate::model::UpdateDataPointRequest::data_point].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_data_point<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DataPoint>,
        {
            self.0.request.data_point = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateDataPoint {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DataPointsService::batch_delete_data_points][crate::client::DataPointsService::batch_delete_data_points] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::data_points_service::BatchDeleteDataPoints;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    /// use google_cloud_lro::Poller;
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.poller().until_done().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> BatchDeleteDataPoints {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct BatchDeleteDataPoints(RequestBuilder<crate::model::BatchDeleteDataPointsRequest>);

    impl BatchDeleteDataPoints {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DataPointsService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::BatchDeleteDataPointsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        ///
        /// # Long running operations
        ///
        /// This starts, but does not poll, a longrunning operation. More information
        /// on [batch_delete_data_points][crate::client::DataPointsService::batch_delete_data_points].
        pub async fn send(self) -> Result<google_cloud_longrunning::model::Operation> {
            (*self.0.stub)
                .batch_delete_data_points(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `batch_delete_data_points`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<
            crate::model::BatchDeleteDataPointsResponse,
            crate::model::BatchDeleteDataPointsOperationMetadata,
        > {
            type Operation = google_cloud_lro::internal::Operation<
                crate::model::BatchDeleteDataPointsResponse,
                crate::model::BatchDeleteDataPointsOperationMetadata,
            >;
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            #[cfg(google_cloud_unstable_tracing)]
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            #[cfg(google_cloud_unstable_tracing)]
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_devicesandservices_health_v4::client::DataPointsService::batch_delete_data_points::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    let op = GetOperation::new(stub)
                        .set_name(name)
                        .with_options(options)
                        .send()
                        .await?;
                    Ok(Operation::new(op))
                }
            };

            let start = move || async {
                let op = self.send().await?;
                Ok(Operation::new(op))
            };

            #[cfg(google_cloud_unstable_tracing)]
            {
                use google_cloud_lro::internal::PollerExt;
                {
                    google_cloud_lro::internal::new_poller(
                        polling_error_policy,
                        polling_backoff_policy,
                        start,
                        query,
                    )
                }
                .with_options(poller_options)
            }

            #[cfg(not(google_cloud_unstable_tracing))]
            {
                google_cloud_lro::internal::new_poller(
                    polling_error_policy,
                    polling_backoff_policy,
                    start,
                    query,
                )
            }
        }

        /// Sets the value of [parent][crate::model::BatchDeleteDataPointsRequest::parent].
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [names][crate::model::BatchDeleteDataPointsRequest::names].
        ///
        /// This is a **required** field for requests.
        pub fn set_names<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<std::string::String>,
        {
            use std::iter::Iterator;
            self.0.request.names = v.into_iter().map(|i| i.into()).collect();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for BatchDeleteDataPoints {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DataPointsService::reconcile_data_points][crate::client::DataPointsService::reconcile_data_points] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::data_points_service::ReconcileDataPoints;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ReconcileDataPoints {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ReconcileDataPoints(RequestBuilder<crate::model::ReconcileDataPointsRequest>);

    impl ReconcileDataPoints {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DataPointsService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ReconcileDataPointsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ReconcileDataPointsResponse> {
            (*self.0.stub)
                .reconcile_data_points(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::ReconcileDataPointsResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ReconcileDataPointsResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [parent][crate::model::ReconcileDataPointsRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [page_size][crate::model::ReconcileDataPointsRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::ReconcileDataPointsRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }

        /// Sets the value of [filter][crate::model::ReconcileDataPointsRequest::filter].
        pub fn set_filter<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.filter = v.into();
            self
        }

        /// Sets the value of [data_source_family][crate::model::ReconcileDataPointsRequest::data_source_family].
        pub fn set_data_source_family<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.data_source_family = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ReconcileDataPoints {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DataPointsService::roll_up_data_points][crate::client::DataPointsService::roll_up_data_points] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::data_points_service::RollUpDataPoints;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> RollUpDataPoints {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct RollUpDataPoints(RequestBuilder<crate::model::RollUpDataPointsRequest>);

    impl RollUpDataPoints {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DataPointsService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::RollUpDataPointsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::RollUpDataPointsResponse> {
            (*self.0.stub)
                .roll_up_data_points(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::RollUpDataPointsResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::RollUpDataPointsResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [parent][crate::model::RollUpDataPointsRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [range][crate::model::RollUpDataPointsRequest::range].
        ///
        /// This is a **required** field for requests.
        pub fn set_range<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<google_cloud_type::model::Interval>,
        {
            self.0.request.range = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [range][crate::model::RollUpDataPointsRequest::range].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_range<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<google_cloud_type::model::Interval>,
        {
            self.0.request.range = v.map(|x| x.into());
            self
        }

        /// Sets the value of [window_size][crate::model::RollUpDataPointsRequest::window_size].
        ///
        /// This is a **required** field for requests.
        pub fn set_window_size<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::Duration>,
        {
            self.0.request.window_size = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [window_size][crate::model::RollUpDataPointsRequest::window_size].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_window_size<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::Duration>,
        {
            self.0.request.window_size = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_size][crate::model::RollUpDataPointsRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::RollUpDataPointsRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }

        /// Sets the value of [data_source_family][crate::model::RollUpDataPointsRequest::data_source_family].
        pub fn set_data_source_family<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.data_source_family = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for RollUpDataPoints {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DataPointsService::daily_roll_up_data_points][crate::client::DataPointsService::daily_roll_up_data_points] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::data_points_service::DailyRollUpDataPoints;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DailyRollUpDataPoints {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DailyRollUpDataPoints(RequestBuilder<crate::model::DailyRollUpDataPointsRequest>);

    impl DailyRollUpDataPoints {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DataPointsService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::DailyRollUpDataPointsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::DailyRollUpDataPointsResponse> {
            (*self.0.stub)
                .daily_roll_up_data_points(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [parent][crate::model::DailyRollUpDataPointsRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [range][crate::model::DailyRollUpDataPointsRequest::range].
        ///
        /// This is a **required** field for requests.
        pub fn set_range<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::CivilTimeInterval>,
        {
            self.0.request.range = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [range][crate::model::DailyRollUpDataPointsRequest::range].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_range<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::CivilTimeInterval>,
        {
            self.0.request.range = v.map(|x| x.into());
            self
        }

        /// Sets the value of [window_size_days][crate::model::DailyRollUpDataPointsRequest::window_size_days].
        pub fn set_window_size_days<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.window_size_days = v.into();
            self
        }

        /// Sets the value of [page_size][crate::model::DailyRollUpDataPointsRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::DailyRollUpDataPointsRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }

        /// Sets the value of [data_source_family][crate::model::DailyRollUpDataPointsRequest::data_source_family].
        pub fn set_data_source_family<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.data_source_family = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DailyRollUpDataPoints {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DataPointsService::export_exercise_tcx][crate::client::DataPointsService::export_exercise_tcx] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::data_points_service::ExportExerciseTcx;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ExportExerciseTcx {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ExportExerciseTcx(RequestBuilder<crate::model::ExportExerciseTcxRequest>);

    impl ExportExerciseTcx {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DataPointsService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ExportExerciseTcxRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ExportExerciseTcxResponse> {
            (*self.0.stub)
                .export_exercise_tcx(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::ExportExerciseTcxRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }

        /// Sets the value of [partial_data][crate::model::ExportExerciseTcxRequest::partial_data].
        pub fn set_partial_data<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.partial_data = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ExportExerciseTcx {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DataPointsService::get_operation][crate::client::DataPointsService::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::data_points_service::GetOperation;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<google_cloud_longrunning::model::GetOperationRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DataPointsService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<google_cloud_longrunning::model::GetOperationRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<google_cloud_longrunning::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][google_cloud_longrunning::model::GetOperationRequest::name].
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [DataSubscriptionService][crate::client::DataSubscriptionService].
pub mod data_subscription_service {
    use crate::Result;

    /// A builder for [DataSubscriptionService][crate::client::DataSubscriptionService].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_devicesandservices_health_v4::*;
    /// # use builder::data_subscription_service::ClientBuilder;
    /// # use client::DataSubscriptionService;
    /// let builder : ClientBuilder = DataSubscriptionService::builder();
    /// let client = builder
    ///     .with_endpoint("https://health.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::DataSubscriptionService;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = DataSubscriptionService;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::DataSubscriptionService] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::DataSubscriptionService>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DataSubscriptionService>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [DataSubscriptionService::create_subscriber][crate::client::DataSubscriptionService::create_subscriber] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::data_subscription_service::CreateSubscriber;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    /// use google_cloud_lro::Poller;
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.poller().until_done().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CreateSubscriber {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CreateSubscriber(RequestBuilder<crate::model::CreateSubscriberRequest>);

    impl CreateSubscriber {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DataSubscriptionService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::CreateSubscriberRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        ///
        /// # Long running operations
        ///
        /// This starts, but does not poll, a longrunning operation. More information
        /// on [create_subscriber][crate::client::DataSubscriptionService::create_subscriber].
        pub async fn send(self) -> Result<google_cloud_longrunning::model::Operation> {
            (*self.0.stub)
                .create_subscriber(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `create_subscriber`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<
            crate::model::Subscriber,
            crate::model::CreateSubscriberMetadata,
        > {
            type Operation = google_cloud_lro::internal::Operation<
                crate::model::Subscriber,
                crate::model::CreateSubscriberMetadata,
            >;
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            #[cfg(google_cloud_unstable_tracing)]
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            #[cfg(google_cloud_unstable_tracing)]
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_devicesandservices_health_v4::client::DataSubscriptionService::create_subscriber::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    let op = GetOperation::new(stub)
                        .set_name(name)
                        .with_options(options)
                        .send()
                        .await?;
                    Ok(Operation::new(op))
                }
            };

            let start = move || async {
                let op = self.send().await?;
                Ok(Operation::new(op))
            };

            #[cfg(google_cloud_unstable_tracing)]
            {
                use google_cloud_lro::internal::PollerExt;
                {
                    google_cloud_lro::internal::new_poller(
                        polling_error_policy,
                        polling_backoff_policy,
                        start,
                        query,
                    )
                }
                .with_options(poller_options)
            }

            #[cfg(not(google_cloud_unstable_tracing))]
            {
                google_cloud_lro::internal::new_poller(
                    polling_error_policy,
                    polling_backoff_policy,
                    start,
                    query,
                )
            }
        }

        /// Sets the value of [parent][crate::model::CreateSubscriberRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [subscriber][crate::model::CreateSubscriberRequest::subscriber].
        ///
        /// This is a **required** field for requests.
        pub fn set_subscriber<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::CreateSubscriberPayload>,
        {
            self.0.request.subscriber = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [subscriber][crate::model::CreateSubscriberRequest::subscriber].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_subscriber<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::CreateSubscriberPayload>,
        {
            self.0.request.subscriber = v.map(|x| x.into());
            self
        }

        /// Sets the value of [subscriber_id][crate::model::CreateSubscriberRequest::subscriber_id].
        pub fn set_subscriber_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.subscriber_id = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CreateSubscriber {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DataSubscriptionService::list_subscribers][crate::client::DataSubscriptionService::list_subscribers] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::data_subscription_service::ListSubscribers;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListSubscribers {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListSubscribers(RequestBuilder<crate::model::ListSubscribersRequest>);

    impl ListSubscribers {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DataSubscriptionService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ListSubscribersRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ListSubscribersResponse> {
            (*self.0.stub)
                .list_subscribers(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::ListSubscribersResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ListSubscribersResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [parent][crate::model::ListSubscribersRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [page_size][crate::model::ListSubscribersRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::ListSubscribersRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListSubscribers {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DataSubscriptionService::update_subscriber][crate::client::DataSubscriptionService::update_subscriber] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::data_subscription_service::UpdateSubscriber;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    /// use google_cloud_lro::Poller;
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.poller().until_done().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateSubscriber {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateSubscriber(RequestBuilder<crate::model::UpdateSubscriberRequest>);

    impl UpdateSubscriber {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DataSubscriptionService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::UpdateSubscriberRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        ///
        /// # Long running operations
        ///
        /// This starts, but does not poll, a longrunning operation. More information
        /// on [update_subscriber][crate::client::DataSubscriptionService::update_subscriber].
        pub async fn send(self) -> Result<google_cloud_longrunning::model::Operation> {
            (*self.0.stub)
                .update_subscriber(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update_subscriber`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<
            crate::model::Subscriber,
            crate::model::UpdateSubscriberMetadata,
        > {
            type Operation = google_cloud_lro::internal::Operation<
                crate::model::Subscriber,
                crate::model::UpdateSubscriberMetadata,
            >;
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            #[cfg(google_cloud_unstable_tracing)]
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            #[cfg(google_cloud_unstable_tracing)]
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_devicesandservices_health_v4::client::DataSubscriptionService::update_subscriber::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    let op = GetOperation::new(stub)
                        .set_name(name)
                        .with_options(options)
                        .send()
                        .await?;
                    Ok(Operation::new(op))
                }
            };

            let start = move || async {
                let op = self.send().await?;
                Ok(Operation::new(op))
            };

            #[cfg(google_cloud_unstable_tracing)]
            {
                use google_cloud_lro::internal::PollerExt;
                {
                    google_cloud_lro::internal::new_poller(
                        polling_error_policy,
                        polling_backoff_policy,
                        start,
                        query,
                    )
                }
                .with_options(poller_options)
            }

            #[cfg(not(google_cloud_unstable_tracing))]
            {
                google_cloud_lro::internal::new_poller(
                    polling_error_policy,
                    polling_backoff_policy,
                    start,
                    query,
                )
            }
        }

        /// Sets the value of [subscriber][crate::model::UpdateSubscriberRequest::subscriber].
        ///
        /// This is a **required** field for requests.
        pub fn set_subscriber<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Subscriber>,
        {
            self.0.request.subscriber = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [subscriber][crate::model::UpdateSubscriberRequest::subscriber].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_subscriber<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Subscriber>,
        {
            self.0.request.subscriber = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::UpdateSubscriberRequest::update_mask].
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::UpdateSubscriberRequest::update_mask].
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateSubscriber {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DataSubscriptionService::delete_subscriber][crate::client::DataSubscriptionService::delete_subscriber] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::data_subscription_service::DeleteSubscriber;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    /// use google_cloud_lro::Poller;
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.poller().until_done().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeleteSubscriber {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeleteSubscriber(RequestBuilder<crate::model::DeleteSubscriberRequest>);

    impl DeleteSubscriber {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DataSubscriptionService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::DeleteSubscriberRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        ///
        /// # Long running operations
        ///
        /// This starts, but does not poll, a longrunning operation. More information
        /// on [delete_subscriber][crate::client::DataSubscriptionService::delete_subscriber].
        pub async fn send(self) -> Result<google_cloud_longrunning::model::Operation> {
            (*self.0.stub)
                .delete_subscriber(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete_subscriber`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<(), crate::model::DeleteSubscriberMetadata> {
            type Operation = google_cloud_lro::internal::Operation<
                wkt::Empty,
                crate::model::DeleteSubscriberMetadata,
            >;
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            #[cfg(google_cloud_unstable_tracing)]
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            #[cfg(google_cloud_unstable_tracing)]
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_devicesandservices_health_v4::client::DataSubscriptionService::delete_subscriber::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    let op = GetOperation::new(stub)
                        .set_name(name)
                        .with_options(options)
                        .send()
                        .await?;
                    Ok(Operation::new(op))
                }
            };

            let start = move || async {
                let op = self.send().await?;
                Ok(Operation::new(op))
            };

            #[cfg(google_cloud_unstable_tracing)]
            {
                use google_cloud_lro::internal::PollerExt;
                {
                    google_cloud_lro::internal::new_unit_response_poller(
                        polling_error_policy,
                        polling_backoff_policy,
                        start,
                        query,
                    )
                }
                .with_options(poller_options)
            }

            #[cfg(not(google_cloud_unstable_tracing))]
            {
                google_cloud_lro::internal::new_unit_response_poller(
                    polling_error_policy,
                    polling_backoff_policy,
                    start,
                    query,
                )
            }
        }

        /// Sets the value of [name][crate::model::DeleteSubscriberRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }

        /// Sets the value of [force][crate::model::DeleteSubscriberRequest::force].
        pub fn set_force<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.force = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeleteSubscriber {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DataSubscriptionService::create_subscription][crate::client::DataSubscriptionService::create_subscription] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::data_subscription_service::CreateSubscription;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CreateSubscription {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CreateSubscription(RequestBuilder<crate::model::CreateSubscriptionRequest>);

    impl CreateSubscription {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DataSubscriptionService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::CreateSubscriptionRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Subscription> {
            (*self.0.stub)
                .create_subscription(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [parent][crate::model::CreateSubscriptionRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [subscription_id][crate::model::CreateSubscriptionRequest::subscription_id].
        pub fn set_subscription_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.subscription_id = v.into();
            self
        }

        /// Sets the value of [subscription][crate::model::CreateSubscriptionRequest::subscription].
        ///
        /// This is a **required** field for requests.
        pub fn set_subscription<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::CreateSubscriptionPayload>,
        {
            self.0.request.subscription = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [subscription][crate::model::CreateSubscriptionRequest::subscription].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_subscription<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::CreateSubscriptionPayload>,
        {
            self.0.request.subscription = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CreateSubscription {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DataSubscriptionService::list_subscriptions][crate::client::DataSubscriptionService::list_subscriptions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::data_subscription_service::ListSubscriptions;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListSubscriptions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListSubscriptions(RequestBuilder<crate::model::ListSubscriptionsRequest>);

    impl ListSubscriptions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DataSubscriptionService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ListSubscriptionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ListSubscriptionsResponse> {
            (*self.0.stub)
                .list_subscriptions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::ListSubscriptionsResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ListSubscriptionsResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [parent][crate::model::ListSubscriptionsRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [filter][crate::model::ListSubscriptionsRequest::filter].
        pub fn set_filter<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.filter = v.into();
            self
        }

        /// Sets the value of [page_size][crate::model::ListSubscriptionsRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::ListSubscriptionsRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListSubscriptions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DataSubscriptionService::update_subscription][crate::client::DataSubscriptionService::update_subscription] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::data_subscription_service::UpdateSubscription;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateSubscription {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateSubscription(RequestBuilder<crate::model::UpdateSubscriptionRequest>);

    impl UpdateSubscription {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DataSubscriptionService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::UpdateSubscriptionRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Subscription> {
            (*self.0.stub)
                .update_subscription(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [subscription][crate::model::UpdateSubscriptionRequest::subscription].
        ///
        /// This is a **required** field for requests.
        pub fn set_subscription<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Subscription>,
        {
            self.0.request.subscription = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [subscription][crate::model::UpdateSubscriptionRequest::subscription].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_subscription<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Subscription>,
        {
            self.0.request.subscription = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::UpdateSubscriptionRequest::update_mask].
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::UpdateSubscriptionRequest::update_mask].
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateSubscription {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DataSubscriptionService::delete_subscription][crate::client::DataSubscriptionService::delete_subscription] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::data_subscription_service::DeleteSubscription;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeleteSubscription {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeleteSubscription(RequestBuilder<crate::model::DeleteSubscriptionRequest>);

    impl DeleteSubscription {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DataSubscriptionService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::DeleteSubscriptionRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<()> {
            (*self.0.stub)
                .delete_subscription(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::DeleteSubscriptionRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeleteSubscription {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DataSubscriptionService::get_operation][crate::client::DataSubscriptionService::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::data_subscription_service::GetOperation;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<google_cloud_longrunning::model::GetOperationRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DataSubscriptionService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<google_cloud_longrunning::model::GetOperationRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<google_cloud_longrunning::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][google_cloud_longrunning::model::GetOperationRequest::name].
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [HealthProfileService][crate::client::HealthProfileService].
pub mod health_profile_service {
    use crate::Result;

    /// A builder for [HealthProfileService][crate::client::HealthProfileService].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_devicesandservices_health_v4::*;
    /// # use builder::health_profile_service::ClientBuilder;
    /// # use client::HealthProfileService;
    /// let builder : ClientBuilder = HealthProfileService::builder();
    /// let client = builder
    ///     .with_endpoint("https://health.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::HealthProfileService;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = HealthProfileService;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::HealthProfileService] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::HealthProfileService>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HealthProfileService>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [HealthProfileService::get_profile][crate::client::HealthProfileService::get_profile] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::health_profile_service::GetProfile;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetProfile {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetProfile(RequestBuilder<crate::model::GetProfileRequest>);

    impl GetProfile {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HealthProfileService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetProfileRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Profile> {
            (*self.0.stub)
                .get_profile(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::GetProfileRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetProfile {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HealthProfileService::update_profile][crate::client::HealthProfileService::update_profile] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::health_profile_service::UpdateProfile;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateProfile {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateProfile(RequestBuilder<crate::model::UpdateProfileRequest>);

    impl UpdateProfile {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HealthProfileService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::UpdateProfileRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Profile> {
            (*self.0.stub)
                .update_profile(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [profile][crate::model::UpdateProfileRequest::profile].
        ///
        /// This is a **required** field for requests.
        pub fn set_profile<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Profile>,
        {
            self.0.request.profile = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [profile][crate::model::UpdateProfileRequest::profile].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_profile<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Profile>,
        {
            self.0.request.profile = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::UpdateProfileRequest::update_mask].
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::UpdateProfileRequest::update_mask].
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateProfile {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HealthProfileService::get_settings][crate::client::HealthProfileService::get_settings] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::health_profile_service::GetSettings;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetSettings {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetSettings(RequestBuilder<crate::model::GetSettingsRequest>);

    impl GetSettings {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HealthProfileService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetSettingsRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Settings> {
            (*self.0.stub)
                .get_settings(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::GetSettingsRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetSettings {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HealthProfileService::update_settings][crate::client::HealthProfileService::update_settings] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::health_profile_service::UpdateSettings;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateSettings {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateSettings(RequestBuilder<crate::model::UpdateSettingsRequest>);

    impl UpdateSettings {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HealthProfileService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::UpdateSettingsRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Settings> {
            (*self.0.stub)
                .update_settings(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [settings][crate::model::UpdateSettingsRequest::settings].
        ///
        /// This is a **required** field for requests.
        pub fn set_settings<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Settings>,
        {
            self.0.request.settings = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [settings][crate::model::UpdateSettingsRequest::settings].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_settings<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Settings>,
        {
            self.0.request.settings = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::UpdateSettingsRequest::update_mask].
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::UpdateSettingsRequest::update_mask].
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateSettings {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HealthProfileService::get_identity][crate::client::HealthProfileService::get_identity] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::health_profile_service::GetIdentity;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIdentity {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIdentity(RequestBuilder<crate::model::GetIdentityRequest>);

    impl GetIdentity {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HealthProfileService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetIdentityRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Identity> {
            (*self.0.stub)
                .get_identity(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::GetIdentityRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIdentity {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HealthProfileService::get_irn_profile][crate::client::HealthProfileService::get_irn_profile] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::health_profile_service::GetIrnProfile;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIrnProfile {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIrnProfile(RequestBuilder<crate::model::GetIrnProfileRequest>);

    impl GetIrnProfile {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HealthProfileService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetIrnProfileRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::IrnProfile> {
            (*self.0.stub)
                .get_irn_profile(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::GetIrnProfileRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIrnProfile {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HealthProfileService::get_paired_device][crate::client::HealthProfileService::get_paired_device] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::health_profile_service::GetPairedDevice;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetPairedDevice {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetPairedDevice(RequestBuilder<crate::model::GetPairedDeviceRequest>);

    impl GetPairedDevice {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HealthProfileService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetPairedDeviceRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::PairedDevice> {
            (*self.0.stub)
                .get_paired_device(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::GetPairedDeviceRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetPairedDevice {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HealthProfileService::list_paired_devices][crate::client::HealthProfileService::list_paired_devices] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::health_profile_service::ListPairedDevices;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListPairedDevices {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListPairedDevices(RequestBuilder<crate::model::ListPairedDevicesRequest>);

    impl ListPairedDevices {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HealthProfileService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ListPairedDevicesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ListPairedDevicesResponse> {
            (*self.0.stub)
                .list_paired_devices(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::ListPairedDevicesResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ListPairedDevicesResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [parent][crate::model::ListPairedDevicesRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [page_size][crate::model::ListPairedDevicesRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::ListPairedDevicesRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListPairedDevices {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HealthProfileService::get_operation][crate::client::HealthProfileService::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::builder::health_profile_service::GetOperation;
    /// # async fn sample() -> google_devicesandservices_health_v4::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<google_cloud_longrunning::model::GetOperationRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HealthProfileService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<google_cloud_longrunning::model::GetOperationRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<google_cloud_longrunning::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][google_cloud_longrunning::model::GetOperationRequest::name].
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}
