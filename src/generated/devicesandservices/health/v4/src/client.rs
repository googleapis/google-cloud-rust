// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// Code generated by sidekick. DO NOT EDIT.
#![allow(rustdoc::redundant_explicit_links)]
#![allow(rustdoc::broken_intra_doc_links)]

/// Implements a client for the Google Health API.
///
/// # Example
/// ```
/// # use google_devicesandservices_health_v4::client::DataPointsService;
/// use google_cloud_gax::paginator::ItemPaginator as _;
/// async fn sample(
///    user_id: &str,
///    data_type_id: &str,
/// ) -> anyhow::Result<()> {
///     let client = DataPointsService::builder().build().await?;
///     let mut list = client.list_data_points()
///         .set_parent(format!("users/{user_id}/dataTypes/{data_type_id}"))
///         .by_item();
///     while let Some(item) = list.next().await.transpose()? {
///         println!("{:?}", item);
///     }
///     Ok(())
/// }
/// ```
///
/// # Service Description
///
/// Data Points Service exposing the user's health and fitness measured and
/// derived data.
///
/// # Configuration
///
/// To configure `DataPointsService` use the `with_*` methods in the type returned
/// by [builder()][DataPointsService::builder]. The default configuration should
/// work for most applications. Common configuration changes include
///
/// * [with_endpoint()]: by default this client uses the global default endpoint
///   (`https://health.googleapis.com`). Applications using regional
///   endpoints or running in restricted networks (e.g. a network configured
///   with [Private Google Access with VPC Service Controls]) may want to
///   override this default.
/// * [with_credentials()]: by default this client uses
///   [Application Default Credentials]. Applications using custom
///   authentication may need to override this default.
///
/// [with_endpoint()]: super::builder::data_points_service::ClientBuilder::with_endpoint
/// [with_credentials()]: super::builder::data_points_service::ClientBuilder::with_credentials
/// [Private Google Access with VPC Service Controls]: https://cloud.google.com/vpc-service-controls/docs/private-connectivity
/// [Application Default Credentials]: https://cloud.google.com/docs/authentication#adc
///
/// # Pooling and Cloning
///
/// `DataPointsService` holds a connection pool internally, it is advised to
/// create one and reuse it. You do not need to wrap `DataPointsService` in
/// an [Rc](std::rc::Rc) or [Arc](std::sync::Arc) to reuse it, because it
/// already uses an `Arc` internally.
#[derive(Clone, Debug)]
pub struct DataPointsService {
    inner: std::sync::Arc<dyn super::stub::dynamic::DataPointsService>,
}

impl DataPointsService {
    /// Returns a builder for [DataPointsService].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_devicesandservices_health_v4::client::DataPointsService;
    /// let client = DataPointsService::builder().build().await?;
    /// # Ok(()) }
    /// ```
    pub fn builder() -> super::builder::data_points_service::ClientBuilder {
        crate::new_client_builder(super::builder::data_points_service::client::Factory)
    }

    /// Creates a new client from the provided stub.
    ///
    /// The most common case for calling this function is in tests mocking the
    /// client's behavior.
    pub fn from_stub<T>(stub: impl Into<std::sync::Arc<T>>) -> Self
    where
        T: super::stub::DataPointsService + 'static,
    {
        Self { inner: stub.into() }
    }

    pub(crate) async fn new(
        config: gaxi::options::ClientConfig,
    ) -> crate::ClientBuilderResult<Self> {
        let inner = Self::build_inner(config).await?;
        Ok(Self { inner })
    }

    async fn build_inner(
        conf: gaxi::options::ClientConfig,
    ) -> crate::ClientBuilderResult<std::sync::Arc<dyn super::stub::dynamic::DataPointsService>>
    {
        if gaxi::options::tracing_enabled(&conf) {
            return Ok(std::sync::Arc::new(Self::build_with_tracing(conf).await?));
        }
        Ok(std::sync::Arc::new(Self::build_transport(conf).await?))
    }

    async fn build_transport(
        conf: gaxi::options::ClientConfig,
    ) -> crate::ClientBuilderResult<impl super::stub::DataPointsService> {
        super::transport::DataPointsService::new(conf).await
    }

    async fn build_with_tracing(
        conf: gaxi::options::ClientConfig,
    ) -> crate::ClientBuilderResult<impl super::stub::DataPointsService> {
        Self::build_transport(conf)
            .await
            .map(super::tracing::DataPointsService::new)
    }

    /// Get a single identifyable data point.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::DataPointsService;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &DataPointsService, user_id: &str, data_type_id: &str, data_point_id: &str
    /// ) -> Result<()> {
    ///     let response = client.get_data_point()
    ///         .set_name(format!("users/{user_id}/dataTypes/{data_type_id}/dataPoints/{data_point_id}"))
    ///         .send().await?;
    ///     println!("response {:?}", response);
    ///     Ok(())
    /// }
    /// ```
    pub fn get_data_point(&self) -> super::builder::data_points_service::GetDataPoint {
        super::builder::data_points_service::GetDataPoint::new(self.inner.clone())
    }

    /// Query user health and fitness data points.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::DataPointsService;
    /// use google_cloud_gax::paginator::ItemPaginator as _;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &DataPointsService, user_id: &str, data_type_id: &str
    /// ) -> Result<()> {
    ///     let mut list = client.list_data_points()
    ///         .set_parent(format!("users/{user_id}/dataTypes/{data_type_id}"))
    ///         .by_item();
    ///     while let Some(item) = list.next().await.transpose()? {
    ///         println!("{:?}", item);
    ///     }
    ///     Ok(())
    /// }
    /// ```
    pub fn list_data_points(&self) -> super::builder::data_points_service::ListDataPoints {
        super::builder::data_points_service::ListDataPoints::new(self.inner.clone())
    }

    /// Creates a single identifiable data point.
    ///
    /// # Long running operations
    ///
    /// This method is used to start, and/or poll a [long-running Operation].
    /// The [Working with long-running operations] chapter in the [user guide]
    /// covers these operations in detail.
    ///
    /// [long-running operation]: https://google.aip.dev/151
    /// [user guide]: https://googleapis.github.io/google-cloud-rust/
    /// [working with long-running operations]: https://googleapis.github.io/google-cloud-rust/working_with_long_running_operations.html
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::DataPointsService;
    /// use google_cloud_lro::Poller;
    /// use google_devicesandservices_health_v4::model::DataPoint;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &DataPointsService, user_id: &str, data_type_id: &str
    /// ) -> Result<()> {
    ///     let response = client.create_data_point()
    ///         .set_parent(format!("users/{user_id}/dataTypes/{data_type_id}"))
    ///         .set_data_point(
    ///             DataPoint::new()/* set fields */
    ///         )
    ///         .poller().until_done().await?;
    ///     println!("response {:?}", response);
    ///     Ok(())
    /// }
    /// ```
    pub fn create_data_point(&self) -> super::builder::data_points_service::CreateDataPoint {
        super::builder::data_points_service::CreateDataPoint::new(self.inner.clone())
    }

    /// Updates a single identifiable data point. If a data point with the
    /// specified `name` is not found, the request will fail.
    ///
    /// # Long running operations
    ///
    /// This method is used to start, and/or poll a [long-running Operation].
    /// The [Working with long-running operations] chapter in the [user guide]
    /// covers these operations in detail.
    ///
    /// [long-running operation]: https://google.aip.dev/151
    /// [user guide]: https://googleapis.github.io/google-cloud-rust/
    /// [working with long-running operations]: https://googleapis.github.io/google-cloud-rust/working_with_long_running_operations.html
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::DataPointsService;
    /// use google_cloud_lro::Poller;
    /// use google_devicesandservices_health_v4::model::DataPoint;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &DataPointsService, user_id: &str, data_type_id: &str, data_point_id: &str
    /// ) -> Result<()> {
    ///     let response = client.update_data_point()
    ///         .set_data_point(
    ///             DataPoint::new().set_name(format!("users/{user_id}/dataTypes/{data_type_id}/dataPoints/{data_point_id}"))/* set fields */
    ///         )
    ///         .poller().until_done().await?;
    ///     println!("response {:?}", response);
    ///     Ok(())
    /// }
    /// ```
    pub fn update_data_point(&self) -> super::builder::data_points_service::UpdateDataPoint {
        super::builder::data_points_service::UpdateDataPoint::new(self.inner.clone())
    }

    /// Delete a batch of identifyable data points.
    ///
    /// # Long running operations
    ///
    /// This method is used to start, and/or poll a [long-running Operation].
    /// The [Working with long-running operations] chapter in the [user guide]
    /// covers these operations in detail.
    ///
    /// [long-running operation]: https://google.aip.dev/151
    /// [user guide]: https://googleapis.github.io/google-cloud-rust/
    /// [working with long-running operations]: https://googleapis.github.io/google-cloud-rust/working_with_long_running_operations.html
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::DataPointsService;
    /// use google_cloud_lro::Poller;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &DataPointsService
    /// ) -> Result<()> {
    ///     let response = client.batch_delete_data_points()
    ///         /* set fields */
    ///         .poller().until_done().await?;
    ///     println!("response {:?}", response);
    ///     Ok(())
    /// }
    /// ```
    pub fn batch_delete_data_points(
        &self,
    ) -> super::builder::data_points_service::BatchDeleteDataPoints {
        super::builder::data_points_service::BatchDeleteDataPoints::new(self.inner.clone())
    }

    /// Reconcile data points from multiple data sources into a single data stream.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::DataPointsService;
    /// use google_cloud_gax::paginator::ItemPaginator as _;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &DataPointsService
    /// ) -> Result<()> {
    ///     let mut list = client.reconcile_data_points()
    ///         /* set fields */
    ///         .by_item();
    ///     while let Some(item) = list.next().await.transpose()? {
    ///         println!("{:?}", item);
    ///     }
    ///     Ok(())
    /// }
    /// ```
    pub fn reconcile_data_points(
        &self,
    ) -> super::builder::data_points_service::ReconcileDataPoints {
        super::builder::data_points_service::ReconcileDataPoints::new(self.inner.clone())
    }

    /// Roll up data points over physical time intervals for supported data types.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::DataPointsService;
    /// use google_cloud_gax::paginator::ItemPaginator as _;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &DataPointsService
    /// ) -> Result<()> {
    ///     let mut list = client.roll_up_data_points()
    ///         /* set fields */
    ///         .by_item();
    ///     while let Some(item) = list.next().await.transpose()? {
    ///         println!("{:?}", item);
    ///     }
    ///     Ok(())
    /// }
    /// ```
    pub fn roll_up_data_points(&self) -> super::builder::data_points_service::RollUpDataPoints {
        super::builder::data_points_service::RollUpDataPoints::new(self.inner.clone())
    }

    /// Roll up data points over civil time intervals for supported data types.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::DataPointsService;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &DataPointsService
    /// ) -> Result<()> {
    ///     let response = client.daily_roll_up_data_points()
    ///         /* set fields */
    ///         .send().await?;
    ///     println!("response {:?}", response);
    ///     Ok(())
    /// }
    /// ```
    pub fn daily_roll_up_data_points(
        &self,
    ) -> super::builder::data_points_service::DailyRollUpDataPoints {
        super::builder::data_points_service::DailyRollUpDataPoints::new(self.inner.clone())
    }

    /// Exports exercise data in TCX format.
    ///
    /// **IMPORTANT:** HTTP clients must append `?alt=media` to the
    /// request URL to download the raw TCX file.
    ///
    /// Example:
    /// `<https://health.googleapis.com/v4/users/me/dataTypes/exercise/dataPoints/EXERCISE_ID:exportExerciseTcx?alt=media>`
    ///
    /// Without `alt=media`, the server returns a JSON response
    /// (`ExportExerciseTcxResponse`)
    /// which is intended primarily for gRPC clients.
    ///
    /// **Note:** While the Authorization section below states that any one of the
    /// listed scopes is accepted, this specific method requires the user to
    /// provide both one of the `activity_and_fitness` scopes (`normal` or
    /// `readonly`) AND one of the `location` scopes (`normal` or `readonly`) in
    /// their access token to succeed.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::DataPointsService;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &DataPointsService
    /// ) -> Result<()> {
    ///     let response = client.export_exercise_tcx()
    ///         /* set fields */
    ///         .send().await?;
    ///     println!("response {:?}", response);
    ///     Ok(())
    /// }
    /// ```
    pub fn export_exercise_tcx(&self) -> super::builder::data_points_service::ExportExerciseTcx {
        super::builder::data_points_service::ExportExerciseTcx::new(self.inner.clone())
    }

    /// Provides the [Operations][google.longrunning.Operations] service functionality in this service.
    ///
    /// [google.longrunning.Operations]: google-cloud-longrunning::client::Operations
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::DataPointsService;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &DataPointsService
    /// ) -> Result<()> {
    ///     let response = client.get_operation()
    ///         /* set fields */
    ///         .send().await?;
    ///     println!("response {:?}", response);
    ///     Ok(())
    /// }
    /// ```
    pub fn get_operation(&self) -> super::builder::data_points_service::GetOperation {
        super::builder::data_points_service::GetOperation::new(self.inner.clone())
    }
}

/// Implements a client for the Google Health API.
///
/// # Example
/// ```
/// # use google_devicesandservices_health_v4::client::DataSubscriptionService;
/// use google_cloud_gax::paginator::ItemPaginator as _;
/// async fn sample(
///    project_id: &str,
/// ) -> anyhow::Result<()> {
///     let client = DataSubscriptionService::builder().build().await?;
///     let mut list = client.list_subscribers()
///         .set_parent(format!("projects/{project_id}"))
///         .by_item();
///     while let Some(item) = list.next().await.transpose()? {
///         println!("{:?}", item);
///     }
///     Ok(())
/// }
/// ```
///
/// # Service Description
///
/// Data Subscription Service that allows clients (e.g., Fitbit 3P
/// applications, internal Fitbit Services) to manage their subscriber endpoints.
/// This service provides CRUD APIs for subscribers,
/// and also offers functionalities for subscriber verification and statistics.
///
/// # Configuration
///
/// To configure `DataSubscriptionService` use the `with_*` methods in the type returned
/// by [builder()][DataSubscriptionService::builder]. The default configuration should
/// work for most applications. Common configuration changes include
///
/// * [with_endpoint()]: by default this client uses the global default endpoint
///   (`https://health.googleapis.com`). Applications using regional
///   endpoints or running in restricted networks (e.g. a network configured
///   with [Private Google Access with VPC Service Controls]) may want to
///   override this default.
/// * [with_credentials()]: by default this client uses
///   [Application Default Credentials]. Applications using custom
///   authentication may need to override this default.
///
/// [with_endpoint()]: super::builder::data_subscription_service::ClientBuilder::with_endpoint
/// [with_credentials()]: super::builder::data_subscription_service::ClientBuilder::with_credentials
/// [Private Google Access with VPC Service Controls]: https://cloud.google.com/vpc-service-controls/docs/private-connectivity
/// [Application Default Credentials]: https://cloud.google.com/docs/authentication#adc
///
/// # Pooling and Cloning
///
/// `DataSubscriptionService` holds a connection pool internally, it is advised to
/// create one and reuse it. You do not need to wrap `DataSubscriptionService` in
/// an [Rc](std::rc::Rc) or [Arc](std::sync::Arc) to reuse it, because it
/// already uses an `Arc` internally.
#[derive(Clone, Debug)]
pub struct DataSubscriptionService {
    inner: std::sync::Arc<dyn super::stub::dynamic::DataSubscriptionService>,
}

impl DataSubscriptionService {
    /// Returns a builder for [DataSubscriptionService].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_devicesandservices_health_v4::client::DataSubscriptionService;
    /// let client = DataSubscriptionService::builder().build().await?;
    /// # Ok(()) }
    /// ```
    pub fn builder() -> super::builder::data_subscription_service::ClientBuilder {
        crate::new_client_builder(super::builder::data_subscription_service::client::Factory)
    }

    /// Creates a new client from the provided stub.
    ///
    /// The most common case for calling this function is in tests mocking the
    /// client's behavior.
    pub fn from_stub<T>(stub: impl Into<std::sync::Arc<T>>) -> Self
    where
        T: super::stub::DataSubscriptionService + 'static,
    {
        Self { inner: stub.into() }
    }

    pub(crate) async fn new(
        config: gaxi::options::ClientConfig,
    ) -> crate::ClientBuilderResult<Self> {
        let inner = Self::build_inner(config).await?;
        Ok(Self { inner })
    }

    async fn build_inner(
        conf: gaxi::options::ClientConfig,
    ) -> crate::ClientBuilderResult<std::sync::Arc<dyn super::stub::dynamic::DataSubscriptionService>>
    {
        if gaxi::options::tracing_enabled(&conf) {
            return Ok(std::sync::Arc::new(Self::build_with_tracing(conf).await?));
        }
        Ok(std::sync::Arc::new(Self::build_transport(conf).await?))
    }

    async fn build_transport(
        conf: gaxi::options::ClientConfig,
    ) -> crate::ClientBuilderResult<impl super::stub::DataSubscriptionService> {
        super::transport::DataSubscriptionService::new(conf).await
    }

    async fn build_with_tracing(
        conf: gaxi::options::ClientConfig,
    ) -> crate::ClientBuilderResult<impl super::stub::DataSubscriptionService> {
        Self::build_transport(conf)
            .await
            .map(super::tracing::DataSubscriptionService::new)
    }

    /// Registers a new subscriber endpoint to receive notifications.
    /// A subscriber represents an application or service that wishes to receive
    /// data change notifications for users who have granted consent.
    ///
    /// **Endpoint Verification:**
    /// For a subscriber to be successfully created, the provided `endpoint_uri`
    /// must be a valid HTTPS endpoint and must pass an automated verification
    /// check. The backend will send two HTTP POST requests to the `endpoint_uri`:
    ///
    /// 1. **Verification with Authorization:**
    ///
    ///    * **Headers:** Includes `Content-Type: application/json` and
    ///      `Authorization` (with the exact value from
    ///      `CreateSubscriberPayload.endpoint_authorization.secret`).
    ///    * **Body:** `{"type": "verification"}`
    ///    * **Expected Response:** HTTP `201 Created`.
    /// 1. **Verification without Authorization:**
    ///
    ///    * **Headers:** Includes `Content-Type: application/json`. The
    ///      `Authorization` header is OMITTED.
    ///    * **Body:** `{"type": "verification"}`
    ///    * **Expected Response:** HTTP `401 Unauthorized` or `403 Forbidden`.
    ///
    /// Both tests must pass for the subscriber creation to succeed. If
    /// verification fails, the operation will not be completed and an error will
    /// be returned. This process ensures the endpoint is reachable and correctly
    /// validates the `Authorization` header.
    ///
    /// # Long running operations
    ///
    /// This method is used to start, and/or poll a [long-running Operation].
    /// The [Working with long-running operations] chapter in the [user guide]
    /// covers these operations in detail.
    ///
    /// [long-running operation]: https://google.aip.dev/151
    /// [user guide]: https://googleapis.github.io/google-cloud-rust/
    /// [working with long-running operations]: https://googleapis.github.io/google-cloud-rust/working_with_long_running_operations.html
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::DataSubscriptionService;
    /// use google_cloud_lro::Poller;
    /// use google_devicesandservices_health_v4::model::CreateSubscriberPayload;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &DataSubscriptionService, project_id: &str
    /// ) -> Result<()> {
    ///     let response = client.create_subscriber()
    ///         .set_parent(format!("projects/{project_id}"))
    ///         .set_subscriber_id("subscriber_id_value")
    ///         .set_subscriber(
    ///             CreateSubscriberPayload::new()/* set fields */
    ///         )
    ///         .poller().until_done().await?;
    ///     println!("response {:?}", response);
    ///     Ok(())
    /// }
    /// ```
    pub fn create_subscriber(&self) -> super::builder::data_subscription_service::CreateSubscriber {
        super::builder::data_subscription_service::CreateSubscriber::new(self.inner.clone())
    }

    /// Lists all subscribers registered within the owned Google Cloud Project.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::DataSubscriptionService;
    /// use google_cloud_gax::paginator::ItemPaginator as _;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &DataSubscriptionService, project_id: &str
    /// ) -> Result<()> {
    ///     let mut list = client.list_subscribers()
    ///         .set_parent(format!("projects/{project_id}"))
    ///         .by_item();
    ///     while let Some(item) = list.next().await.transpose()? {
    ///         println!("{:?}", item);
    ///     }
    ///     Ok(())
    /// }
    /// ```
    pub fn list_subscribers(&self) -> super::builder::data_subscription_service::ListSubscribers {
        super::builder::data_subscription_service::ListSubscribers::new(self.inner.clone())
    }

    /// Updates the configuration of an existing subscriber, such as the
    /// endpoint URI or the data types it's interested in.
    ///
    /// **Endpoint Verification:**
    /// If the `endpoint_uri` or `endpoint_authorization` field is included in the
    /// `update_mask`, the backend will re-verify the endpoint. The verification
    /// process is the same as described in `CreateSubscriber`:
    ///
    /// 1. **Verification with Authorization:** POST to the new or existing
    ///    `endpoint_uri` with the new or existing `Authorization` secret. Expects
    ///    HTTP `201 Created`.
    /// 1. **Verification without Authorization:** POST to the `endpoint_uri`
    ///    without the `Authorization` header. Expects HTTP `401 Unauthorized` or
    ///    `403 Forbidden`.
    ///
    /// Both tests must pass using the potentially updated values for the
    /// subscriber update to succeed. If verification fails, the update will not
    /// be applied, and an error will be returned.
    ///
    /// # Long running operations
    ///
    /// This method is used to start, and/or poll a [long-running Operation].
    /// The [Working with long-running operations] chapter in the [user guide]
    /// covers these operations in detail.
    ///
    /// [long-running operation]: https://google.aip.dev/151
    /// [user guide]: https://googleapis.github.io/google-cloud-rust/
    /// [working with long-running operations]: https://googleapis.github.io/google-cloud-rust/working_with_long_running_operations.html
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::DataSubscriptionService;
    /// use google_cloud_lro::Poller;
    /// # extern crate wkt as google_cloud_wkt;
    /// use google_cloud_wkt::FieldMask;
    /// use google_devicesandservices_health_v4::model::Subscriber;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &DataSubscriptionService, project_id: &str, subscriber_id: &str
    /// ) -> Result<()> {
    ///     let response = client.update_subscriber()
    ///         .set_subscriber(
    ///             Subscriber::new().set_name(format!("projects/{project_id}/subscribers/{subscriber_id}"))/* set fields */
    ///         )
    ///         .set_update_mask(FieldMask::default().set_paths(["updated.field.path1", "updated.field.path2"]))
    ///         .poller().until_done().await?;
    ///     println!("response {:?}", response);
    ///     Ok(())
    /// }
    /// ```
    pub fn update_subscriber(&self) -> super::builder::data_subscription_service::UpdateSubscriber {
        super::builder::data_subscription_service::UpdateSubscriber::new(self.inner.clone())
    }

    /// Deletes a subscriber registration. This will stop all notifications
    /// to the subscriber's endpoint.
    ///
    /// # Long running operations
    ///
    /// This method is used to start, and/or poll a [long-running Operation].
    /// The [Working with long-running operations] chapter in the [user guide]
    /// covers these operations in detail.
    ///
    /// [long-running operation]: https://google.aip.dev/151
    /// [user guide]: https://googleapis.github.io/google-cloud-rust/
    /// [working with long-running operations]: https://googleapis.github.io/google-cloud-rust/working_with_long_running_operations.html
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::DataSubscriptionService;
    /// use google_cloud_lro::Poller;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &DataSubscriptionService, project_id: &str, subscriber_id: &str
    /// ) -> Result<()> {
    ///     client.delete_subscriber()
    ///         .set_name(format!("projects/{project_id}/subscribers/{subscriber_id}"))
    ///         .poller().until_done().await?;
    ///     Ok(())
    /// }
    /// ```
    pub fn delete_subscriber(&self) -> super::builder::data_subscription_service::DeleteSubscriber {
        super::builder::data_subscription_service::DeleteSubscriber::new(self.inner.clone())
    }

    /// Creates a subscription for a specific user to a specific subscriber.
    /// This method requires the subscriber to have a `SubscriptionCreatePolicy`
    /// set to `MANUAL` for the given data types.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::DataSubscriptionService;
    /// use google_devicesandservices_health_v4::model::CreateSubscriptionPayload;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &DataSubscriptionService, project_id: &str, subscriber_id: &str
    /// ) -> Result<()> {
    ///     let response = client.create_subscription()
    ///         .set_parent(format!("projects/{project_id}/subscribers/{subscriber_id}"))
    ///         .set_subscription_id("subscription_id_value")
    ///         .set_subscription(
    ///             CreateSubscriptionPayload::new()/* set fields */
    ///         )
    ///         .send().await?;
    ///     println!("response {:?}", response);
    ///     Ok(())
    /// }
    /// ```
    pub fn create_subscription(
        &self,
    ) -> super::builder::data_subscription_service::CreateSubscription {
        super::builder::data_subscription_service::CreateSubscription::new(self.inner.clone())
    }

    /// Lists all active subscriptions for a given subscriber. This can be
    /// filtered, for example, by user or data type.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::DataSubscriptionService;
    /// use google_cloud_gax::paginator::ItemPaginator as _;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &DataSubscriptionService, project_id: &str, subscriber_id: &str
    /// ) -> Result<()> {
    ///     let mut list = client.list_subscriptions()
    ///         .set_parent(format!("projects/{project_id}/subscribers/{subscriber_id}"))
    ///         .by_item();
    ///     while let Some(item) = list.next().await.transpose()? {
    ///         println!("{:?}", item);
    ///     }
    ///     Ok(())
    /// }
    /// ```
    pub fn list_subscriptions(
        &self,
    ) -> super::builder::data_subscription_service::ListSubscriptions {
        super::builder::data_subscription_service::ListSubscriptions::new(self.inner.clone())
    }

    /// Updates the data types for an existing user subscription.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::DataSubscriptionService;
    /// # extern crate wkt as google_cloud_wkt;
    /// use google_cloud_wkt::FieldMask;
    /// use google_devicesandservices_health_v4::model::Subscription;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &DataSubscriptionService, project_id: &str, subscriber_id: &str, subscription_id: &str
    /// ) -> Result<()> {
    ///     let response = client.update_subscription()
    ///         .set_subscription(
    ///             Subscription::new().set_name(format!("projects/{project_id}/subscribers/{subscriber_id}/subscriptions/{subscription_id}"))/* set fields */
    ///         )
    ///         .set_update_mask(FieldMask::default().set_paths(["updated.field.path1", "updated.field.path2"]))
    ///         .send().await?;
    ///     println!("response {:?}", response);
    ///     Ok(())
    /// }
    /// ```
    pub fn update_subscription(
        &self,
    ) -> super::builder::data_subscription_service::UpdateSubscription {
        super::builder::data_subscription_service::UpdateSubscription::new(self.inner.clone())
    }

    /// Deletes a specific user subscription, stopping notifications for this
    /// user to this subscriber.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::DataSubscriptionService;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &DataSubscriptionService, project_id: &str, subscriber_id: &str, subscription_id: &str
    /// ) -> Result<()> {
    ///     client.delete_subscription()
    ///         .set_name(format!("projects/{project_id}/subscribers/{subscriber_id}/subscriptions/{subscription_id}"))
    ///         .send().await?;
    ///     Ok(())
    /// }
    /// ```
    pub fn delete_subscription(
        &self,
    ) -> super::builder::data_subscription_service::DeleteSubscription {
        super::builder::data_subscription_service::DeleteSubscription::new(self.inner.clone())
    }

    /// Provides the [Operations][google.longrunning.Operations] service functionality in this service.
    ///
    /// [google.longrunning.Operations]: google-cloud-longrunning::client::Operations
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::DataSubscriptionService;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &DataSubscriptionService
    /// ) -> Result<()> {
    ///     let response = client.get_operation()
    ///         /* set fields */
    ///         .send().await?;
    ///     println!("response {:?}", response);
    ///     Ok(())
    /// }
    /// ```
    pub fn get_operation(&self) -> super::builder::data_subscription_service::GetOperation {
        super::builder::data_subscription_service::GetOperation::new(self.inner.clone())
    }
}

/// Implements a client for the Google Health API.
///
/// # Example
/// ```
/// # use google_devicesandservices_health_v4::client::HealthProfileService;
/// use google_cloud_gax::paginator::ItemPaginator as _;
/// async fn sample(
///    user_id: &str,
/// ) -> anyhow::Result<()> {
///     let client = HealthProfileService::builder().build().await?;
///     let mut list = client.list_paired_devices()
///         .set_parent(format!("users/{user_id}"))
///         .by_item();
///     while let Some(item) = list.next().await.transpose()? {
///         println!("{:?}", item);
///     }
///     Ok(())
/// }
/// ```
///
/// # Service Description
///
/// Health Profile Service
///
/// # Configuration
///
/// To configure `HealthProfileService` use the `with_*` methods in the type returned
/// by [builder()][HealthProfileService::builder]. The default configuration should
/// work for most applications. Common configuration changes include
///
/// * [with_endpoint()]: by default this client uses the global default endpoint
///   (`https://health.googleapis.com`). Applications using regional
///   endpoints or running in restricted networks (e.g. a network configured
///   with [Private Google Access with VPC Service Controls]) may want to
///   override this default.
/// * [with_credentials()]: by default this client uses
///   [Application Default Credentials]. Applications using custom
///   authentication may need to override this default.
///
/// [with_endpoint()]: super::builder::health_profile_service::ClientBuilder::with_endpoint
/// [with_credentials()]: super::builder::health_profile_service::ClientBuilder::with_credentials
/// [Private Google Access with VPC Service Controls]: https://cloud.google.com/vpc-service-controls/docs/private-connectivity
/// [Application Default Credentials]: https://cloud.google.com/docs/authentication#adc
///
/// # Pooling and Cloning
///
/// `HealthProfileService` holds a connection pool internally, it is advised to
/// create one and reuse it. You do not need to wrap `HealthProfileService` in
/// an [Rc](std::rc::Rc) or [Arc](std::sync::Arc) to reuse it, because it
/// already uses an `Arc` internally.
#[derive(Clone, Debug)]
pub struct HealthProfileService {
    inner: std::sync::Arc<dyn super::stub::dynamic::HealthProfileService>,
}

impl HealthProfileService {
    /// Returns a builder for [HealthProfileService].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_devicesandservices_health_v4::client::HealthProfileService;
    /// let client = HealthProfileService::builder().build().await?;
    /// # Ok(()) }
    /// ```
    pub fn builder() -> super::builder::health_profile_service::ClientBuilder {
        crate::new_client_builder(super::builder::health_profile_service::client::Factory)
    }

    /// Creates a new client from the provided stub.
    ///
    /// The most common case for calling this function is in tests mocking the
    /// client's behavior.
    pub fn from_stub<T>(stub: impl Into<std::sync::Arc<T>>) -> Self
    where
        T: super::stub::HealthProfileService + 'static,
    {
        Self { inner: stub.into() }
    }

    pub(crate) async fn new(
        config: gaxi::options::ClientConfig,
    ) -> crate::ClientBuilderResult<Self> {
        let inner = Self::build_inner(config).await?;
        Ok(Self { inner })
    }

    async fn build_inner(
        conf: gaxi::options::ClientConfig,
    ) -> crate::ClientBuilderResult<std::sync::Arc<dyn super::stub::dynamic::HealthProfileService>>
    {
        if gaxi::options::tracing_enabled(&conf) {
            return Ok(std::sync::Arc::new(Self::build_with_tracing(conf).await?));
        }
        Ok(std::sync::Arc::new(Self::build_transport(conf).await?))
    }

    async fn build_transport(
        conf: gaxi::options::ClientConfig,
    ) -> crate::ClientBuilderResult<impl super::stub::HealthProfileService> {
        super::transport::HealthProfileService::new(conf).await
    }

    async fn build_with_tracing(
        conf: gaxi::options::ClientConfig,
    ) -> crate::ClientBuilderResult<impl super::stub::HealthProfileService> {
        Self::build_transport(conf)
            .await
            .map(super::tracing::HealthProfileService::new)
    }

    /// Returns user Profile details.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::HealthProfileService;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &HealthProfileService, user_id: &str
    /// ) -> Result<()> {
    ///     let response = client.get_profile()
    ///         .set_name(format!("users/{user_id}/profile"))
    ///         .send().await?;
    ///     println!("response {:?}", response);
    ///     Ok(())
    /// }
    /// ```
    pub fn get_profile(&self) -> super::builder::health_profile_service::GetProfile {
        super::builder::health_profile_service::GetProfile::new(self.inner.clone())
    }

    /// Updates the user's profile details.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::HealthProfileService;
    /// # extern crate wkt as google_cloud_wkt;
    /// use google_cloud_wkt::FieldMask;
    /// use google_devicesandservices_health_v4::model::Profile;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &HealthProfileService, user_id: &str
    /// ) -> Result<()> {
    ///     let response = client.update_profile()
    ///         .set_profile(
    ///             Profile::new().set_name(format!("users/{user_id}/profile"))/* set fields */
    ///         )
    ///         .set_update_mask(FieldMask::default().set_paths(["updated.field.path1", "updated.field.path2"]))
    ///         .send().await?;
    ///     println!("response {:?}", response);
    ///     Ok(())
    /// }
    /// ```
    pub fn update_profile(&self) -> super::builder::health_profile_service::UpdateProfile {
        super::builder::health_profile_service::UpdateProfile::new(self.inner.clone())
    }

    /// Returns user settings details.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::HealthProfileService;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &HealthProfileService, user_id: &str
    /// ) -> Result<()> {
    ///     let response = client.get_settings()
    ///         .set_name(format!("users/{user_id}/settings"))
    ///         .send().await?;
    ///     println!("response {:?}", response);
    ///     Ok(())
    /// }
    /// ```
    pub fn get_settings(&self) -> super::builder::health_profile_service::GetSettings {
        super::builder::health_profile_service::GetSettings::new(self.inner.clone())
    }

    /// Updates the user's settings details.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::HealthProfileService;
    /// # extern crate wkt as google_cloud_wkt;
    /// use google_cloud_wkt::FieldMask;
    /// use google_devicesandservices_health_v4::model::Settings;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &HealthProfileService, user_id: &str
    /// ) -> Result<()> {
    ///     let response = client.update_settings()
    ///         .set_settings(
    ///             Settings::new().set_name(format!("users/{user_id}/settings"))/* set fields */
    ///         )
    ///         .set_update_mask(FieldMask::default().set_paths(["updated.field.path1", "updated.field.path2"]))
    ///         .send().await?;
    ///     println!("response {:?}", response);
    ///     Ok(())
    /// }
    /// ```
    pub fn update_settings(&self) -> super::builder::health_profile_service::UpdateSettings {
        super::builder::health_profile_service::UpdateSettings::new(self.inner.clone())
    }

    /// Gets the user's identity.
    ///
    /// It includes the legacy Fitbit user ID and the Google user ID and it can be
    /// used by migrating clients to map identifiers between the two systems.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::HealthProfileService;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &HealthProfileService, user_id: &str
    /// ) -> Result<()> {
    ///     let response = client.get_identity()
    ///         .set_name(format!("users/{user_id}/identity"))
    ///         .send().await?;
    ///     println!("response {:?}", response);
    ///     Ok(())
    /// }
    /// ```
    pub fn get_identity(&self) -> super::builder::health_profile_service::GetIdentity {
        super::builder::health_profile_service::GetIdentity::new(self.inner.clone())
    }

    /// Returns user's IRN Profile details.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::HealthProfileService;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &HealthProfileService, user_id: &str
    /// ) -> Result<()> {
    ///     let response = client.get_irn_profile()
    ///         .set_name(format!("users/{user_id}/irnProfile"))
    ///         .send().await?;
    ///     println!("response {:?}", response);
    ///     Ok(())
    /// }
    /// ```
    pub fn get_irn_profile(&self) -> super::builder::health_profile_service::GetIrnProfile {
        super::builder::health_profile_service::GetIrnProfile::new(self.inner.clone())
    }

    /// Returns user's Device.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::HealthProfileService;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &HealthProfileService, user_id: &str, paired_device_id: &str
    /// ) -> Result<()> {
    ///     let response = client.get_paired_device()
    ///         .set_name(format!("users/{user_id}/pairedDevices/{paired_device_id}"))
    ///         .send().await?;
    ///     println!("response {:?}", response);
    ///     Ok(())
    /// }
    /// ```
    pub fn get_paired_device(&self) -> super::builder::health_profile_service::GetPairedDevice {
        super::builder::health_profile_service::GetPairedDevice::new(self.inner.clone())
    }

    /// Returns the user's list of paired 1P trackers and smartwatches.
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::HealthProfileService;
    /// use google_cloud_gax::paginator::ItemPaginator as _;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &HealthProfileService, user_id: &str
    /// ) -> Result<()> {
    ///     let mut list = client.list_paired_devices()
    ///         .set_parent(format!("users/{user_id}"))
    ///         .by_item();
    ///     while let Some(item) = list.next().await.transpose()? {
    ///         println!("{:?}", item);
    ///     }
    ///     Ok(())
    /// }
    /// ```
    pub fn list_paired_devices(&self) -> super::builder::health_profile_service::ListPairedDevices {
        super::builder::health_profile_service::ListPairedDevices::new(self.inner.clone())
    }

    /// Provides the [Operations][google.longrunning.Operations] service functionality in this service.
    ///
    /// [google.longrunning.Operations]: google-cloud-longrunning::client::Operations
    ///
    /// # Example
    /// ```
    /// # use google_devicesandservices_health_v4::client::HealthProfileService;
    /// use google_devicesandservices_health_v4::Result;
    /// async fn sample(
    ///    client: &HealthProfileService
    /// ) -> Result<()> {
    ///     let response = client.get_operation()
    ///         /* set fields */
    ///         .send().await?;
    ///     println!("response {:?}", response);
    ///     Ok(())
    /// }
    /// ```
    pub fn get_operation(&self) -> super::builder::health_profile_service::GetOperation {
        super::builder::health_profile_service::GetOperation::new(self.inner.clone())
    }
}
