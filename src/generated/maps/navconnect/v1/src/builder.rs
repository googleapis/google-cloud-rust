// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// Code generated by sidekick. DO NOT EDIT.

/// Request and client builders for [NavConnectService][crate::client::NavConnectService].
pub mod nav_connect_service {
    use crate::Result;

    /// A builder for [NavConnectService][crate::client::NavConnectService].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_maps_navconnect_v1::*;
    /// # use builder::nav_connect_service::ClientBuilder;
    /// # use client::NavConnectService;
    /// let builder : ClientBuilder = NavConnectService::builder();
    /// let client = builder
    ///     .with_endpoint("https://navigationconnect.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::NavConnectService;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = NavConnectService;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::NavConnectService] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::NavConnectService>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NavConnectService>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [NavConnectService::create_trip][crate::client::NavConnectService::create_trip] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_navconnect_v1::builder::nav_connect_service::CreateTrip;
    /// # async fn sample() -> google_maps_navconnect_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CreateTrip {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CreateTrip(RequestBuilder<crate::model::CreateTripRequest>);

    impl CreateTrip {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NavConnectService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::CreateTripRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Trip> {
            (*self.0.stub)
                .create_trip(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [parent][crate::model::CreateTripRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [trip_id][crate::model::CreateTripRequest::trip_id].
        ///
        /// This is a **required** field for requests.
        pub fn set_trip_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.trip_id = v.into();
            self
        }

        /// Sets the value of [trip][crate::model::CreateTripRequest::trip].
        ///
        /// This is a **required** field for requests.
        pub fn set_trip<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Trip>,
        {
            self.0.request.trip = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [trip][crate::model::CreateTripRequest::trip].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_trip<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Trip>,
        {
            self.0.request.trip = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CreateTrip {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NavConnectService::get_trip][crate::client::NavConnectService::get_trip] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_navconnect_v1::builder::nav_connect_service::GetTrip;
    /// # async fn sample() -> google_maps_navconnect_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetTrip {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetTrip(RequestBuilder<crate::model::GetTripRequest>);

    impl GetTrip {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NavConnectService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetTripRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Trip> {
            (*self.0.stub)
                .get_trip(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::GetTripRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetTrip {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}
