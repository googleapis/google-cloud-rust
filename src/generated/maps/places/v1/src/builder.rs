// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// Code generated by sidekick. DO NOT EDIT.

/// Request and client builders for [Places][crate::client::Places].
pub mod places {
    use crate::Result;

    /// A builder for [Places][crate::client::Places].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_maps_places_v1::*;
    /// # use builder::places::ClientBuilder;
    /// # use client::Places;
    /// let builder : ClientBuilder = Places::builder();
    /// let client = builder
    ///     .with_endpoint("https://places.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::Places;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = Places;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::Places] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::Places>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Places>) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [Places::search_nearby][crate::client::Places::search_nearby] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_places_v1::builder::places::SearchNearby;
    /// # async fn sample() -> google_maps_places_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SearchNearby {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SearchNearby(RequestBuilder<crate::model::SearchNearbyRequest>);

    impl SearchNearby {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Places>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::SearchNearbyRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SearchNearbyResponse> {
            (*self.0.stub)
                .search_nearby(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [language_code][crate::model::SearchNearbyRequest::language_code].
        pub fn set_language_code<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.language_code = v.into();
            self
        }

        /// Sets the value of [region_code][crate::model::SearchNearbyRequest::region_code].
        pub fn set_region_code<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region_code = v.into();
            self
        }

        /// Sets the value of [included_types][crate::model::SearchNearbyRequest::included_types].
        pub fn set_included_types<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<std::string::String>,
        {
            use std::iter::Iterator;
            self.0.request.included_types = v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [excluded_types][crate::model::SearchNearbyRequest::excluded_types].
        pub fn set_excluded_types<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<std::string::String>,
        {
            use std::iter::Iterator;
            self.0.request.excluded_types = v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [included_primary_types][crate::model::SearchNearbyRequest::included_primary_types].
        pub fn set_included_primary_types<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<std::string::String>,
        {
            use std::iter::Iterator;
            self.0.request.included_primary_types = v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [excluded_primary_types][crate::model::SearchNearbyRequest::excluded_primary_types].
        pub fn set_excluded_primary_types<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<std::string::String>,
        {
            use std::iter::Iterator;
            self.0.request.excluded_primary_types = v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [max_result_count][crate::model::SearchNearbyRequest::max_result_count].
        pub fn set_max_result_count<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.max_result_count = v.into();
            self
        }

        /// Sets the value of [location_restriction][crate::model::SearchNearbyRequest::location_restriction].
        ///
        /// This is a **required** field for requests.
        pub fn set_location_restriction<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::search_nearby_request::LocationRestriction>,
        {
            self.0.request.location_restriction = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [location_restriction][crate::model::SearchNearbyRequest::location_restriction].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_location_restriction<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::search_nearby_request::LocationRestriction>,
        {
            self.0.request.location_restriction = v.map(|x| x.into());
            self
        }

        /// Sets the value of [rank_preference][crate::model::SearchNearbyRequest::rank_preference].
        pub fn set_rank_preference<T: Into<crate::model::search_nearby_request::RankPreference>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.rank_preference = v.into();
            self
        }

        /// Sets the value of [routing_parameters][crate::model::SearchNearbyRequest::routing_parameters].
        pub fn set_routing_parameters<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RoutingParameters>,
        {
            self.0.request.routing_parameters = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [routing_parameters][crate::model::SearchNearbyRequest::routing_parameters].
        pub fn set_or_clear_routing_parameters<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RoutingParameters>,
        {
            self.0.request.routing_parameters = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_future_opening_businesses][crate::model::SearchNearbyRequest::include_future_opening_businesses].
        pub fn set_include_future_opening_businesses<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.include_future_opening_businesses = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SearchNearby {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Places::search_text][crate::client::Places::search_text] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_places_v1::builder::places::SearchText;
    /// # async fn sample() -> google_maps_places_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SearchText {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SearchText(RequestBuilder<crate::model::SearchTextRequest>);

    impl SearchText {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Places>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::SearchTextRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SearchTextResponse> {
            (*self.0.stub)
                .search_text(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [text_query][crate::model::SearchTextRequest::text_query].
        ///
        /// This is a **required** field for requests.
        pub fn set_text_query<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.text_query = v.into();
            self
        }

        /// Sets the value of [language_code][crate::model::SearchTextRequest::language_code].
        pub fn set_language_code<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.language_code = v.into();
            self
        }

        /// Sets the value of [region_code][crate::model::SearchTextRequest::region_code].
        pub fn set_region_code<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region_code = v.into();
            self
        }

        /// Sets the value of [rank_preference][crate::model::SearchTextRequest::rank_preference].
        pub fn set_rank_preference<T: Into<crate::model::search_text_request::RankPreference>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.rank_preference = v.into();
            self
        }

        /// Sets the value of [included_type][crate::model::SearchTextRequest::included_type].
        pub fn set_included_type<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.included_type = v.into();
            self
        }

        /// Sets the value of [open_now][crate::model::SearchTextRequest::open_now].
        pub fn set_open_now<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.open_now = v.into();
            self
        }

        /// Sets the value of [min_rating][crate::model::SearchTextRequest::min_rating].
        pub fn set_min_rating<T: Into<f64>>(mut self, v: T) -> Self {
            self.0.request.min_rating = v.into();
            self
        }

        /// Sets the value of [max_result_count][crate::model::SearchTextRequest::max_result_count].
        pub fn set_max_result_count<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.max_result_count = v.into();
            self
        }

        /// Sets the value of [price_levels][crate::model::SearchTextRequest::price_levels].
        pub fn set_price_levels<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<crate::model::PriceLevel>,
        {
            use std::iter::Iterator;
            self.0.request.price_levels = v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [strict_type_filtering][crate::model::SearchTextRequest::strict_type_filtering].
        pub fn set_strict_type_filtering<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.strict_type_filtering = v.into();
            self
        }

        /// Sets the value of [location_bias][crate::model::SearchTextRequest::location_bias].
        pub fn set_location_bias<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::search_text_request::LocationBias>,
        {
            self.0.request.location_bias = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [location_bias][crate::model::SearchTextRequest::location_bias].
        pub fn set_or_clear_location_bias<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::search_text_request::LocationBias>,
        {
            self.0.request.location_bias = v.map(|x| x.into());
            self
        }

        /// Sets the value of [location_restriction][crate::model::SearchTextRequest::location_restriction].
        pub fn set_location_restriction<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::search_text_request::LocationRestriction>,
        {
            self.0.request.location_restriction = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [location_restriction][crate::model::SearchTextRequest::location_restriction].
        pub fn set_or_clear_location_restriction<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::search_text_request::LocationRestriction>,
        {
            self.0.request.location_restriction = v.map(|x| x.into());
            self
        }

        /// Sets the value of [ev_options][crate::model::SearchTextRequest::ev_options].
        pub fn set_ev_options<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::search_text_request::EVOptions>,
        {
            self.0.request.ev_options = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [ev_options][crate::model::SearchTextRequest::ev_options].
        pub fn set_or_clear_ev_options<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::search_text_request::EVOptions>,
        {
            self.0.request.ev_options = v.map(|x| x.into());
            self
        }

        /// Sets the value of [routing_parameters][crate::model::SearchTextRequest::routing_parameters].
        pub fn set_routing_parameters<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RoutingParameters>,
        {
            self.0.request.routing_parameters = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [routing_parameters][crate::model::SearchTextRequest::routing_parameters].
        pub fn set_or_clear_routing_parameters<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RoutingParameters>,
        {
            self.0.request.routing_parameters = v.map(|x| x.into());
            self
        }

        /// Sets the value of [search_along_route_parameters][crate::model::SearchTextRequest::search_along_route_parameters].
        pub fn set_search_along_route_parameters<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::search_text_request::SearchAlongRouteParameters>,
        {
            self.0.request.search_along_route_parameters = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [search_along_route_parameters][crate::model::SearchTextRequest::search_along_route_parameters].
        pub fn set_or_clear_search_along_route_parameters<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<crate::model::search_text_request::SearchAlongRouteParameters>,
        {
            self.0.request.search_along_route_parameters = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_pure_service_area_businesses][crate::model::SearchTextRequest::include_pure_service_area_businesses].
        pub fn set_include_pure_service_area_businesses<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.include_pure_service_area_businesses = v.into();
            self
        }

        /// Sets the value of [include_future_opening_businesses][crate::model::SearchTextRequest::include_future_opening_businesses].
        pub fn set_include_future_opening_businesses<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.include_future_opening_businesses = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SearchText {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Places::get_photo_media][crate::client::Places::get_photo_media] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_places_v1::builder::places::GetPhotoMedia;
    /// # async fn sample() -> google_maps_places_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetPhotoMedia {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetPhotoMedia(RequestBuilder<crate::model::GetPhotoMediaRequest>);

    impl GetPhotoMedia {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Places>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetPhotoMediaRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::PhotoMedia> {
            (*self.0.stub)
                .get_photo_media(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::GetPhotoMediaRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }

        /// Sets the value of [max_width_px][crate::model::GetPhotoMediaRequest::max_width_px].
        pub fn set_max_width_px<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.max_width_px = v.into();
            self
        }

        /// Sets the value of [max_height_px][crate::model::GetPhotoMediaRequest::max_height_px].
        pub fn set_max_height_px<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.max_height_px = v.into();
            self
        }

        /// Sets the value of [skip_http_redirect][crate::model::GetPhotoMediaRequest::skip_http_redirect].
        pub fn set_skip_http_redirect<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.skip_http_redirect = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetPhotoMedia {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Places::get_place][crate::client::Places::get_place] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_places_v1::builder::places::GetPlace;
    /// # async fn sample() -> google_maps_places_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetPlace {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetPlace(RequestBuilder<crate::model::GetPlaceRequest>);

    impl GetPlace {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Places>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetPlaceRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Place> {
            (*self.0.stub)
                .get_place(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::GetPlaceRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }

        /// Sets the value of [language_code][crate::model::GetPlaceRequest::language_code].
        pub fn set_language_code<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.language_code = v.into();
            self
        }

        /// Sets the value of [region_code][crate::model::GetPlaceRequest::region_code].
        pub fn set_region_code<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region_code = v.into();
            self
        }

        /// Sets the value of [session_token][crate::model::GetPlaceRequest::session_token].
        pub fn set_session_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.session_token = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetPlace {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Places::autocomplete_places][crate::client::Places::autocomplete_places] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_places_v1::builder::places::AutocompletePlaces;
    /// # async fn sample() -> google_maps_places_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AutocompletePlaces {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AutocompletePlaces(RequestBuilder<crate::model::AutocompletePlacesRequest>);

    impl AutocompletePlaces {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Places>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::AutocompletePlacesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::AutocompletePlacesResponse> {
            (*self.0.stub)
                .autocomplete_places(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [input][crate::model::AutocompletePlacesRequest::input].
        ///
        /// This is a **required** field for requests.
        pub fn set_input<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.input = v.into();
            self
        }

        /// Sets the value of [location_bias][crate::model::AutocompletePlacesRequest::location_bias].
        pub fn set_location_bias<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::autocomplete_places_request::LocationBias>,
        {
            self.0.request.location_bias = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [location_bias][crate::model::AutocompletePlacesRequest::location_bias].
        pub fn set_or_clear_location_bias<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::autocomplete_places_request::LocationBias>,
        {
            self.0.request.location_bias = v.map(|x| x.into());
            self
        }

        /// Sets the value of [location_restriction][crate::model::AutocompletePlacesRequest::location_restriction].
        pub fn set_location_restriction<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::autocomplete_places_request::LocationRestriction>,
        {
            self.0.request.location_restriction = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [location_restriction][crate::model::AutocompletePlacesRequest::location_restriction].
        pub fn set_or_clear_location_restriction<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::autocomplete_places_request::LocationRestriction>,
        {
            self.0.request.location_restriction = v.map(|x| x.into());
            self
        }

        /// Sets the value of [included_primary_types][crate::model::AutocompletePlacesRequest::included_primary_types].
        pub fn set_included_primary_types<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<std::string::String>,
        {
            use std::iter::Iterator;
            self.0.request.included_primary_types = v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [included_region_codes][crate::model::AutocompletePlacesRequest::included_region_codes].
        pub fn set_included_region_codes<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<std::string::String>,
        {
            use std::iter::Iterator;
            self.0.request.included_region_codes = v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [language_code][crate::model::AutocompletePlacesRequest::language_code].
        pub fn set_language_code<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.language_code = v.into();
            self
        }

        /// Sets the value of [region_code][crate::model::AutocompletePlacesRequest::region_code].
        pub fn set_region_code<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region_code = v.into();
            self
        }

        /// Sets the value of [origin][crate::model::AutocompletePlacesRequest::origin].
        pub fn set_origin<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<google_cloud_type::model::LatLng>,
        {
            self.0.request.origin = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [origin][crate::model::AutocompletePlacesRequest::origin].
        pub fn set_or_clear_origin<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<google_cloud_type::model::LatLng>,
        {
            self.0.request.origin = v.map(|x| x.into());
            self
        }

        /// Sets the value of [input_offset][crate::model::AutocompletePlacesRequest::input_offset].
        pub fn set_input_offset<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.input_offset = v.into();
            self
        }

        /// Sets the value of [include_query_predictions][crate::model::AutocompletePlacesRequest::include_query_predictions].
        pub fn set_include_query_predictions<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.include_query_predictions = v.into();
            self
        }

        /// Sets the value of [session_token][crate::model::AutocompletePlacesRequest::session_token].
        pub fn set_session_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.session_token = v.into();
            self
        }

        /// Sets the value of [include_pure_service_area_businesses][crate::model::AutocompletePlacesRequest::include_pure_service_area_businesses].
        pub fn set_include_pure_service_area_businesses<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.include_pure_service_area_businesses = v.into();
            self
        }

        /// Sets the value of [include_future_opening_businesses][crate::model::AutocompletePlacesRequest::include_future_opening_businesses].
        pub fn set_include_future_opening_businesses<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.include_future_opening_businesses = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AutocompletePlaces {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}
