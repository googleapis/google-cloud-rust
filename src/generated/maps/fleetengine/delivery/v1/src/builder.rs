// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// Code generated by sidekick. DO NOT EDIT.

/// Request and client builders for [DeliveryService][crate::client::DeliveryService].
pub mod delivery_service {
    use crate::Result;

    /// A builder for [DeliveryService][crate::client::DeliveryService].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_maps_fleetengine_delivery_v1::*;
    /// # use builder::delivery_service::ClientBuilder;
    /// # use client::DeliveryService;
    /// let builder : ClientBuilder = DeliveryService::builder();
    /// let client = builder
    ///     .with_endpoint("https://fleetengine.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::DeliveryService;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = DeliveryService;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::DeliveryService] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::DeliveryService>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DeliveryService>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [DeliveryService::create_delivery_vehicle][crate::client::DeliveryService::create_delivery_vehicle] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_fleetengine_delivery_v1::builder::delivery_service::CreateDeliveryVehicle;
    /// # async fn sample() -> google_maps_fleetengine_delivery_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CreateDeliveryVehicle {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CreateDeliveryVehicle(RequestBuilder<crate::model::CreateDeliveryVehicleRequest>);

    impl CreateDeliveryVehicle {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DeliveryService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::CreateDeliveryVehicleRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::DeliveryVehicle> {
            (*self.0.stub)
                .create_delivery_vehicle(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [header][crate::model::CreateDeliveryVehicleRequest::header].
        pub fn set_header<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryRequestHeader>,
        {
            self.0.request.header = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [header][crate::model::CreateDeliveryVehicleRequest::header].
        pub fn set_or_clear_header<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryRequestHeader>,
        {
            self.0.request.header = v.map(|x| x.into());
            self
        }

        /// Sets the value of [parent][crate::model::CreateDeliveryVehicleRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [delivery_vehicle_id][crate::model::CreateDeliveryVehicleRequest::delivery_vehicle_id].
        ///
        /// This is a **required** field for requests.
        pub fn set_delivery_vehicle_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.delivery_vehicle_id = v.into();
            self
        }

        /// Sets the value of [delivery_vehicle][crate::model::CreateDeliveryVehicleRequest::delivery_vehicle].
        ///
        /// This is a **required** field for requests.
        pub fn set_delivery_vehicle<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryVehicle>,
        {
            self.0.request.delivery_vehicle = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [delivery_vehicle][crate::model::CreateDeliveryVehicleRequest::delivery_vehicle].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_delivery_vehicle<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryVehicle>,
        {
            self.0.request.delivery_vehicle = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CreateDeliveryVehicle {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DeliveryService::get_delivery_vehicle][crate::client::DeliveryService::get_delivery_vehicle] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_fleetengine_delivery_v1::builder::delivery_service::GetDeliveryVehicle;
    /// # async fn sample() -> google_maps_fleetengine_delivery_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetDeliveryVehicle {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetDeliveryVehicle(RequestBuilder<crate::model::GetDeliveryVehicleRequest>);

    impl GetDeliveryVehicle {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DeliveryService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetDeliveryVehicleRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::DeliveryVehicle> {
            (*self.0.stub)
                .get_delivery_vehicle(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [header][crate::model::GetDeliveryVehicleRequest::header].
        pub fn set_header<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryRequestHeader>,
        {
            self.0.request.header = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [header][crate::model::GetDeliveryVehicleRequest::header].
        pub fn set_or_clear_header<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryRequestHeader>,
        {
            self.0.request.header = v.map(|x| x.into());
            self
        }

        /// Sets the value of [name][crate::model::GetDeliveryVehicleRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetDeliveryVehicle {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DeliveryService::delete_delivery_vehicle][crate::client::DeliveryService::delete_delivery_vehicle] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_fleetengine_delivery_v1::builder::delivery_service::DeleteDeliveryVehicle;
    /// # async fn sample() -> google_maps_fleetengine_delivery_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeleteDeliveryVehicle {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeleteDeliveryVehicle(RequestBuilder<crate::model::DeleteDeliveryVehicleRequest>);

    impl DeleteDeliveryVehicle {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DeliveryService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::DeleteDeliveryVehicleRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<()> {
            (*self.0.stub)
                .delete_delivery_vehicle(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [header][crate::model::DeleteDeliveryVehicleRequest::header].
        pub fn set_header<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryRequestHeader>,
        {
            self.0.request.header = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [header][crate::model::DeleteDeliveryVehicleRequest::header].
        pub fn set_or_clear_header<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryRequestHeader>,
        {
            self.0.request.header = v.map(|x| x.into());
            self
        }

        /// Sets the value of [name][crate::model::DeleteDeliveryVehicleRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeleteDeliveryVehicle {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DeliveryService::update_delivery_vehicle][crate::client::DeliveryService::update_delivery_vehicle] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_fleetengine_delivery_v1::builder::delivery_service::UpdateDeliveryVehicle;
    /// # async fn sample() -> google_maps_fleetengine_delivery_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateDeliveryVehicle {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateDeliveryVehicle(RequestBuilder<crate::model::UpdateDeliveryVehicleRequest>);

    impl UpdateDeliveryVehicle {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DeliveryService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::UpdateDeliveryVehicleRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::DeliveryVehicle> {
            (*self.0.stub)
                .update_delivery_vehicle(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [header][crate::model::UpdateDeliveryVehicleRequest::header].
        pub fn set_header<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryRequestHeader>,
        {
            self.0.request.header = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [header][crate::model::UpdateDeliveryVehicleRequest::header].
        pub fn set_or_clear_header<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryRequestHeader>,
        {
            self.0.request.header = v.map(|x| x.into());
            self
        }

        /// Sets the value of [delivery_vehicle][crate::model::UpdateDeliveryVehicleRequest::delivery_vehicle].
        ///
        /// This is a **required** field for requests.
        pub fn set_delivery_vehicle<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryVehicle>,
        {
            self.0.request.delivery_vehicle = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [delivery_vehicle][crate::model::UpdateDeliveryVehicleRequest::delivery_vehicle].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_delivery_vehicle<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryVehicle>,
        {
            self.0.request.delivery_vehicle = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::UpdateDeliveryVehicleRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::UpdateDeliveryVehicleRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateDeliveryVehicle {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DeliveryService::batch_create_tasks][crate::client::DeliveryService::batch_create_tasks] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_fleetengine_delivery_v1::builder::delivery_service::BatchCreateTasks;
    /// # async fn sample() -> google_maps_fleetengine_delivery_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> BatchCreateTasks {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct BatchCreateTasks(RequestBuilder<crate::model::BatchCreateTasksRequest>);

    impl BatchCreateTasks {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DeliveryService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::BatchCreateTasksRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::BatchCreateTasksResponse> {
            (*self.0.stub)
                .batch_create_tasks(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [header][crate::model::BatchCreateTasksRequest::header].
        pub fn set_header<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryRequestHeader>,
        {
            self.0.request.header = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [header][crate::model::BatchCreateTasksRequest::header].
        pub fn set_or_clear_header<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryRequestHeader>,
        {
            self.0.request.header = v.map(|x| x.into());
            self
        }

        /// Sets the value of [parent][crate::model::BatchCreateTasksRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [requests][crate::model::BatchCreateTasksRequest::requests].
        ///
        /// This is a **required** field for requests.
        pub fn set_requests<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<crate::model::CreateTaskRequest>,
        {
            use std::iter::Iterator;
            self.0.request.requests = v.into_iter().map(|i| i.into()).collect();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for BatchCreateTasks {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DeliveryService::create_task][crate::client::DeliveryService::create_task] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_fleetengine_delivery_v1::builder::delivery_service::CreateTask;
    /// # async fn sample() -> google_maps_fleetengine_delivery_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CreateTask {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CreateTask(RequestBuilder<crate::model::CreateTaskRequest>);

    impl CreateTask {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DeliveryService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::CreateTaskRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Task> {
            (*self.0.stub)
                .create_task(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [header][crate::model::CreateTaskRequest::header].
        pub fn set_header<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryRequestHeader>,
        {
            self.0.request.header = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [header][crate::model::CreateTaskRequest::header].
        pub fn set_or_clear_header<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryRequestHeader>,
        {
            self.0.request.header = v.map(|x| x.into());
            self
        }

        /// Sets the value of [parent][crate::model::CreateTaskRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [task_id][crate::model::CreateTaskRequest::task_id].
        ///
        /// This is a **required** field for requests.
        pub fn set_task_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.task_id = v.into();
            self
        }

        /// Sets the value of [task][crate::model::CreateTaskRequest::task].
        ///
        /// This is a **required** field for requests.
        pub fn set_task<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Task>,
        {
            self.0.request.task = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [task][crate::model::CreateTaskRequest::task].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_task<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Task>,
        {
            self.0.request.task = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CreateTask {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DeliveryService::get_task][crate::client::DeliveryService::get_task] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_fleetengine_delivery_v1::builder::delivery_service::GetTask;
    /// # async fn sample() -> google_maps_fleetengine_delivery_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetTask {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetTask(RequestBuilder<crate::model::GetTaskRequest>);

    impl GetTask {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DeliveryService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetTaskRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Task> {
            (*self.0.stub)
                .get_task(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [header][crate::model::GetTaskRequest::header].
        pub fn set_header<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryRequestHeader>,
        {
            self.0.request.header = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [header][crate::model::GetTaskRequest::header].
        pub fn set_or_clear_header<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryRequestHeader>,
        {
            self.0.request.header = v.map(|x| x.into());
            self
        }

        /// Sets the value of [name][crate::model::GetTaskRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetTask {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DeliveryService::delete_task][crate::client::DeliveryService::delete_task] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_fleetengine_delivery_v1::builder::delivery_service::DeleteTask;
    /// # async fn sample() -> google_maps_fleetengine_delivery_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeleteTask {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeleteTask(RequestBuilder<crate::model::DeleteTaskRequest>);

    impl DeleteTask {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DeliveryService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::DeleteTaskRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<()> {
            (*self.0.stub)
                .delete_task(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [header][crate::model::DeleteTaskRequest::header].
        pub fn set_header<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryRequestHeader>,
        {
            self.0.request.header = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [header][crate::model::DeleteTaskRequest::header].
        pub fn set_or_clear_header<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryRequestHeader>,
        {
            self.0.request.header = v.map(|x| x.into());
            self
        }

        /// Sets the value of [name][crate::model::DeleteTaskRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeleteTask {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DeliveryService::update_task][crate::client::DeliveryService::update_task] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_fleetengine_delivery_v1::builder::delivery_service::UpdateTask;
    /// # async fn sample() -> google_maps_fleetengine_delivery_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateTask {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateTask(RequestBuilder<crate::model::UpdateTaskRequest>);

    impl UpdateTask {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DeliveryService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::UpdateTaskRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Task> {
            (*self.0.stub)
                .update_task(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [header][crate::model::UpdateTaskRequest::header].
        pub fn set_header<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryRequestHeader>,
        {
            self.0.request.header = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [header][crate::model::UpdateTaskRequest::header].
        pub fn set_or_clear_header<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryRequestHeader>,
        {
            self.0.request.header = v.map(|x| x.into());
            self
        }

        /// Sets the value of [task][crate::model::UpdateTaskRequest::task].
        ///
        /// This is a **required** field for requests.
        pub fn set_task<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Task>,
        {
            self.0.request.task = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [task][crate::model::UpdateTaskRequest::task].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_task<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Task>,
        {
            self.0.request.task = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::UpdateTaskRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::UpdateTaskRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateTask {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DeliveryService::list_tasks][crate::client::DeliveryService::list_tasks] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_fleetengine_delivery_v1::builder::delivery_service::ListTasks;
    /// # async fn sample() -> google_maps_fleetengine_delivery_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListTasks {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListTasks(RequestBuilder<crate::model::ListTasksRequest>);

    impl ListTasks {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DeliveryService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ListTasksRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ListTasksResponse> {
            (*self.0.stub)
                .list_tasks(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::ListTasksResponse, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::ListTasksResponse, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [header][crate::model::ListTasksRequest::header].
        pub fn set_header<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryRequestHeader>,
        {
            self.0.request.header = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [header][crate::model::ListTasksRequest::header].
        pub fn set_or_clear_header<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryRequestHeader>,
        {
            self.0.request.header = v.map(|x| x.into());
            self
        }

        /// Sets the value of [parent][crate::model::ListTasksRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [page_size][crate::model::ListTasksRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::ListTasksRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }

        /// Sets the value of [filter][crate::model::ListTasksRequest::filter].
        pub fn set_filter<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.filter = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListTasks {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DeliveryService::get_task_tracking_info][crate::client::DeliveryService::get_task_tracking_info] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_fleetengine_delivery_v1::builder::delivery_service::GetTaskTrackingInfo;
    /// # async fn sample() -> google_maps_fleetengine_delivery_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetTaskTrackingInfo {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetTaskTrackingInfo(RequestBuilder<crate::model::GetTaskTrackingInfoRequest>);

    impl GetTaskTrackingInfo {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DeliveryService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetTaskTrackingInfoRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TaskTrackingInfo> {
            (*self.0.stub)
                .get_task_tracking_info(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [header][crate::model::GetTaskTrackingInfoRequest::header].
        pub fn set_header<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryRequestHeader>,
        {
            self.0.request.header = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [header][crate::model::GetTaskTrackingInfoRequest::header].
        pub fn set_or_clear_header<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryRequestHeader>,
        {
            self.0.request.header = v.map(|x| x.into());
            self
        }

        /// Sets the value of [name][crate::model::GetTaskTrackingInfoRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetTaskTrackingInfo {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DeliveryService::list_delivery_vehicles][crate::client::DeliveryService::list_delivery_vehicles] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_fleetengine_delivery_v1::builder::delivery_service::ListDeliveryVehicles;
    /// # async fn sample() -> google_maps_fleetengine_delivery_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListDeliveryVehicles {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListDeliveryVehicles(RequestBuilder<crate::model::ListDeliveryVehiclesRequest>);

    impl ListDeliveryVehicles {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DeliveryService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ListDeliveryVehiclesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ListDeliveryVehiclesResponse> {
            (*self.0.stub)
                .list_delivery_vehicles(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::ListDeliveryVehiclesResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ListDeliveryVehiclesResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [header][crate::model::ListDeliveryVehiclesRequest::header].
        pub fn set_header<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryRequestHeader>,
        {
            self.0.request.header = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [header][crate::model::ListDeliveryVehiclesRequest::header].
        pub fn set_or_clear_header<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DeliveryRequestHeader>,
        {
            self.0.request.header = v.map(|x| x.into());
            self
        }

        /// Sets the value of [parent][crate::model::ListDeliveryVehiclesRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [page_size][crate::model::ListDeliveryVehiclesRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::ListDeliveryVehiclesRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }

        /// Sets the value of [filter][crate::model::ListDeliveryVehiclesRequest::filter].
        pub fn set_filter<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.filter = v.into();
            self
        }

        /// Sets the value of [viewport][crate::model::ListDeliveryVehiclesRequest::viewport].
        pub fn set_viewport<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<google_geo_type::model::Viewport>,
        {
            self.0.request.viewport = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [viewport][crate::model::ListDeliveryVehiclesRequest::viewport].
        pub fn set_or_clear_viewport<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<google_geo_type::model::Viewport>,
        {
            self.0.request.viewport = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListDeliveryVehicles {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}
