// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// Code generated by sidekick. DO NOT EDIT.

#[allow(unused_imports)]
use crate::Error;
use crate::Result;

/// Implements [TripService](super::stub::TripService) using a [gaxi::http::ReqwestClient].
#[derive(Clone)]
pub struct TripService {
    inner: gaxi::http::ReqwestClient,
}

impl std::fmt::Debug for TripService {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::result::Result<(), std::fmt::Error> {
        f.debug_struct("TripService")
            .field("inner", &self.inner)
            .finish()
    }
}

impl TripService {
    pub async fn new(config: gaxi::options::ClientConfig) -> crate::ClientBuilderResult<Self> {
        let tracing_is_enabled = gaxi::options::tracing_enabled(&config);
        let inner = gaxi::http::ReqwestClient::new(config, crate::DEFAULT_HOST).await?;
        let inner = if tracing_is_enabled {
            inner.with_instrumentation(&super::tracing::info::INSTRUMENTATION_CLIENT_INFO)
        } else {
            inner
        };
        Ok(Self { inner })
    }
}

impl super::stub::TripService for TripService {
    async fn create_trip(
        &self,
        req: crate::model::CreateTripRequest,
        options: crate::RequestOptions,
    ) -> Result<crate::Response<crate::model::Trip>> {
        use gaxi::http::reqwest::{HeaderValue, Method};
        use gaxi::path_parameter::PathMismatchBuilder;
        use gaxi::path_parameter::try_match;
        use gaxi::routing_parameter::Segment;
        use google_cloud_gax::error::binding::BindingError;
        let (builder, method, _path_template, _resource_name) = None
            .or_else(|| {
                let var_parent = try_match(
                    Some(&req).map(|m| &m.parent).map(|s| s.as_str()),
                    &[Segment::Literal("providers/"), Segment::SingleWildcard],
                )?;
                let path = format!("/v1/{}/trips", var_parent,);
                let path_template = "/v1/{parent}/trips";

                let resource_name = format!("//fleetengine.googleapis.com/{}", var_parent,);
                let builder = self.inner.builder(Method::POST, path);
                let builder = (|| {
                    let builder = req
                        .header
                        .as_ref()
                        .map(|p| serde_json::to_value(p).map_err(Error::ser))
                        .transpose()?
                        .into_iter()
                        .fold(builder, |builder, v| {
                            use gaxi::query_parameter::QueryParameter;
                            v.add(builder, "header")
                        });
                    let builder = builder.query(&[("tripId", &req.trip_id)]);
                    Ok(builder)
                })();
                Some(builder.map(|b| (b, Method::POST, path_template, resource_name)))
            })
            .ok_or_else(|| {
                let mut paths = Vec::new();
                {
                    let builder = PathMismatchBuilder::default();
                    let builder = builder.maybe_add(
                        Some(&req).map(|m| &m.parent).map(|s| s.as_str()),
                        &[Segment::Literal("providers/"), Segment::SingleWildcard],
                        "parent",
                        "providers/*",
                    );
                    paths.push(builder.build());
                }
                google_cloud_gax::error::Error::binding(BindingError { paths })
            })??;
        if let Some(recorder) = gaxi::observability::RequestRecorder::current() {
            recorder.on_client_request(
                gaxi::observability::ClientRequestAttributes::default()
                    .set_rpc_method("maps.fleetengine.v1.TripService/CreateTrip")
                    .set_url_template(_path_template)
                    .set_resource_name(_resource_name),
            );
        }
        let options = google_cloud_gax::options::internal::set_default_idempotency(
            options,
            gaxi::http::default_idempotency(&method),
        );
        let builder = builder.query(&[("$alt", "json;enum-encoding=int")]).header(
            "x-goog-api-client",
            HeaderValue::from_static(&crate::info::X_GOOG_API_CLIENT_HEADER),
        );
        let body = gaxi::http::handle_empty(req.trip, &method);
        self.inner.execute(builder, body, options).await
    }

    async fn get_trip(
        &self,
        req: crate::model::GetTripRequest,
        options: crate::RequestOptions,
    ) -> Result<crate::Response<crate::model::Trip>> {
        use gaxi::http::reqwest::{HeaderValue, Method};
        use gaxi::path_parameter::PathMismatchBuilder;
        use gaxi::path_parameter::try_match;
        use gaxi::routing_parameter::Segment;
        use google_cloud_gax::error::binding::BindingError;
        let (builder, method, _path_template, _resource_name) = None
            .or_else(|| {
                let var_name = try_match(
                    Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                    &[
                        Segment::Literal("providers/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/trips/"),
                        Segment::SingleWildcard,
                    ],
                )?;
                let path = format!("/v1/{}", var_name,);
                let path_template = "/v1/{name}";

                let resource_name = format!("//fleetengine.googleapis.com/{}", var_name,);
                let builder = self.inner.builder(Method::GET, path);
                let builder = (|| {
                    let builder = req
                        .header
                        .as_ref()
                        .map(|p| serde_json::to_value(p).map_err(Error::ser))
                        .transpose()?
                        .into_iter()
                        .fold(builder, |builder, v| {
                            use gaxi::query_parameter::QueryParameter;
                            v.add(builder, "header")
                        });
                    let builder = builder.query(&[("view", &req.view)]);
                    let builder = req
                        .current_route_segment_version
                        .as_ref()
                        .map(|p| serde_json::to_value(p).map_err(Error::ser))
                        .transpose()?
                        .into_iter()
                        .fold(builder, |builder, v| {
                            use gaxi::query_parameter::QueryParameter;
                            v.add(builder, "currentRouteSegmentVersion")
                        });
                    let builder = req
                        .remaining_waypoints_version
                        .as_ref()
                        .map(|p| serde_json::to_value(p).map_err(Error::ser))
                        .transpose()?
                        .into_iter()
                        .fold(builder, |builder, v| {
                            use gaxi::query_parameter::QueryParameter;
                            v.add(builder, "remainingWaypointsVersion")
                        });
                    let builder = builder.query(&[("routeFormatType", &req.route_format_type)]);
                    let builder = req
                        .current_route_segment_traffic_version
                        .as_ref()
                        .map(|p| serde_json::to_value(p).map_err(Error::ser))
                        .transpose()?
                        .into_iter()
                        .fold(builder, |builder, v| {
                            use gaxi::query_parameter::QueryParameter;
                            v.add(builder, "currentRouteSegmentTrafficVersion")
                        });
                    let builder = req
                        .remaining_waypoints_route_version
                        .as_ref()
                        .map(|p| serde_json::to_value(p).map_err(Error::ser))
                        .transpose()?
                        .into_iter()
                        .fold(builder, |builder, v| {
                            use gaxi::query_parameter::QueryParameter;
                            v.add(builder, "remainingWaypointsRouteVersion")
                        });
                    Ok(builder)
                })();
                Some(builder.map(|b| (b, Method::GET, path_template, resource_name)))
            })
            .ok_or_else(|| {
                let mut paths = Vec::new();
                {
                    let builder = PathMismatchBuilder::default();
                    let builder = builder.maybe_add(
                        Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                        &[
                            Segment::Literal("providers/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/trips/"),
                            Segment::SingleWildcard,
                        ],
                        "name",
                        "providers/*/trips/*",
                    );
                    paths.push(builder.build());
                }
                google_cloud_gax::error::Error::binding(BindingError { paths })
            })??;
        if let Some(recorder) = gaxi::observability::RequestRecorder::current() {
            recorder.on_client_request(
                gaxi::observability::ClientRequestAttributes::default()
                    .set_rpc_method("maps.fleetengine.v1.TripService/GetTrip")
                    .set_url_template(_path_template)
                    .set_resource_name(_resource_name),
            );
        }
        let options = google_cloud_gax::options::internal::set_default_idempotency(
            options,
            gaxi::http::default_idempotency(&method),
        );
        let builder = builder.query(&[("$alt", "json;enum-encoding=int")]).header(
            "x-goog-api-client",
            HeaderValue::from_static(&crate::info::X_GOOG_API_CLIENT_HEADER),
        );
        let body = gaxi::http::handle_empty(None::<wkt::Empty>, &method);
        self.inner.execute(builder, body, options).await
    }

    async fn delete_trip(
        &self,
        req: crate::model::DeleteTripRequest,
        options: crate::RequestOptions,
    ) -> Result<crate::Response<()>> {
        use gaxi::http::reqwest::{HeaderValue, Method};
        use gaxi::path_parameter::PathMismatchBuilder;
        use gaxi::path_parameter::try_match;
        use gaxi::routing_parameter::Segment;
        use google_cloud_gax::error::binding::BindingError;
        let (builder, method, _path_template, _resource_name) = None
            .or_else(|| {
                let var_name = try_match(
                    Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                    &[
                        Segment::Literal("providers/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/trips/"),
                        Segment::SingleWildcard,
                    ],
                )?;
                let path = format!("/v1/{}", var_name,);
                let path_template = "/v1/{name}";

                let resource_name = format!("//fleetengine.googleapis.com/{}", var_name,);
                let builder = self.inner.builder(Method::DELETE, path);
                let builder = (|| {
                    let builder = req
                        .header
                        .as_ref()
                        .map(|p| serde_json::to_value(p).map_err(Error::ser))
                        .transpose()?
                        .into_iter()
                        .fold(builder, |builder, v| {
                            use gaxi::query_parameter::QueryParameter;
                            v.add(builder, "header")
                        });
                    Ok(builder)
                })();
                Some(builder.map(|b| (b, Method::DELETE, path_template, resource_name)))
            })
            .ok_or_else(|| {
                let mut paths = Vec::new();
                {
                    let builder = PathMismatchBuilder::default();
                    let builder = builder.maybe_add(
                        Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                        &[
                            Segment::Literal("providers/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/trips/"),
                            Segment::SingleWildcard,
                        ],
                        "name",
                        "providers/*/trips/*",
                    );
                    paths.push(builder.build());
                }
                google_cloud_gax::error::Error::binding(BindingError { paths })
            })??;
        if let Some(recorder) = gaxi::observability::RequestRecorder::current() {
            recorder.on_client_request(
                gaxi::observability::ClientRequestAttributes::default()
                    .set_rpc_method("maps.fleetengine.v1.TripService/DeleteTrip")
                    .set_url_template(_path_template)
                    .set_resource_name(_resource_name),
            );
        }
        let options = google_cloud_gax::options::internal::set_default_idempotency(
            options,
            gaxi::http::default_idempotency(&method),
        );
        let builder = builder.query(&[("$alt", "json;enum-encoding=int")]).header(
            "x-goog-api-client",
            HeaderValue::from_static(&crate::info::X_GOOG_API_CLIENT_HEADER),
        );
        let body = gaxi::http::handle_empty(None::<wkt::Empty>, &method);
        self.inner
            .execute(builder, body, options)
            .await
            .map(|r: crate::Response<wkt::Empty>| {
                let (parts, _) = r.into_parts();
                crate::Response::from_parts(parts, ())
            })
    }

    async fn report_billable_trip(
        &self,
        req: crate::model::ReportBillableTripRequest,
        options: crate::RequestOptions,
    ) -> Result<crate::Response<()>> {
        use gaxi::http::reqwest::{HeaderValue, Method};
        use gaxi::path_parameter::PathMismatchBuilder;
        use gaxi::path_parameter::try_match;
        use gaxi::routing_parameter::Segment;
        use google_cloud_gax::error::binding::BindingError;
        let (builder, method, _path_template) = None
            .or_else(|| {
                let var_name = try_match(
                    Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                    &[
                        Segment::Literal("providers/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/billableTrips/"),
                        Segment::SingleWildcard,
                    ],
                )?;
                let path = format!("/v1/{}:report", var_name,);
                let path_template = "/v1/{name}:report";

                let builder = self.inner.builder(Method::POST, path);
                let builder = Ok(builder);
                Some(builder.map(|b| (b, Method::POST, path_template)))
            })
            .ok_or_else(|| {
                let mut paths = Vec::new();
                {
                    let builder = PathMismatchBuilder::default();
                    let builder = builder.maybe_add(
                        Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                        &[
                            Segment::Literal("providers/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/billableTrips/"),
                            Segment::SingleWildcard,
                        ],
                        "name",
                        "providers/*/billableTrips/*",
                    );
                    paths.push(builder.build());
                }
                google_cloud_gax::error::Error::binding(BindingError { paths })
            })??;
        if let Some(recorder) = gaxi::observability::RequestRecorder::current() {
            recorder.on_client_request(
                gaxi::observability::ClientRequestAttributes::default()
                    .set_rpc_method("maps.fleetengine.v1.TripService/ReportBillableTrip")
                    .set_url_template(_path_template),
            );
        }
        let options = google_cloud_gax::options::internal::set_default_idempotency(
            options,
            gaxi::http::default_idempotency(&method),
        );
        let builder = builder.query(&[("$alt", "json;enum-encoding=int")]).header(
            "x-goog-api-client",
            HeaderValue::from_static(&crate::info::X_GOOG_API_CLIENT_HEADER),
        );
        let body = gaxi::http::handle_empty(Some(req), &method);
        self.inner
            .execute(builder, body, options)
            .await
            .map(|r: crate::Response<wkt::Empty>| {
                let (parts, _) = r.into_parts();
                crate::Response::from_parts(parts, ())
            })
    }

    async fn search_trips(
        &self,
        req: crate::model::SearchTripsRequest,
        options: crate::RequestOptions,
    ) -> Result<crate::Response<crate::model::SearchTripsResponse>> {
        use gaxi::http::reqwest::{HeaderValue, Method};
        use gaxi::path_parameter::PathMismatchBuilder;
        use gaxi::path_parameter::try_match;
        use gaxi::routing_parameter::Segment;
        use google_cloud_gax::error::binding::BindingError;
        let (builder, method, _path_template) = None
            .or_else(|| {
                let var_parent = try_match(
                    Some(&req).map(|m| &m.parent).map(|s| s.as_str()),
                    &[Segment::Literal("providers/"), Segment::SingleWildcard],
                )?;
                let path = format!("/v1/{}/trips:search", var_parent,);
                let path_template = "/v1/{parent}/trips:search";

                let builder = self.inner.builder(Method::POST, path);
                let builder = Ok(builder);
                Some(builder.map(|b| (b, Method::POST, path_template)))
            })
            .ok_or_else(|| {
                let mut paths = Vec::new();
                {
                    let builder = PathMismatchBuilder::default();
                    let builder = builder.maybe_add(
                        Some(&req).map(|m| &m.parent).map(|s| s.as_str()),
                        &[Segment::Literal("providers/"), Segment::SingleWildcard],
                        "parent",
                        "providers/*",
                    );
                    paths.push(builder.build());
                }
                google_cloud_gax::error::Error::binding(BindingError { paths })
            })??;
        if let Some(recorder) = gaxi::observability::RequestRecorder::current() {
            recorder.on_client_request(
                gaxi::observability::ClientRequestAttributes::default()
                    .set_rpc_method("maps.fleetengine.v1.TripService/SearchTrips")
                    .set_url_template(_path_template),
            );
        }
        let options = google_cloud_gax::options::internal::set_default_idempotency(
            options,
            gaxi::http::default_idempotency(&method),
        );
        let builder = builder.query(&[("$alt", "json;enum-encoding=int")]).header(
            "x-goog-api-client",
            HeaderValue::from_static(&crate::info::X_GOOG_API_CLIENT_HEADER),
        );
        let body = gaxi::http::handle_empty(Some(req), &method);
        self.inner.execute(builder, body, options).await
    }

    async fn update_trip(
        &self,
        req: crate::model::UpdateTripRequest,
        options: crate::RequestOptions,
    ) -> Result<crate::Response<crate::model::Trip>> {
        use gaxi::http::reqwest::{HeaderValue, Method};
        use gaxi::path_parameter::PathMismatchBuilder;
        use gaxi::path_parameter::try_match;
        use gaxi::routing_parameter::Segment;
        use google_cloud_gax::error::binding::BindingError;
        let (builder, method, _path_template) = None
            .or_else(|| {
                let var_name = try_match(
                    Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                    &[
                        Segment::Literal("providers/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/trips/"),
                        Segment::SingleWildcard,
                    ],
                )?;
                let path = format!("/v1/{}", var_name,);
                let path_template = "/v1/{name}";

                let builder = self.inner.builder(Method::PUT, path);
                let builder = (|| {
                    let builder = req
                        .header
                        .as_ref()
                        .map(|p| serde_json::to_value(p).map_err(Error::ser))
                        .transpose()?
                        .into_iter()
                        .fold(builder, |builder, v| {
                            use gaxi::query_parameter::QueryParameter;
                            v.add(builder, "header")
                        });
                    let builder = req
                        .update_mask
                        .as_ref()
                        .map(|p| serde_json::to_value(p).map_err(Error::ser))
                        .transpose()?
                        .into_iter()
                        .fold(builder, |builder, v| {
                            use gaxi::query_parameter::QueryParameter;
                            v.add(builder, "updateMask")
                        });
                    Ok(builder)
                })();
                Some(builder.map(|b| (b, Method::PUT, path_template)))
            })
            .ok_or_else(|| {
                let mut paths = Vec::new();
                {
                    let builder = PathMismatchBuilder::default();
                    let builder = builder.maybe_add(
                        Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                        &[
                            Segment::Literal("providers/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/trips/"),
                            Segment::SingleWildcard,
                        ],
                        "name",
                        "providers/*/trips/*",
                    );
                    paths.push(builder.build());
                }
                google_cloud_gax::error::Error::binding(BindingError { paths })
            })??;
        if let Some(recorder) = gaxi::observability::RequestRecorder::current() {
            recorder.on_client_request(
                gaxi::observability::ClientRequestAttributes::default()
                    .set_rpc_method("maps.fleetengine.v1.TripService/UpdateTrip")
                    .set_url_template(_path_template),
            );
        }
        let options = google_cloud_gax::options::internal::set_default_idempotency(
            options,
            gaxi::http::default_idempotency(&method),
        );
        let builder = builder.query(&[("$alt", "json;enum-encoding=int")]).header(
            "x-goog-api-client",
            HeaderValue::from_static(&crate::info::X_GOOG_API_CLIENT_HEADER),
        );
        let body = gaxi::http::handle_empty(req.trip, &method);
        self.inner.execute(builder, body, options).await
    }
}

/// Implements [VehicleService](super::stub::VehicleService) using a [gaxi::http::ReqwestClient].
#[derive(Clone)]
pub struct VehicleService {
    inner: gaxi::http::ReqwestClient,
}

impl std::fmt::Debug for VehicleService {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::result::Result<(), std::fmt::Error> {
        f.debug_struct("VehicleService")
            .field("inner", &self.inner)
            .finish()
    }
}

impl VehicleService {
    pub async fn new(config: gaxi::options::ClientConfig) -> crate::ClientBuilderResult<Self> {
        let tracing_is_enabled = gaxi::options::tracing_enabled(&config);
        let inner = gaxi::http::ReqwestClient::new(config, crate::DEFAULT_HOST).await?;
        let inner = if tracing_is_enabled {
            inner.with_instrumentation(&super::tracing::info::INSTRUMENTATION_CLIENT_INFO)
        } else {
            inner
        };
        Ok(Self { inner })
    }
}

impl super::stub::VehicleService for VehicleService {
    async fn create_vehicle(
        &self,
        req: crate::model::CreateVehicleRequest,
        options: crate::RequestOptions,
    ) -> Result<crate::Response<crate::model::Vehicle>> {
        use gaxi::http::reqwest::{HeaderValue, Method};
        use gaxi::path_parameter::PathMismatchBuilder;
        use gaxi::path_parameter::try_match;
        use gaxi::routing_parameter::Segment;
        use google_cloud_gax::error::binding::BindingError;
        let (builder, method, _path_template) = None
            .or_else(|| {
                let var_parent = try_match(
                    Some(&req).map(|m| &m.parent).map(|s| s.as_str()),
                    &[Segment::Literal("providers/"), Segment::SingleWildcard],
                )?;
                let path = format!("/v1/{}/vehicles", var_parent,);
                let path_template = "/v1/{parent}/vehicles";

                let builder = self.inner.builder(Method::POST, path);
                let builder = (|| {
                    let builder = req
                        .header
                        .as_ref()
                        .map(|p| serde_json::to_value(p).map_err(Error::ser))
                        .transpose()?
                        .into_iter()
                        .fold(builder, |builder, v| {
                            use gaxi::query_parameter::QueryParameter;
                            v.add(builder, "header")
                        });
                    let builder = builder.query(&[("vehicleId", &req.vehicle_id)]);
                    Ok(builder)
                })();
                Some(builder.map(|b| (b, Method::POST, path_template)))
            })
            .ok_or_else(|| {
                let mut paths = Vec::new();
                {
                    let builder = PathMismatchBuilder::default();
                    let builder = builder.maybe_add(
                        Some(&req).map(|m| &m.parent).map(|s| s.as_str()),
                        &[Segment::Literal("providers/"), Segment::SingleWildcard],
                        "parent",
                        "providers/*",
                    );
                    paths.push(builder.build());
                }
                google_cloud_gax::error::Error::binding(BindingError { paths })
            })??;
        if let Some(recorder) = gaxi::observability::RequestRecorder::current() {
            recorder.on_client_request(
                gaxi::observability::ClientRequestAttributes::default()
                    .set_rpc_method("maps.fleetengine.v1.VehicleService/CreateVehicle")
                    .set_url_template(_path_template),
            );
        }
        let options = google_cloud_gax::options::internal::set_default_idempotency(
            options,
            gaxi::http::default_idempotency(&method),
        );
        let builder = builder.query(&[("$alt", "json;enum-encoding=int")]).header(
            "x-goog-api-client",
            HeaderValue::from_static(&crate::info::X_GOOG_API_CLIENT_HEADER),
        );
        let body = gaxi::http::handle_empty(req.vehicle, &method);
        self.inner.execute(builder, body, options).await
    }

    async fn get_vehicle(
        &self,
        req: crate::model::GetVehicleRequest,
        options: crate::RequestOptions,
    ) -> Result<crate::Response<crate::model::Vehicle>> {
        use gaxi::http::reqwest::{HeaderValue, Method};
        use gaxi::path_parameter::PathMismatchBuilder;
        use gaxi::path_parameter::try_match;
        use gaxi::routing_parameter::Segment;
        use google_cloud_gax::error::binding::BindingError;
        let (builder, method, _path_template, _resource_name) = None
            .or_else(|| {
                let var_name = try_match(
                    Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                    &[
                        Segment::Literal("providers/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/vehicles/"),
                        Segment::SingleWildcard,
                    ],
                )?;
                let path = format!("/v1/{}", var_name,);
                let path_template = "/v1/{name}";

                let resource_name = format!("//fleetengine.googleapis.com/{}", var_name,);
                let builder = self.inner.builder(Method::GET, path);
                let builder = (|| {
                    let builder = req
                        .header
                        .as_ref()
                        .map(|p| serde_json::to_value(p).map_err(Error::ser))
                        .transpose()?
                        .into_iter()
                        .fold(builder, |builder, v| {
                            use gaxi::query_parameter::QueryParameter;
                            v.add(builder, "header")
                        });
                    let builder = req
                        .current_route_segment_version
                        .as_ref()
                        .map(|p| serde_json::to_value(p).map_err(Error::ser))
                        .transpose()?
                        .into_iter()
                        .fold(builder, |builder, v| {
                            use gaxi::query_parameter::QueryParameter;
                            v.add(builder, "currentRouteSegmentVersion")
                        });
                    let builder = req
                        .waypoints_version
                        .as_ref()
                        .map(|p| serde_json::to_value(p).map_err(Error::ser))
                        .transpose()?
                        .into_iter()
                        .fold(builder, |builder, v| {
                            use gaxi::query_parameter::QueryParameter;
                            v.add(builder, "waypointsVersion")
                        });
                    Ok(builder)
                })();
                Some(builder.map(|b| (b, Method::GET, path_template, resource_name)))
            })
            .ok_or_else(|| {
                let mut paths = Vec::new();
                {
                    let builder = PathMismatchBuilder::default();
                    let builder = builder.maybe_add(
                        Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                        &[
                            Segment::Literal("providers/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/vehicles/"),
                            Segment::SingleWildcard,
                        ],
                        "name",
                        "providers/*/vehicles/*",
                    );
                    paths.push(builder.build());
                }
                google_cloud_gax::error::Error::binding(BindingError { paths })
            })??;
        if let Some(recorder) = gaxi::observability::RequestRecorder::current() {
            recorder.on_client_request(
                gaxi::observability::ClientRequestAttributes::default()
                    .set_rpc_method("maps.fleetengine.v1.VehicleService/GetVehicle")
                    .set_url_template(_path_template)
                    .set_resource_name(_resource_name),
            );
        }
        let options = google_cloud_gax::options::internal::set_default_idempotency(
            options,
            gaxi::http::default_idempotency(&method),
        );
        let builder = builder.query(&[("$alt", "json;enum-encoding=int")]).header(
            "x-goog-api-client",
            HeaderValue::from_static(&crate::info::X_GOOG_API_CLIENT_HEADER),
        );
        let body = gaxi::http::handle_empty(None::<wkt::Empty>, &method);
        self.inner.execute(builder, body, options).await
    }

    async fn delete_vehicle(
        &self,
        req: crate::model::DeleteVehicleRequest,
        options: crate::RequestOptions,
    ) -> Result<crate::Response<()>> {
        use gaxi::http::reqwest::{HeaderValue, Method};
        use gaxi::path_parameter::PathMismatchBuilder;
        use gaxi::path_parameter::try_match;
        use gaxi::routing_parameter::Segment;
        use google_cloud_gax::error::binding::BindingError;
        let (builder, method, _path_template, _resource_name) = None
            .or_else(|| {
                let var_name = try_match(
                    Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                    &[
                        Segment::Literal("providers/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/vehicles/"),
                        Segment::SingleWildcard,
                    ],
                )?;
                let path = format!("/v1/{}", var_name,);
                let path_template = "/v1/{name}";

                let resource_name = format!("//fleetengine.googleapis.com/{}", var_name,);
                let builder = self.inner.builder(Method::DELETE, path);
                let builder = (|| {
                    let builder = req
                        .header
                        .as_ref()
                        .map(|p| serde_json::to_value(p).map_err(Error::ser))
                        .transpose()?
                        .into_iter()
                        .fold(builder, |builder, v| {
                            use gaxi::query_parameter::QueryParameter;
                            v.add(builder, "header")
                        });
                    Ok(builder)
                })();
                Some(builder.map(|b| (b, Method::DELETE, path_template, resource_name)))
            })
            .ok_or_else(|| {
                let mut paths = Vec::new();
                {
                    let builder = PathMismatchBuilder::default();
                    let builder = builder.maybe_add(
                        Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                        &[
                            Segment::Literal("providers/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/vehicles/"),
                            Segment::SingleWildcard,
                        ],
                        "name",
                        "providers/*/vehicles/*",
                    );
                    paths.push(builder.build());
                }
                google_cloud_gax::error::Error::binding(BindingError { paths })
            })??;
        if let Some(recorder) = gaxi::observability::RequestRecorder::current() {
            recorder.on_client_request(
                gaxi::observability::ClientRequestAttributes::default()
                    .set_rpc_method("maps.fleetengine.v1.VehicleService/DeleteVehicle")
                    .set_url_template(_path_template)
                    .set_resource_name(_resource_name),
            );
        }
        let options = google_cloud_gax::options::internal::set_default_idempotency(
            options,
            gaxi::http::default_idempotency(&method),
        );
        let builder = builder.query(&[("$alt", "json;enum-encoding=int")]).header(
            "x-goog-api-client",
            HeaderValue::from_static(&crate::info::X_GOOG_API_CLIENT_HEADER),
        );
        let body = gaxi::http::handle_empty(None::<wkt::Empty>, &method);
        self.inner
            .execute(builder, body, options)
            .await
            .map(|r: crate::Response<wkt::Empty>| {
                let (parts, _) = r.into_parts();
                crate::Response::from_parts(parts, ())
            })
    }

    async fn update_vehicle(
        &self,
        req: crate::model::UpdateVehicleRequest,
        options: crate::RequestOptions,
    ) -> Result<crate::Response<crate::model::Vehicle>> {
        use gaxi::http::reqwest::{HeaderValue, Method};
        use gaxi::path_parameter::PathMismatchBuilder;
        use gaxi::path_parameter::try_match;
        use gaxi::routing_parameter::Segment;
        use google_cloud_gax::error::binding::BindingError;
        let (builder, method, _path_template) = None
            .or_else(|| {
                let var_name = try_match(
                    Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                    &[
                        Segment::Literal("providers/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/vehicles/"),
                        Segment::SingleWildcard,
                    ],
                )?;
                let path = format!("/v1/{}", var_name,);
                let path_template = "/v1/{name}";

                let builder = self.inner.builder(Method::PUT, path);
                let builder = (|| {
                    let builder = req
                        .header
                        .as_ref()
                        .map(|p| serde_json::to_value(p).map_err(Error::ser))
                        .transpose()?
                        .into_iter()
                        .fold(builder, |builder, v| {
                            use gaxi::query_parameter::QueryParameter;
                            v.add(builder, "header")
                        });
                    let builder = req
                        .update_mask
                        .as_ref()
                        .map(|p| serde_json::to_value(p).map_err(Error::ser))
                        .transpose()?
                        .into_iter()
                        .fold(builder, |builder, v| {
                            use gaxi::query_parameter::QueryParameter;
                            v.add(builder, "updateMask")
                        });
                    Ok(builder)
                })();
                Some(builder.map(|b| (b, Method::PUT, path_template)))
            })
            .ok_or_else(|| {
                let mut paths = Vec::new();
                {
                    let builder = PathMismatchBuilder::default();
                    let builder = builder.maybe_add(
                        Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                        &[
                            Segment::Literal("providers/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/vehicles/"),
                            Segment::SingleWildcard,
                        ],
                        "name",
                        "providers/*/vehicles/*",
                    );
                    paths.push(builder.build());
                }
                google_cloud_gax::error::Error::binding(BindingError { paths })
            })??;
        if let Some(recorder) = gaxi::observability::RequestRecorder::current() {
            recorder.on_client_request(
                gaxi::observability::ClientRequestAttributes::default()
                    .set_rpc_method("maps.fleetengine.v1.VehicleService/UpdateVehicle")
                    .set_url_template(_path_template),
            );
        }
        let options = google_cloud_gax::options::internal::set_default_idempotency(
            options,
            gaxi::http::default_idempotency(&method),
        );
        let builder = builder.query(&[("$alt", "json;enum-encoding=int")]).header(
            "x-goog-api-client",
            HeaderValue::from_static(&crate::info::X_GOOG_API_CLIENT_HEADER),
        );
        let body = gaxi::http::handle_empty(req.vehicle, &method);
        self.inner.execute(builder, body, options).await
    }

    async fn update_vehicle_attributes(
        &self,
        req: crate::model::UpdateVehicleAttributesRequest,
        options: crate::RequestOptions,
    ) -> Result<crate::Response<crate::model::UpdateVehicleAttributesResponse>> {
        use gaxi::http::reqwest::{HeaderValue, Method};
        use gaxi::path_parameter::PathMismatchBuilder;
        use gaxi::path_parameter::try_match;
        use gaxi::routing_parameter::Segment;
        use google_cloud_gax::error::binding::BindingError;
        let (builder, method, _path_template) = None
            .or_else(|| {
                let var_name = try_match(
                    Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                    &[
                        Segment::Literal("providers/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/vehicles/"),
                        Segment::SingleWildcard,
                    ],
                )?;
                let path = format!("/v1/{}:updateAttributes", var_name,);
                let path_template = "/v1/{name}:updateAttributes";

                let builder = self.inner.builder(Method::POST, path);
                let builder = Ok(builder);
                Some(builder.map(|b| (b, Method::POST, path_template)))
            })
            .ok_or_else(|| {
                let mut paths = Vec::new();
                {
                    let builder = PathMismatchBuilder::default();
                    let builder = builder.maybe_add(
                        Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                        &[
                            Segment::Literal("providers/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/vehicles/"),
                            Segment::SingleWildcard,
                        ],
                        "name",
                        "providers/*/vehicles/*",
                    );
                    paths.push(builder.build());
                }
                google_cloud_gax::error::Error::binding(BindingError { paths })
            })??;
        if let Some(recorder) = gaxi::observability::RequestRecorder::current() {
            recorder.on_client_request(
                gaxi::observability::ClientRequestAttributes::default()
                    .set_rpc_method("maps.fleetengine.v1.VehicleService/UpdateVehicleAttributes")
                    .set_url_template(_path_template),
            );
        }
        let options = google_cloud_gax::options::internal::set_default_idempotency(
            options,
            gaxi::http::default_idempotency(&method),
        );
        let builder = builder.query(&[("$alt", "json;enum-encoding=int")]).header(
            "x-goog-api-client",
            HeaderValue::from_static(&crate::info::X_GOOG_API_CLIENT_HEADER),
        );
        let body = gaxi::http::handle_empty(Some(req), &method);
        self.inner.execute(builder, body, options).await
    }

    async fn list_vehicles(
        &self,
        req: crate::model::ListVehiclesRequest,
        options: crate::RequestOptions,
    ) -> Result<crate::Response<crate::model::ListVehiclesResponse>> {
        use gaxi::http::reqwest::{HeaderValue, Method};
        use gaxi::path_parameter::PathMismatchBuilder;
        use gaxi::path_parameter::try_match;
        use gaxi::routing_parameter::Segment;
        use google_cloud_gax::error::binding::BindingError;
        let (builder, method, _path_template) = None
            .or_else(|| {
                let var_parent = try_match(
                    Some(&req).map(|m| &m.parent).map(|s| s.as_str()),
                    &[Segment::Literal("providers/"), Segment::SingleWildcard],
                )?;
                let path = format!("/v1/{}/vehicles", var_parent,);
                let path_template = "/v1/{parent}/vehicles";

                let builder = self.inner.builder(Method::GET, path);
                let builder = (|| {
                    let builder = req
                        .header
                        .as_ref()
                        .map(|p| serde_json::to_value(p).map_err(Error::ser))
                        .transpose()?
                        .into_iter()
                        .fold(builder, |builder, v| {
                            use gaxi::query_parameter::QueryParameter;
                            v.add(builder, "header")
                        });
                    let builder = builder.query(&[("pageSize", &req.page_size)]);
                    let builder = builder.query(&[("pageToken", &req.page_token)]);
                    let builder = req
                        .minimum_capacity
                        .as_ref()
                        .map(|p| serde_json::to_value(p).map_err(Error::ser))
                        .transpose()?
                        .into_iter()
                        .fold(builder, |builder, v| {
                            use gaxi::query_parameter::QueryParameter;
                            v.add(builder, "minimumCapacity")
                        });
                    let builder = req
                        .trip_types
                        .iter()
                        .fold(builder, |builder, p| builder.query(&[("tripTypes", p)]));
                    let builder = req
                        .maximum_staleness
                        .as_ref()
                        .map(|p| serde_json::to_value(p).map_err(Error::ser))
                        .transpose()?
                        .into_iter()
                        .fold(builder, |builder, v| {
                            use gaxi::query_parameter::QueryParameter;
                            v.add(builder, "maximumStaleness")
                        });
                    let builder = req
                        .vehicle_type_categories
                        .iter()
                        .fold(builder, |builder, p| {
                            builder.query(&[("vehicleTypeCategories", p)])
                        });
                    let builder = req.required_attributes.iter().fold(builder, |builder, p| {
                        builder.query(&[("requiredAttributes", p)])
                    });
                    let builder = req
                        .required_one_of_attributes
                        .iter()
                        .fold(builder, |builder, p| {
                            builder.query(&[("requiredOneOfAttributes", p)])
                        });
                    let builder = req
                        .required_one_of_attribute_sets
                        .iter()
                        .fold(builder, |builder, p| {
                            builder.query(&[("requiredOneOfAttributeSets", p)])
                        });
                    let builder = builder.query(&[("vehicleState", &req.vehicle_state)]);
                    let builder = builder.query(&[("onTripOnly", &req.on_trip_only)]);
                    let builder = builder.query(&[("filter", &req.filter)]);
                    let builder = req
                        .viewport
                        .as_ref()
                        .map(|p| serde_json::to_value(p).map_err(Error::ser))
                        .transpose()?
                        .into_iter()
                        .fold(builder, |builder, v| {
                            use gaxi::query_parameter::QueryParameter;
                            v.add(builder, "viewport")
                        });
                    Ok(builder)
                })();
                Some(builder.map(|b| (b, Method::GET, path_template)))
            })
            .ok_or_else(|| {
                let mut paths = Vec::new();
                {
                    let builder = PathMismatchBuilder::default();
                    let builder = builder.maybe_add(
                        Some(&req).map(|m| &m.parent).map(|s| s.as_str()),
                        &[Segment::Literal("providers/"), Segment::SingleWildcard],
                        "parent",
                        "providers/*",
                    );
                    paths.push(builder.build());
                }
                google_cloud_gax::error::Error::binding(BindingError { paths })
            })??;
        if let Some(recorder) = gaxi::observability::RequestRecorder::current() {
            recorder.on_client_request(
                gaxi::observability::ClientRequestAttributes::default()
                    .set_rpc_method("maps.fleetengine.v1.VehicleService/ListVehicles")
                    .set_url_template(_path_template),
            );
        }
        let options = google_cloud_gax::options::internal::set_default_idempotency(
            options,
            gaxi::http::default_idempotency(&method),
        );
        let builder = builder.query(&[("$alt", "json;enum-encoding=int")]).header(
            "x-goog-api-client",
            HeaderValue::from_static(&crate::info::X_GOOG_API_CLIENT_HEADER),
        );
        let body = gaxi::http::handle_empty(None::<wkt::Empty>, &method);
        self.inner.execute(builder, body, options).await
    }

    async fn search_vehicles(
        &self,
        req: crate::model::SearchVehiclesRequest,
        options: crate::RequestOptions,
    ) -> Result<crate::Response<crate::model::SearchVehiclesResponse>> {
        use gaxi::http::reqwest::{HeaderValue, Method};
        use gaxi::path_parameter::PathMismatchBuilder;
        use gaxi::path_parameter::try_match;
        use gaxi::routing_parameter::Segment;
        use google_cloud_gax::error::binding::BindingError;
        let (builder, method, _path_template) = None
            .or_else(|| {
                let var_parent = try_match(
                    Some(&req).map(|m| &m.parent).map(|s| s.as_str()),
                    &[Segment::Literal("providers/"), Segment::SingleWildcard],
                )?;
                let path = format!("/v1/{}/vehicles:search", var_parent,);
                let path_template = "/v1/{parent}/vehicles:search";

                let builder = self.inner.builder(Method::POST, path);
                let builder = Ok(builder);
                Some(builder.map(|b| (b, Method::POST, path_template)))
            })
            .ok_or_else(|| {
                let mut paths = Vec::new();
                {
                    let builder = PathMismatchBuilder::default();
                    let builder = builder.maybe_add(
                        Some(&req).map(|m| &m.parent).map(|s| s.as_str()),
                        &[Segment::Literal("providers/"), Segment::SingleWildcard],
                        "parent",
                        "providers/*",
                    );
                    paths.push(builder.build());
                }
                google_cloud_gax::error::Error::binding(BindingError { paths })
            })??;
        if let Some(recorder) = gaxi::observability::RequestRecorder::current() {
            recorder.on_client_request(
                gaxi::observability::ClientRequestAttributes::default()
                    .set_rpc_method("maps.fleetengine.v1.VehicleService/SearchVehicles")
                    .set_url_template(_path_template),
            );
        }
        let options = google_cloud_gax::options::internal::set_default_idempotency(
            options,
            gaxi::http::default_idempotency(&method),
        );
        let builder = builder.query(&[("$alt", "json;enum-encoding=int")]).header(
            "x-goog-api-client",
            HeaderValue::from_static(&crate::info::X_GOOG_API_CLIENT_HEADER),
        );
        let body = gaxi::http::handle_empty(Some(req), &method);
        self.inner.execute(builder, body, options).await
    }
}
