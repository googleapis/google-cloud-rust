// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// Code generated by sidekick. DO NOT EDIT.

/// Request and client builders for [TripService][crate::client::TripService].
pub mod trip_service {
    use crate::Result;

    /// A builder for [TripService][crate::client::TripService].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_maps_fleetengine_v1::*;
    /// # use builder::trip_service::ClientBuilder;
    /// # use client::TripService;
    /// let builder : ClientBuilder = TripService::builder();
    /// let client = builder
    ///     .with_endpoint("https://fleetengine.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::TripService;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = TripService;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::TripService] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::TripService>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TripService>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [TripService::create_trip][crate::client::TripService::create_trip] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_fleetengine_v1::builder::trip_service::CreateTrip;
    /// # async fn sample() -> google_maps_fleetengine_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CreateTrip {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CreateTrip(RequestBuilder<crate::model::CreateTripRequest>);

    impl CreateTrip {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TripService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::CreateTripRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Trip> {
            (*self.0.stub)
                .create_trip(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [header][crate::model::CreateTripRequest::header].
        pub fn set_header<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RequestHeader>,
        {
            self.0.request.header = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [header][crate::model::CreateTripRequest::header].
        pub fn set_or_clear_header<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RequestHeader>,
        {
            self.0.request.header = v.map(|x| x.into());
            self
        }

        /// Sets the value of [parent][crate::model::CreateTripRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [trip_id][crate::model::CreateTripRequest::trip_id].
        ///
        /// This is a **required** field for requests.
        pub fn set_trip_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.trip_id = v.into();
            self
        }

        /// Sets the value of [trip][crate::model::CreateTripRequest::trip].
        ///
        /// This is a **required** field for requests.
        pub fn set_trip<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Trip>,
        {
            self.0.request.trip = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [trip][crate::model::CreateTripRequest::trip].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_trip<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Trip>,
        {
            self.0.request.trip = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CreateTrip {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TripService::get_trip][crate::client::TripService::get_trip] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_fleetengine_v1::builder::trip_service::GetTrip;
    /// # async fn sample() -> google_maps_fleetengine_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetTrip {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetTrip(RequestBuilder<crate::model::GetTripRequest>);

    impl GetTrip {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TripService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetTripRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Trip> {
            (*self.0.stub)
                .get_trip(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [header][crate::model::GetTripRequest::header].
        pub fn set_header<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RequestHeader>,
        {
            self.0.request.header = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [header][crate::model::GetTripRequest::header].
        pub fn set_or_clear_header<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RequestHeader>,
        {
            self.0.request.header = v.map(|x| x.into());
            self
        }

        /// Sets the value of [name][crate::model::GetTripRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }

        /// Sets the value of [view][crate::model::GetTripRequest::view].
        pub fn set_view<T: Into<crate::model::TripView>>(mut self, v: T) -> Self {
            self.0.request.view = v.into();
            self
        }

        /// Sets the value of [current_route_segment_version][crate::model::GetTripRequest::current_route_segment_version].
        pub fn set_current_route_segment_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::Timestamp>,
        {
            self.0.request.current_route_segment_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [current_route_segment_version][crate::model::GetTripRequest::current_route_segment_version].
        pub fn set_or_clear_current_route_segment_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<wkt::Timestamp>,
        {
            self.0.request.current_route_segment_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [remaining_waypoints_version][crate::model::GetTripRequest::remaining_waypoints_version].
        #[deprecated]
        pub fn set_remaining_waypoints_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::Timestamp>,
        {
            self.0.request.remaining_waypoints_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [remaining_waypoints_version][crate::model::GetTripRequest::remaining_waypoints_version].
        #[deprecated]
        pub fn set_or_clear_remaining_waypoints_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<wkt::Timestamp>,
        {
            self.0.request.remaining_waypoints_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [route_format_type][crate::model::GetTripRequest::route_format_type].
        pub fn set_route_format_type<T: Into<crate::model::PolylineFormatType>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.route_format_type = v.into();
            self
        }

        /// Sets the value of [current_route_segment_traffic_version][crate::model::GetTripRequest::current_route_segment_traffic_version].
        pub fn set_current_route_segment_traffic_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::Timestamp>,
        {
            self.0.request.current_route_segment_traffic_version =
                std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [current_route_segment_traffic_version][crate::model::GetTripRequest::current_route_segment_traffic_version].
        pub fn set_or_clear_current_route_segment_traffic_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<wkt::Timestamp>,
        {
            self.0.request.current_route_segment_traffic_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [remaining_waypoints_route_version][crate::model::GetTripRequest::remaining_waypoints_route_version].
        pub fn set_remaining_waypoints_route_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::Timestamp>,
        {
            self.0.request.remaining_waypoints_route_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [remaining_waypoints_route_version][crate::model::GetTripRequest::remaining_waypoints_route_version].
        pub fn set_or_clear_remaining_waypoints_route_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<wkt::Timestamp>,
        {
            self.0.request.remaining_waypoints_route_version = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetTrip {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TripService::delete_trip][crate::client::TripService::delete_trip] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_fleetengine_v1::builder::trip_service::DeleteTrip;
    /// # async fn sample() -> google_maps_fleetengine_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeleteTrip {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeleteTrip(RequestBuilder<crate::model::DeleteTripRequest>);

    impl DeleteTrip {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TripService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::DeleteTripRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<()> {
            (*self.0.stub)
                .delete_trip(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [header][crate::model::DeleteTripRequest::header].
        pub fn set_header<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RequestHeader>,
        {
            self.0.request.header = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [header][crate::model::DeleteTripRequest::header].
        pub fn set_or_clear_header<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RequestHeader>,
        {
            self.0.request.header = v.map(|x| x.into());
            self
        }

        /// Sets the value of [name][crate::model::DeleteTripRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeleteTrip {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TripService::report_billable_trip][crate::client::TripService::report_billable_trip] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_fleetengine_v1::builder::trip_service::ReportBillableTrip;
    /// # async fn sample() -> google_maps_fleetengine_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ReportBillableTrip {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ReportBillableTrip(RequestBuilder<crate::model::ReportBillableTripRequest>);

    impl ReportBillableTrip {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TripService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ReportBillableTripRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<()> {
            (*self.0.stub)
                .report_billable_trip(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::ReportBillableTripRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }

        /// Sets the value of [country_code][crate::model::ReportBillableTripRequest::country_code].
        ///
        /// This is a **required** field for requests.
        pub fn set_country_code<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.country_code = v.into();
            self
        }

        /// Sets the value of [platform][crate::model::ReportBillableTripRequest::platform].
        pub fn set_platform<T: Into<crate::model::BillingPlatformIdentifier>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.platform = v.into();
            self
        }

        /// Sets the value of [related_ids][crate::model::ReportBillableTripRequest::related_ids].
        pub fn set_related_ids<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<std::string::String>,
        {
            use std::iter::Iterator;
            self.0.request.related_ids = v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [solution_type][crate::model::ReportBillableTripRequest::solution_type].
        pub fn set_solution_type<
            T: Into<crate::model::report_billable_trip_request::SolutionType>,
        >(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.solution_type = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ReportBillableTrip {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TripService::search_trips][crate::client::TripService::search_trips] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_fleetengine_v1::builder::trip_service::SearchTrips;
    /// # async fn sample() -> google_maps_fleetengine_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SearchTrips {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SearchTrips(RequestBuilder<crate::model::SearchTripsRequest>);

    impl SearchTrips {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TripService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::SearchTripsRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SearchTripsResponse> {
            (*self.0.stub)
                .search_trips(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::SearchTripsResponse, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::SearchTripsResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [header][crate::model::SearchTripsRequest::header].
        pub fn set_header<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RequestHeader>,
        {
            self.0.request.header = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [header][crate::model::SearchTripsRequest::header].
        pub fn set_or_clear_header<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RequestHeader>,
        {
            self.0.request.header = v.map(|x| x.into());
            self
        }

        /// Sets the value of [parent][crate::model::SearchTripsRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [vehicle_id][crate::model::SearchTripsRequest::vehicle_id].
        pub fn set_vehicle_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.vehicle_id = v.into();
            self
        }

        /// Sets the value of [active_trips_only][crate::model::SearchTripsRequest::active_trips_only].
        pub fn set_active_trips_only<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.active_trips_only = v.into();
            self
        }

        /// Sets the value of [page_size][crate::model::SearchTripsRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::SearchTripsRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }

        /// Sets the value of [minimum_staleness][crate::model::SearchTripsRequest::minimum_staleness].
        pub fn set_minimum_staleness<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::Duration>,
        {
            self.0.request.minimum_staleness = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [minimum_staleness][crate::model::SearchTripsRequest::minimum_staleness].
        pub fn set_or_clear_minimum_staleness<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::Duration>,
        {
            self.0.request.minimum_staleness = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SearchTrips {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TripService::update_trip][crate::client::TripService::update_trip] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_fleetengine_v1::builder::trip_service::UpdateTrip;
    /// # async fn sample() -> google_maps_fleetengine_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateTrip {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateTrip(RequestBuilder<crate::model::UpdateTripRequest>);

    impl UpdateTrip {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TripService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::UpdateTripRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Trip> {
            (*self.0.stub)
                .update_trip(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [header][crate::model::UpdateTripRequest::header].
        pub fn set_header<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RequestHeader>,
        {
            self.0.request.header = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [header][crate::model::UpdateTripRequest::header].
        pub fn set_or_clear_header<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RequestHeader>,
        {
            self.0.request.header = v.map(|x| x.into());
            self
        }

        /// Sets the value of [name][crate::model::UpdateTripRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }

        /// Sets the value of [trip][crate::model::UpdateTripRequest::trip].
        ///
        /// This is a **required** field for requests.
        pub fn set_trip<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Trip>,
        {
            self.0.request.trip = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [trip][crate::model::UpdateTripRequest::trip].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_trip<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Trip>,
        {
            self.0.request.trip = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::UpdateTripRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::UpdateTripRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateTrip {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [VehicleService][crate::client::VehicleService].
pub mod vehicle_service {
    use crate::Result;

    /// A builder for [VehicleService][crate::client::VehicleService].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_maps_fleetengine_v1::*;
    /// # use builder::vehicle_service::ClientBuilder;
    /// # use client::VehicleService;
    /// let builder : ClientBuilder = VehicleService::builder();
    /// let client = builder
    ///     .with_endpoint("https://fleetengine.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::VehicleService;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = VehicleService;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::VehicleService] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::VehicleService>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VehicleService>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [VehicleService::create_vehicle][crate::client::VehicleService::create_vehicle] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_fleetengine_v1::builder::vehicle_service::CreateVehicle;
    /// # async fn sample() -> google_maps_fleetengine_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CreateVehicle {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CreateVehicle(RequestBuilder<crate::model::CreateVehicleRequest>);

    impl CreateVehicle {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VehicleService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::CreateVehicleRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Vehicle> {
            (*self.0.stub)
                .create_vehicle(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [header][crate::model::CreateVehicleRequest::header].
        pub fn set_header<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RequestHeader>,
        {
            self.0.request.header = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [header][crate::model::CreateVehicleRequest::header].
        pub fn set_or_clear_header<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RequestHeader>,
        {
            self.0.request.header = v.map(|x| x.into());
            self
        }

        /// Sets the value of [parent][crate::model::CreateVehicleRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [vehicle_id][crate::model::CreateVehicleRequest::vehicle_id].
        ///
        /// This is a **required** field for requests.
        pub fn set_vehicle_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.vehicle_id = v.into();
            self
        }

        /// Sets the value of [vehicle][crate::model::CreateVehicleRequest::vehicle].
        ///
        /// This is a **required** field for requests.
        pub fn set_vehicle<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Vehicle>,
        {
            self.0.request.vehicle = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [vehicle][crate::model::CreateVehicleRequest::vehicle].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_vehicle<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Vehicle>,
        {
            self.0.request.vehicle = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CreateVehicle {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [VehicleService::get_vehicle][crate::client::VehicleService::get_vehicle] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_fleetengine_v1::builder::vehicle_service::GetVehicle;
    /// # async fn sample() -> google_maps_fleetengine_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetVehicle {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetVehicle(RequestBuilder<crate::model::GetVehicleRequest>);

    impl GetVehicle {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VehicleService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetVehicleRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Vehicle> {
            (*self.0.stub)
                .get_vehicle(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [header][crate::model::GetVehicleRequest::header].
        pub fn set_header<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RequestHeader>,
        {
            self.0.request.header = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [header][crate::model::GetVehicleRequest::header].
        pub fn set_or_clear_header<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RequestHeader>,
        {
            self.0.request.header = v.map(|x| x.into());
            self
        }

        /// Sets the value of [name][crate::model::GetVehicleRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }

        /// Sets the value of [current_route_segment_version][crate::model::GetVehicleRequest::current_route_segment_version].
        pub fn set_current_route_segment_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::Timestamp>,
        {
            self.0.request.current_route_segment_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [current_route_segment_version][crate::model::GetVehicleRequest::current_route_segment_version].
        pub fn set_or_clear_current_route_segment_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<wkt::Timestamp>,
        {
            self.0.request.current_route_segment_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [waypoints_version][crate::model::GetVehicleRequest::waypoints_version].
        pub fn set_waypoints_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::Timestamp>,
        {
            self.0.request.waypoints_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [waypoints_version][crate::model::GetVehicleRequest::waypoints_version].
        pub fn set_or_clear_waypoints_version<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::Timestamp>,
        {
            self.0.request.waypoints_version = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetVehicle {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [VehicleService::delete_vehicle][crate::client::VehicleService::delete_vehicle] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_fleetengine_v1::builder::vehicle_service::DeleteVehicle;
    /// # async fn sample() -> google_maps_fleetengine_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeleteVehicle {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeleteVehicle(RequestBuilder<crate::model::DeleteVehicleRequest>);

    impl DeleteVehicle {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VehicleService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::DeleteVehicleRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<()> {
            (*self.0.stub)
                .delete_vehicle(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [header][crate::model::DeleteVehicleRequest::header].
        pub fn set_header<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RequestHeader>,
        {
            self.0.request.header = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [header][crate::model::DeleteVehicleRequest::header].
        pub fn set_or_clear_header<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RequestHeader>,
        {
            self.0.request.header = v.map(|x| x.into());
            self
        }

        /// Sets the value of [name][crate::model::DeleteVehicleRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeleteVehicle {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [VehicleService::update_vehicle][crate::client::VehicleService::update_vehicle] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_fleetengine_v1::builder::vehicle_service::UpdateVehicle;
    /// # async fn sample() -> google_maps_fleetengine_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateVehicle {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateVehicle(RequestBuilder<crate::model::UpdateVehicleRequest>);

    impl UpdateVehicle {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VehicleService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::UpdateVehicleRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Vehicle> {
            (*self.0.stub)
                .update_vehicle(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [header][crate::model::UpdateVehicleRequest::header].
        pub fn set_header<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RequestHeader>,
        {
            self.0.request.header = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [header][crate::model::UpdateVehicleRequest::header].
        pub fn set_or_clear_header<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RequestHeader>,
        {
            self.0.request.header = v.map(|x| x.into());
            self
        }

        /// Sets the value of [name][crate::model::UpdateVehicleRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }

        /// Sets the value of [vehicle][crate::model::UpdateVehicleRequest::vehicle].
        ///
        /// This is a **required** field for requests.
        pub fn set_vehicle<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Vehicle>,
        {
            self.0.request.vehicle = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [vehicle][crate::model::UpdateVehicleRequest::vehicle].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_vehicle<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Vehicle>,
        {
            self.0.request.vehicle = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::UpdateVehicleRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::UpdateVehicleRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateVehicle {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [VehicleService::update_vehicle_attributes][crate::client::VehicleService::update_vehicle_attributes] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_fleetengine_v1::builder::vehicle_service::UpdateVehicleAttributes;
    /// # async fn sample() -> google_maps_fleetengine_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateVehicleAttributes {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateVehicleAttributes(
        RequestBuilder<crate::model::UpdateVehicleAttributesRequest>,
    );

    impl UpdateVehicleAttributes {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VehicleService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::UpdateVehicleAttributesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::UpdateVehicleAttributesResponse> {
            (*self.0.stub)
                .update_vehicle_attributes(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [header][crate::model::UpdateVehicleAttributesRequest::header].
        pub fn set_header<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RequestHeader>,
        {
            self.0.request.header = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [header][crate::model::UpdateVehicleAttributesRequest::header].
        pub fn set_or_clear_header<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RequestHeader>,
        {
            self.0.request.header = v.map(|x| x.into());
            self
        }

        /// Sets the value of [name][crate::model::UpdateVehicleAttributesRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }

        /// Sets the value of [attributes][crate::model::UpdateVehicleAttributesRequest::attributes].
        ///
        /// This is a **required** field for requests.
        pub fn set_attributes<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<crate::model::VehicleAttribute>,
        {
            use std::iter::Iterator;
            self.0.request.attributes = v.into_iter().map(|i| i.into()).collect();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateVehicleAttributes {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [VehicleService::list_vehicles][crate::client::VehicleService::list_vehicles] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_fleetengine_v1::builder::vehicle_service::ListVehicles;
    /// # async fn sample() -> google_maps_fleetengine_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListVehicles {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListVehicles(RequestBuilder<crate::model::ListVehiclesRequest>);

    impl ListVehicles {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VehicleService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ListVehiclesRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ListVehiclesResponse> {
            (*self.0.stub)
                .list_vehicles(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::ListVehiclesResponse, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ListVehiclesResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [header][crate::model::ListVehiclesRequest::header].
        pub fn set_header<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RequestHeader>,
        {
            self.0.request.header = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [header][crate::model::ListVehiclesRequest::header].
        pub fn set_or_clear_header<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RequestHeader>,
        {
            self.0.request.header = v.map(|x| x.into());
            self
        }

        /// Sets the value of [parent][crate::model::ListVehiclesRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [page_size][crate::model::ListVehiclesRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::ListVehiclesRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }

        /// Sets the value of [minimum_capacity][crate::model::ListVehiclesRequest::minimum_capacity].
        pub fn set_minimum_capacity<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::Int32Value>,
        {
            self.0.request.minimum_capacity = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [minimum_capacity][crate::model::ListVehiclesRequest::minimum_capacity].
        pub fn set_or_clear_minimum_capacity<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::Int32Value>,
        {
            self.0.request.minimum_capacity = v.map(|x| x.into());
            self
        }

        /// Sets the value of [trip_types][crate::model::ListVehiclesRequest::trip_types].
        pub fn set_trip_types<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<crate::model::TripType>,
        {
            use std::iter::Iterator;
            self.0.request.trip_types = v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [maximum_staleness][crate::model::ListVehiclesRequest::maximum_staleness].
        pub fn set_maximum_staleness<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::Duration>,
        {
            self.0.request.maximum_staleness = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [maximum_staleness][crate::model::ListVehiclesRequest::maximum_staleness].
        pub fn set_or_clear_maximum_staleness<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::Duration>,
        {
            self.0.request.maximum_staleness = v.map(|x| x.into());
            self
        }

        /// Sets the value of [vehicle_type_categories][crate::model::ListVehiclesRequest::vehicle_type_categories].
        ///
        /// This is a **required** field for requests.
        pub fn set_vehicle_type_categories<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<crate::model::vehicle::vehicle_type::Category>,
        {
            use std::iter::Iterator;
            self.0.request.vehicle_type_categories = v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [required_attributes][crate::model::ListVehiclesRequest::required_attributes].
        pub fn set_required_attributes<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<std::string::String>,
        {
            use std::iter::Iterator;
            self.0.request.required_attributes = v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [required_one_of_attributes][crate::model::ListVehiclesRequest::required_one_of_attributes].
        pub fn set_required_one_of_attributes<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<std::string::String>,
        {
            use std::iter::Iterator;
            self.0.request.required_one_of_attributes = v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [required_one_of_attribute_sets][crate::model::ListVehiclesRequest::required_one_of_attribute_sets].
        pub fn set_required_one_of_attribute_sets<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<std::string::String>,
        {
            use std::iter::Iterator;
            self.0.request.required_one_of_attribute_sets =
                v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [vehicle_state][crate::model::ListVehiclesRequest::vehicle_state].
        pub fn set_vehicle_state<T: Into<crate::model::VehicleState>>(mut self, v: T) -> Self {
            self.0.request.vehicle_state = v.into();
            self
        }

        /// Sets the value of [on_trip_only][crate::model::ListVehiclesRequest::on_trip_only].
        pub fn set_on_trip_only<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.on_trip_only = v.into();
            self
        }

        /// Sets the value of [filter][crate::model::ListVehiclesRequest::filter].
        pub fn set_filter<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.filter = v.into();
            self
        }

        /// Sets the value of [viewport][crate::model::ListVehiclesRequest::viewport].
        pub fn set_viewport<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<google_geo_type::model::Viewport>,
        {
            self.0.request.viewport = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [viewport][crate::model::ListVehiclesRequest::viewport].
        pub fn set_or_clear_viewport<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<google_geo_type::model::Viewport>,
        {
            self.0.request.viewport = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListVehicles {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [VehicleService::search_vehicles][crate::client::VehicleService::search_vehicles] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_fleetengine_v1::builder::vehicle_service::SearchVehicles;
    /// # async fn sample() -> google_maps_fleetengine_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SearchVehicles {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SearchVehicles(RequestBuilder<crate::model::SearchVehiclesRequest>);

    impl SearchVehicles {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VehicleService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::SearchVehiclesRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SearchVehiclesResponse> {
            (*self.0.stub)
                .search_vehicles(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [header][crate::model::SearchVehiclesRequest::header].
        pub fn set_header<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RequestHeader>,
        {
            self.0.request.header = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [header][crate::model::SearchVehiclesRequest::header].
        pub fn set_or_clear_header<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RequestHeader>,
        {
            self.0.request.header = v.map(|x| x.into());
            self
        }

        /// Sets the value of [parent][crate::model::SearchVehiclesRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [pickup_point][crate::model::SearchVehiclesRequest::pickup_point].
        ///
        /// This is a **required** field for requests.
        pub fn set_pickup_point<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TerminalLocation>,
        {
            self.0.request.pickup_point = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [pickup_point][crate::model::SearchVehiclesRequest::pickup_point].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_pickup_point<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TerminalLocation>,
        {
            self.0.request.pickup_point = v.map(|x| x.into());
            self
        }

        /// Sets the value of [dropoff_point][crate::model::SearchVehiclesRequest::dropoff_point].
        pub fn set_dropoff_point<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TerminalLocation>,
        {
            self.0.request.dropoff_point = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [dropoff_point][crate::model::SearchVehiclesRequest::dropoff_point].
        pub fn set_or_clear_dropoff_point<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TerminalLocation>,
        {
            self.0.request.dropoff_point = v.map(|x| x.into());
            self
        }

        /// Sets the value of [pickup_radius_meters][crate::model::SearchVehiclesRequest::pickup_radius_meters].
        ///
        /// This is a **required** field for requests.
        pub fn set_pickup_radius_meters<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.pickup_radius_meters = v.into();
            self
        }

        /// Sets the value of [count][crate::model::SearchVehiclesRequest::count].
        ///
        /// This is a **required** field for requests.
        pub fn set_count<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.count = v.into();
            self
        }

        /// Sets the value of [minimum_capacity][crate::model::SearchVehiclesRequest::minimum_capacity].
        ///
        /// This is a **required** field for requests.
        pub fn set_minimum_capacity<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.minimum_capacity = v.into();
            self
        }

        /// Sets the value of [trip_types][crate::model::SearchVehiclesRequest::trip_types].
        ///
        /// This is a **required** field for requests.
        pub fn set_trip_types<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<crate::model::TripType>,
        {
            use std::iter::Iterator;
            self.0.request.trip_types = v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [maximum_staleness][crate::model::SearchVehiclesRequest::maximum_staleness].
        pub fn set_maximum_staleness<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::Duration>,
        {
            self.0.request.maximum_staleness = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [maximum_staleness][crate::model::SearchVehiclesRequest::maximum_staleness].
        pub fn set_or_clear_maximum_staleness<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::Duration>,
        {
            self.0.request.maximum_staleness = v.map(|x| x.into());
            self
        }

        /// Sets the value of [vehicle_types][crate::model::SearchVehiclesRequest::vehicle_types].
        ///
        /// This is a **required** field for requests.
        pub fn set_vehicle_types<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<crate::model::vehicle::VehicleType>,
        {
            use std::iter::Iterator;
            self.0.request.vehicle_types = v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [required_attributes][crate::model::SearchVehiclesRequest::required_attributes].
        pub fn set_required_attributes<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<crate::model::VehicleAttribute>,
        {
            use std::iter::Iterator;
            self.0.request.required_attributes = v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [required_one_of_attributes][crate::model::SearchVehiclesRequest::required_one_of_attributes].
        pub fn set_required_one_of_attributes<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<crate::model::VehicleAttributeList>,
        {
            use std::iter::Iterator;
            self.0.request.required_one_of_attributes = v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [required_one_of_attribute_sets][crate::model::SearchVehiclesRequest::required_one_of_attribute_sets].
        pub fn set_required_one_of_attribute_sets<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<crate::model::VehicleAttributeList>,
        {
            use std::iter::Iterator;
            self.0.request.required_one_of_attribute_sets =
                v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [order_by][crate::model::SearchVehiclesRequest::order_by].
        ///
        /// This is a **required** field for requests.
        pub fn set_order_by<T: Into<crate::model::search_vehicles_request::VehicleMatchOrder>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.order_by = v.into();
            self
        }

        /// Sets the value of [include_back_to_back][crate::model::SearchVehiclesRequest::include_back_to_back].
        pub fn set_include_back_to_back<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.include_back_to_back = v.into();
            self
        }

        /// Sets the value of [trip_id][crate::model::SearchVehiclesRequest::trip_id].
        pub fn set_trip_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.trip_id = v.into();
            self
        }

        /// Sets the value of [current_trips_present][crate::model::SearchVehiclesRequest::current_trips_present].
        pub fn set_current_trips_present<
            T: Into<crate::model::search_vehicles_request::CurrentTripsPresent>,
        >(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.current_trips_present = v.into();
            self
        }

        /// Sets the value of [filter][crate::model::SearchVehiclesRequest::filter].
        pub fn set_filter<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.filter = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SearchVehicles {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}
