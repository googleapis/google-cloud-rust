// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// Code generated by sidekick. DO NOT EDIT.
#![allow(rustdoc::bare_urls)]
#![allow(rustdoc::broken_intra_doc_links)]
#![allow(rustdoc::invalid_html_tags)]
#![allow(rustdoc::redundant_explicit_links)]

/// Implements a client for the Aerial View API.
///
/// # Example
/// ```
/// # use google_maps_aerialview_v1::client::AerialView;
/// async fn sample(
/// ) -> anyhow::Result<()> {
///     let client = AerialView::builder().build().await?;
///     let response = client.render_video()
///         /* set fields */
///         .send().await?;
///     println!("response {:?}", response);
///     Ok(())
/// }
/// ```
///
/// # Service Description
///
/// Service definition for the Aerial View API.
///
/// # Configuration
///
/// To configure `AerialView` use the `with_*` methods in the type returned
/// by [builder()][AerialView::builder]. The default configuration should
/// work for most applications. Common configuration changes include
///
/// * [with_endpoint()]: by default this client uses the global default endpoint
///   (`https://aerialview.googleapis.com`). Applications using regional
///   endpoints or running in restricted networks (e.g. a network configured
///   with [Private Google Access with VPC Service Controls]) may want to
///   override this default.
/// * [with_credentials()]: by default this client uses
///   [Application Default Credentials]. Applications using custom
///   authentication may need to override this default.
///
/// [with_endpoint()]: super::builder::aerial_view::ClientBuilder::with_endpoint
/// [with_credentials()]: super::builder::aerial_view::ClientBuilder::with_credentials
/// [Private Google Access with VPC Service Controls]: https://cloud.google.com/vpc-service-controls/docs/private-connectivity
/// [Application Default Credentials]: https://cloud.google.com/docs/authentication#adc
///
/// # Pooling and Cloning
///
/// `AerialView` holds a connection pool internally, it is advised to
/// create one and reuse it. You do not need to wrap `AerialView` in
/// an [Rc](std::rc::Rc) or [Arc](std::sync::Arc) to reuse it, because it
/// already uses an `Arc` internally.
#[derive(Clone, Debug)]
pub struct AerialView {
    inner: std::sync::Arc<dyn super::stub::dynamic::AerialView>,
}

impl AerialView {
    /// Returns a builder for [AerialView].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_maps_aerialview_v1::client::AerialView;
    /// let client = AerialView::builder().build().await?;
    /// # Ok(()) }
    /// ```
    pub fn builder() -> super::builder::aerial_view::ClientBuilder {
        crate::new_client_builder(super::builder::aerial_view::client::Factory)
    }

    /// Creates a new client from the provided stub.
    ///
    /// The most common case for calling this function is in tests mocking the
    /// client's behavior.
    pub fn from_stub<T>(stub: impl Into<std::sync::Arc<T>>) -> Self
    where
        T: super::stub::AerialView + 'static,
    {
        Self { inner: stub.into() }
    }

    pub(crate) async fn new(
        config: gaxi::options::ClientConfig,
    ) -> crate::ClientBuilderResult<Self> {
        let inner = Self::build_inner(config).await?;
        Ok(Self { inner })
    }

    async fn build_inner(
        conf: gaxi::options::ClientConfig,
    ) -> crate::ClientBuilderResult<std::sync::Arc<dyn super::stub::dynamic::AerialView>> {
        if gaxi::options::tracing_enabled(&conf) {
            return Ok(std::sync::Arc::new(Self::build_with_tracing(conf).await?));
        }
        Ok(std::sync::Arc::new(Self::build_transport(conf).await?))
    }

    async fn build_transport(
        conf: gaxi::options::ClientConfig,
    ) -> crate::ClientBuilderResult<impl super::stub::AerialView> {
        super::transport::AerialView::new(conf).await
    }

    async fn build_with_tracing(
        conf: gaxi::options::ClientConfig,
    ) -> crate::ClientBuilderResult<impl super::stub::AerialView> {
        Self::build_transport(conf)
            .await
            .map(super::tracing::AerialView::new)
    }

    /// Adds an address to the renderer's queue if a video hasn't already been
    /// rendered. Otherwise, returns metadata about the video.
    ///
    /// # Example
    /// ```
    /// # use google_maps_aerialview_v1::client::AerialView;
    /// use google_maps_aerialview_v1::Result;
    /// async fn sample(
    ///    client: &AerialView
    /// ) -> Result<()> {
    ///     let response = client.render_video()
    ///         /* set fields */
    ///         .send().await?;
    ///     println!("response {:?}", response);
    ///     Ok(())
    /// }
    /// ```
    pub fn render_video(&self) -> super::builder::aerial_view::RenderVideo {
        super::builder::aerial_view::RenderVideo::new(self.inner.clone())
    }

    /// Fetches a video given its address or videoId. The response will either be
    /// a video with a set of playback URIs for ACTIVE videos, a PROCESSING state
    /// for pending videos, or a 404 error if the video does not exist. Receiving a
    /// video is a billable event, so callers of this method should be ready to use
    /// the returned URIs at the time of request.
    ///
    /// # Example
    /// ```
    /// # use google_maps_aerialview_v1::client::AerialView;
    /// use google_maps_aerialview_v1::Result;
    /// async fn sample(
    ///    client: &AerialView
    /// ) -> Result<()> {
    ///     let response = client.lookup_video()
    ///         /* set fields */
    ///         .send().await?;
    ///     println!("response {:?}", response);
    ///     Ok(())
    /// }
    /// ```
    pub fn lookup_video(&self) -> super::builder::aerial_view::LookupVideo {
        super::builder::aerial_view::LookupVideo::new(self.inner.clone())
    }
}
