// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// Code generated by sidekick. DO NOT EDIT.

/// Request and client builders for [Weather][crate::client::Weather].
pub mod weather {
    use crate::Result;

    /// A builder for [Weather][crate::client::Weather].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_maps_weather_v1::*;
    /// # use builder::weather::ClientBuilder;
    /// # use client::Weather;
    /// let builder : ClientBuilder = Weather::builder();
    /// let client = builder
    ///     .with_endpoint("https://weather.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::Weather;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = Weather;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::Weather] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::Weather>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Weather>) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [Weather::lookup_current_conditions][crate::client::Weather::lookup_current_conditions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_weather_v1::builder::weather::LookupCurrentConditions;
    /// # async fn sample() -> google_maps_weather_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> LookupCurrentConditions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct LookupCurrentConditions(
        RequestBuilder<crate::model::LookupCurrentConditionsRequest>,
    );

    impl LookupCurrentConditions {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Weather>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::LookupCurrentConditionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::LookupCurrentConditionsResponse> {
            (*self.0.stub)
                .lookup_current_conditions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [location][crate::model::LookupCurrentConditionsRequest::location].
        ///
        /// This is a **required** field for requests.
        pub fn set_location<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<google_cloud_type::model::LatLng>,
        {
            self.0.request.location = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [location][crate::model::LookupCurrentConditionsRequest::location].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_location<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<google_cloud_type::model::LatLng>,
        {
            self.0.request.location = v.map(|x| x.into());
            self
        }

        /// Sets the value of [units_system][crate::model::LookupCurrentConditionsRequest::units_system].
        pub fn set_units_system<T: Into<crate::model::UnitsSystem>>(mut self, v: T) -> Self {
            self.0.request.units_system = v.into();
            self
        }

        /// Sets the value of [language_code][crate::model::LookupCurrentConditionsRequest::language_code].
        pub fn set_language_code<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.language_code = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [language_code][crate::model::LookupCurrentConditionsRequest::language_code].
        pub fn set_or_clear_language_code<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.language_code = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for LookupCurrentConditions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Weather::lookup_forecast_hours][crate::client::Weather::lookup_forecast_hours] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_weather_v1::builder::weather::LookupForecastHours;
    /// # async fn sample() -> google_maps_weather_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> LookupForecastHours {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct LookupForecastHours(RequestBuilder<crate::model::LookupForecastHoursRequest>);

    impl LookupForecastHours {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Weather>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::LookupForecastHoursRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::LookupForecastHoursResponse> {
            (*self.0.stub)
                .lookup_forecast_hours(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::LookupForecastHoursResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::LookupForecastHoursResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [location][crate::model::LookupForecastHoursRequest::location].
        ///
        /// This is a **required** field for requests.
        pub fn set_location<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<google_cloud_type::model::LatLng>,
        {
            self.0.request.location = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [location][crate::model::LookupForecastHoursRequest::location].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_location<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<google_cloud_type::model::LatLng>,
        {
            self.0.request.location = v.map(|x| x.into());
            self
        }

        /// Sets the value of [hours][crate::model::LookupForecastHoursRequest::hours].
        pub fn set_hours<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.hours = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [hours][crate::model::LookupForecastHoursRequest::hours].
        pub fn set_or_clear_hours<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.hours = v.map(|x| x.into());
            self
        }

        /// Sets the value of [units_system][crate::model::LookupForecastHoursRequest::units_system].
        pub fn set_units_system<T: Into<crate::model::UnitsSystem>>(mut self, v: T) -> Self {
            self.0.request.units_system = v.into();
            self
        }

        /// Sets the value of [language_code][crate::model::LookupForecastHoursRequest::language_code].
        pub fn set_language_code<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.language_code = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [language_code][crate::model::LookupForecastHoursRequest::language_code].
        pub fn set_or_clear_language_code<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.language_code = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_size][crate::model::LookupForecastHoursRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::LookupForecastHoursRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for LookupForecastHours {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Weather::lookup_forecast_days][crate::client::Weather::lookup_forecast_days] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_weather_v1::builder::weather::LookupForecastDays;
    /// # async fn sample() -> google_maps_weather_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> LookupForecastDays {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct LookupForecastDays(RequestBuilder<crate::model::LookupForecastDaysRequest>);

    impl LookupForecastDays {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Weather>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::LookupForecastDaysRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::LookupForecastDaysResponse> {
            (*self.0.stub)
                .lookup_forecast_days(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::LookupForecastDaysResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::LookupForecastDaysResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [location][crate::model::LookupForecastDaysRequest::location].
        ///
        /// This is a **required** field for requests.
        pub fn set_location<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<google_cloud_type::model::LatLng>,
        {
            self.0.request.location = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [location][crate::model::LookupForecastDaysRequest::location].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_location<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<google_cloud_type::model::LatLng>,
        {
            self.0.request.location = v.map(|x| x.into());
            self
        }

        /// Sets the value of [days][crate::model::LookupForecastDaysRequest::days].
        pub fn set_days<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.days = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [days][crate::model::LookupForecastDaysRequest::days].
        pub fn set_or_clear_days<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.days = v.map(|x| x.into());
            self
        }

        /// Sets the value of [units_system][crate::model::LookupForecastDaysRequest::units_system].
        pub fn set_units_system<T: Into<crate::model::UnitsSystem>>(mut self, v: T) -> Self {
            self.0.request.units_system = v.into();
            self
        }

        /// Sets the value of [language_code][crate::model::LookupForecastDaysRequest::language_code].
        pub fn set_language_code<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.language_code = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [language_code][crate::model::LookupForecastDaysRequest::language_code].
        pub fn set_or_clear_language_code<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.language_code = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_size][crate::model::LookupForecastDaysRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::LookupForecastDaysRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for LookupForecastDays {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Weather::lookup_history_hours][crate::client::Weather::lookup_history_hours] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_weather_v1::builder::weather::LookupHistoryHours;
    /// # async fn sample() -> google_maps_weather_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> LookupHistoryHours {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct LookupHistoryHours(RequestBuilder<crate::model::LookupHistoryHoursRequest>);

    impl LookupHistoryHours {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Weather>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::LookupHistoryHoursRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::LookupHistoryHoursResponse> {
            (*self.0.stub)
                .lookup_history_hours(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::LookupHistoryHoursResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::LookupHistoryHoursResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [location][crate::model::LookupHistoryHoursRequest::location].
        ///
        /// This is a **required** field for requests.
        pub fn set_location<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<google_cloud_type::model::LatLng>,
        {
            self.0.request.location = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [location][crate::model::LookupHistoryHoursRequest::location].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_location<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<google_cloud_type::model::LatLng>,
        {
            self.0.request.location = v.map(|x| x.into());
            self
        }

        /// Sets the value of [hours][crate::model::LookupHistoryHoursRequest::hours].
        pub fn set_hours<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.hours = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [hours][crate::model::LookupHistoryHoursRequest::hours].
        pub fn set_or_clear_hours<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.hours = v.map(|x| x.into());
            self
        }

        /// Sets the value of [units_system][crate::model::LookupHistoryHoursRequest::units_system].
        pub fn set_units_system<T: Into<crate::model::UnitsSystem>>(mut self, v: T) -> Self {
            self.0.request.units_system = v.into();
            self
        }

        /// Sets the value of [language_code][crate::model::LookupHistoryHoursRequest::language_code].
        pub fn set_language_code<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.language_code = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [language_code][crate::model::LookupHistoryHoursRequest::language_code].
        pub fn set_or_clear_language_code<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.language_code = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_size][crate::model::LookupHistoryHoursRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::LookupHistoryHoursRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for LookupHistoryHours {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Weather::lookup_public_alerts][crate::client::Weather::lookup_public_alerts] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_weather_v1::builder::weather::LookupPublicAlerts;
    /// # async fn sample() -> google_maps_weather_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> LookupPublicAlerts {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct LookupPublicAlerts(RequestBuilder<crate::model::LookupPublicAlertsRequest>);

    impl LookupPublicAlerts {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Weather>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::LookupPublicAlertsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::LookupPublicAlertsResponse> {
            (*self.0.stub)
                .lookup_public_alerts(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::LookupPublicAlertsResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::LookupPublicAlertsResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [location][crate::model::LookupPublicAlertsRequest::location].
        ///
        /// This is a **required** field for requests.
        pub fn set_location<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<google_cloud_type::model::LatLng>,
        {
            self.0.request.location = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [location][crate::model::LookupPublicAlertsRequest::location].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_location<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<google_cloud_type::model::LatLng>,
        {
            self.0.request.location = v.map(|x| x.into());
            self
        }

        /// Sets the value of [language_code][crate::model::LookupPublicAlertsRequest::language_code].
        pub fn set_language_code<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.language_code = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [language_code][crate::model::LookupPublicAlertsRequest::language_code].
        pub fn set_or_clear_language_code<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.language_code = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_size][crate::model::LookupPublicAlertsRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::LookupPublicAlertsRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for LookupPublicAlerts {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Weather::lookup_forecast_minutes][crate::client::Weather::lookup_forecast_minutes] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_weather_v1::builder::weather::LookupForecastMinutes;
    /// # async fn sample() -> google_maps_weather_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> LookupForecastMinutes {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct LookupForecastMinutes(RequestBuilder<crate::model::LookupForecastMinutesRequest>);

    impl LookupForecastMinutes {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Weather>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::LookupForecastMinutesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::LookupForecastMinutesResponse> {
            (*self.0.stub)
                .lookup_forecast_minutes(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::LookupForecastMinutesResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::LookupForecastMinutesResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [location][crate::model::LookupForecastMinutesRequest::location].
        ///
        /// This is a **required** field for requests.
        pub fn set_location<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<google_cloud_type::model::LatLng>,
        {
            self.0.request.location = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [location][crate::model::LookupForecastMinutesRequest::location].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_location<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<google_cloud_type::model::LatLng>,
        {
            self.0.request.location = v.map(|x| x.into());
            self
        }

        /// Sets the value of [units_system][crate::model::LookupForecastMinutesRequest::units_system].
        pub fn set_units_system<T: Into<crate::model::UnitsSystem>>(mut self, v: T) -> Self {
            self.0.request.units_system = v.into();
            self
        }

        /// Sets the value of [page_size][crate::model::LookupForecastMinutesRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::LookupForecastMinutesRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for LookupForecastMinutes {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}
