// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// Code generated by sidekick. DO NOT EDIT.

/// Request and client builders for [DestinationService][crate::client::DestinationService].
pub mod destination_service {
    use crate::Result;

    /// A builder for [DestinationService][crate::client::DestinationService].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_maps_geocode_v4::*;
    /// # use builder::destination_service::ClientBuilder;
    /// # use client::DestinationService;
    /// let builder : ClientBuilder = DestinationService::builder();
    /// let client = builder
    ///     .with_endpoint("https://geocoding-backend.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::DestinationService;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = DestinationService;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::DestinationService] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::DestinationService>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DestinationService>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [DestinationService::search_destinations][crate::client::DestinationService::search_destinations] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_geocode_v4::builder::destination_service::SearchDestinations;
    /// # async fn sample() -> google_maps_geocode_v4::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SearchDestinations {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SearchDestinations(RequestBuilder<crate::model::SearchDestinationsRequest>);

    impl SearchDestinations {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DestinationService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::SearchDestinationsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SearchDestinationsResponse> {
            (*self.0.stub)
                .search_destinations(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [travel_modes][crate::model::SearchDestinationsRequest::travel_modes].
        pub fn set_travel_modes<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<crate::model::navigation_point::TravelMode>,
        {
            use std::iter::Iterator;
            self.0.request.travel_modes = v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [language_code][crate::model::SearchDestinationsRequest::language_code].
        pub fn set_language_code<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.language_code = v.into();
            self
        }

        /// Sets the value of [region_code][crate::model::SearchDestinationsRequest::region_code].
        pub fn set_region_code<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region_code = v.into();
            self
        }

        /// Sets the value of [primary_query][crate::model::SearchDestinationsRequest::primary_query].
        ///
        /// Note that all the setters affecting `primary_query` are
        /// mutually exclusive.
        pub fn set_primary_query<
            T: Into<Option<crate::model::search_destinations_request::PrimaryQuery>>,
        >(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.primary_query = v.into();
            self
        }

        /// Sets the value of [primary_query][crate::model::SearchDestinationsRequest::primary_query]
        /// to hold a `Place`.
        ///
        /// Note that all the setters affecting `primary_query` are
        /// mutually exclusive.
        pub fn set_place<T: std::convert::Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request = self.0.request.set_place(v);
            self
        }

        /// Sets the value of [primary_query][crate::model::SearchDestinationsRequest::primary_query]
        /// to hold a `AddressQuery`.
        ///
        /// Note that all the setters affecting `primary_query` are
        /// mutually exclusive.
        pub fn set_address_query<
            T: std::convert::Into<
                    std::boxed::Box<crate::model::search_destinations_request::AddressQuery>,
                >,
        >(
            mut self,
            v: T,
        ) -> Self {
            self.0.request = self.0.request.set_address_query(v);
            self
        }

        /// Sets the value of [primary_query][crate::model::SearchDestinationsRequest::primary_query]
        /// to hold a `LocationQuery`.
        ///
        /// Note that all the setters affecting `primary_query` are
        /// mutually exclusive.
        pub fn set_location_query<
            T: std::convert::Into<
                    std::boxed::Box<crate::model::search_destinations_request::LocationQuery>,
                >,
        >(
            mut self,
            v: T,
        ) -> Self {
            self.0.request = self.0.request.set_location_query(v);
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SearchDestinations {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [GeocodeService][crate::client::GeocodeService].
pub mod geocode_service {
    use crate::Result;

    /// A builder for [GeocodeService][crate::client::GeocodeService].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_maps_geocode_v4::*;
    /// # use builder::geocode_service::ClientBuilder;
    /// # use client::GeocodeService;
    /// let builder : ClientBuilder = GeocodeService::builder();
    /// let client = builder
    ///     .with_endpoint("https://geocoding-backend.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::GeocodeService;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = GeocodeService;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::GeocodeService] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::GeocodeService>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GeocodeService>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [GeocodeService::geocode_address][crate::client::GeocodeService::geocode_address] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_geocode_v4::builder::geocode_service::GeocodeAddress;
    /// # async fn sample() -> google_maps_geocode_v4::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GeocodeAddress {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GeocodeAddress(RequestBuilder<crate::model::GeocodeAddressRequest>);

    impl GeocodeAddress {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GeocodeService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GeocodeAddressRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::GeocodeAddressResponse> {
            (*self.0.stub)
                .geocode_address(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [location_bias][crate::model::GeocodeAddressRequest::location_bias].
        pub fn set_location_bias<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::geocode_address_request::LocationBias>,
        {
            self.0.request.location_bias = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [location_bias][crate::model::GeocodeAddressRequest::location_bias].
        pub fn set_or_clear_location_bias<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::geocode_address_request::LocationBias>,
        {
            self.0.request.location_bias = v.map(|x| x.into());
            self
        }

        /// Sets the value of [language_code][crate::model::GeocodeAddressRequest::language_code].
        pub fn set_language_code<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.language_code = v.into();
            self
        }

        /// Sets the value of [region_code][crate::model::GeocodeAddressRequest::region_code].
        pub fn set_region_code<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region_code = v.into();
            self
        }

        /// Sets the value of [address_input][crate::model::GeocodeAddressRequest::address_input].
        ///
        /// Note that all the setters affecting `address_input` are
        /// mutually exclusive.
        pub fn set_address_input<
            T: Into<Option<crate::model::geocode_address_request::AddressInput>>,
        >(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.address_input = v.into();
            self
        }

        /// Sets the value of [address_input][crate::model::GeocodeAddressRequest::address_input]
        /// to hold a `AddressQuery`.
        ///
        /// Note that all the setters affecting `address_input` are
        /// mutually exclusive.
        pub fn set_address_query<T: std::convert::Into<std::string::String>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request = self.0.request.set_address_query(v);
            self
        }

        /// Sets the value of [address_input][crate::model::GeocodeAddressRequest::address_input]
        /// to hold a `Address`.
        ///
        /// Note that all the setters affecting `address_input` are
        /// mutually exclusive.
        pub fn set_address<
            T: std::convert::Into<std::boxed::Box<google_cloud_type::model::PostalAddress>>,
        >(
            mut self,
            v: T,
        ) -> Self {
            self.0.request = self.0.request.set_address(v);
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GeocodeAddress {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GeocodeService::geocode_location][crate::client::GeocodeService::geocode_location] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_geocode_v4::builder::geocode_service::GeocodeLocation;
    /// # async fn sample() -> google_maps_geocode_v4::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GeocodeLocation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GeocodeLocation(RequestBuilder<crate::model::GeocodeLocationRequest>);

    impl GeocodeLocation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GeocodeService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GeocodeLocationRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::GeocodeLocationResponse> {
            (*self.0.stub)
                .geocode_location(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [language_code][crate::model::GeocodeLocationRequest::language_code].
        pub fn set_language_code<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.language_code = v.into();
            self
        }

        /// Sets the value of [region_code][crate::model::GeocodeLocationRequest::region_code].
        pub fn set_region_code<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region_code = v.into();
            self
        }

        /// Sets the value of [types][crate::model::GeocodeLocationRequest::types].
        pub fn set_types<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<std::string::String>,
        {
            use std::iter::Iterator;
            self.0.request.types = v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [granularity][crate::model::GeocodeLocationRequest::granularity].
        pub fn set_granularity<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<crate::model::geocode_result::Granularity>,
        {
            use std::iter::Iterator;
            self.0.request.granularity = v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [location_input][crate::model::GeocodeLocationRequest::location_input].
        ///
        /// Note that all the setters affecting `location_input` are
        /// mutually exclusive.
        pub fn set_location_input<
            T: Into<Option<crate::model::geocode_location_request::LocationInput>>,
        >(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.location_input = v.into();
            self
        }

        /// Sets the value of [location_input][crate::model::GeocodeLocationRequest::location_input]
        /// to hold a `LocationQuery`.
        ///
        /// Note that all the setters affecting `location_input` are
        /// mutually exclusive.
        pub fn set_location_query<T: std::convert::Into<std::string::String>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request = self.0.request.set_location_query(v);
            self
        }

        /// Sets the value of [location_input][crate::model::GeocodeLocationRequest::location_input]
        /// to hold a `Location`.
        ///
        /// Note that all the setters affecting `location_input` are
        /// mutually exclusive.
        pub fn set_location<
            T: std::convert::Into<std::boxed::Box<google_cloud_type::model::LatLng>>,
        >(
            mut self,
            v: T,
        ) -> Self {
            self.0.request = self.0.request.set_location(v);
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GeocodeLocation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GeocodeService::geocode_place][crate::client::GeocodeService::geocode_place] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_geocode_v4::builder::geocode_service::GeocodePlace;
    /// # async fn sample() -> google_maps_geocode_v4::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GeocodePlace {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GeocodePlace(RequestBuilder<crate::model::GeocodePlaceRequest>);

    impl GeocodePlace {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GeocodeService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GeocodePlaceRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::GeocodeResult> {
            (*self.0.stub)
                .geocode_place(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [place][crate::model::GeocodePlaceRequest::place].
        ///
        /// This is a **required** field for requests.
        pub fn set_place<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.place = v.into();
            self
        }

        /// Sets the value of [language_code][crate::model::GeocodePlaceRequest::language_code].
        pub fn set_language_code<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.language_code = v.into();
            self
        }

        /// Sets the value of [region_code][crate::model::GeocodePlaceRequest::region_code].
        pub fn set_region_code<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region_code = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GeocodePlace {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}
