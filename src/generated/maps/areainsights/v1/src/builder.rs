// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// Code generated by sidekick. DO NOT EDIT.

/// Request and client builders for [AreaInsights][crate::client::AreaInsights].
pub mod area_insights {
    use crate::Result;

    /// A builder for [AreaInsights][crate::client::AreaInsights].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_maps_areainsights_v1::*;
    /// # use builder::area_insights::ClientBuilder;
    /// # use client::AreaInsights;
    /// let builder : ClientBuilder = AreaInsights::builder();
    /// let client = builder
    ///     .with_endpoint("https://areainsights.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::AreaInsights;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = AreaInsights;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::AreaInsights] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::AreaInsights>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::AreaInsights>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [AreaInsights::compute_insights][crate::client::AreaInsights::compute_insights] calls.
    ///
    /// # Example
    /// ```
    /// # use google_maps_areainsights_v1::builder::area_insights::ComputeInsights;
    /// # async fn sample() -> google_maps_areainsights_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ComputeInsights {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ComputeInsights(RequestBuilder<crate::model::ComputeInsightsRequest>);

    impl ComputeInsights {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::AreaInsights>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ComputeInsightsRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ComputeInsightsResponse> {
            (*self.0.stub)
                .compute_insights(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [insights][crate::model::ComputeInsightsRequest::insights].
        ///
        /// This is a **required** field for requests.
        pub fn set_insights<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<crate::model::Insight>,
        {
            use std::iter::Iterator;
            self.0.request.insights = v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [filter][crate::model::ComputeInsightsRequest::filter].
        ///
        /// This is a **required** field for requests.
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Filter>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::ComputeInsightsRequest::filter].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Filter>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ComputeInsights {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}
