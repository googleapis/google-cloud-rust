// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// Code generated by sidekick. DO NOT EDIT.

/// Request and client builders for [ChatService][crate::client::ChatService].
pub mod chat_service {
    use crate::Result;

    /// A builder for [ChatService][crate::client::ChatService].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_chat_v1::*;
    /// # use builder::chat_service::ClientBuilder;
    /// # use client::ChatService;
    /// let builder : ClientBuilder = ChatService::builder();
    /// let client = builder
    ///     .with_endpoint("https://chat.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::ChatService;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = ChatService;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::ChatService] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [ChatService::create_message][crate::client::ChatService::create_message] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::CreateMessage;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CreateMessage {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CreateMessage(RequestBuilder<crate::model::CreateMessageRequest>);

    impl CreateMessage {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::CreateMessageRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Message> {
            (*self.0.stub)
                .create_message(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [parent][crate::model::CreateMessageRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [message][crate::model::CreateMessageRequest::message].
        ///
        /// This is a **required** field for requests.
        pub fn set_message<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Message>,
        {
            self.0.request.message = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [message][crate::model::CreateMessageRequest::message].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_message<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Message>,
        {
            self.0.request.message = v.map(|x| x.into());
            self
        }

        /// Sets the value of [thread_key][crate::model::CreateMessageRequest::thread_key].
        #[deprecated]
        pub fn set_thread_key<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.thread_key = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::CreateMessageRequest::request_id].
        pub fn set_request_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.request_id = v.into();
            self
        }

        /// Sets the value of [message_reply_option][crate::model::CreateMessageRequest::message_reply_option].
        pub fn set_message_reply_option<
            T: Into<crate::model::create_message_request::MessageReplyOption>,
        >(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.message_reply_option = v.into();
            self
        }

        /// Sets the value of [message_id][crate::model::CreateMessageRequest::message_id].
        pub fn set_message_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.message_id = v.into();
            self
        }

        /// Sets the value of [create_message_notification_options][crate::model::CreateMessageRequest::create_message_notification_options].
        pub fn set_create_message_notification_options<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::CreateMessageNotificationOptions>,
        {
            self.0.request.create_message_notification_options =
                std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [create_message_notification_options][crate::model::CreateMessageRequest::create_message_notification_options].
        pub fn set_or_clear_create_message_notification_options<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<crate::model::CreateMessageNotificationOptions>,
        {
            self.0.request.create_message_notification_options = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CreateMessage {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::list_messages][crate::client::ChatService::list_messages] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::ListMessages;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListMessages {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListMessages(RequestBuilder<crate::model::ListMessagesRequest>);

    impl ListMessages {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ListMessagesRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ListMessagesResponse> {
            (*self.0.stub)
                .list_messages(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::ListMessagesResponse, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ListMessagesResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [parent][crate::model::ListMessagesRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [page_size][crate::model::ListMessagesRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::ListMessagesRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }

        /// Sets the value of [filter][crate::model::ListMessagesRequest::filter].
        pub fn set_filter<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.filter = v.into();
            self
        }

        /// Sets the value of [order_by][crate::model::ListMessagesRequest::order_by].
        pub fn set_order_by<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.order_by = v.into();
            self
        }

        /// Sets the value of [show_deleted][crate::model::ListMessagesRequest::show_deleted].
        pub fn set_show_deleted<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.show_deleted = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListMessages {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::list_memberships][crate::client::ChatService::list_memberships] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::ListMemberships;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListMemberships {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListMemberships(RequestBuilder<crate::model::ListMembershipsRequest>);

    impl ListMemberships {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ListMembershipsRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ListMembershipsResponse> {
            (*self.0.stub)
                .list_memberships(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::ListMembershipsResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ListMembershipsResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [parent][crate::model::ListMembershipsRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [page_size][crate::model::ListMembershipsRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::ListMembershipsRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }

        /// Sets the value of [filter][crate::model::ListMembershipsRequest::filter].
        pub fn set_filter<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.filter = v.into();
            self
        }

        /// Sets the value of [show_groups][crate::model::ListMembershipsRequest::show_groups].
        pub fn set_show_groups<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.show_groups = v.into();
            self
        }

        /// Sets the value of [show_invited][crate::model::ListMembershipsRequest::show_invited].
        pub fn set_show_invited<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.show_invited = v.into();
            self
        }

        /// Sets the value of [use_admin_access][crate::model::ListMembershipsRequest::use_admin_access].
        pub fn set_use_admin_access<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.use_admin_access = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListMemberships {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::get_membership][crate::client::ChatService::get_membership] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::GetMembership;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetMembership {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetMembership(RequestBuilder<crate::model::GetMembershipRequest>);

    impl GetMembership {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetMembershipRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Membership> {
            (*self.0.stub)
                .get_membership(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::GetMembershipRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }

        /// Sets the value of [use_admin_access][crate::model::GetMembershipRequest::use_admin_access].
        pub fn set_use_admin_access<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.use_admin_access = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetMembership {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::get_message][crate::client::ChatService::get_message] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::GetMessage;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetMessage {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetMessage(RequestBuilder<crate::model::GetMessageRequest>);

    impl GetMessage {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetMessageRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Message> {
            (*self.0.stub)
                .get_message(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::GetMessageRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetMessage {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::update_message][crate::client::ChatService::update_message] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::UpdateMessage;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateMessage {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateMessage(RequestBuilder<crate::model::UpdateMessageRequest>);

    impl UpdateMessage {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::UpdateMessageRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Message> {
            (*self.0.stub)
                .update_message(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [message][crate::model::UpdateMessageRequest::message].
        ///
        /// This is a **required** field for requests.
        pub fn set_message<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Message>,
        {
            self.0.request.message = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [message][crate::model::UpdateMessageRequest::message].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_message<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Message>,
        {
            self.0.request.message = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::UpdateMessageRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::UpdateMessageRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [allow_missing][crate::model::UpdateMessageRequest::allow_missing].
        pub fn set_allow_missing<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.allow_missing = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateMessage {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::delete_message][crate::client::ChatService::delete_message] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::DeleteMessage;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeleteMessage {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeleteMessage(RequestBuilder<crate::model::DeleteMessageRequest>);

    impl DeleteMessage {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::DeleteMessageRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<()> {
            (*self.0.stub)
                .delete_message(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::DeleteMessageRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }

        /// Sets the value of [force][crate::model::DeleteMessageRequest::force].
        pub fn set_force<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.force = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeleteMessage {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::get_attachment][crate::client::ChatService::get_attachment] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::GetAttachment;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetAttachment {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetAttachment(RequestBuilder<crate::model::GetAttachmentRequest>);

    impl GetAttachment {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetAttachmentRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Attachment> {
            (*self.0.stub)
                .get_attachment(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::GetAttachmentRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetAttachment {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::upload_attachment][crate::client::ChatService::upload_attachment] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::UploadAttachment;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UploadAttachment {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UploadAttachment(RequestBuilder<crate::model::UploadAttachmentRequest>);

    impl UploadAttachment {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::UploadAttachmentRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::UploadAttachmentResponse> {
            (*self.0.stub)
                .upload_attachment(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [parent][crate::model::UploadAttachmentRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [filename][crate::model::UploadAttachmentRequest::filename].
        ///
        /// This is a **required** field for requests.
        pub fn set_filename<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.filename = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UploadAttachment {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::list_spaces][crate::client::ChatService::list_spaces] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::ListSpaces;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListSpaces {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListSpaces(RequestBuilder<crate::model::ListSpacesRequest>);

    impl ListSpaces {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ListSpacesRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ListSpacesResponse> {
            (*self.0.stub)
                .list_spaces(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::ListSpacesResponse, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ListSpacesResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [page_size][crate::model::ListSpacesRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::ListSpacesRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }

        /// Sets the value of [filter][crate::model::ListSpacesRequest::filter].
        pub fn set_filter<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.filter = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListSpaces {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::search_spaces][crate::client::ChatService::search_spaces] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::SearchSpaces;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SearchSpaces {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SearchSpaces(RequestBuilder<crate::model::SearchSpacesRequest>);

    impl SearchSpaces {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::SearchSpacesRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SearchSpacesResponse> {
            (*self.0.stub)
                .search_spaces(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::SearchSpacesResponse, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::SearchSpacesResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [use_admin_access][crate::model::SearchSpacesRequest::use_admin_access].
        pub fn set_use_admin_access<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.use_admin_access = v.into();
            self
        }

        /// Sets the value of [page_size][crate::model::SearchSpacesRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::SearchSpacesRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }

        /// Sets the value of [query][crate::model::SearchSpacesRequest::query].
        ///
        /// This is a **required** field for requests.
        pub fn set_query<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.query = v.into();
            self
        }

        /// Sets the value of [order_by][crate::model::SearchSpacesRequest::order_by].
        pub fn set_order_by<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.order_by = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SearchSpaces {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::get_space][crate::client::ChatService::get_space] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::GetSpace;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetSpace {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetSpace(RequestBuilder<crate::model::GetSpaceRequest>);

    impl GetSpace {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetSpaceRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Space> {
            (*self.0.stub)
                .get_space(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::GetSpaceRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }

        /// Sets the value of [use_admin_access][crate::model::GetSpaceRequest::use_admin_access].
        pub fn set_use_admin_access<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.use_admin_access = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetSpace {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::create_space][crate::client::ChatService::create_space] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::CreateSpace;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CreateSpace {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CreateSpace(RequestBuilder<crate::model::CreateSpaceRequest>);

    impl CreateSpace {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::CreateSpaceRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Space> {
            (*self.0.stub)
                .create_space(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [space][crate::model::CreateSpaceRequest::space].
        ///
        /// This is a **required** field for requests.
        pub fn set_space<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Space>,
        {
            self.0.request.space = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [space][crate::model::CreateSpaceRequest::space].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_space<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Space>,
        {
            self.0.request.space = v.map(|x| x.into());
            self
        }

        /// Sets the value of [request_id][crate::model::CreateSpaceRequest::request_id].
        pub fn set_request_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.request_id = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CreateSpace {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::set_up_space][crate::client::ChatService::set_up_space] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::SetUpSpace;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetUpSpace {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetUpSpace(RequestBuilder<crate::model::SetUpSpaceRequest>);

    impl SetUpSpace {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::SetUpSpaceRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Space> {
            (*self.0.stub)
                .set_up_space(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [space][crate::model::SetUpSpaceRequest::space].
        ///
        /// This is a **required** field for requests.
        pub fn set_space<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Space>,
        {
            self.0.request.space = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [space][crate::model::SetUpSpaceRequest::space].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_space<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Space>,
        {
            self.0.request.space = v.map(|x| x.into());
            self
        }

        /// Sets the value of [request_id][crate::model::SetUpSpaceRequest::request_id].
        pub fn set_request_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.request_id = v.into();
            self
        }

        /// Sets the value of [memberships][crate::model::SetUpSpaceRequest::memberships].
        pub fn set_memberships<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<crate::model::Membership>,
        {
            use std::iter::Iterator;
            self.0.request.memberships = v.into_iter().map(|i| i.into()).collect();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetUpSpace {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::update_space][crate::client::ChatService::update_space] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::UpdateSpace;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateSpace {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateSpace(RequestBuilder<crate::model::UpdateSpaceRequest>);

    impl UpdateSpace {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::UpdateSpaceRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Space> {
            (*self.0.stub)
                .update_space(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [space][crate::model::UpdateSpaceRequest::space].
        ///
        /// This is a **required** field for requests.
        pub fn set_space<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Space>,
        {
            self.0.request.space = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [space][crate::model::UpdateSpaceRequest::space].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_space<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Space>,
        {
            self.0.request.space = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::UpdateSpaceRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::UpdateSpaceRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [use_admin_access][crate::model::UpdateSpaceRequest::use_admin_access].
        pub fn set_use_admin_access<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.use_admin_access = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateSpace {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::delete_space][crate::client::ChatService::delete_space] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::DeleteSpace;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeleteSpace {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeleteSpace(RequestBuilder<crate::model::DeleteSpaceRequest>);

    impl DeleteSpace {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::DeleteSpaceRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<()> {
            (*self.0.stub)
                .delete_space(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::DeleteSpaceRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }

        /// Sets the value of [use_admin_access][crate::model::DeleteSpaceRequest::use_admin_access].
        pub fn set_use_admin_access<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.use_admin_access = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeleteSpace {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::complete_import_space][crate::client::ChatService::complete_import_space] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::CompleteImportSpace;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CompleteImportSpace {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CompleteImportSpace(RequestBuilder<crate::model::CompleteImportSpaceRequest>);

    impl CompleteImportSpace {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::CompleteImportSpaceRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::CompleteImportSpaceResponse> {
            (*self.0.stub)
                .complete_import_space(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::CompleteImportSpaceRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CompleteImportSpace {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::find_direct_message][crate::client::ChatService::find_direct_message] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::FindDirectMessage;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> FindDirectMessage {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct FindDirectMessage(RequestBuilder<crate::model::FindDirectMessageRequest>);

    impl FindDirectMessage {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::FindDirectMessageRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Space> {
            (*self.0.stub)
                .find_direct_message(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::FindDirectMessageRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for FindDirectMessage {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::find_group_chats][crate::client::ChatService::find_group_chats] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::FindGroupChats;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> FindGroupChats {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct FindGroupChats(RequestBuilder<crate::model::FindGroupChatsRequest>);

    impl FindGroupChats {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::FindGroupChatsRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::FindGroupChatsResponse> {
            (*self.0.stub)
                .find_group_chats(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::FindGroupChatsResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::FindGroupChatsResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [users][crate::model::FindGroupChatsRequest::users].
        pub fn set_users<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<std::string::String>,
        {
            use std::iter::Iterator;
            self.0.request.users = v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [page_size][crate::model::FindGroupChatsRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::FindGroupChatsRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }

        /// Sets the value of [space_view][crate::model::FindGroupChatsRequest::space_view].
        pub fn set_space_view<T: Into<crate::model::SpaceView>>(mut self, v: T) -> Self {
            self.0.request.space_view = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for FindGroupChats {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::create_membership][crate::client::ChatService::create_membership] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::CreateMembership;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CreateMembership {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CreateMembership(RequestBuilder<crate::model::CreateMembershipRequest>);

    impl CreateMembership {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::CreateMembershipRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Membership> {
            (*self.0.stub)
                .create_membership(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [parent][crate::model::CreateMembershipRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [membership][crate::model::CreateMembershipRequest::membership].
        ///
        /// This is a **required** field for requests.
        pub fn set_membership<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Membership>,
        {
            self.0.request.membership = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [membership][crate::model::CreateMembershipRequest::membership].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_membership<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Membership>,
        {
            self.0.request.membership = v.map(|x| x.into());
            self
        }

        /// Sets the value of [use_admin_access][crate::model::CreateMembershipRequest::use_admin_access].
        pub fn set_use_admin_access<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.use_admin_access = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CreateMembership {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::update_membership][crate::client::ChatService::update_membership] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::UpdateMembership;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateMembership {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateMembership(RequestBuilder<crate::model::UpdateMembershipRequest>);

    impl UpdateMembership {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::UpdateMembershipRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Membership> {
            (*self.0.stub)
                .update_membership(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [membership][crate::model::UpdateMembershipRequest::membership].
        ///
        /// This is a **required** field for requests.
        pub fn set_membership<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Membership>,
        {
            self.0.request.membership = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [membership][crate::model::UpdateMembershipRequest::membership].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_membership<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Membership>,
        {
            self.0.request.membership = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::UpdateMembershipRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::UpdateMembershipRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [use_admin_access][crate::model::UpdateMembershipRequest::use_admin_access].
        pub fn set_use_admin_access<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.use_admin_access = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateMembership {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::delete_membership][crate::client::ChatService::delete_membership] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::DeleteMembership;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeleteMembership {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeleteMembership(RequestBuilder<crate::model::DeleteMembershipRequest>);

    impl DeleteMembership {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::DeleteMembershipRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Membership> {
            (*self.0.stub)
                .delete_membership(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::DeleteMembershipRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }

        /// Sets the value of [use_admin_access][crate::model::DeleteMembershipRequest::use_admin_access].
        pub fn set_use_admin_access<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.use_admin_access = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeleteMembership {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::create_reaction][crate::client::ChatService::create_reaction] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::CreateReaction;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CreateReaction {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CreateReaction(RequestBuilder<crate::model::CreateReactionRequest>);

    impl CreateReaction {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::CreateReactionRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Reaction> {
            (*self.0.stub)
                .create_reaction(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [parent][crate::model::CreateReactionRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [reaction][crate::model::CreateReactionRequest::reaction].
        ///
        /// This is a **required** field for requests.
        pub fn set_reaction<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Reaction>,
        {
            self.0.request.reaction = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [reaction][crate::model::CreateReactionRequest::reaction].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_reaction<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Reaction>,
        {
            self.0.request.reaction = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CreateReaction {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::list_reactions][crate::client::ChatService::list_reactions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::ListReactions;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListReactions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListReactions(RequestBuilder<crate::model::ListReactionsRequest>);

    impl ListReactions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ListReactionsRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ListReactionsResponse> {
            (*self.0.stub)
                .list_reactions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::ListReactionsResponse, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ListReactionsResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [parent][crate::model::ListReactionsRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [page_size][crate::model::ListReactionsRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::ListReactionsRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }

        /// Sets the value of [filter][crate::model::ListReactionsRequest::filter].
        pub fn set_filter<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.filter = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListReactions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::delete_reaction][crate::client::ChatService::delete_reaction] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::DeleteReaction;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeleteReaction {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeleteReaction(RequestBuilder<crate::model::DeleteReactionRequest>);

    impl DeleteReaction {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::DeleteReactionRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<()> {
            (*self.0.stub)
                .delete_reaction(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::DeleteReactionRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeleteReaction {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::create_custom_emoji][crate::client::ChatService::create_custom_emoji] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::CreateCustomEmoji;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CreateCustomEmoji {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CreateCustomEmoji(RequestBuilder<crate::model::CreateCustomEmojiRequest>);

    impl CreateCustomEmoji {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::CreateCustomEmojiRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::CustomEmoji> {
            (*self.0.stub)
                .create_custom_emoji(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [custom_emoji][crate::model::CreateCustomEmojiRequest::custom_emoji].
        ///
        /// This is a **required** field for requests.
        pub fn set_custom_emoji<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::CustomEmoji>,
        {
            self.0.request.custom_emoji = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [custom_emoji][crate::model::CreateCustomEmojiRequest::custom_emoji].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_custom_emoji<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::CustomEmoji>,
        {
            self.0.request.custom_emoji = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CreateCustomEmoji {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::get_custom_emoji][crate::client::ChatService::get_custom_emoji] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::GetCustomEmoji;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetCustomEmoji {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetCustomEmoji(RequestBuilder<crate::model::GetCustomEmojiRequest>);

    impl GetCustomEmoji {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetCustomEmojiRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::CustomEmoji> {
            (*self.0.stub)
                .get_custom_emoji(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::GetCustomEmojiRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetCustomEmoji {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::list_custom_emojis][crate::client::ChatService::list_custom_emojis] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::ListCustomEmojis;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListCustomEmojis {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListCustomEmojis(RequestBuilder<crate::model::ListCustomEmojisRequest>);

    impl ListCustomEmojis {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ListCustomEmojisRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ListCustomEmojisResponse> {
            (*self.0.stub)
                .list_custom_emojis(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::ListCustomEmojisResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ListCustomEmojisResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [page_size][crate::model::ListCustomEmojisRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::ListCustomEmojisRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }

        /// Sets the value of [filter][crate::model::ListCustomEmojisRequest::filter].
        pub fn set_filter<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.filter = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListCustomEmojis {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::delete_custom_emoji][crate::client::ChatService::delete_custom_emoji] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::DeleteCustomEmoji;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeleteCustomEmoji {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeleteCustomEmoji(RequestBuilder<crate::model::DeleteCustomEmojiRequest>);

    impl DeleteCustomEmoji {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::DeleteCustomEmojiRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<()> {
            (*self.0.stub)
                .delete_custom_emoji(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::DeleteCustomEmojiRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeleteCustomEmoji {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::get_space_read_state][crate::client::ChatService::get_space_read_state] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::GetSpaceReadState;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetSpaceReadState {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetSpaceReadState(RequestBuilder<crate::model::GetSpaceReadStateRequest>);

    impl GetSpaceReadState {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetSpaceReadStateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SpaceReadState> {
            (*self.0.stub)
                .get_space_read_state(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::GetSpaceReadStateRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetSpaceReadState {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::update_space_read_state][crate::client::ChatService::update_space_read_state] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::UpdateSpaceReadState;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateSpaceReadState {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateSpaceReadState(RequestBuilder<crate::model::UpdateSpaceReadStateRequest>);

    impl UpdateSpaceReadState {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::UpdateSpaceReadStateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SpaceReadState> {
            (*self.0.stub)
                .update_space_read_state(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [space_read_state][crate::model::UpdateSpaceReadStateRequest::space_read_state].
        ///
        /// This is a **required** field for requests.
        pub fn set_space_read_state<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SpaceReadState>,
        {
            self.0.request.space_read_state = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [space_read_state][crate::model::UpdateSpaceReadStateRequest::space_read_state].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_space_read_state<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SpaceReadState>,
        {
            self.0.request.space_read_state = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::UpdateSpaceReadStateRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::UpdateSpaceReadStateRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateSpaceReadState {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::get_thread_read_state][crate::client::ChatService::get_thread_read_state] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::GetThreadReadState;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetThreadReadState {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetThreadReadState(RequestBuilder<crate::model::GetThreadReadStateRequest>);

    impl GetThreadReadState {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetThreadReadStateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ThreadReadState> {
            (*self.0.stub)
                .get_thread_read_state(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::GetThreadReadStateRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetThreadReadState {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::get_availability][crate::client::ChatService::get_availability] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::GetAvailability;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetAvailability {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetAvailability(RequestBuilder<crate::model::GetAvailabilityRequest>);

    impl GetAvailability {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetAvailabilityRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Availability> {
            (*self.0.stub)
                .get_availability(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::GetAvailabilityRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetAvailability {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::mark_as_active][crate::client::ChatService::mark_as_active] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::MarkAsActive;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> MarkAsActive {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct MarkAsActive(RequestBuilder<crate::model::MarkAsActiveRequest>);

    impl MarkAsActive {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::MarkAsActiveRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Availability> {
            (*self.0.stub)
                .mark_as_active(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::MarkAsActiveRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }

        /// Sets the value of [expiration][crate::model::MarkAsActiveRequest::expiration].
        ///
        /// Note that all the setters affecting `expiration` are
        /// mutually exclusive.
        pub fn set_expiration<T: Into<Option<crate::model::mark_as_active_request::Expiration>>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.expiration = v.into();
            self
        }

        /// Sets the value of [expiration][crate::model::MarkAsActiveRequest::expiration]
        /// to hold a `ExpireTime`.
        ///
        /// Note that all the setters affecting `expiration` are
        /// mutually exclusive.
        pub fn set_expire_time<T: std::convert::Into<std::boxed::Box<wkt::Timestamp>>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request = self.0.request.set_expire_time(v);
            self
        }

        /// Sets the value of [expiration][crate::model::MarkAsActiveRequest::expiration]
        /// to hold a `Ttl`.
        ///
        /// Note that all the setters affecting `expiration` are
        /// mutually exclusive.
        pub fn set_ttl<T: std::convert::Into<std::boxed::Box<wkt::Duration>>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request = self.0.request.set_ttl(v);
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for MarkAsActive {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::mark_as_away][crate::client::ChatService::mark_as_away] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::MarkAsAway;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> MarkAsAway {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct MarkAsAway(RequestBuilder<crate::model::MarkAsAwayRequest>);

    impl MarkAsAway {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::MarkAsAwayRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Availability> {
            (*self.0.stub)
                .mark_as_away(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::MarkAsAwayRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for MarkAsAway {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::mark_as_do_not_disturb][crate::client::ChatService::mark_as_do_not_disturb] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::MarkAsDoNotDisturb;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> MarkAsDoNotDisturb {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct MarkAsDoNotDisturb(RequestBuilder<crate::model::MarkAsDoNotDisturbRequest>);

    impl MarkAsDoNotDisturb {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::MarkAsDoNotDisturbRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Availability> {
            (*self.0.stub)
                .mark_as_do_not_disturb(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::MarkAsDoNotDisturbRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }

        /// Sets the value of [expiration][crate::model::MarkAsDoNotDisturbRequest::expiration].
        ///
        /// Note that all the setters affecting `expiration` are
        /// mutually exclusive.
        pub fn set_expiration<
            T: Into<Option<crate::model::mark_as_do_not_disturb_request::Expiration>>,
        >(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.expiration = v.into();
            self
        }

        /// Sets the value of [expiration][crate::model::MarkAsDoNotDisturbRequest::expiration]
        /// to hold a `ExpireTime`.
        ///
        /// Note that all the setters affecting `expiration` are
        /// mutually exclusive.
        pub fn set_expire_time<T: std::convert::Into<std::boxed::Box<wkt::Timestamp>>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request = self.0.request.set_expire_time(v);
            self
        }

        /// Sets the value of [expiration][crate::model::MarkAsDoNotDisturbRequest::expiration]
        /// to hold a `Ttl`.
        ///
        /// Note that all the setters affecting `expiration` are
        /// mutually exclusive.
        pub fn set_ttl<T: std::convert::Into<std::boxed::Box<wkt::Duration>>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request = self.0.request.set_ttl(v);
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for MarkAsDoNotDisturb {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::update_availability][crate::client::ChatService::update_availability] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::UpdateAvailability;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateAvailability {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateAvailability(RequestBuilder<crate::model::UpdateAvailabilityRequest>);

    impl UpdateAvailability {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::UpdateAvailabilityRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Availability> {
            (*self.0.stub)
                .update_availability(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [availability][crate::model::UpdateAvailabilityRequest::availability].
        ///
        /// This is a **required** field for requests.
        pub fn set_availability<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Availability>,
        {
            self.0.request.availability = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [availability][crate::model::UpdateAvailabilityRequest::availability].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_availability<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Availability>,
        {
            self.0.request.availability = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::UpdateAvailabilityRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::UpdateAvailabilityRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateAvailability {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::get_space_event][crate::client::ChatService::get_space_event] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::GetSpaceEvent;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetSpaceEvent {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetSpaceEvent(RequestBuilder<crate::model::GetSpaceEventRequest>);

    impl GetSpaceEvent {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetSpaceEventRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SpaceEvent> {
            (*self.0.stub)
                .get_space_event(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::GetSpaceEventRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetSpaceEvent {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::list_space_events][crate::client::ChatService::list_space_events] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::ListSpaceEvents;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListSpaceEvents {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListSpaceEvents(RequestBuilder<crate::model::ListSpaceEventsRequest>);

    impl ListSpaceEvents {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ListSpaceEventsRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ListSpaceEventsResponse> {
            (*self.0.stub)
                .list_space_events(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::ListSpaceEventsResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ListSpaceEventsResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [parent][crate::model::ListSpaceEventsRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [page_size][crate::model::ListSpaceEventsRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::ListSpaceEventsRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }

        /// Sets the value of [filter][crate::model::ListSpaceEventsRequest::filter].
        ///
        /// This is a **required** field for requests.
        pub fn set_filter<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.filter = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListSpaceEvents {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::get_space_notification_setting][crate::client::ChatService::get_space_notification_setting] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::GetSpaceNotificationSetting;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetSpaceNotificationSetting {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetSpaceNotificationSetting(
        RequestBuilder<crate::model::GetSpaceNotificationSettingRequest>,
    );

    impl GetSpaceNotificationSetting {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetSpaceNotificationSettingRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SpaceNotificationSetting> {
            (*self.0.stub)
                .get_space_notification_setting(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::GetSpaceNotificationSettingRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetSpaceNotificationSetting {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::update_space_notification_setting][crate::client::ChatService::update_space_notification_setting] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::UpdateSpaceNotificationSetting;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateSpaceNotificationSetting {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateSpaceNotificationSetting(
        RequestBuilder<crate::model::UpdateSpaceNotificationSettingRequest>,
    );

    impl UpdateSpaceNotificationSetting {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::UpdateSpaceNotificationSettingRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SpaceNotificationSetting> {
            (*self.0.stub)
                .update_space_notification_setting(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [space_notification_setting][crate::model::UpdateSpaceNotificationSettingRequest::space_notification_setting].
        ///
        /// This is a **required** field for requests.
        pub fn set_space_notification_setting<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SpaceNotificationSetting>,
        {
            self.0.request.space_notification_setting = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [space_notification_setting][crate::model::UpdateSpaceNotificationSettingRequest::space_notification_setting].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_space_notification_setting<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<crate::model::SpaceNotificationSetting>,
        {
            self.0.request.space_notification_setting = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::UpdateSpaceNotificationSettingRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::UpdateSpaceNotificationSettingRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateSpaceNotificationSetting {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::create_section][crate::client::ChatService::create_section] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::CreateSection;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CreateSection {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CreateSection(RequestBuilder<crate::model::CreateSectionRequest>);

    impl CreateSection {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::CreateSectionRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Section> {
            (*self.0.stub)
                .create_section(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [parent][crate::model::CreateSectionRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [section][crate::model::CreateSectionRequest::section].
        ///
        /// This is a **required** field for requests.
        pub fn set_section<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Section>,
        {
            self.0.request.section = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [section][crate::model::CreateSectionRequest::section].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_section<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Section>,
        {
            self.0.request.section = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CreateSection {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::delete_section][crate::client::ChatService::delete_section] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::DeleteSection;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeleteSection {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeleteSection(RequestBuilder<crate::model::DeleteSectionRequest>);

    impl DeleteSection {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::DeleteSectionRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<()> {
            (*self.0.stub)
                .delete_section(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::DeleteSectionRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeleteSection {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::update_section][crate::client::ChatService::update_section] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::UpdateSection;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateSection {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateSection(RequestBuilder<crate::model::UpdateSectionRequest>);

    impl UpdateSection {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::UpdateSectionRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Section> {
            (*self.0.stub)
                .update_section(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [section][crate::model::UpdateSectionRequest::section].
        ///
        /// This is a **required** field for requests.
        pub fn set_section<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Section>,
        {
            self.0.request.section = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [section][crate::model::UpdateSectionRequest::section].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_section<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Section>,
        {
            self.0.request.section = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::UpdateSectionRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::UpdateSectionRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateSection {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::list_sections][crate::client::ChatService::list_sections] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::ListSections;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListSections {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListSections(RequestBuilder<crate::model::ListSectionsRequest>);

    impl ListSections {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ListSectionsRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ListSectionsResponse> {
            (*self.0.stub)
                .list_sections(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::ListSectionsResponse, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ListSectionsResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [parent][crate::model::ListSectionsRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [page_size][crate::model::ListSectionsRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::ListSectionsRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListSections {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::position_section][crate::client::ChatService::position_section] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::PositionSection;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> PositionSection {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct PositionSection(RequestBuilder<crate::model::PositionSectionRequest>);

    impl PositionSection {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::PositionSectionRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::PositionSectionResponse> {
            (*self.0.stub)
                .position_section(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::PositionSectionRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }

        /// Sets the value of [position][crate::model::PositionSectionRequest::position].
        ///
        /// Note that all the setters affecting `position` are
        /// mutually exclusive.
        pub fn set_position<
            T: Into<Option<crate::model::position_section_request::PositionOneof>>,
        >(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.position = v.into();
            self
        }

        /// Sets the value of [position][crate::model::PositionSectionRequest::position]
        /// to hold a `SortOrder`.
        ///
        /// Note that all the setters affecting `position` are
        /// mutually exclusive.
        pub fn set_sort_order<T: std::convert::Into<i32>>(mut self, v: T) -> Self {
            self.0.request = self.0.request.set_sort_order(v);
            self
        }

        /// Sets the value of [position][crate::model::PositionSectionRequest::position]
        /// to hold a `RelativePosition`.
        ///
        /// Note that all the setters affecting `position` are
        /// mutually exclusive.
        pub fn set_relative_position<
            T: std::convert::Into<crate::model::position_section_request::Position>,
        >(
            mut self,
            v: T,
        ) -> Self {
            self.0.request = self.0.request.set_relative_position(v);
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for PositionSection {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::list_section_items][crate::client::ChatService::list_section_items] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::ListSectionItems;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListSectionItems {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListSectionItems(RequestBuilder<crate::model::ListSectionItemsRequest>);

    impl ListSectionItems {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ListSectionItemsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ListSectionItemsResponse> {
            (*self.0.stub)
                .list_section_items(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::ListSectionItemsResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ListSectionItemsResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [parent][crate::model::ListSectionItemsRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [page_size][crate::model::ListSectionItemsRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::ListSectionItemsRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }

        /// Sets the value of [filter][crate::model::ListSectionItemsRequest::filter].
        pub fn set_filter<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.filter = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListSectionItems {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ChatService::move_section_item][crate::client::ChatService::move_section_item] calls.
    ///
    /// # Example
    /// ```
    /// # use google_chat_v1::builder::chat_service::MoveSectionItem;
    /// # async fn sample() -> google_chat_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> MoveSectionItem {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct MoveSectionItem(RequestBuilder<crate::model::MoveSectionItemRequest>);

    impl MoveSectionItem {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ChatService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::MoveSectionItemRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::MoveSectionItemResponse> {
            (*self.0.stub)
                .move_section_item(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::MoveSectionItemRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }

        /// Sets the value of [target_section][crate::model::MoveSectionItemRequest::target_section].
        ///
        /// This is a **required** field for requests.
        pub fn set_target_section<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_section = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for MoveSectionItem {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}
