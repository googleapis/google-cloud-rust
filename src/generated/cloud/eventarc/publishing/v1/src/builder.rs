// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// Code generated by sidekick. DO NOT EDIT.

pub mod publisher {
    use crate::Result;

    /// A builder for [Publisher][crate::client::Publisher].
    ///
    /// ```
    /// # async fn sample() -> gax::client_builder::Result<()> {
    /// # use google_cloud_eventarc_publishing_v1::*;
    /// # use builder::publisher::ClientBuilder;
    /// # use client::Publisher;
    /// let builder : ClientBuilder = Publisher::builder();
    /// let client = builder
    ///     .with_endpoint("https://eventarcpublishing.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder =
        gax::client_builder::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::Publisher;
        pub struct Factory;
        impl gax::client_builder::internal::ClientFactory for Factory {
            type Client = Publisher;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> gax::client_builder::Result<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::Publisher] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::Publisher>,
        request: R,
        options: gax::options::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Publisher>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: gax::options::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [Publisher::publish_channel_connection_events][crate::client::Publisher::publish_channel_connection_events] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_eventarc_publishing_v1::builder::publisher::PublishChannelConnectionEvents;
    /// # async fn sample() -> gax::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> PublishChannelConnectionEvents {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct PublishChannelConnectionEvents(
        RequestBuilder<crate::model::PublishChannelConnectionEventsRequest>,
    );

    impl PublishChannelConnectionEvents {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Publisher>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::PublishChannelConnectionEventsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<gax::options::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::PublishChannelConnectionEventsResponse> {
            (*self.0.stub)
                .publish_channel_connection_events(self.0.request, self.0.options)
                .await
                .map(gax::response::Response::into_body)
        }

        /// Sets the value of [channel_connection][crate::model::PublishChannelConnectionEventsRequest::channel_connection].
        pub fn set_channel_connection<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.channel_connection = v.into();
            self
        }

        /// Sets the value of [events][crate::model::PublishChannelConnectionEventsRequest::events].
        pub fn set_events<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<wkt::Any>,
        {
            use std::iter::Iterator;
            self.0.request.events = v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [text_events][crate::model::PublishChannelConnectionEventsRequest::text_events].
        pub fn set_text_events<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<std::string::String>,
        {
            use std::iter::Iterator;
            self.0.request.text_events = v.into_iter().map(|i| i.into()).collect();
            self
        }
    }

    #[doc(hidden)]
    impl gax::options::internal::RequestBuilder for PublishChannelConnectionEvents {
        fn request_options(&mut self) -> &mut gax::options::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Publisher::publish_events][crate::client::Publisher::publish_events] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_eventarc_publishing_v1::builder::publisher::PublishEvents;
    /// # async fn sample() -> gax::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> PublishEvents {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct PublishEvents(RequestBuilder<crate::model::PublishEventsRequest>);

    impl PublishEvents {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Publisher>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::PublishEventsRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<gax::options::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::PublishEventsResponse> {
            (*self.0.stub)
                .publish_events(self.0.request, self.0.options)
                .await
                .map(gax::response::Response::into_body)
        }

        /// Sets the value of [channel][crate::model::PublishEventsRequest::channel].
        pub fn set_channel<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.channel = v.into();
            self
        }

        /// Sets the value of [events][crate::model::PublishEventsRequest::events].
        pub fn set_events<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<wkt::Any>,
        {
            use std::iter::Iterator;
            self.0.request.events = v.into_iter().map(|i| i.into()).collect();
            self
        }

        /// Sets the value of [text_events][crate::model::PublishEventsRequest::text_events].
        pub fn set_text_events<T, V>(mut self, v: T) -> Self
        where
            T: std::iter::IntoIterator<Item = V>,
            V: std::convert::Into<std::string::String>,
        {
            use std::iter::Iterator;
            self.0.request.text_events = v.into_iter().map(|i| i.into()).collect();
            self
        }
    }

    #[doc(hidden)]
    impl gax::options::internal::RequestBuilder for PublishEvents {
        fn request_options(&mut self) -> &mut gax::options::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Publisher::publish][crate::client::Publisher::publish] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_eventarc_publishing_v1::builder::publisher::Publish;
    /// # async fn sample() -> gax::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Publish {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Publish(RequestBuilder<crate::model::PublishRequest>);

    impl Publish {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Publisher>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::PublishRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<gax::options::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::PublishResponse> {
            (*self.0.stub)
                .publish(self.0.request, self.0.options)
                .await
                .map(gax::response::Response::into_body)
        }

        /// Sets the value of [message_bus][crate::model::PublishRequest::message_bus].
        ///
        /// This is a **required** field for requests.
        pub fn set_message_bus<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.message_bus = v.into();
            self
        }

        /// Sets the value of [format][crate::model::PublishRequest::format].
        ///
        /// Note that all the setters affecting `format` are
        /// mutually exclusive.
        pub fn set_format<T: Into<Option<crate::model::publish_request::Format>>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.format = v.into();
            self
        }

        /// Sets the value of [format][crate::model::PublishRequest::format]
        /// to hold a `ProtoMessage`.
        ///
        /// Note that all the setters affecting `format` are
        /// mutually exclusive.
        pub fn set_proto_message<
            T: std::convert::Into<std::boxed::Box<crate::model::CloudEvent>>,
        >(
            mut self,
            v: T,
        ) -> Self {
            self.0.request = self.0.request.set_proto_message(v);
            self
        }

        /// Sets the value of [format][crate::model::PublishRequest::format]
        /// to hold a `JsonMessage`.
        ///
        /// Note that all the setters affecting `format` are
        /// mutually exclusive.
        pub fn set_json_message<T: std::convert::Into<std::string::String>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request = self.0.request.set_json_message(v);
            self
        }

        /// Sets the value of [format][crate::model::PublishRequest::format]
        /// to hold a `AvroMessage`.
        ///
        /// Note that all the setters affecting `format` are
        /// mutually exclusive.
        pub fn set_avro_message<T: std::convert::Into<::bytes::Bytes>>(mut self, v: T) -> Self {
            self.0.request = self.0.request.set_avro_message(v);
            self
        }
    }

    #[doc(hidden)]
    impl gax::options::internal::RequestBuilder for Publish {
        fn request_options(&mut self) -> &mut gax::options::RequestOptions {
            &mut self.0.options
        }
    }
}
