// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// Code generated by sidekick. DO NOT EDIT.

/// Request and client builders for [ShoeService][crate::client::ShoeService].
pub mod shoe_service {
    use crate::Result;

    /// A builder for [ShoeService][crate::client::ShoeService].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_gshoe_v1::*;
    /// # use builder::shoe_service::ClientBuilder;
    /// # use client::ShoeService;
    /// let builder : ClientBuilder = ShoeService::builder();
    /// let client = builder
    ///     .with_endpoint("https://gshoe.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::ShoeService;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = ShoeService;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::ShoeService] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::ShoeService>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ShoeService>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [ShoeService::list_shoes][crate::client::ShoeService::list_shoes] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_gshoe_v1::builder::shoe_service::ListShoes;
    /// # async fn sample() -> google_cloud_gshoe_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListShoes {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListShoes(RequestBuilder<crate::model::ListShoesRequest>);

    impl ListShoes {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ShoeService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ListShoesRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ListShoesResponse> {
            (*self.0.stub)
                .list_shoes(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::ListShoesResponse, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::ListShoesResponse, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [parent][crate::model::ListShoesRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [page_size][crate::model::ListShoesRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::ListShoesRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }

        /// Sets the value of [filter][crate::model::ListShoesRequest::filter].
        pub fn set_filter<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.filter = v.into();
            self
        }

        /// Sets the value of [order_by][crate::model::ListShoesRequest::order_by].
        pub fn set_order_by<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.order_by = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListShoes {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ShoeService::get_shoe][crate::client::ShoeService::get_shoe] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_gshoe_v1::builder::shoe_service::GetShoe;
    /// # async fn sample() -> google_cloud_gshoe_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetShoe {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetShoe(RequestBuilder<crate::model::GetShoeRequest>);

    impl GetShoe {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ShoeService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetShoeRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Shoe> {
            (*self.0.stub)
                .get_shoe(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::GetShoeRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetShoe {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ShoeService::create_shoe][crate::client::ShoeService::create_shoe] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_gshoe_v1::builder::shoe_service::CreateShoe;
    /// # async fn sample() -> google_cloud_gshoe_v1::Result<()> {
    /// use google_cloud_lro::Poller;
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.poller().until_done().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CreateShoe {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CreateShoe(RequestBuilder<crate::model::CreateShoeRequest>);

    impl CreateShoe {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ShoeService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::CreateShoeRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        ///
        /// # Long running operations
        ///
        /// This starts, but does not poll, a longrunning operation. More information
        /// on [create_shoe][crate::client::ShoeService::create_shoe].
        pub async fn send(self) -> Result<google_cloud_longrunning::model::Operation> {
            (*self.0.stub)
                .create_shoe(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `create_shoe`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Shoe, crate::model::OperationMetadata>
        {
            type Operation = google_cloud_lro::internal::Operation<
                crate::model::Shoe,
                crate::model::OperationMetadata,
            >;
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_gshoe_v1::client::ShoeService::create_shoe::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    let op = GetOperation::new(stub)
                        .set_name(name)
                        .with_options(options)
                        .send()
                        .await?;
                    Ok(Operation::new(op))
                }
            };

            let start = move || async {
                let op = self.send().await?;
                Ok(Operation::new(op))
            };

            use google_cloud_lro::internal::PollerExt;
            {
                google_cloud_lro::internal::new_poller(
                    polling_error_policy,
                    polling_backoff_policy,
                    start,
                    query,
                )
            }
            .with_options(poller_options)
        }

        /// Sets the value of [parent][crate::model::CreateShoeRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [shoe_id][crate::model::CreateShoeRequest::shoe_id].
        ///
        /// This is a **required** field for requests.
        pub fn set_shoe_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.shoe_id = v.into();
            self
        }

        /// Sets the value of [shoe][crate::model::CreateShoeRequest::shoe].
        ///
        /// This is a **required** field for requests.
        pub fn set_shoe<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Shoe>,
        {
            self.0.request.shoe = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [shoe][crate::model::CreateShoeRequest::shoe].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_shoe<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Shoe>,
        {
            self.0.request.shoe = v.map(|x| x.into());
            self
        }

        /// Sets the value of [request_id][crate::model::CreateShoeRequest::request_id].
        pub fn set_request_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.request_id = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CreateShoe {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ShoeService::update_shoe][crate::client::ShoeService::update_shoe] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_gshoe_v1::builder::shoe_service::UpdateShoe;
    /// # async fn sample() -> google_cloud_gshoe_v1::Result<()> {
    /// use google_cloud_lro::Poller;
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.poller().until_done().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateShoe {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateShoe(RequestBuilder<crate::model::UpdateShoeRequest>);

    impl UpdateShoe {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ShoeService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::UpdateShoeRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        ///
        /// # Long running operations
        ///
        /// This starts, but does not poll, a longrunning operation. More information
        /// on [update_shoe][crate::client::ShoeService::update_shoe].
        pub async fn send(self) -> Result<google_cloud_longrunning::model::Operation> {
            (*self.0.stub)
                .update_shoe(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update_shoe`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Shoe, crate::model::OperationMetadata>
        {
            type Operation = google_cloud_lro::internal::Operation<
                crate::model::Shoe,
                crate::model::OperationMetadata,
            >;
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_gshoe_v1::client::ShoeService::update_shoe::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    let op = GetOperation::new(stub)
                        .set_name(name)
                        .with_options(options)
                        .send()
                        .await?;
                    Ok(Operation::new(op))
                }
            };

            let start = move || async {
                let op = self.send().await?;
                Ok(Operation::new(op))
            };

            use google_cloud_lro::internal::PollerExt;
            {
                google_cloud_lro::internal::new_poller(
                    polling_error_policy,
                    polling_backoff_policy,
                    start,
                    query,
                )
            }
            .with_options(poller_options)
        }

        /// Sets the value of [update_mask][crate::model::UpdateShoeRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::UpdateShoeRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [shoe][crate::model::UpdateShoeRequest::shoe].
        ///
        /// This is a **required** field for requests.
        pub fn set_shoe<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Shoe>,
        {
            self.0.request.shoe = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [shoe][crate::model::UpdateShoeRequest::shoe].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_shoe<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Shoe>,
        {
            self.0.request.shoe = v.map(|x| x.into());
            self
        }

        /// Sets the value of [request_id][crate::model::UpdateShoeRequest::request_id].
        pub fn set_request_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.request_id = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateShoe {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ShoeService::delete_shoe][crate::client::ShoeService::delete_shoe] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_gshoe_v1::builder::shoe_service::DeleteShoe;
    /// # async fn sample() -> google_cloud_gshoe_v1::Result<()> {
    /// use google_cloud_lro::Poller;
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.poller().until_done().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeleteShoe {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeleteShoe(RequestBuilder<crate::model::DeleteShoeRequest>);

    impl DeleteShoe {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ShoeService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::DeleteShoeRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        ///
        /// # Long running operations
        ///
        /// This starts, but does not poll, a longrunning operation. More information
        /// on [delete_shoe][crate::client::ShoeService::delete_shoe].
        pub async fn send(self) -> Result<google_cloud_longrunning::model::Operation> {
            (*self.0.stub)
                .delete_shoe(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete_shoe`.
        pub fn poller(self) -> impl google_cloud_lro::Poller<(), crate::model::OperationMetadata> {
            type Operation =
                google_cloud_lro::internal::Operation<wkt::Empty, crate::model::OperationMetadata>;
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_gshoe_v1::client::ShoeService::delete_shoe::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    let op = GetOperation::new(stub)
                        .set_name(name)
                        .with_options(options)
                        .send()
                        .await?;
                    Ok(Operation::new(op))
                }
            };

            let start = move || async {
                let op = self.send().await?;
                Ok(Operation::new(op))
            };

            use google_cloud_lro::internal::PollerExt;
            {
                google_cloud_lro::internal::new_unit_response_poller(
                    polling_error_policy,
                    polling_backoff_policy,
                    start,
                    query,
                )
            }
            .with_options(poller_options)
        }

        /// Sets the value of [name][crate::model::DeleteShoeRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::DeleteShoeRequest::request_id].
        pub fn set_request_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.request_id = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeleteShoe {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ShoeService::list_workouts][crate::client::ShoeService::list_workouts] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_gshoe_v1::builder::shoe_service::ListWorkouts;
    /// # async fn sample() -> google_cloud_gshoe_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListWorkouts {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListWorkouts(RequestBuilder<crate::model::ListWorkoutsRequest>);

    impl ListWorkouts {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ShoeService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ListWorkoutsRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ListWorkoutsResponse> {
            (*self.0.stub)
                .list_workouts(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::ListWorkoutsResponse, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ListWorkoutsResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [parent][crate::model::ListWorkoutsRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [page_size][crate::model::ListWorkoutsRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::ListWorkoutsRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }

        /// Sets the value of [filter][crate::model::ListWorkoutsRequest::filter].
        pub fn set_filter<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.filter = v.into();
            self
        }

        /// Sets the value of [order_by][crate::model::ListWorkoutsRequest::order_by].
        pub fn set_order_by<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.order_by = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListWorkouts {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ShoeService::get_workout][crate::client::ShoeService::get_workout] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_gshoe_v1::builder::shoe_service::GetWorkout;
    /// # async fn sample() -> google_cloud_gshoe_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetWorkout {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetWorkout(RequestBuilder<crate::model::GetWorkoutRequest>);

    impl GetWorkout {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ShoeService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetWorkoutRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Workout> {
            (*self.0.stub)
                .get_workout(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::GetWorkoutRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetWorkout {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ShoeService::create_workout][crate::client::ShoeService::create_workout] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_gshoe_v1::builder::shoe_service::CreateWorkout;
    /// # async fn sample() -> google_cloud_gshoe_v1::Result<()> {
    /// use google_cloud_lro::Poller;
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.poller().until_done().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CreateWorkout {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CreateWorkout(RequestBuilder<crate::model::CreateWorkoutRequest>);

    impl CreateWorkout {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ShoeService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::CreateWorkoutRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        ///
        /// # Long running operations
        ///
        /// This starts, but does not poll, a longrunning operation. More information
        /// on [create_workout][crate::client::ShoeService::create_workout].
        pub async fn send(self) -> Result<google_cloud_longrunning::model::Operation> {
            (*self.0.stub)
                .create_workout(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `create_workout`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Workout, crate::model::OperationMetadata>
        {
            type Operation = google_cloud_lro::internal::Operation<
                crate::model::Workout,
                crate::model::OperationMetadata,
            >;
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_gshoe_v1::client::ShoeService::create_workout::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    let op = GetOperation::new(stub)
                        .set_name(name)
                        .with_options(options)
                        .send()
                        .await?;
                    Ok(Operation::new(op))
                }
            };

            let start = move || async {
                let op = self.send().await?;
                Ok(Operation::new(op))
            };

            use google_cloud_lro::internal::PollerExt;
            {
                google_cloud_lro::internal::new_poller(
                    polling_error_policy,
                    polling_backoff_policy,
                    start,
                    query,
                )
            }
            .with_options(poller_options)
        }

        /// Sets the value of [parent][crate::model::CreateWorkoutRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [workout_id][crate::model::CreateWorkoutRequest::workout_id].
        ///
        /// This is a **required** field for requests.
        pub fn set_workout_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.workout_id = v.into();
            self
        }

        /// Sets the value of [workout][crate::model::CreateWorkoutRequest::workout].
        ///
        /// This is a **required** field for requests.
        pub fn set_workout<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Workout>,
        {
            self.0.request.workout = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [workout][crate::model::CreateWorkoutRequest::workout].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_workout<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Workout>,
        {
            self.0.request.workout = v.map(|x| x.into());
            self
        }

        /// Sets the value of [request_id][crate::model::CreateWorkoutRequest::request_id].
        pub fn set_request_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.request_id = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CreateWorkout {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ShoeService::update_workout][crate::client::ShoeService::update_workout] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_gshoe_v1::builder::shoe_service::UpdateWorkout;
    /// # async fn sample() -> google_cloud_gshoe_v1::Result<()> {
    /// use google_cloud_lro::Poller;
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.poller().until_done().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateWorkout {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateWorkout(RequestBuilder<crate::model::UpdateWorkoutRequest>);

    impl UpdateWorkout {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ShoeService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::UpdateWorkoutRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        ///
        /// # Long running operations
        ///
        /// This starts, but does not poll, a longrunning operation. More information
        /// on [update_workout][crate::client::ShoeService::update_workout].
        pub async fn send(self) -> Result<google_cloud_longrunning::model::Operation> {
            (*self.0.stub)
                .update_workout(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update_workout`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Workout, crate::model::OperationMetadata>
        {
            type Operation = google_cloud_lro::internal::Operation<
                crate::model::Workout,
                crate::model::OperationMetadata,
            >;
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_gshoe_v1::client::ShoeService::update_workout::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    let op = GetOperation::new(stub)
                        .set_name(name)
                        .with_options(options)
                        .send()
                        .await?;
                    Ok(Operation::new(op))
                }
            };

            let start = move || async {
                let op = self.send().await?;
                Ok(Operation::new(op))
            };

            use google_cloud_lro::internal::PollerExt;
            {
                google_cloud_lro::internal::new_poller(
                    polling_error_policy,
                    polling_backoff_policy,
                    start,
                    query,
                )
            }
            .with_options(poller_options)
        }

        /// Sets the value of [update_mask][crate::model::UpdateWorkoutRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::UpdateWorkoutRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [workout][crate::model::UpdateWorkoutRequest::workout].
        ///
        /// This is a **required** field for requests.
        pub fn set_workout<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Workout>,
        {
            self.0.request.workout = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [workout][crate::model::UpdateWorkoutRequest::workout].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_workout<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Workout>,
        {
            self.0.request.workout = v.map(|x| x.into());
            self
        }

        /// Sets the value of [request_id][crate::model::UpdateWorkoutRequest::request_id].
        pub fn set_request_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.request_id = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateWorkout {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ShoeService::delete_workout][crate::client::ShoeService::delete_workout] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_gshoe_v1::builder::shoe_service::DeleteWorkout;
    /// # async fn sample() -> google_cloud_gshoe_v1::Result<()> {
    /// use google_cloud_lro::Poller;
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.poller().until_done().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeleteWorkout {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeleteWorkout(RequestBuilder<crate::model::DeleteWorkoutRequest>);

    impl DeleteWorkout {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ShoeService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::DeleteWorkoutRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        ///
        /// # Long running operations
        ///
        /// This starts, but does not poll, a longrunning operation. More information
        /// on [delete_workout][crate::client::ShoeService::delete_workout].
        pub async fn send(self) -> Result<google_cloud_longrunning::model::Operation> {
            (*self.0.stub)
                .delete_workout(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete_workout`.
        pub fn poller(self) -> impl google_cloud_lro::Poller<(), crate::model::OperationMetadata> {
            type Operation =
                google_cloud_lro::internal::Operation<wkt::Empty, crate::model::OperationMetadata>;
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_gshoe_v1::client::ShoeService::delete_workout::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    let op = GetOperation::new(stub)
                        .set_name(name)
                        .with_options(options)
                        .send()
                        .await?;
                    Ok(Operation::new(op))
                }
            };

            let start = move || async {
                let op = self.send().await?;
                Ok(Operation::new(op))
            };

            use google_cloud_lro::internal::PollerExt;
            {
                google_cloud_lro::internal::new_unit_response_poller(
                    polling_error_policy,
                    polling_backoff_policy,
                    start,
                    query,
                )
            }
            .with_options(poller_options)
        }

        /// Sets the value of [name][crate::model::DeleteWorkoutRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::DeleteWorkoutRequest::request_id].
        pub fn set_request_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.request_id = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeleteWorkout {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ShoeService::list_training_plans][crate::client::ShoeService::list_training_plans] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_gshoe_v1::builder::shoe_service::ListTrainingPlans;
    /// # async fn sample() -> google_cloud_gshoe_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListTrainingPlans {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListTrainingPlans(RequestBuilder<crate::model::ListTrainingPlansRequest>);

    impl ListTrainingPlans {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ShoeService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ListTrainingPlansRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ListTrainingPlansResponse> {
            (*self.0.stub)
                .list_training_plans(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::ListTrainingPlansResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ListTrainingPlansResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [parent][crate::model::ListTrainingPlansRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [page_size][crate::model::ListTrainingPlansRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][crate::model::ListTrainingPlansRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }

        /// Sets the value of [filter][crate::model::ListTrainingPlansRequest::filter].
        pub fn set_filter<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.filter = v.into();
            self
        }

        /// Sets the value of [order_by][crate::model::ListTrainingPlansRequest::order_by].
        pub fn set_order_by<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.order_by = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListTrainingPlans {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ShoeService::get_training_plan][crate::client::ShoeService::get_training_plan] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_gshoe_v1::builder::shoe_service::GetTrainingPlan;
    /// # async fn sample() -> google_cloud_gshoe_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetTrainingPlan {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetTrainingPlan(RequestBuilder<crate::model::GetTrainingPlanRequest>);

    impl GetTrainingPlan {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ShoeService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::GetTrainingPlanRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TrainingPlan> {
            (*self.0.stub)
                .get_training_plan(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::GetTrainingPlanRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetTrainingPlan {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ShoeService::create_training_plan][crate::client::ShoeService::create_training_plan] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_gshoe_v1::builder::shoe_service::CreateTrainingPlan;
    /// # async fn sample() -> google_cloud_gshoe_v1::Result<()> {
    /// use google_cloud_lro::Poller;
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.poller().until_done().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CreateTrainingPlan {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CreateTrainingPlan(RequestBuilder<crate::model::CreateTrainingPlanRequest>);

    impl CreateTrainingPlan {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ShoeService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::CreateTrainingPlanRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        ///
        /// # Long running operations
        ///
        /// This starts, but does not poll, a longrunning operation. More information
        /// on [create_training_plan][crate::client::ShoeService::create_training_plan].
        pub async fn send(self) -> Result<google_cloud_longrunning::model::Operation> {
            (*self.0.stub)
                .create_training_plan(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `create_training_plan`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::TrainingPlan, crate::model::OperationMetadata>
        {
            type Operation = google_cloud_lro::internal::Operation<
                crate::model::TrainingPlan,
                crate::model::OperationMetadata,
            >;
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_gshoe_v1::client::ShoeService::create_training_plan::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    let op = GetOperation::new(stub)
                        .set_name(name)
                        .with_options(options)
                        .send()
                        .await?;
                    Ok(Operation::new(op))
                }
            };

            let start = move || async {
                let op = self.send().await?;
                Ok(Operation::new(op))
            };

            use google_cloud_lro::internal::PollerExt;
            {
                google_cloud_lro::internal::new_poller(
                    polling_error_policy,
                    polling_backoff_policy,
                    start,
                    query,
                )
            }
            .with_options(poller_options)
        }

        /// Sets the value of [parent][crate::model::CreateTrainingPlanRequest::parent].
        ///
        /// This is a **required** field for requests.
        pub fn set_parent<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent = v.into();
            self
        }

        /// Sets the value of [training_plan_id][crate::model::CreateTrainingPlanRequest::training_plan_id].
        ///
        /// This is a **required** field for requests.
        pub fn set_training_plan_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.training_plan_id = v.into();
            self
        }

        /// Sets the value of [training_plan][crate::model::CreateTrainingPlanRequest::training_plan].
        ///
        /// This is a **required** field for requests.
        pub fn set_training_plan<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TrainingPlan>,
        {
            self.0.request.training_plan = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [training_plan][crate::model::CreateTrainingPlanRequest::training_plan].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_training_plan<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TrainingPlan>,
        {
            self.0.request.training_plan = v.map(|x| x.into());
            self
        }

        /// Sets the value of [request_id][crate::model::CreateTrainingPlanRequest::request_id].
        pub fn set_request_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.request_id = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CreateTrainingPlan {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ShoeService::update_training_plan][crate::client::ShoeService::update_training_plan] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_gshoe_v1::builder::shoe_service::UpdateTrainingPlan;
    /// # async fn sample() -> google_cloud_gshoe_v1::Result<()> {
    /// use google_cloud_lro::Poller;
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.poller().until_done().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateTrainingPlan {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateTrainingPlan(RequestBuilder<crate::model::UpdateTrainingPlanRequest>);

    impl UpdateTrainingPlan {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ShoeService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::UpdateTrainingPlanRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        ///
        /// # Long running operations
        ///
        /// This starts, but does not poll, a longrunning operation. More information
        /// on [update_training_plan][crate::client::ShoeService::update_training_plan].
        pub async fn send(self) -> Result<google_cloud_longrunning::model::Operation> {
            (*self.0.stub)
                .update_training_plan(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update_training_plan`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::TrainingPlan, crate::model::OperationMetadata>
        {
            type Operation = google_cloud_lro::internal::Operation<
                crate::model::TrainingPlan,
                crate::model::OperationMetadata,
            >;
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_gshoe_v1::client::ShoeService::update_training_plan::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    let op = GetOperation::new(stub)
                        .set_name(name)
                        .with_options(options)
                        .send()
                        .await?;
                    Ok(Operation::new(op))
                }
            };

            let start = move || async {
                let op = self.send().await?;
                Ok(Operation::new(op))
            };

            use google_cloud_lro::internal::PollerExt;
            {
                google_cloud_lro::internal::new_poller(
                    polling_error_policy,
                    polling_backoff_policy,
                    start,
                    query,
                )
            }
            .with_options(poller_options)
        }

        /// Sets the value of [update_mask][crate::model::UpdateTrainingPlanRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::UpdateTrainingPlanRequest::update_mask].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [training_plan][crate::model::UpdateTrainingPlanRequest::training_plan].
        ///
        /// This is a **required** field for requests.
        pub fn set_training_plan<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TrainingPlan>,
        {
            self.0.request.training_plan = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [training_plan][crate::model::UpdateTrainingPlanRequest::training_plan].
        ///
        /// This is a **required** field for requests.
        pub fn set_or_clear_training_plan<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TrainingPlan>,
        {
            self.0.request.training_plan = v.map(|x| x.into());
            self
        }

        /// Sets the value of [request_id][crate::model::UpdateTrainingPlanRequest::request_id].
        pub fn set_request_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.request_id = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateTrainingPlan {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ShoeService::delete_training_plan][crate::client::ShoeService::delete_training_plan] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_gshoe_v1::builder::shoe_service::DeleteTrainingPlan;
    /// # async fn sample() -> google_cloud_gshoe_v1::Result<()> {
    /// use google_cloud_lro::Poller;
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.poller().until_done().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeleteTrainingPlan {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeleteTrainingPlan(RequestBuilder<crate::model::DeleteTrainingPlanRequest>);

    impl DeleteTrainingPlan {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ShoeService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::DeleteTrainingPlanRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        ///
        /// # Long running operations
        ///
        /// This starts, but does not poll, a longrunning operation. More information
        /// on [delete_training_plan][crate::client::ShoeService::delete_training_plan].
        pub async fn send(self) -> Result<google_cloud_longrunning::model::Operation> {
            (*self.0.stub)
                .delete_training_plan(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete_training_plan`.
        pub fn poller(self) -> impl google_cloud_lro::Poller<(), crate::model::OperationMetadata> {
            type Operation =
                google_cloud_lro::internal::Operation<wkt::Empty, crate::model::OperationMetadata>;
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_gshoe_v1::client::ShoeService::delete_training_plan::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    let op = GetOperation::new(stub)
                        .set_name(name)
                        .with_options(options)
                        .send()
                        .await?;
                    Ok(Operation::new(op))
                }
            };

            let start = move || async {
                let op = self.send().await?;
                Ok(Operation::new(op))
            };

            use google_cloud_lro::internal::PollerExt;
            {
                google_cloud_lro::internal::new_unit_response_poller(
                    polling_error_policy,
                    polling_backoff_policy,
                    start,
                    query,
                )
            }
            .with_options(poller_options)
        }

        /// Sets the value of [name][crate::model::DeleteTrainingPlanRequest::name].
        ///
        /// This is a **required** field for requests.
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::DeleteTrainingPlanRequest::request_id].
        pub fn set_request_id<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.request_id = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeleteTrainingPlan {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ShoeService::list_operations][crate::client::ShoeService::list_operations] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_gshoe_v1::builder::shoe_service::ListOperations;
    /// # async fn sample() -> google_cloud_gshoe_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListOperations {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListOperations(
        RequestBuilder<google_cloud_longrunning::model::ListOperationsRequest>,
    );

    impl ListOperations {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ShoeService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<google_cloud_longrunning::model::ListOperationsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<google_cloud_longrunning::model::ListOperationsResponse> {
            (*self.0.stub)
                .list_operations(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            google_cloud_longrunning::model::ListOperationsResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            google_cloud_longrunning::model::ListOperationsResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [name][google_cloud_longrunning::model::ListOperationsRequest::name].
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }

        /// Sets the value of [filter][google_cloud_longrunning::model::ListOperationsRequest::filter].
        pub fn set_filter<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.filter = v.into();
            self
        }

        /// Sets the value of [page_size][google_cloud_longrunning::model::ListOperationsRequest::page_size].
        pub fn set_page_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.page_size = v.into();
            self
        }

        /// Sets the value of [page_token][google_cloud_longrunning::model::ListOperationsRequest::page_token].
        pub fn set_page_token<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.page_token = v.into();
            self
        }

        /// Sets the value of [return_partial_success][google_cloud_longrunning::model::ListOperationsRequest::return_partial_success].
        pub fn set_return_partial_success<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.return_partial_success = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListOperations {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ShoeService::get_operation][crate::client::ShoeService::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_gshoe_v1::builder::shoe_service::GetOperation;
    /// # async fn sample() -> google_cloud_gshoe_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<google_cloud_longrunning::model::GetOperationRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ShoeService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<google_cloud_longrunning::model::GetOperationRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<google_cloud_longrunning::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][google_cloud_longrunning::model::GetOperationRequest::name].
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ShoeService::delete_operation][crate::client::ShoeService::delete_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_gshoe_v1::builder::shoe_service::DeleteOperation;
    /// # async fn sample() -> google_cloud_gshoe_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeleteOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeleteOperation(
        RequestBuilder<google_cloud_longrunning::model::DeleteOperationRequest>,
    );

    impl DeleteOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ShoeService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<google_cloud_longrunning::model::DeleteOperationRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<()> {
            (*self.0.stub)
                .delete_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][google_cloud_longrunning::model::DeleteOperationRequest::name].
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeleteOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ShoeService::cancel_operation][crate::client::ShoeService::cancel_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_gshoe_v1::builder::shoe_service::CancelOperation;
    /// # async fn sample() -> google_cloud_gshoe_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CancelOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CancelOperation(
        RequestBuilder<google_cloud_longrunning::model::CancelOperationRequest>,
    );

    impl CancelOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ShoeService>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<google_cloud_longrunning::model::CancelOperationRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<()> {
            (*self.0.stub)
                .cancel_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][google_cloud_longrunning::model::CancelOperationRequest::name].
        pub fn set_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.name = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CancelOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}
