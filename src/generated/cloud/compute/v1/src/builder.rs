// Copyright 2025 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// Code generated by sidekick. DO NOT EDIT.

/// Request and client builders for [AcceleratorTypes][crate::client::AcceleratorTypes].
#[cfg(feature = "accelerator-types")]
#[cfg_attr(docsrs, doc(cfg(feature = "accelerator-types")))]
pub mod accelerator_types {
    use crate::Result;

    /// A builder for [AcceleratorTypes][crate::client::AcceleratorTypes].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::accelerator_types::ClientBuilder;
    /// # use client::AcceleratorTypes;
    /// let builder : ClientBuilder = AcceleratorTypes::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::AcceleratorTypes;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = AcceleratorTypes;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::AcceleratorTypes] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::AcceleratorTypes>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::AcceleratorTypes>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [AcceleratorTypes::aggregated_list][crate::client::AcceleratorTypes::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::accelerator_types::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::accelerator_types::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::AcceleratorTypes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::accelerator_types::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::AcceleratorTypeAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::AcceleratorTypeAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::AcceleratorTypeAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::accelerator_types::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::accelerator_types::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::accelerator_types::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::accelerator_types::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::accelerator_types::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::accelerator_types::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::accelerator_types::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::accelerator_types::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::accelerator_types::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::accelerator_types::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::accelerator_types::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::accelerator_types::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::accelerator_types::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::accelerator_types::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::accelerator_types::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [AcceleratorTypes::get][crate::client::AcceleratorTypes::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::accelerator_types::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::accelerator_types::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::AcceleratorTypes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::accelerator_types::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::AcceleratorType> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [accelerator_type][crate::model::accelerator_types::GetRequest::accelerator_type].
        pub fn set_accelerator_type<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.accelerator_type = v.into();
            self
        }

        /// Sets the value of [project][crate::model::accelerator_types::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::accelerator_types::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [AcceleratorTypes::list][crate::client::AcceleratorTypes::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::accelerator_types::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::accelerator_types::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::AcceleratorTypes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::accelerator_types::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::AcceleratorTypeList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::AcceleratorTypeList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::AcceleratorTypeList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::accelerator_types::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::accelerator_types::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::accelerator_types::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::accelerator_types::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::accelerator_types::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::accelerator_types::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::accelerator_types::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::accelerator_types::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::accelerator_types::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::accelerator_types::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::accelerator_types::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::accelerator_types::ListRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [Addresses][crate::client::Addresses].
#[cfg(feature = "addresses")]
#[cfg_attr(docsrs, doc(cfg(feature = "addresses")))]
pub mod addresses {
    use crate::Result;

    /// A builder for [Addresses][crate::client::Addresses].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::addresses::ClientBuilder;
    /// # use client::Addresses;
    /// let builder : ClientBuilder = Addresses::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::Addresses;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = Addresses;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::Addresses] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::Addresses>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Addresses>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [Addresses::aggregated_list][crate::client::Addresses::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::addresses::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(RequestBuilder<crate::model::addresses::AggregatedListRequest>);

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Addresses>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::addresses::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::AddressAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::AddressAggregatedList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::AddressAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::addresses::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::addresses::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::addresses::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::addresses::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::addresses::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::addresses::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::addresses::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::addresses::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::addresses::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::addresses::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::addresses::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::addresses::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::addresses::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::addresses::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::addresses::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Addresses::delete][crate::client::Addresses::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::addresses::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::addresses::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Addresses>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::addresses::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Addresses::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [address][crate::model::addresses::DeleteRequest::address].
        pub fn set_address<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.address = v.into();
            self
        }

        /// Sets the value of [project][crate::model::addresses::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::addresses::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::addresses::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::addresses::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Addresses::get][crate::client::Addresses::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::addresses::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::addresses::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Addresses>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::addresses::GetRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Address> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [address][crate::model::addresses::GetRequest::address].
        pub fn set_address<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.address = v.into();
            self
        }

        /// Sets the value of [project][crate::model::addresses::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::addresses::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Addresses::insert][crate::client::Addresses::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::addresses::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::addresses::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Addresses>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::addresses::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Addresses::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::addresses::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::addresses::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::addresses::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::addresses::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::addresses::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Address>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::addresses::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Address>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Addresses::list][crate::client::Addresses::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::addresses::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::addresses::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Addresses>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::addresses::ListRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::AddressList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::AddressList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::AddressList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::addresses::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::addresses::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::addresses::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::addresses::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::addresses::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::addresses::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::addresses::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::addresses::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::addresses::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::addresses::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::addresses::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::addresses::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Addresses::move][crate::client::Addresses::move] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::addresses::Move;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Move {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Move(RequestBuilder<crate::model::addresses::MoveRequest>);

    impl Move {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Addresses>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::addresses::MoveRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .r#move(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `r#move`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Addresses::r#move::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [address][crate::model::addresses::MoveRequest::address].
        pub fn set_address<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.address = v.into();
            self
        }

        /// Sets the value of [project][crate::model::addresses::MoveRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::addresses::MoveRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::addresses::MoveRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::addresses::MoveRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::addresses::MoveRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionAddressesMoveRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::addresses::MoveRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionAddressesMoveRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Move {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Addresses::set_labels][crate::client::Addresses::set_labels] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::addresses::SetLabels;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetLabels {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetLabels(RequestBuilder<crate::model::addresses::SetLabelsRequest>);

    impl SetLabels {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Addresses>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::addresses::SetLabelsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_labels(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_labels`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Addresses::set_labels::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::addresses::SetLabelsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::addresses::SetLabelsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::addresses::SetLabelsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::addresses::SetLabelsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [resource][crate::model::addresses::SetLabelsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::addresses::SetLabelsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetLabelsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::addresses::SetLabelsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetLabelsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetLabels {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Addresses::test_iam_permissions][crate::client::Addresses::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::addresses::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::addresses::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Addresses>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::addresses::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::addresses::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::addresses::TestIamPermissionsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::addresses::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::addresses::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::addresses::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Addresses::get_operation][crate::client::Addresses::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::addresses::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Addresses>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [Advice][crate::client::Advice].
#[cfg(feature = "advice")]
#[cfg_attr(docsrs, doc(cfg(feature = "advice")))]
pub mod advice {
    use crate::Result;

    /// A builder for [Advice][crate::client::Advice].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::advice::ClientBuilder;
    /// # use client::Advice;
    /// let builder : ClientBuilder = Advice::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::Advice;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = Advice;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::Advice] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::Advice>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Advice>) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [Advice::calendar_mode][crate::client::Advice::calendar_mode] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::advice::CalendarMode;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CalendarMode {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CalendarMode(RequestBuilder<crate::model::advice::CalendarModeRequest>);

    impl CalendarMode {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Advice>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::advice::CalendarModeRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::CalendarModeAdviceResponse> {
            (*self.0.stub)
                .calendar_mode(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::advice::CalendarModeRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::advice::CalendarModeRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [body][crate::model::advice::CalendarModeRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::CalendarModeAdviceRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::advice::CalendarModeRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::CalendarModeAdviceRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CalendarMode {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [Autoscalers][crate::client::Autoscalers].
#[cfg(feature = "autoscalers")]
#[cfg_attr(docsrs, doc(cfg(feature = "autoscalers")))]
pub mod autoscalers {
    use crate::Result;

    /// A builder for [Autoscalers][crate::client::Autoscalers].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::autoscalers::ClientBuilder;
    /// # use client::Autoscalers;
    /// let builder : ClientBuilder = Autoscalers::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::Autoscalers;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = Autoscalers;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::Autoscalers] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::Autoscalers>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Autoscalers>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [Autoscalers::aggregated_list][crate::client::Autoscalers::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::autoscalers::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(RequestBuilder<crate::model::autoscalers::AggregatedListRequest>);

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Autoscalers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::autoscalers::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::AutoscalerAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::AutoscalerAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::AutoscalerAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::autoscalers::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::autoscalers::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::autoscalers::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::autoscalers::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::autoscalers::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::autoscalers::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::autoscalers::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::autoscalers::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::autoscalers::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::autoscalers::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::autoscalers::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::autoscalers::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::autoscalers::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::autoscalers::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::autoscalers::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Autoscalers::delete][crate::client::Autoscalers::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::autoscalers::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::autoscalers::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Autoscalers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::autoscalers::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Autoscalers::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [autoscaler][crate::model::autoscalers::DeleteRequest::autoscaler].
        pub fn set_autoscaler<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.autoscaler = v.into();
            self
        }

        /// Sets the value of [project][crate::model::autoscalers::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::autoscalers::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::autoscalers::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::autoscalers::DeleteRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Autoscalers::get][crate::client::Autoscalers::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::autoscalers::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::autoscalers::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Autoscalers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::autoscalers::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Autoscaler> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [autoscaler][crate::model::autoscalers::GetRequest::autoscaler].
        pub fn set_autoscaler<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.autoscaler = v.into();
            self
        }

        /// Sets the value of [project][crate::model::autoscalers::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::autoscalers::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Autoscalers::insert][crate::client::Autoscalers::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::autoscalers::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::autoscalers::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Autoscalers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::autoscalers::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Autoscalers::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::autoscalers::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::autoscalers::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::autoscalers::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::autoscalers::InsertRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::autoscalers::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Autoscaler>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::autoscalers::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Autoscaler>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Autoscalers::list][crate::client::Autoscalers::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::autoscalers::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::autoscalers::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Autoscalers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::autoscalers::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::AutoscalerList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::AutoscalerList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::AutoscalerList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::autoscalers::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::autoscalers::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::autoscalers::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::autoscalers::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::autoscalers::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::autoscalers::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::autoscalers::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::autoscalers::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::autoscalers::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::autoscalers::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::autoscalers::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::autoscalers::ListRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Autoscalers::patch][crate::client::Autoscalers::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::autoscalers::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::autoscalers::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Autoscalers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::autoscalers::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Autoscalers::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [autoscaler][crate::model::autoscalers::PatchRequest::autoscaler].
        pub fn set_autoscaler<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.autoscaler = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [autoscaler][crate::model::autoscalers::PatchRequest::autoscaler].
        pub fn set_or_clear_autoscaler<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.autoscaler = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::autoscalers::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::autoscalers::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::autoscalers::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::autoscalers::PatchRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::autoscalers::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Autoscaler>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::autoscalers::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Autoscaler>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Autoscalers::test_iam_permissions][crate::client::Autoscalers::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::autoscalers::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::autoscalers::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Autoscalers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::autoscalers::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::autoscalers::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::autoscalers::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::autoscalers::TestIamPermissionsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::autoscalers::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::autoscalers::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Autoscalers::update][crate::client::Autoscalers::update] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::autoscalers::Update;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Update {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Update(RequestBuilder<crate::model::autoscalers::UpdateRequest>);

    impl Update {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Autoscalers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::autoscalers::UpdateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Autoscalers::update::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [autoscaler][crate::model::autoscalers::UpdateRequest::autoscaler].
        pub fn set_autoscaler<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.autoscaler = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [autoscaler][crate::model::autoscalers::UpdateRequest::autoscaler].
        pub fn set_or_clear_autoscaler<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.autoscaler = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::autoscalers::UpdateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::autoscalers::UpdateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::autoscalers::UpdateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::autoscalers::UpdateRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::autoscalers::UpdateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Autoscaler>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::autoscalers::UpdateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Autoscaler>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Update {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Autoscalers::get_operation][crate::client::Autoscalers::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::autoscalers::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::zone_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Autoscalers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::zone_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::zone_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zone_operations::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [BackendBuckets][crate::client::BackendBuckets].
#[cfg(feature = "backend-buckets")]
#[cfg_attr(docsrs, doc(cfg(feature = "backend-buckets")))]
pub mod backend_buckets {
    use crate::Result;

    /// A builder for [BackendBuckets][crate::client::BackendBuckets].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::backend_buckets::ClientBuilder;
    /// # use client::BackendBuckets;
    /// let builder : ClientBuilder = BackendBuckets::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::BackendBuckets;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = BackendBuckets;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::BackendBuckets] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendBuckets>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendBuckets>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [BackendBuckets::add_signed_url_key][crate::client::BackendBuckets::add_signed_url_key] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_buckets::AddSignedUrlKey;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AddSignedUrlKey {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AddSignedUrlKey(
        RequestBuilder<crate::model::backend_buckets::AddSignedUrlKeyRequest>,
    );

    impl AddSignedUrlKey {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendBuckets>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_buckets::AddSignedUrlKeyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .add_signed_url_key(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `add_signed_url_key`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::BackendBuckets::add_signed_url_key::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [backend_bucket][crate::model::backend_buckets::AddSignedUrlKeyRequest::backend_bucket].
        pub fn set_backend_bucket<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_bucket = v.into();
            self
        }

        /// Sets the value of [project][crate::model::backend_buckets::AddSignedUrlKeyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::backend_buckets::AddSignedUrlKeyRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::backend_buckets::AddSignedUrlKeyRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::backend_buckets::AddSignedUrlKeyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SignedUrlKey>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::backend_buckets::AddSignedUrlKeyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SignedUrlKey>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AddSignedUrlKey {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendBuckets::aggregated_list][crate::client::BackendBuckets::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_buckets::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(RequestBuilder<crate::model::backend_buckets::AggregatedListRequest>);

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendBuckets>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_buckets::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::BackendBucketAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::BackendBucketAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::BackendBucketAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::backend_buckets::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::backend_buckets::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::backend_buckets::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::backend_buckets::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::backend_buckets::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::backend_buckets::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::backend_buckets::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::backend_buckets::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::backend_buckets::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::backend_buckets::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::backend_buckets::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::backend_buckets::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::backend_buckets::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::backend_buckets::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::backend_buckets::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendBuckets::delete][crate::client::BackendBuckets::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_buckets::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::backend_buckets::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendBuckets>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_buckets::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::BackendBuckets::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [backend_bucket][crate::model::backend_buckets::DeleteRequest::backend_bucket].
        pub fn set_backend_bucket<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_bucket = v.into();
            self
        }

        /// Sets the value of [project][crate::model::backend_buckets::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::backend_buckets::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::backend_buckets::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendBuckets::delete_signed_url_key][crate::client::BackendBuckets::delete_signed_url_key] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_buckets::DeleteSignedUrlKey;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeleteSignedUrlKey {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeleteSignedUrlKey(
        RequestBuilder<crate::model::backend_buckets::DeleteSignedUrlKeyRequest>,
    );

    impl DeleteSignedUrlKey {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendBuckets>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_buckets::DeleteSignedUrlKeyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete_signed_url_key(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete_signed_url_key`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::BackendBuckets::delete_signed_url_key::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [backend_bucket][crate::model::backend_buckets::DeleteSignedUrlKeyRequest::backend_bucket].
        pub fn set_backend_bucket<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_bucket = v.into();
            self
        }

        /// Sets the value of [key_name][crate::model::backend_buckets::DeleteSignedUrlKeyRequest::key_name].
        pub fn set_key_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.key_name = v.into();
            self
        }

        /// Sets the value of [project][crate::model::backend_buckets::DeleteSignedUrlKeyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::backend_buckets::DeleteSignedUrlKeyRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::backend_buckets::DeleteSignedUrlKeyRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeleteSignedUrlKey {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendBuckets::get][crate::client::BackendBuckets::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_buckets::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::backend_buckets::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendBuckets>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_buckets::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::BackendBucket> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [backend_bucket][crate::model::backend_buckets::GetRequest::backend_bucket].
        pub fn set_backend_bucket<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_bucket = v.into();
            self
        }

        /// Sets the value of [project][crate::model::backend_buckets::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendBuckets::get_iam_policy][crate::client::BackendBuckets::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_buckets::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(RequestBuilder<crate::model::backend_buckets::GetIamPolicyRequest>);

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendBuckets>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_buckets::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::backend_buckets::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::backend_buckets::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::backend_buckets::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::backend_buckets::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendBuckets::insert][crate::client::BackendBuckets::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_buckets::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::backend_buckets::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendBuckets>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_buckets::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::BackendBuckets::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::backend_buckets::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::backend_buckets::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::backend_buckets::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::backend_buckets::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::BackendBucket>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::backend_buckets::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::BackendBucket>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendBuckets::list][crate::client::BackendBuckets::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_buckets::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::backend_buckets::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendBuckets>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_buckets::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::BackendBucketList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::BackendBucketList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::BackendBucketList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::backend_buckets::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::backend_buckets::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::backend_buckets::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::backend_buckets::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::backend_buckets::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::backend_buckets::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::backend_buckets::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::backend_buckets::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::backend_buckets::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::backend_buckets::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::backend_buckets::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendBuckets::list_usable][crate::client::BackendBuckets::list_usable] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_buckets::ListUsable;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListUsable {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListUsable(RequestBuilder<crate::model::backend_buckets::ListUsableRequest>);

    impl ListUsable {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendBuckets>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_buckets::ListUsableRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::BackendBucketListUsable> {
            (*self.0.stub)
                .list_usable(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::BackendBucketListUsable,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::BackendBucketListUsable,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::backend_buckets::ListUsableRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::backend_buckets::ListUsableRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::backend_buckets::ListUsableRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::backend_buckets::ListUsableRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::backend_buckets::ListUsableRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::backend_buckets::ListUsableRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::backend_buckets::ListUsableRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::backend_buckets::ListUsableRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::backend_buckets::ListUsableRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::backend_buckets::ListUsableRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::backend_buckets::ListUsableRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListUsable {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendBuckets::patch][crate::client::BackendBuckets::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_buckets::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::backend_buckets::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendBuckets>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_buckets::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::BackendBuckets::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [backend_bucket][crate::model::backend_buckets::PatchRequest::backend_bucket].
        pub fn set_backend_bucket<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_bucket = v.into();
            self
        }

        /// Sets the value of [project][crate::model::backend_buckets::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::backend_buckets::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::backend_buckets::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::backend_buckets::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::BackendBucket>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::backend_buckets::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::BackendBucket>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendBuckets::set_edge_security_policy][crate::client::BackendBuckets::set_edge_security_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_buckets::SetEdgeSecurityPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetEdgeSecurityPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetEdgeSecurityPolicy(
        RequestBuilder<crate::model::backend_buckets::SetEdgeSecurityPolicyRequest>,
    );

    impl SetEdgeSecurityPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendBuckets>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::backend_buckets::SetEdgeSecurityPolicyRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_edge_security_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_edge_security_policy`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::BackendBuckets::set_edge_security_policy::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [backend_bucket][crate::model::backend_buckets::SetEdgeSecurityPolicyRequest::backend_bucket].
        pub fn set_backend_bucket<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_bucket = v.into();
            self
        }

        /// Sets the value of [project][crate::model::backend_buckets::SetEdgeSecurityPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::backend_buckets::SetEdgeSecurityPolicyRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::backend_buckets::SetEdgeSecurityPolicyRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::backend_buckets::SetEdgeSecurityPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyReference>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::backend_buckets::SetEdgeSecurityPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyReference>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetEdgeSecurityPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendBuckets::set_iam_policy][crate::client::BackendBuckets::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_buckets::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(RequestBuilder<crate::model::backend_buckets::SetIamPolicyRequest>);

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendBuckets>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_buckets::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::backend_buckets::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::backend_buckets::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::backend_buckets::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::backend_buckets::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendBuckets::test_iam_permissions][crate::client::BackendBuckets::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_buckets::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::backend_buckets::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendBuckets>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_buckets::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::backend_buckets::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::backend_buckets::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::backend_buckets::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::backend_buckets::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendBuckets::update][crate::client::BackendBuckets::update] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_buckets::Update;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Update {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Update(RequestBuilder<crate::model::backend_buckets::UpdateRequest>);

    impl Update {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendBuckets>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_buckets::UpdateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::BackendBuckets::update::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [backend_bucket][crate::model::backend_buckets::UpdateRequest::backend_bucket].
        pub fn set_backend_bucket<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_bucket = v.into();
            self
        }

        /// Sets the value of [project][crate::model::backend_buckets::UpdateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::backend_buckets::UpdateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::backend_buckets::UpdateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::backend_buckets::UpdateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::BackendBucket>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::backend_buckets::UpdateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::BackendBucket>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Update {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendBuckets::get_operation][crate::client::BackendBuckets::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_buckets::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendBuckets>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [BackendServices][crate::client::BackendServices].
#[cfg(feature = "backend-services")]
#[cfg_attr(docsrs, doc(cfg(feature = "backend-services")))]
pub mod backend_services {
    use crate::Result;

    /// A builder for [BackendServices][crate::client::BackendServices].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::backend_services::ClientBuilder;
    /// # use client::BackendServices;
    /// let builder : ClientBuilder = BackendServices::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::BackendServices;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = BackendServices;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::BackendServices] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendServices>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendServices>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [BackendServices::add_signed_url_key][crate::client::BackendServices::add_signed_url_key] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_services::AddSignedUrlKey;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AddSignedUrlKey {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AddSignedUrlKey(
        RequestBuilder<crate::model::backend_services::AddSignedUrlKeyRequest>,
    );

    impl AddSignedUrlKey {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_services::AddSignedUrlKeyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .add_signed_url_key(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `add_signed_url_key`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::BackendServices::add_signed_url_key::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [backend_service][crate::model::backend_services::AddSignedUrlKeyRequest::backend_service].
        pub fn set_backend_service<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_service = v.into();
            self
        }

        /// Sets the value of [project][crate::model::backend_services::AddSignedUrlKeyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::backend_services::AddSignedUrlKeyRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::backend_services::AddSignedUrlKeyRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::backend_services::AddSignedUrlKeyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SignedUrlKey>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::backend_services::AddSignedUrlKeyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SignedUrlKey>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AddSignedUrlKey {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendServices::aggregated_list][crate::client::BackendServices::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_services::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::backend_services::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_services::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::BackendServiceAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::BackendServiceAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::BackendServiceAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::backend_services::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::backend_services::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::backend_services::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::backend_services::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::backend_services::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::backend_services::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::backend_services::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::backend_services::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::backend_services::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::backend_services::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::backend_services::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::backend_services::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::backend_services::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::backend_services::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::backend_services::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendServices::delete][crate::client::BackendServices::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_services::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::backend_services::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_services::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::BackendServices::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [backend_service][crate::model::backend_services::DeleteRequest::backend_service].
        pub fn set_backend_service<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_service = v.into();
            self
        }

        /// Sets the value of [project][crate::model::backend_services::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::backend_services::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::backend_services::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendServices::delete_signed_url_key][crate::client::BackendServices::delete_signed_url_key] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_services::DeleteSignedUrlKey;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeleteSignedUrlKey {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeleteSignedUrlKey(
        RequestBuilder<crate::model::backend_services::DeleteSignedUrlKeyRequest>,
    );

    impl DeleteSignedUrlKey {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_services::DeleteSignedUrlKeyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete_signed_url_key(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete_signed_url_key`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::BackendServices::delete_signed_url_key::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [backend_service][crate::model::backend_services::DeleteSignedUrlKeyRequest::backend_service].
        pub fn set_backend_service<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_service = v.into();
            self
        }

        /// Sets the value of [key_name][crate::model::backend_services::DeleteSignedUrlKeyRequest::key_name].
        pub fn set_key_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.key_name = v.into();
            self
        }

        /// Sets the value of [project][crate::model::backend_services::DeleteSignedUrlKeyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::backend_services::DeleteSignedUrlKeyRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::backend_services::DeleteSignedUrlKeyRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeleteSignedUrlKey {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendServices::get][crate::client::BackendServices::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_services::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::backend_services::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_services::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::BackendService> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [backend_service][crate::model::backend_services::GetRequest::backend_service].
        pub fn set_backend_service<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_service = v.into();
            self
        }

        /// Sets the value of [project][crate::model::backend_services::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendServices::get_effective_security_policies][crate::client::BackendServices::get_effective_security_policies] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_services::GetEffectiveSecurityPolicies;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetEffectiveSecurityPolicies {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetEffectiveSecurityPolicies(
        RequestBuilder<crate::model::backend_services::GetEffectiveSecurityPoliciesRequest>,
    );

    impl GetEffectiveSecurityPolicies {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::backend_services::GetEffectiveSecurityPoliciesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<()> {
            (*self.0.stub)
                .get_effective_security_policies(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [backend_service][crate::model::backend_services::GetEffectiveSecurityPoliciesRequest::backend_service].
        pub fn set_backend_service<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_service = v.into();
            self
        }

        /// Sets the value of [project][crate::model::backend_services::GetEffectiveSecurityPoliciesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetEffectiveSecurityPolicies {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendServices::get_health][crate::client::BackendServices::get_health] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_services::GetHealth;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetHealth {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetHealth(RequestBuilder<crate::model::backend_services::GetHealthRequest>);

    impl GetHealth {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_services::GetHealthRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::BackendServiceGroupHealth> {
            (*self.0.stub)
                .get_health(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [backend_service][crate::model::backend_services::GetHealthRequest::backend_service].
        pub fn set_backend_service<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_service = v.into();
            self
        }

        /// Sets the value of [project][crate::model::backend_services::GetHealthRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [body][crate::model::backend_services::GetHealthRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ResourceGroupReference>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::backend_services::GetHealthRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ResourceGroupReference>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetHealth {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendServices::get_iam_policy][crate::client::BackendServices::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_services::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(RequestBuilder<crate::model::backend_services::GetIamPolicyRequest>);

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_services::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::backend_services::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::backend_services::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::backend_services::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::backend_services::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendServices::insert][crate::client::BackendServices::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_services::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::backend_services::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_services::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::BackendServices::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::backend_services::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::backend_services::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::backend_services::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::backend_services::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::BackendService>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::backend_services::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::BackendService>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendServices::list][crate::client::BackendServices::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_services::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::backend_services::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_services::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::BackendServiceList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::BackendServiceList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::BackendServiceList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::backend_services::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::backend_services::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::backend_services::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::backend_services::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::backend_services::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::backend_services::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::backend_services::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::backend_services::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::backend_services::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::backend_services::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::backend_services::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendServices::list_usable][crate::client::BackendServices::list_usable] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_services::ListUsable;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListUsable {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListUsable(RequestBuilder<crate::model::backend_services::ListUsableRequest>);

    impl ListUsable {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_services::ListUsableRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::BackendServiceListUsable> {
            (*self.0.stub)
                .list_usable(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::BackendServiceListUsable,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::BackendServiceListUsable,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::backend_services::ListUsableRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::backend_services::ListUsableRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::backend_services::ListUsableRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::backend_services::ListUsableRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::backend_services::ListUsableRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::backend_services::ListUsableRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::backend_services::ListUsableRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::backend_services::ListUsableRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::backend_services::ListUsableRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::backend_services::ListUsableRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::backend_services::ListUsableRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListUsable {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendServices::patch][crate::client::BackendServices::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_services::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::backend_services::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_services::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::BackendServices::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [backend_service][crate::model::backend_services::PatchRequest::backend_service].
        pub fn set_backend_service<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_service = v.into();
            self
        }

        /// Sets the value of [project][crate::model::backend_services::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::backend_services::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::backend_services::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::backend_services::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::BackendService>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::backend_services::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::BackendService>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendServices::set_edge_security_policy][crate::client::BackendServices::set_edge_security_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_services::SetEdgeSecurityPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetEdgeSecurityPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetEdgeSecurityPolicy(
        RequestBuilder<crate::model::backend_services::SetEdgeSecurityPolicyRequest>,
    );

    impl SetEdgeSecurityPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::backend_services::SetEdgeSecurityPolicyRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_edge_security_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_edge_security_policy`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::BackendServices::set_edge_security_policy::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [backend_service][crate::model::backend_services::SetEdgeSecurityPolicyRequest::backend_service].
        pub fn set_backend_service<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_service = v.into();
            self
        }

        /// Sets the value of [project][crate::model::backend_services::SetEdgeSecurityPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::backend_services::SetEdgeSecurityPolicyRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::backend_services::SetEdgeSecurityPolicyRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::backend_services::SetEdgeSecurityPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyReference>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::backend_services::SetEdgeSecurityPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyReference>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetEdgeSecurityPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendServices::set_iam_policy][crate::client::BackendServices::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_services::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(RequestBuilder<crate::model::backend_services::SetIamPolicyRequest>);

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_services::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::backend_services::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::backend_services::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::backend_services::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::backend_services::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendServices::set_security_policy][crate::client::BackendServices::set_security_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_services::SetSecurityPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetSecurityPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetSecurityPolicy(
        RequestBuilder<crate::model::backend_services::SetSecurityPolicyRequest>,
    );

    impl SetSecurityPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_services::SetSecurityPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_security_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_security_policy`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::BackendServices::set_security_policy::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [backend_service][crate::model::backend_services::SetSecurityPolicyRequest::backend_service].
        pub fn set_backend_service<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_service = v.into();
            self
        }

        /// Sets the value of [project][crate::model::backend_services::SetSecurityPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::backend_services::SetSecurityPolicyRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::backend_services::SetSecurityPolicyRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::backend_services::SetSecurityPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyReference>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::backend_services::SetSecurityPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyReference>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetSecurityPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendServices::test_iam_permissions][crate::client::BackendServices::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_services::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::backend_services::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_services::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::backend_services::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::backend_services::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::backend_services::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::backend_services::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendServices::update][crate::client::BackendServices::update] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_services::Update;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Update {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Update(RequestBuilder<crate::model::backend_services::UpdateRequest>);

    impl Update {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::backend_services::UpdateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::BackendServices::update::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [backend_service][crate::model::backend_services::UpdateRequest::backend_service].
        pub fn set_backend_service<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_service = v.into();
            self
        }

        /// Sets the value of [project][crate::model::backend_services::UpdateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::backend_services::UpdateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::backend_services::UpdateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::backend_services::UpdateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::BackendService>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::backend_services::UpdateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::BackendService>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Update {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [BackendServices::get_operation][crate::client::BackendServices::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::backend_services::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::BackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [CrossSiteNetworks][crate::client::CrossSiteNetworks].
#[cfg(feature = "cross-site-networks")]
#[cfg_attr(docsrs, doc(cfg(feature = "cross-site-networks")))]
pub mod cross_site_networks {
    use crate::Result;

    /// A builder for [CrossSiteNetworks][crate::client::CrossSiteNetworks].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::cross_site_networks::ClientBuilder;
    /// # use client::CrossSiteNetworks;
    /// let builder : ClientBuilder = CrossSiteNetworks::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::CrossSiteNetworks;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = CrossSiteNetworks;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::CrossSiteNetworks] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::CrossSiteNetworks>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::CrossSiteNetworks>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [CrossSiteNetworks::delete][crate::client::CrossSiteNetworks::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::cross_site_networks::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::cross_site_networks::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::CrossSiteNetworks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::cross_site_networks::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::CrossSiteNetworks::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [cross_site_network][crate::model::cross_site_networks::DeleteRequest::cross_site_network].
        pub fn set_cross_site_network<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.cross_site_network = v.into();
            self
        }

        /// Sets the value of [project][crate::model::cross_site_networks::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::cross_site_networks::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::cross_site_networks::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [CrossSiteNetworks::get][crate::client::CrossSiteNetworks::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::cross_site_networks::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::cross_site_networks::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::CrossSiteNetworks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::cross_site_networks::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::CrossSiteNetwork> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [cross_site_network][crate::model::cross_site_networks::GetRequest::cross_site_network].
        pub fn set_cross_site_network<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.cross_site_network = v.into();
            self
        }

        /// Sets the value of [project][crate::model::cross_site_networks::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [CrossSiteNetworks::insert][crate::client::CrossSiteNetworks::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::cross_site_networks::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::cross_site_networks::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::CrossSiteNetworks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::cross_site_networks::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::CrossSiteNetworks::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::cross_site_networks::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::cross_site_networks::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::cross_site_networks::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [validate_only][crate::model::cross_site_networks::InsertRequest::validate_only].
        pub fn set_validate_only<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.validate_only = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [validate_only][crate::model::cross_site_networks::InsertRequest::validate_only].
        pub fn set_or_clear_validate_only<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.validate_only = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::cross_site_networks::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::CrossSiteNetwork>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::cross_site_networks::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::CrossSiteNetwork>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [CrossSiteNetworks::list][crate::client::CrossSiteNetworks::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::cross_site_networks::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::cross_site_networks::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::CrossSiteNetworks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::cross_site_networks::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::CrossSiteNetworkList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::CrossSiteNetworkList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::CrossSiteNetworkList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::cross_site_networks::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::cross_site_networks::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::cross_site_networks::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::cross_site_networks::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::cross_site_networks::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::cross_site_networks::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::cross_site_networks::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::cross_site_networks::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::cross_site_networks::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::cross_site_networks::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::cross_site_networks::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [CrossSiteNetworks::patch][crate::client::CrossSiteNetworks::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::cross_site_networks::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::cross_site_networks::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::CrossSiteNetworks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::cross_site_networks::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::CrossSiteNetworks::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [cross_site_network][crate::model::cross_site_networks::PatchRequest::cross_site_network].
        pub fn set_cross_site_network<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.cross_site_network = v.into();
            self
        }

        /// Sets the value of [project][crate::model::cross_site_networks::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::cross_site_networks::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::cross_site_networks::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::cross_site_networks::PatchRequest::update_mask].
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::cross_site_networks::PatchRequest::update_mask].
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [validate_only][crate::model::cross_site_networks::PatchRequest::validate_only].
        pub fn set_validate_only<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.validate_only = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [validate_only][crate::model::cross_site_networks::PatchRequest::validate_only].
        pub fn set_or_clear_validate_only<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.validate_only = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::cross_site_networks::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::CrossSiteNetwork>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::cross_site_networks::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::CrossSiteNetwork>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [CrossSiteNetworks::get_operation][crate::client::CrossSiteNetworks::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::cross_site_networks::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::CrossSiteNetworks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [DiskTypes][crate::client::DiskTypes].
#[cfg(feature = "disk-types")]
#[cfg_attr(docsrs, doc(cfg(feature = "disk-types")))]
pub mod disk_types {
    use crate::Result;

    /// A builder for [DiskTypes][crate::client::DiskTypes].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::disk_types::ClientBuilder;
    /// # use client::DiskTypes;
    /// let builder : ClientBuilder = DiskTypes::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::DiskTypes;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = DiskTypes;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::DiskTypes] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::DiskTypes>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DiskTypes>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [DiskTypes::aggregated_list][crate::client::DiskTypes::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::disk_types::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(RequestBuilder<crate::model::disk_types::AggregatedListRequest>);

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DiskTypes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::disk_types::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::DiskTypeAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::DiskTypeAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::DiskTypeAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::disk_types::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::disk_types::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::disk_types::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::disk_types::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::disk_types::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::disk_types::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::disk_types::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::disk_types::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::disk_types::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::disk_types::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::disk_types::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::disk_types::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::disk_types::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::disk_types::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::disk_types::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DiskTypes::get][crate::client::DiskTypes::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::disk_types::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::disk_types::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DiskTypes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::disk_types::GetRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::DiskType> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [disk_type][crate::model::disk_types::GetRequest::disk_type].
        pub fn set_disk_type<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.disk_type = v.into();
            self
        }

        /// Sets the value of [project][crate::model::disk_types::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::disk_types::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [DiskTypes::list][crate::client::DiskTypes::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::disk_types::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::disk_types::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::DiskTypes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::disk_types::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::DiskTypeList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::DiskTypeList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::DiskTypeList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::disk_types::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::disk_types::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::disk_types::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::disk_types::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::disk_types::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::disk_types::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::disk_types::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::disk_types::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::disk_types::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::disk_types::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::disk_types::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::disk_types::ListRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [Disks][crate::client::Disks].
#[cfg(feature = "disks")]
#[cfg_attr(docsrs, doc(cfg(feature = "disks")))]
pub mod disks {
    use crate::Result;

    /// A builder for [Disks][crate::client::Disks].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::disks::ClientBuilder;
    /// # use client::Disks;
    /// let builder : ClientBuilder = Disks::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::Disks;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = Disks;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::Disks] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::Disks>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Disks>) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [Disks::add_resource_policies][crate::client::Disks::add_resource_policies] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::disks::AddResourcePolicies;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AddResourcePolicies {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AddResourcePolicies(RequestBuilder<crate::model::disks::AddResourcePoliciesRequest>);

    impl AddResourcePolicies {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Disks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::disks::AddResourcePoliciesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .add_resource_policies(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `add_resource_policies`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Disks::add_resource_policies::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [disk][crate::model::disks::AddResourcePoliciesRequest::disk].
        pub fn set_disk<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.disk = v.into();
            self
        }

        /// Sets the value of [project][crate::model::disks::AddResourcePoliciesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::disks::AddResourcePoliciesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::disks::AddResourcePoliciesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::disks::AddResourcePoliciesRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::disks::AddResourcePoliciesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DisksAddResourcePoliciesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::disks::AddResourcePoliciesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DisksAddResourcePoliciesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AddResourcePolicies {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Disks::aggregated_list][crate::client::Disks::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::disks::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(RequestBuilder<crate::model::disks::AggregatedListRequest>);

    impl AggregatedList {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Disks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::disks::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::DiskAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::DiskAggregatedList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::DiskAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::disks::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::disks::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::disks::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::disks::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::disks::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::disks::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::disks::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::disks::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::disks::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::disks::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::disks::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::disks::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::disks::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::disks::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::disks::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Disks::bulk_insert][crate::client::Disks::bulk_insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::disks::BulkInsert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> BulkInsert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct BulkInsert(RequestBuilder<crate::model::disks::BulkInsertRequest>);

    impl BulkInsert {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Disks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::disks::BulkInsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .bulk_insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `bulk_insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Disks::bulk_insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::disks::BulkInsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::disks::BulkInsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::disks::BulkInsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::disks::BulkInsertRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::disks::BulkInsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::BulkInsertDiskResource>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::disks::BulkInsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::BulkInsertDiskResource>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for BulkInsert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Disks::bulk_set_labels][crate::client::Disks::bulk_set_labels] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::disks::BulkSetLabels;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> BulkSetLabels {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct BulkSetLabels(RequestBuilder<crate::model::disks::BulkSetLabelsRequest>);

    impl BulkSetLabels {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Disks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::disks::BulkSetLabelsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .bulk_set_labels(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `bulk_set_labels`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Disks::bulk_set_labels::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::disks::BulkSetLabelsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::disks::BulkSetLabelsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::disks::BulkSetLabelsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [resource][crate::model::disks::BulkSetLabelsRequest::resource].
        pub fn set_resource<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.resource = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [resource][crate::model::disks::BulkSetLabelsRequest::resource].
        pub fn set_or_clear_resource<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.resource = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::disks::BulkSetLabelsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::disks::BulkSetLabelsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::BulkZoneSetLabelsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::disks::BulkSetLabelsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::BulkZoneSetLabelsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for BulkSetLabels {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Disks::create_snapshot][crate::client::Disks::create_snapshot] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::disks::CreateSnapshot;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CreateSnapshot {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CreateSnapshot(RequestBuilder<crate::model::disks::CreateSnapshotRequest>);

    impl CreateSnapshot {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Disks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::disks::CreateSnapshotRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .create_snapshot(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `create_snapshot`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Disks::create_snapshot::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [disk][crate::model::disks::CreateSnapshotRequest::disk].
        pub fn set_disk<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.disk = v.into();
            self
        }

        /// Sets the value of [guest_flush][crate::model::disks::CreateSnapshotRequest::guest_flush].
        pub fn set_guest_flush<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.guest_flush = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [guest_flush][crate::model::disks::CreateSnapshotRequest::guest_flush].
        pub fn set_or_clear_guest_flush<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.guest_flush = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::disks::CreateSnapshotRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::disks::CreateSnapshotRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::disks::CreateSnapshotRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::disks::CreateSnapshotRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::disks::CreateSnapshotRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Snapshot>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::disks::CreateSnapshotRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Snapshot>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CreateSnapshot {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Disks::delete][crate::client::Disks::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::disks::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::disks::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Disks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::disks::DeleteRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Disks::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [disk][crate::model::disks::DeleteRequest::disk].
        pub fn set_disk<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.disk = v.into();
            self
        }

        /// Sets the value of [project][crate::model::disks::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::disks::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::disks::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::disks::DeleteRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Disks::get][crate::client::Disks::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::disks::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::disks::GetRequest>);

    impl Get {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Disks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::disks::GetRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Disk> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [disk][crate::model::disks::GetRequest::disk].
        pub fn set_disk<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.disk = v.into();
            self
        }

        /// Sets the value of [project][crate::model::disks::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::disks::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Disks::get_iam_policy][crate::client::Disks::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::disks::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(RequestBuilder<crate::model::disks::GetIamPolicyRequest>);

    impl GetIamPolicy {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Disks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::disks::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::disks::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::disks::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::disks::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::disks::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::disks::GetIamPolicyRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Disks::insert][crate::client::Disks::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::disks::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::disks::InsertRequest>);

    impl Insert {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Disks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::disks::InsertRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Disks::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::disks::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::disks::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::disks::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [source_image][crate::model::disks::InsertRequest::source_image].
        pub fn set_source_image<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.source_image = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [source_image][crate::model::disks::InsertRequest::source_image].
        pub fn set_or_clear_source_image<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.source_image = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::disks::InsertRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::disks::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Disk>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::disks::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Disk>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Disks::list][crate::client::Disks::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::disks::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::disks::ListRequest>);

    impl List {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Disks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::disks::ListRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::DiskList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::DiskList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::DiskList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::disks::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::disks::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::disks::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::disks::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::disks::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::disks::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::disks::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::disks::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::disks::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::disks::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::disks::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::disks::ListRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Disks::remove_resource_policies][crate::client::Disks::remove_resource_policies] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::disks::RemoveResourcePolicies;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> RemoveResourcePolicies {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct RemoveResourcePolicies(
        RequestBuilder<crate::model::disks::RemoveResourcePoliciesRequest>,
    );

    impl RemoveResourcePolicies {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Disks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::disks::RemoveResourcePoliciesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .remove_resource_policies(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `remove_resource_policies`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Disks::remove_resource_policies::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [disk][crate::model::disks::RemoveResourcePoliciesRequest::disk].
        pub fn set_disk<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.disk = v.into();
            self
        }

        /// Sets the value of [project][crate::model::disks::RemoveResourcePoliciesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::disks::RemoveResourcePoliciesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::disks::RemoveResourcePoliciesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::disks::RemoveResourcePoliciesRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::disks::RemoveResourcePoliciesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DisksRemoveResourcePoliciesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::disks::RemoveResourcePoliciesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DisksRemoveResourcePoliciesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for RemoveResourcePolicies {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Disks::resize][crate::client::Disks::resize] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::disks::Resize;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Resize {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Resize(RequestBuilder<crate::model::disks::ResizeRequest>);

    impl Resize {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Disks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::disks::ResizeRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .resize(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `resize`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Disks::resize::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [disk][crate::model::disks::ResizeRequest::disk].
        pub fn set_disk<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.disk = v.into();
            self
        }

        /// Sets the value of [project][crate::model::disks::ResizeRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::disks::ResizeRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::disks::ResizeRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::disks::ResizeRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::disks::ResizeRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DisksResizeRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::disks::ResizeRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DisksResizeRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Resize {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Disks::set_iam_policy][crate::client::Disks::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::disks::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(RequestBuilder<crate::model::disks::SetIamPolicyRequest>);

    impl SetIamPolicy {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Disks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::disks::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::disks::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::disks::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::disks::SetIamPolicyRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::disks::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ZoneSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::disks::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ZoneSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Disks::set_labels][crate::client::Disks::set_labels] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::disks::SetLabels;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetLabels {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetLabels(RequestBuilder<crate::model::disks::SetLabelsRequest>);

    impl SetLabels {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Disks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::disks::SetLabelsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_labels(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_labels`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Disks::set_labels::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::disks::SetLabelsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::disks::SetLabelsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::disks::SetLabelsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [resource][crate::model::disks::SetLabelsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::disks::SetLabelsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::disks::SetLabelsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ZoneSetLabelsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::disks::SetLabelsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ZoneSetLabelsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetLabels {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Disks::start_async_replication][crate::client::Disks::start_async_replication] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::disks::StartAsyncReplication;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> StartAsyncReplication {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct StartAsyncReplication(
        RequestBuilder<crate::model::disks::StartAsyncReplicationRequest>,
    );

    impl StartAsyncReplication {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Disks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::disks::StartAsyncReplicationRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .start_async_replication(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `start_async_replication`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Disks::start_async_replication::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [disk][crate::model::disks::StartAsyncReplicationRequest::disk].
        pub fn set_disk<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.disk = v.into();
            self
        }

        /// Sets the value of [project][crate::model::disks::StartAsyncReplicationRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::disks::StartAsyncReplicationRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::disks::StartAsyncReplicationRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::disks::StartAsyncReplicationRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::disks::StartAsyncReplicationRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DisksStartAsyncReplicationRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::disks::StartAsyncReplicationRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DisksStartAsyncReplicationRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for StartAsyncReplication {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Disks::stop_async_replication][crate::client::Disks::stop_async_replication] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::disks::StopAsyncReplication;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> StopAsyncReplication {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct StopAsyncReplication(
        RequestBuilder<crate::model::disks::StopAsyncReplicationRequest>,
    );

    impl StopAsyncReplication {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Disks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::disks::StopAsyncReplicationRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .stop_async_replication(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `stop_async_replication`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Disks::stop_async_replication::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [disk][crate::model::disks::StopAsyncReplicationRequest::disk].
        pub fn set_disk<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.disk = v.into();
            self
        }

        /// Sets the value of [project][crate::model::disks::StopAsyncReplicationRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::disks::StopAsyncReplicationRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::disks::StopAsyncReplicationRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::disks::StopAsyncReplicationRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for StopAsyncReplication {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Disks::stop_group_async_replication][crate::client::Disks::stop_group_async_replication] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::disks::StopGroupAsyncReplication;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> StopGroupAsyncReplication {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct StopGroupAsyncReplication(
        RequestBuilder<crate::model::disks::StopGroupAsyncReplicationRequest>,
    );

    impl StopGroupAsyncReplication {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Disks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::disks::StopGroupAsyncReplicationRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .stop_group_async_replication(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `stop_group_async_replication`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Disks::stop_group_async_replication::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::disks::StopGroupAsyncReplicationRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::disks::StopGroupAsyncReplicationRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::disks::StopGroupAsyncReplicationRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::disks::StopGroupAsyncReplicationRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::disks::StopGroupAsyncReplicationRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DisksStopGroupAsyncReplicationResource>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::disks::StopGroupAsyncReplicationRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DisksStopGroupAsyncReplicationResource>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for StopGroupAsyncReplication {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Disks::test_iam_permissions][crate::client::Disks::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::disks::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(RequestBuilder<crate::model::disks::TestIamPermissionsRequest>);

    impl TestIamPermissions {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Disks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::disks::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::disks::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::disks::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::disks::TestIamPermissionsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::disks::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::disks::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Disks::update][crate::client::Disks::update] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::disks::Update;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Update {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Update(RequestBuilder<crate::model::disks::UpdateRequest>);

    impl Update {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Disks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::disks::UpdateRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Disks::update::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [disk][crate::model::disks::UpdateRequest::disk].
        pub fn set_disk<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.disk = v.into();
            self
        }

        /// Sets the value of [paths][crate::model::disks::UpdateRequest::paths].
        pub fn set_paths<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.paths = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [paths][crate::model::disks::UpdateRequest::paths].
        pub fn set_or_clear_paths<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.paths = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::disks::UpdateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::disks::UpdateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::disks::UpdateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::disks::UpdateRequest::update_mask].
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::disks::UpdateRequest::update_mask].
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::disks::UpdateRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::disks::UpdateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Disk>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::disks::UpdateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Disk>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Update {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Disks::update_kms_key][crate::client::Disks::update_kms_key] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::disks::UpdateKmsKey;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateKmsKey {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateKmsKey(RequestBuilder<crate::model::disks::UpdateKmsKeyRequest>);

    impl UpdateKmsKey {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Disks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::disks::UpdateKmsKeyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update_kms_key(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update_kms_key`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Disks::update_kms_key::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [disk][crate::model::disks::UpdateKmsKeyRequest::disk].
        pub fn set_disk<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.disk = v.into();
            self
        }

        /// Sets the value of [project][crate::model::disks::UpdateKmsKeyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::disks::UpdateKmsKeyRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::disks::UpdateKmsKeyRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::disks::UpdateKmsKeyRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::disks::UpdateKmsKeyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DiskUpdateKmsKeyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::disks::UpdateKmsKeyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DiskUpdateKmsKeyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateKmsKey {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Disks::get_operation][crate::client::Disks::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::disks::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::zone_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Disks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::zone_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::zone_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zone_operations::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [ExternalVpnGateways][crate::client::ExternalVpnGateways].
#[cfg(feature = "external-vpn-gateways")]
#[cfg_attr(docsrs, doc(cfg(feature = "external-vpn-gateways")))]
pub mod external_vpn_gateways {
    use crate::Result;

    /// A builder for [ExternalVpnGateways][crate::client::ExternalVpnGateways].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::external_vpn_gateways::ClientBuilder;
    /// # use client::ExternalVpnGateways;
    /// let builder : ClientBuilder = ExternalVpnGateways::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::ExternalVpnGateways;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = ExternalVpnGateways;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::ExternalVpnGateways] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::ExternalVpnGateways>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ExternalVpnGateways>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [ExternalVpnGateways::delete][crate::client::ExternalVpnGateways::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::external_vpn_gateways::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::external_vpn_gateways::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ExternalVpnGateways>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::external_vpn_gateways::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::ExternalVpnGateways::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [external_vpn_gateway][crate::model::external_vpn_gateways::DeleteRequest::external_vpn_gateway].
        pub fn set_external_vpn_gateway<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.external_vpn_gateway = v.into();
            self
        }

        /// Sets the value of [project][crate::model::external_vpn_gateways::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::external_vpn_gateways::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::external_vpn_gateways::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ExternalVpnGateways::get][crate::client::ExternalVpnGateways::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::external_vpn_gateways::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::external_vpn_gateways::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ExternalVpnGateways>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::external_vpn_gateways::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ExternalVpnGateway> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [external_vpn_gateway][crate::model::external_vpn_gateways::GetRequest::external_vpn_gateway].
        pub fn set_external_vpn_gateway<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.external_vpn_gateway = v.into();
            self
        }

        /// Sets the value of [project][crate::model::external_vpn_gateways::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ExternalVpnGateways::insert][crate::client::ExternalVpnGateways::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::external_vpn_gateways::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::external_vpn_gateways::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ExternalVpnGateways>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::external_vpn_gateways::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::ExternalVpnGateways::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::external_vpn_gateways::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::external_vpn_gateways::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::external_vpn_gateways::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::external_vpn_gateways::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ExternalVpnGateway>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::external_vpn_gateways::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ExternalVpnGateway>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ExternalVpnGateways::list][crate::client::ExternalVpnGateways::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::external_vpn_gateways::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::external_vpn_gateways::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ExternalVpnGateways>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::external_vpn_gateways::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ExternalVpnGatewayList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::ExternalVpnGatewayList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ExternalVpnGatewayList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::external_vpn_gateways::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::external_vpn_gateways::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::external_vpn_gateways::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::external_vpn_gateways::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::external_vpn_gateways::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::external_vpn_gateways::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::external_vpn_gateways::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::external_vpn_gateways::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::external_vpn_gateways::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::external_vpn_gateways::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::external_vpn_gateways::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ExternalVpnGateways::set_labels][crate::client::ExternalVpnGateways::set_labels] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::external_vpn_gateways::SetLabels;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetLabels {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetLabels(RequestBuilder<crate::model::external_vpn_gateways::SetLabelsRequest>);

    impl SetLabels {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ExternalVpnGateways>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::external_vpn_gateways::SetLabelsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_labels(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_labels`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::ExternalVpnGateways::set_labels::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::external_vpn_gateways::SetLabelsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::external_vpn_gateways::SetLabelsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::external_vpn_gateways::SetLabelsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetLabelsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::external_vpn_gateways::SetLabelsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetLabelsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetLabels {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ExternalVpnGateways::test_iam_permissions][crate::client::ExternalVpnGateways::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::external_vpn_gateways::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::external_vpn_gateways::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ExternalVpnGateways>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::external_vpn_gateways::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::external_vpn_gateways::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::external_vpn_gateways::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::external_vpn_gateways::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::external_vpn_gateways::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ExternalVpnGateways::get_operation][crate::client::ExternalVpnGateways::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::external_vpn_gateways::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ExternalVpnGateways>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [FirewallPolicies][crate::client::FirewallPolicies].
#[cfg(feature = "firewall-policies")]
#[cfg_attr(docsrs, doc(cfg(feature = "firewall-policies")))]
pub mod firewall_policies {
    use crate::Result;

    /// A builder for [FirewallPolicies][crate::client::FirewallPolicies].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::firewall_policies::ClientBuilder;
    /// # use client::FirewallPolicies;
    /// let builder : ClientBuilder = FirewallPolicies::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::FirewallPolicies;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = FirewallPolicies;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::FirewallPolicies] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::FirewallPolicies>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FirewallPolicies>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [FirewallPolicies::add_association][crate::client::FirewallPolicies::add_association] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewall_policies::AddAssociation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AddAssociation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AddAssociation(
        RequestBuilder<crate::model::firewall_policies::AddAssociationRequest>,
    );

    impl AddAssociation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::firewall_policies::AddAssociationRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .add_association(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `add_association`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::FirewallPolicies::add_association::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    GetOperation::new(stub)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::firewall_policies::AddAssociationRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [replace_existing_association][crate::model::firewall_policies::AddAssociationRequest::replace_existing_association].
        pub fn set_replace_existing_association<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.replace_existing_association = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [replace_existing_association][crate::model::firewall_policies::AddAssociationRequest::replace_existing_association].
        pub fn set_or_clear_replace_existing_association<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.replace_existing_association = v.map(|x| x.into());
            self
        }

        /// Sets the value of [request_id][crate::model::firewall_policies::AddAssociationRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::firewall_policies::AddAssociationRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::firewall_policies::AddAssociationRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicyAssociation>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::firewall_policies::AddAssociationRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicyAssociation>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AddAssociation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [FirewallPolicies::add_rule][crate::client::FirewallPolicies::add_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewall_policies::AddRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AddRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AddRule(RequestBuilder<crate::model::firewall_policies::AddRuleRequest>);

    impl AddRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::firewall_policies::AddRuleRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .add_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `add_rule`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::FirewallPolicies::add_rule::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    GetOperation::new(stub)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::firewall_policies::AddRuleRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::firewall_policies::AddRuleRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::firewall_policies::AddRuleRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::firewall_policies::AddRuleRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicyRule>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::firewall_policies::AddRuleRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicyRule>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AddRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [FirewallPolicies::clone_rules][crate::client::FirewallPolicies::clone_rules] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewall_policies::CloneRules;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CloneRules {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CloneRules(RequestBuilder<crate::model::firewall_policies::CloneRulesRequest>);

    impl CloneRules {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::firewall_policies::CloneRulesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .clone_rules(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `clone_rules`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::FirewallPolicies::clone_rules::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    GetOperation::new(stub)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::firewall_policies::CloneRulesRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::firewall_policies::CloneRulesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::firewall_policies::CloneRulesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [source_firewall_policy][crate::model::firewall_policies::CloneRulesRequest::source_firewall_policy].
        pub fn set_source_firewall_policy<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.source_firewall_policy = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [source_firewall_policy][crate::model::firewall_policies::CloneRulesRequest::source_firewall_policy].
        pub fn set_or_clear_source_firewall_policy<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.source_firewall_policy = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CloneRules {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [FirewallPolicies::delete][crate::client::FirewallPolicies::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewall_policies::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::firewall_policies::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::firewall_policies::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::FirewallPolicies::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    GetOperation::new(stub)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::firewall_policies::DeleteRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::firewall_policies::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::firewall_policies::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [FirewallPolicies::get][crate::client::FirewallPolicies::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewall_policies::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::firewall_policies::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::firewall_policies::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::FirewallPolicy> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [firewall_policy][crate::model::firewall_policies::GetRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [FirewallPolicies::get_association][crate::client::FirewallPolicies::get_association] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewall_policies::GetAssociation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetAssociation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetAssociation(
        RequestBuilder<crate::model::firewall_policies::GetAssociationRequest>,
    );

    impl GetAssociation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::firewall_policies::GetAssociationRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::FirewallPolicyAssociation> {
            (*self.0.stub)
                .get_association(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [firewall_policy][crate::model::firewall_policies::GetAssociationRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [name][crate::model::firewall_policies::GetAssociationRequest::name].
        pub fn set_name<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.name = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [name][crate::model::firewall_policies::GetAssociationRequest::name].
        pub fn set_or_clear_name<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.name = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetAssociation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [FirewallPolicies::get_iam_policy][crate::client::FirewallPolicies::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewall_policies::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(RequestBuilder<crate::model::firewall_policies::GetIamPolicyRequest>);

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::firewall_policies::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::firewall_policies::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::firewall_policies::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [resource][crate::model::firewall_policies::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [FirewallPolicies::get_rule][crate::client::FirewallPolicies::get_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewall_policies::GetRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetRule(RequestBuilder<crate::model::firewall_policies::GetRuleRequest>);

    impl GetRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::firewall_policies::GetRuleRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::FirewallPolicyRule> {
            (*self.0.stub)
                .get_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [firewall_policy][crate::model::firewall_policies::GetRuleRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [priority][crate::model::firewall_policies::GetRuleRequest::priority].
        pub fn set_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [priority][crate::model::firewall_policies::GetRuleRequest::priority].
        pub fn set_or_clear_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [FirewallPolicies::insert][crate::client::FirewallPolicies::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewall_policies::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::firewall_policies::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::firewall_policies::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::FirewallPolicies::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    GetOperation::new(stub)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [parent_id][crate::model::firewall_policies::InsertRequest::parent_id].
        pub fn set_parent_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.parent_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [parent_id][crate::model::firewall_policies::InsertRequest::parent_id].
        pub fn set_or_clear_parent_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.parent_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [request_id][crate::model::firewall_policies::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::firewall_policies::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::firewall_policies::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::firewall_policies::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [FirewallPolicies::list][crate::client::FirewallPolicies::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewall_policies::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::firewall_policies::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::firewall_policies::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::FirewallPolicyList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::FirewallPolicyList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::FirewallPolicyList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::firewall_policies::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::firewall_policies::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::firewall_policies::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::firewall_policies::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::firewall_policies::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::firewall_policies::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::firewall_policies::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::firewall_policies::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [parent_id][crate::model::firewall_policies::ListRequest::parent_id].
        pub fn set_parent_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.parent_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [parent_id][crate::model::firewall_policies::ListRequest::parent_id].
        pub fn set_or_clear_parent_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.parent_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [return_partial_success][crate::model::firewall_policies::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::firewall_policies::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [FirewallPolicies::list_associations][crate::client::FirewallPolicies::list_associations] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewall_policies::ListAssociations;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListAssociations {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListAssociations(
        RequestBuilder<crate::model::firewall_policies::ListAssociationsRequest>,
    );

    impl ListAssociations {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::firewall_policies::ListAssociationsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::FirewallPoliciesListAssociationsResponse> {
            (*self.0.stub)
                .list_associations(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [include_inherited_policies][crate::model::firewall_policies::ListAssociationsRequest::include_inherited_policies].
        pub fn set_include_inherited_policies<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_inherited_policies = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_inherited_policies][crate::model::firewall_policies::ListAssociationsRequest::include_inherited_policies].
        pub fn set_or_clear_include_inherited_policies<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_inherited_policies = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_resource][crate::model::firewall_policies::ListAssociationsRequest::target_resource].
        pub fn set_target_resource<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.target_resource = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [target_resource][crate::model::firewall_policies::ListAssociationsRequest::target_resource].
        pub fn set_or_clear_target_resource<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.target_resource = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListAssociations {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [FirewallPolicies::move][crate::client::FirewallPolicies::move] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewall_policies::Move;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Move {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Move(RequestBuilder<crate::model::firewall_policies::MoveRequest>);

    impl Move {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::firewall_policies::MoveRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .r#move(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `r#move`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::FirewallPolicies::r#move::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    GetOperation::new(stub)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::firewall_policies::MoveRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [parent_id][crate::model::firewall_policies::MoveRequest::parent_id].
        pub fn set_parent_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.parent_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [parent_id][crate::model::firewall_policies::MoveRequest::parent_id].
        pub fn set_or_clear_parent_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.parent_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [request_id][crate::model::firewall_policies::MoveRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::firewall_policies::MoveRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Move {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [FirewallPolicies::patch][crate::client::FirewallPolicies::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewall_policies::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::firewall_policies::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::firewall_policies::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::FirewallPolicies::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    GetOperation::new(stub)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::firewall_policies::PatchRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::firewall_policies::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::firewall_policies::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::firewall_policies::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::firewall_policies::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [FirewallPolicies::patch_rule][crate::client::FirewallPolicies::patch_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewall_policies::PatchRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> PatchRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct PatchRule(RequestBuilder<crate::model::firewall_policies::PatchRuleRequest>);

    impl PatchRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::firewall_policies::PatchRuleRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch_rule`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::FirewallPolicies::patch_rule::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    GetOperation::new(stub)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::firewall_policies::PatchRuleRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [priority][crate::model::firewall_policies::PatchRuleRequest::priority].
        pub fn set_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [priority][crate::model::firewall_policies::PatchRuleRequest::priority].
        pub fn set_or_clear_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [request_id][crate::model::firewall_policies::PatchRuleRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::firewall_policies::PatchRuleRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::firewall_policies::PatchRuleRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicyRule>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::firewall_policies::PatchRuleRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicyRule>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for PatchRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [FirewallPolicies::remove_association][crate::client::FirewallPolicies::remove_association] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewall_policies::RemoveAssociation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> RemoveAssociation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct RemoveAssociation(
        RequestBuilder<crate::model::firewall_policies::RemoveAssociationRequest>,
    );

    impl RemoveAssociation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::firewall_policies::RemoveAssociationRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .remove_association(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `remove_association`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::FirewallPolicies::remove_association::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    GetOperation::new(stub)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::firewall_policies::RemoveAssociationRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [name][crate::model::firewall_policies::RemoveAssociationRequest::name].
        pub fn set_name<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.name = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [name][crate::model::firewall_policies::RemoveAssociationRequest::name].
        pub fn set_or_clear_name<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.name = v.map(|x| x.into());
            self
        }

        /// Sets the value of [request_id][crate::model::firewall_policies::RemoveAssociationRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::firewall_policies::RemoveAssociationRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for RemoveAssociation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [FirewallPolicies::remove_rule][crate::client::FirewallPolicies::remove_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewall_policies::RemoveRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> RemoveRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct RemoveRule(RequestBuilder<crate::model::firewall_policies::RemoveRuleRequest>);

    impl RemoveRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::firewall_policies::RemoveRuleRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .remove_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `remove_rule`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::FirewallPolicies::remove_rule::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    GetOperation::new(stub)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::firewall_policies::RemoveRuleRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [priority][crate::model::firewall_policies::RemoveRuleRequest::priority].
        pub fn set_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [priority][crate::model::firewall_policies::RemoveRuleRequest::priority].
        pub fn set_or_clear_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [request_id][crate::model::firewall_policies::RemoveRuleRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::firewall_policies::RemoveRuleRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for RemoveRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [FirewallPolicies::set_iam_policy][crate::client::FirewallPolicies::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewall_policies::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(RequestBuilder<crate::model::firewall_policies::SetIamPolicyRequest>);

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::firewall_policies::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [resource][crate::model::firewall_policies::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::firewall_policies::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::GlobalOrganizationSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::firewall_policies::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::GlobalOrganizationSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [FirewallPolicies::test_iam_permissions][crate::client::FirewallPolicies::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewall_policies::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::firewall_policies::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::firewall_policies::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [resource][crate::model::firewall_policies::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::firewall_policies::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::firewall_policies::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [FirewallPolicies::get_operation][crate::client::FirewallPolicies::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewall_policies::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(
        RequestBuilder<crate::model::global_organization_operations::GetRequest>,
    );

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_organization_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_organization_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [parent_id][crate::model::global_organization_operations::GetRequest::parent_id].
        pub fn set_parent_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.parent_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [parent_id][crate::model::global_organization_operations::GetRequest::parent_id].
        pub fn set_or_clear_parent_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.parent_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [Firewalls][crate::client::Firewalls].
#[cfg(feature = "firewalls")]
#[cfg_attr(docsrs, doc(cfg(feature = "firewalls")))]
pub mod firewalls {
    use crate::Result;

    /// A builder for [Firewalls][crate::client::Firewalls].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::firewalls::ClientBuilder;
    /// # use client::Firewalls;
    /// let builder : ClientBuilder = Firewalls::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::Firewalls;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = Firewalls;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::Firewalls] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::Firewalls>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Firewalls>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [Firewalls::delete][crate::client::Firewalls::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewalls::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::firewalls::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Firewalls>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::firewalls::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Firewalls::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall][crate::model::firewalls::DeleteRequest::firewall].
        pub fn set_firewall<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall = v.into();
            self
        }

        /// Sets the value of [project][crate::model::firewalls::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::firewalls::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::firewalls::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Firewalls::get][crate::client::Firewalls::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewalls::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::firewalls::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Firewalls>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::firewalls::GetRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Firewall> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [firewall][crate::model::firewalls::GetRequest::firewall].
        pub fn set_firewall<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall = v.into();
            self
        }

        /// Sets the value of [project][crate::model::firewalls::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Firewalls::insert][crate::client::Firewalls::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewalls::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::firewalls::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Firewalls>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::firewalls::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Firewalls::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::firewalls::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::firewalls::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::firewalls::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::firewalls::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Firewall>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::firewalls::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Firewall>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Firewalls::list][crate::client::Firewalls::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewalls::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::firewalls::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Firewalls>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::firewalls::ListRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::FirewallList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::FirewallList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::FirewallList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::firewalls::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::firewalls::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::firewalls::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::firewalls::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::firewalls::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::firewalls::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::firewalls::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::firewalls::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::firewalls::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::firewalls::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::firewalls::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Firewalls::patch][crate::client::Firewalls::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewalls::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::firewalls::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Firewalls>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::firewalls::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Firewalls::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall][crate::model::firewalls::PatchRequest::firewall].
        pub fn set_firewall<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall = v.into();
            self
        }

        /// Sets the value of [project][crate::model::firewalls::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::firewalls::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::firewalls::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::firewalls::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Firewall>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::firewalls::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Firewall>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Firewalls::test_iam_permissions][crate::client::Firewalls::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewalls::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::firewalls::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Firewalls>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::firewalls::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::firewalls::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::firewalls::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::firewalls::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::firewalls::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Firewalls::update][crate::client::Firewalls::update] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewalls::Update;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Update {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Update(RequestBuilder<crate::model::firewalls::UpdateRequest>);

    impl Update {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Firewalls>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::firewalls::UpdateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Firewalls::update::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall][crate::model::firewalls::UpdateRequest::firewall].
        pub fn set_firewall<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall = v.into();
            self
        }

        /// Sets the value of [project][crate::model::firewalls::UpdateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::firewalls::UpdateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::firewalls::UpdateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::firewalls::UpdateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Firewall>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::firewalls::UpdateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Firewall>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Update {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Firewalls::get_operation][crate::client::Firewalls::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::firewalls::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Firewalls>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [ForwardingRules][crate::client::ForwardingRules].
#[cfg(feature = "forwarding-rules")]
#[cfg_attr(docsrs, doc(cfg(feature = "forwarding-rules")))]
pub mod forwarding_rules {
    use crate::Result;

    /// A builder for [ForwardingRules][crate::client::ForwardingRules].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::forwarding_rules::ClientBuilder;
    /// # use client::ForwardingRules;
    /// let builder : ClientBuilder = ForwardingRules::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::ForwardingRules;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = ForwardingRules;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::ForwardingRules] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::ForwardingRules>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ForwardingRules>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [ForwardingRules::aggregated_list][crate::client::ForwardingRules::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::forwarding_rules::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::forwarding_rules::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ForwardingRules>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::forwarding_rules::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ForwardingRuleAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::ForwardingRuleAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ForwardingRuleAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::forwarding_rules::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::forwarding_rules::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::forwarding_rules::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::forwarding_rules::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::forwarding_rules::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::forwarding_rules::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::forwarding_rules::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::forwarding_rules::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::forwarding_rules::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::forwarding_rules::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::forwarding_rules::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::forwarding_rules::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::forwarding_rules::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::forwarding_rules::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::forwarding_rules::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ForwardingRules::delete][crate::client::ForwardingRules::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::forwarding_rules::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::forwarding_rules::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ForwardingRules>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::forwarding_rules::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::ForwardingRules::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [forwarding_rule][crate::model::forwarding_rules::DeleteRequest::forwarding_rule].
        pub fn set_forwarding_rule<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.forwarding_rule = v.into();
            self
        }

        /// Sets the value of [project][crate::model::forwarding_rules::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::forwarding_rules::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::forwarding_rules::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::forwarding_rules::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ForwardingRules::get][crate::client::ForwardingRules::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::forwarding_rules::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::forwarding_rules::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ForwardingRules>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::forwarding_rules::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ForwardingRule> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [forwarding_rule][crate::model::forwarding_rules::GetRequest::forwarding_rule].
        pub fn set_forwarding_rule<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.forwarding_rule = v.into();
            self
        }

        /// Sets the value of [project][crate::model::forwarding_rules::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::forwarding_rules::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [view][crate::model::forwarding_rules::GetRequest::view].
        pub fn set_view<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::forwarding_rules::get_request::View>,
        {
            self.0.request.view = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [view][crate::model::forwarding_rules::GetRequest::view].
        pub fn set_or_clear_view<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::forwarding_rules::get_request::View>,
        {
            self.0.request.view = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ForwardingRules::insert][crate::client::ForwardingRules::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::forwarding_rules::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::forwarding_rules::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ForwardingRules>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::forwarding_rules::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::ForwardingRules::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::forwarding_rules::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::forwarding_rules::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::forwarding_rules::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::forwarding_rules::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::forwarding_rules::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ForwardingRule>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::forwarding_rules::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ForwardingRule>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ForwardingRules::list][crate::client::ForwardingRules::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::forwarding_rules::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::forwarding_rules::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ForwardingRules>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::forwarding_rules::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ForwardingRuleList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::ForwardingRuleList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ForwardingRuleList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::forwarding_rules::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::forwarding_rules::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::forwarding_rules::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::forwarding_rules::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::forwarding_rules::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::forwarding_rules::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::forwarding_rules::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::forwarding_rules::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::forwarding_rules::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::forwarding_rules::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::forwarding_rules::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::forwarding_rules::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ForwardingRules::patch][crate::client::ForwardingRules::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::forwarding_rules::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::forwarding_rules::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ForwardingRules>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::forwarding_rules::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::ForwardingRules::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [forwarding_rule][crate::model::forwarding_rules::PatchRequest::forwarding_rule].
        pub fn set_forwarding_rule<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.forwarding_rule = v.into();
            self
        }

        /// Sets the value of [project][crate::model::forwarding_rules::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::forwarding_rules::PatchRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::forwarding_rules::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::forwarding_rules::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::forwarding_rules::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ForwardingRule>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::forwarding_rules::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ForwardingRule>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ForwardingRules::set_labels][crate::client::ForwardingRules::set_labels] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::forwarding_rules::SetLabels;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetLabels {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetLabels(RequestBuilder<crate::model::forwarding_rules::SetLabelsRequest>);

    impl SetLabels {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ForwardingRules>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::forwarding_rules::SetLabelsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_labels(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_labels`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::ForwardingRules::set_labels::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::forwarding_rules::SetLabelsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::forwarding_rules::SetLabelsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::forwarding_rules::SetLabelsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::forwarding_rules::SetLabelsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [resource][crate::model::forwarding_rules::SetLabelsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::forwarding_rules::SetLabelsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetLabelsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::forwarding_rules::SetLabelsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetLabelsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetLabels {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ForwardingRules::set_target][crate::client::ForwardingRules::set_target] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::forwarding_rules::SetTarget;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetTarget {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetTarget(RequestBuilder<crate::model::forwarding_rules::SetTargetRequest>);

    impl SetTarget {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ForwardingRules>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::forwarding_rules::SetTargetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_target(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_target`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::ForwardingRules::set_target::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [forwarding_rule][crate::model::forwarding_rules::SetTargetRequest::forwarding_rule].
        pub fn set_forwarding_rule<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.forwarding_rule = v.into();
            self
        }

        /// Sets the value of [project][crate::model::forwarding_rules::SetTargetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::forwarding_rules::SetTargetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::forwarding_rules::SetTargetRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::forwarding_rules::SetTargetRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::forwarding_rules::SetTargetRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetReference>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::forwarding_rules::SetTargetRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetReference>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetTarget {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ForwardingRules::get_operation][crate::client::ForwardingRules::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::forwarding_rules::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ForwardingRules>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [FutureReservations][crate::client::FutureReservations].
#[cfg(feature = "future-reservations")]
#[cfg_attr(docsrs, doc(cfg(feature = "future-reservations")))]
pub mod future_reservations {
    use crate::Result;

    /// A builder for [FutureReservations][crate::client::FutureReservations].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::future_reservations::ClientBuilder;
    /// # use client::FutureReservations;
    /// let builder : ClientBuilder = FutureReservations::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::FutureReservations;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = FutureReservations;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::FutureReservations] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::FutureReservations>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FutureReservations>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [FutureReservations::aggregated_list][crate::client::FutureReservations::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::future_reservations::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::future_reservations::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FutureReservations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::future_reservations::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::FutureReservationsAggregatedListResponse> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::FutureReservationsAggregatedListResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::FutureReservationsAggregatedListResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::future_reservations::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::future_reservations::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::future_reservations::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::future_reservations::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::future_reservations::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::future_reservations::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::future_reservations::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::future_reservations::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::future_reservations::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::future_reservations::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::future_reservations::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::future_reservations::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::future_reservations::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::future_reservations::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::future_reservations::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [FutureReservations::cancel][crate::client::FutureReservations::cancel] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::future_reservations::Cancel;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Cancel {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Cancel(RequestBuilder<crate::model::future_reservations::CancelRequest>);

    impl Cancel {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FutureReservations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::future_reservations::CancelRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .cancel(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `cancel`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::FutureReservations::cancel::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [future_reservation][crate::model::future_reservations::CancelRequest::future_reservation].
        pub fn set_future_reservation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.future_reservation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::future_reservations::CancelRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::future_reservations::CancelRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::future_reservations::CancelRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::future_reservations::CancelRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Cancel {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [FutureReservations::delete][crate::client::FutureReservations::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::future_reservations::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::future_reservations::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FutureReservations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::future_reservations::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::FutureReservations::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [future_reservation][crate::model::future_reservations::DeleteRequest::future_reservation].
        pub fn set_future_reservation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.future_reservation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::future_reservations::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::future_reservations::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::future_reservations::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::future_reservations::DeleteRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [FutureReservations::get][crate::client::FutureReservations::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::future_reservations::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::future_reservations::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FutureReservations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::future_reservations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::FutureReservation> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [future_reservation][crate::model::future_reservations::GetRequest::future_reservation].
        pub fn set_future_reservation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.future_reservation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::future_reservations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::future_reservations::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [FutureReservations::insert][crate::client::FutureReservations::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::future_reservations::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::future_reservations::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FutureReservations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::future_reservations::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::FutureReservations::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::future_reservations::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::future_reservations::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::future_reservations::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::future_reservations::InsertRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::future_reservations::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::FutureReservation>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::future_reservations::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::FutureReservation>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [FutureReservations::list][crate::client::FutureReservations::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::future_reservations::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::future_reservations::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FutureReservations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::future_reservations::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::FutureReservationsListResponse> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::FutureReservationsListResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::FutureReservationsListResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::future_reservations::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::future_reservations::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::future_reservations::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::future_reservations::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::future_reservations::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::future_reservations::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::future_reservations::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::future_reservations::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::future_reservations::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::future_reservations::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::future_reservations::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::future_reservations::ListRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [FutureReservations::update][crate::client::FutureReservations::update] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::future_reservations::Update;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Update {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Update(RequestBuilder<crate::model::future_reservations::UpdateRequest>);

    impl Update {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FutureReservations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::future_reservations::UpdateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::FutureReservations::update::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [future_reservation][crate::model::future_reservations::UpdateRequest::future_reservation].
        pub fn set_future_reservation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.future_reservation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::future_reservations::UpdateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::future_reservations::UpdateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::future_reservations::UpdateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::future_reservations::UpdateRequest::update_mask].
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::future_reservations::UpdateRequest::update_mask].
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::future_reservations::UpdateRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::future_reservations::UpdateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::FutureReservation>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::future_reservations::UpdateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::FutureReservation>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Update {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [FutureReservations::get_operation][crate::client::FutureReservations::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::future_reservations::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::zone_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::FutureReservations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::zone_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::zone_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zone_operations::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [GlobalAddresses][crate::client::GlobalAddresses].
#[cfg(feature = "global-addresses")]
#[cfg_attr(docsrs, doc(cfg(feature = "global-addresses")))]
pub mod global_addresses {
    use crate::Result;

    /// A builder for [GlobalAddresses][crate::client::GlobalAddresses].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::global_addresses::ClientBuilder;
    /// # use client::GlobalAddresses;
    /// let builder : ClientBuilder = GlobalAddresses::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::GlobalAddresses;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = GlobalAddresses;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::GlobalAddresses] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalAddresses>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalAddresses>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [GlobalAddresses::delete][crate::client::GlobalAddresses::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_addresses::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::global_addresses::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalAddresses>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_addresses::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::GlobalAddresses::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [address][crate::model::global_addresses::DeleteRequest::address].
        pub fn set_address<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.address = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_addresses::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::global_addresses::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::global_addresses::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalAddresses::get][crate::client::GlobalAddresses::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_addresses::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::global_addresses::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalAddresses>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_addresses::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Address> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [address][crate::model::global_addresses::GetRequest::address].
        pub fn set_address<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.address = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_addresses::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalAddresses::insert][crate::client::GlobalAddresses::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_addresses::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::global_addresses::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalAddresses>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_addresses::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::GlobalAddresses::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::global_addresses::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::global_addresses::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::global_addresses::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::global_addresses::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Address>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::global_addresses::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Address>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalAddresses::list][crate::client::GlobalAddresses::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_addresses::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::global_addresses::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalAddresses>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_addresses::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::AddressList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::AddressList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::AddressList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::global_addresses::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::global_addresses::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::global_addresses::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::global_addresses::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::global_addresses::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::global_addresses::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::global_addresses::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::global_addresses::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::global_addresses::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::global_addresses::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::global_addresses::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalAddresses::move][crate::client::GlobalAddresses::move] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_addresses::Move;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Move {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Move(RequestBuilder<crate::model::global_addresses::MoveRequest>);

    impl Move {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalAddresses>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_addresses::MoveRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .r#move(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `r#move`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::GlobalAddresses::r#move::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [address][crate::model::global_addresses::MoveRequest::address].
        pub fn set_address<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.address = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_addresses::MoveRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::global_addresses::MoveRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::global_addresses::MoveRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::global_addresses::MoveRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::GlobalAddressesMoveRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::global_addresses::MoveRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::GlobalAddressesMoveRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Move {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalAddresses::set_labels][crate::client::GlobalAddresses::set_labels] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_addresses::SetLabels;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetLabels {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetLabels(RequestBuilder<crate::model::global_addresses::SetLabelsRequest>);

    impl SetLabels {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalAddresses>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_addresses::SetLabelsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_labels(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_labels`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::GlobalAddresses::set_labels::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::global_addresses::SetLabelsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::global_addresses::SetLabelsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::global_addresses::SetLabelsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetLabelsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::global_addresses::SetLabelsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetLabelsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetLabels {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalAddresses::test_iam_permissions][crate::client::GlobalAddresses::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_addresses::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::global_addresses::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalAddresses>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_addresses::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::global_addresses::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::global_addresses::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::global_addresses::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::global_addresses::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalAddresses::get_operation][crate::client::GlobalAddresses::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_addresses::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalAddresses>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [GlobalForwardingRules][crate::client::GlobalForwardingRules].
#[cfg(feature = "global-forwarding-rules")]
#[cfg_attr(docsrs, doc(cfg(feature = "global-forwarding-rules")))]
pub mod global_forwarding_rules {
    use crate::Result;

    /// A builder for [GlobalForwardingRules][crate::client::GlobalForwardingRules].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::global_forwarding_rules::ClientBuilder;
    /// # use client::GlobalForwardingRules;
    /// let builder : ClientBuilder = GlobalForwardingRules::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::GlobalForwardingRules;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = GlobalForwardingRules;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::GlobalForwardingRules] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalForwardingRules>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalForwardingRules>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [GlobalForwardingRules::delete][crate::client::GlobalForwardingRules::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_forwarding_rules::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::global_forwarding_rules::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalForwardingRules>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_forwarding_rules::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::GlobalForwardingRules::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [forwarding_rule][crate::model::global_forwarding_rules::DeleteRequest::forwarding_rule].
        pub fn set_forwarding_rule<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.forwarding_rule = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_forwarding_rules::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::global_forwarding_rules::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::global_forwarding_rules::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalForwardingRules::get][crate::client::GlobalForwardingRules::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_forwarding_rules::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::global_forwarding_rules::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalForwardingRules>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_forwarding_rules::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ForwardingRule> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [forwarding_rule][crate::model::global_forwarding_rules::GetRequest::forwarding_rule].
        pub fn set_forwarding_rule<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.forwarding_rule = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_forwarding_rules::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [view][crate::model::global_forwarding_rules::GetRequest::view].
        pub fn set_view<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::global_forwarding_rules::get_request::View>,
        {
            self.0.request.view = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [view][crate::model::global_forwarding_rules::GetRequest::view].
        pub fn set_or_clear_view<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::global_forwarding_rules::get_request::View>,
        {
            self.0.request.view = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalForwardingRules::insert][crate::client::GlobalForwardingRules::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_forwarding_rules::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::global_forwarding_rules::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalForwardingRules>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_forwarding_rules::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::GlobalForwardingRules::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::global_forwarding_rules::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::global_forwarding_rules::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::global_forwarding_rules::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::global_forwarding_rules::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ForwardingRule>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::global_forwarding_rules::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ForwardingRule>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalForwardingRules::list][crate::client::GlobalForwardingRules::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_forwarding_rules::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::global_forwarding_rules::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalForwardingRules>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_forwarding_rules::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ForwardingRuleList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::ForwardingRuleList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ForwardingRuleList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::global_forwarding_rules::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::global_forwarding_rules::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::global_forwarding_rules::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::global_forwarding_rules::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::global_forwarding_rules::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::global_forwarding_rules::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::global_forwarding_rules::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::global_forwarding_rules::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::global_forwarding_rules::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::global_forwarding_rules::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::global_forwarding_rules::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalForwardingRules::patch][crate::client::GlobalForwardingRules::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_forwarding_rules::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::global_forwarding_rules::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalForwardingRules>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_forwarding_rules::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::GlobalForwardingRules::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [forwarding_rule][crate::model::global_forwarding_rules::PatchRequest::forwarding_rule].
        pub fn set_forwarding_rule<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.forwarding_rule = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_forwarding_rules::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::global_forwarding_rules::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::global_forwarding_rules::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::global_forwarding_rules::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ForwardingRule>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::global_forwarding_rules::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ForwardingRule>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalForwardingRules::set_labels][crate::client::GlobalForwardingRules::set_labels] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_forwarding_rules::SetLabels;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetLabels {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetLabels(RequestBuilder<crate::model::global_forwarding_rules::SetLabelsRequest>);

    impl SetLabels {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalForwardingRules>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_forwarding_rules::SetLabelsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_labels(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_labels`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::GlobalForwardingRules::set_labels::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::global_forwarding_rules::SetLabelsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::global_forwarding_rules::SetLabelsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::global_forwarding_rules::SetLabelsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetLabelsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::global_forwarding_rules::SetLabelsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetLabelsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetLabels {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalForwardingRules::set_target][crate::client::GlobalForwardingRules::set_target] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_forwarding_rules::SetTarget;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetTarget {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetTarget(RequestBuilder<crate::model::global_forwarding_rules::SetTargetRequest>);

    impl SetTarget {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalForwardingRules>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_forwarding_rules::SetTargetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_target(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_target`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::GlobalForwardingRules::set_target::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [forwarding_rule][crate::model::global_forwarding_rules::SetTargetRequest::forwarding_rule].
        pub fn set_forwarding_rule<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.forwarding_rule = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_forwarding_rules::SetTargetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::global_forwarding_rules::SetTargetRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::global_forwarding_rules::SetTargetRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::global_forwarding_rules::SetTargetRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetReference>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::global_forwarding_rules::SetTargetRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetReference>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetTarget {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalForwardingRules::get_operation][crate::client::GlobalForwardingRules::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_forwarding_rules::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalForwardingRules>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [GlobalNetworkEndpointGroups][crate::client::GlobalNetworkEndpointGroups].
#[cfg(feature = "global-network-endpoint-groups")]
#[cfg_attr(docsrs, doc(cfg(feature = "global-network-endpoint-groups")))]
pub mod global_network_endpoint_groups {
    use crate::Result;

    /// A builder for [GlobalNetworkEndpointGroups][crate::client::GlobalNetworkEndpointGroups].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::global_network_endpoint_groups::ClientBuilder;
    /// # use client::GlobalNetworkEndpointGroups;
    /// let builder : ClientBuilder = GlobalNetworkEndpointGroups::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::GlobalNetworkEndpointGroups;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = GlobalNetworkEndpointGroups;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::GlobalNetworkEndpointGroups] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalNetworkEndpointGroups>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalNetworkEndpointGroups>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [GlobalNetworkEndpointGroups::attach_network_endpoints][crate::client::GlobalNetworkEndpointGroups::attach_network_endpoints] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_network_endpoint_groups::AttachNetworkEndpoints;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AttachNetworkEndpoints {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AttachNetworkEndpoints(
        RequestBuilder<crate::model::global_network_endpoint_groups::AttachNetworkEndpointsRequest>,
    );

    impl AttachNetworkEndpoints {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalNetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::global_network_endpoint_groups::AttachNetworkEndpointsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .attach_network_endpoints(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `attach_network_endpoints`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::GlobalNetworkEndpointGroups::attach_network_endpoints::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [network_endpoint_group][crate::model::global_network_endpoint_groups::AttachNetworkEndpointsRequest::network_endpoint_group].
        pub fn set_network_endpoint_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network_endpoint_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_network_endpoint_groups::AttachNetworkEndpointsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::global_network_endpoint_groups::AttachNetworkEndpointsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::global_network_endpoint_groups::AttachNetworkEndpointsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::global_network_endpoint_groups::AttachNetworkEndpointsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::GlobalNetworkEndpointGroupsAttachEndpointsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::global_network_endpoint_groups::AttachNetworkEndpointsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::GlobalNetworkEndpointGroupsAttachEndpointsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AttachNetworkEndpoints {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalNetworkEndpointGroups::delete][crate::client::GlobalNetworkEndpointGroups::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_network_endpoint_groups::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::global_network_endpoint_groups::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalNetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::global_network_endpoint_groups::DeleteRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::GlobalNetworkEndpointGroups::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [network_endpoint_group][crate::model::global_network_endpoint_groups::DeleteRequest::network_endpoint_group].
        pub fn set_network_endpoint_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network_endpoint_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_network_endpoint_groups::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::global_network_endpoint_groups::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::global_network_endpoint_groups::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalNetworkEndpointGroups::detach_network_endpoints][crate::client::GlobalNetworkEndpointGroups::detach_network_endpoints] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_network_endpoint_groups::DetachNetworkEndpoints;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DetachNetworkEndpoints {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DetachNetworkEndpoints(
        RequestBuilder<crate::model::global_network_endpoint_groups::DetachNetworkEndpointsRequest>,
    );

    impl DetachNetworkEndpoints {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalNetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::global_network_endpoint_groups::DetachNetworkEndpointsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .detach_network_endpoints(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `detach_network_endpoints`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::GlobalNetworkEndpointGroups::detach_network_endpoints::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [network_endpoint_group][crate::model::global_network_endpoint_groups::DetachNetworkEndpointsRequest::network_endpoint_group].
        pub fn set_network_endpoint_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network_endpoint_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_network_endpoint_groups::DetachNetworkEndpointsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::global_network_endpoint_groups::DetachNetworkEndpointsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::global_network_endpoint_groups::DetachNetworkEndpointsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::global_network_endpoint_groups::DetachNetworkEndpointsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::GlobalNetworkEndpointGroupsDetachEndpointsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::global_network_endpoint_groups::DetachNetworkEndpointsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::GlobalNetworkEndpointGroupsDetachEndpointsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DetachNetworkEndpoints {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalNetworkEndpointGroups::get][crate::client::GlobalNetworkEndpointGroups::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_network_endpoint_groups::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::global_network_endpoint_groups::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalNetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_network_endpoint_groups::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NetworkEndpointGroup> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [network_endpoint_group][crate::model::global_network_endpoint_groups::GetRequest::network_endpoint_group].
        pub fn set_network_endpoint_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network_endpoint_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_network_endpoint_groups::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalNetworkEndpointGroups::insert][crate::client::GlobalNetworkEndpointGroups::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_network_endpoint_groups::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::global_network_endpoint_groups::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalNetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::global_network_endpoint_groups::InsertRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::GlobalNetworkEndpointGroups::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::global_network_endpoint_groups::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::global_network_endpoint_groups::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::global_network_endpoint_groups::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::global_network_endpoint_groups::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NetworkEndpointGroup>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::global_network_endpoint_groups::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NetworkEndpointGroup>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalNetworkEndpointGroups::list][crate::client::GlobalNetworkEndpointGroups::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_network_endpoint_groups::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::global_network_endpoint_groups::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalNetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_network_endpoint_groups::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NetworkEndpointGroupList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::NetworkEndpointGroupList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::NetworkEndpointGroupList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::global_network_endpoint_groups::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::global_network_endpoint_groups::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::global_network_endpoint_groups::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::global_network_endpoint_groups::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::global_network_endpoint_groups::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::global_network_endpoint_groups::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::global_network_endpoint_groups::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::global_network_endpoint_groups::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::global_network_endpoint_groups::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::global_network_endpoint_groups::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::global_network_endpoint_groups::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalNetworkEndpointGroups::list_network_endpoints][crate::client::GlobalNetworkEndpointGroups::list_network_endpoints] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_network_endpoint_groups::ListNetworkEndpoints;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListNetworkEndpoints {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListNetworkEndpoints(
        RequestBuilder<crate::model::global_network_endpoint_groups::ListNetworkEndpointsRequest>,
    );

    impl ListNetworkEndpoints {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalNetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::global_network_endpoint_groups::ListNetworkEndpointsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NetworkEndpointGroupsListNetworkEndpoints> {
            (*self.0.stub)
                .list_network_endpoints(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::NetworkEndpointGroupsListNetworkEndpoints,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::NetworkEndpointGroupsListNetworkEndpoints,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::global_network_endpoint_groups::ListNetworkEndpointsRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::global_network_endpoint_groups::ListNetworkEndpointsRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::global_network_endpoint_groups::ListNetworkEndpointsRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::global_network_endpoint_groups::ListNetworkEndpointsRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [network_endpoint_group][crate::model::global_network_endpoint_groups::ListNetworkEndpointsRequest::network_endpoint_group].
        pub fn set_network_endpoint_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network_endpoint_group = v.into();
            self
        }

        /// Sets the value of [order_by][crate::model::global_network_endpoint_groups::ListNetworkEndpointsRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::global_network_endpoint_groups::ListNetworkEndpointsRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::global_network_endpoint_groups::ListNetworkEndpointsRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::global_network_endpoint_groups::ListNetworkEndpointsRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::global_network_endpoint_groups::ListNetworkEndpointsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::global_network_endpoint_groups::ListNetworkEndpointsRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::global_network_endpoint_groups::ListNetworkEndpointsRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListNetworkEndpoints {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalNetworkEndpointGroups::get_operation][crate::client::GlobalNetworkEndpointGroups::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_network_endpoint_groups::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalNetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [GlobalOperations][crate::client::GlobalOperations].
#[cfg(feature = "global-operations")]
#[cfg_attr(docsrs, doc(cfg(feature = "global-operations")))]
pub mod global_operations {
    use crate::Result;

    /// A builder for [GlobalOperations][crate::client::GlobalOperations].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::global_operations::ClientBuilder;
    /// # use client::GlobalOperations;
    /// let builder : ClientBuilder = GlobalOperations::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::GlobalOperations;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = GlobalOperations;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::GlobalOperations] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalOperations>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalOperations>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [GlobalOperations::aggregated_list][crate::client::GlobalOperations::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_operations::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::global_operations::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalOperations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::OperationAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::OperationAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::OperationAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::global_operations::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::global_operations::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::global_operations::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::global_operations::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::global_operations::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::global_operations::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::global_operations::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::global_operations::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::global_operations::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::global_operations::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::global_operations::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::global_operations::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::global_operations::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::global_operations::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::global_operations::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalOperations::delete][crate::client::GlobalOperations::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_operations::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::global_operations::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalOperations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<()> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::DeleteRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalOperations::get][crate::client::GlobalOperations::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_operations::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalOperations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalOperations::list][crate::client::GlobalOperations::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_operations::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::global_operations::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalOperations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::OperationList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::OperationList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::OperationList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::global_operations::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::global_operations::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::global_operations::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::global_operations::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::global_operations::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::global_operations::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::global_operations::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::global_operations::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::global_operations::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::global_operations::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::global_operations::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalOperations::wait][crate::client::GlobalOperations::wait] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_operations::Wait;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Wait {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Wait(RequestBuilder<crate::model::global_operations::WaitRequest>);

    impl Wait {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalOperations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::WaitRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .wait(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::WaitRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::WaitRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Wait {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [GlobalOrganizationOperations][crate::client::GlobalOrganizationOperations].
#[cfg(feature = "global-organization-operations")]
#[cfg_attr(docsrs, doc(cfg(feature = "global-organization-operations")))]
pub mod global_organization_operations {
    use crate::Result;

    /// A builder for [GlobalOrganizationOperations][crate::client::GlobalOrganizationOperations].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::global_organization_operations::ClientBuilder;
    /// # use client::GlobalOrganizationOperations;
    /// let builder : ClientBuilder = GlobalOrganizationOperations::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::GlobalOrganizationOperations;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = GlobalOrganizationOperations;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::GlobalOrganizationOperations] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalOrganizationOperations>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalOrganizationOperations>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [GlobalOrganizationOperations::delete][crate::client::GlobalOrganizationOperations::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_organization_operations::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::global_organization_operations::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalOrganizationOperations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::global_organization_operations::DeleteRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<()> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_organization_operations::DeleteRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [parent_id][crate::model::global_organization_operations::DeleteRequest::parent_id].
        pub fn set_parent_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.parent_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [parent_id][crate::model::global_organization_operations::DeleteRequest::parent_id].
        pub fn set_or_clear_parent_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.parent_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalOrganizationOperations::get][crate::client::GlobalOrganizationOperations::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_organization_operations::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::global_organization_operations::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalOrganizationOperations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_organization_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_organization_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [parent_id][crate::model::global_organization_operations::GetRequest::parent_id].
        pub fn set_parent_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.parent_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [parent_id][crate::model::global_organization_operations::GetRequest::parent_id].
        pub fn set_or_clear_parent_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.parent_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalOrganizationOperations::list][crate::client::GlobalOrganizationOperations::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_organization_operations::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::global_organization_operations::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalOrganizationOperations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_organization_operations::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::OperationList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::OperationList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::OperationList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::global_organization_operations::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::global_organization_operations::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::global_organization_operations::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::global_organization_operations::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::global_organization_operations::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::global_organization_operations::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::global_organization_operations::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::global_organization_operations::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [parent_id][crate::model::global_organization_operations::ListRequest::parent_id].
        pub fn set_parent_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.parent_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [parent_id][crate::model::global_organization_operations::ListRequest::parent_id].
        pub fn set_or_clear_parent_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.parent_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [return_partial_success][crate::model::global_organization_operations::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::global_organization_operations::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [GlobalPublicDelegatedPrefixes][crate::client::GlobalPublicDelegatedPrefixes].
#[cfg(feature = "global-public-delegated-prefixes")]
#[cfg_attr(docsrs, doc(cfg(feature = "global-public-delegated-prefixes")))]
pub mod global_public_delegated_prefixes {
    use crate::Result;

    /// A builder for [GlobalPublicDelegatedPrefixes][crate::client::GlobalPublicDelegatedPrefixes].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::global_public_delegated_prefixes::ClientBuilder;
    /// # use client::GlobalPublicDelegatedPrefixes;
    /// let builder : ClientBuilder = GlobalPublicDelegatedPrefixes::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::GlobalPublicDelegatedPrefixes;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = GlobalPublicDelegatedPrefixes;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::GlobalPublicDelegatedPrefixes] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalPublicDelegatedPrefixes>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalPublicDelegatedPrefixes>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [GlobalPublicDelegatedPrefixes::delete][crate::client::GlobalPublicDelegatedPrefixes::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_public_delegated_prefixes::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(
        RequestBuilder<crate::model::global_public_delegated_prefixes::DeleteRequest>,
    );

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalPublicDelegatedPrefixes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::global_public_delegated_prefixes::DeleteRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::GlobalPublicDelegatedPrefixes::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::global_public_delegated_prefixes::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [public_delegated_prefix][crate::model::global_public_delegated_prefixes::DeleteRequest::public_delegated_prefix].
        pub fn set_public_delegated_prefix<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.public_delegated_prefix = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::global_public_delegated_prefixes::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::global_public_delegated_prefixes::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalPublicDelegatedPrefixes::get][crate::client::GlobalPublicDelegatedPrefixes::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_public_delegated_prefixes::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::global_public_delegated_prefixes::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalPublicDelegatedPrefixes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_public_delegated_prefixes::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::PublicDelegatedPrefix> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::global_public_delegated_prefixes::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [public_delegated_prefix][crate::model::global_public_delegated_prefixes::GetRequest::public_delegated_prefix].
        pub fn set_public_delegated_prefix<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.public_delegated_prefix = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalPublicDelegatedPrefixes::insert][crate::client::GlobalPublicDelegatedPrefixes::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_public_delegated_prefixes::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(
        RequestBuilder<crate::model::global_public_delegated_prefixes::InsertRequest>,
    );

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalPublicDelegatedPrefixes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::global_public_delegated_prefixes::InsertRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::GlobalPublicDelegatedPrefixes::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::global_public_delegated_prefixes::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::global_public_delegated_prefixes::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::global_public_delegated_prefixes::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::global_public_delegated_prefixes::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::PublicDelegatedPrefix>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::global_public_delegated_prefixes::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::PublicDelegatedPrefix>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalPublicDelegatedPrefixes::list][crate::client::GlobalPublicDelegatedPrefixes::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_public_delegated_prefixes::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::global_public_delegated_prefixes::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalPublicDelegatedPrefixes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::global_public_delegated_prefixes::ListRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::PublicDelegatedPrefixList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::PublicDelegatedPrefixList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::PublicDelegatedPrefixList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::global_public_delegated_prefixes::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::global_public_delegated_prefixes::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::global_public_delegated_prefixes::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::global_public_delegated_prefixes::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::global_public_delegated_prefixes::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::global_public_delegated_prefixes::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::global_public_delegated_prefixes::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::global_public_delegated_prefixes::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::global_public_delegated_prefixes::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::global_public_delegated_prefixes::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::global_public_delegated_prefixes::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalPublicDelegatedPrefixes::patch][crate::client::GlobalPublicDelegatedPrefixes::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_public_delegated_prefixes::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::global_public_delegated_prefixes::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalPublicDelegatedPrefixes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::global_public_delegated_prefixes::PatchRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::GlobalPublicDelegatedPrefixes::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::global_public_delegated_prefixes::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [public_delegated_prefix][crate::model::global_public_delegated_prefixes::PatchRequest::public_delegated_prefix].
        pub fn set_public_delegated_prefix<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.public_delegated_prefix = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::global_public_delegated_prefixes::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::global_public_delegated_prefixes::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::global_public_delegated_prefixes::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::PublicDelegatedPrefix>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::global_public_delegated_prefixes::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::PublicDelegatedPrefix>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalPublicDelegatedPrefixes::get_operation][crate::client::GlobalPublicDelegatedPrefixes::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_public_delegated_prefixes::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalPublicDelegatedPrefixes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [GlobalVmExtensionPolicies][crate::client::GlobalVmExtensionPolicies].
#[cfg(feature = "global-vm-extension-policies")]
#[cfg_attr(docsrs, doc(cfg(feature = "global-vm-extension-policies")))]
pub mod global_vm_extension_policies {
    use crate::Result;

    /// A builder for [GlobalVmExtensionPolicies][crate::client::GlobalVmExtensionPolicies].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::global_vm_extension_policies::ClientBuilder;
    /// # use client::GlobalVmExtensionPolicies;
    /// let builder : ClientBuilder = GlobalVmExtensionPolicies::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::GlobalVmExtensionPolicies;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = GlobalVmExtensionPolicies;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::GlobalVmExtensionPolicies] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalVmExtensionPolicies>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalVmExtensionPolicies>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [GlobalVmExtensionPolicies::aggregated_list][crate::client::GlobalVmExtensionPolicies::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_vm_extension_policies::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::global_vm_extension_policies::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalVmExtensionPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::global_vm_extension_policies::AggregatedListRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::VmExtensionPolicyAggregatedListResponse> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::VmExtensionPolicyAggregatedListResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::VmExtensionPolicyAggregatedListResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::global_vm_extension_policies::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::global_vm_extension_policies::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::global_vm_extension_policies::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::global_vm_extension_policies::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::global_vm_extension_policies::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::global_vm_extension_policies::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::global_vm_extension_policies::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::global_vm_extension_policies::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::global_vm_extension_policies::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::global_vm_extension_policies::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::global_vm_extension_policies::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::global_vm_extension_policies::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::global_vm_extension_policies::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::global_vm_extension_policies::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::global_vm_extension_policies::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalVmExtensionPolicies::delete][crate::client::GlobalVmExtensionPolicies::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_vm_extension_policies::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::global_vm_extension_policies::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalVmExtensionPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_vm_extension_policies::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::GlobalVmExtensionPolicies::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [global_vm_extension_policy][crate::model::global_vm_extension_policies::DeleteRequest::global_vm_extension_policy].
        pub fn set_global_vm_extension_policy<T: Into<std::string::String>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.global_vm_extension_policy = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_vm_extension_policies::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::global_vm_extension_policies::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::global_vm_extension_policies::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::global_vm_extension_policies::DeleteRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<
                    crate::model::GlobalVmExtensionPolicyRolloutOperationRolloutInput,
                >,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::global_vm_extension_policies::DeleteRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<
                    crate::model::GlobalVmExtensionPolicyRolloutOperationRolloutInput,
                >,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalVmExtensionPolicies::get][crate::client::GlobalVmExtensionPolicies::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_vm_extension_policies::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::global_vm_extension_policies::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalVmExtensionPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_vm_extension_policies::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::GlobalVmExtensionPolicy> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [global_vm_extension_policy][crate::model::global_vm_extension_policies::GetRequest::global_vm_extension_policy].
        pub fn set_global_vm_extension_policy<T: Into<std::string::String>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.global_vm_extension_policy = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_vm_extension_policies::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalVmExtensionPolicies::insert][crate::client::GlobalVmExtensionPolicies::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_vm_extension_policies::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::global_vm_extension_policies::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalVmExtensionPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_vm_extension_policies::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::GlobalVmExtensionPolicies::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::global_vm_extension_policies::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::global_vm_extension_policies::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::global_vm_extension_policies::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::global_vm_extension_policies::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::GlobalVmExtensionPolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::global_vm_extension_policies::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::GlobalVmExtensionPolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalVmExtensionPolicies::list][crate::client::GlobalVmExtensionPolicies::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_vm_extension_policies::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::global_vm_extension_policies::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalVmExtensionPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_vm_extension_policies::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::GlobalVmExtensionPolicyList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::GlobalVmExtensionPolicyList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::GlobalVmExtensionPolicyList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::global_vm_extension_policies::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::global_vm_extension_policies::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::global_vm_extension_policies::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::global_vm_extension_policies::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::global_vm_extension_policies::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::global_vm_extension_policies::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::global_vm_extension_policies::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::global_vm_extension_policies::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::global_vm_extension_policies::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::global_vm_extension_policies::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::global_vm_extension_policies::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalVmExtensionPolicies::update][crate::client::GlobalVmExtensionPolicies::update] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_vm_extension_policies::Update;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Update {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Update(RequestBuilder<crate::model::global_vm_extension_policies::UpdateRequest>);

    impl Update {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalVmExtensionPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_vm_extension_policies::UpdateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::GlobalVmExtensionPolicies::update::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [global_vm_extension_policy][crate::model::global_vm_extension_policies::UpdateRequest::global_vm_extension_policy].
        pub fn set_global_vm_extension_policy<T: Into<std::string::String>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.global_vm_extension_policy = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_vm_extension_policies::UpdateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::global_vm_extension_policies::UpdateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::global_vm_extension_policies::UpdateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::global_vm_extension_policies::UpdateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::GlobalVmExtensionPolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::global_vm_extension_policies::UpdateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::GlobalVmExtensionPolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Update {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [GlobalVmExtensionPolicies::get_operation][crate::client::GlobalVmExtensionPolicies::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::global_vm_extension_policies::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::GlobalVmExtensionPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [HealthChecks][crate::client::HealthChecks].
#[cfg(feature = "health-checks")]
#[cfg_attr(docsrs, doc(cfg(feature = "health-checks")))]
pub mod health_checks {
    use crate::Result;

    /// A builder for [HealthChecks][crate::client::HealthChecks].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::health_checks::ClientBuilder;
    /// # use client::HealthChecks;
    /// let builder : ClientBuilder = HealthChecks::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::HealthChecks;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = HealthChecks;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::HealthChecks] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::HealthChecks>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HealthChecks>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [HealthChecks::aggregated_list][crate::client::HealthChecks::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::health_checks::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(RequestBuilder<crate::model::health_checks::AggregatedListRequest>);

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::health_checks::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::HealthChecksAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::HealthChecksAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::HealthChecksAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::health_checks::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::health_checks::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::health_checks::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::health_checks::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::health_checks::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::health_checks::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::health_checks::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::health_checks::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::health_checks::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::health_checks::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::health_checks::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::health_checks::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::health_checks::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::health_checks::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::health_checks::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HealthChecks::delete][crate::client::HealthChecks::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::health_checks::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::health_checks::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::health_checks::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::HealthChecks::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [health_check][crate::model::health_checks::DeleteRequest::health_check].
        pub fn set_health_check<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.health_check = v.into();
            self
        }

        /// Sets the value of [project][crate::model::health_checks::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::health_checks::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::health_checks::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HealthChecks::get][crate::client::HealthChecks::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::health_checks::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::health_checks::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::health_checks::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::HealthCheck> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [health_check][crate::model::health_checks::GetRequest::health_check].
        pub fn set_health_check<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.health_check = v.into();
            self
        }

        /// Sets the value of [project][crate::model::health_checks::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HealthChecks::insert][crate::client::HealthChecks::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::health_checks::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::health_checks::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::health_checks::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::HealthChecks::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::health_checks::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::health_checks::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::health_checks::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::health_checks::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::HealthCheck>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::health_checks::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::HealthCheck>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HealthChecks::list][crate::client::HealthChecks::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::health_checks::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::health_checks::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::health_checks::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::HealthCheckList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::HealthCheckList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::HealthCheckList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::health_checks::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::health_checks::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::health_checks::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::health_checks::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::health_checks::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::health_checks::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::health_checks::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::health_checks::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::health_checks::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::health_checks::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::health_checks::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HealthChecks::patch][crate::client::HealthChecks::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::health_checks::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::health_checks::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::health_checks::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::HealthChecks::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [health_check][crate::model::health_checks::PatchRequest::health_check].
        pub fn set_health_check<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.health_check = v.into();
            self
        }

        /// Sets the value of [project][crate::model::health_checks::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::health_checks::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::health_checks::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::health_checks::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::HealthCheck>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::health_checks::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::HealthCheck>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HealthChecks::test_iam_permissions][crate::client::HealthChecks::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::health_checks::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::health_checks::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::health_checks::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::health_checks::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::health_checks::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::health_checks::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::health_checks::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HealthChecks::update][crate::client::HealthChecks::update] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::health_checks::Update;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Update {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Update(RequestBuilder<crate::model::health_checks::UpdateRequest>);

    impl Update {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::health_checks::UpdateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::HealthChecks::update::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [health_check][crate::model::health_checks::UpdateRequest::health_check].
        pub fn set_health_check<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.health_check = v.into();
            self
        }

        /// Sets the value of [project][crate::model::health_checks::UpdateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::health_checks::UpdateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::health_checks::UpdateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::health_checks::UpdateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::HealthCheck>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::health_checks::UpdateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::HealthCheck>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Update {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HealthChecks::get_operation][crate::client::HealthChecks::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::health_checks::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [HttpHealthChecks][crate::client::HttpHealthChecks].
#[cfg(feature = "http-health-checks")]
#[cfg_attr(docsrs, doc(cfg(feature = "http-health-checks")))]
pub mod http_health_checks {
    use crate::Result;

    /// A builder for [HttpHealthChecks][crate::client::HttpHealthChecks].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::http_health_checks::ClientBuilder;
    /// # use client::HttpHealthChecks;
    /// let builder : ClientBuilder = HttpHealthChecks::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::HttpHealthChecks;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = HttpHealthChecks;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::HttpHealthChecks] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::HttpHealthChecks>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HttpHealthChecks>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [HttpHealthChecks::delete][crate::client::HttpHealthChecks::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::http_health_checks::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::http_health_checks::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HttpHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::http_health_checks::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::HttpHealthChecks::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [http_health_check][crate::model::http_health_checks::DeleteRequest::http_health_check].
        pub fn set_http_health_check<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.http_health_check = v.into();
            self
        }

        /// Sets the value of [project][crate::model::http_health_checks::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::http_health_checks::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::http_health_checks::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HttpHealthChecks::get][crate::client::HttpHealthChecks::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::http_health_checks::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::http_health_checks::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HttpHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::http_health_checks::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::HttpHealthCheck> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [http_health_check][crate::model::http_health_checks::GetRequest::http_health_check].
        pub fn set_http_health_check<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.http_health_check = v.into();
            self
        }

        /// Sets the value of [project][crate::model::http_health_checks::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HttpHealthChecks::insert][crate::client::HttpHealthChecks::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::http_health_checks::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::http_health_checks::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HttpHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::http_health_checks::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::HttpHealthChecks::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::http_health_checks::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::http_health_checks::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::http_health_checks::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::http_health_checks::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::HttpHealthCheck>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::http_health_checks::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::HttpHealthCheck>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HttpHealthChecks::list][crate::client::HttpHealthChecks::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::http_health_checks::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::http_health_checks::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HttpHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::http_health_checks::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::HttpHealthCheckList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::HttpHealthCheckList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::HttpHealthCheckList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::http_health_checks::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::http_health_checks::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::http_health_checks::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::http_health_checks::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::http_health_checks::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::http_health_checks::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::http_health_checks::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::http_health_checks::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::http_health_checks::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::http_health_checks::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::http_health_checks::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HttpHealthChecks::patch][crate::client::HttpHealthChecks::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::http_health_checks::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::http_health_checks::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HttpHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::http_health_checks::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::HttpHealthChecks::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [http_health_check][crate::model::http_health_checks::PatchRequest::http_health_check].
        pub fn set_http_health_check<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.http_health_check = v.into();
            self
        }

        /// Sets the value of [project][crate::model::http_health_checks::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::http_health_checks::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::http_health_checks::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::http_health_checks::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::HttpHealthCheck>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::http_health_checks::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::HttpHealthCheck>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HttpHealthChecks::test_iam_permissions][crate::client::HttpHealthChecks::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::http_health_checks::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::http_health_checks::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HttpHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::http_health_checks::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::http_health_checks::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::http_health_checks::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::http_health_checks::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::http_health_checks::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HttpHealthChecks::update][crate::client::HttpHealthChecks::update] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::http_health_checks::Update;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Update {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Update(RequestBuilder<crate::model::http_health_checks::UpdateRequest>);

    impl Update {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HttpHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::http_health_checks::UpdateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::HttpHealthChecks::update::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [http_health_check][crate::model::http_health_checks::UpdateRequest::http_health_check].
        pub fn set_http_health_check<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.http_health_check = v.into();
            self
        }

        /// Sets the value of [project][crate::model::http_health_checks::UpdateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::http_health_checks::UpdateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::http_health_checks::UpdateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::http_health_checks::UpdateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::HttpHealthCheck>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::http_health_checks::UpdateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::HttpHealthCheck>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Update {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HttpHealthChecks::get_operation][crate::client::HttpHealthChecks::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::http_health_checks::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HttpHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [HttpsHealthChecks][crate::client::HttpsHealthChecks].
#[cfg(feature = "https-health-checks")]
#[cfg_attr(docsrs, doc(cfg(feature = "https-health-checks")))]
pub mod https_health_checks {
    use crate::Result;

    /// A builder for [HttpsHealthChecks][crate::client::HttpsHealthChecks].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::https_health_checks::ClientBuilder;
    /// # use client::HttpsHealthChecks;
    /// let builder : ClientBuilder = HttpsHealthChecks::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::HttpsHealthChecks;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = HttpsHealthChecks;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::HttpsHealthChecks] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::HttpsHealthChecks>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HttpsHealthChecks>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [HttpsHealthChecks::delete][crate::client::HttpsHealthChecks::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::https_health_checks::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::https_health_checks::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HttpsHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::https_health_checks::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::HttpsHealthChecks::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [https_health_check][crate::model::https_health_checks::DeleteRequest::https_health_check].
        pub fn set_https_health_check<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.https_health_check = v.into();
            self
        }

        /// Sets the value of [project][crate::model::https_health_checks::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::https_health_checks::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::https_health_checks::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HttpsHealthChecks::get][crate::client::HttpsHealthChecks::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::https_health_checks::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::https_health_checks::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HttpsHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::https_health_checks::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::HttpsHealthCheck> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [https_health_check][crate::model::https_health_checks::GetRequest::https_health_check].
        pub fn set_https_health_check<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.https_health_check = v.into();
            self
        }

        /// Sets the value of [project][crate::model::https_health_checks::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HttpsHealthChecks::insert][crate::client::HttpsHealthChecks::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::https_health_checks::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::https_health_checks::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HttpsHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::https_health_checks::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::HttpsHealthChecks::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::https_health_checks::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::https_health_checks::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::https_health_checks::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::https_health_checks::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::HttpsHealthCheck>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::https_health_checks::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::HttpsHealthCheck>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HttpsHealthChecks::list][crate::client::HttpsHealthChecks::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::https_health_checks::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::https_health_checks::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HttpsHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::https_health_checks::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::HttpsHealthCheckList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::HttpsHealthCheckList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::HttpsHealthCheckList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::https_health_checks::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::https_health_checks::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::https_health_checks::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::https_health_checks::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::https_health_checks::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::https_health_checks::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::https_health_checks::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::https_health_checks::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::https_health_checks::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::https_health_checks::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::https_health_checks::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HttpsHealthChecks::patch][crate::client::HttpsHealthChecks::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::https_health_checks::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::https_health_checks::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HttpsHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::https_health_checks::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::HttpsHealthChecks::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [https_health_check][crate::model::https_health_checks::PatchRequest::https_health_check].
        pub fn set_https_health_check<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.https_health_check = v.into();
            self
        }

        /// Sets the value of [project][crate::model::https_health_checks::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::https_health_checks::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::https_health_checks::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::https_health_checks::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::HttpsHealthCheck>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::https_health_checks::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::HttpsHealthCheck>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HttpsHealthChecks::test_iam_permissions][crate::client::HttpsHealthChecks::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::https_health_checks::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::https_health_checks::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HttpsHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::https_health_checks::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::https_health_checks::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::https_health_checks::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::https_health_checks::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::https_health_checks::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HttpsHealthChecks::update][crate::client::HttpsHealthChecks::update] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::https_health_checks::Update;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Update {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Update(RequestBuilder<crate::model::https_health_checks::UpdateRequest>);

    impl Update {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HttpsHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::https_health_checks::UpdateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::HttpsHealthChecks::update::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [https_health_check][crate::model::https_health_checks::UpdateRequest::https_health_check].
        pub fn set_https_health_check<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.https_health_check = v.into();
            self
        }

        /// Sets the value of [project][crate::model::https_health_checks::UpdateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::https_health_checks::UpdateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::https_health_checks::UpdateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::https_health_checks::UpdateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::HttpsHealthCheck>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::https_health_checks::UpdateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::HttpsHealthCheck>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Update {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [HttpsHealthChecks::get_operation][crate::client::HttpsHealthChecks::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::https_health_checks::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::HttpsHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [ImageFamilyViews][crate::client::ImageFamilyViews].
#[cfg(feature = "image-family-views")]
#[cfg_attr(docsrs, doc(cfg(feature = "image-family-views")))]
pub mod image_family_views {
    use crate::Result;

    /// A builder for [ImageFamilyViews][crate::client::ImageFamilyViews].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::image_family_views::ClientBuilder;
    /// # use client::ImageFamilyViews;
    /// let builder : ClientBuilder = ImageFamilyViews::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::ImageFamilyViews;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = ImageFamilyViews;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::ImageFamilyViews] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::ImageFamilyViews>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ImageFamilyViews>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [ImageFamilyViews::get][crate::client::ImageFamilyViews::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::image_family_views::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::image_family_views::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ImageFamilyViews>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::image_family_views::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ImageFamilyView> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [family][crate::model::image_family_views::GetRequest::family].
        pub fn set_family<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.family = v.into();
            self
        }

        /// Sets the value of [project][crate::model::image_family_views::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::image_family_views::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [Images][crate::client::Images].
#[cfg(feature = "images")]
#[cfg_attr(docsrs, doc(cfg(feature = "images")))]
pub mod images {
    use crate::Result;

    /// A builder for [Images][crate::client::Images].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::images::ClientBuilder;
    /// # use client::Images;
    /// let builder : ClientBuilder = Images::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::Images;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = Images;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::Images] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::Images>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Images>) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [Images::delete][crate::client::Images::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::images::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::images::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Images>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::images::DeleteRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Images::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [image][crate::model::images::DeleteRequest::image].
        pub fn set_image<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.image = v.into();
            self
        }

        /// Sets the value of [project][crate::model::images::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::images::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::images::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Images::deprecate][crate::client::Images::deprecate] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::images::Deprecate;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Deprecate {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Deprecate(RequestBuilder<crate::model::images::DeprecateRequest>);

    impl Deprecate {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Images>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::images::DeprecateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .deprecate(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `deprecate`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Images::deprecate::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [image][crate::model::images::DeprecateRequest::image].
        pub fn set_image<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.image = v.into();
            self
        }

        /// Sets the value of [project][crate::model::images::DeprecateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::images::DeprecateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::images::DeprecateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::images::DeprecateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DeprecationStatus>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::images::DeprecateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DeprecationStatus>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Deprecate {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Images::get][crate::client::Images::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::images::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::images::GetRequest>);

    impl Get {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Images>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::images::GetRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Image> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [image][crate::model::images::GetRequest::image].
        pub fn set_image<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.image = v.into();
            self
        }

        /// Sets the value of [project][crate::model::images::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Images::get_from_family][crate::client::Images::get_from_family] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::images::GetFromFamily;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetFromFamily {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetFromFamily(RequestBuilder<crate::model::images::GetFromFamilyRequest>);

    impl GetFromFamily {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Images>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::images::GetFromFamilyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Image> {
            (*self.0.stub)
                .get_from_family(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [family][crate::model::images::GetFromFamilyRequest::family].
        pub fn set_family<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.family = v.into();
            self
        }

        /// Sets the value of [project][crate::model::images::GetFromFamilyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetFromFamily {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Images::get_iam_policy][crate::client::Images::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::images::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(RequestBuilder<crate::model::images::GetIamPolicyRequest>);

    impl GetIamPolicy {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Images>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::images::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::images::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::images::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::images::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::images::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Images::insert][crate::client::Images::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::images::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::images::InsertRequest>);

    impl Insert {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Images>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::images::InsertRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Images::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [force_create][crate::model::images::InsertRequest::force_create].
        pub fn set_force_create<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.force_create = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [force_create][crate::model::images::InsertRequest::force_create].
        pub fn set_or_clear_force_create<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.force_create = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::images::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::images::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::images::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::images::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Image>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::images::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Image>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Images::list][crate::client::Images::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::images::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::images::ListRequest>);

    impl List {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Images>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::images::ListRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ImageList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::ImageList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::ImageList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::images::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::images::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::images::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::images::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::images::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::images::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::images::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::images::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::images::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::images::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::images::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Images::patch][crate::client::Images::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::images::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::images::PatchRequest>);

    impl Patch {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Images>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::images::PatchRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Images::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [image][crate::model::images::PatchRequest::image].
        pub fn set_image<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.image = v.into();
            self
        }

        /// Sets the value of [project][crate::model::images::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::images::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::images::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::images::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Image>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::images::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Image>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Images::set_iam_policy][crate::client::Images::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::images::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(RequestBuilder<crate::model::images::SetIamPolicyRequest>);

    impl SetIamPolicy {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Images>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::images::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::images::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::images::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::images::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::images::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Images::set_labels][crate::client::Images::set_labels] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::images::SetLabels;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetLabels {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetLabels(RequestBuilder<crate::model::images::SetLabelsRequest>);

    impl SetLabels {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Images>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::images::SetLabelsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_labels(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_labels`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Images::set_labels::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::images::SetLabelsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::images::SetLabelsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::images::SetLabelsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetLabelsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::images::SetLabelsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetLabelsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetLabels {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Images::test_iam_permissions][crate::client::Images::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::images::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(RequestBuilder<crate::model::images::TestIamPermissionsRequest>);

    impl TestIamPermissions {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Images>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::images::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::images::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::images::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::images::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::images::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Images::get_operation][crate::client::Images::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::images::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Images>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [InstanceGroupManagerResizeRequests][crate::client::InstanceGroupManagerResizeRequests].
#[cfg(feature = "instance-group-manager-resize-requests")]
#[cfg_attr(docsrs, doc(cfg(feature = "instance-group-manager-resize-requests")))]
pub mod instance_group_manager_resize_requests {
    use crate::Result;

    /// A builder for [InstanceGroupManagerResizeRequests][crate::client::InstanceGroupManagerResizeRequests].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::instance_group_manager_resize_requests::ClientBuilder;
    /// # use client::InstanceGroupManagerResizeRequests;
    /// let builder : ClientBuilder = InstanceGroupManagerResizeRequests::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::InstanceGroupManagerResizeRequests;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = InstanceGroupManagerResizeRequests;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::InstanceGroupManagerResizeRequests] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagerResizeRequests>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<
                dyn super::super::stub::dynamic::InstanceGroupManagerResizeRequests,
            >,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [InstanceGroupManagerResizeRequests::cancel][crate::client::InstanceGroupManagerResizeRequests::cancel] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_manager_resize_requests::Cancel;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Cancel {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Cancel(
        RequestBuilder<crate::model::instance_group_manager_resize_requests::CancelRequest>,
    );

    impl Cancel {
        pub(crate) fn new(
            stub: std::sync::Arc<
                dyn super::super::stub::dynamic::InstanceGroupManagerResizeRequests,
            >,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instance_group_manager_resize_requests::CancelRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .cancel(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `cancel`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::InstanceGroupManagerResizeRequests::cancel::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_manager_resize_requests::CancelRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instance_group_manager_resize_requests::CancelRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_group_manager_resize_requests::CancelRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_group_manager_resize_requests::CancelRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [resize_request][crate::model::instance_group_manager_resize_requests::CancelRequest::resize_request].
        pub fn set_resize_request<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resize_request = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_manager_resize_requests::CancelRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Cancel {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagerResizeRequests::delete][crate::client::InstanceGroupManagerResizeRequests::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_manager_resize_requests::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(
        RequestBuilder<crate::model::instance_group_manager_resize_requests::DeleteRequest>,
    );

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<
                dyn super::super::stub::dynamic::InstanceGroupManagerResizeRequests,
            >,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instance_group_manager_resize_requests::DeleteRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::InstanceGroupManagerResizeRequests::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_manager_resize_requests::DeleteRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instance_group_manager_resize_requests::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_group_manager_resize_requests::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_group_manager_resize_requests::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [resize_request][crate::model::instance_group_manager_resize_requests::DeleteRequest::resize_request].
        pub fn set_resize_request<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resize_request = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_manager_resize_requests::DeleteRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagerResizeRequests::get][crate::client::InstanceGroupManagerResizeRequests::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_manager_resize_requests::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(
        RequestBuilder<crate::model::instance_group_manager_resize_requests::GetRequest>,
    );

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<
                dyn super::super::stub::dynamic::InstanceGroupManagerResizeRequests,
            >,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instance_group_manager_resize_requests::GetRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstanceGroupManagerResizeRequest> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_manager_resize_requests::GetRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instance_group_manager_resize_requests::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resize_request][crate::model::instance_group_manager_resize_requests::GetRequest::resize_request].
        pub fn set_resize_request<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resize_request = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_manager_resize_requests::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagerResizeRequests::insert][crate::client::InstanceGroupManagerResizeRequests::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_manager_resize_requests::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(
        RequestBuilder<crate::model::instance_group_manager_resize_requests::InsertRequest>,
    );

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<
                dyn super::super::stub::dynamic::InstanceGroupManagerResizeRequests,
            >,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instance_group_manager_resize_requests::InsertRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::InstanceGroupManagerResizeRequests::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_manager_resize_requests::InsertRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instance_group_manager_resize_requests::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_group_manager_resize_requests::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_group_manager_resize_requests::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_manager_resize_requests::InsertRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_group_manager_resize_requests::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagerResizeRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_group_manager_resize_requests::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagerResizeRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagerResizeRequests::list][crate::client::InstanceGroupManagerResizeRequests::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_manager_resize_requests::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(
        RequestBuilder<crate::model::instance_group_manager_resize_requests::ListRequest>,
    );

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<
                dyn super::super::stub::dynamic::InstanceGroupManagerResizeRequests,
            >,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instance_group_manager_resize_requests::ListRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(
            self,
        ) -> Result<crate::model::InstanceGroupManagerResizeRequestsListResponse> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::InstanceGroupManagerResizeRequestsListResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::InstanceGroupManagerResizeRequestsListResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::instance_group_manager_resize_requests::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::instance_group_manager_resize_requests::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_manager_resize_requests::ListRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [max_results][crate::model::instance_group_manager_resize_requests::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::instance_group_manager_resize_requests::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::instance_group_manager_resize_requests::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::instance_group_manager_resize_requests::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::instance_group_manager_resize_requests::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::instance_group_manager_resize_requests::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instance_group_manager_resize_requests::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::instance_group_manager_resize_requests::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::instance_group_manager_resize_requests::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_manager_resize_requests::ListRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagerResizeRequests::get_operation][crate::client::InstanceGroupManagerResizeRequests::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_manager_resize_requests::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::zone_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<
                dyn super::super::stub::dynamic::InstanceGroupManagerResizeRequests,
            >,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::zone_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::zone_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zone_operations::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [InstanceGroupManagers][crate::client::InstanceGroupManagers].
#[cfg(feature = "instance-group-managers")]
#[cfg_attr(docsrs, doc(cfg(feature = "instance-group-managers")))]
pub mod instance_group_managers {
    use crate::Result;

    /// A builder for [InstanceGroupManagers][crate::client::InstanceGroupManagers].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::instance_group_managers::ClientBuilder;
    /// # use client::InstanceGroupManagers;
    /// let builder : ClientBuilder = InstanceGroupManagers::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::InstanceGroupManagers;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = InstanceGroupManagers;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::InstanceGroupManagers] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [InstanceGroupManagers::abandon_instances][crate::client::InstanceGroupManagers::abandon_instances] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_managers::AbandonInstances;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AbandonInstances {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AbandonInstances(
        RequestBuilder<crate::model::instance_group_managers::AbandonInstancesRequest>,
    );

    impl AbandonInstances {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instance_group_managers::AbandonInstancesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .abandon_instances(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `abandon_instances`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::InstanceGroupManagers::abandon_instances::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_managers::AbandonInstancesRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instance_group_managers::AbandonInstancesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_group_managers::AbandonInstancesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_group_managers::AbandonInstancesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_managers::AbandonInstancesRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_group_managers::AbandonInstancesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersAbandonInstancesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_group_managers::AbandonInstancesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersAbandonInstancesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AbandonInstances {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagers::aggregated_list][crate::client::InstanceGroupManagers::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_managers::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::instance_group_managers::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instance_group_managers::AggregatedListRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstanceGroupManagerAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::InstanceGroupManagerAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::InstanceGroupManagerAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::instance_group_managers::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::instance_group_managers::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::instance_group_managers::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::instance_group_managers::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::instance_group_managers::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::instance_group_managers::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::instance_group_managers::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::instance_group_managers::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::instance_group_managers::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::instance_group_managers::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instance_group_managers::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::instance_group_managers::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::instance_group_managers::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::instance_group_managers::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::instance_group_managers::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagers::apply_updates_to_instances][crate::client::InstanceGroupManagers::apply_updates_to_instances] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_managers::ApplyUpdatesToInstances;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ApplyUpdatesToInstances {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ApplyUpdatesToInstances(
        RequestBuilder<crate::model::instance_group_managers::ApplyUpdatesToInstancesRequest>,
    );

    impl ApplyUpdatesToInstances {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instance_group_managers::ApplyUpdatesToInstancesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .apply_updates_to_instances(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `apply_updates_to_instances`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::InstanceGroupManagers::apply_updates_to_instances::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_managers::ApplyUpdatesToInstancesRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instance_group_managers::ApplyUpdatesToInstancesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_managers::ApplyUpdatesToInstancesRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_group_managers::ApplyUpdatesToInstancesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersApplyUpdatesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_group_managers::ApplyUpdatesToInstancesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersApplyUpdatesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ApplyUpdatesToInstances {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagers::create_instances][crate::client::InstanceGroupManagers::create_instances] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_managers::CreateInstances;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CreateInstances {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CreateInstances(
        RequestBuilder<crate::model::instance_group_managers::CreateInstancesRequest>,
    );

    impl CreateInstances {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instance_group_managers::CreateInstancesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .create_instances(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `create_instances`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::InstanceGroupManagers::create_instances::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_managers::CreateInstancesRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instance_group_managers::CreateInstancesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_group_managers::CreateInstancesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_group_managers::CreateInstancesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_managers::CreateInstancesRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_group_managers::CreateInstancesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersCreateInstancesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_group_managers::CreateInstancesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersCreateInstancesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CreateInstances {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagers::delete][crate::client::InstanceGroupManagers::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_managers::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::instance_group_managers::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_group_managers::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::InstanceGroupManagers::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_managers::DeleteRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [no_graceful_shutdown][crate::model::instance_group_managers::DeleteRequest::no_graceful_shutdown].
        pub fn set_no_graceful_shutdown<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.no_graceful_shutdown = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [no_graceful_shutdown][crate::model::instance_group_managers::DeleteRequest::no_graceful_shutdown].
        pub fn set_or_clear_no_graceful_shutdown<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.no_graceful_shutdown = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instance_group_managers::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_group_managers::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_group_managers::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_managers::DeleteRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagers::delete_instances][crate::client::InstanceGroupManagers::delete_instances] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_managers::DeleteInstances;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeleteInstances {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeleteInstances(
        RequestBuilder<crate::model::instance_group_managers::DeleteInstancesRequest>,
    );

    impl DeleteInstances {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instance_group_managers::DeleteInstancesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete_instances(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete_instances`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::InstanceGroupManagers::delete_instances::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_managers::DeleteInstancesRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [no_graceful_shutdown][crate::model::instance_group_managers::DeleteInstancesRequest::no_graceful_shutdown].
        pub fn set_no_graceful_shutdown<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.no_graceful_shutdown = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [no_graceful_shutdown][crate::model::instance_group_managers::DeleteInstancesRequest::no_graceful_shutdown].
        pub fn set_or_clear_no_graceful_shutdown<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.no_graceful_shutdown = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instance_group_managers::DeleteInstancesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_group_managers::DeleteInstancesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_group_managers::DeleteInstancesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_managers::DeleteInstancesRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_group_managers::DeleteInstancesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersDeleteInstancesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_group_managers::DeleteInstancesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersDeleteInstancesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeleteInstances {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagers::delete_per_instance_configs][crate::client::InstanceGroupManagers::delete_per_instance_configs] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_managers::DeletePerInstanceConfigs;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeletePerInstanceConfigs {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeletePerInstanceConfigs(
        RequestBuilder<crate::model::instance_group_managers::DeletePerInstanceConfigsRequest>,
    );

    impl DeletePerInstanceConfigs {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instance_group_managers::DeletePerInstanceConfigsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete_per_instance_configs(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete_per_instance_configs`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::InstanceGroupManagers::delete_per_instance_configs::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_managers::DeletePerInstanceConfigsRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instance_group_managers::DeletePerInstanceConfigsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_managers::DeletePerInstanceConfigsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_group_managers::DeletePerInstanceConfigsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersDeletePerInstanceConfigsReq>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_group_managers::DeletePerInstanceConfigsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersDeletePerInstanceConfigsReq>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeletePerInstanceConfigs {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagers::get][crate::client::InstanceGroupManagers::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_managers::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::instance_group_managers::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_group_managers::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstanceGroupManager> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_managers::GetRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instance_group_managers::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_managers::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagers::insert][crate::client::InstanceGroupManagers::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_managers::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::instance_group_managers::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_group_managers::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::InstanceGroupManagers::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::instance_group_managers::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_group_managers::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_group_managers::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_managers::InsertRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_group_managers::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManager>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_group_managers::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManager>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagers::list][crate::client::InstanceGroupManagers::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_managers::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::instance_group_managers::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_group_managers::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstanceGroupManagerList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::InstanceGroupManagerList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::InstanceGroupManagerList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::instance_group_managers::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::instance_group_managers::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::instance_group_managers::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::instance_group_managers::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::instance_group_managers::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::instance_group_managers::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::instance_group_managers::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::instance_group_managers::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instance_group_managers::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::instance_group_managers::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::instance_group_managers::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_managers::ListRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagers::list_errors][crate::client::InstanceGroupManagers::list_errors] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_managers::ListErrors;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListErrors {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListErrors(RequestBuilder<crate::model::instance_group_managers::ListErrorsRequest>);

    impl ListErrors {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_group_managers::ListErrorsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstanceGroupManagersListErrorsResponse> {
            (*self.0.stub)
                .list_errors(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::InstanceGroupManagersListErrorsResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::InstanceGroupManagersListErrorsResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::instance_group_managers::ListErrorsRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::instance_group_managers::ListErrorsRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_managers::ListErrorsRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [max_results][crate::model::instance_group_managers::ListErrorsRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::instance_group_managers::ListErrorsRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::instance_group_managers::ListErrorsRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::instance_group_managers::ListErrorsRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::instance_group_managers::ListErrorsRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::instance_group_managers::ListErrorsRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instance_group_managers::ListErrorsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::instance_group_managers::ListErrorsRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::instance_group_managers::ListErrorsRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_managers::ListErrorsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListErrors {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagers::list_managed_instances][crate::client::InstanceGroupManagers::list_managed_instances] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_managers::ListManagedInstances;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListManagedInstances {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListManagedInstances(
        RequestBuilder<crate::model::instance_group_managers::ListManagedInstancesRequest>,
    );

    impl ListManagedInstances {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instance_group_managers::ListManagedInstancesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(
            self,
        ) -> Result<crate::model::InstanceGroupManagersListManagedInstancesResponse> {
            (*self.0.stub)
                .list_managed_instances(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::InstanceGroupManagersListManagedInstancesResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::InstanceGroupManagersListManagedInstancesResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::instance_group_managers::ListManagedInstancesRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::instance_group_managers::ListManagedInstancesRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_managers::ListManagedInstancesRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [max_results][crate::model::instance_group_managers::ListManagedInstancesRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::instance_group_managers::ListManagedInstancesRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::instance_group_managers::ListManagedInstancesRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::instance_group_managers::ListManagedInstancesRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::instance_group_managers::ListManagedInstancesRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::instance_group_managers::ListManagedInstancesRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instance_group_managers::ListManagedInstancesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::instance_group_managers::ListManagedInstancesRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::instance_group_managers::ListManagedInstancesRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_managers::ListManagedInstancesRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListManagedInstances {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagers::list_per_instance_configs][crate::client::InstanceGroupManagers::list_per_instance_configs] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_managers::ListPerInstanceConfigs;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListPerInstanceConfigs {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListPerInstanceConfigs(
        RequestBuilder<crate::model::instance_group_managers::ListPerInstanceConfigsRequest>,
    );

    impl ListPerInstanceConfigs {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instance_group_managers::ListPerInstanceConfigsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(
            self,
        ) -> Result<crate::model::InstanceGroupManagersListPerInstanceConfigsResp> {
            (*self.0.stub)
                .list_per_instance_configs(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::InstanceGroupManagersListPerInstanceConfigsResp,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::InstanceGroupManagersListPerInstanceConfigsResp,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::instance_group_managers::ListPerInstanceConfigsRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::instance_group_managers::ListPerInstanceConfigsRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_managers::ListPerInstanceConfigsRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [max_results][crate::model::instance_group_managers::ListPerInstanceConfigsRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::instance_group_managers::ListPerInstanceConfigsRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::instance_group_managers::ListPerInstanceConfigsRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::instance_group_managers::ListPerInstanceConfigsRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::instance_group_managers::ListPerInstanceConfigsRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::instance_group_managers::ListPerInstanceConfigsRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instance_group_managers::ListPerInstanceConfigsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::instance_group_managers::ListPerInstanceConfigsRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::instance_group_managers::ListPerInstanceConfigsRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_managers::ListPerInstanceConfigsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListPerInstanceConfigs {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagers::patch][crate::client::InstanceGroupManagers::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_managers::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::instance_group_managers::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_group_managers::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::InstanceGroupManagers::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_managers::PatchRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instance_group_managers::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_group_managers::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_group_managers::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_managers::PatchRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_group_managers::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManager>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_group_managers::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManager>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagers::patch_per_instance_configs][crate::client::InstanceGroupManagers::patch_per_instance_configs] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_managers::PatchPerInstanceConfigs;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> PatchPerInstanceConfigs {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct PatchPerInstanceConfigs(
        RequestBuilder<crate::model::instance_group_managers::PatchPerInstanceConfigsRequest>,
    );

    impl PatchPerInstanceConfigs {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instance_group_managers::PatchPerInstanceConfigsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch_per_instance_configs(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch_per_instance_configs`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::InstanceGroupManagers::patch_per_instance_configs::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_managers::PatchPerInstanceConfigsRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instance_group_managers::PatchPerInstanceConfigsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_group_managers::PatchPerInstanceConfigsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_group_managers::PatchPerInstanceConfigsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_managers::PatchPerInstanceConfigsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_group_managers::PatchPerInstanceConfigsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersPatchPerInstanceConfigsReq>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_group_managers::PatchPerInstanceConfigsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersPatchPerInstanceConfigsReq>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for PatchPerInstanceConfigs {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagers::recreate_instances][crate::client::InstanceGroupManagers::recreate_instances] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_managers::RecreateInstances;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> RecreateInstances {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct RecreateInstances(
        RequestBuilder<crate::model::instance_group_managers::RecreateInstancesRequest>,
    );

    impl RecreateInstances {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instance_group_managers::RecreateInstancesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .recreate_instances(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `recreate_instances`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::InstanceGroupManagers::recreate_instances::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_managers::RecreateInstancesRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [no_graceful_shutdown][crate::model::instance_group_managers::RecreateInstancesRequest::no_graceful_shutdown].
        pub fn set_no_graceful_shutdown<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.no_graceful_shutdown = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [no_graceful_shutdown][crate::model::instance_group_managers::RecreateInstancesRequest::no_graceful_shutdown].
        pub fn set_or_clear_no_graceful_shutdown<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.no_graceful_shutdown = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instance_group_managers::RecreateInstancesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_group_managers::RecreateInstancesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_group_managers::RecreateInstancesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_managers::RecreateInstancesRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_group_managers::RecreateInstancesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersRecreateInstancesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_group_managers::RecreateInstancesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersRecreateInstancesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for RecreateInstances {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagers::resize][crate::client::InstanceGroupManagers::resize] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_managers::Resize;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Resize {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Resize(RequestBuilder<crate::model::instance_group_managers::ResizeRequest>);

    impl Resize {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_group_managers::ResizeRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .resize(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `resize`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::InstanceGroupManagers::resize::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_managers::ResizeRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instance_group_managers::ResizeRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_group_managers::ResizeRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_group_managers::ResizeRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [size][crate::model::instance_group_managers::ResizeRequest::size].
        pub fn set_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.size = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_managers::ResizeRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Resize {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagers::resume_instances][crate::client::InstanceGroupManagers::resume_instances] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_managers::ResumeInstances;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ResumeInstances {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ResumeInstances(
        RequestBuilder<crate::model::instance_group_managers::ResumeInstancesRequest>,
    );

    impl ResumeInstances {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instance_group_managers::ResumeInstancesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .resume_instances(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `resume_instances`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::InstanceGroupManagers::resume_instances::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_managers::ResumeInstancesRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instance_group_managers::ResumeInstancesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_group_managers::ResumeInstancesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_group_managers::ResumeInstancesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_managers::ResumeInstancesRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_group_managers::ResumeInstancesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersResumeInstancesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_group_managers::ResumeInstancesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersResumeInstancesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ResumeInstances {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagers::set_instance_template][crate::client::InstanceGroupManagers::set_instance_template] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_managers::SetInstanceTemplate;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetInstanceTemplate {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetInstanceTemplate(
        RequestBuilder<crate::model::instance_group_managers::SetInstanceTemplateRequest>,
    );

    impl SetInstanceTemplate {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instance_group_managers::SetInstanceTemplateRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_instance_template(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_instance_template`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::InstanceGroupManagers::set_instance_template::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_managers::SetInstanceTemplateRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instance_group_managers::SetInstanceTemplateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_group_managers::SetInstanceTemplateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_group_managers::SetInstanceTemplateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_managers::SetInstanceTemplateRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_group_managers::SetInstanceTemplateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersSetInstanceTemplateRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_group_managers::SetInstanceTemplateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersSetInstanceTemplateRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetInstanceTemplate {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagers::set_target_pools][crate::client::InstanceGroupManagers::set_target_pools] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_managers::SetTargetPools;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetTargetPools {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetTargetPools(
        RequestBuilder<crate::model::instance_group_managers::SetTargetPoolsRequest>,
    );

    impl SetTargetPools {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instance_group_managers::SetTargetPoolsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_target_pools(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_target_pools`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::InstanceGroupManagers::set_target_pools::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_managers::SetTargetPoolsRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instance_group_managers::SetTargetPoolsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_group_managers::SetTargetPoolsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_group_managers::SetTargetPoolsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_managers::SetTargetPoolsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_group_managers::SetTargetPoolsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersSetTargetPoolsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_group_managers::SetTargetPoolsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersSetTargetPoolsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetTargetPools {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagers::start_instances][crate::client::InstanceGroupManagers::start_instances] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_managers::StartInstances;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> StartInstances {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct StartInstances(
        RequestBuilder<crate::model::instance_group_managers::StartInstancesRequest>,
    );

    impl StartInstances {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instance_group_managers::StartInstancesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .start_instances(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `start_instances`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::InstanceGroupManagers::start_instances::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_managers::StartInstancesRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instance_group_managers::StartInstancesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_group_managers::StartInstancesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_group_managers::StartInstancesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_managers::StartInstancesRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_group_managers::StartInstancesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersStartInstancesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_group_managers::StartInstancesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersStartInstancesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for StartInstances {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagers::stop_instances][crate::client::InstanceGroupManagers::stop_instances] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_managers::StopInstances;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> StopInstances {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct StopInstances(
        RequestBuilder<crate::model::instance_group_managers::StopInstancesRequest>,
    );

    impl StopInstances {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instance_group_managers::StopInstancesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .stop_instances(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `stop_instances`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::InstanceGroupManagers::stop_instances::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_managers::StopInstancesRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [no_graceful_shutdown][crate::model::instance_group_managers::StopInstancesRequest::no_graceful_shutdown].
        pub fn set_no_graceful_shutdown<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.no_graceful_shutdown = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [no_graceful_shutdown][crate::model::instance_group_managers::StopInstancesRequest::no_graceful_shutdown].
        pub fn set_or_clear_no_graceful_shutdown<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.no_graceful_shutdown = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instance_group_managers::StopInstancesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_group_managers::StopInstancesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_group_managers::StopInstancesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_managers::StopInstancesRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_group_managers::StopInstancesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersStopInstancesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_group_managers::StopInstancesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersStopInstancesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for StopInstances {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagers::suspend_instances][crate::client::InstanceGroupManagers::suspend_instances] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_managers::SuspendInstances;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SuspendInstances {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SuspendInstances(
        RequestBuilder<crate::model::instance_group_managers::SuspendInstancesRequest>,
    );

    impl SuspendInstances {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instance_group_managers::SuspendInstancesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .suspend_instances(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `suspend_instances`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::InstanceGroupManagers::suspend_instances::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_managers::SuspendInstancesRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instance_group_managers::SuspendInstancesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_group_managers::SuspendInstancesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_group_managers::SuspendInstancesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_managers::SuspendInstancesRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_group_managers::SuspendInstancesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersSuspendInstancesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_group_managers::SuspendInstancesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersSuspendInstancesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SuspendInstances {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagers::update_per_instance_configs][crate::client::InstanceGroupManagers::update_per_instance_configs] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_managers::UpdatePerInstanceConfigs;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdatePerInstanceConfigs {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdatePerInstanceConfigs(
        RequestBuilder<crate::model::instance_group_managers::UpdatePerInstanceConfigsRequest>,
    );

    impl UpdatePerInstanceConfigs {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instance_group_managers::UpdatePerInstanceConfigsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update_per_instance_configs(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update_per_instance_configs`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::InstanceGroupManagers::update_per_instance_configs::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::instance_group_managers::UpdatePerInstanceConfigsRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instance_group_managers::UpdatePerInstanceConfigsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_group_managers::UpdatePerInstanceConfigsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_group_managers::UpdatePerInstanceConfigsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_group_managers::UpdatePerInstanceConfigsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_group_managers::UpdatePerInstanceConfigsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersUpdatePerInstanceConfigsReq>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_group_managers::UpdatePerInstanceConfigsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagersUpdatePerInstanceConfigsReq>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdatePerInstanceConfigs {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroupManagers::get_operation][crate::client::InstanceGroupManagers::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_group_managers::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::zone_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::zone_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::zone_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zone_operations::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [InstanceGroups][crate::client::InstanceGroups].
#[cfg(feature = "instance-groups")]
#[cfg_attr(docsrs, doc(cfg(feature = "instance-groups")))]
pub mod instance_groups {
    use crate::Result;

    /// A builder for [InstanceGroups][crate::client::InstanceGroups].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::instance_groups::ClientBuilder;
    /// # use client::InstanceGroups;
    /// let builder : ClientBuilder = InstanceGroups::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::InstanceGroups;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = InstanceGroups;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::InstanceGroups] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroups>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroups>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [InstanceGroups::add_instances][crate::client::InstanceGroups::add_instances] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_groups::AddInstances;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AddInstances {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AddInstances(RequestBuilder<crate::model::instance_groups::AddInstancesRequest>);

    impl AddInstances {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_groups::AddInstancesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .add_instances(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `add_instances`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::InstanceGroups::add_instances::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group][crate::model::instance_groups::AddInstancesRequest::instance_group].
        pub fn set_instance_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instance_groups::AddInstancesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_groups::AddInstancesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_groups::AddInstancesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_groups::AddInstancesRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_groups::AddInstancesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupsAddInstancesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_groups::AddInstancesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupsAddInstancesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AddInstances {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroups::aggregated_list][crate::client::InstanceGroups::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_groups::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(RequestBuilder<crate::model::instance_groups::AggregatedListRequest>);

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_groups::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstanceGroupAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::InstanceGroupAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::InstanceGroupAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::instance_groups::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::instance_groups::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::instance_groups::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::instance_groups::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::instance_groups::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::instance_groups::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::instance_groups::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::instance_groups::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::instance_groups::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::instance_groups::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instance_groups::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::instance_groups::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::instance_groups::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::instance_groups::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::instance_groups::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroups::delete][crate::client::InstanceGroups::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_groups::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::instance_groups::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_groups::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::InstanceGroups::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group][crate::model::instance_groups::DeleteRequest::instance_group].
        pub fn set_instance_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instance_groups::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_groups::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_groups::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_groups::DeleteRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroups::get][crate::client::InstanceGroups::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_groups::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::instance_groups::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_groups::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstanceGroup> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [instance_group][crate::model::instance_groups::GetRequest::instance_group].
        pub fn set_instance_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instance_groups::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instance_groups::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroups::insert][crate::client::InstanceGroups::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_groups::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::instance_groups::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_groups::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::InstanceGroups::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::instance_groups::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_groups::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_groups::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_groups::InsertRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_groups::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroup>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_groups::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroup>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroups::list][crate::client::InstanceGroups::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_groups::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::instance_groups::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_groups::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstanceGroupList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::InstanceGroupList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::InstanceGroupList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::instance_groups::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::instance_groups::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::instance_groups::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::instance_groups::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::instance_groups::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::instance_groups::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::instance_groups::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::instance_groups::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instance_groups::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::instance_groups::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::instance_groups::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_groups::ListRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroups::list_instances][crate::client::InstanceGroups::list_instances] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_groups::ListInstances;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListInstances {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListInstances(RequestBuilder<crate::model::instance_groups::ListInstancesRequest>);

    impl ListInstances {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_groups::ListInstancesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstanceGroupsListInstances> {
            (*self.0.stub)
                .list_instances(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::InstanceGroupsListInstances,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::InstanceGroupsListInstances,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::instance_groups::ListInstancesRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::instance_groups::ListInstancesRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [instance_group][crate::model::instance_groups::ListInstancesRequest::instance_group].
        pub fn set_instance_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group = v.into();
            self
        }

        /// Sets the value of [max_results][crate::model::instance_groups::ListInstancesRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::instance_groups::ListInstancesRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::instance_groups::ListInstancesRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::instance_groups::ListInstancesRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::instance_groups::ListInstancesRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::instance_groups::ListInstancesRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instance_groups::ListInstancesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::instance_groups::ListInstancesRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::instance_groups::ListInstancesRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_groups::ListInstancesRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_groups::ListInstancesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupsListInstancesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_groups::ListInstancesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupsListInstancesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListInstances {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroups::remove_instances][crate::client::InstanceGroups::remove_instances] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_groups::RemoveInstances;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> RemoveInstances {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct RemoveInstances(
        RequestBuilder<crate::model::instance_groups::RemoveInstancesRequest>,
    );

    impl RemoveInstances {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_groups::RemoveInstancesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .remove_instances(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `remove_instances`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::InstanceGroups::remove_instances::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group][crate::model::instance_groups::RemoveInstancesRequest::instance_group].
        pub fn set_instance_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instance_groups::RemoveInstancesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_groups::RemoveInstancesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_groups::RemoveInstancesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_groups::RemoveInstancesRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_groups::RemoveInstancesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupsRemoveInstancesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_groups::RemoveInstancesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupsRemoveInstancesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for RemoveInstances {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroups::set_named_ports][crate::client::InstanceGroups::set_named_ports] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_groups::SetNamedPorts;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetNamedPorts {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetNamedPorts(RequestBuilder<crate::model::instance_groups::SetNamedPortsRequest>);

    impl SetNamedPorts {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_groups::SetNamedPortsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_named_ports(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_named_ports`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::InstanceGroups::set_named_ports::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group][crate::model::instance_groups::SetNamedPortsRequest::instance_group].
        pub fn set_instance_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instance_groups::SetNamedPortsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_groups::SetNamedPortsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_groups::SetNamedPortsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_groups::SetNamedPortsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_groups::SetNamedPortsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupsSetNamedPortsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_groups::SetNamedPortsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupsSetNamedPortsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetNamedPorts {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroups::test_iam_permissions][crate::client::InstanceGroups::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_groups::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::instance_groups::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_groups::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::instance_groups::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::instance_groups::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instance_groups::TestIamPermissionsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_groups::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_groups::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceGroups::get_operation][crate::client::InstanceGroups::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_groups::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::zone_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::zone_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::zone_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zone_operations::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [InstanceSettings][crate::client::InstanceSettings].
#[cfg(feature = "instance-settings")]
#[cfg_attr(docsrs, doc(cfg(feature = "instance-settings")))]
pub mod instance_settings {
    use crate::Result;

    /// A builder for [InstanceSettings][crate::client::InstanceSettings].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::instance_settings::ClientBuilder;
    /// # use client::InstanceSettings;
    /// let builder : ClientBuilder = InstanceSettings::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::InstanceSettings;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = InstanceSettings;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::InstanceSettings] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceSettings>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceSettings>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [InstanceSettings::get][crate::client::InstanceSettings::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_settings::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::instance_settings::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceSettings>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_settings::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstanceSettings> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::instance_settings::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instance_settings::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceSettings::patch][crate::client::InstanceSettings::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_settings::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::instance_settings::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceSettings>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_settings::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::InstanceSettings::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::instance_settings::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_settings::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_settings::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::instance_settings::PatchRequest::update_mask].
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::instance_settings::PatchRequest::update_mask].
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instance_settings::PatchRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_settings::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceSettings>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_settings::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceSettings>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceSettings::get_operation][crate::client::InstanceSettings::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_settings::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::zone_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceSettings>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::zone_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::zone_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zone_operations::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [InstanceTemplates][crate::client::InstanceTemplates].
#[cfg(feature = "instance-templates")]
#[cfg_attr(docsrs, doc(cfg(feature = "instance-templates")))]
pub mod instance_templates {
    use crate::Result;

    /// A builder for [InstanceTemplates][crate::client::InstanceTemplates].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::instance_templates::ClientBuilder;
    /// # use client::InstanceTemplates;
    /// let builder : ClientBuilder = InstanceTemplates::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::InstanceTemplates;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = InstanceTemplates;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::InstanceTemplates] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceTemplates>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceTemplates>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [InstanceTemplates::aggregated_list][crate::client::InstanceTemplates::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_templates::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::instance_templates::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceTemplates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_templates::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstanceTemplateAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::InstanceTemplateAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::InstanceTemplateAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::instance_templates::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::instance_templates::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::instance_templates::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::instance_templates::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::instance_templates::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::instance_templates::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::instance_templates::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::instance_templates::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::instance_templates::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::instance_templates::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instance_templates::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::instance_templates::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::instance_templates::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::instance_templates::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::instance_templates::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceTemplates::delete][crate::client::InstanceTemplates::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_templates::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::instance_templates::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceTemplates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_templates::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::InstanceTemplates::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_template][crate::model::instance_templates::DeleteRequest::instance_template].
        pub fn set_instance_template<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_template = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instance_templates::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_templates::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_templates::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceTemplates::get][crate::client::InstanceTemplates::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_templates::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::instance_templates::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceTemplates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_templates::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstanceTemplate> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [instance_template][crate::model::instance_templates::GetRequest::instance_template].
        pub fn set_instance_template<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_template = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instance_templates::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceTemplates::get_iam_policy][crate::client::InstanceTemplates::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_templates::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(RequestBuilder<crate::model::instance_templates::GetIamPolicyRequest>);

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceTemplates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_templates::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::instance_templates::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::instance_templates::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instance_templates::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::instance_templates::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceTemplates::insert][crate::client::InstanceTemplates::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_templates::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::instance_templates::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceTemplates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_templates::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::InstanceTemplates::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::instance_templates::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instance_templates::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instance_templates::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::instance_templates::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceTemplate>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_templates::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceTemplate>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceTemplates::list][crate::client::InstanceTemplates::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_templates::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::instance_templates::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceTemplates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_templates::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstanceTemplateList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::InstanceTemplateList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::InstanceTemplateList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::instance_templates::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::instance_templates::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::instance_templates::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::instance_templates::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::instance_templates::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::instance_templates::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::instance_templates::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::instance_templates::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instance_templates::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::instance_templates::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::instance_templates::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceTemplates::set_iam_policy][crate::client::InstanceTemplates::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_templates::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(RequestBuilder<crate::model::instance_templates::SetIamPolicyRequest>);

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceTemplates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instance_templates::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::instance_templates::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::instance_templates::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_templates::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_templates::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceTemplates::test_iam_permissions][crate::client::InstanceTemplates::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_templates::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::instance_templates::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceTemplates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instance_templates::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::instance_templates::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::instance_templates::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instance_templates::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instance_templates::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstanceTemplates::get_operation][crate::client::InstanceTemplates::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instance_templates::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstanceTemplates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [Instances][crate::client::Instances].
#[cfg(feature = "instances")]
#[cfg_attr(docsrs, doc(cfg(feature = "instances")))]
pub mod instances {
    use crate::Result;

    /// A builder for [Instances][crate::client::Instances].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::instances::ClientBuilder;
    /// # use client::Instances;
    /// let builder : ClientBuilder = Instances::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::Instances;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = Instances;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::Instances] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [Instances::add_access_config][crate::client::Instances::add_access_config] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::AddAccessConfig;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AddAccessConfig {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AddAccessConfig(RequestBuilder<crate::model::instances::AddAccessConfigRequest>);

    impl AddAccessConfig {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::AddAccessConfigRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .add_access_config(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `add_access_config`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::add_access_config::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::AddAccessConfigRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [network_interface][crate::model::instances::AddAccessConfigRequest::network_interface].
        pub fn set_network_interface<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network_interface = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::AddAccessConfigRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::AddAccessConfigRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::AddAccessConfigRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::AddAccessConfigRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::AddAccessConfigRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::AccessConfig>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::AddAccessConfigRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::AccessConfig>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AddAccessConfig {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::add_network_interface][crate::client::Instances::add_network_interface] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::AddNetworkInterface;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AddNetworkInterface {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AddNetworkInterface(
        RequestBuilder<crate::model::instances::AddNetworkInterfaceRequest>,
    );

    impl AddNetworkInterface {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::AddNetworkInterfaceRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .add_network_interface(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `add_network_interface`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::add_network_interface::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::AddNetworkInterfaceRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::AddNetworkInterfaceRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::AddNetworkInterfaceRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::AddNetworkInterfaceRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::AddNetworkInterfaceRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::AddNetworkInterfaceRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NetworkInterface>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::AddNetworkInterfaceRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NetworkInterface>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AddNetworkInterface {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::add_resource_policies][crate::client::Instances::add_resource_policies] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::AddResourcePolicies;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AddResourcePolicies {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AddResourcePolicies(
        RequestBuilder<crate::model::instances::AddResourcePoliciesRequest>,
    );

    impl AddResourcePolicies {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::AddResourcePoliciesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .add_resource_policies(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `add_resource_policies`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::add_resource_policies::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::AddResourcePoliciesRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::AddResourcePoliciesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::AddResourcePoliciesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::AddResourcePoliciesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::AddResourcePoliciesRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::AddResourcePoliciesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstancesAddResourcePoliciesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::AddResourcePoliciesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstancesAddResourcePoliciesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AddResourcePolicies {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::aggregated_list][crate::client::Instances::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(RequestBuilder<crate::model::instances::AggregatedListRequest>);

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstanceAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::InstanceAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::InstanceAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::instances::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::instances::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::instances::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::instances::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::instances::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::instances::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::instances::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::instances::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::instances::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::instances::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instances::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::instances::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::instances::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::instances::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::instances::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::attach_disk][crate::client::Instances::attach_disk] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::AttachDisk;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AttachDisk {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AttachDisk(RequestBuilder<crate::model::instances::AttachDiskRequest>);

    impl AttachDisk {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::AttachDiskRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .attach_disk(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `attach_disk`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::attach_disk::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [force_attach][crate::model::instances::AttachDiskRequest::force_attach].
        pub fn set_force_attach<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.force_attach = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [force_attach][crate::model::instances::AttachDiskRequest::force_attach].
        pub fn set_or_clear_force_attach<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.force_attach = v.map(|x| x.into());
            self
        }

        /// Sets the value of [instance][crate::model::instances::AttachDiskRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::AttachDiskRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::AttachDiskRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::AttachDiskRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::AttachDiskRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::AttachDiskRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::AttachedDisk>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::AttachDiskRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::AttachedDisk>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AttachDisk {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::bulk_insert][crate::client::Instances::bulk_insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::BulkInsert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> BulkInsert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct BulkInsert(RequestBuilder<crate::model::instances::BulkInsertRequest>);

    impl BulkInsert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::BulkInsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .bulk_insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `bulk_insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::bulk_insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::instances::BulkInsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::BulkInsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::BulkInsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::BulkInsertRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::BulkInsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::BulkInsertInstanceResource>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::BulkInsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::BulkInsertInstanceResource>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for BulkInsert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::delete][crate::client::Instances::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::instances::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::DeleteRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::DeleteRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::delete_access_config][crate::client::Instances::delete_access_config] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::DeleteAccessConfig;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeleteAccessConfig {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeleteAccessConfig(
        RequestBuilder<crate::model::instances::DeleteAccessConfigRequest>,
    );

    impl DeleteAccessConfig {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::DeleteAccessConfigRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete_access_config(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete_access_config`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::delete_access_config::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [access_config][crate::model::instances::DeleteAccessConfigRequest::access_config].
        pub fn set_access_config<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.access_config = v.into();
            self
        }

        /// Sets the value of [instance][crate::model::instances::DeleteAccessConfigRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [network_interface][crate::model::instances::DeleteAccessConfigRequest::network_interface].
        pub fn set_network_interface<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network_interface = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::DeleteAccessConfigRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::DeleteAccessConfigRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::DeleteAccessConfigRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::DeleteAccessConfigRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeleteAccessConfig {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::delete_network_interface][crate::client::Instances::delete_network_interface] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::DeleteNetworkInterface;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeleteNetworkInterface {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeleteNetworkInterface(
        RequestBuilder<crate::model::instances::DeleteNetworkInterfaceRequest>,
    );

    impl DeleteNetworkInterface {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::DeleteNetworkInterfaceRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete_network_interface(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete_network_interface`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Instances::delete_network_interface::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::DeleteNetworkInterfaceRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [network_interface_name][crate::model::instances::DeleteNetworkInterfaceRequest::network_interface_name].
        pub fn set_network_interface_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network_interface_name = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::DeleteNetworkInterfaceRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::DeleteNetworkInterfaceRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::DeleteNetworkInterfaceRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::DeleteNetworkInterfaceRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeleteNetworkInterface {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::detach_disk][crate::client::Instances::detach_disk] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::DetachDisk;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DetachDisk {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DetachDisk(RequestBuilder<crate::model::instances::DetachDiskRequest>);

    impl DetachDisk {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::DetachDiskRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .detach_disk(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `detach_disk`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::detach_disk::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [device_name][crate::model::instances::DetachDiskRequest::device_name].
        pub fn set_device_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.device_name = v.into();
            self
        }

        /// Sets the value of [instance][crate::model::instances::DetachDiskRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::DetachDiskRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::DetachDiskRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::DetachDiskRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::DetachDiskRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DetachDisk {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::get][crate::client::Instances::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::instances::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::GetRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Instance> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [instance][crate::model::instances::GetRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instances::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::get_effective_firewalls][crate::client::Instances::get_effective_firewalls] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::GetEffectiveFirewalls;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetEffectiveFirewalls {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetEffectiveFirewalls(
        RequestBuilder<crate::model::instances::GetEffectiveFirewallsRequest>,
    );

    impl GetEffectiveFirewalls {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::GetEffectiveFirewallsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstancesGetEffectiveFirewallsResponse> {
            (*self.0.stub)
                .get_effective_firewalls(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [instance][crate::model::instances::GetEffectiveFirewallsRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [network_interface][crate::model::instances::GetEffectiveFirewallsRequest::network_interface].
        pub fn set_network_interface<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network_interface = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::GetEffectiveFirewallsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instances::GetEffectiveFirewallsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetEffectiveFirewalls {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::get_guest_attributes][crate::client::Instances::get_guest_attributes] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::GetGuestAttributes;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetGuestAttributes {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetGuestAttributes(
        RequestBuilder<crate::model::instances::GetGuestAttributesRequest>,
    );

    impl GetGuestAttributes {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::GetGuestAttributesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::GuestAttributes> {
            (*self.0.stub)
                .get_guest_attributes(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [instance][crate::model::instances::GetGuestAttributesRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::GetGuestAttributesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [query_path][crate::model::instances::GetGuestAttributesRequest::query_path].
        pub fn set_query_path<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.query_path = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [query_path][crate::model::instances::GetGuestAttributesRequest::query_path].
        pub fn set_or_clear_query_path<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.query_path = v.map(|x| x.into());
            self
        }

        /// Sets the value of [variable_key][crate::model::instances::GetGuestAttributesRequest::variable_key].
        pub fn set_variable_key<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.variable_key = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [variable_key][crate::model::instances::GetGuestAttributesRequest::variable_key].
        pub fn set_or_clear_variable_key<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.variable_key = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::GetGuestAttributesRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetGuestAttributes {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::get_iam_policy][crate::client::Instances::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(RequestBuilder<crate::model::instances::GetIamPolicyRequest>);

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::instances::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::instances::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instances::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::instances::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instances::GetIamPolicyRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::get_screenshot][crate::client::Instances::get_screenshot] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::GetScreenshot;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetScreenshot {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetScreenshot(RequestBuilder<crate::model::instances::GetScreenshotRequest>);

    impl GetScreenshot {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::GetScreenshotRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Screenshot> {
            (*self.0.stub)
                .get_screenshot(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [instance][crate::model::instances::GetScreenshotRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::GetScreenshotRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instances::GetScreenshotRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetScreenshot {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::get_serial_port_output][crate::client::Instances::get_serial_port_output] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::GetSerialPortOutput;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetSerialPortOutput {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetSerialPortOutput(
        RequestBuilder<crate::model::instances::GetSerialPortOutputRequest>,
    );

    impl GetSerialPortOutput {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::GetSerialPortOutputRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SerialPortOutput> {
            (*self.0.stub)
                .get_serial_port_output(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [instance][crate::model::instances::GetSerialPortOutputRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [port][crate::model::instances::GetSerialPortOutputRequest::port].
        pub fn set_port<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.port = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [port][crate::model::instances::GetSerialPortOutputRequest::port].
        pub fn set_or_clear_port<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.port = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instances::GetSerialPortOutputRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [start][crate::model::instances::GetSerialPortOutputRequest::start].
        pub fn set_start<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.start = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [start][crate::model::instances::GetSerialPortOutputRequest::start].
        pub fn set_or_clear_start<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.start = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::GetSerialPortOutputRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetSerialPortOutput {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::get_shielded_instance_identity][crate::client::Instances::get_shielded_instance_identity] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::GetShieldedInstanceIdentity;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetShieldedInstanceIdentity {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetShieldedInstanceIdentity(
        RequestBuilder<crate::model::instances::GetShieldedInstanceIdentityRequest>,
    );

    impl GetShieldedInstanceIdentity {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instances::GetShieldedInstanceIdentityRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ShieldedInstanceIdentity> {
            (*self.0.stub)
                .get_shielded_instance_identity(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [instance][crate::model::instances::GetShieldedInstanceIdentityRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::GetShieldedInstanceIdentityRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instances::GetShieldedInstanceIdentityRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetShieldedInstanceIdentity {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::insert][crate::client::Instances::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::instances::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::instances::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [source_instance_template][crate::model::instances::InsertRequest::source_instance_template].
        pub fn set_source_instance_template<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.source_instance_template = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [source_instance_template][crate::model::instances::InsertRequest::source_instance_template].
        pub fn set_or_clear_source_instance_template<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.source_instance_template = v.map(|x| x.into());
            self
        }

        /// Sets the value of [source_machine_image][crate::model::instances::InsertRequest::source_machine_image].
        pub fn set_source_machine_image<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.source_machine_image = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [source_machine_image][crate::model::instances::InsertRequest::source_machine_image].
        pub fn set_or_clear_source_machine_image<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.source_machine_image = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::InsertRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Instance>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Instance>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::list][crate::client::Instances::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::instances::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::ListRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstanceList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::InstanceList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::InstanceList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::instances::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::instances::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::instances::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::instances::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::instances::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::instances::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::instances::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::instances::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instances::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::instances::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::instances::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::ListRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::list_referrers][crate::client::Instances::list_referrers] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::ListReferrers;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListReferrers {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListReferrers(RequestBuilder<crate::model::instances::ListReferrersRequest>);

    impl ListReferrers {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::ListReferrersRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstanceListReferrers> {
            (*self.0.stub)
                .list_referrers(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::InstanceListReferrers, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::InstanceListReferrers,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::instances::ListReferrersRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::instances::ListReferrersRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [instance][crate::model::instances::ListReferrersRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [max_results][crate::model::instances::ListReferrersRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::instances::ListReferrersRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::instances::ListReferrersRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::instances::ListReferrersRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::instances::ListReferrersRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::instances::ListReferrersRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instances::ListReferrersRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::instances::ListReferrersRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::instances::ListReferrersRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::ListReferrersRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListReferrers {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::perform_maintenance][crate::client::Instances::perform_maintenance] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::PerformMaintenance;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> PerformMaintenance {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct PerformMaintenance(
        RequestBuilder<crate::model::instances::PerformMaintenanceRequest>,
    );

    impl PerformMaintenance {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::PerformMaintenanceRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .perform_maintenance(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `perform_maintenance`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::perform_maintenance::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::PerformMaintenanceRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::PerformMaintenanceRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::PerformMaintenanceRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::PerformMaintenanceRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::PerformMaintenanceRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for PerformMaintenance {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::remove_resource_policies][crate::client::Instances::remove_resource_policies] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::RemoveResourcePolicies;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> RemoveResourcePolicies {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct RemoveResourcePolicies(
        RequestBuilder<crate::model::instances::RemoveResourcePoliciesRequest>,
    );

    impl RemoveResourcePolicies {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::RemoveResourcePoliciesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .remove_resource_policies(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `remove_resource_policies`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Instances::remove_resource_policies::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::RemoveResourcePoliciesRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::RemoveResourcePoliciesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::RemoveResourcePoliciesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::RemoveResourcePoliciesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::RemoveResourcePoliciesRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::RemoveResourcePoliciesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstancesRemoveResourcePoliciesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::RemoveResourcePoliciesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstancesRemoveResourcePoliciesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for RemoveResourcePolicies {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::report_host_as_faulty][crate::client::Instances::report_host_as_faulty] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::ReportHostAsFaulty;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ReportHostAsFaulty {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ReportHostAsFaulty(
        RequestBuilder<crate::model::instances::ReportHostAsFaultyRequest>,
    );

    impl ReportHostAsFaulty {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::ReportHostAsFaultyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .report_host_as_faulty(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `report_host_as_faulty`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::report_host_as_faulty::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::ReportHostAsFaultyRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::ReportHostAsFaultyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::ReportHostAsFaultyRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::ReportHostAsFaultyRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::ReportHostAsFaultyRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::ReportHostAsFaultyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstancesReportHostAsFaultyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::ReportHostAsFaultyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstancesReportHostAsFaultyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ReportHostAsFaulty {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::reset][crate::client::Instances::reset] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::Reset;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Reset {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Reset(RequestBuilder<crate::model::instances::ResetRequest>);

    impl Reset {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::ResetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .reset(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `reset`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::reset::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::ResetRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::ResetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::ResetRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::ResetRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::ResetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Reset {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::resume][crate::client::Instances::resume] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::Resume;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Resume {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Resume(RequestBuilder<crate::model::instances::ResumeRequest>);

    impl Resume {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::ResumeRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .resume(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `resume`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::resume::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::ResumeRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::ResumeRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::ResumeRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::ResumeRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::ResumeRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Resume {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::send_diagnostic_interrupt][crate::client::Instances::send_diagnostic_interrupt] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::SendDiagnosticInterrupt;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SendDiagnosticInterrupt {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SendDiagnosticInterrupt(
        RequestBuilder<crate::model::instances::SendDiagnosticInterruptRequest>,
    );

    impl SendDiagnosticInterrupt {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::SendDiagnosticInterruptRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<()> {
            (*self.0.stub)
                .send_diagnostic_interrupt(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [instance][crate::model::instances::SendDiagnosticInterruptRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::SendDiagnosticInterruptRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instances::SendDiagnosticInterruptRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SendDiagnosticInterrupt {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::set_deletion_protection][crate::client::Instances::set_deletion_protection] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::SetDeletionProtection;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetDeletionProtection {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetDeletionProtection(
        RequestBuilder<crate::model::instances::SetDeletionProtectionRequest>,
    );

    impl SetDeletionProtection {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::SetDeletionProtectionRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_deletion_protection(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_deletion_protection`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Instances::set_deletion_protection::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [deletion_protection][crate::model::instances::SetDeletionProtectionRequest::deletion_protection].
        pub fn set_deletion_protection<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.deletion_protection = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [deletion_protection][crate::model::instances::SetDeletionProtectionRequest::deletion_protection].
        pub fn set_or_clear_deletion_protection<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.deletion_protection = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instances::SetDeletionProtectionRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::SetDeletionProtectionRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::SetDeletionProtectionRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [resource][crate::model::instances::SetDeletionProtectionRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instances::SetDeletionProtectionRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetDeletionProtection {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::set_disk_auto_delete][crate::client::Instances::set_disk_auto_delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::SetDiskAutoDelete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetDiskAutoDelete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetDiskAutoDelete(RequestBuilder<crate::model::instances::SetDiskAutoDeleteRequest>);

    impl SetDiskAutoDelete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::SetDiskAutoDeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_disk_auto_delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_disk_auto_delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::set_disk_auto_delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [auto_delete][crate::model::instances::SetDiskAutoDeleteRequest::auto_delete].
        pub fn set_auto_delete<T: Into<bool>>(mut self, v: T) -> Self {
            self.0.request.auto_delete = v.into();
            self
        }

        /// Sets the value of [device_name][crate::model::instances::SetDiskAutoDeleteRequest::device_name].
        pub fn set_device_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.device_name = v.into();
            self
        }

        /// Sets the value of [instance][crate::model::instances::SetDiskAutoDeleteRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::SetDiskAutoDeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::SetDiskAutoDeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::SetDiskAutoDeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::SetDiskAutoDeleteRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetDiskAutoDelete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::set_iam_policy][crate::client::Instances::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(RequestBuilder<crate::model::instances::SetIamPolicyRequest>);

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::instances::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::instances::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instances::SetIamPolicyRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ZoneSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ZoneSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::set_labels][crate::client::Instances::set_labels] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::SetLabels;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetLabels {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetLabels(RequestBuilder<crate::model::instances::SetLabelsRequest>);

    impl SetLabels {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::SetLabelsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_labels(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_labels`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::set_labels::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::SetLabelsRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::SetLabelsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::SetLabelsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::SetLabelsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::SetLabelsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::SetLabelsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstancesSetLabelsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::SetLabelsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstancesSetLabelsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetLabels {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::set_machine_resources][crate::client::Instances::set_machine_resources] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::SetMachineResources;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetMachineResources {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetMachineResources(
        RequestBuilder<crate::model::instances::SetMachineResourcesRequest>,
    );

    impl SetMachineResources {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::SetMachineResourcesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_machine_resources(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_machine_resources`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::set_machine_resources::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::SetMachineResourcesRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::SetMachineResourcesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::SetMachineResourcesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::SetMachineResourcesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::SetMachineResourcesRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::SetMachineResourcesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstancesSetMachineResourcesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::SetMachineResourcesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstancesSetMachineResourcesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetMachineResources {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::set_machine_type][crate::client::Instances::set_machine_type] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::SetMachineType;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetMachineType {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetMachineType(RequestBuilder<crate::model::instances::SetMachineTypeRequest>);

    impl SetMachineType {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::SetMachineTypeRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_machine_type(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_machine_type`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::set_machine_type::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::SetMachineTypeRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::SetMachineTypeRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::SetMachineTypeRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::SetMachineTypeRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::SetMachineTypeRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::SetMachineTypeRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstancesSetMachineTypeRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::SetMachineTypeRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstancesSetMachineTypeRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetMachineType {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::set_metadata][crate::client::Instances::set_metadata] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::SetMetadata;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetMetadata {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetMetadata(RequestBuilder<crate::model::instances::SetMetadataRequest>);

    impl SetMetadata {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::SetMetadataRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_metadata(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_metadata`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::set_metadata::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::SetMetadataRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::SetMetadataRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::SetMetadataRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::SetMetadataRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::SetMetadataRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::SetMetadataRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Metadata>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::SetMetadataRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Metadata>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetMetadata {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::set_min_cpu_platform][crate::client::Instances::set_min_cpu_platform] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::SetMinCpuPlatform;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetMinCpuPlatform {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetMinCpuPlatform(RequestBuilder<crate::model::instances::SetMinCpuPlatformRequest>);

    impl SetMinCpuPlatform {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::SetMinCpuPlatformRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_min_cpu_platform(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_min_cpu_platform`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::set_min_cpu_platform::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::SetMinCpuPlatformRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::SetMinCpuPlatformRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::SetMinCpuPlatformRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::SetMinCpuPlatformRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::SetMinCpuPlatformRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::SetMinCpuPlatformRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstancesSetMinCpuPlatformRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::SetMinCpuPlatformRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstancesSetMinCpuPlatformRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetMinCpuPlatform {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::set_name][crate::client::Instances::set_name] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::SetName;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetName {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetName(RequestBuilder<crate::model::instances::SetNameRequest>);

    impl SetName {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::SetNameRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_name(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_name`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::set_name::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::SetNameRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::SetNameRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::SetNameRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::SetNameRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::SetNameRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::SetNameRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstancesSetNameRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::SetNameRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstancesSetNameRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetName {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::set_scheduling][crate::client::Instances::set_scheduling] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::SetScheduling;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetScheduling {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetScheduling(RequestBuilder<crate::model::instances::SetSchedulingRequest>);

    impl SetScheduling {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::SetSchedulingRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_scheduling(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_scheduling`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::set_scheduling::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::SetSchedulingRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::SetSchedulingRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::SetSchedulingRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::SetSchedulingRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::SetSchedulingRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::SetSchedulingRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Scheduling>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::SetSchedulingRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Scheduling>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetScheduling {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::set_security_policy][crate::client::Instances::set_security_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::SetSecurityPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetSecurityPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetSecurityPolicy(RequestBuilder<crate::model::instances::SetSecurityPolicyRequest>);

    impl SetSecurityPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::SetSecurityPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_security_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_security_policy`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::set_security_policy::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::SetSecurityPolicyRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::SetSecurityPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::SetSecurityPolicyRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::SetSecurityPolicyRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::SetSecurityPolicyRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::SetSecurityPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstancesSetSecurityPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::SetSecurityPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstancesSetSecurityPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetSecurityPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::set_service_account][crate::client::Instances::set_service_account] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::SetServiceAccount;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetServiceAccount {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetServiceAccount(RequestBuilder<crate::model::instances::SetServiceAccountRequest>);

    impl SetServiceAccount {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::SetServiceAccountRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_service_account(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_service_account`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::set_service_account::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::SetServiceAccountRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::SetServiceAccountRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::SetServiceAccountRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::SetServiceAccountRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::SetServiceAccountRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::SetServiceAccountRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstancesSetServiceAccountRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::SetServiceAccountRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstancesSetServiceAccountRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetServiceAccount {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::set_shielded_instance_integrity_policy][crate::client::Instances::set_shielded_instance_integrity_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::SetShieldedInstanceIntegrityPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetShieldedInstanceIntegrityPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetShieldedInstanceIntegrityPolicy(
        RequestBuilder<crate::model::instances::SetShieldedInstanceIntegrityPolicyRequest>,
    );

    impl SetShieldedInstanceIntegrityPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instances::SetShieldedInstanceIntegrityPolicyRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_shielded_instance_integrity_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_shielded_instance_integrity_policy`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Instances::set_shielded_instance_integrity_policy::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::SetShieldedInstanceIntegrityPolicyRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::SetShieldedInstanceIntegrityPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::SetShieldedInstanceIntegrityPolicyRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::SetShieldedInstanceIntegrityPolicyRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::SetShieldedInstanceIntegrityPolicyRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::SetShieldedInstanceIntegrityPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ShieldedInstanceIntegrityPolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::SetShieldedInstanceIntegrityPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ShieldedInstanceIntegrityPolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetShieldedInstanceIntegrityPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::set_tags][crate::client::Instances::set_tags] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::SetTags;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetTags {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetTags(RequestBuilder<crate::model::instances::SetTagsRequest>);

    impl SetTags {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::SetTagsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_tags(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_tags`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::set_tags::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::SetTagsRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::SetTagsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::SetTagsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::SetTagsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::SetTagsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::SetTagsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Tags>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::SetTagsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Tags>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetTags {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::simulate_maintenance_event][crate::client::Instances::simulate_maintenance_event] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::SimulateMaintenanceEvent;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SimulateMaintenanceEvent {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SimulateMaintenanceEvent(
        RequestBuilder<crate::model::instances::SimulateMaintenanceEventRequest>,
    );

    impl SimulateMaintenanceEvent {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::SimulateMaintenanceEventRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .simulate_maintenance_event(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `simulate_maintenance_event`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Instances::simulate_maintenance_event::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::SimulateMaintenanceEventRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::SimulateMaintenanceEventRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::SimulateMaintenanceEventRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::SimulateMaintenanceEventRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [with_extended_notifications][crate::model::instances::SimulateMaintenanceEventRequest::with_extended_notifications].
        pub fn set_with_extended_notifications<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.with_extended_notifications = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [with_extended_notifications][crate::model::instances::SimulateMaintenanceEventRequest::with_extended_notifications].
        pub fn set_or_clear_with_extended_notifications<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.with_extended_notifications = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::SimulateMaintenanceEventRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SimulateMaintenanceEvent {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::start][crate::client::Instances::start] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::Start;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Start {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Start(RequestBuilder<crate::model::instances::StartRequest>);

    impl Start {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::StartRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .start(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `start`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::start::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::StartRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::StartRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::StartRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::StartRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::StartRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Start {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::start_with_encryption_key][crate::client::Instances::start_with_encryption_key] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::StartWithEncryptionKey;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> StartWithEncryptionKey {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct StartWithEncryptionKey(
        RequestBuilder<crate::model::instances::StartWithEncryptionKeyRequest>,
    );

    impl StartWithEncryptionKey {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::StartWithEncryptionKeyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .start_with_encryption_key(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `start_with_encryption_key`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Instances::start_with_encryption_key::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::StartWithEncryptionKeyRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::StartWithEncryptionKeyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::StartWithEncryptionKeyRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::StartWithEncryptionKeyRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::StartWithEncryptionKeyRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::StartWithEncryptionKeyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstancesStartWithEncryptionKeyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::StartWithEncryptionKeyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstancesStartWithEncryptionKeyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for StartWithEncryptionKey {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::stop][crate::client::Instances::stop] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::Stop;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Stop {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Stop(RequestBuilder<crate::model::instances::StopRequest>);

    impl Stop {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::StopRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .stop(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `stop`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::stop::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [discard_local_ssd][crate::model::instances::StopRequest::discard_local_ssd].
        pub fn set_discard_local_ssd<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.discard_local_ssd = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [discard_local_ssd][crate::model::instances::StopRequest::discard_local_ssd].
        pub fn set_or_clear_discard_local_ssd<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.discard_local_ssd = v.map(|x| x.into());
            self
        }

        /// Sets the value of [instance][crate::model::instances::StopRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::StopRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::StopRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::StopRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::StopRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Stop {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::suspend][crate::client::Instances::suspend] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::Suspend;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Suspend {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Suspend(RequestBuilder<crate::model::instances::SuspendRequest>);

    impl Suspend {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::SuspendRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .suspend(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `suspend`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::suspend::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [discard_local_ssd][crate::model::instances::SuspendRequest::discard_local_ssd].
        pub fn set_discard_local_ssd<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.discard_local_ssd = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [discard_local_ssd][crate::model::instances::SuspendRequest::discard_local_ssd].
        pub fn set_or_clear_discard_local_ssd<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.discard_local_ssd = v.map(|x| x.into());
            self
        }

        /// Sets the value of [instance][crate::model::instances::SuspendRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::SuspendRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::SuspendRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::SuspendRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::SuspendRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Suspend {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::test_iam_permissions][crate::client::Instances::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::instances::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::instances::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::instances::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instances::TestIamPermissionsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::update][crate::client::Instances::update] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::Update;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Update {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Update(RequestBuilder<crate::model::instances::UpdateRequest>);

    impl Update {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::UpdateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::update::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [discard_local_ssd][crate::model::instances::UpdateRequest::discard_local_ssd].
        pub fn set_discard_local_ssd<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.discard_local_ssd = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [discard_local_ssd][crate::model::instances::UpdateRequest::discard_local_ssd].
        pub fn set_or_clear_discard_local_ssd<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.discard_local_ssd = v.map(|x| x.into());
            self
        }

        /// Sets the value of [instance][crate::model::instances::UpdateRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [minimal_action][crate::model::instances::UpdateRequest::minimal_action].
        pub fn set_minimal_action<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::instances::update_request::MinimalAction>,
        {
            self.0.request.minimal_action = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [minimal_action][crate::model::instances::UpdateRequest::minimal_action].
        pub fn set_or_clear_minimal_action<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::instances::update_request::MinimalAction>,
        {
            self.0.request.minimal_action = v.map(|x| x.into());
            self
        }

        /// Sets the value of [most_disruptive_allowed_action][crate::model::instances::UpdateRequest::most_disruptive_allowed_action].
        pub fn set_most_disruptive_allowed_action<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<
                    crate::model::instances::update_request::MostDisruptiveAllowedAction,
                >,
        {
            self.0.request.most_disruptive_allowed_action = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [most_disruptive_allowed_action][crate::model::instances::UpdateRequest::most_disruptive_allowed_action].
        pub fn set_or_clear_most_disruptive_allowed_action<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<
                    crate::model::instances::update_request::MostDisruptiveAllowedAction,
                >,
        {
            self.0.request.most_disruptive_allowed_action = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instances::UpdateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::UpdateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::UpdateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::UpdateRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::UpdateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Instance>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::UpdateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Instance>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Update {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::update_access_config][crate::client::Instances::update_access_config] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::UpdateAccessConfig;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateAccessConfig {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateAccessConfig(
        RequestBuilder<crate::model::instances::UpdateAccessConfigRequest>,
    );

    impl UpdateAccessConfig {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::UpdateAccessConfigRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update_access_config(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update_access_config`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::update_access_config::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::UpdateAccessConfigRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [network_interface][crate::model::instances::UpdateAccessConfigRequest::network_interface].
        pub fn set_network_interface<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network_interface = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::UpdateAccessConfigRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::UpdateAccessConfigRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::UpdateAccessConfigRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::UpdateAccessConfigRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::UpdateAccessConfigRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::AccessConfig>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::UpdateAccessConfigRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::AccessConfig>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateAccessConfig {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::update_display_device][crate::client::Instances::update_display_device] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::UpdateDisplayDevice;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateDisplayDevice {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateDisplayDevice(
        RequestBuilder<crate::model::instances::UpdateDisplayDeviceRequest>,
    );

    impl UpdateDisplayDevice {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::UpdateDisplayDeviceRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update_display_device(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update_display_device`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Instances::update_display_device::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::UpdateDisplayDeviceRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::UpdateDisplayDeviceRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::UpdateDisplayDeviceRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::UpdateDisplayDeviceRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::UpdateDisplayDeviceRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::UpdateDisplayDeviceRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DisplayDevice>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::UpdateDisplayDeviceRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DisplayDevice>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateDisplayDevice {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::update_network_interface][crate::client::Instances::update_network_interface] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::UpdateNetworkInterface;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateNetworkInterface {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateNetworkInterface(
        RequestBuilder<crate::model::instances::UpdateNetworkInterfaceRequest>,
    );

    impl UpdateNetworkInterface {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instances::UpdateNetworkInterfaceRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update_network_interface(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update_network_interface`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Instances::update_network_interface::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::UpdateNetworkInterfaceRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [network_interface][crate::model::instances::UpdateNetworkInterfaceRequest::network_interface].
        pub fn set_network_interface<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network_interface = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::UpdateNetworkInterfaceRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::UpdateNetworkInterfaceRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::UpdateNetworkInterfaceRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::UpdateNetworkInterfaceRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::UpdateNetworkInterfaceRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NetworkInterface>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::UpdateNetworkInterfaceRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NetworkInterface>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateNetworkInterface {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::update_shielded_instance_config][crate::client::Instances::update_shielded_instance_config] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::UpdateShieldedInstanceConfig;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateShieldedInstanceConfig {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateShieldedInstanceConfig(
        RequestBuilder<crate::model::instances::UpdateShieldedInstanceConfigRequest>,
    );

    impl UpdateShieldedInstanceConfig {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instances::UpdateShieldedInstanceConfigRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update_shielded_instance_config(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update_shielded_instance_config`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Instances::update_shielded_instance_config::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance][crate::model::instances::UpdateShieldedInstanceConfigRequest::instance].
        pub fn set_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instances::UpdateShieldedInstanceConfigRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instances::UpdateShieldedInstanceConfigRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instances::UpdateShieldedInstanceConfigRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instances::UpdateShieldedInstanceConfigRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instances::UpdateShieldedInstanceConfigRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ShieldedInstanceConfig>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instances::UpdateShieldedInstanceConfigRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ShieldedInstanceConfig>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateShieldedInstanceConfig {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Instances::get_operation][crate::client::Instances::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instances::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::zone_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Instances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::zone_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::zone_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zone_operations::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [InstantSnapshotGroups][crate::client::InstantSnapshotGroups].
#[cfg(feature = "instant-snapshot-groups")]
#[cfg_attr(docsrs, doc(cfg(feature = "instant-snapshot-groups")))]
pub mod instant_snapshot_groups {
    use crate::Result;

    /// A builder for [InstantSnapshotGroups][crate::client::InstantSnapshotGroups].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::instant_snapshot_groups::ClientBuilder;
    /// # use client::InstantSnapshotGroups;
    /// let builder : ClientBuilder = InstantSnapshotGroups::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::InstantSnapshotGroups;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = InstantSnapshotGroups;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::InstantSnapshotGroups] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::InstantSnapshotGroups>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstantSnapshotGroups>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [InstantSnapshotGroups::delete][crate::client::InstantSnapshotGroups::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instant_snapshot_groups::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::instant_snapshot_groups::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstantSnapshotGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instant_snapshot_groups::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::InstantSnapshotGroups::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instant_snapshot_group][crate::model::instant_snapshot_groups::DeleteRequest::instant_snapshot_group].
        pub fn set_instant_snapshot_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instant_snapshot_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instant_snapshot_groups::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instant_snapshot_groups::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instant_snapshot_groups::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instant_snapshot_groups::DeleteRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstantSnapshotGroups::get][crate::client::InstantSnapshotGroups::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instant_snapshot_groups::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::instant_snapshot_groups::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstantSnapshotGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instant_snapshot_groups::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstantSnapshotGroup> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [instant_snapshot_group][crate::model::instant_snapshot_groups::GetRequest::instant_snapshot_group].
        pub fn set_instant_snapshot_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instant_snapshot_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instant_snapshot_groups::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instant_snapshot_groups::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstantSnapshotGroups::get_iam_policy][crate::client::InstantSnapshotGroups::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instant_snapshot_groups::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(
        RequestBuilder<crate::model::instant_snapshot_groups::GetIamPolicyRequest>,
    );

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstantSnapshotGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instant_snapshot_groups::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::instant_snapshot_groups::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::instant_snapshot_groups::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instant_snapshot_groups::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::instant_snapshot_groups::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instant_snapshot_groups::GetIamPolicyRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstantSnapshotGroups::insert][crate::client::InstantSnapshotGroups::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instant_snapshot_groups::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::instant_snapshot_groups::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstantSnapshotGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instant_snapshot_groups::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::InstantSnapshotGroups::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::instant_snapshot_groups::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instant_snapshot_groups::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instant_snapshot_groups::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [source_consistency_group][crate::model::instant_snapshot_groups::InsertRequest::source_consistency_group].
        pub fn set_source_consistency_group<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.source_consistency_group = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [source_consistency_group][crate::model::instant_snapshot_groups::InsertRequest::source_consistency_group].
        pub fn set_or_clear_source_consistency_group<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.source_consistency_group = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instant_snapshot_groups::InsertRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instant_snapshot_groups::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstantSnapshotGroup>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instant_snapshot_groups::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstantSnapshotGroup>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstantSnapshotGroups::list][crate::client::InstantSnapshotGroups::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instant_snapshot_groups::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::instant_snapshot_groups::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstantSnapshotGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instant_snapshot_groups::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ListInstantSnapshotGroups> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::ListInstantSnapshotGroups,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ListInstantSnapshotGroups,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::instant_snapshot_groups::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::instant_snapshot_groups::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::instant_snapshot_groups::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::instant_snapshot_groups::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::instant_snapshot_groups::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::instant_snapshot_groups::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::instant_snapshot_groups::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::instant_snapshot_groups::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instant_snapshot_groups::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::instant_snapshot_groups::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::instant_snapshot_groups::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instant_snapshot_groups::ListRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstantSnapshotGroups::set_iam_policy][crate::client::InstantSnapshotGroups::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instant_snapshot_groups::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(
        RequestBuilder<crate::model::instant_snapshot_groups::SetIamPolicyRequest>,
    );

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstantSnapshotGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instant_snapshot_groups::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::instant_snapshot_groups::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::instant_snapshot_groups::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instant_snapshot_groups::SetIamPolicyRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instant_snapshot_groups::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ZoneSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instant_snapshot_groups::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ZoneSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstantSnapshotGroups::test_iam_permissions][crate::client::InstantSnapshotGroups::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instant_snapshot_groups::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::instant_snapshot_groups::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstantSnapshotGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::instant_snapshot_groups::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::instant_snapshot_groups::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::instant_snapshot_groups::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instant_snapshot_groups::TestIamPermissionsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instant_snapshot_groups::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instant_snapshot_groups::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstantSnapshotGroups::get_operation][crate::client::InstantSnapshotGroups::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instant_snapshot_groups::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::zone_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstantSnapshotGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::zone_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::zone_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zone_operations::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [InstantSnapshots][crate::client::InstantSnapshots].
#[cfg(feature = "instant-snapshots")]
#[cfg_attr(docsrs, doc(cfg(feature = "instant-snapshots")))]
pub mod instant_snapshots {
    use crate::Result;

    /// A builder for [InstantSnapshots][crate::client::InstantSnapshots].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::instant_snapshots::ClientBuilder;
    /// # use client::InstantSnapshots;
    /// let builder : ClientBuilder = InstantSnapshots::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::InstantSnapshots;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = InstantSnapshots;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::InstantSnapshots] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::InstantSnapshots>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstantSnapshots>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [InstantSnapshots::aggregated_list][crate::client::InstantSnapshots::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instant_snapshots::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::instant_snapshots::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstantSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instant_snapshots::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstantSnapshotAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::InstantSnapshotAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::InstantSnapshotAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::instant_snapshots::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::instant_snapshots::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::instant_snapshots::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::instant_snapshots::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::instant_snapshots::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::instant_snapshots::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::instant_snapshots::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::instant_snapshots::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::instant_snapshots::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::instant_snapshots::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instant_snapshots::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::instant_snapshots::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::instant_snapshots::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::instant_snapshots::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::instant_snapshots::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstantSnapshots::delete][crate::client::InstantSnapshots::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instant_snapshots::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::instant_snapshots::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstantSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instant_snapshots::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::InstantSnapshots::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instant_snapshot][crate::model::instant_snapshots::DeleteRequest::instant_snapshot].
        pub fn set_instant_snapshot<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instant_snapshot = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instant_snapshots::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instant_snapshots::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instant_snapshots::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instant_snapshots::DeleteRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstantSnapshots::get][crate::client::InstantSnapshots::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instant_snapshots::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::instant_snapshots::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstantSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instant_snapshots::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstantSnapshot> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [instant_snapshot][crate::model::instant_snapshots::GetRequest::instant_snapshot].
        pub fn set_instant_snapshot<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instant_snapshot = v.into();
            self
        }

        /// Sets the value of [project][crate::model::instant_snapshots::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instant_snapshots::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstantSnapshots::get_iam_policy][crate::client::InstantSnapshots::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instant_snapshots::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(RequestBuilder<crate::model::instant_snapshots::GetIamPolicyRequest>);

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstantSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instant_snapshots::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::instant_snapshots::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::instant_snapshots::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instant_snapshots::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::instant_snapshots::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instant_snapshots::GetIamPolicyRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstantSnapshots::insert][crate::client::InstantSnapshots::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instant_snapshots::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::instant_snapshots::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstantSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instant_snapshots::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::InstantSnapshots::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::instant_snapshots::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instant_snapshots::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instant_snapshots::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instant_snapshots::InsertRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instant_snapshots::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstantSnapshot>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instant_snapshots::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstantSnapshot>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstantSnapshots::list][crate::client::InstantSnapshots::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instant_snapshots::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::instant_snapshots::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstantSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instant_snapshots::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstantSnapshotList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::InstantSnapshotList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::InstantSnapshotList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::instant_snapshots::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::instant_snapshots::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::instant_snapshots::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::instant_snapshots::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::instant_snapshots::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::instant_snapshots::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::instant_snapshots::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::instant_snapshots::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::instant_snapshots::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::instant_snapshots::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::instant_snapshots::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::instant_snapshots::ListRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstantSnapshots::set_iam_policy][crate::client::InstantSnapshots::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instant_snapshots::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(RequestBuilder<crate::model::instant_snapshots::SetIamPolicyRequest>);

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstantSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instant_snapshots::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::instant_snapshots::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::instant_snapshots::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instant_snapshots::SetIamPolicyRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instant_snapshots::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ZoneSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instant_snapshots::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ZoneSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstantSnapshots::set_labels][crate::client::InstantSnapshots::set_labels] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instant_snapshots::SetLabels;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetLabels {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetLabels(RequestBuilder<crate::model::instant_snapshots::SetLabelsRequest>);

    impl SetLabels {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstantSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instant_snapshots::SetLabelsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_labels(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_labels`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::InstantSnapshots::set_labels::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::instant_snapshots::SetLabelsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::instant_snapshots::SetLabelsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::instant_snapshots::SetLabelsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [resource][crate::model::instant_snapshots::SetLabelsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instant_snapshots::SetLabelsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instant_snapshots::SetLabelsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ZoneSetLabelsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instant_snapshots::SetLabelsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ZoneSetLabelsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetLabels {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstantSnapshots::test_iam_permissions][crate::client::InstantSnapshots::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instant_snapshots::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::instant_snapshots::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstantSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::instant_snapshots::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::instant_snapshots::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::instant_snapshots::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::instant_snapshots::TestIamPermissionsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::instant_snapshots::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::instant_snapshots::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InstantSnapshots::get_operation][crate::client::InstantSnapshots::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::instant_snapshots::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::zone_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InstantSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::zone_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::zone_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zone_operations::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [InterconnectAttachmentGroups][crate::client::InterconnectAttachmentGroups].
#[cfg(feature = "interconnect-attachment-groups")]
#[cfg_attr(docsrs, doc(cfg(feature = "interconnect-attachment-groups")))]
pub mod interconnect_attachment_groups {
    use crate::Result;

    /// A builder for [InterconnectAttachmentGroups][crate::client::InterconnectAttachmentGroups].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::interconnect_attachment_groups::ClientBuilder;
    /// # use client::InterconnectAttachmentGroups;
    /// let builder : ClientBuilder = InterconnectAttachmentGroups::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::InterconnectAttachmentGroups;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = InterconnectAttachmentGroups;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::InterconnectAttachmentGroups] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectAttachmentGroups>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectAttachmentGroups>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [InterconnectAttachmentGroups::delete][crate::client::InterconnectAttachmentGroups::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_attachment_groups::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::interconnect_attachment_groups::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectAttachmentGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::interconnect_attachment_groups::DeleteRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::InterconnectAttachmentGroups::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [interconnect_attachment_group][crate::model::interconnect_attachment_groups::DeleteRequest::interconnect_attachment_group].
        pub fn set_interconnect_attachment_group<T: Into<std::string::String>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.interconnect_attachment_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::interconnect_attachment_groups::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::interconnect_attachment_groups::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::interconnect_attachment_groups::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectAttachmentGroups::get][crate::client::InterconnectAttachmentGroups::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_attachment_groups::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::interconnect_attachment_groups::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectAttachmentGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnect_attachment_groups::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InterconnectAttachmentGroup> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [interconnect_attachment_group][crate::model::interconnect_attachment_groups::GetRequest::interconnect_attachment_group].
        pub fn set_interconnect_attachment_group<T: Into<std::string::String>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.interconnect_attachment_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::interconnect_attachment_groups::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectAttachmentGroups::get_iam_policy][crate::client::InterconnectAttachmentGroups::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_attachment_groups::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(
        RequestBuilder<crate::model::interconnect_attachment_groups::GetIamPolicyRequest>,
    );

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectAttachmentGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::interconnect_attachment_groups::GetIamPolicyRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::interconnect_attachment_groups::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::interconnect_attachment_groups::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::interconnect_attachment_groups::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::interconnect_attachment_groups::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectAttachmentGroups::get_operational_status][crate::client::InterconnectAttachmentGroups::get_operational_status] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_attachment_groups::GetOperationalStatus;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperationalStatus {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperationalStatus(
        RequestBuilder<crate::model::interconnect_attachment_groups::GetOperationalStatusRequest>,
    );

    impl GetOperationalStatus {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectAttachmentGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::interconnect_attachment_groups::GetOperationalStatusRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(
            self,
        ) -> Result<crate::model::InterconnectAttachmentGroupsGetOperationalStatusResponse>
        {
            (*self.0.stub)
                .get_operational_status(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [interconnect_attachment_group][crate::model::interconnect_attachment_groups::GetOperationalStatusRequest::interconnect_attachment_group].
        pub fn set_interconnect_attachment_group<T: Into<std::string::String>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.interconnect_attachment_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::interconnect_attachment_groups::GetOperationalStatusRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperationalStatus {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectAttachmentGroups::insert][crate::client::InterconnectAttachmentGroups::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_attachment_groups::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::interconnect_attachment_groups::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectAttachmentGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::interconnect_attachment_groups::InsertRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::InterconnectAttachmentGroups::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::interconnect_attachment_groups::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::interconnect_attachment_groups::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::interconnect_attachment_groups::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::interconnect_attachment_groups::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InterconnectAttachmentGroup>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::interconnect_attachment_groups::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InterconnectAttachmentGroup>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectAttachmentGroups::list][crate::client::InterconnectAttachmentGroups::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_attachment_groups::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::interconnect_attachment_groups::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectAttachmentGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnect_attachment_groups::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InterconnectAttachmentGroupsListResponse> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::InterconnectAttachmentGroupsListResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::InterconnectAttachmentGroupsListResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::interconnect_attachment_groups::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::interconnect_attachment_groups::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::interconnect_attachment_groups::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::interconnect_attachment_groups::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::interconnect_attachment_groups::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::interconnect_attachment_groups::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::interconnect_attachment_groups::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::interconnect_attachment_groups::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::interconnect_attachment_groups::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::interconnect_attachment_groups::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::interconnect_attachment_groups::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectAttachmentGroups::patch][crate::client::InterconnectAttachmentGroups::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_attachment_groups::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::interconnect_attachment_groups::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectAttachmentGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnect_attachment_groups::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::InterconnectAttachmentGroups::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [interconnect_attachment_group][crate::model::interconnect_attachment_groups::PatchRequest::interconnect_attachment_group].
        pub fn set_interconnect_attachment_group<T: Into<std::string::String>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.interconnect_attachment_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::interconnect_attachment_groups::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::interconnect_attachment_groups::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::interconnect_attachment_groups::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::interconnect_attachment_groups::PatchRequest::update_mask].
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::interconnect_attachment_groups::PatchRequest::update_mask].
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::interconnect_attachment_groups::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InterconnectAttachmentGroup>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::interconnect_attachment_groups::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InterconnectAttachmentGroup>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectAttachmentGroups::set_iam_policy][crate::client::InterconnectAttachmentGroups::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_attachment_groups::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(
        RequestBuilder<crate::model::interconnect_attachment_groups::SetIamPolicyRequest>,
    );

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectAttachmentGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::interconnect_attachment_groups::SetIamPolicyRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::interconnect_attachment_groups::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::interconnect_attachment_groups::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::interconnect_attachment_groups::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::interconnect_attachment_groups::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectAttachmentGroups::test_iam_permissions][crate::client::InterconnectAttachmentGroups::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_attachment_groups::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::interconnect_attachment_groups::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectAttachmentGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::interconnect_attachment_groups::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::interconnect_attachment_groups::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::interconnect_attachment_groups::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::interconnect_attachment_groups::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::interconnect_attachment_groups::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectAttachmentGroups::get_operation][crate::client::InterconnectAttachmentGroups::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_attachment_groups::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectAttachmentGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [InterconnectAttachments][crate::client::InterconnectAttachments].
#[cfg(feature = "interconnect-attachments")]
#[cfg_attr(docsrs, doc(cfg(feature = "interconnect-attachments")))]
pub mod interconnect_attachments {
    use crate::Result;

    /// A builder for [InterconnectAttachments][crate::client::InterconnectAttachments].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::interconnect_attachments::ClientBuilder;
    /// # use client::InterconnectAttachments;
    /// let builder : ClientBuilder = InterconnectAttachments::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::InterconnectAttachments;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = InterconnectAttachments;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::InterconnectAttachments] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectAttachments>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectAttachments>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [InterconnectAttachments::aggregated_list][crate::client::InterconnectAttachments::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_attachments::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::interconnect_attachments::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::interconnect_attachments::AggregatedListRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InterconnectAttachmentAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::InterconnectAttachmentAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::InterconnectAttachmentAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::interconnect_attachments::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::interconnect_attachments::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::interconnect_attachments::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::interconnect_attachments::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::interconnect_attachments::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::interconnect_attachments::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::interconnect_attachments::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::interconnect_attachments::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::interconnect_attachments::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::interconnect_attachments::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::interconnect_attachments::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::interconnect_attachments::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::interconnect_attachments::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::interconnect_attachments::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::interconnect_attachments::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectAttachments::delete][crate::client::InterconnectAttachments::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_attachments::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::interconnect_attachments::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnect_attachments::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::InterconnectAttachments::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [interconnect_attachment][crate::model::interconnect_attachments::DeleteRequest::interconnect_attachment].
        pub fn set_interconnect_attachment<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.interconnect_attachment = v.into();
            self
        }

        /// Sets the value of [project][crate::model::interconnect_attachments::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::interconnect_attachments::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::interconnect_attachments::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::interconnect_attachments::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectAttachments::get][crate::client::InterconnectAttachments::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_attachments::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::interconnect_attachments::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnect_attachments::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InterconnectAttachment> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [interconnect_attachment][crate::model::interconnect_attachments::GetRequest::interconnect_attachment].
        pub fn set_interconnect_attachment<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.interconnect_attachment = v.into();
            self
        }

        /// Sets the value of [project][crate::model::interconnect_attachments::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::interconnect_attachments::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectAttachments::insert][crate::client::InterconnectAttachments::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_attachments::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::interconnect_attachments::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnect_attachments::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::InterconnectAttachments::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::interconnect_attachments::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::interconnect_attachments::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::interconnect_attachments::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::interconnect_attachments::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [validate_only][crate::model::interconnect_attachments::InsertRequest::validate_only].
        pub fn set_validate_only<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.validate_only = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [validate_only][crate::model::interconnect_attachments::InsertRequest::validate_only].
        pub fn set_or_clear_validate_only<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.validate_only = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::interconnect_attachments::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InterconnectAttachment>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::interconnect_attachments::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InterconnectAttachment>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectAttachments::list][crate::client::InterconnectAttachments::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_attachments::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::interconnect_attachments::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnect_attachments::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InterconnectAttachmentList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::InterconnectAttachmentList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::InterconnectAttachmentList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::interconnect_attachments::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::interconnect_attachments::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::interconnect_attachments::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::interconnect_attachments::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::interconnect_attachments::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::interconnect_attachments::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::interconnect_attachments::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::interconnect_attachments::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::interconnect_attachments::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::interconnect_attachments::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::interconnect_attachments::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::interconnect_attachments::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectAttachments::patch][crate::client::InterconnectAttachments::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_attachments::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::interconnect_attachments::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnect_attachments::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::InterconnectAttachments::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [interconnect_attachment][crate::model::interconnect_attachments::PatchRequest::interconnect_attachment].
        pub fn set_interconnect_attachment<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.interconnect_attachment = v.into();
            self
        }

        /// Sets the value of [project][crate::model::interconnect_attachments::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::interconnect_attachments::PatchRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::interconnect_attachments::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::interconnect_attachments::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::interconnect_attachments::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InterconnectAttachment>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::interconnect_attachments::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InterconnectAttachment>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectAttachments::set_labels][crate::client::InterconnectAttachments::set_labels] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_attachments::SetLabels;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetLabels {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetLabels(RequestBuilder<crate::model::interconnect_attachments::SetLabelsRequest>);

    impl SetLabels {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnect_attachments::SetLabelsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_labels(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_labels`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::InterconnectAttachments::set_labels::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::interconnect_attachments::SetLabelsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::interconnect_attachments::SetLabelsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::interconnect_attachments::SetLabelsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::interconnect_attachments::SetLabelsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [resource][crate::model::interconnect_attachments::SetLabelsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::interconnect_attachments::SetLabelsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetLabelsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::interconnect_attachments::SetLabelsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetLabelsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetLabels {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectAttachments::get_operation][crate::client::InterconnectAttachments::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_attachments::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [InterconnectGroups][crate::client::InterconnectGroups].
#[cfg(feature = "interconnect-groups")]
#[cfg_attr(docsrs, doc(cfg(feature = "interconnect-groups")))]
pub mod interconnect_groups {
    use crate::Result;

    /// A builder for [InterconnectGroups][crate::client::InterconnectGroups].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::interconnect_groups::ClientBuilder;
    /// # use client::InterconnectGroups;
    /// let builder : ClientBuilder = InterconnectGroups::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::InterconnectGroups;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = InterconnectGroups;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::InterconnectGroups] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectGroups>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectGroups>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [InterconnectGroups::create_members][crate::client::InterconnectGroups::create_members] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_groups::CreateMembers;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CreateMembers {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CreateMembers(
        RequestBuilder<crate::model::interconnect_groups::CreateMembersRequest>,
    );

    impl CreateMembers {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnect_groups::CreateMembersRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .create_members(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `create_members`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::InterconnectGroups::create_members::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [interconnect_group][crate::model::interconnect_groups::CreateMembersRequest::interconnect_group].
        pub fn set_interconnect_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.interconnect_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::interconnect_groups::CreateMembersRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [body][crate::model::interconnect_groups::CreateMembersRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InterconnectGroupsCreateMembersRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::interconnect_groups::CreateMembersRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InterconnectGroupsCreateMembersRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CreateMembers {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectGroups::delete][crate::client::InterconnectGroups::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_groups::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::interconnect_groups::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnect_groups::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::InterconnectGroups::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [interconnect_group][crate::model::interconnect_groups::DeleteRequest::interconnect_group].
        pub fn set_interconnect_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.interconnect_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::interconnect_groups::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::interconnect_groups::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::interconnect_groups::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectGroups::get][crate::client::InterconnectGroups::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_groups::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::interconnect_groups::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnect_groups::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InterconnectGroup> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [interconnect_group][crate::model::interconnect_groups::GetRequest::interconnect_group].
        pub fn set_interconnect_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.interconnect_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::interconnect_groups::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectGroups::get_iam_policy][crate::client::InterconnectGroups::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_groups::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(RequestBuilder<crate::model::interconnect_groups::GetIamPolicyRequest>);

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnect_groups::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::interconnect_groups::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::interconnect_groups::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::interconnect_groups::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::interconnect_groups::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectGroups::get_operational_status][crate::client::InterconnectGroups::get_operational_status] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_groups::GetOperationalStatus;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperationalStatus {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperationalStatus(
        RequestBuilder<crate::model::interconnect_groups::GetOperationalStatusRequest>,
    );

    impl GetOperationalStatus {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::interconnect_groups::GetOperationalStatusRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(
            self,
        ) -> Result<crate::model::InterconnectGroupsGetOperationalStatusResponse> {
            (*self.0.stub)
                .get_operational_status(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [interconnect_group][crate::model::interconnect_groups::GetOperationalStatusRequest::interconnect_group].
        pub fn set_interconnect_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.interconnect_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::interconnect_groups::GetOperationalStatusRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperationalStatus {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectGroups::insert][crate::client::InterconnectGroups::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_groups::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::interconnect_groups::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnect_groups::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::InterconnectGroups::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::interconnect_groups::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::interconnect_groups::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::interconnect_groups::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::interconnect_groups::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InterconnectGroup>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::interconnect_groups::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InterconnectGroup>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectGroups::list][crate::client::InterconnectGroups::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_groups::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::interconnect_groups::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnect_groups::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InterconnectGroupsListResponse> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::InterconnectGroupsListResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::InterconnectGroupsListResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::interconnect_groups::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::interconnect_groups::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::interconnect_groups::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::interconnect_groups::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::interconnect_groups::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::interconnect_groups::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::interconnect_groups::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::interconnect_groups::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::interconnect_groups::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::interconnect_groups::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::interconnect_groups::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectGroups::patch][crate::client::InterconnectGroups::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_groups::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::interconnect_groups::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnect_groups::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::InterconnectGroups::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [interconnect_group][crate::model::interconnect_groups::PatchRequest::interconnect_group].
        pub fn set_interconnect_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.interconnect_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::interconnect_groups::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::interconnect_groups::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::interconnect_groups::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::interconnect_groups::PatchRequest::update_mask].
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::interconnect_groups::PatchRequest::update_mask].
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::interconnect_groups::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InterconnectGroup>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::interconnect_groups::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InterconnectGroup>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectGroups::set_iam_policy][crate::client::InterconnectGroups::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_groups::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(RequestBuilder<crate::model::interconnect_groups::SetIamPolicyRequest>);

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnect_groups::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::interconnect_groups::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::interconnect_groups::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::interconnect_groups::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::interconnect_groups::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectGroups::test_iam_permissions][crate::client::InterconnectGroups::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_groups::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::interconnect_groups::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::interconnect_groups::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::interconnect_groups::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::interconnect_groups::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::interconnect_groups::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::interconnect_groups::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectGroups::get_operation][crate::client::InterconnectGroups::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_groups::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [InterconnectLocations][crate::client::InterconnectLocations].
#[cfg(feature = "interconnect-locations")]
#[cfg_attr(docsrs, doc(cfg(feature = "interconnect-locations")))]
pub mod interconnect_locations {
    use crate::Result;

    /// A builder for [InterconnectLocations][crate::client::InterconnectLocations].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::interconnect_locations::ClientBuilder;
    /// # use client::InterconnectLocations;
    /// let builder : ClientBuilder = InterconnectLocations::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::InterconnectLocations;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = InterconnectLocations;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::InterconnectLocations] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectLocations>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectLocations>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [InterconnectLocations::get][crate::client::InterconnectLocations::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_locations::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::interconnect_locations::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectLocations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnect_locations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InterconnectLocation> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [interconnect_location][crate::model::interconnect_locations::GetRequest::interconnect_location].
        pub fn set_interconnect_location<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.interconnect_location = v.into();
            self
        }

        /// Sets the value of [project][crate::model::interconnect_locations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectLocations::list][crate::client::InterconnectLocations::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_locations::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::interconnect_locations::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectLocations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnect_locations::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InterconnectLocationList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::InterconnectLocationList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::InterconnectLocationList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::interconnect_locations::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::interconnect_locations::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::interconnect_locations::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::interconnect_locations::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::interconnect_locations::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::interconnect_locations::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::interconnect_locations::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::interconnect_locations::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::interconnect_locations::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::interconnect_locations::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::interconnect_locations::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [InterconnectRemoteLocations][crate::client::InterconnectRemoteLocations].
#[cfg(feature = "interconnect-remote-locations")]
#[cfg_attr(docsrs, doc(cfg(feature = "interconnect-remote-locations")))]
pub mod interconnect_remote_locations {
    use crate::Result;

    /// A builder for [InterconnectRemoteLocations][crate::client::InterconnectRemoteLocations].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::interconnect_remote_locations::ClientBuilder;
    /// # use client::InterconnectRemoteLocations;
    /// let builder : ClientBuilder = InterconnectRemoteLocations::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::InterconnectRemoteLocations;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = InterconnectRemoteLocations;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::InterconnectRemoteLocations] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectRemoteLocations>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectRemoteLocations>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [InterconnectRemoteLocations::get][crate::client::InterconnectRemoteLocations::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_remote_locations::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::interconnect_remote_locations::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectRemoteLocations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnect_remote_locations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InterconnectRemoteLocation> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [interconnect_remote_location][crate::model::interconnect_remote_locations::GetRequest::interconnect_remote_location].
        pub fn set_interconnect_remote_location<T: Into<std::string::String>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.interconnect_remote_location = v.into();
            self
        }

        /// Sets the value of [project][crate::model::interconnect_remote_locations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [InterconnectRemoteLocations::list][crate::client::InterconnectRemoteLocations::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnect_remote_locations::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::interconnect_remote_locations::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::InterconnectRemoteLocations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnect_remote_locations::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InterconnectRemoteLocationList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::InterconnectRemoteLocationList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::InterconnectRemoteLocationList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::interconnect_remote_locations::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::interconnect_remote_locations::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::interconnect_remote_locations::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::interconnect_remote_locations::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::interconnect_remote_locations::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::interconnect_remote_locations::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::interconnect_remote_locations::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::interconnect_remote_locations::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::interconnect_remote_locations::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::interconnect_remote_locations::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::interconnect_remote_locations::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [Interconnects][crate::client::Interconnects].
#[cfg(feature = "interconnects")]
#[cfg_attr(docsrs, doc(cfg(feature = "interconnects")))]
pub mod interconnects {
    use crate::Result;

    /// A builder for [Interconnects][crate::client::Interconnects].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::interconnects::ClientBuilder;
    /// # use client::Interconnects;
    /// let builder : ClientBuilder = Interconnects::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::Interconnects;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = Interconnects;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::Interconnects] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::Interconnects>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Interconnects>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [Interconnects::delete][crate::client::Interconnects::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnects::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::interconnects::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Interconnects>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnects::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Interconnects::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [interconnect][crate::model::interconnects::DeleteRequest::interconnect].
        pub fn set_interconnect<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.interconnect = v.into();
            self
        }

        /// Sets the value of [project][crate::model::interconnects::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::interconnects::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::interconnects::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Interconnects::get][crate::client::Interconnects::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnects::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::interconnects::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Interconnects>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnects::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Interconnect> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [interconnect][crate::model::interconnects::GetRequest::interconnect].
        pub fn set_interconnect<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.interconnect = v.into();
            self
        }

        /// Sets the value of [project][crate::model::interconnects::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Interconnects::get_diagnostics][crate::client::Interconnects::get_diagnostics] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnects::GetDiagnostics;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetDiagnostics {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetDiagnostics(RequestBuilder<crate::model::interconnects::GetDiagnosticsRequest>);

    impl GetDiagnostics {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Interconnects>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnects::GetDiagnosticsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InterconnectsGetDiagnosticsResponse> {
            (*self.0.stub)
                .get_diagnostics(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [interconnect][crate::model::interconnects::GetDiagnosticsRequest::interconnect].
        pub fn set_interconnect<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.interconnect = v.into();
            self
        }

        /// Sets the value of [project][crate::model::interconnects::GetDiagnosticsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetDiagnostics {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Interconnects::get_macsec_config][crate::client::Interconnects::get_macsec_config] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnects::GetMacsecConfig;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetMacsecConfig {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetMacsecConfig(RequestBuilder<crate::model::interconnects::GetMacsecConfigRequest>);

    impl GetMacsecConfig {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Interconnects>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnects::GetMacsecConfigRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InterconnectsGetMacsecConfigResponse> {
            (*self.0.stub)
                .get_macsec_config(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [interconnect][crate::model::interconnects::GetMacsecConfigRequest::interconnect].
        pub fn set_interconnect<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.interconnect = v.into();
            self
        }

        /// Sets the value of [project][crate::model::interconnects::GetMacsecConfigRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetMacsecConfig {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Interconnects::insert][crate::client::Interconnects::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnects::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::interconnects::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Interconnects>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnects::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Interconnects::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::interconnects::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::interconnects::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::interconnects::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::interconnects::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Interconnect>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::interconnects::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Interconnect>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Interconnects::list][crate::client::Interconnects::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnects::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::interconnects::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Interconnects>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnects::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InterconnectList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::InterconnectList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::InterconnectList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::interconnects::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::interconnects::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::interconnects::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::interconnects::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::interconnects::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::interconnects::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::interconnects::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::interconnects::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::interconnects::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::interconnects::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::interconnects::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Interconnects::patch][crate::client::Interconnects::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnects::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::interconnects::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Interconnects>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnects::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Interconnects::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [interconnect][crate::model::interconnects::PatchRequest::interconnect].
        pub fn set_interconnect<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.interconnect = v.into();
            self
        }

        /// Sets the value of [project][crate::model::interconnects::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::interconnects::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::interconnects::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::interconnects::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Interconnect>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::interconnects::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Interconnect>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Interconnects::set_labels][crate::client::Interconnects::set_labels] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnects::SetLabels;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetLabels {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetLabels(RequestBuilder<crate::model::interconnects::SetLabelsRequest>);

    impl SetLabels {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Interconnects>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::interconnects::SetLabelsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_labels(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_labels`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Interconnects::set_labels::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::interconnects::SetLabelsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::interconnects::SetLabelsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::interconnects::SetLabelsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetLabelsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::interconnects::SetLabelsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetLabelsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetLabels {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Interconnects::get_operation][crate::client::Interconnects::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::interconnects::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Interconnects>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [LicenseCodes][crate::client::LicenseCodes].
#[cfg(feature = "license-codes")]
#[cfg_attr(docsrs, doc(cfg(feature = "license-codes")))]
pub mod license_codes {
    use crate::Result;

    /// A builder for [LicenseCodes][crate::client::LicenseCodes].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::license_codes::ClientBuilder;
    /// # use client::LicenseCodes;
    /// let builder : ClientBuilder = LicenseCodes::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::LicenseCodes;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = LicenseCodes;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::LicenseCodes] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::LicenseCodes>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::LicenseCodes>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [LicenseCodes::get][crate::client::LicenseCodes::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::license_codes::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::license_codes::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::LicenseCodes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::license_codes::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::LicenseCode> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [license_code][crate::model::license_codes::GetRequest::license_code].
        pub fn set_license_code<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.license_code = v.into();
            self
        }

        /// Sets the value of [project][crate::model::license_codes::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [LicenseCodes::get_iam_policy][crate::client::LicenseCodes::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::license_codes::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(RequestBuilder<crate::model::license_codes::GetIamPolicyRequest>);

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::LicenseCodes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::license_codes::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::license_codes::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::license_codes::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::license_codes::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::license_codes::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [LicenseCodes::set_iam_policy][crate::client::LicenseCodes::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::license_codes::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(RequestBuilder<crate::model::license_codes::SetIamPolicyRequest>);

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::LicenseCodes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::license_codes::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::license_codes::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::license_codes::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::license_codes::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::license_codes::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [LicenseCodes::test_iam_permissions][crate::client::LicenseCodes::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::license_codes::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::license_codes::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::LicenseCodes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::license_codes::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::license_codes::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::license_codes::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::license_codes::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::license_codes::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [Licenses][crate::client::Licenses].
#[cfg(feature = "licenses")]
#[cfg_attr(docsrs, doc(cfg(feature = "licenses")))]
pub mod licenses {
    use crate::Result;

    /// A builder for [Licenses][crate::client::Licenses].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::licenses::ClientBuilder;
    /// # use client::Licenses;
    /// let builder : ClientBuilder = Licenses::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::Licenses;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = Licenses;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::Licenses] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::Licenses>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Licenses>) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [Licenses::delete][crate::client::Licenses::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::licenses::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::licenses::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Licenses>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::licenses::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Licenses::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [license][crate::model::licenses::DeleteRequest::license].
        pub fn set_license<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.license = v.into();
            self
        }

        /// Sets the value of [project][crate::model::licenses::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::licenses::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::licenses::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Licenses::get][crate::client::Licenses::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::licenses::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::licenses::GetRequest>);

    impl Get {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Licenses>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::licenses::GetRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::License> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [license][crate::model::licenses::GetRequest::license].
        pub fn set_license<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.license = v.into();
            self
        }

        /// Sets the value of [project][crate::model::licenses::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Licenses::get_iam_policy][crate::client::Licenses::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::licenses::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(RequestBuilder<crate::model::licenses::GetIamPolicyRequest>);

    impl GetIamPolicy {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Licenses>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::licenses::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::licenses::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::licenses::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::licenses::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::licenses::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Licenses::insert][crate::client::Licenses::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::licenses::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::licenses::InsertRequest>);

    impl Insert {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Licenses>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::licenses::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Licenses::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::licenses::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::licenses::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::licenses::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::licenses::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::License>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::licenses::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::License>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Licenses::list][crate::client::Licenses::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::licenses::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::licenses::ListRequest>);

    impl List {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Licenses>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::licenses::ListRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::LicensesListResponse> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::LicensesListResponse, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::LicensesListResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::licenses::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::licenses::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::licenses::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::licenses::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::licenses::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::licenses::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::licenses::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::licenses::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::licenses::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::licenses::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::licenses::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Licenses::set_iam_policy][crate::client::Licenses::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::licenses::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(RequestBuilder<crate::model::licenses::SetIamPolicyRequest>);

    impl SetIamPolicy {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Licenses>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::licenses::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::licenses::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::licenses::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::licenses::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::licenses::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Licenses::test_iam_permissions][crate::client::Licenses::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::licenses::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::licenses::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Licenses>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::licenses::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::licenses::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::licenses::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::licenses::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::licenses::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Licenses::update][crate::client::Licenses::update] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::licenses::Update;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Update {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Update(RequestBuilder<crate::model::licenses::UpdateRequest>);

    impl Update {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Licenses>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::licenses::UpdateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Licenses::update::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [license][crate::model::licenses::UpdateRequest::license].
        pub fn set_license<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.license = v.into();
            self
        }

        /// Sets the value of [project][crate::model::licenses::UpdateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::licenses::UpdateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::licenses::UpdateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::licenses::UpdateRequest::update_mask].
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::licenses::UpdateRequest::update_mask].
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::licenses::UpdateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::License>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::licenses::UpdateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::License>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Update {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Licenses::get_operation][crate::client::Licenses::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::licenses::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Licenses>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [MachineImages][crate::client::MachineImages].
#[cfg(feature = "machine-images")]
#[cfg_attr(docsrs, doc(cfg(feature = "machine-images")))]
pub mod machine_images {
    use crate::Result;

    /// A builder for [MachineImages][crate::client::MachineImages].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::machine_images::ClientBuilder;
    /// # use client::MachineImages;
    /// let builder : ClientBuilder = MachineImages::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::MachineImages;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = MachineImages;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::MachineImages] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::MachineImages>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::MachineImages>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [MachineImages::delete][crate::client::MachineImages::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::machine_images::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::machine_images::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::MachineImages>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::machine_images::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::MachineImages::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [machine_image][crate::model::machine_images::DeleteRequest::machine_image].
        pub fn set_machine_image<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.machine_image = v.into();
            self
        }

        /// Sets the value of [project][crate::model::machine_images::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::machine_images::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::machine_images::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [MachineImages::get][crate::client::MachineImages::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::machine_images::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::machine_images::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::MachineImages>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::machine_images::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::MachineImage> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [machine_image][crate::model::machine_images::GetRequest::machine_image].
        pub fn set_machine_image<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.machine_image = v.into();
            self
        }

        /// Sets the value of [project][crate::model::machine_images::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [MachineImages::get_iam_policy][crate::client::MachineImages::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::machine_images::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(RequestBuilder<crate::model::machine_images::GetIamPolicyRequest>);

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::MachineImages>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::machine_images::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::machine_images::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::machine_images::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::machine_images::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::machine_images::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [MachineImages::insert][crate::client::MachineImages::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::machine_images::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::machine_images::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::MachineImages>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::machine_images::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::MachineImages::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::machine_images::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::machine_images::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::machine_images::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [source_instance][crate::model::machine_images::InsertRequest::source_instance].
        pub fn set_source_instance<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.source_instance = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [source_instance][crate::model::machine_images::InsertRequest::source_instance].
        pub fn set_or_clear_source_instance<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.source_instance = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::machine_images::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::MachineImage>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::machine_images::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::MachineImage>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [MachineImages::list][crate::client::MachineImages::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::machine_images::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::machine_images::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::MachineImages>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::machine_images::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::MachineImageList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::MachineImageList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::MachineImageList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::machine_images::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::machine_images::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::machine_images::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::machine_images::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::machine_images::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::machine_images::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::machine_images::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::machine_images::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::machine_images::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::machine_images::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::machine_images::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [MachineImages::set_iam_policy][crate::client::MachineImages::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::machine_images::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(RequestBuilder<crate::model::machine_images::SetIamPolicyRequest>);

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::MachineImages>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::machine_images::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::machine_images::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::machine_images::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::machine_images::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::machine_images::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [MachineImages::set_labels][crate::client::MachineImages::set_labels] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::machine_images::SetLabels;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetLabels {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetLabels(RequestBuilder<crate::model::machine_images::SetLabelsRequest>);

    impl SetLabels {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::MachineImages>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::machine_images::SetLabelsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_labels(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_labels`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::MachineImages::set_labels::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::machine_images::SetLabelsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::machine_images::SetLabelsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::machine_images::SetLabelsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetLabelsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::machine_images::SetLabelsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetLabelsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetLabels {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [MachineImages::test_iam_permissions][crate::client::MachineImages::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::machine_images::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::machine_images::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::MachineImages>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::machine_images::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::machine_images::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::machine_images::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::machine_images::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::machine_images::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [MachineImages::get_operation][crate::client::MachineImages::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::machine_images::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::MachineImages>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [MachineTypes][crate::client::MachineTypes].
#[cfg(feature = "machine-types")]
#[cfg_attr(docsrs, doc(cfg(feature = "machine-types")))]
pub mod machine_types {
    use crate::Result;

    /// A builder for [MachineTypes][crate::client::MachineTypes].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::machine_types::ClientBuilder;
    /// # use client::MachineTypes;
    /// let builder : ClientBuilder = MachineTypes::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::MachineTypes;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = MachineTypes;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::MachineTypes] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::MachineTypes>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::MachineTypes>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [MachineTypes::aggregated_list][crate::client::MachineTypes::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::machine_types::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(RequestBuilder<crate::model::machine_types::AggregatedListRequest>);

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::MachineTypes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::machine_types::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::MachineTypeAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::MachineTypeAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::MachineTypeAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::machine_types::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::machine_types::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::machine_types::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::machine_types::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::machine_types::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::machine_types::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::machine_types::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::machine_types::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::machine_types::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::machine_types::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::machine_types::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::machine_types::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::machine_types::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::machine_types::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::machine_types::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [MachineTypes::get][crate::client::MachineTypes::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::machine_types::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::machine_types::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::MachineTypes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::machine_types::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::MachineType> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [machine_type][crate::model::machine_types::GetRequest::machine_type].
        pub fn set_machine_type<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.machine_type = v.into();
            self
        }

        /// Sets the value of [project][crate::model::machine_types::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::machine_types::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [MachineTypes::list][crate::client::MachineTypes::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::machine_types::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::machine_types::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::MachineTypes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::machine_types::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::MachineTypeList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::MachineTypeList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::MachineTypeList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::machine_types::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::machine_types::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::machine_types::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::machine_types::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::machine_types::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::machine_types::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::machine_types::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::machine_types::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::machine_types::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::machine_types::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::machine_types::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::machine_types::ListRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [NetworkAttachments][crate::client::NetworkAttachments].
#[cfg(feature = "network-attachments")]
#[cfg_attr(docsrs, doc(cfg(feature = "network-attachments")))]
pub mod network_attachments {
    use crate::Result;

    /// A builder for [NetworkAttachments][crate::client::NetworkAttachments].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::network_attachments::ClientBuilder;
    /// # use client::NetworkAttachments;
    /// let builder : ClientBuilder = NetworkAttachments::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::NetworkAttachments;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = NetworkAttachments;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::NetworkAttachments] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkAttachments>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkAttachments>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [NetworkAttachments::aggregated_list][crate::client::NetworkAttachments::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_attachments::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::network_attachments::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_attachments::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NetworkAttachmentAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::NetworkAttachmentAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::NetworkAttachmentAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::network_attachments::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::network_attachments::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::network_attachments::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::network_attachments::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::network_attachments::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::network_attachments::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::network_attachments::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::network_attachments::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::network_attachments::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::network_attachments::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::network_attachments::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::network_attachments::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::network_attachments::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::network_attachments::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::network_attachments::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkAttachments::delete][crate::client::NetworkAttachments::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_attachments::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::network_attachments::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_attachments::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::NetworkAttachments::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [network_attachment][crate::model::network_attachments::DeleteRequest::network_attachment].
        pub fn set_network_attachment<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network_attachment = v.into();
            self
        }

        /// Sets the value of [project][crate::model::network_attachments::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::network_attachments::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::network_attachments::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::network_attachments::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkAttachments::get][crate::client::NetworkAttachments::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_attachments::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::network_attachments::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_attachments::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NetworkAttachment> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [network_attachment][crate::model::network_attachments::GetRequest::network_attachment].
        pub fn set_network_attachment<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network_attachment = v.into();
            self
        }

        /// Sets the value of [project][crate::model::network_attachments::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::network_attachments::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkAttachments::get_iam_policy][crate::client::NetworkAttachments::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_attachments::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(RequestBuilder<crate::model::network_attachments::GetIamPolicyRequest>);

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_attachments::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::network_attachments::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::network_attachments::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::network_attachments::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::network_attachments::GetIamPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::network_attachments::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkAttachments::insert][crate::client::NetworkAttachments::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_attachments::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::network_attachments::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_attachments::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::NetworkAttachments::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::network_attachments::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::network_attachments::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::network_attachments::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::network_attachments::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::network_attachments::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NetworkAttachment>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::network_attachments::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NetworkAttachment>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkAttachments::list][crate::client::NetworkAttachments::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_attachments::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::network_attachments::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_attachments::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NetworkAttachmentList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::NetworkAttachmentList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::NetworkAttachmentList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::network_attachments::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::network_attachments::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::network_attachments::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::network_attachments::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::network_attachments::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::network_attachments::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::network_attachments::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::network_attachments::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::network_attachments::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::network_attachments::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::network_attachments::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::network_attachments::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkAttachments::patch][crate::client::NetworkAttachments::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_attachments::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::network_attachments::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_attachments::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::NetworkAttachments::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [network_attachment][crate::model::network_attachments::PatchRequest::network_attachment].
        pub fn set_network_attachment<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network_attachment = v.into();
            self
        }

        /// Sets the value of [project][crate::model::network_attachments::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::network_attachments::PatchRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::network_attachments::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::network_attachments::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::network_attachments::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NetworkAttachment>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::network_attachments::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NetworkAttachment>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkAttachments::set_iam_policy][crate::client::NetworkAttachments::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_attachments::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(RequestBuilder<crate::model::network_attachments::SetIamPolicyRequest>);

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_attachments::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::network_attachments::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::network_attachments::SetIamPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::network_attachments::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::network_attachments::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::network_attachments::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkAttachments::test_iam_permissions][crate::client::NetworkAttachments::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_attachments::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::network_attachments::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::network_attachments::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::network_attachments::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::network_attachments::TestIamPermissionsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::network_attachments::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::network_attachments::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::network_attachments::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkAttachments::get_operation][crate::client::NetworkAttachments::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_attachments::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [NetworkEdgeSecurityServices][crate::client::NetworkEdgeSecurityServices].
#[cfg(feature = "network-edge-security-services")]
#[cfg_attr(docsrs, doc(cfg(feature = "network-edge-security-services")))]
pub mod network_edge_security_services {
    use crate::Result;

    /// A builder for [NetworkEdgeSecurityServices][crate::client::NetworkEdgeSecurityServices].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::network_edge_security_services::ClientBuilder;
    /// # use client::NetworkEdgeSecurityServices;
    /// let builder : ClientBuilder = NetworkEdgeSecurityServices::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::NetworkEdgeSecurityServices;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = NetworkEdgeSecurityServices;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::NetworkEdgeSecurityServices] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkEdgeSecurityServices>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkEdgeSecurityServices>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [NetworkEdgeSecurityServices::aggregated_list][crate::client::NetworkEdgeSecurityServices::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_edge_security_services::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::network_edge_security_services::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkEdgeSecurityServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::network_edge_security_services::AggregatedListRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NetworkEdgeSecurityServiceAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::NetworkEdgeSecurityServiceAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::NetworkEdgeSecurityServiceAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::network_edge_security_services::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::network_edge_security_services::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::network_edge_security_services::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::network_edge_security_services::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::network_edge_security_services::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::network_edge_security_services::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::network_edge_security_services::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::network_edge_security_services::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::network_edge_security_services::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::network_edge_security_services::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::network_edge_security_services::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::network_edge_security_services::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::network_edge_security_services::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::network_edge_security_services::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::network_edge_security_services::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkEdgeSecurityServices::delete][crate::client::NetworkEdgeSecurityServices::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_edge_security_services::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::network_edge_security_services::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkEdgeSecurityServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::network_edge_security_services::DeleteRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::NetworkEdgeSecurityServices::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [network_edge_security_service][crate::model::network_edge_security_services::DeleteRequest::network_edge_security_service].
        pub fn set_network_edge_security_service<T: Into<std::string::String>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.network_edge_security_service = v.into();
            self
        }

        /// Sets the value of [project][crate::model::network_edge_security_services::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::network_edge_security_services::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::network_edge_security_services::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::network_edge_security_services::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkEdgeSecurityServices::get][crate::client::NetworkEdgeSecurityServices::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_edge_security_services::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::network_edge_security_services::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkEdgeSecurityServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_edge_security_services::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NetworkEdgeSecurityService> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [network_edge_security_service][crate::model::network_edge_security_services::GetRequest::network_edge_security_service].
        pub fn set_network_edge_security_service<T: Into<std::string::String>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.network_edge_security_service = v.into();
            self
        }

        /// Sets the value of [project][crate::model::network_edge_security_services::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::network_edge_security_services::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkEdgeSecurityServices::insert][crate::client::NetworkEdgeSecurityServices::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_edge_security_services::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::network_edge_security_services::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkEdgeSecurityServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::network_edge_security_services::InsertRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::NetworkEdgeSecurityServices::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::network_edge_security_services::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::network_edge_security_services::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::network_edge_security_services::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::network_edge_security_services::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [validate_only][crate::model::network_edge_security_services::InsertRequest::validate_only].
        pub fn set_validate_only<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.validate_only = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [validate_only][crate::model::network_edge_security_services::InsertRequest::validate_only].
        pub fn set_or_clear_validate_only<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.validate_only = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::network_edge_security_services::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NetworkEdgeSecurityService>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::network_edge_security_services::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NetworkEdgeSecurityService>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkEdgeSecurityServices::patch][crate::client::NetworkEdgeSecurityServices::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_edge_security_services::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::network_edge_security_services::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkEdgeSecurityServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_edge_security_services::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::NetworkEdgeSecurityServices::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [network_edge_security_service][crate::model::network_edge_security_services::PatchRequest::network_edge_security_service].
        pub fn set_network_edge_security_service<T: Into<std::string::String>>(
            mut self,
            v: T,
        ) -> Self {
            self.0.request.network_edge_security_service = v.into();
            self
        }

        /// Sets the value of [paths][crate::model::network_edge_security_services::PatchRequest::paths].
        pub fn set_paths<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.paths = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [paths][crate::model::network_edge_security_services::PatchRequest::paths].
        pub fn set_or_clear_paths<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.paths = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::network_edge_security_services::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::network_edge_security_services::PatchRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::network_edge_security_services::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::network_edge_security_services::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::network_edge_security_services::PatchRequest::update_mask].
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::network_edge_security_services::PatchRequest::update_mask].
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::network_edge_security_services::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NetworkEdgeSecurityService>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::network_edge_security_services::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NetworkEdgeSecurityService>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkEdgeSecurityServices::get_operation][crate::client::NetworkEdgeSecurityServices::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_edge_security_services::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkEdgeSecurityServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [NetworkEndpointGroups][crate::client::NetworkEndpointGroups].
#[cfg(feature = "network-endpoint-groups")]
#[cfg_attr(docsrs, doc(cfg(feature = "network-endpoint-groups")))]
pub mod network_endpoint_groups {
    use crate::Result;

    /// A builder for [NetworkEndpointGroups][crate::client::NetworkEndpointGroups].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::network_endpoint_groups::ClientBuilder;
    /// # use client::NetworkEndpointGroups;
    /// let builder : ClientBuilder = NetworkEndpointGroups::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::NetworkEndpointGroups;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = NetworkEndpointGroups;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::NetworkEndpointGroups] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkEndpointGroups>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkEndpointGroups>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [NetworkEndpointGroups::aggregated_list][crate::client::NetworkEndpointGroups::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_endpoint_groups::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::network_endpoint_groups::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::network_endpoint_groups::AggregatedListRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NetworkEndpointGroupAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::NetworkEndpointGroupAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::NetworkEndpointGroupAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::network_endpoint_groups::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::network_endpoint_groups::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::network_endpoint_groups::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::network_endpoint_groups::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::network_endpoint_groups::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::network_endpoint_groups::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::network_endpoint_groups::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::network_endpoint_groups::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::network_endpoint_groups::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::network_endpoint_groups::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::network_endpoint_groups::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::network_endpoint_groups::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::network_endpoint_groups::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::network_endpoint_groups::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::network_endpoint_groups::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkEndpointGroups::attach_network_endpoints][crate::client::NetworkEndpointGroups::attach_network_endpoints] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_endpoint_groups::AttachNetworkEndpoints;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AttachNetworkEndpoints {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AttachNetworkEndpoints(
        RequestBuilder<crate::model::network_endpoint_groups::AttachNetworkEndpointsRequest>,
    );

    impl AttachNetworkEndpoints {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::network_endpoint_groups::AttachNetworkEndpointsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .attach_network_endpoints(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `attach_network_endpoints`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::NetworkEndpointGroups::attach_network_endpoints::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [network_endpoint_group][crate::model::network_endpoint_groups::AttachNetworkEndpointsRequest::network_endpoint_group].
        pub fn set_network_endpoint_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network_endpoint_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::network_endpoint_groups::AttachNetworkEndpointsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::network_endpoint_groups::AttachNetworkEndpointsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::network_endpoint_groups::AttachNetworkEndpointsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::network_endpoint_groups::AttachNetworkEndpointsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::network_endpoint_groups::AttachNetworkEndpointsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NetworkEndpointGroupsAttachEndpointsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::network_endpoint_groups::AttachNetworkEndpointsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NetworkEndpointGroupsAttachEndpointsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AttachNetworkEndpoints {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkEndpointGroups::delete][crate::client::NetworkEndpointGroups::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_endpoint_groups::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::network_endpoint_groups::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_endpoint_groups::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::NetworkEndpointGroups::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [network_endpoint_group][crate::model::network_endpoint_groups::DeleteRequest::network_endpoint_group].
        pub fn set_network_endpoint_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network_endpoint_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::network_endpoint_groups::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::network_endpoint_groups::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::network_endpoint_groups::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::network_endpoint_groups::DeleteRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkEndpointGroups::detach_network_endpoints][crate::client::NetworkEndpointGroups::detach_network_endpoints] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_endpoint_groups::DetachNetworkEndpoints;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DetachNetworkEndpoints {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DetachNetworkEndpoints(
        RequestBuilder<crate::model::network_endpoint_groups::DetachNetworkEndpointsRequest>,
    );

    impl DetachNetworkEndpoints {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::network_endpoint_groups::DetachNetworkEndpointsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .detach_network_endpoints(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `detach_network_endpoints`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::NetworkEndpointGroups::detach_network_endpoints::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [network_endpoint_group][crate::model::network_endpoint_groups::DetachNetworkEndpointsRequest::network_endpoint_group].
        pub fn set_network_endpoint_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network_endpoint_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::network_endpoint_groups::DetachNetworkEndpointsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::network_endpoint_groups::DetachNetworkEndpointsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::network_endpoint_groups::DetachNetworkEndpointsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::network_endpoint_groups::DetachNetworkEndpointsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::network_endpoint_groups::DetachNetworkEndpointsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NetworkEndpointGroupsDetachEndpointsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::network_endpoint_groups::DetachNetworkEndpointsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NetworkEndpointGroupsDetachEndpointsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DetachNetworkEndpoints {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkEndpointGroups::get][crate::client::NetworkEndpointGroups::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_endpoint_groups::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::network_endpoint_groups::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_endpoint_groups::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NetworkEndpointGroup> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [network_endpoint_group][crate::model::network_endpoint_groups::GetRequest::network_endpoint_group].
        pub fn set_network_endpoint_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network_endpoint_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::network_endpoint_groups::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::network_endpoint_groups::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkEndpointGroups::insert][crate::client::NetworkEndpointGroups::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_endpoint_groups::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::network_endpoint_groups::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_endpoint_groups::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::NetworkEndpointGroups::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::network_endpoint_groups::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::network_endpoint_groups::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::network_endpoint_groups::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::network_endpoint_groups::InsertRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::network_endpoint_groups::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NetworkEndpointGroup>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::network_endpoint_groups::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NetworkEndpointGroup>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkEndpointGroups::list][crate::client::NetworkEndpointGroups::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_endpoint_groups::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::network_endpoint_groups::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_endpoint_groups::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NetworkEndpointGroupList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::NetworkEndpointGroupList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::NetworkEndpointGroupList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::network_endpoint_groups::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::network_endpoint_groups::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::network_endpoint_groups::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::network_endpoint_groups::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::network_endpoint_groups::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::network_endpoint_groups::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::network_endpoint_groups::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::network_endpoint_groups::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::network_endpoint_groups::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::network_endpoint_groups::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::network_endpoint_groups::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::network_endpoint_groups::ListRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkEndpointGroups::list_network_endpoints][crate::client::NetworkEndpointGroups::list_network_endpoints] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_endpoint_groups::ListNetworkEndpoints;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListNetworkEndpoints {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListNetworkEndpoints(
        RequestBuilder<crate::model::network_endpoint_groups::ListNetworkEndpointsRequest>,
    );

    impl ListNetworkEndpoints {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::network_endpoint_groups::ListNetworkEndpointsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NetworkEndpointGroupsListNetworkEndpoints> {
            (*self.0.stub)
                .list_network_endpoints(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::NetworkEndpointGroupsListNetworkEndpoints,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::NetworkEndpointGroupsListNetworkEndpoints,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::network_endpoint_groups::ListNetworkEndpointsRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::network_endpoint_groups::ListNetworkEndpointsRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::network_endpoint_groups::ListNetworkEndpointsRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::network_endpoint_groups::ListNetworkEndpointsRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [network_endpoint_group][crate::model::network_endpoint_groups::ListNetworkEndpointsRequest::network_endpoint_group].
        pub fn set_network_endpoint_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network_endpoint_group = v.into();
            self
        }

        /// Sets the value of [order_by][crate::model::network_endpoint_groups::ListNetworkEndpointsRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::network_endpoint_groups::ListNetworkEndpointsRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::network_endpoint_groups::ListNetworkEndpointsRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::network_endpoint_groups::ListNetworkEndpointsRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::network_endpoint_groups::ListNetworkEndpointsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::network_endpoint_groups::ListNetworkEndpointsRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::network_endpoint_groups::ListNetworkEndpointsRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::network_endpoint_groups::ListNetworkEndpointsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::network_endpoint_groups::ListNetworkEndpointsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NetworkEndpointGroupsListEndpointsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::network_endpoint_groups::ListNetworkEndpointsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NetworkEndpointGroupsListEndpointsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListNetworkEndpoints {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkEndpointGroups::test_iam_permissions][crate::client::NetworkEndpointGroups::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_endpoint_groups::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::network_endpoint_groups::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::network_endpoint_groups::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::network_endpoint_groups::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::network_endpoint_groups::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::network_endpoint_groups::TestIamPermissionsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::network_endpoint_groups::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::network_endpoint_groups::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkEndpointGroups::get_operation][crate::client::NetworkEndpointGroups::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_endpoint_groups::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::zone_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::zone_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::zone_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zone_operations::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [NetworkFirewallPolicies][crate::client::NetworkFirewallPolicies].
#[cfg(feature = "network-firewall-policies")]
#[cfg_attr(docsrs, doc(cfg(feature = "network-firewall-policies")))]
pub mod network_firewall_policies {
    use crate::Result;

    /// A builder for [NetworkFirewallPolicies][crate::client::NetworkFirewallPolicies].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::network_firewall_policies::ClientBuilder;
    /// # use client::NetworkFirewallPolicies;
    /// let builder : ClientBuilder = NetworkFirewallPolicies::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::NetworkFirewallPolicies;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = NetworkFirewallPolicies;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::NetworkFirewallPolicies] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkFirewallPolicies>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkFirewallPolicies>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [NetworkFirewallPolicies::add_association][crate::client::NetworkFirewallPolicies::add_association] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_firewall_policies::AddAssociation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AddAssociation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AddAssociation(
        RequestBuilder<crate::model::network_firewall_policies::AddAssociationRequest>,
    );

    impl AddAssociation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::network_firewall_policies::AddAssociationRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .add_association(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `add_association`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::NetworkFirewallPolicies::add_association::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::network_firewall_policies::AddAssociationRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [project][crate::model::network_firewall_policies::AddAssociationRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [replace_existing_association][crate::model::network_firewall_policies::AddAssociationRequest::replace_existing_association].
        pub fn set_replace_existing_association<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.replace_existing_association = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [replace_existing_association][crate::model::network_firewall_policies::AddAssociationRequest::replace_existing_association].
        pub fn set_or_clear_replace_existing_association<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.replace_existing_association = v.map(|x| x.into());
            self
        }

        /// Sets the value of [request_id][crate::model::network_firewall_policies::AddAssociationRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::network_firewall_policies::AddAssociationRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::network_firewall_policies::AddAssociationRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicyAssociation>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::network_firewall_policies::AddAssociationRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicyAssociation>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AddAssociation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkFirewallPolicies::add_packet_mirroring_rule][crate::client::NetworkFirewallPolicies::add_packet_mirroring_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_firewall_policies::AddPacketMirroringRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AddPacketMirroringRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AddPacketMirroringRule(
        RequestBuilder<crate::model::network_firewall_policies::AddPacketMirroringRuleRequest>,
    );

    impl AddPacketMirroringRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::network_firewall_policies::AddPacketMirroringRuleRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .add_packet_mirroring_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `add_packet_mirroring_rule`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::NetworkFirewallPolicies::add_packet_mirroring_rule::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::network_firewall_policies::AddPacketMirroringRuleRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [max_priority][crate::model::network_firewall_policies::AddPacketMirroringRuleRequest::max_priority].
        pub fn set_max_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.max_priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_priority][crate::model::network_firewall_policies::AddPacketMirroringRuleRequest::max_priority].
        pub fn set_or_clear_max_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.max_priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [min_priority][crate::model::network_firewall_policies::AddPacketMirroringRuleRequest::min_priority].
        pub fn set_min_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.min_priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [min_priority][crate::model::network_firewall_policies::AddPacketMirroringRuleRequest::min_priority].
        pub fn set_or_clear_min_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.min_priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::network_firewall_policies::AddPacketMirroringRuleRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::network_firewall_policies::AddPacketMirroringRuleRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::network_firewall_policies::AddPacketMirroringRuleRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::network_firewall_policies::AddPacketMirroringRuleRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicyRule>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::network_firewall_policies::AddPacketMirroringRuleRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicyRule>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AddPacketMirroringRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkFirewallPolicies::add_rule][crate::client::NetworkFirewallPolicies::add_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_firewall_policies::AddRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AddRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AddRule(RequestBuilder<crate::model::network_firewall_policies::AddRuleRequest>);

    impl AddRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_firewall_policies::AddRuleRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .add_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `add_rule`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::NetworkFirewallPolicies::add_rule::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::network_firewall_policies::AddRuleRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [max_priority][crate::model::network_firewall_policies::AddRuleRequest::max_priority].
        pub fn set_max_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.max_priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_priority][crate::model::network_firewall_policies::AddRuleRequest::max_priority].
        pub fn set_or_clear_max_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.max_priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [min_priority][crate::model::network_firewall_policies::AddRuleRequest::min_priority].
        pub fn set_min_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.min_priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [min_priority][crate::model::network_firewall_policies::AddRuleRequest::min_priority].
        pub fn set_or_clear_min_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.min_priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::network_firewall_policies::AddRuleRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::network_firewall_policies::AddRuleRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::network_firewall_policies::AddRuleRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::network_firewall_policies::AddRuleRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicyRule>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::network_firewall_policies::AddRuleRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicyRule>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AddRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkFirewallPolicies::aggregated_list][crate::client::NetworkFirewallPolicies::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_firewall_policies::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::network_firewall_policies::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::network_firewall_policies::AggregatedListRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NetworkFirewallPolicyAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::NetworkFirewallPolicyAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::NetworkFirewallPolicyAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::network_firewall_policies::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::network_firewall_policies::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::network_firewall_policies::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::network_firewall_policies::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::network_firewall_policies::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::network_firewall_policies::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::network_firewall_policies::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::network_firewall_policies::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::network_firewall_policies::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::network_firewall_policies::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::network_firewall_policies::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::network_firewall_policies::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::network_firewall_policies::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::network_firewall_policies::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::network_firewall_policies::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkFirewallPolicies::clone_rules][crate::client::NetworkFirewallPolicies::clone_rules] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_firewall_policies::CloneRules;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CloneRules {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CloneRules(
        RequestBuilder<crate::model::network_firewall_policies::CloneRulesRequest>,
    );

    impl CloneRules {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_firewall_policies::CloneRulesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .clone_rules(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `clone_rules`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::NetworkFirewallPolicies::clone_rules::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::network_firewall_policies::CloneRulesRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [project][crate::model::network_firewall_policies::CloneRulesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::network_firewall_policies::CloneRulesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::network_firewall_policies::CloneRulesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [source_firewall_policy][crate::model::network_firewall_policies::CloneRulesRequest::source_firewall_policy].
        pub fn set_source_firewall_policy<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.source_firewall_policy = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [source_firewall_policy][crate::model::network_firewall_policies::CloneRulesRequest::source_firewall_policy].
        pub fn set_or_clear_source_firewall_policy<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.source_firewall_policy = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CloneRules {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkFirewallPolicies::delete][crate::client::NetworkFirewallPolicies::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_firewall_policies::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::network_firewall_policies::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_firewall_policies::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::NetworkFirewallPolicies::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::network_firewall_policies::DeleteRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [project][crate::model::network_firewall_policies::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::network_firewall_policies::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::network_firewall_policies::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkFirewallPolicies::get][crate::client::NetworkFirewallPolicies::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_firewall_policies::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::network_firewall_policies::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_firewall_policies::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::FirewallPolicy> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [firewall_policy][crate::model::network_firewall_policies::GetRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [project][crate::model::network_firewall_policies::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkFirewallPolicies::get_association][crate::client::NetworkFirewallPolicies::get_association] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_firewall_policies::GetAssociation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetAssociation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetAssociation(
        RequestBuilder<crate::model::network_firewall_policies::GetAssociationRequest>,
    );

    impl GetAssociation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::network_firewall_policies::GetAssociationRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::FirewallPolicyAssociation> {
            (*self.0.stub)
                .get_association(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [firewall_policy][crate::model::network_firewall_policies::GetAssociationRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [name][crate::model::network_firewall_policies::GetAssociationRequest::name].
        pub fn set_name<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.name = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [name][crate::model::network_firewall_policies::GetAssociationRequest::name].
        pub fn set_or_clear_name<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.name = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::network_firewall_policies::GetAssociationRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetAssociation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkFirewallPolicies::get_iam_policy][crate::client::NetworkFirewallPolicies::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_firewall_policies::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(
        RequestBuilder<crate::model::network_firewall_policies::GetIamPolicyRequest>,
    );

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::network_firewall_policies::GetIamPolicyRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::network_firewall_policies::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::network_firewall_policies::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::network_firewall_policies::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::network_firewall_policies::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkFirewallPolicies::get_packet_mirroring_rule][crate::client::NetworkFirewallPolicies::get_packet_mirroring_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_firewall_policies::GetPacketMirroringRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetPacketMirroringRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetPacketMirroringRule(
        RequestBuilder<crate::model::network_firewall_policies::GetPacketMirroringRuleRequest>,
    );

    impl GetPacketMirroringRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::network_firewall_policies::GetPacketMirroringRuleRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::FirewallPolicyRule> {
            (*self.0.stub)
                .get_packet_mirroring_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [firewall_policy][crate::model::network_firewall_policies::GetPacketMirroringRuleRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [priority][crate::model::network_firewall_policies::GetPacketMirroringRuleRequest::priority].
        pub fn set_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [priority][crate::model::network_firewall_policies::GetPacketMirroringRuleRequest::priority].
        pub fn set_or_clear_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::network_firewall_policies::GetPacketMirroringRuleRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetPacketMirroringRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkFirewallPolicies::get_rule][crate::client::NetworkFirewallPolicies::get_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_firewall_policies::GetRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetRule(RequestBuilder<crate::model::network_firewall_policies::GetRuleRequest>);

    impl GetRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_firewall_policies::GetRuleRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::FirewallPolicyRule> {
            (*self.0.stub)
                .get_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [firewall_policy][crate::model::network_firewall_policies::GetRuleRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [priority][crate::model::network_firewall_policies::GetRuleRequest::priority].
        pub fn set_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [priority][crate::model::network_firewall_policies::GetRuleRequest::priority].
        pub fn set_or_clear_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::network_firewall_policies::GetRuleRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkFirewallPolicies::insert][crate::client::NetworkFirewallPolicies::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_firewall_policies::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::network_firewall_policies::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_firewall_policies::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::NetworkFirewallPolicies::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::network_firewall_policies::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::network_firewall_policies::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::network_firewall_policies::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::network_firewall_policies::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::network_firewall_policies::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkFirewallPolicies::list][crate::client::NetworkFirewallPolicies::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_firewall_policies::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::network_firewall_policies::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_firewall_policies::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::FirewallPolicyList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::FirewallPolicyList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::FirewallPolicyList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::network_firewall_policies::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::network_firewall_policies::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::network_firewall_policies::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::network_firewall_policies::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::network_firewall_policies::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::network_firewall_policies::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::network_firewall_policies::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::network_firewall_policies::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::network_firewall_policies::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::network_firewall_policies::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::network_firewall_policies::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkFirewallPolicies::patch][crate::client::NetworkFirewallPolicies::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_firewall_policies::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::network_firewall_policies::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_firewall_policies::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::NetworkFirewallPolicies::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::network_firewall_policies::PatchRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [project][crate::model::network_firewall_policies::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::network_firewall_policies::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::network_firewall_policies::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::network_firewall_policies::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::network_firewall_policies::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkFirewallPolicies::patch_packet_mirroring_rule][crate::client::NetworkFirewallPolicies::patch_packet_mirroring_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_firewall_policies::PatchPacketMirroringRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> PatchPacketMirroringRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct PatchPacketMirroringRule(
        RequestBuilder<crate::model::network_firewall_policies::PatchPacketMirroringRuleRequest>,
    );

    impl PatchPacketMirroringRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::network_firewall_policies::PatchPacketMirroringRuleRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch_packet_mirroring_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch_packet_mirroring_rule`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::NetworkFirewallPolicies::patch_packet_mirroring_rule::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::network_firewall_policies::PatchPacketMirroringRuleRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [priority][crate::model::network_firewall_policies::PatchPacketMirroringRuleRequest::priority].
        pub fn set_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [priority][crate::model::network_firewall_policies::PatchPacketMirroringRuleRequest::priority].
        pub fn set_or_clear_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::network_firewall_policies::PatchPacketMirroringRuleRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::network_firewall_policies::PatchPacketMirroringRuleRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::network_firewall_policies::PatchPacketMirroringRuleRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::network_firewall_policies::PatchPacketMirroringRuleRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicyRule>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::network_firewall_policies::PatchPacketMirroringRuleRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicyRule>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for PatchPacketMirroringRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkFirewallPolicies::patch_rule][crate::client::NetworkFirewallPolicies::patch_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_firewall_policies::PatchRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> PatchRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct PatchRule(RequestBuilder<crate::model::network_firewall_policies::PatchRuleRequest>);

    impl PatchRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_firewall_policies::PatchRuleRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch_rule`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::NetworkFirewallPolicies::patch_rule::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::network_firewall_policies::PatchRuleRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [priority][crate::model::network_firewall_policies::PatchRuleRequest::priority].
        pub fn set_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [priority][crate::model::network_firewall_policies::PatchRuleRequest::priority].
        pub fn set_or_clear_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::network_firewall_policies::PatchRuleRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::network_firewall_policies::PatchRuleRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::network_firewall_policies::PatchRuleRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::network_firewall_policies::PatchRuleRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicyRule>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::network_firewall_policies::PatchRuleRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicyRule>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for PatchRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkFirewallPolicies::remove_association][crate::client::NetworkFirewallPolicies::remove_association] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_firewall_policies::RemoveAssociation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> RemoveAssociation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct RemoveAssociation(
        RequestBuilder<crate::model::network_firewall_policies::RemoveAssociationRequest>,
    );

    impl RemoveAssociation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::network_firewall_policies::RemoveAssociationRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .remove_association(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `remove_association`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::NetworkFirewallPolicies::remove_association::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::network_firewall_policies::RemoveAssociationRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [name][crate::model::network_firewall_policies::RemoveAssociationRequest::name].
        pub fn set_name<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.name = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [name][crate::model::network_firewall_policies::RemoveAssociationRequest::name].
        pub fn set_or_clear_name<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.name = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::network_firewall_policies::RemoveAssociationRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::network_firewall_policies::RemoveAssociationRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::network_firewall_policies::RemoveAssociationRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for RemoveAssociation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkFirewallPolicies::remove_packet_mirroring_rule][crate::client::NetworkFirewallPolicies::remove_packet_mirroring_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_firewall_policies::RemovePacketMirroringRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> RemovePacketMirroringRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct RemovePacketMirroringRule(
        RequestBuilder<crate::model::network_firewall_policies::RemovePacketMirroringRuleRequest>,
    );

    impl RemovePacketMirroringRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::network_firewall_policies::RemovePacketMirroringRuleRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .remove_packet_mirroring_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `remove_packet_mirroring_rule`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::NetworkFirewallPolicies::remove_packet_mirroring_rule::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::network_firewall_policies::RemovePacketMirroringRuleRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [priority][crate::model::network_firewall_policies::RemovePacketMirroringRuleRequest::priority].
        pub fn set_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [priority][crate::model::network_firewall_policies::RemovePacketMirroringRuleRequest::priority].
        pub fn set_or_clear_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::network_firewall_policies::RemovePacketMirroringRuleRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::network_firewall_policies::RemovePacketMirroringRuleRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::network_firewall_policies::RemovePacketMirroringRuleRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for RemovePacketMirroringRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkFirewallPolicies::remove_rule][crate::client::NetworkFirewallPolicies::remove_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_firewall_policies::RemoveRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> RemoveRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct RemoveRule(
        RequestBuilder<crate::model::network_firewall_policies::RemoveRuleRequest>,
    );

    impl RemoveRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_firewall_policies::RemoveRuleRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .remove_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `remove_rule`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::NetworkFirewallPolicies::remove_rule::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::network_firewall_policies::RemoveRuleRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [priority][crate::model::network_firewall_policies::RemoveRuleRequest::priority].
        pub fn set_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [priority][crate::model::network_firewall_policies::RemoveRuleRequest::priority].
        pub fn set_or_clear_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::network_firewall_policies::RemoveRuleRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::network_firewall_policies::RemoveRuleRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::network_firewall_policies::RemoveRuleRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for RemoveRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkFirewallPolicies::set_iam_policy][crate::client::NetworkFirewallPolicies::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_firewall_policies::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(
        RequestBuilder<crate::model::network_firewall_policies::SetIamPolicyRequest>,
    );

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::network_firewall_policies::SetIamPolicyRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::network_firewall_policies::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::network_firewall_policies::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::network_firewall_policies::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::network_firewall_policies::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkFirewallPolicies::test_iam_permissions][crate::client::NetworkFirewallPolicies::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_firewall_policies::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::network_firewall_policies::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::network_firewall_policies::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::network_firewall_policies::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::network_firewall_policies::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::network_firewall_policies::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::network_firewall_policies::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkFirewallPolicies::get_operation][crate::client::NetworkFirewallPolicies::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_firewall_policies::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [NetworkProfiles][crate::client::NetworkProfiles].
#[cfg(feature = "network-profiles")]
#[cfg_attr(docsrs, doc(cfg(feature = "network-profiles")))]
pub mod network_profiles {
    use crate::Result;

    /// A builder for [NetworkProfiles][crate::client::NetworkProfiles].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::network_profiles::ClientBuilder;
    /// # use client::NetworkProfiles;
    /// let builder : ClientBuilder = NetworkProfiles::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::NetworkProfiles;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = NetworkProfiles;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::NetworkProfiles] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkProfiles>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkProfiles>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [NetworkProfiles::get][crate::client::NetworkProfiles::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_profiles::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::network_profiles::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkProfiles>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_profiles::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NetworkProfile> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [network_profile][crate::model::network_profiles::GetRequest::network_profile].
        pub fn set_network_profile<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network_profile = v.into();
            self
        }

        /// Sets the value of [project][crate::model::network_profiles::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NetworkProfiles::list][crate::client::NetworkProfiles::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::network_profiles::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::network_profiles::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NetworkProfiles>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::network_profiles::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NetworkProfilesListResponse> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::NetworkProfilesListResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::NetworkProfilesListResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::network_profiles::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::network_profiles::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::network_profiles::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::network_profiles::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::network_profiles::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::network_profiles::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::network_profiles::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::network_profiles::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::network_profiles::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::network_profiles::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::network_profiles::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [Networks][crate::client::Networks].
#[cfg(feature = "networks")]
#[cfg_attr(docsrs, doc(cfg(feature = "networks")))]
pub mod networks {
    use crate::Result;

    /// A builder for [Networks][crate::client::Networks].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::networks::ClientBuilder;
    /// # use client::Networks;
    /// let builder : ClientBuilder = Networks::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::Networks;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = Networks;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::Networks] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::Networks>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Networks>) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [Networks::add_peering][crate::client::Networks::add_peering] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::networks::AddPeering;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AddPeering {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AddPeering(RequestBuilder<crate::model::networks::AddPeeringRequest>);

    impl AddPeering {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Networks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::networks::AddPeeringRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .add_peering(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `add_peering`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Networks::add_peering::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [network][crate::model::networks::AddPeeringRequest::network].
        pub fn set_network<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network = v.into();
            self
        }

        /// Sets the value of [project][crate::model::networks::AddPeeringRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::networks::AddPeeringRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::networks::AddPeeringRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::networks::AddPeeringRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NetworksAddPeeringRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::networks::AddPeeringRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NetworksAddPeeringRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AddPeering {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Networks::cancel_request_remove_peering][crate::client::Networks::cancel_request_remove_peering] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::networks::CancelRequestRemovePeering;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CancelRequestRemovePeering {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CancelRequestRemovePeering(
        RequestBuilder<crate::model::networks::CancelRequestRemovePeeringRequest>,
    );

    impl CancelRequestRemovePeering {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Networks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::networks::CancelRequestRemovePeeringRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .cancel_request_remove_peering(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `cancel_request_remove_peering`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Networks::cancel_request_remove_peering::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [network][crate::model::networks::CancelRequestRemovePeeringRequest::network].
        pub fn set_network<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network = v.into();
            self
        }

        /// Sets the value of [project][crate::model::networks::CancelRequestRemovePeeringRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::networks::CancelRequestRemovePeeringRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::networks::CancelRequestRemovePeeringRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::networks::CancelRequestRemovePeeringRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NetworksCancelRequestRemovePeeringRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::networks::CancelRequestRemovePeeringRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NetworksCancelRequestRemovePeeringRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CancelRequestRemovePeering {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Networks::delete][crate::client::Networks::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::networks::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::networks::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Networks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::networks::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Networks::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [network][crate::model::networks::DeleteRequest::network].
        pub fn set_network<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network = v.into();
            self
        }

        /// Sets the value of [project][crate::model::networks::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::networks::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::networks::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Networks::get][crate::client::Networks::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::networks::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::networks::GetRequest>);

    impl Get {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Networks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::networks::GetRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Network> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [network][crate::model::networks::GetRequest::network].
        pub fn set_network<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network = v.into();
            self
        }

        /// Sets the value of [project][crate::model::networks::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Networks::get_effective_firewalls][crate::client::Networks::get_effective_firewalls] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::networks::GetEffectiveFirewalls;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetEffectiveFirewalls {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetEffectiveFirewalls(
        RequestBuilder<crate::model::networks::GetEffectiveFirewallsRequest>,
    );

    impl GetEffectiveFirewalls {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Networks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::networks::GetEffectiveFirewallsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NetworksGetEffectiveFirewallsResponse> {
            (*self.0.stub)
                .get_effective_firewalls(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [network][crate::model::networks::GetEffectiveFirewallsRequest::network].
        pub fn set_network<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network = v.into();
            self
        }

        /// Sets the value of [project][crate::model::networks::GetEffectiveFirewallsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetEffectiveFirewalls {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Networks::insert][crate::client::Networks::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::networks::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::networks::InsertRequest>);

    impl Insert {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Networks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::networks::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Networks::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::networks::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::networks::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::networks::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::networks::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Network>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::networks::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Network>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Networks::list][crate::client::Networks::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::networks::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::networks::ListRequest>);

    impl List {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Networks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::networks::ListRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NetworkList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::NetworkList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::NetworkList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::networks::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::networks::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::networks::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::networks::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::networks::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::networks::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::networks::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::networks::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::networks::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::networks::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::networks::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Networks::list_peering_routes][crate::client::Networks::list_peering_routes] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::networks::ListPeeringRoutes;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListPeeringRoutes {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListPeeringRoutes(RequestBuilder<crate::model::networks::ListPeeringRoutesRequest>);

    impl ListPeeringRoutes {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Networks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::networks::ListPeeringRoutesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ExchangedPeeringRoutesList> {
            (*self.0.stub)
                .list_peering_routes(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::ExchangedPeeringRoutesList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ExchangedPeeringRoutesList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [direction][crate::model::networks::ListPeeringRoutesRequest::direction].
        pub fn set_direction<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::networks::list_peering_routes_request::Direction>,
        {
            self.0.request.direction = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [direction][crate::model::networks::ListPeeringRoutesRequest::direction].
        pub fn set_or_clear_direction<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::networks::list_peering_routes_request::Direction>,
        {
            self.0.request.direction = v.map(|x| x.into());
            self
        }

        /// Sets the value of [filter][crate::model::networks::ListPeeringRoutesRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::networks::ListPeeringRoutesRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::networks::ListPeeringRoutesRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::networks::ListPeeringRoutesRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [network][crate::model::networks::ListPeeringRoutesRequest::network].
        pub fn set_network<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network = v.into();
            self
        }

        /// Sets the value of [order_by][crate::model::networks::ListPeeringRoutesRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::networks::ListPeeringRoutesRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::networks::ListPeeringRoutesRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::networks::ListPeeringRoutesRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [peering_name][crate::model::networks::ListPeeringRoutesRequest::peering_name].
        pub fn set_peering_name<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.peering_name = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [peering_name][crate::model::networks::ListPeeringRoutesRequest::peering_name].
        pub fn set_or_clear_peering_name<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.peering_name = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::networks::ListPeeringRoutesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::networks::ListPeeringRoutesRequest::region].
        pub fn set_region<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.region = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [region][crate::model::networks::ListPeeringRoutesRequest::region].
        pub fn set_or_clear_region<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.region = v.map(|x| x.into());
            self
        }

        /// Sets the value of [return_partial_success][crate::model::networks::ListPeeringRoutesRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::networks::ListPeeringRoutesRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListPeeringRoutes {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Networks::patch][crate::client::Networks::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::networks::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::networks::PatchRequest>);

    impl Patch {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Networks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::networks::PatchRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Networks::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [network][crate::model::networks::PatchRequest::network].
        pub fn set_network<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network = v.into();
            self
        }

        /// Sets the value of [project][crate::model::networks::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::networks::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::networks::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::networks::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Network>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::networks::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Network>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Networks::remove_peering][crate::client::Networks::remove_peering] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::networks::RemovePeering;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> RemovePeering {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct RemovePeering(RequestBuilder<crate::model::networks::RemovePeeringRequest>);

    impl RemovePeering {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Networks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::networks::RemovePeeringRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .remove_peering(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `remove_peering`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Networks::remove_peering::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [network][crate::model::networks::RemovePeeringRequest::network].
        pub fn set_network<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network = v.into();
            self
        }

        /// Sets the value of [project][crate::model::networks::RemovePeeringRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::networks::RemovePeeringRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::networks::RemovePeeringRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::networks::RemovePeeringRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NetworksRemovePeeringRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::networks::RemovePeeringRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NetworksRemovePeeringRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for RemovePeering {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Networks::request_remove_peering][crate::client::Networks::request_remove_peering] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::networks::RequestRemovePeering;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> RequestRemovePeering {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct RequestRemovePeering(
        RequestBuilder<crate::model::networks::RequestRemovePeeringRequest>,
    );

    impl RequestRemovePeering {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Networks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::networks::RequestRemovePeeringRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .request_remove_peering(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `request_remove_peering`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Networks::request_remove_peering::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [network][crate::model::networks::RequestRemovePeeringRequest::network].
        pub fn set_network<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network = v.into();
            self
        }

        /// Sets the value of [project][crate::model::networks::RequestRemovePeeringRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::networks::RequestRemovePeeringRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::networks::RequestRemovePeeringRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::networks::RequestRemovePeeringRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NetworksRequestRemovePeeringRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::networks::RequestRemovePeeringRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NetworksRequestRemovePeeringRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for RequestRemovePeering {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Networks::switch_to_custom_mode][crate::client::Networks::switch_to_custom_mode] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::networks::SwitchToCustomMode;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SwitchToCustomMode {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SwitchToCustomMode(
        RequestBuilder<crate::model::networks::SwitchToCustomModeRequest>,
    );

    impl SwitchToCustomMode {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Networks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::networks::SwitchToCustomModeRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .switch_to_custom_mode(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `switch_to_custom_mode`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Networks::switch_to_custom_mode::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [network][crate::model::networks::SwitchToCustomModeRequest::network].
        pub fn set_network<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network = v.into();
            self
        }

        /// Sets the value of [project][crate::model::networks::SwitchToCustomModeRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::networks::SwitchToCustomModeRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::networks::SwitchToCustomModeRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SwitchToCustomMode {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Networks::update_peering][crate::client::Networks::update_peering] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::networks::UpdatePeering;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdatePeering {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdatePeering(RequestBuilder<crate::model::networks::UpdatePeeringRequest>);

    impl UpdatePeering {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Networks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::networks::UpdatePeeringRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update_peering(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update_peering`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Networks::update_peering::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [network][crate::model::networks::UpdatePeeringRequest::network].
        pub fn set_network<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network = v.into();
            self
        }

        /// Sets the value of [project][crate::model::networks::UpdatePeeringRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::networks::UpdatePeeringRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::networks::UpdatePeeringRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::networks::UpdatePeeringRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NetworksUpdatePeeringRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::networks::UpdatePeeringRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NetworksUpdatePeeringRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdatePeering {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Networks::get_operation][crate::client::Networks::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::networks::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Networks>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [NodeGroups][crate::client::NodeGroups].
#[cfg(feature = "node-groups")]
#[cfg_attr(docsrs, doc(cfg(feature = "node-groups")))]
pub mod node_groups {
    use crate::Result;

    /// A builder for [NodeGroups][crate::client::NodeGroups].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::node_groups::ClientBuilder;
    /// # use client::NodeGroups;
    /// let builder : ClientBuilder = NodeGroups::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::NodeGroups;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = NodeGroups;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::NodeGroups] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeGroups>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeGroups>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [NodeGroups::add_nodes][crate::client::NodeGroups::add_nodes] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_groups::AddNodes;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AddNodes {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AddNodes(RequestBuilder<crate::model::node_groups::AddNodesRequest>);

    impl AddNodes {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_groups::AddNodesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .add_nodes(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `add_nodes`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::NodeGroups::add_nodes::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [node_group][crate::model::node_groups::AddNodesRequest::node_group].
        pub fn set_node_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.node_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::node_groups::AddNodesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::node_groups::AddNodesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::node_groups::AddNodesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::node_groups::AddNodesRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::node_groups::AddNodesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NodeGroupsAddNodesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::node_groups::AddNodesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NodeGroupsAddNodesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AddNodes {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NodeGroups::aggregated_list][crate::client::NodeGroups::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_groups::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(RequestBuilder<crate::model::node_groups::AggregatedListRequest>);

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_groups::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NodeGroupAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::NodeGroupAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::NodeGroupAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::node_groups::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::node_groups::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::node_groups::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::node_groups::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::node_groups::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::node_groups::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::node_groups::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::node_groups::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::node_groups::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::node_groups::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::node_groups::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::node_groups::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::node_groups::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::node_groups::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::node_groups::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NodeGroups::delete][crate::client::NodeGroups::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_groups::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::node_groups::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_groups::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::NodeGroups::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [node_group][crate::model::node_groups::DeleteRequest::node_group].
        pub fn set_node_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.node_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::node_groups::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::node_groups::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::node_groups::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::node_groups::DeleteRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NodeGroups::delete_nodes][crate::client::NodeGroups::delete_nodes] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_groups::DeleteNodes;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeleteNodes {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeleteNodes(RequestBuilder<crate::model::node_groups::DeleteNodesRequest>);

    impl DeleteNodes {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_groups::DeleteNodesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete_nodes(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete_nodes`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::NodeGroups::delete_nodes::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [node_group][crate::model::node_groups::DeleteNodesRequest::node_group].
        pub fn set_node_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.node_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::node_groups::DeleteNodesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::node_groups::DeleteNodesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::node_groups::DeleteNodesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::node_groups::DeleteNodesRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::node_groups::DeleteNodesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NodeGroupsDeleteNodesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::node_groups::DeleteNodesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NodeGroupsDeleteNodesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeleteNodes {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NodeGroups::get][crate::client::NodeGroups::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_groups::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::node_groups::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_groups::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NodeGroup> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [node_group][crate::model::node_groups::GetRequest::node_group].
        pub fn set_node_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.node_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::node_groups::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::node_groups::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NodeGroups::get_iam_policy][crate::client::NodeGroups::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_groups::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(RequestBuilder<crate::model::node_groups::GetIamPolicyRequest>);

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_groups::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::node_groups::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::node_groups::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::node_groups::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::node_groups::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::node_groups::GetIamPolicyRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NodeGroups::insert][crate::client::NodeGroups::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_groups::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::node_groups::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_groups::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::NodeGroups::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [initial_node_count][crate::model::node_groups::InsertRequest::initial_node_count].
        pub fn set_initial_node_count<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.initial_node_count = v.into();
            self
        }

        /// Sets the value of [project][crate::model::node_groups::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::node_groups::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::node_groups::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::node_groups::InsertRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::node_groups::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NodeGroup>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::node_groups::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NodeGroup>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NodeGroups::list][crate::client::NodeGroups::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_groups::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::node_groups::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_groups::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NodeGroupList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::NodeGroupList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::NodeGroupList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::node_groups::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::node_groups::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::node_groups::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::node_groups::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::node_groups::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::node_groups::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::node_groups::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::node_groups::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::node_groups::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::node_groups::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::node_groups::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::node_groups::ListRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NodeGroups::list_nodes][crate::client::NodeGroups::list_nodes] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_groups::ListNodes;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListNodes {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListNodes(RequestBuilder<crate::model::node_groups::ListNodesRequest>);

    impl ListNodes {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_groups::ListNodesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NodeGroupsListNodes> {
            (*self.0.stub)
                .list_nodes(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::NodeGroupsListNodes, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::NodeGroupsListNodes,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::node_groups::ListNodesRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::node_groups::ListNodesRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::node_groups::ListNodesRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::node_groups::ListNodesRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [node_group][crate::model::node_groups::ListNodesRequest::node_group].
        pub fn set_node_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.node_group = v.into();
            self
        }

        /// Sets the value of [order_by][crate::model::node_groups::ListNodesRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::node_groups::ListNodesRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::node_groups::ListNodesRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::node_groups::ListNodesRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::node_groups::ListNodesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::node_groups::ListNodesRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::node_groups::ListNodesRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::node_groups::ListNodesRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListNodes {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NodeGroups::patch][crate::client::NodeGroups::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_groups::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::node_groups::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_groups::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::NodeGroups::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [node_group][crate::model::node_groups::PatchRequest::node_group].
        pub fn set_node_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.node_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::node_groups::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::node_groups::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::node_groups::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::node_groups::PatchRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::node_groups::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NodeGroup>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::node_groups::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NodeGroup>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NodeGroups::perform_maintenance][crate::client::NodeGroups::perform_maintenance] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_groups::PerformMaintenance;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> PerformMaintenance {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct PerformMaintenance(
        RequestBuilder<crate::model::node_groups::PerformMaintenanceRequest>,
    );

    impl PerformMaintenance {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_groups::PerformMaintenanceRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .perform_maintenance(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `perform_maintenance`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::NodeGroups::perform_maintenance::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [node_group][crate::model::node_groups::PerformMaintenanceRequest::node_group].
        pub fn set_node_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.node_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::node_groups::PerformMaintenanceRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::node_groups::PerformMaintenanceRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::node_groups::PerformMaintenanceRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::node_groups::PerformMaintenanceRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::node_groups::PerformMaintenanceRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NodeGroupsPerformMaintenanceRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::node_groups::PerformMaintenanceRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NodeGroupsPerformMaintenanceRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for PerformMaintenance {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NodeGroups::set_iam_policy][crate::client::NodeGroups::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_groups::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(RequestBuilder<crate::model::node_groups::SetIamPolicyRequest>);

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_groups::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::node_groups::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::node_groups::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::node_groups::SetIamPolicyRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::node_groups::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ZoneSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::node_groups::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ZoneSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NodeGroups::set_node_template][crate::client::NodeGroups::set_node_template] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_groups::SetNodeTemplate;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetNodeTemplate {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetNodeTemplate(RequestBuilder<crate::model::node_groups::SetNodeTemplateRequest>);

    impl SetNodeTemplate {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_groups::SetNodeTemplateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_node_template(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_node_template`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::NodeGroups::set_node_template::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [node_group][crate::model::node_groups::SetNodeTemplateRequest::node_group].
        pub fn set_node_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.node_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::node_groups::SetNodeTemplateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::node_groups::SetNodeTemplateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::node_groups::SetNodeTemplateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::node_groups::SetNodeTemplateRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::node_groups::SetNodeTemplateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NodeGroupsSetNodeTemplateRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::node_groups::SetNodeTemplateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NodeGroupsSetNodeTemplateRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetNodeTemplate {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NodeGroups::simulate_maintenance_event][crate::client::NodeGroups::simulate_maintenance_event] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_groups::SimulateMaintenanceEvent;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SimulateMaintenanceEvent {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SimulateMaintenanceEvent(
        RequestBuilder<crate::model::node_groups::SimulateMaintenanceEventRequest>,
    );

    impl SimulateMaintenanceEvent {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_groups::SimulateMaintenanceEventRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .simulate_maintenance_event(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `simulate_maintenance_event`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::NodeGroups::simulate_maintenance_event::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [node_group][crate::model::node_groups::SimulateMaintenanceEventRequest::node_group].
        pub fn set_node_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.node_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::node_groups::SimulateMaintenanceEventRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::node_groups::SimulateMaintenanceEventRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::node_groups::SimulateMaintenanceEventRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::node_groups::SimulateMaintenanceEventRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::node_groups::SimulateMaintenanceEventRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NodeGroupsSimulateMaintenanceEventRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::node_groups::SimulateMaintenanceEventRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NodeGroupsSimulateMaintenanceEventRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SimulateMaintenanceEvent {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NodeGroups::test_iam_permissions][crate::client::NodeGroups::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_groups::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::node_groups::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_groups::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::node_groups::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::node_groups::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::node_groups::TestIamPermissionsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::node_groups::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::node_groups::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NodeGroups::get_operation][crate::client::NodeGroups::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_groups::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::zone_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::zone_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::zone_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zone_operations::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [NodeTemplates][crate::client::NodeTemplates].
#[cfg(feature = "node-templates")]
#[cfg_attr(docsrs, doc(cfg(feature = "node-templates")))]
pub mod node_templates {
    use crate::Result;

    /// A builder for [NodeTemplates][crate::client::NodeTemplates].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::node_templates::ClientBuilder;
    /// # use client::NodeTemplates;
    /// let builder : ClientBuilder = NodeTemplates::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::NodeTemplates;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = NodeTemplates;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::NodeTemplates] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeTemplates>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeTemplates>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [NodeTemplates::aggregated_list][crate::client::NodeTemplates::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_templates::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(RequestBuilder<crate::model::node_templates::AggregatedListRequest>);

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeTemplates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_templates::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NodeTemplateAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::NodeTemplateAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::NodeTemplateAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::node_templates::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::node_templates::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::node_templates::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::node_templates::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::node_templates::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::node_templates::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::node_templates::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::node_templates::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::node_templates::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::node_templates::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::node_templates::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::node_templates::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::node_templates::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::node_templates::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::node_templates::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NodeTemplates::delete][crate::client::NodeTemplates::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_templates::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::node_templates::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeTemplates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_templates::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::NodeTemplates::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [node_template][crate::model::node_templates::DeleteRequest::node_template].
        pub fn set_node_template<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.node_template = v.into();
            self
        }

        /// Sets the value of [project][crate::model::node_templates::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::node_templates::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::node_templates::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::node_templates::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NodeTemplates::get][crate::client::NodeTemplates::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_templates::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::node_templates::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeTemplates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_templates::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NodeTemplate> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [node_template][crate::model::node_templates::GetRequest::node_template].
        pub fn set_node_template<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.node_template = v.into();
            self
        }

        /// Sets the value of [project][crate::model::node_templates::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::node_templates::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NodeTemplates::get_iam_policy][crate::client::NodeTemplates::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_templates::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(RequestBuilder<crate::model::node_templates::GetIamPolicyRequest>);

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeTemplates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_templates::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::node_templates::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::node_templates::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::node_templates::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::node_templates::GetIamPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::node_templates::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NodeTemplates::insert][crate::client::NodeTemplates::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_templates::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::node_templates::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeTemplates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_templates::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::NodeTemplates::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::node_templates::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::node_templates::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::node_templates::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::node_templates::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::node_templates::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NodeTemplate>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::node_templates::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NodeTemplate>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NodeTemplates::list][crate::client::NodeTemplates::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_templates::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::node_templates::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeTemplates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_templates::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NodeTemplateList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::NodeTemplateList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::NodeTemplateList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::node_templates::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::node_templates::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::node_templates::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::node_templates::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::node_templates::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::node_templates::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::node_templates::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::node_templates::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::node_templates::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::node_templates::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::node_templates::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::node_templates::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NodeTemplates::set_iam_policy][crate::client::NodeTemplates::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_templates::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(RequestBuilder<crate::model::node_templates::SetIamPolicyRequest>);

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeTemplates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_templates::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::node_templates::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::node_templates::SetIamPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::node_templates::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::node_templates::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::node_templates::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NodeTemplates::test_iam_permissions][crate::client::NodeTemplates::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_templates::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::node_templates::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeTemplates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_templates::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::node_templates::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::node_templates::TestIamPermissionsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::node_templates::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::node_templates::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::node_templates::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NodeTemplates::get_operation][crate::client::NodeTemplates::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_templates::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeTemplates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [NodeTypes][crate::client::NodeTypes].
#[cfg(feature = "node-types")]
#[cfg_attr(docsrs, doc(cfg(feature = "node-types")))]
pub mod node_types {
    use crate::Result;

    /// A builder for [NodeTypes][crate::client::NodeTypes].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::node_types::ClientBuilder;
    /// # use client::NodeTypes;
    /// let builder : ClientBuilder = NodeTypes::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::NodeTypes;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = NodeTypes;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::NodeTypes] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeTypes>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeTypes>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [NodeTypes::aggregated_list][crate::client::NodeTypes::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_types::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(RequestBuilder<crate::model::node_types::AggregatedListRequest>);

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeTypes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_types::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NodeTypeAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::NodeTypeAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::NodeTypeAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::node_types::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::node_types::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::node_types::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::node_types::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::node_types::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::node_types::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::node_types::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::node_types::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::node_types::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::node_types::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::node_types::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::node_types::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::node_types::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::node_types::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::node_types::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NodeTypes::get][crate::client::NodeTypes::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_types::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::node_types::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeTypes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_types::GetRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NodeType> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [node_type][crate::model::node_types::GetRequest::node_type].
        pub fn set_node_type<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.node_type = v.into();
            self
        }

        /// Sets the value of [project][crate::model::node_types::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::node_types::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [NodeTypes::list][crate::client::NodeTypes::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::node_types::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::node_types::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::NodeTypes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::node_types::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NodeTypeList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::NodeTypeList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::NodeTypeList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::node_types::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::node_types::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::node_types::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::node_types::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::node_types::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::node_types::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::node_types::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::node_types::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::node_types::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::node_types::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::node_types::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::node_types::ListRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [OrganizationSecurityPolicies][crate::client::OrganizationSecurityPolicies].
#[cfg(feature = "organization-security-policies")]
#[cfg_attr(docsrs, doc(cfg(feature = "organization-security-policies")))]
pub mod organization_security_policies {
    use crate::Result;

    /// A builder for [OrganizationSecurityPolicies][crate::client::OrganizationSecurityPolicies].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::organization_security_policies::ClientBuilder;
    /// # use client::OrganizationSecurityPolicies;
    /// let builder : ClientBuilder = OrganizationSecurityPolicies::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::OrganizationSecurityPolicies;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = OrganizationSecurityPolicies;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::OrganizationSecurityPolicies] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::OrganizationSecurityPolicies>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::OrganizationSecurityPolicies>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [OrganizationSecurityPolicies::add_association][crate::client::OrganizationSecurityPolicies::add_association] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::organization_security_policies::AddAssociation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AddAssociation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AddAssociation(
        RequestBuilder<crate::model::organization_security_policies::AddAssociationRequest>,
    );

    impl AddAssociation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::OrganizationSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::organization_security_policies::AddAssociationRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .add_association(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `add_association`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::OrganizationSecurityPolicies::add_association::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    GetOperation::new(stub)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [replace_existing_association][crate::model::organization_security_policies::AddAssociationRequest::replace_existing_association].
        pub fn set_replace_existing_association<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.replace_existing_association = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [replace_existing_association][crate::model::organization_security_policies::AddAssociationRequest::replace_existing_association].
        pub fn set_or_clear_replace_existing_association<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.replace_existing_association = v.map(|x| x.into());
            self
        }

        /// Sets the value of [request_id][crate::model::organization_security_policies::AddAssociationRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::organization_security_policies::AddAssociationRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [security_policy][crate::model::organization_security_policies::AddAssociationRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::organization_security_policies::AddAssociationRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyAssociation>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::organization_security_policies::AddAssociationRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyAssociation>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AddAssociation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [OrganizationSecurityPolicies::add_rule][crate::client::OrganizationSecurityPolicies::add_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::organization_security_policies::AddRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AddRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AddRule(
        RequestBuilder<crate::model::organization_security_policies::AddRuleRequest>,
    );

    impl AddRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::OrganizationSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::organization_security_policies::AddRuleRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .add_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `add_rule`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::OrganizationSecurityPolicies::add_rule::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    GetOperation::new(stub)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [request_id][crate::model::organization_security_policies::AddRuleRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::organization_security_policies::AddRuleRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [security_policy][crate::model::organization_security_policies::AddRuleRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::organization_security_policies::AddRuleRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyRule>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::organization_security_policies::AddRuleRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyRule>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AddRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [OrganizationSecurityPolicies::copy_rules][crate::client::OrganizationSecurityPolicies::copy_rules] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::organization_security_policies::CopyRules;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CopyRules {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CopyRules(
        RequestBuilder<crate::model::organization_security_policies::CopyRulesRequest>,
    );

    impl CopyRules {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::OrganizationSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::organization_security_policies::CopyRulesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .copy_rules(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `copy_rules`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::OrganizationSecurityPolicies::copy_rules::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    GetOperation::new(stub)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [request_id][crate::model::organization_security_policies::CopyRulesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::organization_security_policies::CopyRulesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [security_policy][crate::model::organization_security_policies::CopyRulesRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }

        /// Sets the value of [source_security_policy][crate::model::organization_security_policies::CopyRulesRequest::source_security_policy].
        pub fn set_source_security_policy<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.source_security_policy = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [source_security_policy][crate::model::organization_security_policies::CopyRulesRequest::source_security_policy].
        pub fn set_or_clear_source_security_policy<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.source_security_policy = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CopyRules {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [OrganizationSecurityPolicies::delete][crate::client::OrganizationSecurityPolicies::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::organization_security_policies::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::organization_security_policies::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::OrganizationSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::organization_security_policies::DeleteRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::OrganizationSecurityPolicies::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    GetOperation::new(stub)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [request_id][crate::model::organization_security_policies::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::organization_security_policies::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [security_policy][crate::model::organization_security_policies::DeleteRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [OrganizationSecurityPolicies::get][crate::client::OrganizationSecurityPolicies::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::organization_security_policies::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::organization_security_policies::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::OrganizationSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::organization_security_policies::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SecurityPolicy> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [security_policy][crate::model::organization_security_policies::GetRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [OrganizationSecurityPolicies::get_association][crate::client::OrganizationSecurityPolicies::get_association] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::organization_security_policies::GetAssociation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetAssociation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetAssociation(
        RequestBuilder<crate::model::organization_security_policies::GetAssociationRequest>,
    );

    impl GetAssociation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::OrganizationSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::organization_security_policies::GetAssociationRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SecurityPolicyAssociation> {
            (*self.0.stub)
                .get_association(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [name][crate::model::organization_security_policies::GetAssociationRequest::name].
        pub fn set_name<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.name = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [name][crate::model::organization_security_policies::GetAssociationRequest::name].
        pub fn set_or_clear_name<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.name = v.map(|x| x.into());
            self
        }

        /// Sets the value of [security_policy][crate::model::organization_security_policies::GetAssociationRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetAssociation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [OrganizationSecurityPolicies::get_rule][crate::client::OrganizationSecurityPolicies::get_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::organization_security_policies::GetRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetRule(
        RequestBuilder<crate::model::organization_security_policies::GetRuleRequest>,
    );

    impl GetRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::OrganizationSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::organization_security_policies::GetRuleRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SecurityPolicyRule> {
            (*self.0.stub)
                .get_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [priority][crate::model::organization_security_policies::GetRuleRequest::priority].
        pub fn set_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [priority][crate::model::organization_security_policies::GetRuleRequest::priority].
        pub fn set_or_clear_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [security_policy][crate::model::organization_security_policies::GetRuleRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [OrganizationSecurityPolicies::insert][crate::client::OrganizationSecurityPolicies::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::organization_security_policies::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::organization_security_policies::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::OrganizationSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::organization_security_policies::InsertRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::OrganizationSecurityPolicies::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    GetOperation::new(stub)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [parent_id][crate::model::organization_security_policies::InsertRequest::parent_id].
        pub fn set_parent_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.parent_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [parent_id][crate::model::organization_security_policies::InsertRequest::parent_id].
        pub fn set_or_clear_parent_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.parent_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [request_id][crate::model::organization_security_policies::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::organization_security_policies::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::organization_security_policies::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::organization_security_policies::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [OrganizationSecurityPolicies::list][crate::client::OrganizationSecurityPolicies::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::organization_security_policies::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::organization_security_policies::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::OrganizationSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::organization_security_policies::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SecurityPolicyList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::SecurityPolicyList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::SecurityPolicyList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::organization_security_policies::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::organization_security_policies::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::organization_security_policies::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::organization_security_policies::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::organization_security_policies::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::organization_security_policies::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::organization_security_policies::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::organization_security_policies::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [parent_id][crate::model::organization_security_policies::ListRequest::parent_id].
        pub fn set_parent_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.parent_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [parent_id][crate::model::organization_security_policies::ListRequest::parent_id].
        pub fn set_or_clear_parent_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.parent_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [return_partial_success][crate::model::organization_security_policies::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::organization_security_policies::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [OrganizationSecurityPolicies::list_associations][crate::client::OrganizationSecurityPolicies::list_associations] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::organization_security_policies::ListAssociations;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListAssociations {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListAssociations(
        RequestBuilder<crate::model::organization_security_policies::ListAssociationsRequest>,
    );

    impl ListAssociations {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::OrganizationSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::organization_security_policies::ListAssociationsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(
            self,
        ) -> Result<crate::model::OrganizationSecurityPoliciesListAssociationsResponse> {
            (*self.0.stub)
                .list_associations(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [target_resource][crate::model::organization_security_policies::ListAssociationsRequest::target_resource].
        pub fn set_target_resource<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.target_resource = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [target_resource][crate::model::organization_security_policies::ListAssociationsRequest::target_resource].
        pub fn set_or_clear_target_resource<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.target_resource = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListAssociations {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [OrganizationSecurityPolicies::list_preconfigured_expression_sets][crate::client::OrganizationSecurityPolicies::list_preconfigured_expression_sets] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::organization_security_policies::ListPreconfiguredExpressionSets;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListPreconfiguredExpressionSets {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListPreconfiguredExpressionSets(
        RequestBuilder<
            crate::model::organization_security_policies::ListPreconfiguredExpressionSetsRequest,
        >,
    );

    impl ListPreconfiguredExpressionSets {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::OrganizationSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::organization_security_policies::ListPreconfiguredExpressionSetsRequest>>(mut self, v: V) -> Self{
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(
            self,
        ) -> Result<crate::model::SecurityPoliciesListPreconfiguredExpressionSetsResponse> {
            (*self.0.stub)
                .list_preconfigured_expression_sets(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [filter][crate::model::organization_security_policies::ListPreconfiguredExpressionSetsRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::organization_security_policies::ListPreconfiguredExpressionSetsRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::organization_security_policies::ListPreconfiguredExpressionSetsRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::organization_security_policies::ListPreconfiguredExpressionSetsRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::organization_security_policies::ListPreconfiguredExpressionSetsRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::organization_security_policies::ListPreconfiguredExpressionSetsRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::organization_security_policies::ListPreconfiguredExpressionSetsRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::organization_security_policies::ListPreconfiguredExpressionSetsRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [parent_id][crate::model::organization_security_policies::ListPreconfiguredExpressionSetsRequest::parent_id].
        pub fn set_parent_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.parent_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [parent_id][crate::model::organization_security_policies::ListPreconfiguredExpressionSetsRequest::parent_id].
        pub fn set_or_clear_parent_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.parent_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [return_partial_success][crate::model::organization_security_policies::ListPreconfiguredExpressionSetsRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::organization_security_policies::ListPreconfiguredExpressionSetsRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListPreconfiguredExpressionSets {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [OrganizationSecurityPolicies::move][crate::client::OrganizationSecurityPolicies::move] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::organization_security_policies::Move;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Move {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Move(RequestBuilder<crate::model::organization_security_policies::MoveRequest>);

    impl Move {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::OrganizationSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::organization_security_policies::MoveRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .r#move(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `r#move`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::OrganizationSecurityPolicies::r#move::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    GetOperation::new(stub)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [parent_id][crate::model::organization_security_policies::MoveRequest::parent_id].
        pub fn set_parent_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.parent_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [parent_id][crate::model::organization_security_policies::MoveRequest::parent_id].
        pub fn set_or_clear_parent_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.parent_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [request_id][crate::model::organization_security_policies::MoveRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::organization_security_policies::MoveRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [security_policy][crate::model::organization_security_policies::MoveRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Move {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [OrganizationSecurityPolicies::patch][crate::client::OrganizationSecurityPolicies::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::organization_security_policies::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::organization_security_policies::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::OrganizationSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::organization_security_policies::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::OrganizationSecurityPolicies::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    GetOperation::new(stub)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [request_id][crate::model::organization_security_policies::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::organization_security_policies::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [security_policy][crate::model::organization_security_policies::PatchRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::organization_security_policies::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::organization_security_policies::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [OrganizationSecurityPolicies::patch_rule][crate::client::OrganizationSecurityPolicies::patch_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::organization_security_policies::PatchRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> PatchRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct PatchRule(
        RequestBuilder<crate::model::organization_security_policies::PatchRuleRequest>,
    );

    impl PatchRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::OrganizationSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::organization_security_policies::PatchRuleRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch_rule`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::OrganizationSecurityPolicies::patch_rule::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    GetOperation::new(stub)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [priority][crate::model::organization_security_policies::PatchRuleRequest::priority].
        pub fn set_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [priority][crate::model::organization_security_policies::PatchRuleRequest::priority].
        pub fn set_or_clear_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [request_id][crate::model::organization_security_policies::PatchRuleRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::organization_security_policies::PatchRuleRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [security_policy][crate::model::organization_security_policies::PatchRuleRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::organization_security_policies::PatchRuleRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyRule>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::organization_security_policies::PatchRuleRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyRule>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for PatchRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [OrganizationSecurityPolicies::remove_association][crate::client::OrganizationSecurityPolicies::remove_association] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::organization_security_policies::RemoveAssociation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> RemoveAssociation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct RemoveAssociation(
        RequestBuilder<crate::model::organization_security_policies::RemoveAssociationRequest>,
    );

    impl RemoveAssociation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::OrganizationSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::organization_security_policies::RemoveAssociationRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .remove_association(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `remove_association`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::OrganizationSecurityPolicies::remove_association::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    GetOperation::new(stub)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [name][crate::model::organization_security_policies::RemoveAssociationRequest::name].
        pub fn set_name<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.name = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [name][crate::model::organization_security_policies::RemoveAssociationRequest::name].
        pub fn set_or_clear_name<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.name = v.map(|x| x.into());
            self
        }

        /// Sets the value of [request_id][crate::model::organization_security_policies::RemoveAssociationRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::organization_security_policies::RemoveAssociationRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [security_policy][crate::model::organization_security_policies::RemoveAssociationRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for RemoveAssociation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [OrganizationSecurityPolicies::remove_rule][crate::client::OrganizationSecurityPolicies::remove_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::organization_security_policies::RemoveRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> RemoveRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct RemoveRule(
        RequestBuilder<crate::model::organization_security_policies::RemoveRuleRequest>,
    );

    impl RemoveRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::OrganizationSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::organization_security_policies::RemoveRuleRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .remove_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `remove_rule`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::OrganizationSecurityPolicies::remove_rule::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                async {
                    GetOperation::new(stub)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [priority][crate::model::organization_security_policies::RemoveRuleRequest::priority].
        pub fn set_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [priority][crate::model::organization_security_policies::RemoveRuleRequest::priority].
        pub fn set_or_clear_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [request_id][crate::model::organization_security_policies::RemoveRuleRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::organization_security_policies::RemoveRuleRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [security_policy][crate::model::organization_security_policies::RemoveRuleRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for RemoveRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [OrganizationSecurityPolicies::get_operation][crate::client::OrganizationSecurityPolicies::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::organization_security_policies::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(
        RequestBuilder<crate::model::global_organization_operations::GetRequest>,
    );

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::OrganizationSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_organization_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_organization_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [parent_id][crate::model::global_organization_operations::GetRequest::parent_id].
        pub fn set_parent_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.parent_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [parent_id][crate::model::global_organization_operations::GetRequest::parent_id].
        pub fn set_or_clear_parent_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.parent_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [PacketMirrorings][crate::client::PacketMirrorings].
#[cfg(feature = "packet-mirrorings")]
#[cfg_attr(docsrs, doc(cfg(feature = "packet-mirrorings")))]
pub mod packet_mirrorings {
    use crate::Result;

    /// A builder for [PacketMirrorings][crate::client::PacketMirrorings].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::packet_mirrorings::ClientBuilder;
    /// # use client::PacketMirrorings;
    /// let builder : ClientBuilder = PacketMirrorings::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::PacketMirrorings;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = PacketMirrorings;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::PacketMirrorings] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::PacketMirrorings>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PacketMirrorings>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [PacketMirrorings::aggregated_list][crate::client::PacketMirrorings::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::packet_mirrorings::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::packet_mirrorings::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PacketMirrorings>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::packet_mirrorings::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::PacketMirroringAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::PacketMirroringAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::PacketMirroringAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::packet_mirrorings::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::packet_mirrorings::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::packet_mirrorings::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::packet_mirrorings::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::packet_mirrorings::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::packet_mirrorings::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::packet_mirrorings::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::packet_mirrorings::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::packet_mirrorings::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::packet_mirrorings::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::packet_mirrorings::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::packet_mirrorings::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::packet_mirrorings::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::packet_mirrorings::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::packet_mirrorings::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [PacketMirrorings::delete][crate::client::PacketMirrorings::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::packet_mirrorings::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::packet_mirrorings::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PacketMirrorings>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::packet_mirrorings::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::PacketMirrorings::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [packet_mirroring][crate::model::packet_mirrorings::DeleteRequest::packet_mirroring].
        pub fn set_packet_mirroring<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.packet_mirroring = v.into();
            self
        }

        /// Sets the value of [project][crate::model::packet_mirrorings::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::packet_mirrorings::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::packet_mirrorings::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::packet_mirrorings::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [PacketMirrorings::get][crate::client::PacketMirrorings::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::packet_mirrorings::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::packet_mirrorings::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PacketMirrorings>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::packet_mirrorings::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::PacketMirroring> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [packet_mirroring][crate::model::packet_mirrorings::GetRequest::packet_mirroring].
        pub fn set_packet_mirroring<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.packet_mirroring = v.into();
            self
        }

        /// Sets the value of [project][crate::model::packet_mirrorings::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::packet_mirrorings::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [PacketMirrorings::insert][crate::client::PacketMirrorings::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::packet_mirrorings::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::packet_mirrorings::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PacketMirrorings>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::packet_mirrorings::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::PacketMirrorings::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::packet_mirrorings::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::packet_mirrorings::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::packet_mirrorings::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::packet_mirrorings::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::packet_mirrorings::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::PacketMirroring>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::packet_mirrorings::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::PacketMirroring>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [PacketMirrorings::list][crate::client::PacketMirrorings::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::packet_mirrorings::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::packet_mirrorings::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PacketMirrorings>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::packet_mirrorings::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::PacketMirroringList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::PacketMirroringList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::PacketMirroringList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::packet_mirrorings::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::packet_mirrorings::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::packet_mirrorings::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::packet_mirrorings::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::packet_mirrorings::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::packet_mirrorings::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::packet_mirrorings::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::packet_mirrorings::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::packet_mirrorings::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::packet_mirrorings::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::packet_mirrorings::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::packet_mirrorings::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [PacketMirrorings::patch][crate::client::PacketMirrorings::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::packet_mirrorings::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::packet_mirrorings::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PacketMirrorings>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::packet_mirrorings::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::PacketMirrorings::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [packet_mirroring][crate::model::packet_mirrorings::PatchRequest::packet_mirroring].
        pub fn set_packet_mirroring<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.packet_mirroring = v.into();
            self
        }

        /// Sets the value of [project][crate::model::packet_mirrorings::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::packet_mirrorings::PatchRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::packet_mirrorings::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::packet_mirrorings::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::packet_mirrorings::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::PacketMirroring>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::packet_mirrorings::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::PacketMirroring>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [PacketMirrorings::test_iam_permissions][crate::client::PacketMirrorings::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::packet_mirrorings::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::packet_mirrorings::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PacketMirrorings>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::packet_mirrorings::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::packet_mirrorings::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::packet_mirrorings::TestIamPermissionsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::packet_mirrorings::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::packet_mirrorings::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::packet_mirrorings::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [PacketMirrorings::get_operation][crate::client::PacketMirrorings::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::packet_mirrorings::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PacketMirrorings>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [PreviewFeatures][crate::client::PreviewFeatures].
#[cfg(feature = "preview-features")]
#[cfg_attr(docsrs, doc(cfg(feature = "preview-features")))]
pub mod preview_features {
    use crate::Result;

    /// A builder for [PreviewFeatures][crate::client::PreviewFeatures].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::preview_features::ClientBuilder;
    /// # use client::PreviewFeatures;
    /// let builder : ClientBuilder = PreviewFeatures::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::PreviewFeatures;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = PreviewFeatures;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::PreviewFeatures] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::PreviewFeatures>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PreviewFeatures>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [PreviewFeatures::get][crate::client::PreviewFeatures::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::preview_features::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::preview_features::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PreviewFeatures>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::preview_features::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::PreviewFeature> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [preview_feature][crate::model::preview_features::GetRequest::preview_feature].
        pub fn set_preview_feature<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.preview_feature = v.into();
            self
        }

        /// Sets the value of [project][crate::model::preview_features::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [PreviewFeatures::list][crate::client::PreviewFeatures::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::preview_features::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::preview_features::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PreviewFeatures>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::preview_features::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::PreviewFeatureList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::PreviewFeatureList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::PreviewFeatureList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::preview_features::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::preview_features::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::preview_features::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::preview_features::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::preview_features::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::preview_features::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::preview_features::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::preview_features::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::preview_features::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::preview_features::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::preview_features::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [PreviewFeatures::update][crate::client::PreviewFeatures::update] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::preview_features::Update;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Update {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Update(RequestBuilder<crate::model::preview_features::UpdateRequest>);

    impl Update {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PreviewFeatures>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::preview_features::UpdateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::PreviewFeatures::update::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [preview_feature][crate::model::preview_features::UpdateRequest::preview_feature].
        pub fn set_preview_feature<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.preview_feature = v.into();
            self
        }

        /// Sets the value of [project][crate::model::preview_features::UpdateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::preview_features::UpdateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::preview_features::UpdateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::preview_features::UpdateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::PreviewFeature>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::preview_features::UpdateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::PreviewFeature>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Update {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [PreviewFeatures::get_operation][crate::client::PreviewFeatures::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::preview_features::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PreviewFeatures>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [Projects][crate::client::Projects].
#[cfg(feature = "projects")]
#[cfg_attr(docsrs, doc(cfg(feature = "projects")))]
pub mod projects {
    use crate::Result;

    /// A builder for [Projects][crate::client::Projects].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::projects::ClientBuilder;
    /// # use client::Projects;
    /// let builder : ClientBuilder = Projects::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::Projects;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = Projects;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::Projects] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::Projects>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Projects>) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [Projects::disable_xpn_host][crate::client::Projects::disable_xpn_host] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::projects::DisableXpnHost;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DisableXpnHost {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DisableXpnHost(RequestBuilder<crate::model::projects::DisableXpnHostRequest>);

    impl DisableXpnHost {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Projects>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::projects::DisableXpnHostRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .disable_xpn_host(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `disable_xpn_host`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Projects::disable_xpn_host::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::projects::DisableXpnHostRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::projects::DisableXpnHostRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::projects::DisableXpnHostRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DisableXpnHost {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Projects::disable_xpn_resource][crate::client::Projects::disable_xpn_resource] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::projects::DisableXpnResource;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DisableXpnResource {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DisableXpnResource(
        RequestBuilder<crate::model::projects::DisableXpnResourceRequest>,
    );

    impl DisableXpnResource {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Projects>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::projects::DisableXpnResourceRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .disable_xpn_resource(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `disable_xpn_resource`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Projects::disable_xpn_resource::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::projects::DisableXpnResourceRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::projects::DisableXpnResourceRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::projects::DisableXpnResourceRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::projects::DisableXpnResourceRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ProjectsDisableXpnResourceRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::projects::DisableXpnResourceRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ProjectsDisableXpnResourceRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DisableXpnResource {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Projects::enable_xpn_host][crate::client::Projects::enable_xpn_host] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::projects::EnableXpnHost;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> EnableXpnHost {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct EnableXpnHost(RequestBuilder<crate::model::projects::EnableXpnHostRequest>);

    impl EnableXpnHost {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Projects>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::projects::EnableXpnHostRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .enable_xpn_host(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `enable_xpn_host`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Projects::enable_xpn_host::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::projects::EnableXpnHostRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::projects::EnableXpnHostRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::projects::EnableXpnHostRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for EnableXpnHost {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Projects::enable_xpn_resource][crate::client::Projects::enable_xpn_resource] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::projects::EnableXpnResource;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> EnableXpnResource {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct EnableXpnResource(RequestBuilder<crate::model::projects::EnableXpnResourceRequest>);

    impl EnableXpnResource {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Projects>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::projects::EnableXpnResourceRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .enable_xpn_resource(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `enable_xpn_resource`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Projects::enable_xpn_resource::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::projects::EnableXpnResourceRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::projects::EnableXpnResourceRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::projects::EnableXpnResourceRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::projects::EnableXpnResourceRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ProjectsEnableXpnResourceRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::projects::EnableXpnResourceRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ProjectsEnableXpnResourceRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for EnableXpnResource {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Projects::get][crate::client::Projects::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::projects::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::projects::GetRequest>);

    impl Get {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Projects>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::projects::GetRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Project> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::projects::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Projects::get_xpn_host][crate::client::Projects::get_xpn_host] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::projects::GetXpnHost;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetXpnHost {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetXpnHost(RequestBuilder<crate::model::projects::GetXpnHostRequest>);

    impl GetXpnHost {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Projects>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::projects::GetXpnHostRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Project> {
            (*self.0.stub)
                .get_xpn_host(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::projects::GetXpnHostRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetXpnHost {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Projects::get_xpn_resources][crate::client::Projects::get_xpn_resources] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::projects::GetXpnResources;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetXpnResources {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetXpnResources(RequestBuilder<crate::model::projects::GetXpnResourcesRequest>);

    impl GetXpnResources {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Projects>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::projects::GetXpnResourcesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ProjectsGetXpnResources> {
            (*self.0.stub)
                .get_xpn_resources(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::ProjectsGetXpnResources,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ProjectsGetXpnResources,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::projects::GetXpnResourcesRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::projects::GetXpnResourcesRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::projects::GetXpnResourcesRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::projects::GetXpnResourcesRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::projects::GetXpnResourcesRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::projects::GetXpnResourcesRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::projects::GetXpnResourcesRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::projects::GetXpnResourcesRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::projects::GetXpnResourcesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::projects::GetXpnResourcesRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::projects::GetXpnResourcesRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetXpnResources {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Projects::list_xpn_hosts][crate::client::Projects::list_xpn_hosts] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::projects::ListXpnHosts;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListXpnHosts {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListXpnHosts(RequestBuilder<crate::model::projects::ListXpnHostsRequest>);

    impl ListXpnHosts {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Projects>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::projects::ListXpnHostsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::XpnHostList> {
            (*self.0.stub)
                .list_xpn_hosts(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::XpnHostList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::XpnHostList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::projects::ListXpnHostsRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::projects::ListXpnHostsRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::projects::ListXpnHostsRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::projects::ListXpnHostsRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::projects::ListXpnHostsRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::projects::ListXpnHostsRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::projects::ListXpnHostsRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::projects::ListXpnHostsRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::projects::ListXpnHostsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::projects::ListXpnHostsRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::projects::ListXpnHostsRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::projects::ListXpnHostsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ProjectsListXpnHostsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::projects::ListXpnHostsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ProjectsListXpnHostsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListXpnHosts {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Projects::move_disk][crate::client::Projects::move_disk] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::projects::MoveDisk;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> MoveDisk {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct MoveDisk(RequestBuilder<crate::model::projects::MoveDiskRequest>);

    impl MoveDisk {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Projects>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::projects::MoveDiskRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .move_disk(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `move_disk`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Projects::move_disk::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::projects::MoveDiskRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::projects::MoveDiskRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::projects::MoveDiskRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::projects::MoveDiskRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DiskMoveRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::projects::MoveDiskRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DiskMoveRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for MoveDisk {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Projects::move_instance][crate::client::Projects::move_instance] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::projects::MoveInstance;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> MoveInstance {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct MoveInstance(RequestBuilder<crate::model::projects::MoveInstanceRequest>);

    impl MoveInstance {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Projects>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::projects::MoveInstanceRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .move_instance(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `move_instance`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Projects::move_instance::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::projects::MoveInstanceRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::projects::MoveInstanceRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::projects::MoveInstanceRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::projects::MoveInstanceRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceMoveRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::projects::MoveInstanceRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceMoveRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for MoveInstance {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Projects::set_cloud_armor_tier][crate::client::Projects::set_cloud_armor_tier] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::projects::SetCloudArmorTier;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetCloudArmorTier {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetCloudArmorTier(RequestBuilder<crate::model::projects::SetCloudArmorTierRequest>);

    impl SetCloudArmorTier {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Projects>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::projects::SetCloudArmorTierRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_cloud_armor_tier(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_cloud_armor_tier`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Projects::set_cloud_armor_tier::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::projects::SetCloudArmorTierRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::projects::SetCloudArmorTierRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::projects::SetCloudArmorTierRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::projects::SetCloudArmorTierRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ProjectsSetCloudArmorTierRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::projects::SetCloudArmorTierRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ProjectsSetCloudArmorTierRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetCloudArmorTier {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Projects::set_common_instance_metadata][crate::client::Projects::set_common_instance_metadata] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::projects::SetCommonInstanceMetadata;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetCommonInstanceMetadata {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetCommonInstanceMetadata(
        RequestBuilder<crate::model::projects::SetCommonInstanceMetadataRequest>,
    );

    impl SetCommonInstanceMetadata {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Projects>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::projects::SetCommonInstanceMetadataRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_common_instance_metadata(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_common_instance_metadata`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Projects::set_common_instance_metadata::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::projects::SetCommonInstanceMetadataRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::projects::SetCommonInstanceMetadataRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::projects::SetCommonInstanceMetadataRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::projects::SetCommonInstanceMetadataRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Metadata>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::projects::SetCommonInstanceMetadataRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Metadata>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetCommonInstanceMetadata {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Projects::set_default_network_tier][crate::client::Projects::set_default_network_tier] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::projects::SetDefaultNetworkTier;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetDefaultNetworkTier {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetDefaultNetworkTier(
        RequestBuilder<crate::model::projects::SetDefaultNetworkTierRequest>,
    );

    impl SetDefaultNetworkTier {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Projects>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::projects::SetDefaultNetworkTierRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_default_network_tier(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_default_network_tier`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Projects::set_default_network_tier::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::projects::SetDefaultNetworkTierRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::projects::SetDefaultNetworkTierRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::projects::SetDefaultNetworkTierRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::projects::SetDefaultNetworkTierRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ProjectsSetDefaultNetworkTierRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::projects::SetDefaultNetworkTierRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ProjectsSetDefaultNetworkTierRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetDefaultNetworkTier {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Projects::set_usage_export_bucket][crate::client::Projects::set_usage_export_bucket] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::projects::SetUsageExportBucket;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetUsageExportBucket {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetUsageExportBucket(
        RequestBuilder<crate::model::projects::SetUsageExportBucketRequest>,
    );

    impl SetUsageExportBucket {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Projects>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::projects::SetUsageExportBucketRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_usage_export_bucket(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_usage_export_bucket`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Projects::set_usage_export_bucket::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::projects::SetUsageExportBucketRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::projects::SetUsageExportBucketRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::projects::SetUsageExportBucketRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::projects::SetUsageExportBucketRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::UsageExportLocation>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::projects::SetUsageExportBucketRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::UsageExportLocation>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetUsageExportBucket {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Projects::get_operation][crate::client::Projects::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::projects::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Projects>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [PublicAdvertisedPrefixes][crate::client::PublicAdvertisedPrefixes].
#[cfg(feature = "public-advertised-prefixes")]
#[cfg_attr(docsrs, doc(cfg(feature = "public-advertised-prefixes")))]
pub mod public_advertised_prefixes {
    use crate::Result;

    /// A builder for [PublicAdvertisedPrefixes][crate::client::PublicAdvertisedPrefixes].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::public_advertised_prefixes::ClientBuilder;
    /// # use client::PublicAdvertisedPrefixes;
    /// let builder : ClientBuilder = PublicAdvertisedPrefixes::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::PublicAdvertisedPrefixes;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = PublicAdvertisedPrefixes;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::PublicAdvertisedPrefixes] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::PublicAdvertisedPrefixes>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PublicAdvertisedPrefixes>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [PublicAdvertisedPrefixes::announce][crate::client::PublicAdvertisedPrefixes::announce] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::public_advertised_prefixes::Announce;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Announce {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Announce(RequestBuilder<crate::model::public_advertised_prefixes::AnnounceRequest>);

    impl Announce {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PublicAdvertisedPrefixes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::public_advertised_prefixes::AnnounceRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .announce(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `announce`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::PublicAdvertisedPrefixes::announce::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::public_advertised_prefixes::AnnounceRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [public_advertised_prefix][crate::model::public_advertised_prefixes::AnnounceRequest::public_advertised_prefix].
        pub fn set_public_advertised_prefix<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.public_advertised_prefix = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::public_advertised_prefixes::AnnounceRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::public_advertised_prefixes::AnnounceRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Announce {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [PublicAdvertisedPrefixes::delete][crate::client::PublicAdvertisedPrefixes::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::public_advertised_prefixes::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::public_advertised_prefixes::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PublicAdvertisedPrefixes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::public_advertised_prefixes::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::PublicAdvertisedPrefixes::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::public_advertised_prefixes::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [public_advertised_prefix][crate::model::public_advertised_prefixes::DeleteRequest::public_advertised_prefix].
        pub fn set_public_advertised_prefix<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.public_advertised_prefix = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::public_advertised_prefixes::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::public_advertised_prefixes::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [PublicAdvertisedPrefixes::get][crate::client::PublicAdvertisedPrefixes::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::public_advertised_prefixes::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::public_advertised_prefixes::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PublicAdvertisedPrefixes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::public_advertised_prefixes::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::PublicAdvertisedPrefix> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::public_advertised_prefixes::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [public_advertised_prefix][crate::model::public_advertised_prefixes::GetRequest::public_advertised_prefix].
        pub fn set_public_advertised_prefix<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.public_advertised_prefix = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [PublicAdvertisedPrefixes::insert][crate::client::PublicAdvertisedPrefixes::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::public_advertised_prefixes::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::public_advertised_prefixes::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PublicAdvertisedPrefixes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::public_advertised_prefixes::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::PublicAdvertisedPrefixes::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::public_advertised_prefixes::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::public_advertised_prefixes::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::public_advertised_prefixes::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::public_advertised_prefixes::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::PublicAdvertisedPrefix>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::public_advertised_prefixes::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::PublicAdvertisedPrefix>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [PublicAdvertisedPrefixes::list][crate::client::PublicAdvertisedPrefixes::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::public_advertised_prefixes::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::public_advertised_prefixes::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PublicAdvertisedPrefixes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::public_advertised_prefixes::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::PublicAdvertisedPrefixList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::PublicAdvertisedPrefixList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::PublicAdvertisedPrefixList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::public_advertised_prefixes::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::public_advertised_prefixes::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::public_advertised_prefixes::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::public_advertised_prefixes::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::public_advertised_prefixes::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::public_advertised_prefixes::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::public_advertised_prefixes::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::public_advertised_prefixes::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::public_advertised_prefixes::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::public_advertised_prefixes::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::public_advertised_prefixes::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [PublicAdvertisedPrefixes::patch][crate::client::PublicAdvertisedPrefixes::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::public_advertised_prefixes::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::public_advertised_prefixes::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PublicAdvertisedPrefixes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::public_advertised_prefixes::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::PublicAdvertisedPrefixes::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::public_advertised_prefixes::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [public_advertised_prefix][crate::model::public_advertised_prefixes::PatchRequest::public_advertised_prefix].
        pub fn set_public_advertised_prefix<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.public_advertised_prefix = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::public_advertised_prefixes::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::public_advertised_prefixes::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::public_advertised_prefixes::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::PublicAdvertisedPrefix>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::public_advertised_prefixes::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::PublicAdvertisedPrefix>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [PublicAdvertisedPrefixes::withdraw][crate::client::PublicAdvertisedPrefixes::withdraw] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::public_advertised_prefixes::Withdraw;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Withdraw {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Withdraw(RequestBuilder<crate::model::public_advertised_prefixes::WithdrawRequest>);

    impl Withdraw {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PublicAdvertisedPrefixes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::public_advertised_prefixes::WithdrawRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .withdraw(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `withdraw`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::PublicAdvertisedPrefixes::withdraw::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::public_advertised_prefixes::WithdrawRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [public_advertised_prefix][crate::model::public_advertised_prefixes::WithdrawRequest::public_advertised_prefix].
        pub fn set_public_advertised_prefix<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.public_advertised_prefix = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::public_advertised_prefixes::WithdrawRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::public_advertised_prefixes::WithdrawRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Withdraw {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [PublicAdvertisedPrefixes::get_operation][crate::client::PublicAdvertisedPrefixes::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::public_advertised_prefixes::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PublicAdvertisedPrefixes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [PublicDelegatedPrefixes][crate::client::PublicDelegatedPrefixes].
#[cfg(feature = "public-delegated-prefixes")]
#[cfg_attr(docsrs, doc(cfg(feature = "public-delegated-prefixes")))]
pub mod public_delegated_prefixes {
    use crate::Result;

    /// A builder for [PublicDelegatedPrefixes][crate::client::PublicDelegatedPrefixes].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::public_delegated_prefixes::ClientBuilder;
    /// # use client::PublicDelegatedPrefixes;
    /// let builder : ClientBuilder = PublicDelegatedPrefixes::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::PublicDelegatedPrefixes;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = PublicDelegatedPrefixes;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::PublicDelegatedPrefixes] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::PublicDelegatedPrefixes>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PublicDelegatedPrefixes>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [PublicDelegatedPrefixes::aggregated_list][crate::client::PublicDelegatedPrefixes::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::public_delegated_prefixes::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::public_delegated_prefixes::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PublicDelegatedPrefixes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::public_delegated_prefixes::AggregatedListRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::PublicDelegatedPrefixAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::PublicDelegatedPrefixAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::PublicDelegatedPrefixAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::public_delegated_prefixes::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::public_delegated_prefixes::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::public_delegated_prefixes::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::public_delegated_prefixes::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::public_delegated_prefixes::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::public_delegated_prefixes::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::public_delegated_prefixes::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::public_delegated_prefixes::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::public_delegated_prefixes::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::public_delegated_prefixes::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::public_delegated_prefixes::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::public_delegated_prefixes::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::public_delegated_prefixes::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::public_delegated_prefixes::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::public_delegated_prefixes::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [PublicDelegatedPrefixes::announce][crate::client::PublicDelegatedPrefixes::announce] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::public_delegated_prefixes::Announce;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Announce {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Announce(RequestBuilder<crate::model::public_delegated_prefixes::AnnounceRequest>);

    impl Announce {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PublicDelegatedPrefixes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::public_delegated_prefixes::AnnounceRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .announce(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `announce`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::PublicDelegatedPrefixes::announce::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::public_delegated_prefixes::AnnounceRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [public_delegated_prefix][crate::model::public_delegated_prefixes::AnnounceRequest::public_delegated_prefix].
        pub fn set_public_delegated_prefix<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.public_delegated_prefix = v.into();
            self
        }

        /// Sets the value of [region][crate::model::public_delegated_prefixes::AnnounceRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::public_delegated_prefixes::AnnounceRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::public_delegated_prefixes::AnnounceRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Announce {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [PublicDelegatedPrefixes::delete][crate::client::PublicDelegatedPrefixes::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::public_delegated_prefixes::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::public_delegated_prefixes::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PublicDelegatedPrefixes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::public_delegated_prefixes::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::PublicDelegatedPrefixes::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::public_delegated_prefixes::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [public_delegated_prefix][crate::model::public_delegated_prefixes::DeleteRequest::public_delegated_prefix].
        pub fn set_public_delegated_prefix<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.public_delegated_prefix = v.into();
            self
        }

        /// Sets the value of [region][crate::model::public_delegated_prefixes::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::public_delegated_prefixes::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::public_delegated_prefixes::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [PublicDelegatedPrefixes::get][crate::client::PublicDelegatedPrefixes::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::public_delegated_prefixes::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::public_delegated_prefixes::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PublicDelegatedPrefixes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::public_delegated_prefixes::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::PublicDelegatedPrefix> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::public_delegated_prefixes::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [public_delegated_prefix][crate::model::public_delegated_prefixes::GetRequest::public_delegated_prefix].
        pub fn set_public_delegated_prefix<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.public_delegated_prefix = v.into();
            self
        }

        /// Sets the value of [region][crate::model::public_delegated_prefixes::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [PublicDelegatedPrefixes::insert][crate::client::PublicDelegatedPrefixes::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::public_delegated_prefixes::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::public_delegated_prefixes::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PublicDelegatedPrefixes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::public_delegated_prefixes::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::PublicDelegatedPrefixes::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::public_delegated_prefixes::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::public_delegated_prefixes::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::public_delegated_prefixes::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::public_delegated_prefixes::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::public_delegated_prefixes::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::PublicDelegatedPrefix>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::public_delegated_prefixes::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::PublicDelegatedPrefix>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [PublicDelegatedPrefixes::list][crate::client::PublicDelegatedPrefixes::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::public_delegated_prefixes::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::public_delegated_prefixes::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PublicDelegatedPrefixes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::public_delegated_prefixes::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::PublicDelegatedPrefixList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::PublicDelegatedPrefixList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::PublicDelegatedPrefixList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::public_delegated_prefixes::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::public_delegated_prefixes::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::public_delegated_prefixes::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::public_delegated_prefixes::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::public_delegated_prefixes::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::public_delegated_prefixes::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::public_delegated_prefixes::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::public_delegated_prefixes::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::public_delegated_prefixes::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::public_delegated_prefixes::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::public_delegated_prefixes::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::public_delegated_prefixes::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [PublicDelegatedPrefixes::patch][crate::client::PublicDelegatedPrefixes::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::public_delegated_prefixes::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::public_delegated_prefixes::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PublicDelegatedPrefixes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::public_delegated_prefixes::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::PublicDelegatedPrefixes::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::public_delegated_prefixes::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [public_delegated_prefix][crate::model::public_delegated_prefixes::PatchRequest::public_delegated_prefix].
        pub fn set_public_delegated_prefix<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.public_delegated_prefix = v.into();
            self
        }

        /// Sets the value of [region][crate::model::public_delegated_prefixes::PatchRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::public_delegated_prefixes::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::public_delegated_prefixes::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::public_delegated_prefixes::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::PublicDelegatedPrefix>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::public_delegated_prefixes::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::PublicDelegatedPrefix>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [PublicDelegatedPrefixes::withdraw][crate::client::PublicDelegatedPrefixes::withdraw] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::public_delegated_prefixes::Withdraw;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Withdraw {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Withdraw(RequestBuilder<crate::model::public_delegated_prefixes::WithdrawRequest>);

    impl Withdraw {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PublicDelegatedPrefixes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::public_delegated_prefixes::WithdrawRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .withdraw(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `withdraw`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::PublicDelegatedPrefixes::withdraw::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::public_delegated_prefixes::WithdrawRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [public_delegated_prefix][crate::model::public_delegated_prefixes::WithdrawRequest::public_delegated_prefix].
        pub fn set_public_delegated_prefix<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.public_delegated_prefix = v.into();
            self
        }

        /// Sets the value of [region][crate::model::public_delegated_prefixes::WithdrawRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::public_delegated_prefixes::WithdrawRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::public_delegated_prefixes::WithdrawRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Withdraw {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [PublicDelegatedPrefixes::get_operation][crate::client::PublicDelegatedPrefixes::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::public_delegated_prefixes::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::PublicDelegatedPrefixes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionAutoscalers][crate::client::RegionAutoscalers].
#[cfg(feature = "region-autoscalers")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-autoscalers")))]
pub mod region_autoscalers {
    use crate::Result;

    /// A builder for [RegionAutoscalers][crate::client::RegionAutoscalers].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_autoscalers::ClientBuilder;
    /// # use client::RegionAutoscalers;
    /// let builder : ClientBuilder = RegionAutoscalers::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionAutoscalers;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionAutoscalers;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionAutoscalers] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionAutoscalers>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionAutoscalers>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionAutoscalers::delete][crate::client::RegionAutoscalers::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_autoscalers::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::region_autoscalers::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionAutoscalers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_autoscalers::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionAutoscalers::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [autoscaler][crate::model::region_autoscalers::DeleteRequest::autoscaler].
        pub fn set_autoscaler<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.autoscaler = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_autoscalers::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_autoscalers::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_autoscalers::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_autoscalers::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionAutoscalers::get][crate::client::RegionAutoscalers::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_autoscalers::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_autoscalers::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionAutoscalers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_autoscalers::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Autoscaler> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [autoscaler][crate::model::region_autoscalers::GetRequest::autoscaler].
        pub fn set_autoscaler<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.autoscaler = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_autoscalers::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_autoscalers::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionAutoscalers::insert][crate::client::RegionAutoscalers::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_autoscalers::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::region_autoscalers::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionAutoscalers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_autoscalers::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionAutoscalers::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_autoscalers::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_autoscalers::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_autoscalers::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_autoscalers::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_autoscalers::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Autoscaler>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_autoscalers::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Autoscaler>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionAutoscalers::list][crate::client::RegionAutoscalers::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_autoscalers::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_autoscalers::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionAutoscalers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_autoscalers::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::RegionAutoscalerList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::RegionAutoscalerList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::RegionAutoscalerList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_autoscalers::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_autoscalers::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_autoscalers::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_autoscalers::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_autoscalers::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_autoscalers::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_autoscalers::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_autoscalers::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_autoscalers::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_autoscalers::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_autoscalers::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_autoscalers::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionAutoscalers::patch][crate::client::RegionAutoscalers::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_autoscalers::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::region_autoscalers::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionAutoscalers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_autoscalers::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionAutoscalers::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [autoscaler][crate::model::region_autoscalers::PatchRequest::autoscaler].
        pub fn set_autoscaler<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.autoscaler = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [autoscaler][crate::model::region_autoscalers::PatchRequest::autoscaler].
        pub fn set_or_clear_autoscaler<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.autoscaler = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_autoscalers::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_autoscalers::PatchRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_autoscalers::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_autoscalers::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_autoscalers::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Autoscaler>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_autoscalers::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Autoscaler>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionAutoscalers::test_iam_permissions][crate::client::RegionAutoscalers::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_autoscalers::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::region_autoscalers::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionAutoscalers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_autoscalers::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_autoscalers::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_autoscalers::TestIamPermissionsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_autoscalers::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_autoscalers::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_autoscalers::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionAutoscalers::update][crate::client::RegionAutoscalers::update] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_autoscalers::Update;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Update {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Update(RequestBuilder<crate::model::region_autoscalers::UpdateRequest>);

    impl Update {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionAutoscalers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_autoscalers::UpdateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionAutoscalers::update::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [autoscaler][crate::model::region_autoscalers::UpdateRequest::autoscaler].
        pub fn set_autoscaler<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.autoscaler = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [autoscaler][crate::model::region_autoscalers::UpdateRequest::autoscaler].
        pub fn set_or_clear_autoscaler<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.autoscaler = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_autoscalers::UpdateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_autoscalers::UpdateRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_autoscalers::UpdateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_autoscalers::UpdateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_autoscalers::UpdateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Autoscaler>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_autoscalers::UpdateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Autoscaler>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Update {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionAutoscalers::get_operation][crate::client::RegionAutoscalers::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_autoscalers::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionAutoscalers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionBackendBuckets][crate::client::RegionBackendBuckets].
#[cfg(feature = "region-backend-buckets")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-backend-buckets")))]
pub mod region_backend_buckets {
    use crate::Result;

    /// A builder for [RegionBackendBuckets][crate::client::RegionBackendBuckets].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_backend_buckets::ClientBuilder;
    /// # use client::RegionBackendBuckets;
    /// let builder : ClientBuilder = RegionBackendBuckets::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionBackendBuckets;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionBackendBuckets;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionBackendBuckets] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendBuckets>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendBuckets>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionBackendBuckets::delete][crate::client::RegionBackendBuckets::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_backend_buckets::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::region_backend_buckets::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendBuckets>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_backend_buckets::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionBackendBuckets::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [backend_bucket][crate::model::region_backend_buckets::DeleteRequest::backend_bucket].
        pub fn set_backend_bucket<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_bucket = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_backend_buckets::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_backend_buckets::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_backend_buckets::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_backend_buckets::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionBackendBuckets::get][crate::client::RegionBackendBuckets::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_backend_buckets::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_backend_buckets::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendBuckets>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_backend_buckets::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::BackendBucket> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [backend_bucket][crate::model::region_backend_buckets::GetRequest::backend_bucket].
        pub fn set_backend_bucket<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_bucket = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_backend_buckets::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_backend_buckets::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionBackendBuckets::get_iam_policy][crate::client::RegionBackendBuckets::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_backend_buckets::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(
        RequestBuilder<crate::model::region_backend_buckets::GetIamPolicyRequest>,
    );

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendBuckets>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_backend_buckets::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::region_backend_buckets::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::region_backend_buckets::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_backend_buckets::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_backend_buckets::GetIamPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_backend_buckets::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionBackendBuckets::insert][crate::client::RegionBackendBuckets::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_backend_buckets::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::region_backend_buckets::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendBuckets>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_backend_buckets::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionBackendBuckets::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_backend_buckets::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_backend_buckets::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_backend_buckets::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_backend_buckets::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_backend_buckets::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::BackendBucket>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_backend_buckets::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::BackendBucket>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionBackendBuckets::list][crate::client::RegionBackendBuckets::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_backend_buckets::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_backend_buckets::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendBuckets>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_backend_buckets::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::BackendBucketList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::BackendBucketList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::BackendBucketList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_backend_buckets::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_backend_buckets::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_backend_buckets::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_backend_buckets::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_backend_buckets::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_backend_buckets::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_backend_buckets::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_backend_buckets::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_backend_buckets::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_backend_buckets::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_backend_buckets::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_backend_buckets::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionBackendBuckets::list_usable][crate::client::RegionBackendBuckets::list_usable] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_backend_buckets::ListUsable;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListUsable {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListUsable(RequestBuilder<crate::model::region_backend_buckets::ListUsableRequest>);

    impl ListUsable {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendBuckets>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_backend_buckets::ListUsableRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::BackendBucketListUsable> {
            (*self.0.stub)
                .list_usable(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::BackendBucketListUsable,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::BackendBucketListUsable,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_backend_buckets::ListUsableRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_backend_buckets::ListUsableRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_backend_buckets::ListUsableRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_backend_buckets::ListUsableRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_backend_buckets::ListUsableRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_backend_buckets::ListUsableRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_backend_buckets::ListUsableRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_backend_buckets::ListUsableRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_backend_buckets::ListUsableRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_backend_buckets::ListUsableRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_backend_buckets::ListUsableRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_backend_buckets::ListUsableRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListUsable {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionBackendBuckets::patch][crate::client::RegionBackendBuckets::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_backend_buckets::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::region_backend_buckets::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendBuckets>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_backend_buckets::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionBackendBuckets::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [backend_bucket][crate::model::region_backend_buckets::PatchRequest::backend_bucket].
        pub fn set_backend_bucket<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_bucket = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_backend_buckets::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_backend_buckets::PatchRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_backend_buckets::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_backend_buckets::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_backend_buckets::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::BackendBucket>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_backend_buckets::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::BackendBucket>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionBackendBuckets::set_iam_policy][crate::client::RegionBackendBuckets::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_backend_buckets::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(
        RequestBuilder<crate::model::region_backend_buckets::SetIamPolicyRequest>,
    );

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendBuckets>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_backend_buckets::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_backend_buckets::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_backend_buckets::SetIamPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_backend_buckets::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_backend_buckets::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_backend_buckets::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionBackendBuckets::test_iam_permissions][crate::client::RegionBackendBuckets::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_backend_buckets::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::region_backend_buckets::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendBuckets>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_backend_buckets::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_backend_buckets::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_backend_buckets::TestIamPermissionsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_backend_buckets::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_backend_buckets::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_backend_buckets::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionBackendBuckets::get_operation][crate::client::RegionBackendBuckets::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_backend_buckets::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendBuckets>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionBackendServices][crate::client::RegionBackendServices].
#[cfg(feature = "region-backend-services")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-backend-services")))]
pub mod region_backend_services {
    use crate::Result;

    /// A builder for [RegionBackendServices][crate::client::RegionBackendServices].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_backend_services::ClientBuilder;
    /// # use client::RegionBackendServices;
    /// let builder : ClientBuilder = RegionBackendServices::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionBackendServices;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionBackendServices;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionBackendServices] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendServices>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendServices>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionBackendServices::delete][crate::client::RegionBackendServices::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_backend_services::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::region_backend_services::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_backend_services::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionBackendServices::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [backend_service][crate::model::region_backend_services::DeleteRequest::backend_service].
        pub fn set_backend_service<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_service = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_backend_services::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_backend_services::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_backend_services::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_backend_services::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionBackendServices::get][crate::client::RegionBackendServices::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_backend_services::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_backend_services::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_backend_services::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::BackendService> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [backend_service][crate::model::region_backend_services::GetRequest::backend_service].
        pub fn set_backend_service<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_service = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_backend_services::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_backend_services::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionBackendServices::get_health][crate::client::RegionBackendServices::get_health] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_backend_services::GetHealth;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetHealth {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetHealth(RequestBuilder<crate::model::region_backend_services::GetHealthRequest>);

    impl GetHealth {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_backend_services::GetHealthRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::BackendServiceGroupHealth> {
            (*self.0.stub)
                .get_health(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [backend_service][crate::model::region_backend_services::GetHealthRequest::backend_service].
        pub fn set_backend_service<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_service = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_backend_services::GetHealthRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_backend_services::GetHealthRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_backend_services::GetHealthRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ResourceGroupReference>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_backend_services::GetHealthRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ResourceGroupReference>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetHealth {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionBackendServices::get_iam_policy][crate::client::RegionBackendServices::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_backend_services::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(
        RequestBuilder<crate::model::region_backend_services::GetIamPolicyRequest>,
    );

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_backend_services::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::region_backend_services::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::region_backend_services::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_backend_services::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_backend_services::GetIamPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_backend_services::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionBackendServices::insert][crate::client::RegionBackendServices::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_backend_services::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::region_backend_services::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_backend_services::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionBackendServices::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_backend_services::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_backend_services::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_backend_services::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_backend_services::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_backend_services::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::BackendService>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_backend_services::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::BackendService>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionBackendServices::list][crate::client::RegionBackendServices::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_backend_services::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_backend_services::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_backend_services::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::BackendServiceList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::BackendServiceList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::BackendServiceList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_backend_services::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_backend_services::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_backend_services::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_backend_services::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_backend_services::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_backend_services::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_backend_services::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_backend_services::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_backend_services::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_backend_services::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_backend_services::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_backend_services::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionBackendServices::list_usable][crate::client::RegionBackendServices::list_usable] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_backend_services::ListUsable;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListUsable {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListUsable(RequestBuilder<crate::model::region_backend_services::ListUsableRequest>);

    impl ListUsable {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_backend_services::ListUsableRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::BackendServiceListUsable> {
            (*self.0.stub)
                .list_usable(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::BackendServiceListUsable,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::BackendServiceListUsable,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_backend_services::ListUsableRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_backend_services::ListUsableRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_backend_services::ListUsableRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_backend_services::ListUsableRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_backend_services::ListUsableRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_backend_services::ListUsableRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_backend_services::ListUsableRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_backend_services::ListUsableRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_backend_services::ListUsableRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_backend_services::ListUsableRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_backend_services::ListUsableRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_backend_services::ListUsableRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListUsable {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionBackendServices::patch][crate::client::RegionBackendServices::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_backend_services::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::region_backend_services::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_backend_services::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionBackendServices::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [backend_service][crate::model::region_backend_services::PatchRequest::backend_service].
        pub fn set_backend_service<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_service = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_backend_services::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_backend_services::PatchRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_backend_services::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_backend_services::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_backend_services::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::BackendService>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_backend_services::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::BackendService>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionBackendServices::set_iam_policy][crate::client::RegionBackendServices::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_backend_services::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(
        RequestBuilder<crate::model::region_backend_services::SetIamPolicyRequest>,
    );

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_backend_services::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_backend_services::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_backend_services::SetIamPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_backend_services::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_backend_services::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_backend_services::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionBackendServices::set_security_policy][crate::client::RegionBackendServices::set_security_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_backend_services::SetSecurityPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetSecurityPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetSecurityPolicy(
        RequestBuilder<crate::model::region_backend_services::SetSecurityPolicyRequest>,
    );

    impl SetSecurityPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_backend_services::SetSecurityPolicyRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_security_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_security_policy`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionBackendServices::set_security_policy::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [backend_service][crate::model::region_backend_services::SetSecurityPolicyRequest::backend_service].
        pub fn set_backend_service<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_service = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_backend_services::SetSecurityPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_backend_services::SetSecurityPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_backend_services::SetSecurityPolicyRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_backend_services::SetSecurityPolicyRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_backend_services::SetSecurityPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyReference>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_backend_services::SetSecurityPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyReference>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetSecurityPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionBackendServices::test_iam_permissions][crate::client::RegionBackendServices::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_backend_services::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::region_backend_services::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_backend_services::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_backend_services::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_backend_services::TestIamPermissionsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_backend_services::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_backend_services::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_backend_services::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionBackendServices::update][crate::client::RegionBackendServices::update] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_backend_services::Update;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Update {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Update(RequestBuilder<crate::model::region_backend_services::UpdateRequest>);

    impl Update {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_backend_services::UpdateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionBackendServices::update::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [backend_service][crate::model::region_backend_services::UpdateRequest::backend_service].
        pub fn set_backend_service<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.backend_service = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_backend_services::UpdateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_backend_services::UpdateRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_backend_services::UpdateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_backend_services::UpdateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_backend_services::UpdateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::BackendService>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_backend_services::UpdateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::BackendService>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Update {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionBackendServices::get_operation][crate::client::RegionBackendServices::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_backend_services::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionBackendServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionCommitments][crate::client::RegionCommitments].
#[cfg(feature = "region-commitments")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-commitments")))]
pub mod region_commitments {
    use crate::Result;

    /// A builder for [RegionCommitments][crate::client::RegionCommitments].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_commitments::ClientBuilder;
    /// # use client::RegionCommitments;
    /// let builder : ClientBuilder = RegionCommitments::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionCommitments;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionCommitments;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionCommitments] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionCommitments>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionCommitments>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionCommitments::aggregated_list][crate::client::RegionCommitments::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_commitments::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::region_commitments::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionCommitments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_commitments::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::CommitmentAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::CommitmentAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::CommitmentAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_commitments::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_commitments::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::region_commitments::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::region_commitments::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_commitments::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_commitments::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_commitments::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_commitments::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_commitments::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_commitments::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_commitments::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_commitments::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_commitments::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::region_commitments::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::region_commitments::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionCommitments::get][crate::client::RegionCommitments::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_commitments::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_commitments::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionCommitments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_commitments::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Commitment> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [commitment][crate::model::region_commitments::GetRequest::commitment].
        pub fn set_commitment<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.commitment = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_commitments::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_commitments::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionCommitments::insert][crate::client::RegionCommitments::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_commitments::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::region_commitments::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionCommitments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_commitments::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionCommitments::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_commitments::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_commitments::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_commitments::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_commitments::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_commitments::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Commitment>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_commitments::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Commitment>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionCommitments::list][crate::client::RegionCommitments::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_commitments::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_commitments::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionCommitments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_commitments::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::CommitmentList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::CommitmentList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::CommitmentList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_commitments::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_commitments::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_commitments::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_commitments::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_commitments::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_commitments::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_commitments::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_commitments::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_commitments::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_commitments::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_commitments::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_commitments::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionCommitments::update][crate::client::RegionCommitments::update] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_commitments::Update;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Update {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Update(RequestBuilder<crate::model::region_commitments::UpdateRequest>);

    impl Update {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionCommitments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_commitments::UpdateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionCommitments::update::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [commitment][crate::model::region_commitments::UpdateRequest::commitment].
        pub fn set_commitment<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.commitment = v.into();
            self
        }

        /// Sets the value of [paths][crate::model::region_commitments::UpdateRequest::paths].
        pub fn set_paths<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.paths = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [paths][crate::model::region_commitments::UpdateRequest::paths].
        pub fn set_or_clear_paths<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.paths = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_commitments::UpdateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_commitments::UpdateRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_commitments::UpdateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_commitments::UpdateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::region_commitments::UpdateRequest::update_mask].
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::region_commitments::UpdateRequest::update_mask].
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_commitments::UpdateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Commitment>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_commitments::UpdateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Commitment>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Update {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionCommitments::get_operation][crate::client::RegionCommitments::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_commitments::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionCommitments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionCompositeHealthChecks][crate::client::RegionCompositeHealthChecks].
#[cfg(feature = "region-composite-health-checks")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-composite-health-checks")))]
pub mod region_composite_health_checks {
    use crate::Result;

    /// A builder for [RegionCompositeHealthChecks][crate::client::RegionCompositeHealthChecks].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_composite_health_checks::ClientBuilder;
    /// # use client::RegionCompositeHealthChecks;
    /// let builder : ClientBuilder = RegionCompositeHealthChecks::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionCompositeHealthChecks;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionCompositeHealthChecks;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionCompositeHealthChecks] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionCompositeHealthChecks>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionCompositeHealthChecks>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionCompositeHealthChecks::aggregated_list][crate::client::RegionCompositeHealthChecks::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_composite_health_checks::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::region_composite_health_checks::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionCompositeHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_composite_health_checks::AggregatedListRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::CompositeHealthCheckAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::CompositeHealthCheckAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::CompositeHealthCheckAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_composite_health_checks::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_composite_health_checks::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::region_composite_health_checks::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::region_composite_health_checks::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_composite_health_checks::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_composite_health_checks::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_composite_health_checks::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_composite_health_checks::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_composite_health_checks::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_composite_health_checks::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_composite_health_checks::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_composite_health_checks::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_composite_health_checks::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::region_composite_health_checks::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::region_composite_health_checks::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionCompositeHealthChecks::delete][crate::client::RegionCompositeHealthChecks::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_composite_health_checks::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::region_composite_health_checks::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionCompositeHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_composite_health_checks::DeleteRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionCompositeHealthChecks::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [composite_health_check][crate::model::region_composite_health_checks::DeleteRequest::composite_health_check].
        pub fn set_composite_health_check<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.composite_health_check = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_composite_health_checks::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_composite_health_checks::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_composite_health_checks::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_composite_health_checks::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionCompositeHealthChecks::get][crate::client::RegionCompositeHealthChecks::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_composite_health_checks::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_composite_health_checks::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionCompositeHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_composite_health_checks::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::CompositeHealthCheck> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [composite_health_check][crate::model::region_composite_health_checks::GetRequest::composite_health_check].
        pub fn set_composite_health_check<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.composite_health_check = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_composite_health_checks::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_composite_health_checks::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionCompositeHealthChecks::get_health][crate::client::RegionCompositeHealthChecks::get_health] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_composite_health_checks::GetHealth;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetHealth {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetHealth(
        RequestBuilder<crate::model::region_composite_health_checks::GetHealthRequest>,
    );

    impl GetHealth {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionCompositeHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_composite_health_checks::GetHealthRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::CompositeHealthCheckHealth> {
            (*self.0.stub)
                .get_health(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [composite_health_check][crate::model::region_composite_health_checks::GetHealthRequest::composite_health_check].
        pub fn set_composite_health_check<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.composite_health_check = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_composite_health_checks::GetHealthRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_composite_health_checks::GetHealthRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetHealth {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionCompositeHealthChecks::insert][crate::client::RegionCompositeHealthChecks::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_composite_health_checks::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::region_composite_health_checks::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionCompositeHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_composite_health_checks::InsertRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionCompositeHealthChecks::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_composite_health_checks::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_composite_health_checks::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_composite_health_checks::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_composite_health_checks::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_composite_health_checks::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::CompositeHealthCheck>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_composite_health_checks::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::CompositeHealthCheck>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionCompositeHealthChecks::list][crate::client::RegionCompositeHealthChecks::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_composite_health_checks::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_composite_health_checks::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionCompositeHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_composite_health_checks::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::CompositeHealthCheckList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::CompositeHealthCheckList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::CompositeHealthCheckList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_composite_health_checks::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_composite_health_checks::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_composite_health_checks::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_composite_health_checks::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_composite_health_checks::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_composite_health_checks::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_composite_health_checks::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_composite_health_checks::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_composite_health_checks::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_composite_health_checks::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_composite_health_checks::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_composite_health_checks::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionCompositeHealthChecks::patch][crate::client::RegionCompositeHealthChecks::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_composite_health_checks::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::region_composite_health_checks::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionCompositeHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_composite_health_checks::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionCompositeHealthChecks::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [composite_health_check][crate::model::region_composite_health_checks::PatchRequest::composite_health_check].
        pub fn set_composite_health_check<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.composite_health_check = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_composite_health_checks::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_composite_health_checks::PatchRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_composite_health_checks::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_composite_health_checks::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_composite_health_checks::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::CompositeHealthCheck>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_composite_health_checks::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::CompositeHealthCheck>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionCompositeHealthChecks::test_iam_permissions][crate::client::RegionCompositeHealthChecks::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_composite_health_checks::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::region_composite_health_checks::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionCompositeHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_composite_health_checks::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_composite_health_checks::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_composite_health_checks::TestIamPermissionsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_composite_health_checks::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_composite_health_checks::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_composite_health_checks::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionCompositeHealthChecks::get_operation][crate::client::RegionCompositeHealthChecks::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_composite_health_checks::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionCompositeHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionDiskTypes][crate::client::RegionDiskTypes].
#[cfg(feature = "region-disk-types")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-disk-types")))]
pub mod region_disk_types {
    use crate::Result;

    /// A builder for [RegionDiskTypes][crate::client::RegionDiskTypes].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_disk_types::ClientBuilder;
    /// # use client::RegionDiskTypes;
    /// let builder : ClientBuilder = RegionDiskTypes::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionDiskTypes;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionDiskTypes;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionDiskTypes] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionDiskTypes>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionDiskTypes>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionDiskTypes::get][crate::client::RegionDiskTypes::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_disk_types::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_disk_types::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionDiskTypes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_disk_types::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::DiskType> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [disk_type][crate::model::region_disk_types::GetRequest::disk_type].
        pub fn set_disk_type<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.disk_type = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_disk_types::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_disk_types::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionDiskTypes::list][crate::client::RegionDiskTypes::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_disk_types::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_disk_types::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionDiskTypes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_disk_types::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::RegionDiskTypeList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::RegionDiskTypeList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::RegionDiskTypeList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_disk_types::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_disk_types::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_disk_types::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_disk_types::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_disk_types::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_disk_types::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_disk_types::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_disk_types::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_disk_types::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_disk_types::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_disk_types::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_disk_types::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionDisks][crate::client::RegionDisks].
#[cfg(feature = "region-disks")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-disks")))]
pub mod region_disks {
    use crate::Result;

    /// A builder for [RegionDisks][crate::client::RegionDisks].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_disks::ClientBuilder;
    /// # use client::RegionDisks;
    /// let builder : ClientBuilder = RegionDisks::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionDisks;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionDisks;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionDisks] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionDisks>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionDisks>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionDisks::add_resource_policies][crate::client::RegionDisks::add_resource_policies] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_disks::AddResourcePolicies;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AddResourcePolicies {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AddResourcePolicies(
        RequestBuilder<crate::model::region_disks::AddResourcePoliciesRequest>,
    );

    impl AddResourcePolicies {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionDisks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_disks::AddResourcePoliciesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .add_resource_policies(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `add_resource_policies`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionDisks::add_resource_policies::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [disk][crate::model::region_disks::AddResourcePoliciesRequest::disk].
        pub fn set_disk<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.disk = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_disks::AddResourcePoliciesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_disks::AddResourcePoliciesRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_disks::AddResourcePoliciesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_disks::AddResourcePoliciesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_disks::AddResourcePoliciesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionDisksAddResourcePoliciesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_disks::AddResourcePoliciesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionDisksAddResourcePoliciesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AddResourcePolicies {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionDisks::bulk_insert][crate::client::RegionDisks::bulk_insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_disks::BulkInsert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> BulkInsert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct BulkInsert(RequestBuilder<crate::model::region_disks::BulkInsertRequest>);

    impl BulkInsert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionDisks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_disks::BulkInsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .bulk_insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `bulk_insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionDisks::bulk_insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_disks::BulkInsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_disks::BulkInsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_disks::BulkInsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_disks::BulkInsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_disks::BulkInsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::BulkInsertDiskResource>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_disks::BulkInsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::BulkInsertDiskResource>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for BulkInsert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionDisks::create_snapshot][crate::client::RegionDisks::create_snapshot] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_disks::CreateSnapshot;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CreateSnapshot {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CreateSnapshot(RequestBuilder<crate::model::region_disks::CreateSnapshotRequest>);

    impl CreateSnapshot {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionDisks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_disks::CreateSnapshotRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .create_snapshot(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `create_snapshot`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionDisks::create_snapshot::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [disk][crate::model::region_disks::CreateSnapshotRequest::disk].
        pub fn set_disk<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.disk = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_disks::CreateSnapshotRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_disks::CreateSnapshotRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_disks::CreateSnapshotRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_disks::CreateSnapshotRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_disks::CreateSnapshotRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Snapshot>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_disks::CreateSnapshotRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Snapshot>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CreateSnapshot {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionDisks::delete][crate::client::RegionDisks::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_disks::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::region_disks::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionDisks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_disks::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionDisks::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [disk][crate::model::region_disks::DeleteRequest::disk].
        pub fn set_disk<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.disk = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_disks::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_disks::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_disks::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_disks::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionDisks::get][crate::client::RegionDisks::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_disks::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_disks::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionDisks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_disks::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Disk> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [disk][crate::model::region_disks::GetRequest::disk].
        pub fn set_disk<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.disk = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_disks::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_disks::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionDisks::get_iam_policy][crate::client::RegionDisks::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_disks::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(RequestBuilder<crate::model::region_disks::GetIamPolicyRequest>);

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionDisks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_disks::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::region_disks::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::region_disks::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_disks::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_disks::GetIamPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_disks::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionDisks::insert][crate::client::RegionDisks::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_disks::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::region_disks::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionDisks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_disks::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionDisks::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_disks::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_disks::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_disks::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_disks::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [source_image][crate::model::region_disks::InsertRequest::source_image].
        pub fn set_source_image<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.source_image = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [source_image][crate::model::region_disks::InsertRequest::source_image].
        pub fn set_or_clear_source_image<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.source_image = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_disks::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Disk>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_disks::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Disk>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionDisks::list][crate::client::RegionDisks::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_disks::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_disks::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionDisks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_disks::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::DiskList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::DiskList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::DiskList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_disks::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_disks::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_disks::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_disks::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_disks::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_disks::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_disks::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_disks::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_disks::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_disks::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_disks::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_disks::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionDisks::remove_resource_policies][crate::client::RegionDisks::remove_resource_policies] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_disks::RemoveResourcePolicies;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> RemoveResourcePolicies {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct RemoveResourcePolicies(
        RequestBuilder<crate::model::region_disks::RemoveResourcePoliciesRequest>,
    );

    impl RemoveResourcePolicies {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionDisks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_disks::RemoveResourcePoliciesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .remove_resource_policies(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `remove_resource_policies`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionDisks::remove_resource_policies::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [disk][crate::model::region_disks::RemoveResourcePoliciesRequest::disk].
        pub fn set_disk<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.disk = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_disks::RemoveResourcePoliciesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_disks::RemoveResourcePoliciesRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_disks::RemoveResourcePoliciesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_disks::RemoveResourcePoliciesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_disks::RemoveResourcePoliciesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionDisksRemoveResourcePoliciesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_disks::RemoveResourcePoliciesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionDisksRemoveResourcePoliciesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for RemoveResourcePolicies {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionDisks::resize][crate::client::RegionDisks::resize] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_disks::Resize;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Resize {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Resize(RequestBuilder<crate::model::region_disks::ResizeRequest>);

    impl Resize {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionDisks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_disks::ResizeRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .resize(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `resize`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionDisks::resize::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [disk][crate::model::region_disks::ResizeRequest::disk].
        pub fn set_disk<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.disk = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_disks::ResizeRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_disks::ResizeRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_disks::ResizeRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_disks::ResizeRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_disks::ResizeRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionDisksResizeRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_disks::ResizeRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionDisksResizeRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Resize {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionDisks::set_iam_policy][crate::client::RegionDisks::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_disks::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(RequestBuilder<crate::model::region_disks::SetIamPolicyRequest>);

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionDisks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_disks::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_disks::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_disks::SetIamPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_disks::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_disks::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_disks::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionDisks::set_labels][crate::client::RegionDisks::set_labels] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_disks::SetLabels;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetLabels {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetLabels(RequestBuilder<crate::model::region_disks::SetLabelsRequest>);

    impl SetLabels {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionDisks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_disks::SetLabelsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_labels(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_labels`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionDisks::set_labels::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_disks::SetLabelsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_disks::SetLabelsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_disks::SetLabelsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_disks::SetLabelsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [resource][crate::model::region_disks::SetLabelsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_disks::SetLabelsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetLabelsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_disks::SetLabelsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetLabelsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetLabels {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionDisks::start_async_replication][crate::client::RegionDisks::start_async_replication] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_disks::StartAsyncReplication;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> StartAsyncReplication {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct StartAsyncReplication(
        RequestBuilder<crate::model::region_disks::StartAsyncReplicationRequest>,
    );

    impl StartAsyncReplication {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionDisks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_disks::StartAsyncReplicationRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .start_async_replication(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `start_async_replication`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionDisks::start_async_replication::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [disk][crate::model::region_disks::StartAsyncReplicationRequest::disk].
        pub fn set_disk<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.disk = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_disks::StartAsyncReplicationRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_disks::StartAsyncReplicationRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_disks::StartAsyncReplicationRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_disks::StartAsyncReplicationRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_disks::StartAsyncReplicationRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionDisksStartAsyncReplicationRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_disks::StartAsyncReplicationRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionDisksStartAsyncReplicationRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for StartAsyncReplication {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionDisks::stop_async_replication][crate::client::RegionDisks::stop_async_replication] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_disks::StopAsyncReplication;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> StopAsyncReplication {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct StopAsyncReplication(
        RequestBuilder<crate::model::region_disks::StopAsyncReplicationRequest>,
    );

    impl StopAsyncReplication {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionDisks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_disks::StopAsyncReplicationRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .stop_async_replication(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `stop_async_replication`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionDisks::stop_async_replication::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [disk][crate::model::region_disks::StopAsyncReplicationRequest::disk].
        pub fn set_disk<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.disk = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_disks::StopAsyncReplicationRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_disks::StopAsyncReplicationRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_disks::StopAsyncReplicationRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_disks::StopAsyncReplicationRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for StopAsyncReplication {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionDisks::stop_group_async_replication][crate::client::RegionDisks::stop_group_async_replication] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_disks::StopGroupAsyncReplication;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> StopGroupAsyncReplication {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct StopGroupAsyncReplication(
        RequestBuilder<crate::model::region_disks::StopGroupAsyncReplicationRequest>,
    );

    impl StopGroupAsyncReplication {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionDisks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_disks::StopGroupAsyncReplicationRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .stop_group_async_replication(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `stop_group_async_replication`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionDisks::stop_group_async_replication::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_disks::StopGroupAsyncReplicationRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_disks::StopGroupAsyncReplicationRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_disks::StopGroupAsyncReplicationRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_disks::StopGroupAsyncReplicationRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_disks::StopGroupAsyncReplicationRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::DisksStopGroupAsyncReplicationResource>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_disks::StopGroupAsyncReplicationRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::DisksStopGroupAsyncReplicationResource>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for StopGroupAsyncReplication {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionDisks::test_iam_permissions][crate::client::RegionDisks::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_disks::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::region_disks::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionDisks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_disks::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_disks::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_disks::TestIamPermissionsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_disks::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_disks::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_disks::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionDisks::update][crate::client::RegionDisks::update] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_disks::Update;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Update {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Update(RequestBuilder<crate::model::region_disks::UpdateRequest>);

    impl Update {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionDisks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_disks::UpdateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionDisks::update::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [disk][crate::model::region_disks::UpdateRequest::disk].
        pub fn set_disk<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.disk = v.into();
            self
        }

        /// Sets the value of [paths][crate::model::region_disks::UpdateRequest::paths].
        pub fn set_paths<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.paths = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [paths][crate::model::region_disks::UpdateRequest::paths].
        pub fn set_or_clear_paths<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.paths = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_disks::UpdateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_disks::UpdateRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_disks::UpdateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_disks::UpdateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::region_disks::UpdateRequest::update_mask].
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::region_disks::UpdateRequest::update_mask].
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_disks::UpdateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Disk>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_disks::UpdateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Disk>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Update {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionDisks::update_kms_key][crate::client::RegionDisks::update_kms_key] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_disks::UpdateKmsKey;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateKmsKey {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateKmsKey(RequestBuilder<crate::model::region_disks::UpdateKmsKeyRequest>);

    impl UpdateKmsKey {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionDisks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_disks::UpdateKmsKeyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update_kms_key(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update_kms_key`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionDisks::update_kms_key::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [disk][crate::model::region_disks::UpdateKmsKeyRequest::disk].
        pub fn set_disk<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.disk = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_disks::UpdateKmsKeyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_disks::UpdateKmsKeyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_disks::UpdateKmsKeyRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_disks::UpdateKmsKeyRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_disks::UpdateKmsKeyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionDiskUpdateKmsKeyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_disks::UpdateKmsKeyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionDiskUpdateKmsKeyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateKmsKey {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionDisks::get_operation][crate::client::RegionDisks::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_disks::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionDisks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionHealthAggregationPolicies][crate::client::RegionHealthAggregationPolicies].
#[cfg(feature = "region-health-aggregation-policies")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-health-aggregation-policies")))]
pub mod region_health_aggregation_policies {
    use crate::Result;

    /// A builder for [RegionHealthAggregationPolicies][crate::client::RegionHealthAggregationPolicies].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_health_aggregation_policies::ClientBuilder;
    /// # use client::RegionHealthAggregationPolicies;
    /// let builder : ClientBuilder = RegionHealthAggregationPolicies::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionHealthAggregationPolicies;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionHealthAggregationPolicies;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionHealthAggregationPolicies] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthAggregationPolicies>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthAggregationPolicies>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionHealthAggregationPolicies::aggregated_list][crate::client::RegionHealthAggregationPolicies::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_aggregation_policies::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::region_health_aggregation_policies::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthAggregationPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_health_aggregation_policies::AggregatedListRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::HealthAggregationPolicyAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::HealthAggregationPolicyAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::HealthAggregationPolicyAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_health_aggregation_policies::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_health_aggregation_policies::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::region_health_aggregation_policies::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::region_health_aggregation_policies::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_health_aggregation_policies::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_health_aggregation_policies::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_health_aggregation_policies::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_health_aggregation_policies::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_health_aggregation_policies::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_health_aggregation_policies::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_health_aggregation_policies::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_health_aggregation_policies::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_health_aggregation_policies::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::region_health_aggregation_policies::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::region_health_aggregation_policies::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthAggregationPolicies::delete][crate::client::RegionHealthAggregationPolicies::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_aggregation_policies::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(
        RequestBuilder<crate::model::region_health_aggregation_policies::DeleteRequest>,
    );

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthAggregationPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_health_aggregation_policies::DeleteRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionHealthAggregationPolicies::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [health_aggregation_policy][crate::model::region_health_aggregation_policies::DeleteRequest::health_aggregation_policy].
        pub fn set_health_aggregation_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.health_aggregation_policy = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_health_aggregation_policies::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_aggregation_policies::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_health_aggregation_policies::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_health_aggregation_policies::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthAggregationPolicies::get][crate::client::RegionHealthAggregationPolicies::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_aggregation_policies::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_health_aggregation_policies::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthAggregationPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_health_aggregation_policies::GetRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::HealthAggregationPolicy> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [health_aggregation_policy][crate::model::region_health_aggregation_policies::GetRequest::health_aggregation_policy].
        pub fn set_health_aggregation_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.health_aggregation_policy = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_health_aggregation_policies::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_aggregation_policies::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthAggregationPolicies::insert][crate::client::RegionHealthAggregationPolicies::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_aggregation_policies::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(
        RequestBuilder<crate::model::region_health_aggregation_policies::InsertRequest>,
    );

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthAggregationPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_health_aggregation_policies::InsertRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionHealthAggregationPolicies::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_health_aggregation_policies::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_aggregation_policies::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_health_aggregation_policies::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_health_aggregation_policies::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_health_aggregation_policies::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::HealthAggregationPolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_health_aggregation_policies::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::HealthAggregationPolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthAggregationPolicies::list][crate::client::RegionHealthAggregationPolicies::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_aggregation_policies::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_health_aggregation_policies::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthAggregationPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_health_aggregation_policies::ListRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::HealthAggregationPolicyList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::HealthAggregationPolicyList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::HealthAggregationPolicyList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_health_aggregation_policies::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_health_aggregation_policies::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_health_aggregation_policies::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_health_aggregation_policies::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_health_aggregation_policies::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_health_aggregation_policies::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_health_aggregation_policies::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_health_aggregation_policies::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_health_aggregation_policies::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_aggregation_policies::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_health_aggregation_policies::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_health_aggregation_policies::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthAggregationPolicies::patch][crate::client::RegionHealthAggregationPolicies::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_aggregation_policies::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(
        RequestBuilder<crate::model::region_health_aggregation_policies::PatchRequest>,
    );

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthAggregationPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_health_aggregation_policies::PatchRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionHealthAggregationPolicies::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [health_aggregation_policy][crate::model::region_health_aggregation_policies::PatchRequest::health_aggregation_policy].
        pub fn set_health_aggregation_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.health_aggregation_policy = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_health_aggregation_policies::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_aggregation_policies::PatchRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_health_aggregation_policies::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_health_aggregation_policies::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_health_aggregation_policies::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::HealthAggregationPolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_health_aggregation_policies::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::HealthAggregationPolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthAggregationPolicies::test_iam_permissions][crate::client::RegionHealthAggregationPolicies::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_aggregation_policies::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::region_health_aggregation_policies::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthAggregationPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_health_aggregation_policies::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_health_aggregation_policies::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_aggregation_policies::TestIamPermissionsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_health_aggregation_policies::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_health_aggregation_policies::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_health_aggregation_policies::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthAggregationPolicies::get_operation][crate::client::RegionHealthAggregationPolicies::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_aggregation_policies::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthAggregationPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionHealthCheckServices][crate::client::RegionHealthCheckServices].
#[cfg(feature = "region-health-check-services")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-health-check-services")))]
pub mod region_health_check_services {
    use crate::Result;

    /// A builder for [RegionHealthCheckServices][crate::client::RegionHealthCheckServices].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_health_check_services::ClientBuilder;
    /// # use client::RegionHealthCheckServices;
    /// let builder : ClientBuilder = RegionHealthCheckServices::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionHealthCheckServices;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionHealthCheckServices;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionHealthCheckServices] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthCheckServices>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthCheckServices>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionHealthCheckServices::aggregated_list][crate::client::RegionHealthCheckServices::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_check_services::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::region_health_check_services::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthCheckServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_health_check_services::AggregatedListRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::HealthCheckServiceAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::HealthCheckServiceAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::HealthCheckServiceAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_health_check_services::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_health_check_services::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::region_health_check_services::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::region_health_check_services::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_health_check_services::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_health_check_services::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_health_check_services::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_health_check_services::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_health_check_services::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_health_check_services::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_health_check_services::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_health_check_services::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_health_check_services::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::region_health_check_services::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::region_health_check_services::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthCheckServices::delete][crate::client::RegionHealthCheckServices::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_check_services::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::region_health_check_services::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthCheckServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_health_check_services::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionHealthCheckServices::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [health_check_service][crate::model::region_health_check_services::DeleteRequest::health_check_service].
        pub fn set_health_check_service<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.health_check_service = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_health_check_services::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_check_services::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_health_check_services::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_health_check_services::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthCheckServices::get][crate::client::RegionHealthCheckServices::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_check_services::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_health_check_services::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthCheckServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_health_check_services::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::HealthCheckService> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [health_check_service][crate::model::region_health_check_services::GetRequest::health_check_service].
        pub fn set_health_check_service<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.health_check_service = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_health_check_services::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_check_services::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthCheckServices::insert][crate::client::RegionHealthCheckServices::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_check_services::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::region_health_check_services::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthCheckServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_health_check_services::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionHealthCheckServices::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_health_check_services::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_check_services::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_health_check_services::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_health_check_services::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_health_check_services::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::HealthCheckService>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_health_check_services::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::HealthCheckService>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthCheckServices::list][crate::client::RegionHealthCheckServices::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_check_services::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_health_check_services::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthCheckServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_health_check_services::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::HealthCheckServicesList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::HealthCheckServicesList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::HealthCheckServicesList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_health_check_services::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_health_check_services::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_health_check_services::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_health_check_services::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_health_check_services::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_health_check_services::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_health_check_services::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_health_check_services::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_health_check_services::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_check_services::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_health_check_services::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_health_check_services::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthCheckServices::patch][crate::client::RegionHealthCheckServices::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_check_services::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::region_health_check_services::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthCheckServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_health_check_services::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionHealthCheckServices::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [health_check_service][crate::model::region_health_check_services::PatchRequest::health_check_service].
        pub fn set_health_check_service<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.health_check_service = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_health_check_services::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_check_services::PatchRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_health_check_services::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_health_check_services::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_health_check_services::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::HealthCheckService>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_health_check_services::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::HealthCheckService>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthCheckServices::test_iam_permissions][crate::client::RegionHealthCheckServices::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_check_services::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::region_health_check_services::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthCheckServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_health_check_services::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_health_check_services::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_check_services::TestIamPermissionsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_health_check_services::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_health_check_services::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_health_check_services::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthCheckServices::get_operation][crate::client::RegionHealthCheckServices::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_check_services::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthCheckServices>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionHealthChecks][crate::client::RegionHealthChecks].
#[cfg(feature = "region-health-checks")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-health-checks")))]
pub mod region_health_checks {
    use crate::Result;

    /// A builder for [RegionHealthChecks][crate::client::RegionHealthChecks].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_health_checks::ClientBuilder;
    /// # use client::RegionHealthChecks;
    /// let builder : ClientBuilder = RegionHealthChecks::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionHealthChecks;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionHealthChecks;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionHealthChecks] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthChecks>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthChecks>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionHealthChecks::delete][crate::client::RegionHealthChecks::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_checks::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::region_health_checks::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_health_checks::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionHealthChecks::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [health_check][crate::model::region_health_checks::DeleteRequest::health_check].
        pub fn set_health_check<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.health_check = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_health_checks::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_checks::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_health_checks::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_health_checks::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthChecks::get][crate::client::RegionHealthChecks::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_checks::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_health_checks::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_health_checks::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::HealthCheck> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [health_check][crate::model::region_health_checks::GetRequest::health_check].
        pub fn set_health_check<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.health_check = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_health_checks::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_checks::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthChecks::insert][crate::client::RegionHealthChecks::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_checks::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::region_health_checks::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_health_checks::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionHealthChecks::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_health_checks::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_checks::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_health_checks::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_health_checks::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_health_checks::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::HealthCheck>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_health_checks::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::HealthCheck>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthChecks::list][crate::client::RegionHealthChecks::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_checks::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_health_checks::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_health_checks::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::HealthCheckList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::HealthCheckList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::HealthCheckList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_health_checks::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_health_checks::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_health_checks::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_health_checks::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_health_checks::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_health_checks::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_health_checks::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_health_checks::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_health_checks::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_checks::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_health_checks::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_health_checks::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthChecks::patch][crate::client::RegionHealthChecks::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_checks::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::region_health_checks::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_health_checks::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionHealthChecks::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [health_check][crate::model::region_health_checks::PatchRequest::health_check].
        pub fn set_health_check<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.health_check = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_health_checks::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_checks::PatchRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_health_checks::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_health_checks::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_health_checks::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::HealthCheck>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_health_checks::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::HealthCheck>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthChecks::test_iam_permissions][crate::client::RegionHealthChecks::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_checks::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::region_health_checks::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_health_checks::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_health_checks::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_checks::TestIamPermissionsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_health_checks::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_health_checks::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_health_checks::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthChecks::update][crate::client::RegionHealthChecks::update] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_checks::Update;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Update {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Update(RequestBuilder<crate::model::region_health_checks::UpdateRequest>);

    impl Update {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_health_checks::UpdateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionHealthChecks::update::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [health_check][crate::model::region_health_checks::UpdateRequest::health_check].
        pub fn set_health_check<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.health_check = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_health_checks::UpdateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_checks::UpdateRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_health_checks::UpdateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_health_checks::UpdateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_health_checks::UpdateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::HealthCheck>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_health_checks::UpdateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::HealthCheck>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Update {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthChecks::get_operation][crate::client::RegionHealthChecks::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_checks::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthChecks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionHealthSources][crate::client::RegionHealthSources].
#[cfg(feature = "region-health-sources")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-health-sources")))]
pub mod region_health_sources {
    use crate::Result;

    /// A builder for [RegionHealthSources][crate::client::RegionHealthSources].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_health_sources::ClientBuilder;
    /// # use client::RegionHealthSources;
    /// let builder : ClientBuilder = RegionHealthSources::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionHealthSources;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionHealthSources;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionHealthSources] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthSources>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthSources>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionHealthSources::aggregated_list][crate::client::RegionHealthSources::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_sources::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::region_health_sources::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthSources>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_health_sources::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::HealthSourceAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::HealthSourceAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::HealthSourceAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_health_sources::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_health_sources::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::region_health_sources::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::region_health_sources::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_health_sources::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_health_sources::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_health_sources::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_health_sources::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_health_sources::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_health_sources::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_health_sources::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_health_sources::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_health_sources::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::region_health_sources::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::region_health_sources::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthSources::delete][crate::client::RegionHealthSources::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_sources::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::region_health_sources::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthSources>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_health_sources::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionHealthSources::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [health_source][crate::model::region_health_sources::DeleteRequest::health_source].
        pub fn set_health_source<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.health_source = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_health_sources::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_sources::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_health_sources::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_health_sources::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthSources::get][crate::client::RegionHealthSources::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_sources::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_health_sources::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthSources>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_health_sources::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::HealthSource> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [health_source][crate::model::region_health_sources::GetRequest::health_source].
        pub fn set_health_source<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.health_source = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_health_sources::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_sources::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthSources::get_health][crate::client::RegionHealthSources::get_health] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_sources::GetHealth;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetHealth {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetHealth(RequestBuilder<crate::model::region_health_sources::GetHealthRequest>);

    impl GetHealth {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthSources>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_health_sources::GetHealthRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::HealthSourceHealth> {
            (*self.0.stub)
                .get_health(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [health_source][crate::model::region_health_sources::GetHealthRequest::health_source].
        pub fn set_health_source<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.health_source = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_health_sources::GetHealthRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_sources::GetHealthRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetHealth {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthSources::insert][crate::client::RegionHealthSources::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_sources::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::region_health_sources::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthSources>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_health_sources::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionHealthSources::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_health_sources::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_sources::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_health_sources::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_health_sources::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_health_sources::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::HealthSource>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_health_sources::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::HealthSource>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthSources::list][crate::client::RegionHealthSources::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_sources::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_health_sources::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthSources>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_health_sources::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::HealthSourceList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::HealthSourceList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::HealthSourceList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_health_sources::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_health_sources::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_health_sources::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_health_sources::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_health_sources::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_health_sources::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_health_sources::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_health_sources::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_health_sources::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_sources::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_health_sources::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_health_sources::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthSources::patch][crate::client::RegionHealthSources::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_sources::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::region_health_sources::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthSources>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_health_sources::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionHealthSources::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [health_source][crate::model::region_health_sources::PatchRequest::health_source].
        pub fn set_health_source<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.health_source = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_health_sources::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_sources::PatchRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_health_sources::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_health_sources::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_health_sources::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::HealthSource>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_health_sources::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::HealthSource>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthSources::test_iam_permissions][crate::client::RegionHealthSources::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_sources::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::region_health_sources::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthSources>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_health_sources::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_health_sources::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_health_sources::TestIamPermissionsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_health_sources::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_health_sources::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_health_sources::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionHealthSources::get_operation][crate::client::RegionHealthSources::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_health_sources::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionHealthSources>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionInstanceGroupManagerResizeRequests][crate::client::RegionInstanceGroupManagerResizeRequests].
#[cfg(feature = "region-instance-group-manager-resize-requests")]
#[cfg_attr(
    docsrs,
    doc(cfg(feature = "region-instance-group-manager-resize-requests"))
)]
pub mod region_instance_group_manager_resize_requests {
    use crate::Result;

    /// A builder for [RegionInstanceGroupManagerResizeRequests][crate::client::RegionInstanceGroupManagerResizeRequests].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_instance_group_manager_resize_requests::ClientBuilder;
    /// # use client::RegionInstanceGroupManagerResizeRequests;
    /// let builder : ClientBuilder = RegionInstanceGroupManagerResizeRequests::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionInstanceGroupManagerResizeRequests;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionInstanceGroupManagerResizeRequests;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionInstanceGroupManagerResizeRequests] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<
            dyn super::super::stub::dynamic::RegionInstanceGroupManagerResizeRequests,
        >,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<
                dyn super::super::stub::dynamic::RegionInstanceGroupManagerResizeRequests,
            >,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionInstanceGroupManagerResizeRequests::cancel][crate::client::RegionInstanceGroupManagerResizeRequests::cancel] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_manager_resize_requests::Cancel;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Cancel {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Cancel(
        RequestBuilder<crate::model::region_instance_group_manager_resize_requests::CancelRequest>,
    );

    impl Cancel {
        pub(crate) fn new(
            stub: std::sync::Arc<
                dyn super::super::stub::dynamic::RegionInstanceGroupManagerResizeRequests,
            >,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_group_manager_resize_requests::CancelRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .cancel(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `cancel`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionInstanceGroupManagerResizeRequests::cancel::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_manager_resize_requests::CancelRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_manager_resize_requests::CancelRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_manager_resize_requests::CancelRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instance_group_manager_resize_requests::CancelRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instance_group_manager_resize_requests::CancelRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [resize_request][crate::model::region_instance_group_manager_resize_requests::CancelRequest::resize_request].
        pub fn set_resize_request<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resize_request = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Cancel {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagerResizeRequests::delete][crate::client::RegionInstanceGroupManagerResizeRequests::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_manager_resize_requests::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(
        RequestBuilder<crate::model::region_instance_group_manager_resize_requests::DeleteRequest>,
    );

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<
                dyn super::super::stub::dynamic::RegionInstanceGroupManagerResizeRequests,
            >,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_group_manager_resize_requests::DeleteRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionInstanceGroupManagerResizeRequests::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_manager_resize_requests::DeleteRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_manager_resize_requests::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_manager_resize_requests::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instance_group_manager_resize_requests::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instance_group_manager_resize_requests::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [resize_request][crate::model::region_instance_group_manager_resize_requests::DeleteRequest::resize_request].
        pub fn set_resize_request<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resize_request = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagerResizeRequests::get][crate::client::RegionInstanceGroupManagerResizeRequests::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_manager_resize_requests::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(
        RequestBuilder<crate::model::region_instance_group_manager_resize_requests::GetRequest>,
    );

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<
                dyn super::super::stub::dynamic::RegionInstanceGroupManagerResizeRequests,
            >,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_group_manager_resize_requests::GetRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstanceGroupManagerResizeRequest> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_manager_resize_requests::GetRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_manager_resize_requests::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_manager_resize_requests::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resize_request][crate::model::region_instance_group_manager_resize_requests::GetRequest::resize_request].
        pub fn set_resize_request<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resize_request = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagerResizeRequests::insert][crate::client::RegionInstanceGroupManagerResizeRequests::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_manager_resize_requests::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(
        RequestBuilder<crate::model::region_instance_group_manager_resize_requests::InsertRequest>,
    );

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<
                dyn super::super::stub::dynamic::RegionInstanceGroupManagerResizeRequests,
            >,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_group_manager_resize_requests::InsertRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionInstanceGroupManagerResizeRequests::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_manager_resize_requests::InsertRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_manager_resize_requests::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_manager_resize_requests::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instance_group_manager_resize_requests::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instance_group_manager_resize_requests::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_instance_group_manager_resize_requests::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagerResizeRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instance_group_manager_resize_requests::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManagerResizeRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagerResizeRequests::list][crate::client::RegionInstanceGroupManagerResizeRequests::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_manager_resize_requests::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(
        RequestBuilder<crate::model::region_instance_group_manager_resize_requests::ListRequest>,
    );

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<
                dyn super::super::stub::dynamic::RegionInstanceGroupManagerResizeRequests,
            >,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_group_manager_resize_requests::ListRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(
            self,
        ) -> Result<crate::model::RegionInstanceGroupManagerResizeRequestsListResponse> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::RegionInstanceGroupManagerResizeRequestsListResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::RegionInstanceGroupManagerResizeRequestsListResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_instance_group_manager_resize_requests::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_instance_group_manager_resize_requests::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_manager_resize_requests::ListRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [max_results][crate::model::region_instance_group_manager_resize_requests::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_instance_group_manager_resize_requests::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_instance_group_manager_resize_requests::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_instance_group_manager_resize_requests::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_instance_group_manager_resize_requests::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_instance_group_manager_resize_requests::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_manager_resize_requests::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_manager_resize_requests::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_instance_group_manager_resize_requests::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_instance_group_manager_resize_requests::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagerResizeRequests::get_operation][crate::client::RegionInstanceGroupManagerResizeRequests::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_manager_resize_requests::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<
                dyn super::super::stub::dynamic::RegionInstanceGroupManagerResizeRequests,
            >,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionInstanceGroupManagers][crate::client::RegionInstanceGroupManagers].
#[cfg(feature = "region-instance-group-managers")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-instance-group-managers")))]
pub mod region_instance_group_managers {
    use crate::Result;

    /// A builder for [RegionInstanceGroupManagers][crate::client::RegionInstanceGroupManagers].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_instance_group_managers::ClientBuilder;
    /// # use client::RegionInstanceGroupManagers;
    /// let builder : ClientBuilder = RegionInstanceGroupManagers::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionInstanceGroupManagers;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionInstanceGroupManagers;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionInstanceGroupManagers] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionInstanceGroupManagers::abandon_instances][crate::client::RegionInstanceGroupManagers::abandon_instances] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_managers::AbandonInstances;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AbandonInstances {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AbandonInstances(
        RequestBuilder<crate::model::region_instance_group_managers::AbandonInstancesRequest>,
    );

    impl AbandonInstances {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_group_managers::AbandonInstancesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .abandon_instances(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `abandon_instances`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionInstanceGroupManagers::abandon_instances::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_managers::AbandonInstancesRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_managers::AbandonInstancesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_managers::AbandonInstancesRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instance_group_managers::AbandonInstancesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instance_group_managers::AbandonInstancesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_instance_group_managers::AbandonInstancesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagersAbandonInstancesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instance_group_managers::AbandonInstancesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagersAbandonInstancesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AbandonInstances {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagers::apply_updates_to_instances][crate::client::RegionInstanceGroupManagers::apply_updates_to_instances] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_managers::ApplyUpdatesToInstances;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ApplyUpdatesToInstances {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ApplyUpdatesToInstances(
        RequestBuilder<
            crate::model::region_instance_group_managers::ApplyUpdatesToInstancesRequest,
        >,
    );

    impl ApplyUpdatesToInstances {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_group_managers::ApplyUpdatesToInstancesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .apply_updates_to_instances(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `apply_updates_to_instances`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionInstanceGroupManagers::apply_updates_to_instances::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_managers::ApplyUpdatesToInstancesRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_managers::ApplyUpdatesToInstancesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_managers::ApplyUpdatesToInstancesRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_instance_group_managers::ApplyUpdatesToInstancesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagersApplyUpdatesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instance_group_managers::ApplyUpdatesToInstancesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagersApplyUpdatesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ApplyUpdatesToInstances {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagers::create_instances][crate::client::RegionInstanceGroupManagers::create_instances] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_managers::CreateInstances;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CreateInstances {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CreateInstances(
        RequestBuilder<crate::model::region_instance_group_managers::CreateInstancesRequest>,
    );

    impl CreateInstances {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_group_managers::CreateInstancesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .create_instances(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `create_instances`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionInstanceGroupManagers::create_instances::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_managers::CreateInstancesRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_managers::CreateInstancesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_managers::CreateInstancesRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instance_group_managers::CreateInstancesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instance_group_managers::CreateInstancesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_instance_group_managers::CreateInstancesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagersCreateInstancesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instance_group_managers::CreateInstancesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagersCreateInstancesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CreateInstances {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagers::delete][crate::client::RegionInstanceGroupManagers::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_managers::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::region_instance_group_managers::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_group_managers::DeleteRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionInstanceGroupManagers::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_managers::DeleteRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [no_graceful_shutdown][crate::model::region_instance_group_managers::DeleteRequest::no_graceful_shutdown].
        pub fn set_no_graceful_shutdown<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.no_graceful_shutdown = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [no_graceful_shutdown][crate::model::region_instance_group_managers::DeleteRequest::no_graceful_shutdown].
        pub fn set_or_clear_no_graceful_shutdown<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.no_graceful_shutdown = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_managers::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_managers::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instance_group_managers::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instance_group_managers::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagers::delete_instances][crate::client::RegionInstanceGroupManagers::delete_instances] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_managers::DeleteInstances;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeleteInstances {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeleteInstances(
        RequestBuilder<crate::model::region_instance_group_managers::DeleteInstancesRequest>,
    );

    impl DeleteInstances {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_group_managers::DeleteInstancesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete_instances(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete_instances`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionInstanceGroupManagers::delete_instances::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_managers::DeleteInstancesRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [no_graceful_shutdown][crate::model::region_instance_group_managers::DeleteInstancesRequest::no_graceful_shutdown].
        pub fn set_no_graceful_shutdown<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.no_graceful_shutdown = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [no_graceful_shutdown][crate::model::region_instance_group_managers::DeleteInstancesRequest::no_graceful_shutdown].
        pub fn set_or_clear_no_graceful_shutdown<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.no_graceful_shutdown = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_managers::DeleteInstancesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_managers::DeleteInstancesRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instance_group_managers::DeleteInstancesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instance_group_managers::DeleteInstancesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_instance_group_managers::DeleteInstancesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagersDeleteInstancesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instance_group_managers::DeleteInstancesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagersDeleteInstancesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeleteInstances {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagers::delete_per_instance_configs][crate::client::RegionInstanceGroupManagers::delete_per_instance_configs] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_managers::DeletePerInstanceConfigs;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeletePerInstanceConfigs {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeletePerInstanceConfigs(
        RequestBuilder<
            crate::model::region_instance_group_managers::DeletePerInstanceConfigsRequest,
        >,
    );

    impl DeletePerInstanceConfigs {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_group_managers::DeletePerInstanceConfigsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete_per_instance_configs(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete_per_instance_configs`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionInstanceGroupManagers::delete_per_instance_configs::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_managers::DeletePerInstanceConfigsRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_managers::DeletePerInstanceConfigsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_managers::DeletePerInstanceConfigsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_instance_group_managers::DeletePerInstanceConfigsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagerDeleteInstanceConfigReq>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instance_group_managers::DeletePerInstanceConfigsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagerDeleteInstanceConfigReq>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeletePerInstanceConfigs {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagers::get][crate::client::RegionInstanceGroupManagers::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_managers::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_instance_group_managers::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_instance_group_managers::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstanceGroupManager> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_managers::GetRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_managers::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_managers::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagers::insert][crate::client::RegionInstanceGroupManagers::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_managers::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::region_instance_group_managers::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_group_managers::InsertRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionInstanceGroupManagers::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_instance_group_managers::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_managers::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instance_group_managers::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instance_group_managers::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_instance_group_managers::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManager>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instance_group_managers::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManager>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagers::list][crate::client::RegionInstanceGroupManagers::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_managers::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_instance_group_managers::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_instance_group_managers::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::RegionInstanceGroupManagerList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::RegionInstanceGroupManagerList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::RegionInstanceGroupManagerList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_instance_group_managers::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_instance_group_managers::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_instance_group_managers::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_instance_group_managers::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_instance_group_managers::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_instance_group_managers::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_instance_group_managers::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_instance_group_managers::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_managers::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_managers::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_instance_group_managers::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_instance_group_managers::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagers::list_errors][crate::client::RegionInstanceGroupManagers::list_errors] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_managers::ListErrors;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListErrors {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListErrors(
        RequestBuilder<crate::model::region_instance_group_managers::ListErrorsRequest>,
    );

    impl ListErrors {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_group_managers::ListErrorsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(
            self,
        ) -> Result<crate::model::RegionInstanceGroupManagersListErrorsResponse> {
            (*self.0.stub)
                .list_errors(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::RegionInstanceGroupManagersListErrorsResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::RegionInstanceGroupManagersListErrorsResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_instance_group_managers::ListErrorsRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_instance_group_managers::ListErrorsRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_managers::ListErrorsRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [max_results][crate::model::region_instance_group_managers::ListErrorsRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_instance_group_managers::ListErrorsRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_instance_group_managers::ListErrorsRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_instance_group_managers::ListErrorsRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_instance_group_managers::ListErrorsRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_instance_group_managers::ListErrorsRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_managers::ListErrorsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_managers::ListErrorsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_instance_group_managers::ListErrorsRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_instance_group_managers::ListErrorsRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListErrors {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagers::list_managed_instances][crate::client::RegionInstanceGroupManagers::list_managed_instances] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_managers::ListManagedInstances;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListManagedInstances {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListManagedInstances(
        RequestBuilder<crate::model::region_instance_group_managers::ListManagedInstancesRequest>,
    );

    impl ListManagedInstances {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_group_managers::ListManagedInstancesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(
            self,
        ) -> Result<crate::model::RegionInstanceGroupManagersListInstancesResponse> {
            (*self.0.stub)
                .list_managed_instances(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::RegionInstanceGroupManagersListInstancesResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::RegionInstanceGroupManagersListInstancesResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_instance_group_managers::ListManagedInstancesRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_instance_group_managers::ListManagedInstancesRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_managers::ListManagedInstancesRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [max_results][crate::model::region_instance_group_managers::ListManagedInstancesRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_instance_group_managers::ListManagedInstancesRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_instance_group_managers::ListManagedInstancesRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_instance_group_managers::ListManagedInstancesRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_instance_group_managers::ListManagedInstancesRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_instance_group_managers::ListManagedInstancesRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_managers::ListManagedInstancesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_managers::ListManagedInstancesRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_instance_group_managers::ListManagedInstancesRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_instance_group_managers::ListManagedInstancesRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListManagedInstances {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagers::list_per_instance_configs][crate::client::RegionInstanceGroupManagers::list_per_instance_configs] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_managers::ListPerInstanceConfigs;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListPerInstanceConfigs {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListPerInstanceConfigs(
        RequestBuilder<crate::model::region_instance_group_managers::ListPerInstanceConfigsRequest>,
    );

    impl ListPerInstanceConfigs {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_group_managers::ListPerInstanceConfigsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(
            self,
        ) -> Result<crate::model::RegionInstanceGroupManagersListInstanceConfigsResp> {
            (*self.0.stub)
                .list_per_instance_configs(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::RegionInstanceGroupManagersListInstanceConfigsResp,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::RegionInstanceGroupManagersListInstanceConfigsResp,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_instance_group_managers::ListPerInstanceConfigsRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_instance_group_managers::ListPerInstanceConfigsRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_managers::ListPerInstanceConfigsRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [max_results][crate::model::region_instance_group_managers::ListPerInstanceConfigsRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_instance_group_managers::ListPerInstanceConfigsRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_instance_group_managers::ListPerInstanceConfigsRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_instance_group_managers::ListPerInstanceConfigsRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_instance_group_managers::ListPerInstanceConfigsRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_instance_group_managers::ListPerInstanceConfigsRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_managers::ListPerInstanceConfigsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_managers::ListPerInstanceConfigsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_instance_group_managers::ListPerInstanceConfigsRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_instance_group_managers::ListPerInstanceConfigsRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListPerInstanceConfigs {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagers::patch][crate::client::RegionInstanceGroupManagers::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_managers::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::region_instance_group_managers::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_instance_group_managers::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionInstanceGroupManagers::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_managers::PatchRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_managers::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_managers::PatchRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instance_group_managers::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instance_group_managers::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_instance_group_managers::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManager>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instance_group_managers::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceGroupManager>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagers::patch_per_instance_configs][crate::client::RegionInstanceGroupManagers::patch_per_instance_configs] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_managers::PatchPerInstanceConfigs;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> PatchPerInstanceConfigs {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct PatchPerInstanceConfigs(
        RequestBuilder<
            crate::model::region_instance_group_managers::PatchPerInstanceConfigsRequest,
        >,
    );

    impl PatchPerInstanceConfigs {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_group_managers::PatchPerInstanceConfigsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch_per_instance_configs(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch_per_instance_configs`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionInstanceGroupManagers::patch_per_instance_configs::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_managers::PatchPerInstanceConfigsRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_managers::PatchPerInstanceConfigsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_managers::PatchPerInstanceConfigsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instance_group_managers::PatchPerInstanceConfigsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instance_group_managers::PatchPerInstanceConfigsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_instance_group_managers::PatchPerInstanceConfigsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagerPatchInstanceConfigReq>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instance_group_managers::PatchPerInstanceConfigsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagerPatchInstanceConfigReq>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for PatchPerInstanceConfigs {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagers::recreate_instances][crate::client::RegionInstanceGroupManagers::recreate_instances] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_managers::RecreateInstances;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> RecreateInstances {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct RecreateInstances(
        RequestBuilder<crate::model::region_instance_group_managers::RecreateInstancesRequest>,
    );

    impl RecreateInstances {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_group_managers::RecreateInstancesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .recreate_instances(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `recreate_instances`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionInstanceGroupManagers::recreate_instances::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_managers::RecreateInstancesRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [no_graceful_shutdown][crate::model::region_instance_group_managers::RecreateInstancesRequest::no_graceful_shutdown].
        pub fn set_no_graceful_shutdown<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.no_graceful_shutdown = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [no_graceful_shutdown][crate::model::region_instance_group_managers::RecreateInstancesRequest::no_graceful_shutdown].
        pub fn set_or_clear_no_graceful_shutdown<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.no_graceful_shutdown = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_managers::RecreateInstancesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_managers::RecreateInstancesRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instance_group_managers::RecreateInstancesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instance_group_managers::RecreateInstancesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_instance_group_managers::RecreateInstancesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagersRecreateRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instance_group_managers::RecreateInstancesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagersRecreateRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for RecreateInstances {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagers::resize][crate::client::RegionInstanceGroupManagers::resize] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_managers::Resize;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Resize {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Resize(RequestBuilder<crate::model::region_instance_group_managers::ResizeRequest>);

    impl Resize {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_group_managers::ResizeRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .resize(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `resize`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionInstanceGroupManagers::resize::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_managers::ResizeRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_managers::ResizeRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_managers::ResizeRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instance_group_managers::ResizeRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instance_group_managers::ResizeRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [size][crate::model::region_instance_group_managers::ResizeRequest::size].
        pub fn set_size<T: Into<i32>>(mut self, v: T) -> Self {
            self.0.request.size = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Resize {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagers::resume_instances][crate::client::RegionInstanceGroupManagers::resume_instances] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_managers::ResumeInstances;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ResumeInstances {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ResumeInstances(
        RequestBuilder<crate::model::region_instance_group_managers::ResumeInstancesRequest>,
    );

    impl ResumeInstances {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_group_managers::ResumeInstancesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .resume_instances(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `resume_instances`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionInstanceGroupManagers::resume_instances::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_managers::ResumeInstancesRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_managers::ResumeInstancesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_managers::ResumeInstancesRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instance_group_managers::ResumeInstancesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instance_group_managers::ResumeInstancesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_instance_group_managers::ResumeInstancesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagersResumeInstancesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instance_group_managers::ResumeInstancesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagersResumeInstancesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ResumeInstances {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagers::set_instance_template][crate::client::RegionInstanceGroupManagers::set_instance_template] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_managers::SetInstanceTemplate;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetInstanceTemplate {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetInstanceTemplate(
        RequestBuilder<crate::model::region_instance_group_managers::SetInstanceTemplateRequest>,
    );

    impl SetInstanceTemplate {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_group_managers::SetInstanceTemplateRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_instance_template(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_instance_template`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionInstanceGroupManagers::set_instance_template::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_managers::SetInstanceTemplateRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_managers::SetInstanceTemplateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_managers::SetInstanceTemplateRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instance_group_managers::SetInstanceTemplateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instance_group_managers::SetInstanceTemplateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_instance_group_managers::SetInstanceTemplateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagersSetTemplateRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instance_group_managers::SetInstanceTemplateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagersSetTemplateRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetInstanceTemplate {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagers::set_target_pools][crate::client::RegionInstanceGroupManagers::set_target_pools] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_managers::SetTargetPools;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetTargetPools {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetTargetPools(
        RequestBuilder<crate::model::region_instance_group_managers::SetTargetPoolsRequest>,
    );

    impl SetTargetPools {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_group_managers::SetTargetPoolsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_target_pools(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_target_pools`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionInstanceGroupManagers::set_target_pools::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_managers::SetTargetPoolsRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_managers::SetTargetPoolsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_managers::SetTargetPoolsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instance_group_managers::SetTargetPoolsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instance_group_managers::SetTargetPoolsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_instance_group_managers::SetTargetPoolsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagersSetTargetPoolsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instance_group_managers::SetTargetPoolsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagersSetTargetPoolsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetTargetPools {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagers::start_instances][crate::client::RegionInstanceGroupManagers::start_instances] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_managers::StartInstances;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> StartInstances {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct StartInstances(
        RequestBuilder<crate::model::region_instance_group_managers::StartInstancesRequest>,
    );

    impl StartInstances {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_group_managers::StartInstancesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .start_instances(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `start_instances`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionInstanceGroupManagers::start_instances::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_managers::StartInstancesRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_managers::StartInstancesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_managers::StartInstancesRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instance_group_managers::StartInstancesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instance_group_managers::StartInstancesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_instance_group_managers::StartInstancesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagersStartInstancesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instance_group_managers::StartInstancesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagersStartInstancesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for StartInstances {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagers::stop_instances][crate::client::RegionInstanceGroupManagers::stop_instances] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_managers::StopInstances;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> StopInstances {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct StopInstances(
        RequestBuilder<crate::model::region_instance_group_managers::StopInstancesRequest>,
    );

    impl StopInstances {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_group_managers::StopInstancesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .stop_instances(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `stop_instances`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionInstanceGroupManagers::stop_instances::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_managers::StopInstancesRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [no_graceful_shutdown][crate::model::region_instance_group_managers::StopInstancesRequest::no_graceful_shutdown].
        pub fn set_no_graceful_shutdown<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.no_graceful_shutdown = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [no_graceful_shutdown][crate::model::region_instance_group_managers::StopInstancesRequest::no_graceful_shutdown].
        pub fn set_or_clear_no_graceful_shutdown<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.no_graceful_shutdown = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_managers::StopInstancesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_managers::StopInstancesRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instance_group_managers::StopInstancesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instance_group_managers::StopInstancesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_instance_group_managers::StopInstancesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagersStopInstancesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instance_group_managers::StopInstancesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagersStopInstancesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for StopInstances {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagers::suspend_instances][crate::client::RegionInstanceGroupManagers::suspend_instances] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_managers::SuspendInstances;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SuspendInstances {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SuspendInstances(
        RequestBuilder<crate::model::region_instance_group_managers::SuspendInstancesRequest>,
    );

    impl SuspendInstances {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_group_managers::SuspendInstancesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .suspend_instances(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `suspend_instances`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionInstanceGroupManagers::suspend_instances::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_managers::SuspendInstancesRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_managers::SuspendInstancesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_managers::SuspendInstancesRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instance_group_managers::SuspendInstancesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instance_group_managers::SuspendInstancesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_instance_group_managers::SuspendInstancesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagersSuspendInstancesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instance_group_managers::SuspendInstancesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagersSuspendInstancesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SuspendInstances {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagers::update_per_instance_configs][crate::client::RegionInstanceGroupManagers::update_per_instance_configs] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_managers::UpdatePerInstanceConfigs;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdatePerInstanceConfigs {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdatePerInstanceConfigs(
        RequestBuilder<
            crate::model::region_instance_group_managers::UpdatePerInstanceConfigsRequest,
        >,
    );

    impl UpdatePerInstanceConfigs {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_group_managers::UpdatePerInstanceConfigsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update_per_instance_configs(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update_per_instance_configs`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionInstanceGroupManagers::update_per_instance_configs::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group_manager][crate::model::region_instance_group_managers::UpdatePerInstanceConfigsRequest::instance_group_manager].
        pub fn set_instance_group_manager<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group_manager = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instance_group_managers::UpdatePerInstanceConfigsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_group_managers::UpdatePerInstanceConfigsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instance_group_managers::UpdatePerInstanceConfigsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instance_group_managers::UpdatePerInstanceConfigsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_instance_group_managers::UpdatePerInstanceConfigsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagerUpdateInstanceConfigReq>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instance_group_managers::UpdatePerInstanceConfigsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupManagerUpdateInstanceConfigReq>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdatePerInstanceConfigs {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroupManagers::get_operation][crate::client::RegionInstanceGroupManagers::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_group_managers::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroupManagers>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionInstanceGroups][crate::client::RegionInstanceGroups].
#[cfg(feature = "region-instance-groups")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-instance-groups")))]
pub mod region_instance_groups {
    use crate::Result;

    /// A builder for [RegionInstanceGroups][crate::client::RegionInstanceGroups].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_instance_groups::ClientBuilder;
    /// # use client::RegionInstanceGroups;
    /// let builder : ClientBuilder = RegionInstanceGroups::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionInstanceGroups;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionInstanceGroups;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionInstanceGroups] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroups>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroups>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionInstanceGroups::get][crate::client::RegionInstanceGroups::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_groups::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_instance_groups::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_instance_groups::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstanceGroup> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [instance_group][crate::model::region_instance_groups::GetRequest::instance_group].
        pub fn set_instance_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instance_groups::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_groups::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroups::list][crate::client::RegionInstanceGroups::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_groups::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_instance_groups::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_instance_groups::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::RegionInstanceGroupList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::RegionInstanceGroupList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::RegionInstanceGroupList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_instance_groups::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_instance_groups::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_instance_groups::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_instance_groups::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_instance_groups::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_instance_groups::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_instance_groups::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_instance_groups::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_instance_groups::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_groups::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_instance_groups::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_instance_groups::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroups::list_instances][crate::client::RegionInstanceGroups::list_instances] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_groups::ListInstances;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListInstances {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListInstances(
        RequestBuilder<crate::model::region_instance_groups::ListInstancesRequest>,
    );

    impl ListInstances {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_instance_groups::ListInstancesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::RegionInstanceGroupsListInstances> {
            (*self.0.stub)
                .list_instances(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::RegionInstanceGroupsListInstances,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::RegionInstanceGroupsListInstances,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_instance_groups::ListInstancesRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_instance_groups::ListInstancesRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [instance_group][crate::model::region_instance_groups::ListInstancesRequest::instance_group].
        pub fn set_instance_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group = v.into();
            self
        }

        /// Sets the value of [max_results][crate::model::region_instance_groups::ListInstancesRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_instance_groups::ListInstancesRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_instance_groups::ListInstancesRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_instance_groups::ListInstancesRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_instance_groups::ListInstancesRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_instance_groups::ListInstancesRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_instance_groups::ListInstancesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_groups::ListInstancesRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_instance_groups::ListInstancesRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_instance_groups::ListInstancesRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_instance_groups::ListInstancesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupsListInstancesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instance_groups::ListInstancesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupsListInstancesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListInstances {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroups::set_named_ports][crate::client::RegionInstanceGroups::set_named_ports] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_groups::SetNamedPorts;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetNamedPorts {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetNamedPorts(
        RequestBuilder<crate::model::region_instance_groups::SetNamedPortsRequest>,
    );

    impl SetNamedPorts {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_instance_groups::SetNamedPortsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_named_ports(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_named_ports`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionInstanceGroups::set_named_ports::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_group][crate::model::region_instance_groups::SetNamedPortsRequest::instance_group].
        pub fn set_instance_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instance_groups::SetNamedPortsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_groups::SetNamedPortsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instance_groups::SetNamedPortsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instance_groups::SetNamedPortsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_instance_groups::SetNamedPortsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupsSetNamedPortsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instance_groups::SetNamedPortsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionInstanceGroupsSetNamedPortsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetNamedPorts {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroups::test_iam_permissions][crate::client::RegionInstanceGroups::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_groups::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::region_instance_groups::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instance_groups::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_instance_groups::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_groups::TestIamPermissionsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_instance_groups::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_instance_groups::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instance_groups::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceGroups::get_operation][crate::client::RegionInstanceGroups::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_groups::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionInstanceTemplates][crate::client::RegionInstanceTemplates].
#[cfg(feature = "region-instance-templates")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-instance-templates")))]
pub mod region_instance_templates {
    use crate::Result;

    /// A builder for [RegionInstanceTemplates][crate::client::RegionInstanceTemplates].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_instance_templates::ClientBuilder;
    /// # use client::RegionInstanceTemplates;
    /// let builder : ClientBuilder = RegionInstanceTemplates::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionInstanceTemplates;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionInstanceTemplates;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionInstanceTemplates] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceTemplates>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceTemplates>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionInstanceTemplates::delete][crate::client::RegionInstanceTemplates::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_templates::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::region_instance_templates::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceTemplates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_instance_templates::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionInstanceTemplates::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instance_template][crate::model::region_instance_templates::DeleteRequest::instance_template].
        pub fn set_instance_template<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_template = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instance_templates::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_templates::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instance_templates::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instance_templates::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceTemplates::get][crate::client::RegionInstanceTemplates::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_templates::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_instance_templates::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceTemplates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_instance_templates::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstanceTemplate> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [instance_template][crate::model::region_instance_templates::GetRequest::instance_template].
        pub fn set_instance_template<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instance_template = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instance_templates::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_templates::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceTemplates::insert][crate::client::RegionInstanceTemplates::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_templates::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::region_instance_templates::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceTemplates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_instance_templates::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionInstanceTemplates::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_instance_templates::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_templates::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instance_templates::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instance_templates::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_instance_templates::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceTemplate>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instance_templates::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceTemplate>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceTemplates::list][crate::client::RegionInstanceTemplates::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_templates::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_instance_templates::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceTemplates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_instance_templates::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstanceTemplateList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::InstanceTemplateList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::InstanceTemplateList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_instance_templates::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_instance_templates::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_instance_templates::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_instance_templates::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_instance_templates::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_instance_templates::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_instance_templates::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_instance_templates::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_instance_templates::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instance_templates::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_instance_templates::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_instance_templates::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstanceTemplates::get_operation][crate::client::RegionInstanceTemplates::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instance_templates::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstanceTemplates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionInstances][crate::client::RegionInstances].
#[cfg(feature = "region-instances")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-instances")))]
pub mod region_instances {
    use crate::Result;

    /// A builder for [RegionInstances][crate::client::RegionInstances].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_instances::ClientBuilder;
    /// # use client::RegionInstances;
    /// let builder : ClientBuilder = RegionInstances::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionInstances;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionInstances;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionInstances] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstances>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstances>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionInstances::bulk_insert][crate::client::RegionInstances::bulk_insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instances::BulkInsert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> BulkInsert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct BulkInsert(RequestBuilder<crate::model::region_instances::BulkInsertRequest>);

    impl BulkInsert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_instances::BulkInsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .bulk_insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `bulk_insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionInstances::bulk_insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_instances::BulkInsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instances::BulkInsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instances::BulkInsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instances::BulkInsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_instances::BulkInsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::BulkInsertInstanceResource>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instances::BulkInsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::BulkInsertInstanceResource>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for BulkInsert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstances::get_operation][crate::client::RegionInstances::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instances::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionInstantSnapshotGroups][crate::client::RegionInstantSnapshotGroups].
#[cfg(feature = "region-instant-snapshot-groups")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-instant-snapshot-groups")))]
pub mod region_instant_snapshot_groups {
    use crate::Result;

    /// A builder for [RegionInstantSnapshotGroups][crate::client::RegionInstantSnapshotGroups].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_instant_snapshot_groups::ClientBuilder;
    /// # use client::RegionInstantSnapshotGroups;
    /// let builder : ClientBuilder = RegionInstantSnapshotGroups::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionInstantSnapshotGroups;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionInstantSnapshotGroups;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionInstantSnapshotGroups] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstantSnapshotGroups>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstantSnapshotGroups>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionInstantSnapshotGroups::delete][crate::client::RegionInstantSnapshotGroups::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instant_snapshot_groups::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::region_instant_snapshot_groups::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstantSnapshotGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instant_snapshot_groups::DeleteRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionInstantSnapshotGroups::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instant_snapshot_group][crate::model::region_instant_snapshot_groups::DeleteRequest::instant_snapshot_group].
        pub fn set_instant_snapshot_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instant_snapshot_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instant_snapshot_groups::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instant_snapshot_groups::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instant_snapshot_groups::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instant_snapshot_groups::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstantSnapshotGroups::get][crate::client::RegionInstantSnapshotGroups::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instant_snapshot_groups::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_instant_snapshot_groups::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstantSnapshotGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_instant_snapshot_groups::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstantSnapshotGroup> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [instant_snapshot_group][crate::model::region_instant_snapshot_groups::GetRequest::instant_snapshot_group].
        pub fn set_instant_snapshot_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instant_snapshot_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instant_snapshot_groups::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instant_snapshot_groups::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstantSnapshotGroups::get_iam_policy][crate::client::RegionInstantSnapshotGroups::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instant_snapshot_groups::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(
        RequestBuilder<crate::model::region_instant_snapshot_groups::GetIamPolicyRequest>,
    );

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstantSnapshotGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instant_snapshot_groups::GetIamPolicyRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::region_instant_snapshot_groups::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::region_instant_snapshot_groups::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_instant_snapshot_groups::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instant_snapshot_groups::GetIamPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_instant_snapshot_groups::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstantSnapshotGroups::insert][crate::client::RegionInstantSnapshotGroups::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instant_snapshot_groups::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::region_instant_snapshot_groups::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstantSnapshotGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instant_snapshot_groups::InsertRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionInstantSnapshotGroups::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_instant_snapshot_groups::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instant_snapshot_groups::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instant_snapshot_groups::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instant_snapshot_groups::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [source_consistency_group][crate::model::region_instant_snapshot_groups::InsertRequest::source_consistency_group].
        pub fn set_source_consistency_group<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.source_consistency_group = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [source_consistency_group][crate::model::region_instant_snapshot_groups::InsertRequest::source_consistency_group].
        pub fn set_or_clear_source_consistency_group<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.source_consistency_group = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_instant_snapshot_groups::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstantSnapshotGroup>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instant_snapshot_groups::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstantSnapshotGroup>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstantSnapshotGroups::list][crate::client::RegionInstantSnapshotGroups::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instant_snapshot_groups::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_instant_snapshot_groups::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstantSnapshotGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_instant_snapshot_groups::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ListInstantSnapshotGroups> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::ListInstantSnapshotGroups,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ListInstantSnapshotGroups,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_instant_snapshot_groups::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_instant_snapshot_groups::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_instant_snapshot_groups::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_instant_snapshot_groups::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_instant_snapshot_groups::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_instant_snapshot_groups::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_instant_snapshot_groups::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_instant_snapshot_groups::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_instant_snapshot_groups::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instant_snapshot_groups::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_instant_snapshot_groups::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_instant_snapshot_groups::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstantSnapshotGroups::set_iam_policy][crate::client::RegionInstantSnapshotGroups::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instant_snapshot_groups::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(
        RequestBuilder<crate::model::region_instant_snapshot_groups::SetIamPolicyRequest>,
    );

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstantSnapshotGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instant_snapshot_groups::SetIamPolicyRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_instant_snapshot_groups::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instant_snapshot_groups::SetIamPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_instant_snapshot_groups::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_instant_snapshot_groups::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instant_snapshot_groups::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstantSnapshotGroups::test_iam_permissions][crate::client::RegionInstantSnapshotGroups::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instant_snapshot_groups::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::region_instant_snapshot_groups::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstantSnapshotGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instant_snapshot_groups::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_instant_snapshot_groups::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instant_snapshot_groups::TestIamPermissionsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_instant_snapshot_groups::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_instant_snapshot_groups::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instant_snapshot_groups::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstantSnapshotGroups::get_operation][crate::client::RegionInstantSnapshotGroups::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instant_snapshot_groups::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstantSnapshotGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionInstantSnapshots][crate::client::RegionInstantSnapshots].
#[cfg(feature = "region-instant-snapshots")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-instant-snapshots")))]
pub mod region_instant_snapshots {
    use crate::Result;

    /// A builder for [RegionInstantSnapshots][crate::client::RegionInstantSnapshots].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_instant_snapshots::ClientBuilder;
    /// # use client::RegionInstantSnapshots;
    /// let builder : ClientBuilder = RegionInstantSnapshots::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionInstantSnapshots;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionInstantSnapshots;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionInstantSnapshots] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstantSnapshots>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstantSnapshots>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionInstantSnapshots::delete][crate::client::RegionInstantSnapshots::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instant_snapshots::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::region_instant_snapshots::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstantSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_instant_snapshots::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionInstantSnapshots::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [instant_snapshot][crate::model::region_instant_snapshots::DeleteRequest::instant_snapshot].
        pub fn set_instant_snapshot<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instant_snapshot = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instant_snapshots::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instant_snapshots::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instant_snapshots::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instant_snapshots::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstantSnapshots::get][crate::client::RegionInstantSnapshots::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instant_snapshots::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_instant_snapshots::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstantSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_instant_snapshots::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstantSnapshot> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [instant_snapshot][crate::model::region_instant_snapshots::GetRequest::instant_snapshot].
        pub fn set_instant_snapshot<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.instant_snapshot = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_instant_snapshots::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instant_snapshots::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstantSnapshots::get_iam_policy][crate::client::RegionInstantSnapshots::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instant_snapshots::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(
        RequestBuilder<crate::model::region_instant_snapshots::GetIamPolicyRequest>,
    );

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstantSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instant_snapshots::GetIamPolicyRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::region_instant_snapshots::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::region_instant_snapshots::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_instant_snapshots::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instant_snapshots::GetIamPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_instant_snapshots::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstantSnapshots::insert][crate::client::RegionInstantSnapshots::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instant_snapshots::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::region_instant_snapshots::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstantSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_instant_snapshots::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionInstantSnapshots::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_instant_snapshots::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instant_snapshots::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instant_snapshots::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instant_snapshots::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_instant_snapshots::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstantSnapshot>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instant_snapshots::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstantSnapshot>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstantSnapshots::list][crate::client::RegionInstantSnapshots::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instant_snapshots::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_instant_snapshots::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstantSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_instant_snapshots::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::InstantSnapshotList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::InstantSnapshotList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::InstantSnapshotList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_instant_snapshots::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_instant_snapshots::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_instant_snapshots::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_instant_snapshots::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_instant_snapshots::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_instant_snapshots::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_instant_snapshots::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_instant_snapshots::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_instant_snapshots::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instant_snapshots::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_instant_snapshots::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_instant_snapshots::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstantSnapshots::set_iam_policy][crate::client::RegionInstantSnapshots::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instant_snapshots::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(
        RequestBuilder<crate::model::region_instant_snapshots::SetIamPolicyRequest>,
    );

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstantSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instant_snapshots::SetIamPolicyRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_instant_snapshots::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instant_snapshots::SetIamPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_instant_snapshots::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_instant_snapshots::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instant_snapshots::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstantSnapshots::set_labels][crate::client::RegionInstantSnapshots::set_labels] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instant_snapshots::SetLabels;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetLabels {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetLabels(RequestBuilder<crate::model::region_instant_snapshots::SetLabelsRequest>);

    impl SetLabels {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstantSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_instant_snapshots::SetLabelsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_labels(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_labels`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionInstantSnapshots::set_labels::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_instant_snapshots::SetLabelsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instant_snapshots::SetLabelsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_instant_snapshots::SetLabelsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_instant_snapshots::SetLabelsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [resource][crate::model::region_instant_snapshots::SetLabelsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_instant_snapshots::SetLabelsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetLabelsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instant_snapshots::SetLabelsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetLabelsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetLabels {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstantSnapshots::test_iam_permissions][crate::client::RegionInstantSnapshots::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instant_snapshots::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::region_instant_snapshots::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstantSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_instant_snapshots::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_instant_snapshots::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_instant_snapshots::TestIamPermissionsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_instant_snapshots::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_instant_snapshots::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_instant_snapshots::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionInstantSnapshots::get_operation][crate::client::RegionInstantSnapshots::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_instant_snapshots::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionInstantSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionNetworkEndpointGroups][crate::client::RegionNetworkEndpointGroups].
#[cfg(feature = "region-network-endpoint-groups")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-network-endpoint-groups")))]
pub mod region_network_endpoint_groups {
    use crate::Result;

    /// A builder for [RegionNetworkEndpointGroups][crate::client::RegionNetworkEndpointGroups].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_network_endpoint_groups::ClientBuilder;
    /// # use client::RegionNetworkEndpointGroups;
    /// let builder : ClientBuilder = RegionNetworkEndpointGroups::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionNetworkEndpointGroups;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionNetworkEndpointGroups;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionNetworkEndpointGroups] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkEndpointGroups>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkEndpointGroups>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionNetworkEndpointGroups::attach_network_endpoints][crate::client::RegionNetworkEndpointGroups::attach_network_endpoints] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_endpoint_groups::AttachNetworkEndpoints;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AttachNetworkEndpoints {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AttachNetworkEndpoints(
        RequestBuilder<crate::model::region_network_endpoint_groups::AttachNetworkEndpointsRequest>,
    );

    impl AttachNetworkEndpoints {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_network_endpoint_groups::AttachNetworkEndpointsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .attach_network_endpoints(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `attach_network_endpoints`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionNetworkEndpointGroups::attach_network_endpoints::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [network_endpoint_group][crate::model::region_network_endpoint_groups::AttachNetworkEndpointsRequest::network_endpoint_group].
        pub fn set_network_endpoint_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network_endpoint_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_network_endpoint_groups::AttachNetworkEndpointsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_network_endpoint_groups::AttachNetworkEndpointsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_network_endpoint_groups::AttachNetworkEndpointsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_network_endpoint_groups::AttachNetworkEndpointsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_network_endpoint_groups::AttachNetworkEndpointsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionNetworkEndpointGroupsAttachEndpointsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_network_endpoint_groups::AttachNetworkEndpointsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionNetworkEndpointGroupsAttachEndpointsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AttachNetworkEndpoints {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNetworkEndpointGroups::delete][crate::client::RegionNetworkEndpointGroups::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_endpoint_groups::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::region_network_endpoint_groups::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_network_endpoint_groups::DeleteRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionNetworkEndpointGroups::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [network_endpoint_group][crate::model::region_network_endpoint_groups::DeleteRequest::network_endpoint_group].
        pub fn set_network_endpoint_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network_endpoint_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_network_endpoint_groups::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_network_endpoint_groups::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_network_endpoint_groups::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_network_endpoint_groups::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNetworkEndpointGroups::detach_network_endpoints][crate::client::RegionNetworkEndpointGroups::detach_network_endpoints] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_endpoint_groups::DetachNetworkEndpoints;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DetachNetworkEndpoints {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DetachNetworkEndpoints(
        RequestBuilder<crate::model::region_network_endpoint_groups::DetachNetworkEndpointsRequest>,
    );

    impl DetachNetworkEndpoints {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_network_endpoint_groups::DetachNetworkEndpointsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .detach_network_endpoints(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `detach_network_endpoints`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionNetworkEndpointGroups::detach_network_endpoints::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [network_endpoint_group][crate::model::region_network_endpoint_groups::DetachNetworkEndpointsRequest::network_endpoint_group].
        pub fn set_network_endpoint_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network_endpoint_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_network_endpoint_groups::DetachNetworkEndpointsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_network_endpoint_groups::DetachNetworkEndpointsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_network_endpoint_groups::DetachNetworkEndpointsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_network_endpoint_groups::DetachNetworkEndpointsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_network_endpoint_groups::DetachNetworkEndpointsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionNetworkEndpointGroupsDetachEndpointsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_network_endpoint_groups::DetachNetworkEndpointsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionNetworkEndpointGroupsDetachEndpointsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DetachNetworkEndpoints {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNetworkEndpointGroups::get][crate::client::RegionNetworkEndpointGroups::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_endpoint_groups::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_network_endpoint_groups::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_network_endpoint_groups::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NetworkEndpointGroup> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [network_endpoint_group][crate::model::region_network_endpoint_groups::GetRequest::network_endpoint_group].
        pub fn set_network_endpoint_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network_endpoint_group = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_network_endpoint_groups::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_network_endpoint_groups::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNetworkEndpointGroups::insert][crate::client::RegionNetworkEndpointGroups::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_endpoint_groups::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::region_network_endpoint_groups::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_network_endpoint_groups::InsertRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionNetworkEndpointGroups::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_network_endpoint_groups::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_network_endpoint_groups::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_network_endpoint_groups::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_network_endpoint_groups::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_network_endpoint_groups::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NetworkEndpointGroup>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_network_endpoint_groups::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NetworkEndpointGroup>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNetworkEndpointGroups::list][crate::client::RegionNetworkEndpointGroups::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_endpoint_groups::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_network_endpoint_groups::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_network_endpoint_groups::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NetworkEndpointGroupList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::NetworkEndpointGroupList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::NetworkEndpointGroupList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_network_endpoint_groups::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_network_endpoint_groups::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_network_endpoint_groups::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_network_endpoint_groups::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_network_endpoint_groups::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_network_endpoint_groups::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_network_endpoint_groups::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_network_endpoint_groups::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_network_endpoint_groups::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_network_endpoint_groups::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_network_endpoint_groups::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_network_endpoint_groups::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNetworkEndpointGroups::list_network_endpoints][crate::client::RegionNetworkEndpointGroups::list_network_endpoints] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_endpoint_groups::ListNetworkEndpoints;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListNetworkEndpoints {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListNetworkEndpoints(
        RequestBuilder<crate::model::region_network_endpoint_groups::ListNetworkEndpointsRequest>,
    );

    impl ListNetworkEndpoints {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_network_endpoint_groups::ListNetworkEndpointsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NetworkEndpointGroupsListNetworkEndpoints> {
            (*self.0.stub)
                .list_network_endpoints(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::NetworkEndpointGroupsListNetworkEndpoints,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::NetworkEndpointGroupsListNetworkEndpoints,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_network_endpoint_groups::ListNetworkEndpointsRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_network_endpoint_groups::ListNetworkEndpointsRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_network_endpoint_groups::ListNetworkEndpointsRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_network_endpoint_groups::ListNetworkEndpointsRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [network_endpoint_group][crate::model::region_network_endpoint_groups::ListNetworkEndpointsRequest::network_endpoint_group].
        pub fn set_network_endpoint_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network_endpoint_group = v.into();
            self
        }

        /// Sets the value of [order_by][crate::model::region_network_endpoint_groups::ListNetworkEndpointsRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_network_endpoint_groups::ListNetworkEndpointsRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_network_endpoint_groups::ListNetworkEndpointsRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_network_endpoint_groups::ListNetworkEndpointsRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_network_endpoint_groups::ListNetworkEndpointsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_network_endpoint_groups::ListNetworkEndpointsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_network_endpoint_groups::ListNetworkEndpointsRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_network_endpoint_groups::ListNetworkEndpointsRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListNetworkEndpoints {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNetworkEndpointGroups::get_operation][crate::client::RegionNetworkEndpointGroups::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_endpoint_groups::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkEndpointGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionNetworkFirewallPolicies][crate::client::RegionNetworkFirewallPolicies].
#[cfg(feature = "region-network-firewall-policies")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-network-firewall-policies")))]
pub mod region_network_firewall_policies {
    use crate::Result;

    /// A builder for [RegionNetworkFirewallPolicies][crate::client::RegionNetworkFirewallPolicies].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_network_firewall_policies::ClientBuilder;
    /// # use client::RegionNetworkFirewallPolicies;
    /// let builder : ClientBuilder = RegionNetworkFirewallPolicies::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionNetworkFirewallPolicies;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionNetworkFirewallPolicies;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionNetworkFirewallPolicies] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkFirewallPolicies>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkFirewallPolicies>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionNetworkFirewallPolicies::add_association][crate::client::RegionNetworkFirewallPolicies::add_association] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_firewall_policies::AddAssociation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AddAssociation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AddAssociation(
        RequestBuilder<crate::model::region_network_firewall_policies::AddAssociationRequest>,
    );

    impl AddAssociation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_network_firewall_policies::AddAssociationRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .add_association(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `add_association`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionNetworkFirewallPolicies::add_association::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::region_network_firewall_policies::AddAssociationRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_network_firewall_policies::AddAssociationRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_network_firewall_policies::AddAssociationRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [replace_existing_association][crate::model::region_network_firewall_policies::AddAssociationRequest::replace_existing_association].
        pub fn set_replace_existing_association<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.replace_existing_association = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [replace_existing_association][crate::model::region_network_firewall_policies::AddAssociationRequest::replace_existing_association].
        pub fn set_or_clear_replace_existing_association<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.replace_existing_association = v.map(|x| x.into());
            self
        }

        /// Sets the value of [request_id][crate::model::region_network_firewall_policies::AddAssociationRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_network_firewall_policies::AddAssociationRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_network_firewall_policies::AddAssociationRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicyAssociation>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_network_firewall_policies::AddAssociationRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicyAssociation>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AddAssociation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNetworkFirewallPolicies::add_rule][crate::client::RegionNetworkFirewallPolicies::add_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_firewall_policies::AddRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AddRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AddRule(
        RequestBuilder<crate::model::region_network_firewall_policies::AddRuleRequest>,
    );

    impl AddRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_network_firewall_policies::AddRuleRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .add_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `add_rule`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionNetworkFirewallPolicies::add_rule::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::region_network_firewall_policies::AddRuleRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [max_priority][crate::model::region_network_firewall_policies::AddRuleRequest::max_priority].
        pub fn set_max_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.max_priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_priority][crate::model::region_network_firewall_policies::AddRuleRequest::max_priority].
        pub fn set_or_clear_max_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.max_priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [min_priority][crate::model::region_network_firewall_policies::AddRuleRequest::min_priority].
        pub fn set_min_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.min_priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [min_priority][crate::model::region_network_firewall_policies::AddRuleRequest::min_priority].
        pub fn set_or_clear_min_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.min_priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_network_firewall_policies::AddRuleRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_network_firewall_policies::AddRuleRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_network_firewall_policies::AddRuleRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_network_firewall_policies::AddRuleRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_network_firewall_policies::AddRuleRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicyRule>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_network_firewall_policies::AddRuleRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicyRule>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AddRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNetworkFirewallPolicies::clone_rules][crate::client::RegionNetworkFirewallPolicies::clone_rules] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_firewall_policies::CloneRules;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> CloneRules {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct CloneRules(
        RequestBuilder<crate::model::region_network_firewall_policies::CloneRulesRequest>,
    );

    impl CloneRules {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_network_firewall_policies::CloneRulesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .clone_rules(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `clone_rules`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionNetworkFirewallPolicies::clone_rules::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::region_network_firewall_policies::CloneRulesRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_network_firewall_policies::CloneRulesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_network_firewall_policies::CloneRulesRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_network_firewall_policies::CloneRulesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_network_firewall_policies::CloneRulesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [source_firewall_policy][crate::model::region_network_firewall_policies::CloneRulesRequest::source_firewall_policy].
        pub fn set_source_firewall_policy<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.source_firewall_policy = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [source_firewall_policy][crate::model::region_network_firewall_policies::CloneRulesRequest::source_firewall_policy].
        pub fn set_or_clear_source_firewall_policy<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.source_firewall_policy = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for CloneRules {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNetworkFirewallPolicies::delete][crate::client::RegionNetworkFirewallPolicies::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_firewall_policies::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(
        RequestBuilder<crate::model::region_network_firewall_policies::DeleteRequest>,
    );

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_network_firewall_policies::DeleteRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionNetworkFirewallPolicies::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::region_network_firewall_policies::DeleteRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_network_firewall_policies::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_network_firewall_policies::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_network_firewall_policies::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_network_firewall_policies::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNetworkFirewallPolicies::get][crate::client::RegionNetworkFirewallPolicies::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_firewall_policies::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_network_firewall_policies::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_network_firewall_policies::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::FirewallPolicy> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [firewall_policy][crate::model::region_network_firewall_policies::GetRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_network_firewall_policies::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_network_firewall_policies::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNetworkFirewallPolicies::get_association][crate::client::RegionNetworkFirewallPolicies::get_association] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_firewall_policies::GetAssociation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetAssociation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetAssociation(
        RequestBuilder<crate::model::region_network_firewall_policies::GetAssociationRequest>,
    );

    impl GetAssociation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_network_firewall_policies::GetAssociationRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::FirewallPolicyAssociation> {
            (*self.0.stub)
                .get_association(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [firewall_policy][crate::model::region_network_firewall_policies::GetAssociationRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [name][crate::model::region_network_firewall_policies::GetAssociationRequest::name].
        pub fn set_name<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.name = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [name][crate::model::region_network_firewall_policies::GetAssociationRequest::name].
        pub fn set_or_clear_name<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.name = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_network_firewall_policies::GetAssociationRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_network_firewall_policies::GetAssociationRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetAssociation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNetworkFirewallPolicies::get_effective_firewalls][crate::client::RegionNetworkFirewallPolicies::get_effective_firewalls] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_firewall_policies::GetEffectiveFirewalls;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetEffectiveFirewalls {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetEffectiveFirewalls(
        RequestBuilder<
            crate::model::region_network_firewall_policies::GetEffectiveFirewallsRequest,
        >,
    );

    impl GetEffectiveFirewalls {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_network_firewall_policies::GetEffectiveFirewallsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(
            self,
        ) -> Result<crate::model::RegionNetworkFirewallPoliciesGetEffectiveFirewallsResponse>
        {
            (*self.0.stub)
                .get_effective_firewalls(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [network][crate::model::region_network_firewall_policies::GetEffectiveFirewallsRequest::network].
        pub fn set_network<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.network = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_network_firewall_policies::GetEffectiveFirewallsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_network_firewall_policies::GetEffectiveFirewallsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetEffectiveFirewalls {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNetworkFirewallPolicies::get_iam_policy][crate::client::RegionNetworkFirewallPolicies::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_firewall_policies::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(
        RequestBuilder<crate::model::region_network_firewall_policies::GetIamPolicyRequest>,
    );

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_network_firewall_policies::GetIamPolicyRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::region_network_firewall_policies::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::region_network_firewall_policies::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_network_firewall_policies::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_network_firewall_policies::GetIamPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_network_firewall_policies::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNetworkFirewallPolicies::get_rule][crate::client::RegionNetworkFirewallPolicies::get_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_firewall_policies::GetRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetRule(
        RequestBuilder<crate::model::region_network_firewall_policies::GetRuleRequest>,
    );

    impl GetRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_network_firewall_policies::GetRuleRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::FirewallPolicyRule> {
            (*self.0.stub)
                .get_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [firewall_policy][crate::model::region_network_firewall_policies::GetRuleRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [priority][crate::model::region_network_firewall_policies::GetRuleRequest::priority].
        pub fn set_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [priority][crate::model::region_network_firewall_policies::GetRuleRequest::priority].
        pub fn set_or_clear_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_network_firewall_policies::GetRuleRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_network_firewall_policies::GetRuleRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNetworkFirewallPolicies::insert][crate::client::RegionNetworkFirewallPolicies::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_firewall_policies::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(
        RequestBuilder<crate::model::region_network_firewall_policies::InsertRequest>,
    );

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_network_firewall_policies::InsertRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionNetworkFirewallPolicies::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_network_firewall_policies::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_network_firewall_policies::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_network_firewall_policies::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_network_firewall_policies::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_network_firewall_policies::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_network_firewall_policies::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNetworkFirewallPolicies::list][crate::client::RegionNetworkFirewallPolicies::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_firewall_policies::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_network_firewall_policies::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_network_firewall_policies::ListRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::FirewallPolicyList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::FirewallPolicyList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::FirewallPolicyList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_network_firewall_policies::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_network_firewall_policies::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_network_firewall_policies::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_network_firewall_policies::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_network_firewall_policies::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_network_firewall_policies::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_network_firewall_policies::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_network_firewall_policies::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_network_firewall_policies::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_network_firewall_policies::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_network_firewall_policies::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_network_firewall_policies::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNetworkFirewallPolicies::patch][crate::client::RegionNetworkFirewallPolicies::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_firewall_policies::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::region_network_firewall_policies::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_network_firewall_policies::PatchRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionNetworkFirewallPolicies::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::region_network_firewall_policies::PatchRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_network_firewall_policies::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_network_firewall_policies::PatchRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_network_firewall_policies::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_network_firewall_policies::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_network_firewall_policies::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_network_firewall_policies::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNetworkFirewallPolicies::patch_rule][crate::client::RegionNetworkFirewallPolicies::patch_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_firewall_policies::PatchRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> PatchRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct PatchRule(
        RequestBuilder<crate::model::region_network_firewall_policies::PatchRuleRequest>,
    );

    impl PatchRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_network_firewall_policies::PatchRuleRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch_rule`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionNetworkFirewallPolicies::patch_rule::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::region_network_firewall_policies::PatchRuleRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [priority][crate::model::region_network_firewall_policies::PatchRuleRequest::priority].
        pub fn set_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [priority][crate::model::region_network_firewall_policies::PatchRuleRequest::priority].
        pub fn set_or_clear_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_network_firewall_policies::PatchRuleRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_network_firewall_policies::PatchRuleRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_network_firewall_policies::PatchRuleRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_network_firewall_policies::PatchRuleRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_network_firewall_policies::PatchRuleRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicyRule>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_network_firewall_policies::PatchRuleRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::FirewallPolicyRule>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for PatchRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNetworkFirewallPolicies::remove_association][crate::client::RegionNetworkFirewallPolicies::remove_association] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_firewall_policies::RemoveAssociation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> RemoveAssociation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct RemoveAssociation(
        RequestBuilder<crate::model::region_network_firewall_policies::RemoveAssociationRequest>,
    );

    impl RemoveAssociation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_network_firewall_policies::RemoveAssociationRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .remove_association(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `remove_association`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionNetworkFirewallPolicies::remove_association::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::region_network_firewall_policies::RemoveAssociationRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [name][crate::model::region_network_firewall_policies::RemoveAssociationRequest::name].
        pub fn set_name<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.name = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [name][crate::model::region_network_firewall_policies::RemoveAssociationRequest::name].
        pub fn set_or_clear_name<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.name = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_network_firewall_policies::RemoveAssociationRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_network_firewall_policies::RemoveAssociationRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_network_firewall_policies::RemoveAssociationRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_network_firewall_policies::RemoveAssociationRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for RemoveAssociation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNetworkFirewallPolicies::remove_rule][crate::client::RegionNetworkFirewallPolicies::remove_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_firewall_policies::RemoveRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> RemoveRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct RemoveRule(
        RequestBuilder<crate::model::region_network_firewall_policies::RemoveRuleRequest>,
    );

    impl RemoveRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_network_firewall_policies::RemoveRuleRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .remove_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `remove_rule`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionNetworkFirewallPolicies::remove_rule::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [firewall_policy][crate::model::region_network_firewall_policies::RemoveRuleRequest::firewall_policy].
        pub fn set_firewall_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.firewall_policy = v.into();
            self
        }

        /// Sets the value of [priority][crate::model::region_network_firewall_policies::RemoveRuleRequest::priority].
        pub fn set_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [priority][crate::model::region_network_firewall_policies::RemoveRuleRequest::priority].
        pub fn set_or_clear_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_network_firewall_policies::RemoveRuleRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_network_firewall_policies::RemoveRuleRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_network_firewall_policies::RemoveRuleRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_network_firewall_policies::RemoveRuleRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for RemoveRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNetworkFirewallPolicies::set_iam_policy][crate::client::RegionNetworkFirewallPolicies::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_firewall_policies::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(
        RequestBuilder<crate::model::region_network_firewall_policies::SetIamPolicyRequest>,
    );

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_network_firewall_policies::SetIamPolicyRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_network_firewall_policies::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_network_firewall_policies::SetIamPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_network_firewall_policies::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_network_firewall_policies::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_network_firewall_policies::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNetworkFirewallPolicies::test_iam_permissions][crate::client::RegionNetworkFirewallPolicies::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_firewall_policies::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::region_network_firewall_policies::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_network_firewall_policies::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_network_firewall_policies::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_network_firewall_policies::TestIamPermissionsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_network_firewall_policies::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_network_firewall_policies::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_network_firewall_policies::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNetworkFirewallPolicies::get_operation][crate::client::RegionNetworkFirewallPolicies::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_network_firewall_policies::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNetworkFirewallPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionNotificationEndpoints][crate::client::RegionNotificationEndpoints].
#[cfg(feature = "region-notification-endpoints")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-notification-endpoints")))]
pub mod region_notification_endpoints {
    use crate::Result;

    /// A builder for [RegionNotificationEndpoints][crate::client::RegionNotificationEndpoints].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_notification_endpoints::ClientBuilder;
    /// # use client::RegionNotificationEndpoints;
    /// let builder : ClientBuilder = RegionNotificationEndpoints::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionNotificationEndpoints;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionNotificationEndpoints;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionNotificationEndpoints] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNotificationEndpoints>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNotificationEndpoints>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionNotificationEndpoints::aggregated_list][crate::client::RegionNotificationEndpoints::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_notification_endpoints::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::region_notification_endpoints::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNotificationEndpoints>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_notification_endpoints::AggregatedListRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NotificationEndpointAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::NotificationEndpointAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::NotificationEndpointAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_notification_endpoints::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_notification_endpoints::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::region_notification_endpoints::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::region_notification_endpoints::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_notification_endpoints::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_notification_endpoints::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_notification_endpoints::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_notification_endpoints::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_notification_endpoints::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_notification_endpoints::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_notification_endpoints::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_notification_endpoints::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_notification_endpoints::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::region_notification_endpoints::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::region_notification_endpoints::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNotificationEndpoints::delete][crate::client::RegionNotificationEndpoints::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_notification_endpoints::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::region_notification_endpoints::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNotificationEndpoints>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_notification_endpoints::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionNotificationEndpoints::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [notification_endpoint][crate::model::region_notification_endpoints::DeleteRequest::notification_endpoint].
        pub fn set_notification_endpoint<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.notification_endpoint = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_notification_endpoints::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_notification_endpoints::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_notification_endpoints::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_notification_endpoints::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNotificationEndpoints::get][crate::client::RegionNotificationEndpoints::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_notification_endpoints::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_notification_endpoints::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNotificationEndpoints>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_notification_endpoints::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NotificationEndpoint> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [notification_endpoint][crate::model::region_notification_endpoints::GetRequest::notification_endpoint].
        pub fn set_notification_endpoint<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.notification_endpoint = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_notification_endpoints::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_notification_endpoints::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNotificationEndpoints::insert][crate::client::RegionNotificationEndpoints::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_notification_endpoints::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::region_notification_endpoints::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNotificationEndpoints>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_notification_endpoints::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionNotificationEndpoints::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_notification_endpoints::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_notification_endpoints::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_notification_endpoints::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_notification_endpoints::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_notification_endpoints::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NotificationEndpoint>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_notification_endpoints::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NotificationEndpoint>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNotificationEndpoints::list][crate::client::RegionNotificationEndpoints::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_notification_endpoints::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_notification_endpoints::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNotificationEndpoints>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_notification_endpoints::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NotificationEndpointList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::NotificationEndpointList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::NotificationEndpointList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_notification_endpoints::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_notification_endpoints::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_notification_endpoints::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_notification_endpoints::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_notification_endpoints::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_notification_endpoints::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_notification_endpoints::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_notification_endpoints::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_notification_endpoints::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_notification_endpoints::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_notification_endpoints::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_notification_endpoints::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNotificationEndpoints::test_iam_permissions][crate::client::RegionNotificationEndpoints::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_notification_endpoints::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::region_notification_endpoints::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNotificationEndpoints>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_notification_endpoints::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_notification_endpoints::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_notification_endpoints::TestIamPermissionsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_notification_endpoints::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_notification_endpoints::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_notification_endpoints::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionNotificationEndpoints::get_operation][crate::client::RegionNotificationEndpoints::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_notification_endpoints::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionNotificationEndpoints>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionOperations][crate::client::RegionOperations].
#[cfg(feature = "region-operations")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-operations")))]
pub mod region_operations {
    use crate::Result;

    /// A builder for [RegionOperations][crate::client::RegionOperations].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_operations::ClientBuilder;
    /// # use client::RegionOperations;
    /// let builder : ClientBuilder = RegionOperations::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionOperations;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionOperations;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionOperations] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionOperations>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionOperations>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionOperations::delete][crate::client::RegionOperations::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_operations::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::region_operations::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionOperations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<()> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::DeleteRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionOperations::get][crate::client::RegionOperations::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_operations::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionOperations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionOperations::list][crate::client::RegionOperations::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_operations::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_operations::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionOperations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::OperationList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::OperationList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::OperationList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_operations::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_operations::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_operations::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_operations::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_operations::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_operations::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_operations::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_operations::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_operations::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_operations::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_operations::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionOperations::wait][crate::client::RegionOperations::wait] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_operations::Wait;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Wait {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Wait(RequestBuilder<crate::model::region_operations::WaitRequest>);

    impl Wait {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionOperations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::WaitRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .wait(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::WaitRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::WaitRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::WaitRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Wait {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionSecurityPolicies][crate::client::RegionSecurityPolicies].
#[cfg(feature = "region-security-policies")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-security-policies")))]
pub mod region_security_policies {
    use crate::Result;

    /// A builder for [RegionSecurityPolicies][crate::client::RegionSecurityPolicies].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_security_policies::ClientBuilder;
    /// # use client::RegionSecurityPolicies;
    /// let builder : ClientBuilder = RegionSecurityPolicies::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionSecurityPolicies;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionSecurityPolicies;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionSecurityPolicies] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSecurityPolicies>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSecurityPolicies>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionSecurityPolicies::add_rule][crate::client::RegionSecurityPolicies::add_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_security_policies::AddRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AddRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AddRule(RequestBuilder<crate::model::region_security_policies::AddRuleRequest>);

    impl AddRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_security_policies::AddRuleRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .add_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `add_rule`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionSecurityPolicies::add_rule::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_security_policies::AddRuleRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_security_policies::AddRuleRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [security_policy][crate::model::region_security_policies::AddRuleRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }

        /// Sets the value of [validate_only][crate::model::region_security_policies::AddRuleRequest::validate_only].
        pub fn set_validate_only<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.validate_only = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [validate_only][crate::model::region_security_policies::AddRuleRequest::validate_only].
        pub fn set_or_clear_validate_only<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.validate_only = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_security_policies::AddRuleRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyRule>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_security_policies::AddRuleRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyRule>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AddRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSecurityPolicies::delete][crate::client::RegionSecurityPolicies::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_security_policies::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::region_security_policies::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_security_policies::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionSecurityPolicies::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_security_policies::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_security_policies::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_security_policies::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_security_policies::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [security_policy][crate::model::region_security_policies::DeleteRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSecurityPolicies::get][crate::client::RegionSecurityPolicies::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_security_policies::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_security_policies::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_security_policies::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SecurityPolicy> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_security_policies::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_security_policies::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [security_policy][crate::model::region_security_policies::GetRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSecurityPolicies::get_rule][crate::client::RegionSecurityPolicies::get_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_security_policies::GetRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetRule(RequestBuilder<crate::model::region_security_policies::GetRuleRequest>);

    impl GetRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_security_policies::GetRuleRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SecurityPolicyRule> {
            (*self.0.stub)
                .get_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [priority][crate::model::region_security_policies::GetRuleRequest::priority].
        pub fn set_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [priority][crate::model::region_security_policies::GetRuleRequest::priority].
        pub fn set_or_clear_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_security_policies::GetRuleRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_security_policies::GetRuleRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [security_policy][crate::model::region_security_policies::GetRuleRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSecurityPolicies::insert][crate::client::RegionSecurityPolicies::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_security_policies::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::region_security_policies::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_security_policies::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionSecurityPolicies::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_security_policies::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_security_policies::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_security_policies::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_security_policies::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [validate_only][crate::model::region_security_policies::InsertRequest::validate_only].
        pub fn set_validate_only<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.validate_only = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [validate_only][crate::model::region_security_policies::InsertRequest::validate_only].
        pub fn set_or_clear_validate_only<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.validate_only = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_security_policies::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_security_policies::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSecurityPolicies::list][crate::client::RegionSecurityPolicies::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_security_policies::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_security_policies::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_security_policies::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SecurityPolicyList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::SecurityPolicyList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::SecurityPolicyList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_security_policies::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_security_policies::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_security_policies::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_security_policies::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_security_policies::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_security_policies::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_security_policies::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_security_policies::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_security_policies::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_security_policies::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_security_policies::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_security_policies::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSecurityPolicies::patch][crate::client::RegionSecurityPolicies::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_security_policies::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::region_security_policies::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_security_policies::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionSecurityPolicies::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_security_policies::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_security_policies::PatchRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_security_policies::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_security_policies::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [security_policy][crate::model::region_security_policies::PatchRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }

        /// Sets the value of [update_mask][crate::model::region_security_policies::PatchRequest::update_mask].
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::region_security_policies::PatchRequest::update_mask].
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_security_policies::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_security_policies::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSecurityPolicies::patch_rule][crate::client::RegionSecurityPolicies::patch_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_security_policies::PatchRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> PatchRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct PatchRule(RequestBuilder<crate::model::region_security_policies::PatchRuleRequest>);

    impl PatchRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_security_policies::PatchRuleRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch_rule`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionSecurityPolicies::patch_rule::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [priority][crate::model::region_security_policies::PatchRuleRequest::priority].
        pub fn set_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [priority][crate::model::region_security_policies::PatchRuleRequest::priority].
        pub fn set_or_clear_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_security_policies::PatchRuleRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_security_policies::PatchRuleRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [security_policy][crate::model::region_security_policies::PatchRuleRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }

        /// Sets the value of [update_mask][crate::model::region_security_policies::PatchRuleRequest::update_mask].
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::region_security_policies::PatchRuleRequest::update_mask].
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [validate_only][crate::model::region_security_policies::PatchRuleRequest::validate_only].
        pub fn set_validate_only<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.validate_only = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [validate_only][crate::model::region_security_policies::PatchRuleRequest::validate_only].
        pub fn set_or_clear_validate_only<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.validate_only = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_security_policies::PatchRuleRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyRule>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_security_policies::PatchRuleRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyRule>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for PatchRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSecurityPolicies::remove_rule][crate::client::RegionSecurityPolicies::remove_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_security_policies::RemoveRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> RemoveRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct RemoveRule(
        RequestBuilder<crate::model::region_security_policies::RemoveRuleRequest>,
    );

    impl RemoveRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_security_policies::RemoveRuleRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .remove_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `remove_rule`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionSecurityPolicies::remove_rule::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [priority][crate::model::region_security_policies::RemoveRuleRequest::priority].
        pub fn set_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [priority][crate::model::region_security_policies::RemoveRuleRequest::priority].
        pub fn set_or_clear_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_security_policies::RemoveRuleRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_security_policies::RemoveRuleRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [security_policy][crate::model::region_security_policies::RemoveRuleRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for RemoveRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSecurityPolicies::set_labels][crate::client::RegionSecurityPolicies::set_labels] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_security_policies::SetLabels;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetLabels {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetLabels(RequestBuilder<crate::model::region_security_policies::SetLabelsRequest>);

    impl SetLabels {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_security_policies::SetLabelsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_labels(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_labels`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionSecurityPolicies::set_labels::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_security_policies::SetLabelsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_security_policies::SetLabelsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_security_policies::SetLabelsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_security_policies::SetLabelsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [resource][crate::model::region_security_policies::SetLabelsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_security_policies::SetLabelsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetLabelsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_security_policies::SetLabelsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetLabelsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetLabels {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSecurityPolicies::get_operation][crate::client::RegionSecurityPolicies::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_security_policies::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionSnapshotSettings][crate::client::RegionSnapshotSettings].
#[cfg(feature = "region-snapshot-settings")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-snapshot-settings")))]
pub mod region_snapshot_settings {
    use crate::Result;

    /// A builder for [RegionSnapshotSettings][crate::client::RegionSnapshotSettings].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_snapshot_settings::ClientBuilder;
    /// # use client::RegionSnapshotSettings;
    /// let builder : ClientBuilder = RegionSnapshotSettings::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionSnapshotSettings;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionSnapshotSettings;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionSnapshotSettings] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSnapshotSettings>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSnapshotSettings>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionSnapshotSettings::get][crate::client::RegionSnapshotSettings::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_snapshot_settings::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_snapshot_settings::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSnapshotSettings>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_snapshot_settings::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SnapshotSettings> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_snapshot_settings::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_snapshot_settings::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSnapshotSettings::patch][crate::client::RegionSnapshotSettings::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_snapshot_settings::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::region_snapshot_settings::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSnapshotSettings>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_snapshot_settings::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionSnapshotSettings::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_snapshot_settings::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_snapshot_settings::PatchRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_snapshot_settings::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_snapshot_settings::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::region_snapshot_settings::PatchRequest::update_mask].
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::region_snapshot_settings::PatchRequest::update_mask].
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_snapshot_settings::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SnapshotSettings>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_snapshot_settings::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SnapshotSettings>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSnapshotSettings::get_operation][crate::client::RegionSnapshotSettings::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_snapshot_settings::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSnapshotSettings>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionSnapshots][crate::client::RegionSnapshots].
#[cfg(feature = "region-snapshots")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-snapshots")))]
pub mod region_snapshots {
    use crate::Result;

    /// A builder for [RegionSnapshots][crate::client::RegionSnapshots].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_snapshots::ClientBuilder;
    /// # use client::RegionSnapshots;
    /// let builder : ClientBuilder = RegionSnapshots::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionSnapshots;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionSnapshots;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionSnapshots] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSnapshots>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSnapshots>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionSnapshots::delete][crate::client::RegionSnapshots::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_snapshots::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::region_snapshots::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_snapshots::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionSnapshots::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_snapshots::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_snapshots::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_snapshots::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_snapshots::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [snapshot][crate::model::region_snapshots::DeleteRequest::snapshot].
        pub fn set_snapshot<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.snapshot = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSnapshots::get][crate::client::RegionSnapshots::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_snapshots::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_snapshots::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_snapshots::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Snapshot> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_snapshots::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_snapshots::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [snapshot][crate::model::region_snapshots::GetRequest::snapshot].
        pub fn set_snapshot<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.snapshot = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSnapshots::get_iam_policy][crate::client::RegionSnapshots::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_snapshots::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(RequestBuilder<crate::model::region_snapshots::GetIamPolicyRequest>);

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_snapshots::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::region_snapshots::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::region_snapshots::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_snapshots::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_snapshots::GetIamPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_snapshots::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSnapshots::insert][crate::client::RegionSnapshots::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_snapshots::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::region_snapshots::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_snapshots::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionSnapshots::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_snapshots::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_snapshots::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_snapshots::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_snapshots::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_snapshots::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Snapshot>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_snapshots::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Snapshot>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSnapshots::list][crate::client::RegionSnapshots::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_snapshots::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_snapshots::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_snapshots::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SnapshotList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::SnapshotList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::SnapshotList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_snapshots::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_snapshots::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_snapshots::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_snapshots::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_snapshots::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_snapshots::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_snapshots::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_snapshots::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_snapshots::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_snapshots::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_snapshots::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_snapshots::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSnapshots::set_iam_policy][crate::client::RegionSnapshots::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_snapshots::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(RequestBuilder<crate::model::region_snapshots::SetIamPolicyRequest>);

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_snapshots::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_snapshots::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_snapshots::SetIamPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_snapshots::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_snapshots::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_snapshots::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSnapshots::set_labels][crate::client::RegionSnapshots::set_labels] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_snapshots::SetLabels;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetLabels {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetLabels(RequestBuilder<crate::model::region_snapshots::SetLabelsRequest>);

    impl SetLabels {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_snapshots::SetLabelsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_labels(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_labels`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionSnapshots::set_labels::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_snapshots::SetLabelsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_snapshots::SetLabelsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_snapshots::SetLabelsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_snapshots::SetLabelsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [resource][crate::model::region_snapshots::SetLabelsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_snapshots::SetLabelsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetLabelsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_snapshots::SetLabelsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetLabelsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetLabels {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSnapshots::test_iam_permissions][crate::client::RegionSnapshots::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_snapshots::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::region_snapshots::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_snapshots::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_snapshots::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_snapshots::TestIamPermissionsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::region_snapshots::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_snapshots::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_snapshots::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSnapshots::update_kms_key][crate::client::RegionSnapshots::update_kms_key] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_snapshots::UpdateKmsKey;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateKmsKey {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateKmsKey(RequestBuilder<crate::model::region_snapshots::UpdateKmsKeyRequest>);

    impl UpdateKmsKey {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_snapshots::UpdateKmsKeyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update_kms_key(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update_kms_key`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionSnapshots::update_kms_key::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_snapshots::UpdateKmsKeyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_snapshots::UpdateKmsKeyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_snapshots::UpdateKmsKeyRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_snapshots::UpdateKmsKeyRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [snapshot][crate::model::region_snapshots::UpdateKmsKeyRequest::snapshot].
        pub fn set_snapshot<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.snapshot = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_snapshots::UpdateKmsKeyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionSnapshotUpdateKmsKeyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_snapshots::UpdateKmsKeyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionSnapshotUpdateKmsKeyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateKmsKey {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSnapshots::get_operation][crate::client::RegionSnapshots::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_snapshots::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSnapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionSslCertificates][crate::client::RegionSslCertificates].
#[cfg(feature = "region-ssl-certificates")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-ssl-certificates")))]
pub mod region_ssl_certificates {
    use crate::Result;

    /// A builder for [RegionSslCertificates][crate::client::RegionSslCertificates].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_ssl_certificates::ClientBuilder;
    /// # use client::RegionSslCertificates;
    /// let builder : ClientBuilder = RegionSslCertificates::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionSslCertificates;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionSslCertificates;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionSslCertificates] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSslCertificates>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSslCertificates>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionSslCertificates::delete][crate::client::RegionSslCertificates::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_ssl_certificates::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::region_ssl_certificates::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSslCertificates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_ssl_certificates::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionSslCertificates::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_ssl_certificates::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_ssl_certificates::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_ssl_certificates::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_ssl_certificates::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [ssl_certificate][crate::model::region_ssl_certificates::DeleteRequest::ssl_certificate].
        pub fn set_ssl_certificate<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.ssl_certificate = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSslCertificates::get][crate::client::RegionSslCertificates::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_ssl_certificates::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_ssl_certificates::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSslCertificates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_ssl_certificates::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SslCertificate> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_ssl_certificates::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_ssl_certificates::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [ssl_certificate][crate::model::region_ssl_certificates::GetRequest::ssl_certificate].
        pub fn set_ssl_certificate<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.ssl_certificate = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSslCertificates::insert][crate::client::RegionSslCertificates::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_ssl_certificates::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::region_ssl_certificates::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSslCertificates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_ssl_certificates::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionSslCertificates::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_ssl_certificates::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_ssl_certificates::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_ssl_certificates::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_ssl_certificates::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_ssl_certificates::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SslCertificate>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_ssl_certificates::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SslCertificate>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSslCertificates::list][crate::client::RegionSslCertificates::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_ssl_certificates::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_ssl_certificates::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSslCertificates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_ssl_certificates::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SslCertificateList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::SslCertificateList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::SslCertificateList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_ssl_certificates::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_ssl_certificates::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_ssl_certificates::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_ssl_certificates::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_ssl_certificates::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_ssl_certificates::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_ssl_certificates::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_ssl_certificates::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_ssl_certificates::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_ssl_certificates::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_ssl_certificates::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_ssl_certificates::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSslCertificates::get_operation][crate::client::RegionSslCertificates::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_ssl_certificates::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSslCertificates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionSslPolicies][crate::client::RegionSslPolicies].
#[cfg(feature = "region-ssl-policies")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-ssl-policies")))]
pub mod region_ssl_policies {
    use crate::Result;

    /// A builder for [RegionSslPolicies][crate::client::RegionSslPolicies].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_ssl_policies::ClientBuilder;
    /// # use client::RegionSslPolicies;
    /// let builder : ClientBuilder = RegionSslPolicies::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionSslPolicies;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionSslPolicies;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionSslPolicies] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSslPolicies>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSslPolicies>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionSslPolicies::delete][crate::client::RegionSslPolicies::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_ssl_policies::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::region_ssl_policies::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSslPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_ssl_policies::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionSslPolicies::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_ssl_policies::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_ssl_policies::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_ssl_policies::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_ssl_policies::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [ssl_policy][crate::model::region_ssl_policies::DeleteRequest::ssl_policy].
        pub fn set_ssl_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.ssl_policy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSslPolicies::get][crate::client::RegionSslPolicies::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_ssl_policies::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_ssl_policies::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSslPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_ssl_policies::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SslPolicy> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_ssl_policies::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_ssl_policies::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [ssl_policy][crate::model::region_ssl_policies::GetRequest::ssl_policy].
        pub fn set_ssl_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.ssl_policy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSslPolicies::insert][crate::client::RegionSslPolicies::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_ssl_policies::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::region_ssl_policies::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSslPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_ssl_policies::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionSslPolicies::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_ssl_policies::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_ssl_policies::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_ssl_policies::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_ssl_policies::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_ssl_policies::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SslPolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_ssl_policies::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SslPolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSslPolicies::list][crate::client::RegionSslPolicies::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_ssl_policies::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_ssl_policies::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSslPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_ssl_policies::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SslPoliciesList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::SslPoliciesList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::SslPoliciesList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_ssl_policies::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_ssl_policies::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_ssl_policies::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_ssl_policies::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_ssl_policies::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_ssl_policies::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_ssl_policies::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_ssl_policies::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_ssl_policies::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_ssl_policies::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_ssl_policies::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_ssl_policies::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSslPolicies::list_available_features][crate::client::RegionSslPolicies::list_available_features] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_ssl_policies::ListAvailableFeatures;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListAvailableFeatures {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListAvailableFeatures(
        RequestBuilder<crate::model::region_ssl_policies::ListAvailableFeaturesRequest>,
    );

    impl ListAvailableFeatures {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSslPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_ssl_policies::ListAvailableFeaturesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SslPoliciesListAvailableFeaturesResponse> {
            (*self.0.stub)
                .list_available_features(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [filter][crate::model::region_ssl_policies::ListAvailableFeaturesRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_ssl_policies::ListAvailableFeaturesRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_ssl_policies::ListAvailableFeaturesRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_ssl_policies::ListAvailableFeaturesRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_ssl_policies::ListAvailableFeaturesRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_ssl_policies::ListAvailableFeaturesRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_ssl_policies::ListAvailableFeaturesRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_ssl_policies::ListAvailableFeaturesRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_ssl_policies::ListAvailableFeaturesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_ssl_policies::ListAvailableFeaturesRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_ssl_policies::ListAvailableFeaturesRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_ssl_policies::ListAvailableFeaturesRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListAvailableFeatures {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSslPolicies::patch][crate::client::RegionSslPolicies::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_ssl_policies::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::region_ssl_policies::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSslPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_ssl_policies::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionSslPolicies::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_ssl_policies::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_ssl_policies::PatchRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_ssl_policies::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_ssl_policies::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [ssl_policy][crate::model::region_ssl_policies::PatchRequest::ssl_policy].
        pub fn set_ssl_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.ssl_policy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_ssl_policies::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SslPolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_ssl_policies::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SslPolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionSslPolicies::get_operation][crate::client::RegionSslPolicies::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_ssl_policies::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionSslPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionTargetHttpProxies][crate::client::RegionTargetHttpProxies].
#[cfg(feature = "region-target-http-proxies")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-target-http-proxies")))]
pub mod region_target_http_proxies {
    use crate::Result;

    /// A builder for [RegionTargetHttpProxies][crate::client::RegionTargetHttpProxies].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_target_http_proxies::ClientBuilder;
    /// # use client::RegionTargetHttpProxies;
    /// let builder : ClientBuilder = RegionTargetHttpProxies::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionTargetHttpProxies;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionTargetHttpProxies;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionTargetHttpProxies] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionTargetHttpProxies>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionTargetHttpProxies>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionTargetHttpProxies::delete][crate::client::RegionTargetHttpProxies::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_target_http_proxies::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::region_target_http_proxies::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionTargetHttpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_target_http_proxies::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionTargetHttpProxies::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_target_http_proxies::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_target_http_proxies::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_target_http_proxies::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_target_http_proxies::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_http_proxy][crate::model::region_target_http_proxies::DeleteRequest::target_http_proxy].
        pub fn set_target_http_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_http_proxy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionTargetHttpProxies::get][crate::client::RegionTargetHttpProxies::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_target_http_proxies::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_target_http_proxies::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionTargetHttpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_target_http_proxies::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetHttpProxy> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_target_http_proxies::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_target_http_proxies::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [target_http_proxy][crate::model::region_target_http_proxies::GetRequest::target_http_proxy].
        pub fn set_target_http_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_http_proxy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionTargetHttpProxies::insert][crate::client::RegionTargetHttpProxies::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_target_http_proxies::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::region_target_http_proxies::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionTargetHttpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_target_http_proxies::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionTargetHttpProxies::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_target_http_proxies::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_target_http_proxies::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_target_http_proxies::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_target_http_proxies::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_target_http_proxies::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetHttpProxy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_target_http_proxies::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetHttpProxy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionTargetHttpProxies::list][crate::client::RegionTargetHttpProxies::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_target_http_proxies::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_target_http_proxies::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionTargetHttpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_target_http_proxies::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetHttpProxyList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::TargetHttpProxyList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::TargetHttpProxyList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_target_http_proxies::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_target_http_proxies::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_target_http_proxies::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_target_http_proxies::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_target_http_proxies::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_target_http_proxies::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_target_http_proxies::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_target_http_proxies::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_target_http_proxies::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_target_http_proxies::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_target_http_proxies::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_target_http_proxies::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionTargetHttpProxies::set_url_map][crate::client::RegionTargetHttpProxies::set_url_map] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_target_http_proxies::SetUrlMap;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetUrlMap {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetUrlMap(
        RequestBuilder<crate::model::region_target_http_proxies::SetUrlMapRequest>,
    );

    impl SetUrlMap {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionTargetHttpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_target_http_proxies::SetUrlMapRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_url_map(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_url_map`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionTargetHttpProxies::set_url_map::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_target_http_proxies::SetUrlMapRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_target_http_proxies::SetUrlMapRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_target_http_proxies::SetUrlMapRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_target_http_proxies::SetUrlMapRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_http_proxy][crate::model::region_target_http_proxies::SetUrlMapRequest::target_http_proxy].
        pub fn set_target_http_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_http_proxy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_target_http_proxies::SetUrlMapRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::UrlMapReference>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_target_http_proxies::SetUrlMapRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::UrlMapReference>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetUrlMap {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionTargetHttpProxies::get_operation][crate::client::RegionTargetHttpProxies::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_target_http_proxies::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionTargetHttpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionTargetHttpsProxies][crate::client::RegionTargetHttpsProxies].
#[cfg(feature = "region-target-https-proxies")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-target-https-proxies")))]
pub mod region_target_https_proxies {
    use crate::Result;

    /// A builder for [RegionTargetHttpsProxies][crate::client::RegionTargetHttpsProxies].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_target_https_proxies::ClientBuilder;
    /// # use client::RegionTargetHttpsProxies;
    /// let builder : ClientBuilder = RegionTargetHttpsProxies::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionTargetHttpsProxies;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionTargetHttpsProxies;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionTargetHttpsProxies] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionTargetHttpsProxies>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionTargetHttpsProxies>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionTargetHttpsProxies::delete][crate::client::RegionTargetHttpsProxies::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_target_https_proxies::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::region_target_https_proxies::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionTargetHttpsProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_target_https_proxies::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionTargetHttpsProxies::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_target_https_proxies::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_target_https_proxies::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_target_https_proxies::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_target_https_proxies::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_https_proxy][crate::model::region_target_https_proxies::DeleteRequest::target_https_proxy].
        pub fn set_target_https_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_https_proxy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionTargetHttpsProxies::get][crate::client::RegionTargetHttpsProxies::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_target_https_proxies::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_target_https_proxies::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionTargetHttpsProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_target_https_proxies::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetHttpsProxy> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_target_https_proxies::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_target_https_proxies::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [target_https_proxy][crate::model::region_target_https_proxies::GetRequest::target_https_proxy].
        pub fn set_target_https_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_https_proxy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionTargetHttpsProxies::insert][crate::client::RegionTargetHttpsProxies::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_target_https_proxies::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::region_target_https_proxies::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionTargetHttpsProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_target_https_proxies::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionTargetHttpsProxies::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_target_https_proxies::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_target_https_proxies::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_target_https_proxies::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_target_https_proxies::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_target_https_proxies::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetHttpsProxy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_target_https_proxies::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetHttpsProxy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionTargetHttpsProxies::list][crate::client::RegionTargetHttpsProxies::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_target_https_proxies::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_target_https_proxies::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionTargetHttpsProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_target_https_proxies::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetHttpsProxyList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::TargetHttpsProxyList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::TargetHttpsProxyList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_target_https_proxies::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_target_https_proxies::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_target_https_proxies::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_target_https_proxies::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_target_https_proxies::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_target_https_proxies::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_target_https_proxies::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_target_https_proxies::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_target_https_proxies::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_target_https_proxies::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_target_https_proxies::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_target_https_proxies::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionTargetHttpsProxies::patch][crate::client::RegionTargetHttpsProxies::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_target_https_proxies::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::region_target_https_proxies::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionTargetHttpsProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_target_https_proxies::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionTargetHttpsProxies::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_target_https_proxies::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_target_https_proxies::PatchRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_target_https_proxies::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_target_https_proxies::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_https_proxy][crate::model::region_target_https_proxies::PatchRequest::target_https_proxy].
        pub fn set_target_https_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_https_proxy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_target_https_proxies::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetHttpsProxy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_target_https_proxies::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetHttpsProxy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionTargetHttpsProxies::set_ssl_certificates][crate::client::RegionTargetHttpsProxies::set_ssl_certificates] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_target_https_proxies::SetSslCertificates;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetSslCertificates {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetSslCertificates(
        RequestBuilder<crate::model::region_target_https_proxies::SetSslCertificatesRequest>,
    );

    impl SetSslCertificates {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionTargetHttpsProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_target_https_proxies::SetSslCertificatesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_ssl_certificates(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_ssl_certificates`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionTargetHttpsProxies::set_ssl_certificates::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_target_https_proxies::SetSslCertificatesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_target_https_proxies::SetSslCertificatesRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_target_https_proxies::SetSslCertificatesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_target_https_proxies::SetSslCertificatesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_https_proxy][crate::model::region_target_https_proxies::SetSslCertificatesRequest::target_https_proxy].
        pub fn set_target_https_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_https_proxy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_target_https_proxies::SetSslCertificatesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionTargetHttpsProxiesSetSslCertificatesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_target_https_proxies::SetSslCertificatesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionTargetHttpsProxiesSetSslCertificatesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetSslCertificates {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionTargetHttpsProxies::set_url_map][crate::client::RegionTargetHttpsProxies::set_url_map] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_target_https_proxies::SetUrlMap;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetUrlMap {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetUrlMap(
        RequestBuilder<crate::model::region_target_https_proxies::SetUrlMapRequest>,
    );

    impl SetUrlMap {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionTargetHttpsProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::region_target_https_proxies::SetUrlMapRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_url_map(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_url_map`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::RegionTargetHttpsProxies::set_url_map::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_target_https_proxies::SetUrlMapRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_target_https_proxies::SetUrlMapRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_target_https_proxies::SetUrlMapRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_target_https_proxies::SetUrlMapRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_https_proxy][crate::model::region_target_https_proxies::SetUrlMapRequest::target_https_proxy].
        pub fn set_target_https_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_https_proxy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_target_https_proxies::SetUrlMapRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::UrlMapReference>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_target_https_proxies::SetUrlMapRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::UrlMapReference>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetUrlMap {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionTargetHttpsProxies::get_operation][crate::client::RegionTargetHttpsProxies::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_target_https_proxies::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionTargetHttpsProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionTargetTcpProxies][crate::client::RegionTargetTcpProxies].
#[cfg(feature = "region-target-tcp-proxies")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-target-tcp-proxies")))]
pub mod region_target_tcp_proxies {
    use crate::Result;

    /// A builder for [RegionTargetTcpProxies][crate::client::RegionTargetTcpProxies].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_target_tcp_proxies::ClientBuilder;
    /// # use client::RegionTargetTcpProxies;
    /// let builder : ClientBuilder = RegionTargetTcpProxies::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionTargetTcpProxies;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionTargetTcpProxies;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionTargetTcpProxies] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionTargetTcpProxies>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionTargetTcpProxies>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionTargetTcpProxies::delete][crate::client::RegionTargetTcpProxies::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_target_tcp_proxies::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::region_target_tcp_proxies::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionTargetTcpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_target_tcp_proxies::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionTargetTcpProxies::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_target_tcp_proxies::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_target_tcp_proxies::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_target_tcp_proxies::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_target_tcp_proxies::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_tcp_proxy][crate::model::region_target_tcp_proxies::DeleteRequest::target_tcp_proxy].
        pub fn set_target_tcp_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_tcp_proxy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionTargetTcpProxies::get][crate::client::RegionTargetTcpProxies::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_target_tcp_proxies::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_target_tcp_proxies::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionTargetTcpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_target_tcp_proxies::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetTcpProxy> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_target_tcp_proxies::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_target_tcp_proxies::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [target_tcp_proxy][crate::model::region_target_tcp_proxies::GetRequest::target_tcp_proxy].
        pub fn set_target_tcp_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_tcp_proxy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionTargetTcpProxies::insert][crate::client::RegionTargetTcpProxies::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_target_tcp_proxies::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::region_target_tcp_proxies::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionTargetTcpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_target_tcp_proxies::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionTargetTcpProxies::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_target_tcp_proxies::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_target_tcp_proxies::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_target_tcp_proxies::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_target_tcp_proxies::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_target_tcp_proxies::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetTcpProxy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_target_tcp_proxies::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetTcpProxy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionTargetTcpProxies::list][crate::client::RegionTargetTcpProxies::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_target_tcp_proxies::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_target_tcp_proxies::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionTargetTcpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_target_tcp_proxies::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetTcpProxyList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::TargetTcpProxyList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::TargetTcpProxyList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_target_tcp_proxies::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_target_tcp_proxies::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_target_tcp_proxies::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_target_tcp_proxies::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_target_tcp_proxies::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_target_tcp_proxies::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_target_tcp_proxies::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_target_tcp_proxies::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_target_tcp_proxies::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_target_tcp_proxies::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_target_tcp_proxies::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_target_tcp_proxies::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionTargetTcpProxies::get_operation][crate::client::RegionTargetTcpProxies::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_target_tcp_proxies::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionTargetTcpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionUrlMaps][crate::client::RegionUrlMaps].
#[cfg(feature = "region-url-maps")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-url-maps")))]
pub mod region_url_maps {
    use crate::Result;

    /// A builder for [RegionUrlMaps][crate::client::RegionUrlMaps].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_url_maps::ClientBuilder;
    /// # use client::RegionUrlMaps;
    /// let builder : ClientBuilder = RegionUrlMaps::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionUrlMaps;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionUrlMaps;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionUrlMaps] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionUrlMaps>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionUrlMaps>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionUrlMaps::delete][crate::client::RegionUrlMaps::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_url_maps::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::region_url_maps::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionUrlMaps>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_url_maps::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionUrlMaps::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_url_maps::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_url_maps::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_url_maps::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_url_maps::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [url_map][crate::model::region_url_maps::DeleteRequest::url_map].
        pub fn set_url_map<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.url_map = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionUrlMaps::get][crate::client::RegionUrlMaps::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_url_maps::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::region_url_maps::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionUrlMaps>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_url_maps::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::UrlMap> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_url_maps::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_url_maps::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [url_map][crate::model::region_url_maps::GetRequest::url_map].
        pub fn set_url_map<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.url_map = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionUrlMaps::insert][crate::client::RegionUrlMaps::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_url_maps::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::region_url_maps::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionUrlMaps>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_url_maps::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionUrlMaps::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_url_maps::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_url_maps::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_url_maps::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_url_maps::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::region_url_maps::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::UrlMap>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_url_maps::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::UrlMap>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionUrlMaps::list][crate::client::RegionUrlMaps::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_url_maps::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_url_maps::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionUrlMaps>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_url_maps::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::UrlMapList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::UrlMapList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::UrlMapList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_url_maps::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_url_maps::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_url_maps::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_url_maps::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_url_maps::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_url_maps::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_url_maps::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_url_maps::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_url_maps::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_url_maps::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_url_maps::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_url_maps::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionUrlMaps::patch][crate::client::RegionUrlMaps::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_url_maps::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::region_url_maps::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionUrlMaps>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_url_maps::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionUrlMaps::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_url_maps::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_url_maps::PatchRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_url_maps::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_url_maps::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [url_map][crate::model::region_url_maps::PatchRequest::url_map].
        pub fn set_url_map<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.url_map = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_url_maps::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::UrlMap>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_url_maps::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::UrlMap>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionUrlMaps::update][crate::client::RegionUrlMaps::update] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_url_maps::Update;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Update {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Update(RequestBuilder<crate::model::region_url_maps::UpdateRequest>);

    impl Update {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionUrlMaps>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_url_maps::UpdateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RegionUrlMaps::update::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::region_url_maps::UpdateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_url_maps::UpdateRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::region_url_maps::UpdateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::region_url_maps::UpdateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [url_map][crate::model::region_url_maps::UpdateRequest::url_map].
        pub fn set_url_map<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.url_map = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_url_maps::UpdateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::UrlMap>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_url_maps::UpdateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::UrlMap>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Update {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionUrlMaps::validate][crate::client::RegionUrlMaps::validate] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_url_maps::Validate;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Validate {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Validate(RequestBuilder<crate::model::region_url_maps::ValidateRequest>);

    impl Validate {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionUrlMaps>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_url_maps::ValidateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::UrlMapsValidateResponse> {
            (*self.0.stub)
                .validate(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::region_url_maps::ValidateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_url_maps::ValidateRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [url_map][crate::model::region_url_maps::ValidateRequest::url_map].
        pub fn set_url_map<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.url_map = v.into();
            self
        }

        /// Sets the value of [body][crate::model::region_url_maps::ValidateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionUrlMapsValidateRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::region_url_maps::ValidateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionUrlMapsValidateRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Validate {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RegionUrlMaps::get_operation][crate::client::RegionUrlMaps::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_url_maps::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionUrlMaps>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RegionZones][crate::client::RegionZones].
#[cfg(feature = "region-zones")]
#[cfg_attr(docsrs, doc(cfg(feature = "region-zones")))]
pub mod region_zones {
    use crate::Result;

    /// A builder for [RegionZones][crate::client::RegionZones].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::region_zones::ClientBuilder;
    /// # use client::RegionZones;
    /// let builder : ClientBuilder = RegionZones::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RegionZones;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RegionZones;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RegionZones] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionZones>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionZones>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RegionZones::list][crate::client::RegionZones::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::region_zones::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::region_zones::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RegionZones>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_zones::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ZoneList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::ZoneList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::ZoneList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::region_zones::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::region_zones::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::region_zones::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::region_zones::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::region_zones::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::region_zones::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::region_zones::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::region_zones::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::region_zones::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_zones::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::region_zones::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::region_zones::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [Regions][crate::client::Regions].
#[cfg(feature = "regions")]
#[cfg_attr(docsrs, doc(cfg(feature = "regions")))]
pub mod regions {
    use crate::Result;

    /// A builder for [Regions][crate::client::Regions].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::regions::ClientBuilder;
    /// # use client::Regions;
    /// let builder : ClientBuilder = Regions::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::Regions;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = Regions;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::Regions] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::Regions>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Regions>) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [Regions::get][crate::client::Regions::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::regions::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::regions::GetRequest>);

    impl Get {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Regions>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::regions::GetRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Region> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::regions::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::regions::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Regions::list][crate::client::Regions::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::regions::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::regions::ListRequest>);

    impl List {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Regions>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::regions::ListRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::RegionList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::RegionList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::RegionList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::regions::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::regions::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::regions::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::regions::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::regions::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::regions::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::regions::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::regions::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::regions::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::regions::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::regions::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [ReservationBlocks][crate::client::ReservationBlocks].
#[cfg(feature = "reservation-blocks")]
#[cfg_attr(docsrs, doc(cfg(feature = "reservation-blocks")))]
pub mod reservation_blocks {
    use crate::Result;

    /// A builder for [ReservationBlocks][crate::client::ReservationBlocks].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::reservation_blocks::ClientBuilder;
    /// # use client::ReservationBlocks;
    /// let builder : ClientBuilder = ReservationBlocks::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::ReservationBlocks;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = ReservationBlocks;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::ReservationBlocks] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationBlocks>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationBlocks>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [ReservationBlocks::get][crate::client::ReservationBlocks::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservation_blocks::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::reservation_blocks::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationBlocks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::reservation_blocks::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ReservationBlocksGetResponse> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::reservation_blocks::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [reservation][crate::model::reservation_blocks::GetRequest::reservation].
        pub fn set_reservation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.reservation = v.into();
            self
        }

        /// Sets the value of [reservation_block][crate::model::reservation_blocks::GetRequest::reservation_block].
        pub fn set_reservation_block<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.reservation_block = v.into();
            self
        }

        /// Sets the value of [view][crate::model::reservation_blocks::GetRequest::view].
        pub fn set_view<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::reservation_blocks::get_request::View>,
        {
            self.0.request.view = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [view][crate::model::reservation_blocks::GetRequest::view].
        pub fn set_or_clear_view<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::reservation_blocks::get_request::View>,
        {
            self.0.request.view = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::reservation_blocks::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ReservationBlocks::get_iam_policy][crate::client::ReservationBlocks::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservation_blocks::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(RequestBuilder<crate::model::reservation_blocks::GetIamPolicyRequest>);

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationBlocks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::reservation_blocks::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::reservation_blocks::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::reservation_blocks::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [parent_resource][crate::model::reservation_blocks::GetIamPolicyRequest::parent_resource].
        pub fn set_parent_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent_resource = v.into();
            self
        }

        /// Sets the value of [project][crate::model::reservation_blocks::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::reservation_blocks::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::reservation_blocks::GetIamPolicyRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ReservationBlocks::list][crate::client::ReservationBlocks::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservation_blocks::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::reservation_blocks::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationBlocks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::reservation_blocks::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ReservationBlocksListResponse> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::ReservationBlocksListResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ReservationBlocksListResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::reservation_blocks::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::reservation_blocks::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::reservation_blocks::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::reservation_blocks::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::reservation_blocks::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::reservation_blocks::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::reservation_blocks::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::reservation_blocks::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::reservation_blocks::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [reservation][crate::model::reservation_blocks::ListRequest::reservation].
        pub fn set_reservation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.reservation = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::reservation_blocks::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::reservation_blocks::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::reservation_blocks::ListRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ReservationBlocks::perform_maintenance][crate::client::ReservationBlocks::perform_maintenance] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservation_blocks::PerformMaintenance;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> PerformMaintenance {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct PerformMaintenance(
        RequestBuilder<crate::model::reservation_blocks::PerformMaintenanceRequest>,
    );

    impl PerformMaintenance {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationBlocks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::reservation_blocks::PerformMaintenanceRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .perform_maintenance(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `perform_maintenance`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::ReservationBlocks::perform_maintenance::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::reservation_blocks::PerformMaintenanceRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::reservation_blocks::PerformMaintenanceRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::reservation_blocks::PerformMaintenanceRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [reservation][crate::model::reservation_blocks::PerformMaintenanceRequest::reservation].
        pub fn set_reservation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.reservation = v.into();
            self
        }

        /// Sets the value of [reservation_block][crate::model::reservation_blocks::PerformMaintenanceRequest::reservation_block].
        pub fn set_reservation_block<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.reservation_block = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::reservation_blocks::PerformMaintenanceRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::reservation_blocks::PerformMaintenanceRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ReservationsBlocksPerformMaintenanceRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::reservation_blocks::PerformMaintenanceRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ReservationsBlocksPerformMaintenanceRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for PerformMaintenance {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ReservationBlocks::set_iam_policy][crate::client::ReservationBlocks::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservation_blocks::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(RequestBuilder<crate::model::reservation_blocks::SetIamPolicyRequest>);

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationBlocks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::reservation_blocks::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [parent_resource][crate::model::reservation_blocks::SetIamPolicyRequest::parent_resource].
        pub fn set_parent_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent_resource = v.into();
            self
        }

        /// Sets the value of [project][crate::model::reservation_blocks::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::reservation_blocks::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::reservation_blocks::SetIamPolicyRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::reservation_blocks::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ZoneSetNestedPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::reservation_blocks::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ZoneSetNestedPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ReservationBlocks::test_iam_permissions][crate::client::ReservationBlocks::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservation_blocks::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::reservation_blocks::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationBlocks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::reservation_blocks::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [parent_resource][crate::model::reservation_blocks::TestIamPermissionsRequest::parent_resource].
        pub fn set_parent_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent_resource = v.into();
            self
        }

        /// Sets the value of [project][crate::model::reservation_blocks::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::reservation_blocks::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::reservation_blocks::TestIamPermissionsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::reservation_blocks::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::reservation_blocks::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ReservationBlocks::get_operation][crate::client::ReservationBlocks::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservation_blocks::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::zone_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationBlocks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::zone_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::zone_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zone_operations::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [ReservationSlots][crate::client::ReservationSlots].
#[cfg(feature = "reservation-slots")]
#[cfg_attr(docsrs, doc(cfg(feature = "reservation-slots")))]
pub mod reservation_slots {
    use crate::Result;

    /// A builder for [ReservationSlots][crate::client::ReservationSlots].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::reservation_slots::ClientBuilder;
    /// # use client::ReservationSlots;
    /// let builder : ClientBuilder = ReservationSlots::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::ReservationSlots;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = ReservationSlots;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::ReservationSlots] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationSlots>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationSlots>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [ReservationSlots::get][crate::client::ReservationSlots::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservation_slots::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::reservation_slots::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationSlots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::reservation_slots::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ReservationSlotsGetResponse> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [parent_name][crate::model::reservation_slots::GetRequest::parent_name].
        pub fn set_parent_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent_name = v.into();
            self
        }

        /// Sets the value of [project][crate::model::reservation_slots::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [reservation_slot][crate::model::reservation_slots::GetRequest::reservation_slot].
        pub fn set_reservation_slot<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.reservation_slot = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::reservation_slots::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ReservationSlots::get_version][crate::client::ReservationSlots::get_version] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservation_slots::GetVersion;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetVersion {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetVersion(RequestBuilder<crate::model::reservation_slots::GetVersionRequest>);

    impl GetVersion {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationSlots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::reservation_slots::GetVersionRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_version(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `get_version`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::ReservationSlots::get_version::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [parent_name][crate::model::reservation_slots::GetVersionRequest::parent_name].
        pub fn set_parent_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent_name = v.into();
            self
        }

        /// Sets the value of [project][crate::model::reservation_slots::GetVersionRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::reservation_slots::GetVersionRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::reservation_slots::GetVersionRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [reservation_slot][crate::model::reservation_slots::GetVersionRequest::reservation_slot].
        pub fn set_reservation_slot<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.reservation_slot = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::reservation_slots::GetVersionRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::reservation_slots::GetVersionRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ReservationSlotsGetVersionRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::reservation_slots::GetVersionRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ReservationSlotsGetVersionRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetVersion {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ReservationSlots::list][crate::client::ReservationSlots::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservation_slots::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::reservation_slots::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationSlots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::reservation_slots::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ReservationSlotsListResponse> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::ReservationSlotsListResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ReservationSlotsListResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::reservation_slots::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::reservation_slots::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::reservation_slots::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::reservation_slots::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::reservation_slots::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::reservation_slots::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::reservation_slots::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::reservation_slots::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [parent_name][crate::model::reservation_slots::ListRequest::parent_name].
        pub fn set_parent_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent_name = v.into();
            self
        }

        /// Sets the value of [project][crate::model::reservation_slots::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::reservation_slots::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::reservation_slots::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::reservation_slots::ListRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ReservationSlots::update][crate::client::ReservationSlots::update] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservation_slots::Update;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Update {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Update(RequestBuilder<crate::model::reservation_slots::UpdateRequest>);

    impl Update {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationSlots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::reservation_slots::UpdateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::ReservationSlots::update::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [parent_name][crate::model::reservation_slots::UpdateRequest::parent_name].
        pub fn set_parent_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent_name = v.into();
            self
        }

        /// Sets the value of [project][crate::model::reservation_slots::UpdateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [reservation_slot][crate::model::reservation_slots::UpdateRequest::reservation_slot].
        pub fn set_reservation_slot<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.reservation_slot = v.into();
            self
        }

        /// Sets the value of [update_mask][crate::model::reservation_slots::UpdateRequest::update_mask].
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::reservation_slots::UpdateRequest::update_mask].
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::reservation_slots::UpdateRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::reservation_slots::UpdateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ReservationSlot>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::reservation_slots::UpdateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ReservationSlot>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Update {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ReservationSlots::get_operation][crate::client::ReservationSlots::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservation_slots::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::zone_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationSlots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::zone_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::zone_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zone_operations::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [ReservationSubBlocks][crate::client::ReservationSubBlocks].
#[cfg(feature = "reservation-sub-blocks")]
#[cfg_attr(docsrs, doc(cfg(feature = "reservation-sub-blocks")))]
pub mod reservation_sub_blocks {
    use crate::Result;

    /// A builder for [ReservationSubBlocks][crate::client::ReservationSubBlocks].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::reservation_sub_blocks::ClientBuilder;
    /// # use client::ReservationSubBlocks;
    /// let builder : ClientBuilder = ReservationSubBlocks::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::ReservationSubBlocks;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = ReservationSubBlocks;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::ReservationSubBlocks] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationSubBlocks>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationSubBlocks>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [ReservationSubBlocks::get][crate::client::ReservationSubBlocks::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservation_sub_blocks::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::reservation_sub_blocks::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationSubBlocks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::reservation_sub_blocks::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ReservationSubBlocksGetResponse> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [parent_name][crate::model::reservation_sub_blocks::GetRequest::parent_name].
        pub fn set_parent_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent_name = v.into();
            self
        }

        /// Sets the value of [project][crate::model::reservation_sub_blocks::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [reservation_sub_block][crate::model::reservation_sub_blocks::GetRequest::reservation_sub_block].
        pub fn set_reservation_sub_block<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.reservation_sub_block = v.into();
            self
        }

        /// Sets the value of [view][crate::model::reservation_sub_blocks::GetRequest::view].
        pub fn set_view<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::reservation_sub_blocks::get_request::View>,
        {
            self.0.request.view = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [view][crate::model::reservation_sub_blocks::GetRequest::view].
        pub fn set_or_clear_view<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::reservation_sub_blocks::get_request::View>,
        {
            self.0.request.view = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::reservation_sub_blocks::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ReservationSubBlocks::get_iam_policy][crate::client::ReservationSubBlocks::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservation_sub_blocks::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(
        RequestBuilder<crate::model::reservation_sub_blocks::GetIamPolicyRequest>,
    );

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationSubBlocks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::reservation_sub_blocks::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::reservation_sub_blocks::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::reservation_sub_blocks::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [parent_resource][crate::model::reservation_sub_blocks::GetIamPolicyRequest::parent_resource].
        pub fn set_parent_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent_resource = v.into();
            self
        }

        /// Sets the value of [project][crate::model::reservation_sub_blocks::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::reservation_sub_blocks::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::reservation_sub_blocks::GetIamPolicyRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ReservationSubBlocks::get_version][crate::client::ReservationSubBlocks::get_version] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservation_sub_blocks::GetVersion;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetVersion {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetVersion(RequestBuilder<crate::model::reservation_sub_blocks::GetVersionRequest>);

    impl GetVersion {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationSubBlocks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::reservation_sub_blocks::GetVersionRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_version(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `get_version`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::ReservationSubBlocks::get_version::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [parent_name][crate::model::reservation_sub_blocks::GetVersionRequest::parent_name].
        pub fn set_parent_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent_name = v.into();
            self
        }

        /// Sets the value of [project][crate::model::reservation_sub_blocks::GetVersionRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::reservation_sub_blocks::GetVersionRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::reservation_sub_blocks::GetVersionRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [reservation_sub_block][crate::model::reservation_sub_blocks::GetVersionRequest::reservation_sub_block].
        pub fn set_reservation_sub_block<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.reservation_sub_block = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::reservation_sub_blocks::GetVersionRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::reservation_sub_blocks::GetVersionRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ReservationSubBlocksGetVersionRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::reservation_sub_blocks::GetVersionRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ReservationSubBlocksGetVersionRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetVersion {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ReservationSubBlocks::list][crate::client::ReservationSubBlocks::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservation_sub_blocks::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::reservation_sub_blocks::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationSubBlocks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::reservation_sub_blocks::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ReservationSubBlocksListResponse> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::ReservationSubBlocksListResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ReservationSubBlocksListResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::reservation_sub_blocks::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::reservation_sub_blocks::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::reservation_sub_blocks::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::reservation_sub_blocks::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::reservation_sub_blocks::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::reservation_sub_blocks::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::reservation_sub_blocks::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::reservation_sub_blocks::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [parent_name][crate::model::reservation_sub_blocks::ListRequest::parent_name].
        pub fn set_parent_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent_name = v.into();
            self
        }

        /// Sets the value of [project][crate::model::reservation_sub_blocks::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::reservation_sub_blocks::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::reservation_sub_blocks::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::reservation_sub_blocks::ListRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ReservationSubBlocks::perform_maintenance][crate::client::ReservationSubBlocks::perform_maintenance] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservation_sub_blocks::PerformMaintenance;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> PerformMaintenance {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct PerformMaintenance(
        RequestBuilder<crate::model::reservation_sub_blocks::PerformMaintenanceRequest>,
    );

    impl PerformMaintenance {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationSubBlocks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::reservation_sub_blocks::PerformMaintenanceRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .perform_maintenance(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `perform_maintenance`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::ReservationSubBlocks::perform_maintenance::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [parent_name][crate::model::reservation_sub_blocks::PerformMaintenanceRequest::parent_name].
        pub fn set_parent_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent_name = v.into();
            self
        }

        /// Sets the value of [project][crate::model::reservation_sub_blocks::PerformMaintenanceRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::reservation_sub_blocks::PerformMaintenanceRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::reservation_sub_blocks::PerformMaintenanceRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [reservation_sub_block][crate::model::reservation_sub_blocks::PerformMaintenanceRequest::reservation_sub_block].
        pub fn set_reservation_sub_block<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.reservation_sub_block = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::reservation_sub_blocks::PerformMaintenanceRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for PerformMaintenance {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ReservationSubBlocks::report_faulty][crate::client::ReservationSubBlocks::report_faulty] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservation_sub_blocks::ReportFaulty;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ReportFaulty {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ReportFaulty(
        RequestBuilder<crate::model::reservation_sub_blocks::ReportFaultyRequest>,
    );

    impl ReportFaulty {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationSubBlocks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::reservation_sub_blocks::ReportFaultyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .report_faulty(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `report_faulty`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::ReservationSubBlocks::report_faulty::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [parent_name][crate::model::reservation_sub_blocks::ReportFaultyRequest::parent_name].
        pub fn set_parent_name<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent_name = v.into();
            self
        }

        /// Sets the value of [project][crate::model::reservation_sub_blocks::ReportFaultyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::reservation_sub_blocks::ReportFaultyRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::reservation_sub_blocks::ReportFaultyRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [reservation_sub_block][crate::model::reservation_sub_blocks::ReportFaultyRequest::reservation_sub_block].
        pub fn set_reservation_sub_block<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.reservation_sub_block = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::reservation_sub_blocks::ReportFaultyRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::reservation_sub_blocks::ReportFaultyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ReservationSubBlocksReportFaultyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::reservation_sub_blocks::ReportFaultyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ReservationSubBlocksReportFaultyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ReportFaulty {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ReservationSubBlocks::set_iam_policy][crate::client::ReservationSubBlocks::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservation_sub_blocks::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(
        RequestBuilder<crate::model::reservation_sub_blocks::SetIamPolicyRequest>,
    );

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationSubBlocks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::reservation_sub_blocks::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [parent_resource][crate::model::reservation_sub_blocks::SetIamPolicyRequest::parent_resource].
        pub fn set_parent_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent_resource = v.into();
            self
        }

        /// Sets the value of [project][crate::model::reservation_sub_blocks::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::reservation_sub_blocks::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::reservation_sub_blocks::SetIamPolicyRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::reservation_sub_blocks::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ZoneSetNestedPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::reservation_sub_blocks::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ZoneSetNestedPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ReservationSubBlocks::test_iam_permissions][crate::client::ReservationSubBlocks::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservation_sub_blocks::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::reservation_sub_blocks::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationSubBlocks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::reservation_sub_blocks::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [parent_resource][crate::model::reservation_sub_blocks::TestIamPermissionsRequest::parent_resource].
        pub fn set_parent_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.parent_resource = v.into();
            self
        }

        /// Sets the value of [project][crate::model::reservation_sub_blocks::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::reservation_sub_blocks::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::reservation_sub_blocks::TestIamPermissionsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::reservation_sub_blocks::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::reservation_sub_blocks::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ReservationSubBlocks::get_operation][crate::client::ReservationSubBlocks::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservation_sub_blocks::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::zone_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ReservationSubBlocks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::zone_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::zone_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zone_operations::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [Reservations][crate::client::Reservations].
#[cfg(feature = "reservations")]
#[cfg_attr(docsrs, doc(cfg(feature = "reservations")))]
pub mod reservations {
    use crate::Result;

    /// A builder for [Reservations][crate::client::Reservations].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::reservations::ClientBuilder;
    /// # use client::Reservations;
    /// let builder : ClientBuilder = Reservations::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::Reservations;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = Reservations;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::Reservations] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::Reservations>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Reservations>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [Reservations::aggregated_list][crate::client::Reservations::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservations::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(RequestBuilder<crate::model::reservations::AggregatedListRequest>);

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Reservations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::reservations::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ReservationAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::ReservationAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ReservationAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::reservations::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::reservations::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::reservations::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::reservations::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::reservations::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::reservations::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::reservations::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::reservations::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::reservations::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::reservations::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::reservations::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::reservations::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::reservations::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::reservations::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::reservations::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Reservations::delete][crate::client::Reservations::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservations::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::reservations::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Reservations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::reservations::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Reservations::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::reservations::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::reservations::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::reservations::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [reservation][crate::model::reservations::DeleteRequest::reservation].
        pub fn set_reservation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.reservation = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::reservations::DeleteRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Reservations::get][crate::client::Reservations::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservations::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::reservations::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Reservations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::reservations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Reservation> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::reservations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [reservation][crate::model::reservations::GetRequest::reservation].
        pub fn set_reservation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.reservation = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::reservations::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Reservations::get_iam_policy][crate::client::Reservations::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservations::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(RequestBuilder<crate::model::reservations::GetIamPolicyRequest>);

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Reservations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::reservations::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::reservations::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::reservations::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::reservations::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::reservations::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::reservations::GetIamPolicyRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Reservations::insert][crate::client::Reservations::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservations::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::reservations::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Reservations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::reservations::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Reservations::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::reservations::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::reservations::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::reservations::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::reservations::InsertRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::reservations::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Reservation>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::reservations::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Reservation>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Reservations::list][crate::client::Reservations::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservations::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::reservations::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Reservations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::reservations::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ReservationList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::ReservationList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::ReservationList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::reservations::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::reservations::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::reservations::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::reservations::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::reservations::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::reservations::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::reservations::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::reservations::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::reservations::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::reservations::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::reservations::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::reservations::ListRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Reservations::perform_maintenance][crate::client::Reservations::perform_maintenance] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservations::PerformMaintenance;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> PerformMaintenance {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct PerformMaintenance(
        RequestBuilder<crate::model::reservations::PerformMaintenanceRequest>,
    );

    impl PerformMaintenance {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Reservations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::reservations::PerformMaintenanceRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .perform_maintenance(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `perform_maintenance`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Reservations::perform_maintenance::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::reservations::PerformMaintenanceRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::reservations::PerformMaintenanceRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::reservations::PerformMaintenanceRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [reservation][crate::model::reservations::PerformMaintenanceRequest::reservation].
        pub fn set_reservation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.reservation = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::reservations::PerformMaintenanceRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::reservations::PerformMaintenanceRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ReservationsPerformMaintenanceRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::reservations::PerformMaintenanceRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ReservationsPerformMaintenanceRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for PerformMaintenance {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Reservations::resize][crate::client::Reservations::resize] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservations::Resize;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Resize {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Resize(RequestBuilder<crate::model::reservations::ResizeRequest>);

    impl Resize {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Reservations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::reservations::ResizeRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .resize(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `resize`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Reservations::resize::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::reservations::ResizeRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::reservations::ResizeRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::reservations::ResizeRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [reservation][crate::model::reservations::ResizeRequest::reservation].
        pub fn set_reservation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.reservation = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::reservations::ResizeRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::reservations::ResizeRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ReservationsResizeRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::reservations::ResizeRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ReservationsResizeRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Resize {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Reservations::set_iam_policy][crate::client::Reservations::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservations::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(RequestBuilder<crate::model::reservations::SetIamPolicyRequest>);

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Reservations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::reservations::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::reservations::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::reservations::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::reservations::SetIamPolicyRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::reservations::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ZoneSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::reservations::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ZoneSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Reservations::test_iam_permissions][crate::client::Reservations::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservations::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::reservations::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Reservations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::reservations::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::reservations::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::reservations::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::reservations::TestIamPermissionsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::reservations::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::reservations::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Reservations::update][crate::client::Reservations::update] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservations::Update;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Update {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Update(RequestBuilder<crate::model::reservations::UpdateRequest>);

    impl Update {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Reservations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::reservations::UpdateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Reservations::update::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [paths][crate::model::reservations::UpdateRequest::paths].
        pub fn set_paths<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.paths = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [paths][crate::model::reservations::UpdateRequest::paths].
        pub fn set_or_clear_paths<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.paths = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::reservations::UpdateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::reservations::UpdateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::reservations::UpdateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [reservation][crate::model::reservations::UpdateRequest::reservation].
        pub fn set_reservation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.reservation = v.into();
            self
        }

        /// Sets the value of [update_mask][crate::model::reservations::UpdateRequest::update_mask].
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::reservations::UpdateRequest::update_mask].
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::reservations::UpdateRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::reservations::UpdateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Reservation>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::reservations::UpdateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Reservation>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Update {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Reservations::get_operation][crate::client::Reservations::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::reservations::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::zone_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Reservations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::zone_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::zone_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zone_operations::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [ResourcePolicies][crate::client::ResourcePolicies].
#[cfg(feature = "resource-policies")]
#[cfg_attr(docsrs, doc(cfg(feature = "resource-policies")))]
pub mod resource_policies {
    use crate::Result;

    /// A builder for [ResourcePolicies][crate::client::ResourcePolicies].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::resource_policies::ClientBuilder;
    /// # use client::ResourcePolicies;
    /// let builder : ClientBuilder = ResourcePolicies::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::ResourcePolicies;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = ResourcePolicies;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::ResourcePolicies] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::ResourcePolicies>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ResourcePolicies>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [ResourcePolicies::aggregated_list][crate::client::ResourcePolicies::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::resource_policies::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::resource_policies::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ResourcePolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::resource_policies::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ResourcePolicyAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::ResourcePolicyAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ResourcePolicyAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::resource_policies::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::resource_policies::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::resource_policies::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::resource_policies::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::resource_policies::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::resource_policies::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::resource_policies::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::resource_policies::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::resource_policies::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::resource_policies::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::resource_policies::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::resource_policies::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::resource_policies::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::resource_policies::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::resource_policies::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ResourcePolicies::delete][crate::client::ResourcePolicies::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::resource_policies::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::resource_policies::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ResourcePolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::resource_policies::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::ResourcePolicies::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::resource_policies::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::resource_policies::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::resource_policies::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::resource_policies::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [resource_policy][crate::model::resource_policies::DeleteRequest::resource_policy].
        pub fn set_resource_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource_policy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ResourcePolicies::get][crate::client::ResourcePolicies::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::resource_policies::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::resource_policies::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ResourcePolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::resource_policies::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ResourcePolicy> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::resource_policies::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::resource_policies::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource_policy][crate::model::resource_policies::GetRequest::resource_policy].
        pub fn set_resource_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource_policy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ResourcePolicies::get_iam_policy][crate::client::ResourcePolicies::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::resource_policies::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(RequestBuilder<crate::model::resource_policies::GetIamPolicyRequest>);

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ResourcePolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::resource_policies::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::resource_policies::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::resource_policies::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::resource_policies::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::resource_policies::GetIamPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::resource_policies::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ResourcePolicies::insert][crate::client::ResourcePolicies::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::resource_policies::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::resource_policies::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ResourcePolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::resource_policies::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::ResourcePolicies::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::resource_policies::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::resource_policies::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::resource_policies::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::resource_policies::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::resource_policies::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ResourcePolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::resource_policies::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ResourcePolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ResourcePolicies::list][crate::client::ResourcePolicies::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::resource_policies::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::resource_policies::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ResourcePolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::resource_policies::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ResourcePolicyList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::ResourcePolicyList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ResourcePolicyList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::resource_policies::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::resource_policies::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::resource_policies::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::resource_policies::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::resource_policies::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::resource_policies::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::resource_policies::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::resource_policies::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::resource_policies::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::resource_policies::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::resource_policies::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::resource_policies::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ResourcePolicies::patch][crate::client::ResourcePolicies::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::resource_policies::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::resource_policies::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ResourcePolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::resource_policies::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::ResourcePolicies::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::resource_policies::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::resource_policies::PatchRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::resource_policies::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::resource_policies::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [resource_policy][crate::model::resource_policies::PatchRequest::resource_policy].
        pub fn set_resource_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource_policy = v.into();
            self
        }

        /// Sets the value of [update_mask][crate::model::resource_policies::PatchRequest::update_mask].
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::resource_policies::PatchRequest::update_mask].
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::resource_policies::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ResourcePolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::resource_policies::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ResourcePolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ResourcePolicies::set_iam_policy][crate::client::ResourcePolicies::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::resource_policies::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(RequestBuilder<crate::model::resource_policies::SetIamPolicyRequest>);

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ResourcePolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::resource_policies::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::resource_policies::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::resource_policies::SetIamPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::resource_policies::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::resource_policies::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::resource_policies::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ResourcePolicies::test_iam_permissions][crate::client::ResourcePolicies::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::resource_policies::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::resource_policies::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ResourcePolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::resource_policies::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::resource_policies::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::resource_policies::TestIamPermissionsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::resource_policies::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::resource_policies::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::resource_policies::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ResourcePolicies::get_operation][crate::client::ResourcePolicies::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::resource_policies::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ResourcePolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [RolloutPlans][crate::client::RolloutPlans].
#[cfg(feature = "rollout-plans")]
#[cfg_attr(docsrs, doc(cfg(feature = "rollout-plans")))]
pub mod rollout_plans {
    use crate::Result;

    /// A builder for [RolloutPlans][crate::client::RolloutPlans].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::rollout_plans::ClientBuilder;
    /// # use client::RolloutPlans;
    /// let builder : ClientBuilder = RolloutPlans::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::RolloutPlans;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = RolloutPlans;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::RolloutPlans] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::RolloutPlans>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RolloutPlans>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [RolloutPlans::delete][crate::client::RolloutPlans::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::rollout_plans::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::rollout_plans::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RolloutPlans>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::rollout_plans::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RolloutPlans::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::rollout_plans::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::rollout_plans::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::rollout_plans::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [rollout_plan][crate::model::rollout_plans::DeleteRequest::rollout_plan].
        pub fn set_rollout_plan<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.rollout_plan = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RolloutPlans::get][crate::client::RolloutPlans::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::rollout_plans::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::rollout_plans::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RolloutPlans>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::rollout_plans::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::RolloutPlan> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::rollout_plans::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [rollout_plan][crate::model::rollout_plans::GetRequest::rollout_plan].
        pub fn set_rollout_plan<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.rollout_plan = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RolloutPlans::insert][crate::client::RolloutPlans::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::rollout_plans::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::rollout_plans::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RolloutPlans>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::rollout_plans::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::RolloutPlans::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::rollout_plans::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::rollout_plans::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::rollout_plans::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::rollout_plans::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RolloutPlan>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::rollout_plans::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RolloutPlan>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RolloutPlans::list][crate::client::RolloutPlans::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::rollout_plans::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::rollout_plans::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RolloutPlans>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::rollout_plans::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::RolloutPlansListResponse> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::RolloutPlansListResponse,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::RolloutPlansListResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::rollout_plans::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::rollout_plans::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::rollout_plans::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::rollout_plans::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::rollout_plans::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::rollout_plans::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::rollout_plans::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::rollout_plans::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::rollout_plans::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::rollout_plans::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::rollout_plans::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [RolloutPlans::get_operation][crate::client::RolloutPlans::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::rollout_plans::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::RolloutPlans>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [Rollouts][crate::client::Rollouts].
#[cfg(feature = "rollouts")]
#[cfg_attr(docsrs, doc(cfg(feature = "rollouts")))]
pub mod rollouts {
    use crate::Result;

    /// A builder for [Rollouts][crate::client::Rollouts].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::rollouts::ClientBuilder;
    /// # use client::Rollouts;
    /// let builder : ClientBuilder = Rollouts::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::Rollouts;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = Rollouts;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::Rollouts] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::Rollouts>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Rollouts>) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [Rollouts::advance][crate::client::Rollouts::advance] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::rollouts::Advance;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Advance {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Advance(RequestBuilder<crate::model::rollouts::AdvanceRequest>);

    impl Advance {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Rollouts>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::rollouts::AdvanceRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .advance(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `advance`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Rollouts::advance::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [current_wave_number][crate::model::rollouts::AdvanceRequest::current_wave_number].
        pub fn set_current_wave_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.current_wave_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [current_wave_number][crate::model::rollouts::AdvanceRequest::current_wave_number].
        pub fn set_or_clear_current_wave_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.current_wave_number = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::rollouts::AdvanceRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::rollouts::AdvanceRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::rollouts::AdvanceRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [rollout][crate::model::rollouts::AdvanceRequest::rollout].
        pub fn set_rollout<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.rollout = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Advance {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Rollouts::cancel][crate::client::Rollouts::cancel] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::rollouts::Cancel;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Cancel {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Cancel(RequestBuilder<crate::model::rollouts::CancelRequest>);

    impl Cancel {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Rollouts>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::rollouts::CancelRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .cancel(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `cancel`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Rollouts::cancel::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::rollouts::CancelRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::rollouts::CancelRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::rollouts::CancelRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [rollback][crate::model::rollouts::CancelRequest::rollback].
        pub fn set_rollback<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.rollback = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [rollback][crate::model::rollouts::CancelRequest::rollback].
        pub fn set_or_clear_rollback<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.rollback = v.map(|x| x.into());
            self
        }

        /// Sets the value of [rollout][crate::model::rollouts::CancelRequest::rollout].
        pub fn set_rollout<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.rollout = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Cancel {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Rollouts::delete][crate::client::Rollouts::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::rollouts::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::rollouts::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Rollouts>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::rollouts::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Rollouts::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::rollouts::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::rollouts::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::rollouts::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [rollout][crate::model::rollouts::DeleteRequest::rollout].
        pub fn set_rollout<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.rollout = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Rollouts::get][crate::client::Rollouts::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::rollouts::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::rollouts::GetRequest>);

    impl Get {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Rollouts>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::rollouts::GetRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Rollout> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::rollouts::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [rollout][crate::model::rollouts::GetRequest::rollout].
        pub fn set_rollout<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.rollout = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Rollouts::list][crate::client::Rollouts::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::rollouts::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::rollouts::ListRequest>);

    impl List {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Rollouts>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::rollouts::ListRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::RolloutsListResponse> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::RolloutsListResponse, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::RolloutsListResponse,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::rollouts::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::rollouts::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::rollouts::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::rollouts::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::rollouts::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::rollouts::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::rollouts::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::rollouts::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::rollouts::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::rollouts::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::rollouts::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Rollouts::pause][crate::client::Rollouts::pause] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::rollouts::Pause;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Pause {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Pause(RequestBuilder<crate::model::rollouts::PauseRequest>);

    impl Pause {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Rollouts>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::rollouts::PauseRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .pause(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `pause`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Rollouts::pause::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [etag][crate::model::rollouts::PauseRequest::etag].
        pub fn set_etag<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.etag = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [etag][crate::model::rollouts::PauseRequest::etag].
        pub fn set_or_clear_etag<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.etag = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::rollouts::PauseRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::rollouts::PauseRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::rollouts::PauseRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [rollout][crate::model::rollouts::PauseRequest::rollout].
        pub fn set_rollout<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.rollout = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Pause {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Rollouts::resume][crate::client::Rollouts::resume] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::rollouts::Resume;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Resume {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Resume(RequestBuilder<crate::model::rollouts::ResumeRequest>);

    impl Resume {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Rollouts>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::rollouts::ResumeRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .resume(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `resume`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Rollouts::resume::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [etag][crate::model::rollouts::ResumeRequest::etag].
        pub fn set_etag<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.etag = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [etag][crate::model::rollouts::ResumeRequest::etag].
        pub fn set_or_clear_etag<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.etag = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::rollouts::ResumeRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::rollouts::ResumeRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::rollouts::ResumeRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [rollout][crate::model::rollouts::ResumeRequest::rollout].
        pub fn set_rollout<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.rollout = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Resume {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Rollouts::get_operation][crate::client::Rollouts::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::rollouts::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Rollouts>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [Routers][crate::client::Routers].
#[cfg(feature = "routers")]
#[cfg_attr(docsrs, doc(cfg(feature = "routers")))]
pub mod routers {
    use crate::Result;

    /// A builder for [Routers][crate::client::Routers].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::routers::ClientBuilder;
    /// # use client::Routers;
    /// let builder : ClientBuilder = Routers::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::Routers;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = Routers;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::Routers] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::Routers>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routers>) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [Routers::aggregated_list][crate::client::Routers::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routers::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(RequestBuilder<crate::model::routers::AggregatedListRequest>);

    impl AggregatedList {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routers>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routers::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::RouterAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::RouterAggregatedList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::RouterAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::routers::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::routers::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::routers::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::routers::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::routers::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::routers::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::routers::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::routers::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::routers::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::routers::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::routers::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::routers::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::routers::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::routers::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::routers::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routers::delete][crate::client::Routers::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routers::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::routers::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routers>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routers::DeleteRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Routers::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::routers::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::routers::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::routers::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::routers::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [router][crate::model::routers::DeleteRequest::router].
        pub fn set_router<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.router = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routers::delete_named_set][crate::client::Routers::delete_named_set] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routers::DeleteNamedSet;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeleteNamedSet {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeleteNamedSet(RequestBuilder<crate::model::routers::DeleteNamedSetRequest>);

    impl DeleteNamedSet {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routers>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routers::DeleteNamedSetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete_named_set(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete_named_set`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Routers::delete_named_set::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [named_set][crate::model::routers::DeleteNamedSetRequest::named_set].
        pub fn set_named_set<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.named_set = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [named_set][crate::model::routers::DeleteNamedSetRequest::named_set].
        pub fn set_or_clear_named_set<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.named_set = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::routers::DeleteNamedSetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::routers::DeleteNamedSetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::routers::DeleteNamedSetRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::routers::DeleteNamedSetRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [router][crate::model::routers::DeleteNamedSetRequest::router].
        pub fn set_router<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.router = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeleteNamedSet {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routers::delete_route_policy][crate::client::Routers::delete_route_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routers::DeleteRoutePolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> DeleteRoutePolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct DeleteRoutePolicy(RequestBuilder<crate::model::routers::DeleteRoutePolicyRequest>);

    impl DeleteRoutePolicy {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routers>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routers::DeleteRoutePolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete_route_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete_route_policy`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Routers::delete_route_policy::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [policy][crate::model::routers::DeleteRoutePolicyRequest::policy].
        pub fn set_policy<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.policy = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [policy][crate::model::routers::DeleteRoutePolicyRequest::policy].
        pub fn set_or_clear_policy<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.policy = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::routers::DeleteRoutePolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::routers::DeleteRoutePolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::routers::DeleteRoutePolicyRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::routers::DeleteRoutePolicyRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [router][crate::model::routers::DeleteRoutePolicyRequest::router].
        pub fn set_router<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.router = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for DeleteRoutePolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routers::get][crate::client::Routers::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routers::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::routers::GetRequest>);

    impl Get {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routers>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routers::GetRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Router> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::routers::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::routers::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [router][crate::model::routers::GetRequest::router].
        pub fn set_router<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.router = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routers::get_named_set][crate::client::Routers::get_named_set] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routers::GetNamedSet;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetNamedSet {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetNamedSet(RequestBuilder<crate::model::routers::GetNamedSetRequest>);

    impl GetNamedSet {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routers>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routers::GetNamedSetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::RoutersGetNamedSetResponse> {
            (*self.0.stub)
                .get_named_set(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [named_set][crate::model::routers::GetNamedSetRequest::named_set].
        pub fn set_named_set<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.named_set = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [named_set][crate::model::routers::GetNamedSetRequest::named_set].
        pub fn set_or_clear_named_set<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.named_set = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::routers::GetNamedSetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::routers::GetNamedSetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [router][crate::model::routers::GetNamedSetRequest::router].
        pub fn set_router<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.router = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetNamedSet {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routers::get_nat_ip_info][crate::client::Routers::get_nat_ip_info] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routers::GetNatIpInfo;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetNatIpInfo {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetNatIpInfo(RequestBuilder<crate::model::routers::GetNatIpInfoRequest>);

    impl GetNatIpInfo {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routers>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routers::GetNatIpInfoRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::NatIpInfoResponse> {
            (*self.0.stub)
                .get_nat_ip_info(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [nat_name][crate::model::routers::GetNatIpInfoRequest::nat_name].
        pub fn set_nat_name<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.nat_name = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [nat_name][crate::model::routers::GetNatIpInfoRequest::nat_name].
        pub fn set_or_clear_nat_name<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.nat_name = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::routers::GetNatIpInfoRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::routers::GetNatIpInfoRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [router][crate::model::routers::GetNatIpInfoRequest::router].
        pub fn set_router<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.router = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetNatIpInfo {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routers::get_nat_mapping_info][crate::client::Routers::get_nat_mapping_info] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routers::GetNatMappingInfo;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetNatMappingInfo {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetNatMappingInfo(RequestBuilder<crate::model::routers::GetNatMappingInfoRequest>);

    impl GetNatMappingInfo {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routers>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routers::GetNatMappingInfoRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::VmEndpointNatMappingsList> {
            (*self.0.stub)
                .get_nat_mapping_info(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::VmEndpointNatMappingsList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::VmEndpointNatMappingsList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::routers::GetNatMappingInfoRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::routers::GetNatMappingInfoRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::routers::GetNatMappingInfoRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::routers::GetNatMappingInfoRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [nat_name][crate::model::routers::GetNatMappingInfoRequest::nat_name].
        pub fn set_nat_name<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.nat_name = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [nat_name][crate::model::routers::GetNatMappingInfoRequest::nat_name].
        pub fn set_or_clear_nat_name<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.nat_name = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::routers::GetNatMappingInfoRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::routers::GetNatMappingInfoRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::routers::GetNatMappingInfoRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::routers::GetNatMappingInfoRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::routers::GetNatMappingInfoRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::routers::GetNatMappingInfoRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::routers::GetNatMappingInfoRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::routers::GetNatMappingInfoRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [router][crate::model::routers::GetNatMappingInfoRequest::router].
        pub fn set_router<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.router = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetNatMappingInfo {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routers::get_route_policy][crate::client::Routers::get_route_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routers::GetRoutePolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetRoutePolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetRoutePolicy(RequestBuilder<crate::model::routers::GetRoutePolicyRequest>);

    impl GetRoutePolicy {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routers>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routers::GetRoutePolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::RoutersGetRoutePolicyResponse> {
            (*self.0.stub)
                .get_route_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [policy][crate::model::routers::GetRoutePolicyRequest::policy].
        pub fn set_policy<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.policy = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [policy][crate::model::routers::GetRoutePolicyRequest::policy].
        pub fn set_or_clear_policy<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.policy = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::routers::GetRoutePolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::routers::GetRoutePolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [router][crate::model::routers::GetRoutePolicyRequest::router].
        pub fn set_router<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.router = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetRoutePolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routers::get_router_status][crate::client::Routers::get_router_status] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routers::GetRouterStatus;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetRouterStatus {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetRouterStatus(RequestBuilder<crate::model::routers::GetRouterStatusRequest>);

    impl GetRouterStatus {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routers>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routers::GetRouterStatusRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::RouterStatusResponse> {
            (*self.0.stub)
                .get_router_status(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::routers::GetRouterStatusRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::routers::GetRouterStatusRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [router][crate::model::routers::GetRouterStatusRequest::router].
        pub fn set_router<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.router = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetRouterStatus {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routers::insert][crate::client::Routers::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routers::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::routers::InsertRequest>);

    impl Insert {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routers>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routers::InsertRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Routers::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::routers::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::routers::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::routers::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::routers::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::routers::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Router>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::routers::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Router>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routers::list][crate::client::Routers::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routers::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::routers::ListRequest>);

    impl List {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routers>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routers::ListRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::RouterList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::RouterList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::RouterList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::routers::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::routers::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::routers::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::routers::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::routers::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::routers::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::routers::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::routers::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::routers::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::routers::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::routers::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::routers::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routers::list_bgp_routes][crate::client::Routers::list_bgp_routes] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routers::ListBgpRoutes;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListBgpRoutes {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListBgpRoutes(RequestBuilder<crate::model::routers::ListBgpRoutesRequest>);

    impl ListBgpRoutes {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routers>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routers::ListBgpRoutesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::RoutersListBgpRoutes> {
            (*self.0.stub)
                .list_bgp_routes(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::RoutersListBgpRoutes, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::RoutersListBgpRoutes,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [address_family][crate::model::routers::ListBgpRoutesRequest::address_family].
        pub fn set_address_family<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::routers::list_bgp_routes_request::AddressFamily>,
        {
            self.0.request.address_family = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [address_family][crate::model::routers::ListBgpRoutesRequest::address_family].
        pub fn set_or_clear_address_family<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::routers::list_bgp_routes_request::AddressFamily>,
        {
            self.0.request.address_family = v.map(|x| x.into());
            self
        }

        /// Sets the value of [destination_prefix][crate::model::routers::ListBgpRoutesRequest::destination_prefix].
        pub fn set_destination_prefix<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.destination_prefix = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [destination_prefix][crate::model::routers::ListBgpRoutesRequest::destination_prefix].
        pub fn set_or_clear_destination_prefix<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.destination_prefix = v.map(|x| x.into());
            self
        }

        /// Sets the value of [filter][crate::model::routers::ListBgpRoutesRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::routers::ListBgpRoutesRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::routers::ListBgpRoutesRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::routers::ListBgpRoutesRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::routers::ListBgpRoutesRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::routers::ListBgpRoutesRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::routers::ListBgpRoutesRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::routers::ListBgpRoutesRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [peer][crate::model::routers::ListBgpRoutesRequest::peer].
        pub fn set_peer<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.peer = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [peer][crate::model::routers::ListBgpRoutesRequest::peer].
        pub fn set_or_clear_peer<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.peer = v.map(|x| x.into());
            self
        }

        /// Sets the value of [policy_applied][crate::model::routers::ListBgpRoutesRequest::policy_applied].
        pub fn set_policy_applied<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.policy_applied = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [policy_applied][crate::model::routers::ListBgpRoutesRequest::policy_applied].
        pub fn set_or_clear_policy_applied<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.policy_applied = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::routers::ListBgpRoutesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::routers::ListBgpRoutesRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::routers::ListBgpRoutesRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::routers::ListBgpRoutesRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [route_type][crate::model::routers::ListBgpRoutesRequest::route_type].
        pub fn set_route_type<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::routers::list_bgp_routes_request::RouteType>,
        {
            self.0.request.route_type = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [route_type][crate::model::routers::ListBgpRoutesRequest::route_type].
        pub fn set_or_clear_route_type<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::routers::list_bgp_routes_request::RouteType>,
        {
            self.0.request.route_type = v.map(|x| x.into());
            self
        }

        /// Sets the value of [router][crate::model::routers::ListBgpRoutesRequest::router].
        pub fn set_router<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.router = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListBgpRoutes {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routers::list_named_sets][crate::client::Routers::list_named_sets] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routers::ListNamedSets;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListNamedSets {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListNamedSets(RequestBuilder<crate::model::routers::ListNamedSetsRequest>);

    impl ListNamedSets {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routers>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routers::ListNamedSetsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::RoutersListNamedSets> {
            (*self.0.stub)
                .list_named_sets(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::RoutersListNamedSets, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::RoutersListNamedSets,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::routers::ListNamedSetsRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::routers::ListNamedSetsRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::routers::ListNamedSetsRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::routers::ListNamedSetsRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::routers::ListNamedSetsRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::routers::ListNamedSetsRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::routers::ListNamedSetsRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::routers::ListNamedSetsRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::routers::ListNamedSetsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::routers::ListNamedSetsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::routers::ListNamedSetsRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::routers::ListNamedSetsRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [router][crate::model::routers::ListNamedSetsRequest::router].
        pub fn set_router<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.router = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListNamedSets {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routers::list_route_policies][crate::client::Routers::list_route_policies] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routers::ListRoutePolicies;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListRoutePolicies {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListRoutePolicies(RequestBuilder<crate::model::routers::ListRoutePoliciesRequest>);

    impl ListRoutePolicies {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routers>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routers::ListRoutePoliciesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::RoutersListRoutePolicies> {
            (*self.0.stub)
                .list_route_policies(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::RoutersListRoutePolicies,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::RoutersListRoutePolicies,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::routers::ListRoutePoliciesRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::routers::ListRoutePoliciesRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::routers::ListRoutePoliciesRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::routers::ListRoutePoliciesRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::routers::ListRoutePoliciesRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::routers::ListRoutePoliciesRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::routers::ListRoutePoliciesRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::routers::ListRoutePoliciesRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::routers::ListRoutePoliciesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::routers::ListRoutePoliciesRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::routers::ListRoutePoliciesRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::routers::ListRoutePoliciesRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [router][crate::model::routers::ListRoutePoliciesRequest::router].
        pub fn set_router<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.router = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListRoutePolicies {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routers::patch][crate::client::Routers::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routers::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::routers::PatchRequest>);

    impl Patch {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routers>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routers::PatchRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Routers::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::routers::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::routers::PatchRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::routers::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::routers::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [router][crate::model::routers::PatchRequest::router].
        pub fn set_router<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.router = v.into();
            self
        }

        /// Sets the value of [body][crate::model::routers::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Router>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::routers::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Router>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routers::patch_named_set][crate::client::Routers::patch_named_set] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routers::PatchNamedSet;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> PatchNamedSet {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct PatchNamedSet(RequestBuilder<crate::model::routers::PatchNamedSetRequest>);

    impl PatchNamedSet {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routers>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routers::PatchNamedSetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch_named_set(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch_named_set`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Routers::patch_named_set::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::routers::PatchNamedSetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::routers::PatchNamedSetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::routers::PatchNamedSetRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::routers::PatchNamedSetRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [router][crate::model::routers::PatchNamedSetRequest::router].
        pub fn set_router<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.router = v.into();
            self
        }

        /// Sets the value of [body][crate::model::routers::PatchNamedSetRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NamedSet>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::routers::PatchNamedSetRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NamedSet>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for PatchNamedSet {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routers::patch_route_policy][crate::client::Routers::patch_route_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routers::PatchRoutePolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> PatchRoutePolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct PatchRoutePolicy(RequestBuilder<crate::model::routers::PatchRoutePolicyRequest>);

    impl PatchRoutePolicy {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routers>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routers::PatchRoutePolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch_route_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch_route_policy`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Routers::patch_route_policy::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::routers::PatchRoutePolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::routers::PatchRoutePolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::routers::PatchRoutePolicyRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::routers::PatchRoutePolicyRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [router][crate::model::routers::PatchRoutePolicyRequest::router].
        pub fn set_router<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.router = v.into();
            self
        }

        /// Sets the value of [body][crate::model::routers::PatchRoutePolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RoutePolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::routers::PatchRoutePolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RoutePolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for PatchRoutePolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routers::preview][crate::client::Routers::preview] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routers::Preview;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Preview {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Preview(RequestBuilder<crate::model::routers::PreviewRequest>);

    impl Preview {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routers>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routers::PreviewRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::RoutersPreviewResponse> {
            (*self.0.stub)
                .preview(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::routers::PreviewRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::routers::PreviewRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [router][crate::model::routers::PreviewRequest::router].
        pub fn set_router<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.router = v.into();
            self
        }

        /// Sets the value of [body][crate::model::routers::PreviewRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Router>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::routers::PreviewRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Router>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Preview {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routers::update][crate::client::Routers::update] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routers::Update;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Update {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Update(RequestBuilder<crate::model::routers::UpdateRequest>);

    impl Update {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routers>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routers::UpdateRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Routers::update::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::routers::UpdateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::routers::UpdateRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::routers::UpdateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::routers::UpdateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [router][crate::model::routers::UpdateRequest::router].
        pub fn set_router<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.router = v.into();
            self
        }

        /// Sets the value of [body][crate::model::routers::UpdateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Router>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::routers::UpdateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Router>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Update {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routers::update_named_set][crate::client::Routers::update_named_set] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routers::UpdateNamedSet;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateNamedSet {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateNamedSet(RequestBuilder<crate::model::routers::UpdateNamedSetRequest>);

    impl UpdateNamedSet {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routers>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routers::UpdateNamedSetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update_named_set(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update_named_set`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Routers::update_named_set::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::routers::UpdateNamedSetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::routers::UpdateNamedSetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::routers::UpdateNamedSetRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::routers::UpdateNamedSetRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [router][crate::model::routers::UpdateNamedSetRequest::router].
        pub fn set_router<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.router = v.into();
            self
        }

        /// Sets the value of [body][crate::model::routers::UpdateNamedSetRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::NamedSet>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::routers::UpdateNamedSetRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::NamedSet>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateNamedSet {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routers::update_route_policy][crate::client::Routers::update_route_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routers::UpdateRoutePolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateRoutePolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateRoutePolicy(RequestBuilder<crate::model::routers::UpdateRoutePolicyRequest>);

    impl UpdateRoutePolicy {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routers>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routers::UpdateRoutePolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update_route_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update_route_policy`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Routers::update_route_policy::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::routers::UpdateRoutePolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::routers::UpdateRoutePolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::routers::UpdateRoutePolicyRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::routers::UpdateRoutePolicyRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [router][crate::model::routers::UpdateRoutePolicyRequest::router].
        pub fn set_router<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.router = v.into();
            self
        }

        /// Sets the value of [body][crate::model::routers::UpdateRoutePolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RoutePolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::routers::UpdateRoutePolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RoutePolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateRoutePolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routers::get_operation][crate::client::Routers::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routers::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routers>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [Routes][crate::client::Routes].
#[cfg(feature = "routes")]
#[cfg_attr(docsrs, doc(cfg(feature = "routes")))]
pub mod routes {
    use crate::Result;

    /// A builder for [Routes][crate::client::Routes].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::routes::ClientBuilder;
    /// # use client::Routes;
    /// let builder : ClientBuilder = Routes::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::Routes;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = Routes;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::Routes] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::Routes>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routes>) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [Routes::delete][crate::client::Routes::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routes::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::routes::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routes>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routes::DeleteRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Routes::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::routes::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::routes::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::routes::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [route][crate::model::routes::DeleteRequest::route].
        pub fn set_route<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.route = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routes::get][crate::client::Routes::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routes::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::routes::GetRequest>);

    impl Get {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routes>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routes::GetRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Route> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::routes::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [route][crate::model::routes::GetRequest::route].
        pub fn set_route<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.route = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routes::insert][crate::client::Routes::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routes::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::routes::InsertRequest>);

    impl Insert {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routes>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routes::InsertRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Routes::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::routes::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::routes::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::routes::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::routes::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Route>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::routes::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Route>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routes::list][crate::client::Routes::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routes::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::routes::ListRequest>);

    impl List {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routes>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routes::ListRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::RouteList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::RouteList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::RouteList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::routes::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::routes::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::routes::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::routes::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::routes::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::routes::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::routes::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::routes::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::routes::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::routes::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::routes::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routes::test_iam_permissions][crate::client::Routes::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routes::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(RequestBuilder<crate::model::routes::TestIamPermissionsRequest>);

    impl TestIamPermissions {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routes>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::routes::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::routes::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::routes::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::routes::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::routes::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Routes::get_operation][crate::client::Routes::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::routes::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Routes>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [SecurityPolicies][crate::client::SecurityPolicies].
#[cfg(feature = "security-policies")]
#[cfg_attr(docsrs, doc(cfg(feature = "security-policies")))]
pub mod security_policies {
    use crate::Result;

    /// A builder for [SecurityPolicies][crate::client::SecurityPolicies].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::security_policies::ClientBuilder;
    /// # use client::SecurityPolicies;
    /// let builder : ClientBuilder = SecurityPolicies::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::SecurityPolicies;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = SecurityPolicies;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::SecurityPolicies] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::SecurityPolicies>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SecurityPolicies>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [SecurityPolicies::add_rule][crate::client::SecurityPolicies::add_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::security_policies::AddRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AddRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AddRule(RequestBuilder<crate::model::security_policies::AddRuleRequest>);

    impl AddRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::security_policies::AddRuleRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .add_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `add_rule`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::SecurityPolicies::add_rule::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::security_policies::AddRuleRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [security_policy][crate::model::security_policies::AddRuleRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }

        /// Sets the value of [validate_only][crate::model::security_policies::AddRuleRequest::validate_only].
        pub fn set_validate_only<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.validate_only = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [validate_only][crate::model::security_policies::AddRuleRequest::validate_only].
        pub fn set_or_clear_validate_only<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.validate_only = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::security_policies::AddRuleRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyRule>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::security_policies::AddRuleRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyRule>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AddRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SecurityPolicies::aggregated_list][crate::client::SecurityPolicies::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::security_policies::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::security_policies::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::security_policies::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SecurityPoliciesAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::SecurityPoliciesAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::SecurityPoliciesAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::security_policies::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::security_policies::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::security_policies::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::security_policies::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::security_policies::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::security_policies::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::security_policies::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::security_policies::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::security_policies::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::security_policies::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::security_policies::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::security_policies::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::security_policies::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::security_policies::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::security_policies::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SecurityPolicies::delete][crate::client::SecurityPolicies::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::security_policies::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::security_policies::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::security_policies::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::SecurityPolicies::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::security_policies::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::security_policies::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::security_policies::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [security_policy][crate::model::security_policies::DeleteRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SecurityPolicies::get][crate::client::SecurityPolicies::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::security_policies::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::security_policies::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::security_policies::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SecurityPolicy> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::security_policies::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [security_policy][crate::model::security_policies::GetRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SecurityPolicies::get_rule][crate::client::SecurityPolicies::get_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::security_policies::GetRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetRule(RequestBuilder<crate::model::security_policies::GetRuleRequest>);

    impl GetRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::security_policies::GetRuleRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SecurityPolicyRule> {
            (*self.0.stub)
                .get_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [priority][crate::model::security_policies::GetRuleRequest::priority].
        pub fn set_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [priority][crate::model::security_policies::GetRuleRequest::priority].
        pub fn set_or_clear_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::security_policies::GetRuleRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [security_policy][crate::model::security_policies::GetRuleRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SecurityPolicies::insert][crate::client::SecurityPolicies::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::security_policies::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::security_policies::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::security_policies::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::SecurityPolicies::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::security_policies::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::security_policies::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::security_policies::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [validate_only][crate::model::security_policies::InsertRequest::validate_only].
        pub fn set_validate_only<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.validate_only = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [validate_only][crate::model::security_policies::InsertRequest::validate_only].
        pub fn set_or_clear_validate_only<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.validate_only = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::security_policies::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::security_policies::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SecurityPolicies::list][crate::client::SecurityPolicies::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::security_policies::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::security_policies::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::security_policies::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SecurityPolicyList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::SecurityPolicyList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::SecurityPolicyList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::security_policies::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::security_policies::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::security_policies::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::security_policies::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::security_policies::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::security_policies::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::security_policies::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::security_policies::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::security_policies::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::security_policies::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::security_policies::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SecurityPolicies::list_preconfigured_expression_sets][crate::client::SecurityPolicies::list_preconfigured_expression_sets] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::security_policies::ListPreconfiguredExpressionSets;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListPreconfiguredExpressionSets {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListPreconfiguredExpressionSets(
        RequestBuilder<crate::model::security_policies::ListPreconfiguredExpressionSetsRequest>,
    );

    impl ListPreconfiguredExpressionSets {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::security_policies::ListPreconfiguredExpressionSetsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(
            self,
        ) -> Result<crate::model::SecurityPoliciesListPreconfiguredExpressionSetsResponse> {
            (*self.0.stub)
                .list_preconfigured_expression_sets(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [filter][crate::model::security_policies::ListPreconfiguredExpressionSetsRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::security_policies::ListPreconfiguredExpressionSetsRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::security_policies::ListPreconfiguredExpressionSetsRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::security_policies::ListPreconfiguredExpressionSetsRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::security_policies::ListPreconfiguredExpressionSetsRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::security_policies::ListPreconfiguredExpressionSetsRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::security_policies::ListPreconfiguredExpressionSetsRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::security_policies::ListPreconfiguredExpressionSetsRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::security_policies::ListPreconfiguredExpressionSetsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::security_policies::ListPreconfiguredExpressionSetsRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::security_policies::ListPreconfiguredExpressionSetsRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListPreconfiguredExpressionSets {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SecurityPolicies::patch][crate::client::SecurityPolicies::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::security_policies::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::security_policies::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::security_policies::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::SecurityPolicies::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::security_policies::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::security_policies::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::security_policies::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [security_policy][crate::model::security_policies::PatchRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }

        /// Sets the value of [update_mask][crate::model::security_policies::PatchRequest::update_mask].
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::security_policies::PatchRequest::update_mask].
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::security_policies::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::security_policies::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SecurityPolicies::patch_rule][crate::client::SecurityPolicies::patch_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::security_policies::PatchRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> PatchRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct PatchRule(RequestBuilder<crate::model::security_policies::PatchRuleRequest>);

    impl PatchRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::security_policies::PatchRuleRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch_rule`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::SecurityPolicies::patch_rule::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [priority][crate::model::security_policies::PatchRuleRequest::priority].
        pub fn set_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [priority][crate::model::security_policies::PatchRuleRequest::priority].
        pub fn set_or_clear_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::security_policies::PatchRuleRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [security_policy][crate::model::security_policies::PatchRuleRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }

        /// Sets the value of [update_mask][crate::model::security_policies::PatchRuleRequest::update_mask].
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::security_policies::PatchRuleRequest::update_mask].
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [validate_only][crate::model::security_policies::PatchRuleRequest::validate_only].
        pub fn set_validate_only<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.validate_only = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [validate_only][crate::model::security_policies::PatchRuleRequest::validate_only].
        pub fn set_or_clear_validate_only<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.validate_only = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::security_policies::PatchRuleRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyRule>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::security_policies::PatchRuleRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyRule>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for PatchRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SecurityPolicies::remove_rule][crate::client::SecurityPolicies::remove_rule] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::security_policies::RemoveRule;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> RemoveRule {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct RemoveRule(RequestBuilder<crate::model::security_policies::RemoveRuleRequest>);

    impl RemoveRule {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::security_policies::RemoveRuleRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .remove_rule(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `remove_rule`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::SecurityPolicies::remove_rule::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [priority][crate::model::security_policies::RemoveRuleRequest::priority].
        pub fn set_priority<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [priority][crate::model::security_policies::RemoveRuleRequest::priority].
        pub fn set_or_clear_priority<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.priority = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::security_policies::RemoveRuleRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [security_policy][crate::model::security_policies::RemoveRuleRequest::security_policy].
        pub fn set_security_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.security_policy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for RemoveRule {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SecurityPolicies::set_labels][crate::client::SecurityPolicies::set_labels] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::security_policies::SetLabels;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetLabels {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetLabels(RequestBuilder<crate::model::security_policies::SetLabelsRequest>);

    impl SetLabels {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::security_policies::SetLabelsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_labels(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_labels`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::SecurityPolicies::set_labels::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::security_policies::SetLabelsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::security_policies::SetLabelsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::security_policies::SetLabelsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetLabelsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::security_policies::SetLabelsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetLabelsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetLabels {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SecurityPolicies::get_operation][crate::client::SecurityPolicies::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::security_policies::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SecurityPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [ServiceAttachments][crate::client::ServiceAttachments].
#[cfg(feature = "service-attachments")]
#[cfg_attr(docsrs, doc(cfg(feature = "service-attachments")))]
pub mod service_attachments {
    use crate::Result;

    /// A builder for [ServiceAttachments][crate::client::ServiceAttachments].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::service_attachments::ClientBuilder;
    /// # use client::ServiceAttachments;
    /// let builder : ClientBuilder = ServiceAttachments::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::ServiceAttachments;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = ServiceAttachments;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::ServiceAttachments] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::ServiceAttachments>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ServiceAttachments>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [ServiceAttachments::aggregated_list][crate::client::ServiceAttachments::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::service_attachments::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::service_attachments::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ServiceAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::service_attachments::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ServiceAttachmentAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::ServiceAttachmentAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ServiceAttachmentAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::service_attachments::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::service_attachments::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::service_attachments::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::service_attachments::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::service_attachments::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::service_attachments::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::service_attachments::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::service_attachments::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::service_attachments::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::service_attachments::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::service_attachments::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::service_attachments::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::service_attachments::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::service_attachments::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::service_attachments::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ServiceAttachments::delete][crate::client::ServiceAttachments::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::service_attachments::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::service_attachments::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ServiceAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::service_attachments::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::ServiceAttachments::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::service_attachments::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::service_attachments::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::service_attachments::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::service_attachments::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_attachment][crate::model::service_attachments::DeleteRequest::service_attachment].
        pub fn set_service_attachment<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.service_attachment = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ServiceAttachments::get][crate::client::ServiceAttachments::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::service_attachments::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::service_attachments::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ServiceAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::service_attachments::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ServiceAttachment> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::service_attachments::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::service_attachments::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [service_attachment][crate::model::service_attachments::GetRequest::service_attachment].
        pub fn set_service_attachment<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.service_attachment = v.into();
            self
        }

        /// Sets the value of [show_nat_ips][crate::model::service_attachments::GetRequest::show_nat_ips].
        pub fn set_show_nat_ips<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.show_nat_ips = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [show_nat_ips][crate::model::service_attachments::GetRequest::show_nat_ips].
        pub fn set_or_clear_show_nat_ips<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.show_nat_ips = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ServiceAttachments::get_iam_policy][crate::client::ServiceAttachments::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::service_attachments::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(RequestBuilder<crate::model::service_attachments::GetIamPolicyRequest>);

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ServiceAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::service_attachments::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::service_attachments::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::service_attachments::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::service_attachments::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::service_attachments::GetIamPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::service_attachments::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ServiceAttachments::insert][crate::client::ServiceAttachments::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::service_attachments::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::service_attachments::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ServiceAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::service_attachments::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::ServiceAttachments::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::service_attachments::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::service_attachments::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::service_attachments::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::service_attachments::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::service_attachments::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ServiceAttachment>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::service_attachments::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ServiceAttachment>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ServiceAttachments::list][crate::client::ServiceAttachments::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::service_attachments::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::service_attachments::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ServiceAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::service_attachments::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ServiceAttachmentList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::ServiceAttachmentList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::ServiceAttachmentList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::service_attachments::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::service_attachments::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::service_attachments::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::service_attachments::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::service_attachments::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::service_attachments::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::service_attachments::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::service_attachments::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::service_attachments::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::service_attachments::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::service_attachments::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::service_attachments::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ServiceAttachments::patch][crate::client::ServiceAttachments::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::service_attachments::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::service_attachments::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ServiceAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::service_attachments::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::ServiceAttachments::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::service_attachments::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::service_attachments::PatchRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::service_attachments::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::service_attachments::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_attachment][crate::model::service_attachments::PatchRequest::service_attachment].
        pub fn set_service_attachment<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.service_attachment = v.into();
            self
        }

        /// Sets the value of [body][crate::model::service_attachments::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ServiceAttachment>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::service_attachments::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ServiceAttachment>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ServiceAttachments::set_iam_policy][crate::client::ServiceAttachments::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::service_attachments::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(RequestBuilder<crate::model::service_attachments::SetIamPolicyRequest>);

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ServiceAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::service_attachments::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::service_attachments::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::service_attachments::SetIamPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::service_attachments::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::service_attachments::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::service_attachments::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ServiceAttachments::test_iam_permissions][crate::client::ServiceAttachments::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::service_attachments::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::service_attachments::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ServiceAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::service_attachments::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::service_attachments::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::service_attachments::TestIamPermissionsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::service_attachments::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::service_attachments::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::service_attachments::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ServiceAttachments::get_operation][crate::client::ServiceAttachments::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::service_attachments::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ServiceAttachments>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [SnapshotSettings][crate::client::SnapshotSettings].
#[cfg(feature = "snapshot-settings")]
#[cfg_attr(docsrs, doc(cfg(feature = "snapshot-settings")))]
pub mod snapshot_settings {
    use crate::Result;

    /// A builder for [SnapshotSettings][crate::client::SnapshotSettings].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::snapshot_settings::ClientBuilder;
    /// # use client::SnapshotSettings;
    /// let builder : ClientBuilder = SnapshotSettings::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::SnapshotSettings;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = SnapshotSettings;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::SnapshotSettings] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::SnapshotSettings>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SnapshotSettings>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [SnapshotSettings::get][crate::client::SnapshotSettings::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::snapshot_settings::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::snapshot_settings::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SnapshotSettings>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::snapshot_settings::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SnapshotSettings> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::snapshot_settings::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SnapshotSettings::patch][crate::client::SnapshotSettings::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::snapshot_settings::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::snapshot_settings::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SnapshotSettings>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::snapshot_settings::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::SnapshotSettings::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::snapshot_settings::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::snapshot_settings::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::snapshot_settings::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::snapshot_settings::PatchRequest::update_mask].
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::snapshot_settings::PatchRequest::update_mask].
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::snapshot_settings::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SnapshotSettings>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::snapshot_settings::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SnapshotSettings>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SnapshotSettings::get_operation][crate::client::SnapshotSettings::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::snapshot_settings::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SnapshotSettings>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [Snapshots][crate::client::Snapshots].
#[cfg(feature = "snapshots")]
#[cfg_attr(docsrs, doc(cfg(feature = "snapshots")))]
pub mod snapshots {
    use crate::Result;

    /// A builder for [Snapshots][crate::client::Snapshots].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::snapshots::ClientBuilder;
    /// # use client::Snapshots;
    /// let builder : ClientBuilder = Snapshots::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::Snapshots;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = Snapshots;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::Snapshots] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::Snapshots>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Snapshots>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [Snapshots::delete][crate::client::Snapshots::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::snapshots::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::snapshots::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Snapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::snapshots::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Snapshots::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::snapshots::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::snapshots::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::snapshots::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [snapshot][crate::model::snapshots::DeleteRequest::snapshot].
        pub fn set_snapshot<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.snapshot = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Snapshots::get][crate::client::Snapshots::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::snapshots::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::snapshots::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Snapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::snapshots::GetRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Snapshot> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::snapshots::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [snapshot][crate::model::snapshots::GetRequest::snapshot].
        pub fn set_snapshot<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.snapshot = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Snapshots::get_iam_policy][crate::client::Snapshots::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::snapshots::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(RequestBuilder<crate::model::snapshots::GetIamPolicyRequest>);

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Snapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::snapshots::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::snapshots::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::snapshots::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::snapshots::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::snapshots::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Snapshots::insert][crate::client::Snapshots::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::snapshots::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::snapshots::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Snapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::snapshots::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Snapshots::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::snapshots::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::snapshots::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::snapshots::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::snapshots::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Snapshot>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::snapshots::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Snapshot>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Snapshots::list][crate::client::Snapshots::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::snapshots::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::snapshots::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Snapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::snapshots::ListRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SnapshotList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::SnapshotList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::SnapshotList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::snapshots::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::snapshots::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::snapshots::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::snapshots::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::snapshots::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::snapshots::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::snapshots::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::snapshots::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::snapshots::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::snapshots::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::snapshots::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Snapshots::set_iam_policy][crate::client::Snapshots::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::snapshots::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(RequestBuilder<crate::model::snapshots::SetIamPolicyRequest>);

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Snapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::snapshots::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::snapshots::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::snapshots::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::snapshots::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::snapshots::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Snapshots::set_labels][crate::client::Snapshots::set_labels] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::snapshots::SetLabels;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetLabels {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetLabels(RequestBuilder<crate::model::snapshots::SetLabelsRequest>);

    impl SetLabels {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Snapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::snapshots::SetLabelsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_labels(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_labels`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Snapshots::set_labels::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::snapshots::SetLabelsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::snapshots::SetLabelsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::snapshots::SetLabelsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetLabelsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::snapshots::SetLabelsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::GlobalSetLabelsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetLabels {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Snapshots::test_iam_permissions][crate::client::Snapshots::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::snapshots::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::snapshots::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Snapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::snapshots::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::snapshots::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::snapshots::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::snapshots::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::snapshots::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Snapshots::update_kms_key][crate::client::Snapshots::update_kms_key] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::snapshots::UpdateKmsKey;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> UpdateKmsKey {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct UpdateKmsKey(RequestBuilder<crate::model::snapshots::UpdateKmsKeyRequest>);

    impl UpdateKmsKey {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Snapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::snapshots::UpdateKmsKeyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update_kms_key(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update_kms_key`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Snapshots::update_kms_key::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::snapshots::UpdateKmsKeyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::snapshots::UpdateKmsKeyRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::snapshots::UpdateKmsKeyRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [snapshot][crate::model::snapshots::UpdateKmsKeyRequest::snapshot].
        pub fn set_snapshot<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.snapshot = v.into();
            self
        }

        /// Sets the value of [body][crate::model::snapshots::UpdateKmsKeyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SnapshotUpdateKmsKeyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::snapshots::UpdateKmsKeyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SnapshotUpdateKmsKeyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for UpdateKmsKey {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Snapshots::get_operation][crate::client::Snapshots::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::snapshots::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Snapshots>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [SslCertificates][crate::client::SslCertificates].
#[cfg(feature = "ssl-certificates")]
#[cfg_attr(docsrs, doc(cfg(feature = "ssl-certificates")))]
pub mod ssl_certificates {
    use crate::Result;

    /// A builder for [SslCertificates][crate::client::SslCertificates].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::ssl_certificates::ClientBuilder;
    /// # use client::SslCertificates;
    /// let builder : ClientBuilder = SslCertificates::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::SslCertificates;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = SslCertificates;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::SslCertificates] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::SslCertificates>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SslCertificates>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [SslCertificates::aggregated_list][crate::client::SslCertificates::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::ssl_certificates::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::ssl_certificates::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SslCertificates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ssl_certificates::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SslCertificateAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::SslCertificateAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::SslCertificateAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::ssl_certificates::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::ssl_certificates::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::ssl_certificates::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::ssl_certificates::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::ssl_certificates::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::ssl_certificates::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::ssl_certificates::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::ssl_certificates::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::ssl_certificates::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::ssl_certificates::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::ssl_certificates::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::ssl_certificates::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::ssl_certificates::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::ssl_certificates::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::ssl_certificates::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SslCertificates::delete][crate::client::SslCertificates::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::ssl_certificates::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::ssl_certificates::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SslCertificates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ssl_certificates::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::SslCertificates::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::ssl_certificates::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::ssl_certificates::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::ssl_certificates::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [ssl_certificate][crate::model::ssl_certificates::DeleteRequest::ssl_certificate].
        pub fn set_ssl_certificate<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.ssl_certificate = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SslCertificates::get][crate::client::SslCertificates::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::ssl_certificates::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::ssl_certificates::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SslCertificates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ssl_certificates::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SslCertificate> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::ssl_certificates::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [ssl_certificate][crate::model::ssl_certificates::GetRequest::ssl_certificate].
        pub fn set_ssl_certificate<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.ssl_certificate = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SslCertificates::insert][crate::client::SslCertificates::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::ssl_certificates::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::ssl_certificates::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SslCertificates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ssl_certificates::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::SslCertificates::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::ssl_certificates::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::ssl_certificates::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::ssl_certificates::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::ssl_certificates::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SslCertificate>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::ssl_certificates::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SslCertificate>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SslCertificates::list][crate::client::SslCertificates::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::ssl_certificates::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::ssl_certificates::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SslCertificates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ssl_certificates::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SslCertificateList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::SslCertificateList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::SslCertificateList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::ssl_certificates::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::ssl_certificates::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::ssl_certificates::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::ssl_certificates::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::ssl_certificates::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::ssl_certificates::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::ssl_certificates::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::ssl_certificates::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::ssl_certificates::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::ssl_certificates::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::ssl_certificates::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SslCertificates::get_operation][crate::client::SslCertificates::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::ssl_certificates::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SslCertificates>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [SslPolicies][crate::client::SslPolicies].
#[cfg(feature = "ssl-policies")]
#[cfg_attr(docsrs, doc(cfg(feature = "ssl-policies")))]
pub mod ssl_policies {
    use crate::Result;

    /// A builder for [SslPolicies][crate::client::SslPolicies].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::ssl_policies::ClientBuilder;
    /// # use client::SslPolicies;
    /// let builder : ClientBuilder = SslPolicies::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::SslPolicies;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = SslPolicies;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::SslPolicies] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::SslPolicies>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SslPolicies>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [SslPolicies::aggregated_list][crate::client::SslPolicies::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::ssl_policies::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(RequestBuilder<crate::model::ssl_policies::AggregatedListRequest>);

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SslPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ssl_policies::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SslPoliciesAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::SslPoliciesAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::SslPoliciesAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::ssl_policies::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::ssl_policies::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::ssl_policies::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::ssl_policies::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::ssl_policies::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::ssl_policies::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::ssl_policies::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::ssl_policies::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::ssl_policies::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::ssl_policies::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::ssl_policies::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::ssl_policies::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::ssl_policies::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::ssl_policies::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::ssl_policies::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SslPolicies::delete][crate::client::SslPolicies::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::ssl_policies::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::ssl_policies::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SslPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ssl_policies::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::SslPolicies::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::ssl_policies::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::ssl_policies::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::ssl_policies::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [ssl_policy][crate::model::ssl_policies::DeleteRequest::ssl_policy].
        pub fn set_ssl_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.ssl_policy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SslPolicies::get][crate::client::SslPolicies::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::ssl_policies::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::ssl_policies::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SslPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ssl_policies::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SslPolicy> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::ssl_policies::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [ssl_policy][crate::model::ssl_policies::GetRequest::ssl_policy].
        pub fn set_ssl_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.ssl_policy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SslPolicies::insert][crate::client::SslPolicies::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::ssl_policies::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::ssl_policies::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SslPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ssl_policies::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::SslPolicies::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::ssl_policies::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::ssl_policies::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::ssl_policies::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::ssl_policies::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SslPolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::ssl_policies::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SslPolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SslPolicies::list][crate::client::SslPolicies::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::ssl_policies::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::ssl_policies::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SslPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ssl_policies::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SslPoliciesList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::SslPoliciesList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::SslPoliciesList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::ssl_policies::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::ssl_policies::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::ssl_policies::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::ssl_policies::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::ssl_policies::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::ssl_policies::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::ssl_policies::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::ssl_policies::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::ssl_policies::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::ssl_policies::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::ssl_policies::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SslPolicies::list_available_features][crate::client::SslPolicies::list_available_features] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::ssl_policies::ListAvailableFeatures;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListAvailableFeatures {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListAvailableFeatures(
        RequestBuilder<crate::model::ssl_policies::ListAvailableFeaturesRequest>,
    );

    impl ListAvailableFeatures {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SslPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ssl_policies::ListAvailableFeaturesRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SslPoliciesListAvailableFeaturesResponse> {
            (*self.0.stub)
                .list_available_features(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [filter][crate::model::ssl_policies::ListAvailableFeaturesRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::ssl_policies::ListAvailableFeaturesRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::ssl_policies::ListAvailableFeaturesRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::ssl_policies::ListAvailableFeaturesRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::ssl_policies::ListAvailableFeaturesRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::ssl_policies::ListAvailableFeaturesRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::ssl_policies::ListAvailableFeaturesRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::ssl_policies::ListAvailableFeaturesRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::ssl_policies::ListAvailableFeaturesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::ssl_policies::ListAvailableFeaturesRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::ssl_policies::ListAvailableFeaturesRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListAvailableFeatures {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SslPolicies::patch][crate::client::SslPolicies::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::ssl_policies::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::ssl_policies::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SslPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::ssl_policies::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::SslPolicies::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::ssl_policies::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::ssl_policies::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::ssl_policies::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [ssl_policy][crate::model::ssl_policies::PatchRequest::ssl_policy].
        pub fn set_ssl_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.ssl_policy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::ssl_policies::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SslPolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::ssl_policies::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SslPolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [SslPolicies::get_operation][crate::client::SslPolicies::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::ssl_policies::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::SslPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [StoragePoolTypes][crate::client::StoragePoolTypes].
#[cfg(feature = "storage-pool-types")]
#[cfg_attr(docsrs, doc(cfg(feature = "storage-pool-types")))]
pub mod storage_pool_types {
    use crate::Result;

    /// A builder for [StoragePoolTypes][crate::client::StoragePoolTypes].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::storage_pool_types::ClientBuilder;
    /// # use client::StoragePoolTypes;
    /// let builder : ClientBuilder = StoragePoolTypes::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::StoragePoolTypes;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = StoragePoolTypes;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::StoragePoolTypes] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::StoragePoolTypes>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StoragePoolTypes>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [StoragePoolTypes::aggregated_list][crate::client::StoragePoolTypes::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::storage_pool_types::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::storage_pool_types::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StoragePoolTypes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::storage_pool_types::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::StoragePoolTypeAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::StoragePoolTypeAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::StoragePoolTypeAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::storage_pool_types::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::storage_pool_types::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::storage_pool_types::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::storage_pool_types::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::storage_pool_types::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::storage_pool_types::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::storage_pool_types::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::storage_pool_types::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::storage_pool_types::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::storage_pool_types::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::storage_pool_types::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::storage_pool_types::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::storage_pool_types::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::storage_pool_types::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::storage_pool_types::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StoragePoolTypes::get][crate::client::StoragePoolTypes::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::storage_pool_types::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::storage_pool_types::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StoragePoolTypes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::storage_pool_types::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::StoragePoolType> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::storage_pool_types::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [storage_pool_type][crate::model::storage_pool_types::GetRequest::storage_pool_type].
        pub fn set_storage_pool_type<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.storage_pool_type = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::storage_pool_types::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StoragePoolTypes::list][crate::client::StoragePoolTypes::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::storage_pool_types::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::storage_pool_types::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StoragePoolTypes>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::storage_pool_types::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::StoragePoolTypeList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::StoragePoolTypeList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::StoragePoolTypeList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::storage_pool_types::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::storage_pool_types::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::storage_pool_types::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::storage_pool_types::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::storage_pool_types::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::storage_pool_types::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::storage_pool_types::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::storage_pool_types::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::storage_pool_types::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::storage_pool_types::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::storage_pool_types::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::storage_pool_types::ListRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [StoragePools][crate::client::StoragePools].
#[cfg(feature = "storage-pools")]
#[cfg_attr(docsrs, doc(cfg(feature = "storage-pools")))]
pub mod storage_pools {
    use crate::Result;

    /// A builder for [StoragePools][crate::client::StoragePools].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::storage_pools::ClientBuilder;
    /// # use client::StoragePools;
    /// let builder : ClientBuilder = StoragePools::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::StoragePools;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = StoragePools;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::StoragePools] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::StoragePools>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StoragePools>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [StoragePools::aggregated_list][crate::client::StoragePools::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::storage_pools::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(RequestBuilder<crate::model::storage_pools::AggregatedListRequest>);

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StoragePools>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::storage_pools::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::StoragePoolAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::StoragePoolAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::StoragePoolAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::storage_pools::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::storage_pools::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::storage_pools::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::storage_pools::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::storage_pools::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::storage_pools::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::storage_pools::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::storage_pools::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::storage_pools::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::storage_pools::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::storage_pools::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::storage_pools::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::storage_pools::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::storage_pools::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::storage_pools::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StoragePools::delete][crate::client::StoragePools::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::storage_pools::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::storage_pools::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StoragePools>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::storage_pools::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::StoragePools::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::storage_pools::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::storage_pools::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::storage_pools::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [storage_pool][crate::model::storage_pools::DeleteRequest::storage_pool].
        pub fn set_storage_pool<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.storage_pool = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::storage_pools::DeleteRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StoragePools::get][crate::client::StoragePools::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::storage_pools::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::storage_pools::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StoragePools>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::storage_pools::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::StoragePool> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::storage_pools::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [storage_pool][crate::model::storage_pools::GetRequest::storage_pool].
        pub fn set_storage_pool<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.storage_pool = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::storage_pools::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StoragePools::get_iam_policy][crate::client::StoragePools::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::storage_pools::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(RequestBuilder<crate::model::storage_pools::GetIamPolicyRequest>);

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StoragePools>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::storage_pools::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::storage_pools::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::storage_pools::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::storage_pools::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::storage_pools::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::storage_pools::GetIamPolicyRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StoragePools::insert][crate::client::StoragePools::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::storage_pools::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::storage_pools::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StoragePools>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::storage_pools::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::StoragePools::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::storage_pools::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::storage_pools::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::storage_pools::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::storage_pools::InsertRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::storage_pools::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::StoragePool>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::storage_pools::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::StoragePool>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StoragePools::list][crate::client::StoragePools::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::storage_pools::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::storage_pools::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StoragePools>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::storage_pools::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::StoragePoolList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::StoragePoolList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::StoragePoolList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::storage_pools::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::storage_pools::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::storage_pools::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::storage_pools::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::storage_pools::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::storage_pools::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::storage_pools::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::storage_pools::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::storage_pools::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::storage_pools::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::storage_pools::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::storage_pools::ListRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StoragePools::list_disks][crate::client::StoragePools::list_disks] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::storage_pools::ListDisks;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListDisks {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListDisks(RequestBuilder<crate::model::storage_pools::ListDisksRequest>);

    impl ListDisks {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StoragePools>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::storage_pools::ListDisksRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::StoragePoolListDisks> {
            (*self.0.stub)
                .list_disks(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::StoragePoolListDisks, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::StoragePoolListDisks,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::storage_pools::ListDisksRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::storage_pools::ListDisksRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::storage_pools::ListDisksRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::storage_pools::ListDisksRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::storage_pools::ListDisksRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::storage_pools::ListDisksRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::storage_pools::ListDisksRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::storage_pools::ListDisksRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::storage_pools::ListDisksRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::storage_pools::ListDisksRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::storage_pools::ListDisksRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [storage_pool][crate::model::storage_pools::ListDisksRequest::storage_pool].
        pub fn set_storage_pool<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.storage_pool = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::storage_pools::ListDisksRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListDisks {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StoragePools::set_iam_policy][crate::client::StoragePools::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::storage_pools::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(RequestBuilder<crate::model::storage_pools::SetIamPolicyRequest>);

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StoragePools>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::storage_pools::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::storage_pools::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::storage_pools::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::storage_pools::SetIamPolicyRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::storage_pools::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::ZoneSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::storage_pools::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::ZoneSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StoragePools::test_iam_permissions][crate::client::StoragePools::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::storage_pools::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::storage_pools::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StoragePools>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::storage_pools::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::storage_pools::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::storage_pools::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::storage_pools::TestIamPermissionsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::storage_pools::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::storage_pools::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StoragePools::update][crate::client::StoragePools::update] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::storage_pools::Update;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Update {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Update(RequestBuilder<crate::model::storage_pools::UpdateRequest>);

    impl Update {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StoragePools>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::storage_pools::UpdateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::StoragePools::update::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::storage_pools::UpdateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::storage_pools::UpdateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::storage_pools::UpdateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [storage_pool][crate::model::storage_pools::UpdateRequest::storage_pool].
        pub fn set_storage_pool<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.storage_pool = v.into();
            self
        }

        /// Sets the value of [update_mask][crate::model::storage_pools::UpdateRequest::update_mask].
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::storage_pools::UpdateRequest::update_mask].
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::storage_pools::UpdateRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::storage_pools::UpdateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::StoragePool>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::storage_pools::UpdateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::StoragePool>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Update {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [StoragePools::get_operation][crate::client::StoragePools::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::storage_pools::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::zone_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::StoragePools>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::zone_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::zone_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zone_operations::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [Subnetworks][crate::client::Subnetworks].
#[cfg(feature = "subnetworks")]
#[cfg_attr(docsrs, doc(cfg(feature = "subnetworks")))]
pub mod subnetworks {
    use crate::Result;

    /// A builder for [Subnetworks][crate::client::Subnetworks].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::subnetworks::ClientBuilder;
    /// # use client::Subnetworks;
    /// let builder : ClientBuilder = Subnetworks::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::Subnetworks;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = Subnetworks;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::Subnetworks] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::Subnetworks>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Subnetworks>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [Subnetworks::aggregated_list][crate::client::Subnetworks::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::subnetworks::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(RequestBuilder<crate::model::subnetworks::AggregatedListRequest>);

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Subnetworks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::subnetworks::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SubnetworkAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::SubnetworkAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::SubnetworkAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::subnetworks::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::subnetworks::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::subnetworks::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::subnetworks::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::subnetworks::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::subnetworks::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::subnetworks::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::subnetworks::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::subnetworks::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::subnetworks::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::subnetworks::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::subnetworks::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::subnetworks::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::subnetworks::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::subnetworks::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }

        /// Sets the value of [views][crate::model::subnetworks::AggregatedListRequest::views].
        pub fn set_views<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::subnetworks::aggregated_list_request::Views>,
        {
            self.0.request.views = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [views][crate::model::subnetworks::AggregatedListRequest::views].
        pub fn set_or_clear_views<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::subnetworks::aggregated_list_request::Views>,
        {
            self.0.request.views = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Subnetworks::delete][crate::client::Subnetworks::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::subnetworks::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::subnetworks::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Subnetworks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::subnetworks::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Subnetworks::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::subnetworks::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::subnetworks::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::subnetworks::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::subnetworks::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [subnetwork][crate::model::subnetworks::DeleteRequest::subnetwork].
        pub fn set_subnetwork<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.subnetwork = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Subnetworks::expand_ip_cidr_range][crate::client::Subnetworks::expand_ip_cidr_range] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::subnetworks::ExpandIpCidrRange;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ExpandIpCidrRange {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ExpandIpCidrRange(
        RequestBuilder<crate::model::subnetworks::ExpandIpCidrRangeRequest>,
    );

    impl ExpandIpCidrRange {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Subnetworks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::subnetworks::ExpandIpCidrRangeRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .expand_ip_cidr_range(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `expand_ip_cidr_range`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Subnetworks::expand_ip_cidr_range::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::subnetworks::ExpandIpCidrRangeRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::subnetworks::ExpandIpCidrRangeRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::subnetworks::ExpandIpCidrRangeRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::subnetworks::ExpandIpCidrRangeRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [subnetwork][crate::model::subnetworks::ExpandIpCidrRangeRequest::subnetwork].
        pub fn set_subnetwork<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.subnetwork = v.into();
            self
        }

        /// Sets the value of [body][crate::model::subnetworks::ExpandIpCidrRangeRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SubnetworksExpandIpCidrRangeRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::subnetworks::ExpandIpCidrRangeRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SubnetworksExpandIpCidrRangeRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ExpandIpCidrRange {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Subnetworks::get][crate::client::Subnetworks::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::subnetworks::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::subnetworks::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Subnetworks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::subnetworks::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Subnetwork> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::subnetworks::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::subnetworks::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [subnetwork][crate::model::subnetworks::GetRequest::subnetwork].
        pub fn set_subnetwork<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.subnetwork = v.into();
            self
        }

        /// Sets the value of [views][crate::model::subnetworks::GetRequest::views].
        pub fn set_views<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::subnetworks::get_request::Views>,
        {
            self.0.request.views = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [views][crate::model::subnetworks::GetRequest::views].
        pub fn set_or_clear_views<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::subnetworks::get_request::Views>,
        {
            self.0.request.views = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Subnetworks::get_iam_policy][crate::client::Subnetworks::get_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::subnetworks::GetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetIamPolicy(RequestBuilder<crate::model::subnetworks::GetIamPolicyRequest>);

    impl GetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Subnetworks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::subnetworks::GetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .get_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [options_requested_policy_version][crate::model::subnetworks::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_options_requested_policy_version<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [options_requested_policy_version][crate::model::subnetworks::GetIamPolicyRequest::options_requested_policy_version].
        pub fn set_or_clear_options_requested_policy_version<T>(
            mut self,
            v: std::option::Option<T>,
        ) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.options_requested_policy_version = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::subnetworks::GetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::subnetworks::GetIamPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::subnetworks::GetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Subnetworks::insert][crate::client::Subnetworks::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::subnetworks::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::subnetworks::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Subnetworks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::subnetworks::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Subnetworks::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::subnetworks::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::subnetworks::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::subnetworks::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::subnetworks::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::subnetworks::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Subnetwork>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::subnetworks::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Subnetwork>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Subnetworks::list][crate::client::Subnetworks::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::subnetworks::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::subnetworks::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Subnetworks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::subnetworks::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::SubnetworkList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::SubnetworkList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::SubnetworkList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::subnetworks::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::subnetworks::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::subnetworks::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::subnetworks::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::subnetworks::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::subnetworks::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::subnetworks::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::subnetworks::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::subnetworks::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::subnetworks::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::subnetworks::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::subnetworks::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [views][crate::model::subnetworks::ListRequest::views].
        pub fn set_views<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::subnetworks::list_request::Views>,
        {
            self.0.request.views = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [views][crate::model::subnetworks::ListRequest::views].
        pub fn set_or_clear_views<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::subnetworks::list_request::Views>,
        {
            self.0.request.views = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Subnetworks::list_usable][crate::client::Subnetworks::list_usable] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::subnetworks::ListUsable;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> ListUsable {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct ListUsable(RequestBuilder<crate::model::subnetworks::ListUsableRequest>);

    impl ListUsable {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Subnetworks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::subnetworks::ListUsableRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::UsableSubnetworksAggregatedList> {
            (*self.0.stub)
                .list_usable(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::UsableSubnetworksAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::UsableSubnetworksAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::subnetworks::ListUsableRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::subnetworks::ListUsableRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::subnetworks::ListUsableRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::subnetworks::ListUsableRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::subnetworks::ListUsableRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::subnetworks::ListUsableRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::subnetworks::ListUsableRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::subnetworks::ListUsableRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::subnetworks::ListUsableRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::subnetworks::ListUsableRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::subnetworks::ListUsableRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project][crate::model::subnetworks::ListUsableRequest::service_project].
        pub fn set_service_project<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.service_project = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project][crate::model::subnetworks::ListUsableRequest::service_project].
        pub fn set_or_clear_service_project<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.service_project = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for ListUsable {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Subnetworks::patch][crate::client::Subnetworks::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::subnetworks::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::subnetworks::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Subnetworks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::subnetworks::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::Subnetworks::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [drain_timeout_seconds][crate::model::subnetworks::PatchRequest::drain_timeout_seconds].
        pub fn set_drain_timeout_seconds<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.drain_timeout_seconds = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [drain_timeout_seconds][crate::model::subnetworks::PatchRequest::drain_timeout_seconds].
        pub fn set_or_clear_drain_timeout_seconds<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i32>,
        {
            self.0.request.drain_timeout_seconds = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::subnetworks::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::subnetworks::PatchRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::subnetworks::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::subnetworks::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [subnetwork][crate::model::subnetworks::PatchRequest::subnetwork].
        pub fn set_subnetwork<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.subnetwork = v.into();
            self
        }

        /// Sets the value of [body][crate::model::subnetworks::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::Subnetwork>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::subnetworks::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::Subnetwork>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Subnetworks::set_iam_policy][crate::client::Subnetworks::set_iam_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::subnetworks::SetIamPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetIamPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetIamPolicy(RequestBuilder<crate::model::subnetworks::SetIamPolicyRequest>);

    impl SetIamPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Subnetworks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::subnetworks::SetIamPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Policy> {
            (*self.0.stub)
                .set_iam_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::subnetworks::SetIamPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::subnetworks::SetIamPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::subnetworks::SetIamPolicyRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::subnetworks::SetIamPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetPolicyRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::subnetworks::SetIamPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetPolicyRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetIamPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Subnetworks::set_private_ip_google_access][crate::client::Subnetworks::set_private_ip_google_access] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::subnetworks::SetPrivateIpGoogleAccess;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetPrivateIpGoogleAccess {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetPrivateIpGoogleAccess(
        RequestBuilder<crate::model::subnetworks::SetPrivateIpGoogleAccessRequest>,
    );

    impl SetPrivateIpGoogleAccess {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Subnetworks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::subnetworks::SetPrivateIpGoogleAccessRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_private_ip_google_access(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_private_ip_google_access`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::Subnetworks::set_private_ip_google_access::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::subnetworks::SetPrivateIpGoogleAccessRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::subnetworks::SetPrivateIpGoogleAccessRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::subnetworks::SetPrivateIpGoogleAccessRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::subnetworks::SetPrivateIpGoogleAccessRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [subnetwork][crate::model::subnetworks::SetPrivateIpGoogleAccessRequest::subnetwork].
        pub fn set_subnetwork<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.subnetwork = v.into();
            self
        }

        /// Sets the value of [body][crate::model::subnetworks::SetPrivateIpGoogleAccessRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SubnetworksSetPrivateIpGoogleAccessRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::subnetworks::SetPrivateIpGoogleAccessRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SubnetworksSetPrivateIpGoogleAccessRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetPrivateIpGoogleAccess {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Subnetworks::test_iam_permissions][crate::client::Subnetworks::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::subnetworks::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::subnetworks::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Subnetworks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::subnetworks::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::subnetworks::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::subnetworks::TestIamPermissionsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::subnetworks::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::subnetworks::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::subnetworks::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Subnetworks::get_operation][crate::client::Subnetworks::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::subnetworks::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::Subnetworks>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [TargetGrpcProxies][crate::client::TargetGrpcProxies].
#[cfg(feature = "target-grpc-proxies")]
#[cfg_attr(docsrs, doc(cfg(feature = "target-grpc-proxies")))]
pub mod target_grpc_proxies {
    use crate::Result;

    /// A builder for [TargetGrpcProxies][crate::client::TargetGrpcProxies].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::target_grpc_proxies::ClientBuilder;
    /// # use client::TargetGrpcProxies;
    /// let builder : ClientBuilder = TargetGrpcProxies::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::TargetGrpcProxies;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = TargetGrpcProxies;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::TargetGrpcProxies] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetGrpcProxies>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetGrpcProxies>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [TargetGrpcProxies::delete][crate::client::TargetGrpcProxies::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_grpc_proxies::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::target_grpc_proxies::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetGrpcProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_grpc_proxies::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetGrpcProxies::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_grpc_proxies::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_grpc_proxies::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_grpc_proxies::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_grpc_proxy][crate::model::target_grpc_proxies::DeleteRequest::target_grpc_proxy].
        pub fn set_target_grpc_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_grpc_proxy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetGrpcProxies::get][crate::client::TargetGrpcProxies::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_grpc_proxies::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::target_grpc_proxies::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetGrpcProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_grpc_proxies::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetGrpcProxy> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::target_grpc_proxies::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [target_grpc_proxy][crate::model::target_grpc_proxies::GetRequest::target_grpc_proxy].
        pub fn set_target_grpc_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_grpc_proxy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetGrpcProxies::insert][crate::client::TargetGrpcProxies::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_grpc_proxies::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::target_grpc_proxies::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetGrpcProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_grpc_proxies::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetGrpcProxies::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_grpc_proxies::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_grpc_proxies::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_grpc_proxies::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::target_grpc_proxies::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetGrpcProxy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_grpc_proxies::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetGrpcProxy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetGrpcProxies::list][crate::client::TargetGrpcProxies::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_grpc_proxies::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::target_grpc_proxies::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetGrpcProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_grpc_proxies::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetGrpcProxyList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::TargetGrpcProxyList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::TargetGrpcProxyList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::target_grpc_proxies::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::target_grpc_proxies::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::target_grpc_proxies::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::target_grpc_proxies::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::target_grpc_proxies::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::target_grpc_proxies::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::target_grpc_proxies::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::target_grpc_proxies::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::target_grpc_proxies::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::target_grpc_proxies::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::target_grpc_proxies::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetGrpcProxies::patch][crate::client::TargetGrpcProxies::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_grpc_proxies::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::target_grpc_proxies::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetGrpcProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_grpc_proxies::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetGrpcProxies::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_grpc_proxies::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_grpc_proxies::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_grpc_proxies::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_grpc_proxy][crate::model::target_grpc_proxies::PatchRequest::target_grpc_proxy].
        pub fn set_target_grpc_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_grpc_proxy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_grpc_proxies::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetGrpcProxy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_grpc_proxies::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetGrpcProxy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetGrpcProxies::get_operation][crate::client::TargetGrpcProxies::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_grpc_proxies::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetGrpcProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [TargetHttpProxies][crate::client::TargetHttpProxies].
#[cfg(feature = "target-http-proxies")]
#[cfg_attr(docsrs, doc(cfg(feature = "target-http-proxies")))]
pub mod target_http_proxies {
    use crate::Result;

    /// A builder for [TargetHttpProxies][crate::client::TargetHttpProxies].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::target_http_proxies::ClientBuilder;
    /// # use client::TargetHttpProxies;
    /// let builder : ClientBuilder = TargetHttpProxies::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::TargetHttpProxies;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = TargetHttpProxies;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::TargetHttpProxies] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetHttpProxies>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetHttpProxies>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [TargetHttpProxies::aggregated_list][crate::client::TargetHttpProxies::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_http_proxies::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::target_http_proxies::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetHttpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_http_proxies::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetHttpProxyAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::TargetHttpProxyAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::TargetHttpProxyAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::target_http_proxies::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::target_http_proxies::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::target_http_proxies::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::target_http_proxies::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::target_http_proxies::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::target_http_proxies::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::target_http_proxies::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::target_http_proxies::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::target_http_proxies::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::target_http_proxies::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::target_http_proxies::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::target_http_proxies::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::target_http_proxies::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::target_http_proxies::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::target_http_proxies::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetHttpProxies::delete][crate::client::TargetHttpProxies::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_http_proxies::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::target_http_proxies::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetHttpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_http_proxies::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetHttpProxies::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_http_proxies::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_http_proxies::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_http_proxies::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_http_proxy][crate::model::target_http_proxies::DeleteRequest::target_http_proxy].
        pub fn set_target_http_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_http_proxy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetHttpProxies::get][crate::client::TargetHttpProxies::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_http_proxies::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::target_http_proxies::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetHttpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_http_proxies::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetHttpProxy> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::target_http_proxies::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [target_http_proxy][crate::model::target_http_proxies::GetRequest::target_http_proxy].
        pub fn set_target_http_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_http_proxy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetHttpProxies::insert][crate::client::TargetHttpProxies::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_http_proxies::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::target_http_proxies::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetHttpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_http_proxies::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetHttpProxies::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_http_proxies::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_http_proxies::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_http_proxies::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::target_http_proxies::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetHttpProxy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_http_proxies::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetHttpProxy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetHttpProxies::list][crate::client::TargetHttpProxies::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_http_proxies::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::target_http_proxies::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetHttpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_http_proxies::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetHttpProxyList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::TargetHttpProxyList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::TargetHttpProxyList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::target_http_proxies::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::target_http_proxies::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::target_http_proxies::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::target_http_proxies::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::target_http_proxies::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::target_http_proxies::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::target_http_proxies::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::target_http_proxies::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::target_http_proxies::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::target_http_proxies::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::target_http_proxies::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetHttpProxies::patch][crate::client::TargetHttpProxies::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_http_proxies::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::target_http_proxies::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetHttpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_http_proxies::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetHttpProxies::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_http_proxies::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_http_proxies::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_http_proxies::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_http_proxy][crate::model::target_http_proxies::PatchRequest::target_http_proxy].
        pub fn set_target_http_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_http_proxy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_http_proxies::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetHttpProxy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_http_proxies::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetHttpProxy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetHttpProxies::set_url_map][crate::client::TargetHttpProxies::set_url_map] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_http_proxies::SetUrlMap;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetUrlMap {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetUrlMap(RequestBuilder<crate::model::target_http_proxies::SetUrlMapRequest>);

    impl SetUrlMap {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetHttpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_http_proxies::SetUrlMapRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_url_map(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_url_map`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetHttpProxies::set_url_map::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_http_proxies::SetUrlMapRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_http_proxies::SetUrlMapRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_http_proxies::SetUrlMapRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_http_proxy][crate::model::target_http_proxies::SetUrlMapRequest::target_http_proxy].
        pub fn set_target_http_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_http_proxy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_http_proxies::SetUrlMapRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::UrlMapReference>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_http_proxies::SetUrlMapRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::UrlMapReference>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetUrlMap {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetHttpProxies::get_operation][crate::client::TargetHttpProxies::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_http_proxies::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetHttpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [TargetHttpsProxies][crate::client::TargetHttpsProxies].
#[cfg(feature = "target-https-proxies")]
#[cfg_attr(docsrs, doc(cfg(feature = "target-https-proxies")))]
pub mod target_https_proxies {
    use crate::Result;

    /// A builder for [TargetHttpsProxies][crate::client::TargetHttpsProxies].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::target_https_proxies::ClientBuilder;
    /// # use client::TargetHttpsProxies;
    /// let builder : ClientBuilder = TargetHttpsProxies::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::TargetHttpsProxies;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = TargetHttpsProxies;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::TargetHttpsProxies] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetHttpsProxies>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetHttpsProxies>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [TargetHttpsProxies::aggregated_list][crate::client::TargetHttpsProxies::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_https_proxies::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::target_https_proxies::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetHttpsProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_https_proxies::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetHttpsProxyAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::TargetHttpsProxyAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::TargetHttpsProxyAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::target_https_proxies::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::target_https_proxies::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::target_https_proxies::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::target_https_proxies::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::target_https_proxies::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::target_https_proxies::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::target_https_proxies::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::target_https_proxies::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::target_https_proxies::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::target_https_proxies::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::target_https_proxies::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::target_https_proxies::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::target_https_proxies::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::target_https_proxies::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::target_https_proxies::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetHttpsProxies::delete][crate::client::TargetHttpsProxies::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_https_proxies::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::target_https_proxies::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetHttpsProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_https_proxies::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetHttpsProxies::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_https_proxies::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_https_proxies::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_https_proxies::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_https_proxy][crate::model::target_https_proxies::DeleteRequest::target_https_proxy].
        pub fn set_target_https_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_https_proxy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetHttpsProxies::get][crate::client::TargetHttpsProxies::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_https_proxies::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::target_https_proxies::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetHttpsProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_https_proxies::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetHttpsProxy> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::target_https_proxies::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [target_https_proxy][crate::model::target_https_proxies::GetRequest::target_https_proxy].
        pub fn set_target_https_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_https_proxy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetHttpsProxies::insert][crate::client::TargetHttpsProxies::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_https_proxies::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::target_https_proxies::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetHttpsProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_https_proxies::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetHttpsProxies::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_https_proxies::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_https_proxies::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_https_proxies::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::target_https_proxies::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetHttpsProxy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_https_proxies::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetHttpsProxy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetHttpsProxies::list][crate::client::TargetHttpsProxies::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_https_proxies::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::target_https_proxies::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetHttpsProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_https_proxies::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetHttpsProxyList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::TargetHttpsProxyList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::TargetHttpsProxyList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::target_https_proxies::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::target_https_proxies::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::target_https_proxies::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::target_https_proxies::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::target_https_proxies::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::target_https_proxies::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::target_https_proxies::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::target_https_proxies::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::target_https_proxies::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::target_https_proxies::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::target_https_proxies::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetHttpsProxies::patch][crate::client::TargetHttpsProxies::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_https_proxies::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::target_https_proxies::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetHttpsProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_https_proxies::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetHttpsProxies::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_https_proxies::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_https_proxies::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_https_proxies::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_https_proxy][crate::model::target_https_proxies::PatchRequest::target_https_proxy].
        pub fn set_target_https_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_https_proxy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_https_proxies::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetHttpsProxy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_https_proxies::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetHttpsProxy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetHttpsProxies::set_certificate_map][crate::client::TargetHttpsProxies::set_certificate_map] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_https_proxies::SetCertificateMap;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetCertificateMap {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetCertificateMap(
        RequestBuilder<crate::model::target_https_proxies::SetCertificateMapRequest>,
    );

    impl SetCertificateMap {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetHttpsProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::target_https_proxies::SetCertificateMapRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_certificate_map(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_certificate_map`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::TargetHttpsProxies::set_certificate_map::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_https_proxies::SetCertificateMapRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_https_proxies::SetCertificateMapRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_https_proxies::SetCertificateMapRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_https_proxy][crate::model::target_https_proxies::SetCertificateMapRequest::target_https_proxy].
        pub fn set_target_https_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_https_proxy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_https_proxies::SetCertificateMapRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetHttpsProxiesSetCertificateMapRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_https_proxies::SetCertificateMapRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetHttpsProxiesSetCertificateMapRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetCertificateMap {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetHttpsProxies::set_quic_override][crate::client::TargetHttpsProxies::set_quic_override] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_https_proxies::SetQuicOverride;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetQuicOverride {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetQuicOverride(
        RequestBuilder<crate::model::target_https_proxies::SetQuicOverrideRequest>,
    );

    impl SetQuicOverride {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetHttpsProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_https_proxies::SetQuicOverrideRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_quic_override(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_quic_override`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::TargetHttpsProxies::set_quic_override::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_https_proxies::SetQuicOverrideRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_https_proxies::SetQuicOverrideRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_https_proxies::SetQuicOverrideRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_https_proxy][crate::model::target_https_proxies::SetQuicOverrideRequest::target_https_proxy].
        pub fn set_target_https_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_https_proxy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_https_proxies::SetQuicOverrideRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetHttpsProxiesSetQuicOverrideRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_https_proxies::SetQuicOverrideRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetHttpsProxiesSetQuicOverrideRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetQuicOverride {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetHttpsProxies::set_ssl_certificates][crate::client::TargetHttpsProxies::set_ssl_certificates] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_https_proxies::SetSslCertificates;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetSslCertificates {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetSslCertificates(
        RequestBuilder<crate::model::target_https_proxies::SetSslCertificatesRequest>,
    );

    impl SetSslCertificates {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetHttpsProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::target_https_proxies::SetSslCertificatesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_ssl_certificates(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_ssl_certificates`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::TargetHttpsProxies::set_ssl_certificates::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_https_proxies::SetSslCertificatesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_https_proxies::SetSslCertificatesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_https_proxies::SetSslCertificatesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_https_proxy][crate::model::target_https_proxies::SetSslCertificatesRequest::target_https_proxy].
        pub fn set_target_https_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_https_proxy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_https_proxies::SetSslCertificatesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetHttpsProxiesSetSslCertificatesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_https_proxies::SetSslCertificatesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetHttpsProxiesSetSslCertificatesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetSslCertificates {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetHttpsProxies::set_ssl_policy][crate::client::TargetHttpsProxies::set_ssl_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_https_proxies::SetSslPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetSslPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetSslPolicy(
        RequestBuilder<crate::model::target_https_proxies::SetSslPolicyRequest>,
    );

    impl SetSslPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetHttpsProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_https_proxies::SetSslPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_ssl_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_ssl_policy`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::TargetHttpsProxies::set_ssl_policy::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_https_proxies::SetSslPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_https_proxies::SetSslPolicyRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_https_proxies::SetSslPolicyRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_https_proxy][crate::model::target_https_proxies::SetSslPolicyRequest::target_https_proxy].
        pub fn set_target_https_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_https_proxy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_https_proxies::SetSslPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SslPolicyReference>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_https_proxies::SetSslPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SslPolicyReference>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetSslPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetHttpsProxies::set_url_map][crate::client::TargetHttpsProxies::set_url_map] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_https_proxies::SetUrlMap;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetUrlMap {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetUrlMap(RequestBuilder<crate::model::target_https_proxies::SetUrlMapRequest>);

    impl SetUrlMap {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetHttpsProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_https_proxies::SetUrlMapRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_url_map(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_url_map`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetHttpsProxies::set_url_map::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_https_proxies::SetUrlMapRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_https_proxies::SetUrlMapRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_https_proxies::SetUrlMapRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_https_proxy][crate::model::target_https_proxies::SetUrlMapRequest::target_https_proxy].
        pub fn set_target_https_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_https_proxy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_https_proxies::SetUrlMapRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::UrlMapReference>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_https_proxies::SetUrlMapRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::UrlMapReference>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetUrlMap {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetHttpsProxies::get_operation][crate::client::TargetHttpsProxies::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_https_proxies::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetHttpsProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [TargetInstances][crate::client::TargetInstances].
#[cfg(feature = "target-instances")]
#[cfg_attr(docsrs, doc(cfg(feature = "target-instances")))]
pub mod target_instances {
    use crate::Result;

    /// A builder for [TargetInstances][crate::client::TargetInstances].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::target_instances::ClientBuilder;
    /// # use client::TargetInstances;
    /// let builder : ClientBuilder = TargetInstances::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::TargetInstances;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = TargetInstances;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::TargetInstances] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetInstances>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetInstances>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [TargetInstances::aggregated_list][crate::client::TargetInstances::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_instances::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::target_instances::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetInstances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_instances::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetInstanceAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::TargetInstanceAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::TargetInstanceAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::target_instances::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::target_instances::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::target_instances::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::target_instances::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::target_instances::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::target_instances::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::target_instances::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::target_instances::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::target_instances::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::target_instances::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::target_instances::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::target_instances::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::target_instances::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::target_instances::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::target_instances::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetInstances::delete][crate::client::TargetInstances::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_instances::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::target_instances::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetInstances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_instances::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetInstances::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_instances::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_instances::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_instances::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_instance][crate::model::target_instances::DeleteRequest::target_instance].
        pub fn set_target_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_instance = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::target_instances::DeleteRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetInstances::get][crate::client::TargetInstances::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_instances::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::target_instances::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetInstances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_instances::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetInstance> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::target_instances::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [target_instance][crate::model::target_instances::GetRequest::target_instance].
        pub fn set_target_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_instance = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::target_instances::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetInstances::insert][crate::client::TargetInstances::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_instances::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::target_instances::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetInstances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_instances::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetInstances::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_instances::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_instances::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_instances::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::target_instances::InsertRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_instances::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetInstance>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_instances::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetInstance>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetInstances::list][crate::client::TargetInstances::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_instances::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::target_instances::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetInstances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_instances::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetInstanceList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::TargetInstanceList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::TargetInstanceList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::target_instances::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::target_instances::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::target_instances::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::target_instances::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::target_instances::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::target_instances::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::target_instances::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::target_instances::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::target_instances::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::target_instances::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::target_instances::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::target_instances::ListRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetInstances::set_security_policy][crate::client::TargetInstances::set_security_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_instances::SetSecurityPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetSecurityPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetSecurityPolicy(
        RequestBuilder<crate::model::target_instances::SetSecurityPolicyRequest>,
    );

    impl SetSecurityPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetInstances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_instances::SetSecurityPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_security_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_security_policy`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::TargetInstances::set_security_policy::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_instances::SetSecurityPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_instances::SetSecurityPolicyRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_instances::SetSecurityPolicyRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_instance][crate::model::target_instances::SetSecurityPolicyRequest::target_instance].
        pub fn set_target_instance<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_instance = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::target_instances::SetSecurityPolicyRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_instances::SetSecurityPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyReference>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_instances::SetSecurityPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyReference>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetSecurityPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetInstances::test_iam_permissions][crate::client::TargetInstances::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_instances::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::target_instances::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetInstances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_instances::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::target_instances::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::target_instances::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::target_instances::TestIamPermissionsRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_instances::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_instances::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetInstances::get_operation][crate::client::TargetInstances::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_instances::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::zone_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetInstances>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::zone_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::zone_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zone_operations::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [TargetPools][crate::client::TargetPools].
#[cfg(feature = "target-pools")]
#[cfg_attr(docsrs, doc(cfg(feature = "target-pools")))]
pub mod target_pools {
    use crate::Result;

    /// A builder for [TargetPools][crate::client::TargetPools].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::target_pools::ClientBuilder;
    /// # use client::TargetPools;
    /// let builder : ClientBuilder = TargetPools::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::TargetPools;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = TargetPools;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::TargetPools] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetPools>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetPools>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [TargetPools::add_health_check][crate::client::TargetPools::add_health_check] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_pools::AddHealthCheck;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AddHealthCheck {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AddHealthCheck(RequestBuilder<crate::model::target_pools::AddHealthCheckRequest>);

    impl AddHealthCheck {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetPools>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_pools::AddHealthCheckRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .add_health_check(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `add_health_check`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetPools::add_health_check::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_pools::AddHealthCheckRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::target_pools::AddHealthCheckRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_pools::AddHealthCheckRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_pools::AddHealthCheckRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_pool][crate::model::target_pools::AddHealthCheckRequest::target_pool].
        pub fn set_target_pool<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_pool = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_pools::AddHealthCheckRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetPoolsAddHealthCheckRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_pools::AddHealthCheckRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetPoolsAddHealthCheckRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AddHealthCheck {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetPools::add_instance][crate::client::TargetPools::add_instance] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_pools::AddInstance;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AddInstance {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AddInstance(RequestBuilder<crate::model::target_pools::AddInstanceRequest>);

    impl AddInstance {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetPools>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_pools::AddInstanceRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .add_instance(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `add_instance`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetPools::add_instance::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_pools::AddInstanceRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::target_pools::AddInstanceRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_pools::AddInstanceRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_pools::AddInstanceRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_pool][crate::model::target_pools::AddInstanceRequest::target_pool].
        pub fn set_target_pool<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_pool = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_pools::AddInstanceRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetPoolsAddInstanceRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_pools::AddInstanceRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetPoolsAddInstanceRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AddInstance {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetPools::aggregated_list][crate::client::TargetPools::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_pools::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(RequestBuilder<crate::model::target_pools::AggregatedListRequest>);

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetPools>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_pools::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetPoolAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::TargetPoolAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::TargetPoolAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::target_pools::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::target_pools::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::target_pools::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::target_pools::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::target_pools::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::target_pools::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::target_pools::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::target_pools::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::target_pools::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::target_pools::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::target_pools::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::target_pools::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::target_pools::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::target_pools::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::target_pools::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetPools::delete][crate::client::TargetPools::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_pools::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::target_pools::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetPools>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_pools::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetPools::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_pools::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::target_pools::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_pools::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_pools::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_pool][crate::model::target_pools::DeleteRequest::target_pool].
        pub fn set_target_pool<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_pool = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetPools::get][crate::client::TargetPools::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_pools::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::target_pools::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetPools>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_pools::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetPool> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::target_pools::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::target_pools::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [target_pool][crate::model::target_pools::GetRequest::target_pool].
        pub fn set_target_pool<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_pool = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetPools::get_health][crate::client::TargetPools::get_health] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_pools::GetHealth;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetHealth {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetHealth(RequestBuilder<crate::model::target_pools::GetHealthRequest>);

    impl GetHealth {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetPools>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_pools::GetHealthRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetPoolInstanceHealth> {
            (*self.0.stub)
                .get_health(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::target_pools::GetHealthRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::target_pools::GetHealthRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [target_pool][crate::model::target_pools::GetHealthRequest::target_pool].
        pub fn set_target_pool<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_pool = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_pools::GetHealthRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::InstanceReference>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_pools::GetHealthRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::InstanceReference>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetHealth {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetPools::insert][crate::client::TargetPools::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_pools::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::target_pools::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetPools>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_pools::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetPools::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_pools::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::target_pools::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_pools::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_pools::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::target_pools::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetPool>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_pools::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetPool>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetPools::list][crate::client::TargetPools::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_pools::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::target_pools::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetPools>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_pools::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetPoolList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::TargetPoolList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::TargetPoolList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::target_pools::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::target_pools::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::target_pools::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::target_pools::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::target_pools::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::target_pools::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::target_pools::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::target_pools::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::target_pools::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::target_pools::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::target_pools::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::target_pools::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetPools::remove_health_check][crate::client::TargetPools::remove_health_check] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_pools::RemoveHealthCheck;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> RemoveHealthCheck {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct RemoveHealthCheck(
        RequestBuilder<crate::model::target_pools::RemoveHealthCheckRequest>,
    );

    impl RemoveHealthCheck {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetPools>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_pools::RemoveHealthCheckRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .remove_health_check(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `remove_health_check`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetPools::remove_health_check::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_pools::RemoveHealthCheckRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::target_pools::RemoveHealthCheckRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_pools::RemoveHealthCheckRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_pools::RemoveHealthCheckRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_pool][crate::model::target_pools::RemoveHealthCheckRequest::target_pool].
        pub fn set_target_pool<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_pool = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_pools::RemoveHealthCheckRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetPoolsRemoveHealthCheckRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_pools::RemoveHealthCheckRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetPoolsRemoveHealthCheckRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for RemoveHealthCheck {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetPools::remove_instance][crate::client::TargetPools::remove_instance] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_pools::RemoveInstance;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> RemoveInstance {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct RemoveInstance(RequestBuilder<crate::model::target_pools::RemoveInstanceRequest>);

    impl RemoveInstance {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetPools>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_pools::RemoveInstanceRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .remove_instance(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `remove_instance`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetPools::remove_instance::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_pools::RemoveInstanceRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::target_pools::RemoveInstanceRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_pools::RemoveInstanceRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_pools::RemoveInstanceRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_pool][crate::model::target_pools::RemoveInstanceRequest::target_pool].
        pub fn set_target_pool<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_pool = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_pools::RemoveInstanceRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetPoolsRemoveInstanceRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_pools::RemoveInstanceRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetPoolsRemoveInstanceRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for RemoveInstance {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetPools::set_backup][crate::client::TargetPools::set_backup] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_pools::SetBackup;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetBackup {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetBackup(RequestBuilder<crate::model::target_pools::SetBackupRequest>);

    impl SetBackup {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetPools>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_pools::SetBackupRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_backup(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_backup`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetPools::set_backup::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [failover_ratio][crate::model::target_pools::SetBackupRequest::failover_ratio].
        pub fn set_failover_ratio<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<f32>,
        {
            self.0.request.failover_ratio = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [failover_ratio][crate::model::target_pools::SetBackupRequest::failover_ratio].
        pub fn set_or_clear_failover_ratio<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<f32>,
        {
            self.0.request.failover_ratio = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::target_pools::SetBackupRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::target_pools::SetBackupRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_pools::SetBackupRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_pools::SetBackupRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_pool][crate::model::target_pools::SetBackupRequest::target_pool].
        pub fn set_target_pool<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_pool = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_pools::SetBackupRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetReference>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_pools::SetBackupRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetReference>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetBackup {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetPools::set_security_policy][crate::client::TargetPools::set_security_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_pools::SetSecurityPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetSecurityPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetSecurityPolicy(
        RequestBuilder<crate::model::target_pools::SetSecurityPolicyRequest>,
    );

    impl SetSecurityPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetPools>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_pools::SetSecurityPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_security_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_security_policy`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetPools::set_security_policy::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_pools::SetSecurityPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::target_pools::SetSecurityPolicyRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_pools::SetSecurityPolicyRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_pools::SetSecurityPolicyRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_pool][crate::model::target_pools::SetSecurityPolicyRequest::target_pool].
        pub fn set_target_pool<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_pool = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_pools::SetSecurityPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyReference>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_pools::SetSecurityPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SecurityPolicyReference>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetSecurityPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetPools::test_iam_permissions][crate::client::TargetPools::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_pools::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::target_pools::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetPools>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_pools::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::target_pools::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::target_pools::TestIamPermissionsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::target_pools::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_pools::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_pools::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetPools::get_operation][crate::client::TargetPools::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_pools::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetPools>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [TargetSslProxies][crate::client::TargetSslProxies].
#[cfg(feature = "target-ssl-proxies")]
#[cfg_attr(docsrs, doc(cfg(feature = "target-ssl-proxies")))]
pub mod target_ssl_proxies {
    use crate::Result;

    /// A builder for [TargetSslProxies][crate::client::TargetSslProxies].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::target_ssl_proxies::ClientBuilder;
    /// # use client::TargetSslProxies;
    /// let builder : ClientBuilder = TargetSslProxies::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::TargetSslProxies;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = TargetSslProxies;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::TargetSslProxies] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetSslProxies>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetSslProxies>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [TargetSslProxies::delete][crate::client::TargetSslProxies::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_ssl_proxies::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::target_ssl_proxies::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetSslProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_ssl_proxies::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetSslProxies::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_ssl_proxies::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_ssl_proxies::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_ssl_proxies::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_ssl_proxy][crate::model::target_ssl_proxies::DeleteRequest::target_ssl_proxy].
        pub fn set_target_ssl_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_ssl_proxy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetSslProxies::get][crate::client::TargetSslProxies::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_ssl_proxies::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::target_ssl_proxies::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetSslProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_ssl_proxies::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetSslProxy> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::target_ssl_proxies::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [target_ssl_proxy][crate::model::target_ssl_proxies::GetRequest::target_ssl_proxy].
        pub fn set_target_ssl_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_ssl_proxy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetSslProxies::insert][crate::client::TargetSslProxies::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_ssl_proxies::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::target_ssl_proxies::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetSslProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_ssl_proxies::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetSslProxies::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_ssl_proxies::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_ssl_proxies::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_ssl_proxies::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::target_ssl_proxies::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetSslProxy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_ssl_proxies::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetSslProxy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetSslProxies::list][crate::client::TargetSslProxies::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_ssl_proxies::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::target_ssl_proxies::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetSslProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_ssl_proxies::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetSslProxyList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::TargetSslProxyList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::TargetSslProxyList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::target_ssl_proxies::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::target_ssl_proxies::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::target_ssl_proxies::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::target_ssl_proxies::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::target_ssl_proxies::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::target_ssl_proxies::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::target_ssl_proxies::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::target_ssl_proxies::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::target_ssl_proxies::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::target_ssl_proxies::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::target_ssl_proxies::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetSslProxies::set_backend_service][crate::client::TargetSslProxies::set_backend_service] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_ssl_proxies::SetBackendService;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetBackendService {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetBackendService(
        RequestBuilder<crate::model::target_ssl_proxies::SetBackendServiceRequest>,
    );

    impl SetBackendService {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetSslProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_ssl_proxies::SetBackendServiceRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_backend_service(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_backend_service`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::TargetSslProxies::set_backend_service::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_ssl_proxies::SetBackendServiceRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_ssl_proxies::SetBackendServiceRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_ssl_proxies::SetBackendServiceRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_ssl_proxy][crate::model::target_ssl_proxies::SetBackendServiceRequest::target_ssl_proxy].
        pub fn set_target_ssl_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_ssl_proxy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_ssl_proxies::SetBackendServiceRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetSslProxiesSetBackendServiceRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_ssl_proxies::SetBackendServiceRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetSslProxiesSetBackendServiceRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetBackendService {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetSslProxies::set_certificate_map][crate::client::TargetSslProxies::set_certificate_map] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_ssl_proxies::SetCertificateMap;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetCertificateMap {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetCertificateMap(
        RequestBuilder<crate::model::target_ssl_proxies::SetCertificateMapRequest>,
    );

    impl SetCertificateMap {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetSslProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_ssl_proxies::SetCertificateMapRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_certificate_map(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_certificate_map`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::TargetSslProxies::set_certificate_map::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_ssl_proxies::SetCertificateMapRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_ssl_proxies::SetCertificateMapRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_ssl_proxies::SetCertificateMapRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_ssl_proxy][crate::model::target_ssl_proxies::SetCertificateMapRequest::target_ssl_proxy].
        pub fn set_target_ssl_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_ssl_proxy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_ssl_proxies::SetCertificateMapRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetSslProxiesSetCertificateMapRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_ssl_proxies::SetCertificateMapRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetSslProxiesSetCertificateMapRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetCertificateMap {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetSslProxies::set_proxy_header][crate::client::TargetSslProxies::set_proxy_header] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_ssl_proxies::SetProxyHeader;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetProxyHeader {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetProxyHeader(
        RequestBuilder<crate::model::target_ssl_proxies::SetProxyHeaderRequest>,
    );

    impl SetProxyHeader {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetSslProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_ssl_proxies::SetProxyHeaderRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_proxy_header(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_proxy_header`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::TargetSslProxies::set_proxy_header::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_ssl_proxies::SetProxyHeaderRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_ssl_proxies::SetProxyHeaderRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_ssl_proxies::SetProxyHeaderRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_ssl_proxy][crate::model::target_ssl_proxies::SetProxyHeaderRequest::target_ssl_proxy].
        pub fn set_target_ssl_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_ssl_proxy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_ssl_proxies::SetProxyHeaderRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetSslProxiesSetProxyHeaderRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_ssl_proxies::SetProxyHeaderRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetSslProxiesSetProxyHeaderRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetProxyHeader {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetSslProxies::set_ssl_certificates][crate::client::TargetSslProxies::set_ssl_certificates] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_ssl_proxies::SetSslCertificates;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetSslCertificates {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetSslCertificates(
        RequestBuilder<crate::model::target_ssl_proxies::SetSslCertificatesRequest>,
    );

    impl SetSslCertificates {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetSslProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::target_ssl_proxies::SetSslCertificatesRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_ssl_certificates(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_ssl_certificates`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::TargetSslProxies::set_ssl_certificates::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_ssl_proxies::SetSslCertificatesRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_ssl_proxies::SetSslCertificatesRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_ssl_proxies::SetSslCertificatesRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_ssl_proxy][crate::model::target_ssl_proxies::SetSslCertificatesRequest::target_ssl_proxy].
        pub fn set_target_ssl_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_ssl_proxy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_ssl_proxies::SetSslCertificatesRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetSslProxiesSetSslCertificatesRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_ssl_proxies::SetSslCertificatesRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetSslProxiesSetSslCertificatesRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetSslCertificates {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetSslProxies::set_ssl_policy][crate::client::TargetSslProxies::set_ssl_policy] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_ssl_proxies::SetSslPolicy;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetSslPolicy {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetSslPolicy(RequestBuilder<crate::model::target_ssl_proxies::SetSslPolicyRequest>);

    impl SetSslPolicy {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetSslProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_ssl_proxies::SetSslPolicyRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_ssl_policy(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_ssl_policy`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetSslProxies::set_ssl_policy::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_ssl_proxies::SetSslPolicyRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_ssl_proxies::SetSslPolicyRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_ssl_proxies::SetSslPolicyRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_ssl_proxy][crate::model::target_ssl_proxies::SetSslPolicyRequest::target_ssl_proxy].
        pub fn set_target_ssl_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_ssl_proxy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_ssl_proxies::SetSslPolicyRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::SslPolicyReference>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_ssl_proxies::SetSslPolicyRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::SslPolicyReference>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetSslPolicy {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetSslProxies::test_iam_permissions][crate::client::TargetSslProxies::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_ssl_proxies::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::target_ssl_proxies::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetSslProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::target_ssl_proxies::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::target_ssl_proxies::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::target_ssl_proxies::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_ssl_proxies::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_ssl_proxies::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetSslProxies::get_operation][crate::client::TargetSslProxies::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_ssl_proxies::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetSslProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [TargetTcpProxies][crate::client::TargetTcpProxies].
#[cfg(feature = "target-tcp-proxies")]
#[cfg_attr(docsrs, doc(cfg(feature = "target-tcp-proxies")))]
pub mod target_tcp_proxies {
    use crate::Result;

    /// A builder for [TargetTcpProxies][crate::client::TargetTcpProxies].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::target_tcp_proxies::ClientBuilder;
    /// # use client::TargetTcpProxies;
    /// let builder : ClientBuilder = TargetTcpProxies::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::TargetTcpProxies;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = TargetTcpProxies;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::TargetTcpProxies] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetTcpProxies>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetTcpProxies>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [TargetTcpProxies::aggregated_list][crate::client::TargetTcpProxies::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_tcp_proxies::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::target_tcp_proxies::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetTcpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_tcp_proxies::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetTcpProxyAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::TargetTcpProxyAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::TargetTcpProxyAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::target_tcp_proxies::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::target_tcp_proxies::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::target_tcp_proxies::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::target_tcp_proxies::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::target_tcp_proxies::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::target_tcp_proxies::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::target_tcp_proxies::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::target_tcp_proxies::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::target_tcp_proxies::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::target_tcp_proxies::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::target_tcp_proxies::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::target_tcp_proxies::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::target_tcp_proxies::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::target_tcp_proxies::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::target_tcp_proxies::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetTcpProxies::delete][crate::client::TargetTcpProxies::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_tcp_proxies::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::target_tcp_proxies::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetTcpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_tcp_proxies::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetTcpProxies::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_tcp_proxies::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_tcp_proxies::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_tcp_proxies::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_tcp_proxy][crate::model::target_tcp_proxies::DeleteRequest::target_tcp_proxy].
        pub fn set_target_tcp_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_tcp_proxy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetTcpProxies::get][crate::client::TargetTcpProxies::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_tcp_proxies::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::target_tcp_proxies::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetTcpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_tcp_proxies::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetTcpProxy> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::target_tcp_proxies::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [target_tcp_proxy][crate::model::target_tcp_proxies::GetRequest::target_tcp_proxy].
        pub fn set_target_tcp_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_tcp_proxy = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetTcpProxies::insert][crate::client::TargetTcpProxies::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_tcp_proxies::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::target_tcp_proxies::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetTcpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_tcp_proxies::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetTcpProxies::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_tcp_proxies::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_tcp_proxies::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_tcp_proxies::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::target_tcp_proxies::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetTcpProxy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_tcp_proxies::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetTcpProxy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetTcpProxies::list][crate::client::TargetTcpProxies::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_tcp_proxies::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::target_tcp_proxies::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetTcpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_tcp_proxies::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetTcpProxyList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::TargetTcpProxyList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::TargetTcpProxyList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::target_tcp_proxies::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::target_tcp_proxies::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::target_tcp_proxies::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::target_tcp_proxies::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::target_tcp_proxies::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::target_tcp_proxies::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::target_tcp_proxies::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::target_tcp_proxies::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::target_tcp_proxies::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::target_tcp_proxies::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::target_tcp_proxies::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetTcpProxies::set_backend_service][crate::client::TargetTcpProxies::set_backend_service] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_tcp_proxies::SetBackendService;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetBackendService {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetBackendService(
        RequestBuilder<crate::model::target_tcp_proxies::SetBackendServiceRequest>,
    );

    impl SetBackendService {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetTcpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_tcp_proxies::SetBackendServiceRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_backend_service(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_backend_service`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::TargetTcpProxies::set_backend_service::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_tcp_proxies::SetBackendServiceRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_tcp_proxies::SetBackendServiceRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_tcp_proxies::SetBackendServiceRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_tcp_proxy][crate::model::target_tcp_proxies::SetBackendServiceRequest::target_tcp_proxy].
        pub fn set_target_tcp_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_tcp_proxy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_tcp_proxies::SetBackendServiceRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetTcpProxiesSetBackendServiceRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_tcp_proxies::SetBackendServiceRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetTcpProxiesSetBackendServiceRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetBackendService {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetTcpProxies::set_proxy_header][crate::client::TargetTcpProxies::set_proxy_header] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_tcp_proxies::SetProxyHeader;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetProxyHeader {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetProxyHeader(
        RequestBuilder<crate::model::target_tcp_proxies::SetProxyHeaderRequest>,
    );

    impl SetProxyHeader {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetTcpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_tcp_proxies::SetProxyHeaderRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_proxy_header(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_proxy_header`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::TargetTcpProxies::set_proxy_header::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_tcp_proxies::SetProxyHeaderRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_tcp_proxies::SetProxyHeaderRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_tcp_proxies::SetProxyHeaderRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_tcp_proxy][crate::model::target_tcp_proxies::SetProxyHeaderRequest::target_tcp_proxy].
        pub fn set_target_tcp_proxy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_tcp_proxy = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_tcp_proxies::SetProxyHeaderRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetTcpProxiesSetProxyHeaderRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_tcp_proxies::SetProxyHeaderRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetTcpProxiesSetProxyHeaderRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetProxyHeader {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetTcpProxies::test_iam_permissions][crate::client::TargetTcpProxies::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_tcp_proxies::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::target_tcp_proxies::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetTcpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<
            V: Into<crate::model::target_tcp_proxies::TestIamPermissionsRequest>,
        >(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::target_tcp_proxies::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::target_tcp_proxies::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_tcp_proxies::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_tcp_proxies::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetTcpProxies::get_operation][crate::client::TargetTcpProxies::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_tcp_proxies::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetTcpProxies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [TargetVpnGateways][crate::client::TargetVpnGateways].
#[cfg(feature = "target-vpn-gateways")]
#[cfg_attr(docsrs, doc(cfg(feature = "target-vpn-gateways")))]
pub mod target_vpn_gateways {
    use crate::Result;

    /// A builder for [TargetVpnGateways][crate::client::TargetVpnGateways].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::target_vpn_gateways::ClientBuilder;
    /// # use client::TargetVpnGateways;
    /// let builder : ClientBuilder = TargetVpnGateways::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::TargetVpnGateways;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = TargetVpnGateways;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::TargetVpnGateways] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetVpnGateways>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetVpnGateways>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [TargetVpnGateways::aggregated_list][crate::client::TargetVpnGateways::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_vpn_gateways::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(
        RequestBuilder<crate::model::target_vpn_gateways::AggregatedListRequest>,
    );

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetVpnGateways>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_vpn_gateways::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetVpnGatewayAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::TargetVpnGatewayAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::TargetVpnGatewayAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::target_vpn_gateways::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::target_vpn_gateways::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::target_vpn_gateways::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::target_vpn_gateways::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::target_vpn_gateways::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::target_vpn_gateways::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::target_vpn_gateways::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::target_vpn_gateways::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::target_vpn_gateways::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::target_vpn_gateways::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::target_vpn_gateways::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::target_vpn_gateways::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::target_vpn_gateways::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::target_vpn_gateways::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::target_vpn_gateways::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetVpnGateways::delete][crate::client::TargetVpnGateways::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_vpn_gateways::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::target_vpn_gateways::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetVpnGateways>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_vpn_gateways::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetVpnGateways::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_vpn_gateways::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::target_vpn_gateways::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_vpn_gateways::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_vpn_gateways::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [target_vpn_gateway][crate::model::target_vpn_gateways::DeleteRequest::target_vpn_gateway].
        pub fn set_target_vpn_gateway<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_vpn_gateway = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetVpnGateways::get][crate::client::TargetVpnGateways::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_vpn_gateways::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::target_vpn_gateways::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetVpnGateways>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_vpn_gateways::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetVpnGateway> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::target_vpn_gateways::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::target_vpn_gateways::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [target_vpn_gateway][crate::model::target_vpn_gateways::GetRequest::target_vpn_gateway].
        pub fn set_target_vpn_gateway<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.target_vpn_gateway = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetVpnGateways::insert][crate::client::TargetVpnGateways::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_vpn_gateways::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::target_vpn_gateways::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetVpnGateways>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_vpn_gateways::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetVpnGateways::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_vpn_gateways::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::target_vpn_gateways::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_vpn_gateways::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_vpn_gateways::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::target_vpn_gateways::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TargetVpnGateway>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_vpn_gateways::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TargetVpnGateway>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetVpnGateways::list][crate::client::TargetVpnGateways::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_vpn_gateways::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::target_vpn_gateways::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetVpnGateways>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_vpn_gateways::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TargetVpnGatewayList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::TargetVpnGatewayList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::TargetVpnGatewayList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::target_vpn_gateways::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::target_vpn_gateways::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::target_vpn_gateways::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::target_vpn_gateways::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::target_vpn_gateways::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::target_vpn_gateways::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::target_vpn_gateways::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::target_vpn_gateways::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::target_vpn_gateways::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::target_vpn_gateways::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::target_vpn_gateways::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::target_vpn_gateways::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetVpnGateways::set_labels][crate::client::TargetVpnGateways::set_labels] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_vpn_gateways::SetLabels;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetLabels {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetLabels(RequestBuilder<crate::model::target_vpn_gateways::SetLabelsRequest>);

    impl SetLabels {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetVpnGateways>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::target_vpn_gateways::SetLabelsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_labels(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_labels`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::TargetVpnGateways::set_labels::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::target_vpn_gateways::SetLabelsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::target_vpn_gateways::SetLabelsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::target_vpn_gateways::SetLabelsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::target_vpn_gateways::SetLabelsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [resource][crate::model::target_vpn_gateways::SetLabelsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::target_vpn_gateways::SetLabelsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetLabelsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::target_vpn_gateways::SetLabelsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetLabelsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetLabels {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [TargetVpnGateways::get_operation][crate::client::TargetVpnGateways::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::target_vpn_gateways::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::TargetVpnGateways>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [UrlMaps][crate::client::UrlMaps].
#[cfg(feature = "url-maps")]
#[cfg_attr(docsrs, doc(cfg(feature = "url-maps")))]
pub mod url_maps {
    use crate::Result;

    /// A builder for [UrlMaps][crate::client::UrlMaps].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::url_maps::ClientBuilder;
    /// # use client::UrlMaps;
    /// let builder : ClientBuilder = UrlMaps::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::UrlMaps;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = UrlMaps;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::UrlMaps] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::UrlMaps>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::UrlMaps>) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [UrlMaps::aggregated_list][crate::client::UrlMaps::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::url_maps::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(RequestBuilder<crate::model::url_maps::AggregatedListRequest>);

    impl AggregatedList {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::UrlMaps>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::url_maps::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::UrlMapsAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::UrlMapsAggregatedList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::UrlMapsAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::url_maps::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::url_maps::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::url_maps::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::url_maps::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::url_maps::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::url_maps::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::url_maps::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::url_maps::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::url_maps::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::url_maps::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::url_maps::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::url_maps::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::url_maps::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::url_maps::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::url_maps::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [UrlMaps::delete][crate::client::UrlMaps::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::url_maps::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::url_maps::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::UrlMaps>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::url_maps::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::UrlMaps::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::url_maps::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::url_maps::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::url_maps::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [url_map][crate::model::url_maps::DeleteRequest::url_map].
        pub fn set_url_map<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.url_map = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [UrlMaps::get][crate::client::UrlMaps::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::url_maps::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::url_maps::GetRequest>);

    impl Get {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::UrlMaps>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::url_maps::GetRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::UrlMap> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::url_maps::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [url_map][crate::model::url_maps::GetRequest::url_map].
        pub fn set_url_map<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.url_map = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [UrlMaps::insert][crate::client::UrlMaps::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::url_maps::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::url_maps::InsertRequest>);

    impl Insert {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::UrlMaps>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::url_maps::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::UrlMaps::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::url_maps::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::url_maps::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::url_maps::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::url_maps::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::UrlMap>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::url_maps::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::UrlMap>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [UrlMaps::invalidate_cache][crate::client::UrlMaps::invalidate_cache] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::url_maps::InvalidateCache;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> InvalidateCache {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct InvalidateCache(RequestBuilder<crate::model::url_maps::InvalidateCacheRequest>);

    impl InvalidateCache {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::UrlMaps>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::url_maps::InvalidateCacheRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .invalidate_cache(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `invalidate_cache`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::UrlMaps::invalidate_cache::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::url_maps::InvalidateCacheRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::url_maps::InvalidateCacheRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::url_maps::InvalidateCacheRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [url_map][crate::model::url_maps::InvalidateCacheRequest::url_map].
        pub fn set_url_map<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.url_map = v.into();
            self
        }

        /// Sets the value of [body][crate::model::url_maps::InvalidateCacheRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::CacheInvalidationRule>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::url_maps::InvalidateCacheRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::CacheInvalidationRule>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for InvalidateCache {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [UrlMaps::list][crate::client::UrlMaps::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::url_maps::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::url_maps::ListRequest>);

    impl List {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::UrlMaps>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::url_maps::ListRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::UrlMapList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::UrlMapList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::UrlMapList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::url_maps::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::url_maps::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::url_maps::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::url_maps::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::url_maps::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::url_maps::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::url_maps::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::url_maps::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::url_maps::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::url_maps::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::url_maps::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [UrlMaps::patch][crate::client::UrlMaps::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::url_maps::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::url_maps::PatchRequest>);

    impl Patch {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::UrlMaps>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::url_maps::PatchRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name = "google_cloud_compute_v1::client::UrlMaps::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::url_maps::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::url_maps::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::url_maps::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [url_map][crate::model::url_maps::PatchRequest::url_map].
        pub fn set_url_map<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.url_map = v.into();
            self
        }

        /// Sets the value of [body][crate::model::url_maps::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::UrlMap>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::url_maps::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::UrlMap>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [UrlMaps::test_iam_permissions][crate::client::UrlMaps::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::url_maps::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::url_maps::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::UrlMaps>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::url_maps::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::url_maps::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::url_maps::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::url_maps::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::url_maps::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [UrlMaps::update][crate::client::UrlMaps::update] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::url_maps::Update;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Update {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Update(RequestBuilder<crate::model::url_maps::UpdateRequest>);

    impl Update {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::UrlMaps>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::url_maps::UpdateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::UrlMaps::update::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::url_maps::UpdateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::url_maps::UpdateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::url_maps::UpdateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [url_map][crate::model::url_maps::UpdateRequest::url_map].
        pub fn set_url_map<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.url_map = v.into();
            self
        }

        /// Sets the value of [body][crate::model::url_maps::UpdateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::UrlMap>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::url_maps::UpdateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::UrlMap>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Update {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [UrlMaps::validate][crate::client::UrlMaps::validate] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::url_maps::Validate;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Validate {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Validate(RequestBuilder<crate::model::url_maps::ValidateRequest>);

    impl Validate {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::UrlMaps>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::url_maps::ValidateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::UrlMapsValidateResponse> {
            (*self.0.stub)
                .validate(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::url_maps::ValidateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [url_map][crate::model::url_maps::ValidateRequest::url_map].
        pub fn set_url_map<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.url_map = v.into();
            self
        }

        /// Sets the value of [body][crate::model::url_maps::ValidateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::UrlMapsValidateRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::url_maps::ValidateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::UrlMapsValidateRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Validate {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [UrlMaps::get_operation][crate::client::UrlMaps::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::url_maps::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::UrlMaps>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [VpnGateways][crate::client::VpnGateways].
#[cfg(feature = "vpn-gateways")]
#[cfg_attr(docsrs, doc(cfg(feature = "vpn-gateways")))]
pub mod vpn_gateways {
    use crate::Result;

    /// A builder for [VpnGateways][crate::client::VpnGateways].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::vpn_gateways::ClientBuilder;
    /// # use client::VpnGateways;
    /// let builder : ClientBuilder = VpnGateways::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::VpnGateways;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = VpnGateways;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::VpnGateways] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::VpnGateways>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VpnGateways>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [VpnGateways::aggregated_list][crate::client::VpnGateways::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::vpn_gateways::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(RequestBuilder<crate::model::vpn_gateways::AggregatedListRequest>);

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VpnGateways>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::vpn_gateways::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::VpnGatewayAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::VpnGatewayAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::VpnGatewayAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::vpn_gateways::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::vpn_gateways::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::vpn_gateways::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::vpn_gateways::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::vpn_gateways::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::vpn_gateways::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::vpn_gateways::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::vpn_gateways::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::vpn_gateways::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::vpn_gateways::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::vpn_gateways::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::vpn_gateways::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::vpn_gateways::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::vpn_gateways::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::vpn_gateways::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [VpnGateways::delete][crate::client::VpnGateways::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::vpn_gateways::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::vpn_gateways::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VpnGateways>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::vpn_gateways::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::VpnGateways::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::vpn_gateways::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::vpn_gateways::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::vpn_gateways::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::vpn_gateways::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [vpn_gateway][crate::model::vpn_gateways::DeleteRequest::vpn_gateway].
        pub fn set_vpn_gateway<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.vpn_gateway = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [VpnGateways::get][crate::client::VpnGateways::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::vpn_gateways::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::vpn_gateways::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VpnGateways>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::vpn_gateways::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::VpnGateway> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::vpn_gateways::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::vpn_gateways::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [vpn_gateway][crate::model::vpn_gateways::GetRequest::vpn_gateway].
        pub fn set_vpn_gateway<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.vpn_gateway = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [VpnGateways::get_status][crate::client::VpnGateways::get_status] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::vpn_gateways::GetStatus;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetStatus {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetStatus(RequestBuilder<crate::model::vpn_gateways::GetStatusRequest>);

    impl GetStatus {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VpnGateways>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::vpn_gateways::GetStatusRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::VpnGatewaysGetStatusResponse> {
            (*self.0.stub)
                .get_status(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::vpn_gateways::GetStatusRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::vpn_gateways::GetStatusRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [vpn_gateway][crate::model::vpn_gateways::GetStatusRequest::vpn_gateway].
        pub fn set_vpn_gateway<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.vpn_gateway = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetStatus {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [VpnGateways::insert][crate::client::VpnGateways::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::vpn_gateways::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::vpn_gateways::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VpnGateways>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::vpn_gateways::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::VpnGateways::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::vpn_gateways::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::vpn_gateways::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::vpn_gateways::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::vpn_gateways::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::vpn_gateways::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::VpnGateway>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::vpn_gateways::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::VpnGateway>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [VpnGateways::list][crate::client::VpnGateways::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::vpn_gateways::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::vpn_gateways::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VpnGateways>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::vpn_gateways::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::VpnGatewayList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::VpnGatewayList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::VpnGatewayList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::vpn_gateways::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::vpn_gateways::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::vpn_gateways::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::vpn_gateways::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::vpn_gateways::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::vpn_gateways::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::vpn_gateways::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::vpn_gateways::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::vpn_gateways::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::vpn_gateways::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::vpn_gateways::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::vpn_gateways::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [VpnGateways::set_labels][crate::client::VpnGateways::set_labels] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::vpn_gateways::SetLabels;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetLabels {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetLabels(RequestBuilder<crate::model::vpn_gateways::SetLabelsRequest>);

    impl SetLabels {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VpnGateways>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::vpn_gateways::SetLabelsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_labels(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_labels`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::VpnGateways::set_labels::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::vpn_gateways::SetLabelsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::vpn_gateways::SetLabelsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::vpn_gateways::SetLabelsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::vpn_gateways::SetLabelsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [resource][crate::model::vpn_gateways::SetLabelsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::vpn_gateways::SetLabelsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetLabelsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::vpn_gateways::SetLabelsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetLabelsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetLabels {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [VpnGateways::test_iam_permissions][crate::client::VpnGateways::test_iam_permissions] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::vpn_gateways::TestIamPermissions;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> TestIamPermissions {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct TestIamPermissions(
        RequestBuilder<crate::model::vpn_gateways::TestIamPermissionsRequest>,
    );

    impl TestIamPermissions {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VpnGateways>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::vpn_gateways::TestIamPermissionsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::TestPermissionsResponse> {
            (*self.0.stub)
                .test_iam_permissions(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::vpn_gateways::TestIamPermissionsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::vpn_gateways::TestIamPermissionsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [resource][crate::model::vpn_gateways::TestIamPermissionsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::vpn_gateways::TestIamPermissionsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::vpn_gateways::TestIamPermissionsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::TestPermissionsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for TestIamPermissions {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [VpnGateways::get_operation][crate::client::VpnGateways::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::vpn_gateways::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VpnGateways>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [VpnTunnels][crate::client::VpnTunnels].
#[cfg(feature = "vpn-tunnels")]
#[cfg_attr(docsrs, doc(cfg(feature = "vpn-tunnels")))]
pub mod vpn_tunnels {
    use crate::Result;

    /// A builder for [VpnTunnels][crate::client::VpnTunnels].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::vpn_tunnels::ClientBuilder;
    /// # use client::VpnTunnels;
    /// let builder : ClientBuilder = VpnTunnels::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::VpnTunnels;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = VpnTunnels;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::VpnTunnels] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::VpnTunnels>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VpnTunnels>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [VpnTunnels::aggregated_list][crate::client::VpnTunnels::aggregated_list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::vpn_tunnels::AggregatedList;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> AggregatedList {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct AggregatedList(RequestBuilder<crate::model::vpn_tunnels::AggregatedListRequest>);

    impl AggregatedList {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VpnTunnels>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::vpn_tunnels::AggregatedListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::VpnTunnelAggregatedList> {
            (*self.0.stub)
                .aggregated_list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<
            crate::model::VpnTunnelAggregatedList,
            crate::Error,
        > {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::VpnTunnelAggregatedList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::vpn_tunnels::AggregatedListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::vpn_tunnels::AggregatedListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [include_all_scopes][crate::model::vpn_tunnels::AggregatedListRequest::include_all_scopes].
        pub fn set_include_all_scopes<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [include_all_scopes][crate::model::vpn_tunnels::AggregatedListRequest::include_all_scopes].
        pub fn set_or_clear_include_all_scopes<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.include_all_scopes = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::vpn_tunnels::AggregatedListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::vpn_tunnels::AggregatedListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::vpn_tunnels::AggregatedListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::vpn_tunnels::AggregatedListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::vpn_tunnels::AggregatedListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::vpn_tunnels::AggregatedListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::vpn_tunnels::AggregatedListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::vpn_tunnels::AggregatedListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::vpn_tunnels::AggregatedListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [service_project_number][crate::model::vpn_tunnels::AggregatedListRequest::service_project_number].
        pub fn set_service_project_number<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [service_project_number][crate::model::vpn_tunnels::AggregatedListRequest::service_project_number].
        pub fn set_or_clear_service_project_number<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<i64>,
        {
            self.0.request.service_project_number = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for AggregatedList {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [VpnTunnels::delete][crate::client::VpnTunnels::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::vpn_tunnels::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::vpn_tunnels::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VpnTunnels>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::vpn_tunnels::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::VpnTunnels::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::vpn_tunnels::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::vpn_tunnels::DeleteRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::vpn_tunnels::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::vpn_tunnels::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [vpn_tunnel][crate::model::vpn_tunnels::DeleteRequest::vpn_tunnel].
        pub fn set_vpn_tunnel<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.vpn_tunnel = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [VpnTunnels::get][crate::client::VpnTunnels::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::vpn_tunnels::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::vpn_tunnels::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VpnTunnels>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::vpn_tunnels::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::VpnTunnel> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::vpn_tunnels::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::vpn_tunnels::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [vpn_tunnel][crate::model::vpn_tunnels::GetRequest::vpn_tunnel].
        pub fn set_vpn_tunnel<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.vpn_tunnel = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [VpnTunnels::insert][crate::client::VpnTunnels::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::vpn_tunnels::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::vpn_tunnels::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VpnTunnels>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::vpn_tunnels::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::VpnTunnels::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::vpn_tunnels::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::vpn_tunnels::InsertRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::vpn_tunnels::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::vpn_tunnels::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::vpn_tunnels::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::VpnTunnel>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::vpn_tunnels::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::VpnTunnel>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [VpnTunnels::list][crate::client::VpnTunnels::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::vpn_tunnels::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::vpn_tunnels::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VpnTunnels>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::vpn_tunnels::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::VpnTunnelList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::VpnTunnelList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::VpnTunnelList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::vpn_tunnels::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::vpn_tunnels::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::vpn_tunnels::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::vpn_tunnels::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::vpn_tunnels::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::vpn_tunnels::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::vpn_tunnels::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::vpn_tunnels::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::vpn_tunnels::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::vpn_tunnels::ListRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::vpn_tunnels::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::vpn_tunnels::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [VpnTunnels::set_labels][crate::client::VpnTunnels::set_labels] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::vpn_tunnels::SetLabels;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> SetLabels {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct SetLabels(RequestBuilder<crate::model::vpn_tunnels::SetLabelsRequest>);

    impl SetLabels {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VpnTunnels>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::vpn_tunnels::SetLabelsRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .set_labels(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `set_labels`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::VpnTunnels::set_labels::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let region = self.0.request.region.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let region = region.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_region(region)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::vpn_tunnels::SetLabelsRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::vpn_tunnels::SetLabelsRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::vpn_tunnels::SetLabelsRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::vpn_tunnels::SetLabelsRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [resource][crate::model::vpn_tunnels::SetLabelsRequest::resource].
        pub fn set_resource<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.resource = v.into();
            self
        }

        /// Sets the value of [body][crate::model::vpn_tunnels::SetLabelsRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetLabelsRequest>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::vpn_tunnels::SetLabelsRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::RegionSetLabelsRequest>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for SetLabels {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [VpnTunnels::get_operation][crate::client::VpnTunnels::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::vpn_tunnels::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::region_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::VpnTunnels>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::region_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::region_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::region_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [region][crate::model::region_operations::GetRequest::region].
        pub fn set_region<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.region = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [WireGroups][crate::client::WireGroups].
#[cfg(feature = "wire-groups")]
#[cfg_attr(docsrs, doc(cfg(feature = "wire-groups")))]
pub mod wire_groups {
    use crate::Result;

    /// A builder for [WireGroups][crate::client::WireGroups].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::wire_groups::ClientBuilder;
    /// # use client::WireGroups;
    /// let builder : ClientBuilder = WireGroups::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::WireGroups;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = WireGroups;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::WireGroups] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::WireGroups>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::WireGroups>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [WireGroups::delete][crate::client::WireGroups::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::wire_groups::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::wire_groups::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::WireGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::wire_groups::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::WireGroups::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [cross_site_network][crate::model::wire_groups::DeleteRequest::cross_site_network].
        pub fn set_cross_site_network<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.cross_site_network = v.into();
            self
        }

        /// Sets the value of [project][crate::model::wire_groups::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::wire_groups::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::wire_groups::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [wire_group][crate::model::wire_groups::DeleteRequest::wire_group].
        pub fn set_wire_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.wire_group = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [WireGroups::get][crate::client::WireGroups::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::wire_groups::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::wire_groups::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::WireGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::wire_groups::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::WireGroup> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [cross_site_network][crate::model::wire_groups::GetRequest::cross_site_network].
        pub fn set_cross_site_network<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.cross_site_network = v.into();
            self
        }

        /// Sets the value of [project][crate::model::wire_groups::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [wire_group][crate::model::wire_groups::GetRequest::wire_group].
        pub fn set_wire_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.wire_group = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [WireGroups::insert][crate::client::WireGroups::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::wire_groups::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::wire_groups::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::WireGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::wire_groups::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::WireGroups::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [cross_site_network][crate::model::wire_groups::InsertRequest::cross_site_network].
        pub fn set_cross_site_network<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.cross_site_network = v.into();
            self
        }

        /// Sets the value of [project][crate::model::wire_groups::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::wire_groups::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::wire_groups::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [validate_only][crate::model::wire_groups::InsertRequest::validate_only].
        pub fn set_validate_only<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.validate_only = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [validate_only][crate::model::wire_groups::InsertRequest::validate_only].
        pub fn set_or_clear_validate_only<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.validate_only = v.map(|x| x.into());
            self
        }

        /// Sets the value of [body][crate::model::wire_groups::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::WireGroup>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::wire_groups::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::WireGroup>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [WireGroups::list][crate::client::WireGroups::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::wire_groups::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::wire_groups::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::WireGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::wire_groups::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::WireGroupList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::WireGroupList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::WireGroupList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [cross_site_network][crate::model::wire_groups::ListRequest::cross_site_network].
        pub fn set_cross_site_network<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.cross_site_network = v.into();
            self
        }

        /// Sets the value of [filter][crate::model::wire_groups::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::wire_groups::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::wire_groups::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::wire_groups::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::wire_groups::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::wire_groups::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::wire_groups::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::wire_groups::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::wire_groups::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::wire_groups::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::wire_groups::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [WireGroups::patch][crate::client::WireGroups::patch] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::wire_groups::Patch;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Patch {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Patch(RequestBuilder<crate::model::wire_groups::PatchRequest>);

    impl Patch {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::WireGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::wire_groups::PatchRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .patch(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `patch`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::WireGroups::patch::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [cross_site_network][crate::model::wire_groups::PatchRequest::cross_site_network].
        pub fn set_cross_site_network<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.cross_site_network = v.into();
            self
        }

        /// Sets the value of [project][crate::model::wire_groups::PatchRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::wire_groups::PatchRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::wire_groups::PatchRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [update_mask][crate::model::wire_groups::PatchRequest::update_mask].
        pub fn set_update_mask<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [update_mask][crate::model::wire_groups::PatchRequest::update_mask].
        pub fn set_or_clear_update_mask<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<wkt::FieldMask>,
        {
            self.0.request.update_mask = v.map(|x| x.into());
            self
        }

        /// Sets the value of [validate_only][crate::model::wire_groups::PatchRequest::validate_only].
        pub fn set_validate_only<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.validate_only = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [validate_only][crate::model::wire_groups::PatchRequest::validate_only].
        pub fn set_or_clear_validate_only<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.validate_only = v.map(|x| x.into());
            self
        }

        /// Sets the value of [wire_group][crate::model::wire_groups::PatchRequest::wire_group].
        pub fn set_wire_group<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.wire_group = v.into();
            self
        }

        /// Sets the value of [body][crate::model::wire_groups::PatchRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::WireGroup>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::wire_groups::PatchRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::WireGroup>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Patch {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [WireGroups::get_operation][crate::client::WireGroups::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::wire_groups::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::global_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::WireGroups>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::global_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::global_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::global_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [ZoneOperations][crate::client::ZoneOperations].
#[cfg(feature = "zone-operations")]
#[cfg_attr(docsrs, doc(cfg(feature = "zone-operations")))]
pub mod zone_operations {
    use crate::Result;

    /// A builder for [ZoneOperations][crate::client::ZoneOperations].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::zone_operations::ClientBuilder;
    /// # use client::ZoneOperations;
    /// let builder : ClientBuilder = ZoneOperations::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::ZoneOperations;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = ZoneOperations;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::ZoneOperations] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::ZoneOperations>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ZoneOperations>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [ZoneOperations::delete][crate::client::ZoneOperations::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::zone_operations::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::zone_operations::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ZoneOperations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_operations::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<()> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::zone_operations::DeleteRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::zone_operations::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zone_operations::DeleteRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ZoneOperations::get][crate::client::ZoneOperations::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::zone_operations::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::zone_operations::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ZoneOperations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::zone_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::zone_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zone_operations::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ZoneOperations::list][crate::client::ZoneOperations::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::zone_operations::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::zone_operations::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ZoneOperations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_operations::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::OperationList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::OperationList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::OperationList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::zone_operations::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::zone_operations::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::zone_operations::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::zone_operations::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::zone_operations::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::zone_operations::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::zone_operations::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::zone_operations::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::zone_operations::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::zone_operations::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::zone_operations::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::zone_operations::ListRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ZoneOperations::wait][crate::client::ZoneOperations::wait] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::zone_operations::Wait;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Wait {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Wait(RequestBuilder<crate::model::zone_operations::WaitRequest>);

    impl Wait {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ZoneOperations>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_operations::WaitRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .wait(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::zone_operations::WaitRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::zone_operations::WaitRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zone_operations::WaitRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Wait {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [ZoneVmExtensionPolicies][crate::client::ZoneVmExtensionPolicies].
#[cfg(feature = "zone-vm-extension-policies")]
#[cfg_attr(docsrs, doc(cfg(feature = "zone-vm-extension-policies")))]
pub mod zone_vm_extension_policies {
    use crate::Result;

    /// A builder for [ZoneVmExtensionPolicies][crate::client::ZoneVmExtensionPolicies].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::zone_vm_extension_policies::ClientBuilder;
    /// # use client::ZoneVmExtensionPolicies;
    /// let builder : ClientBuilder = ZoneVmExtensionPolicies::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::ZoneVmExtensionPolicies;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = ZoneVmExtensionPolicies;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::ZoneVmExtensionPolicies] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::ZoneVmExtensionPolicies>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ZoneVmExtensionPolicies>,
        ) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [ZoneVmExtensionPolicies::delete][crate::client::ZoneVmExtensionPolicies::delete] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::zone_vm_extension_policies::Delete;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Delete {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Delete(RequestBuilder<crate::model::zone_vm_extension_policies::DeleteRequest>);

    impl Delete {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ZoneVmExtensionPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_vm_extension_policies::DeleteRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .delete(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `delete`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::ZoneVmExtensionPolicies::delete::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::zone_vm_extension_policies::DeleteRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::zone_vm_extension_policies::DeleteRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::zone_vm_extension_policies::DeleteRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [vm_extension_policy][crate::model::zone_vm_extension_policies::DeleteRequest::vm_extension_policy].
        pub fn set_vm_extension_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.vm_extension_policy = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zone_vm_extension_policies::DeleteRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Delete {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ZoneVmExtensionPolicies::get][crate::client::ZoneVmExtensionPolicies::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::zone_vm_extension_policies::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::zone_vm_extension_policies::GetRequest>);

    impl Get {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ZoneVmExtensionPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_vm_extension_policies::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::VmExtensionPolicy> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::zone_vm_extension_policies::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [vm_extension_policy][crate::model::zone_vm_extension_policies::GetRequest::vm_extension_policy].
        pub fn set_vm_extension_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.vm_extension_policy = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zone_vm_extension_policies::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ZoneVmExtensionPolicies::insert][crate::client::ZoneVmExtensionPolicies::insert] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::zone_vm_extension_policies::Insert;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Insert {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Insert(RequestBuilder<crate::model::zone_vm_extension_policies::InsertRequest>);

    impl Insert {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ZoneVmExtensionPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_vm_extension_policies::InsertRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .insert(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `insert`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::ZoneVmExtensionPolicies::insert::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::zone_vm_extension_policies::InsertRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::zone_vm_extension_policies::InsertRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::zone_vm_extension_policies::InsertRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::zone_vm_extension_policies::InsertRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::zone_vm_extension_policies::InsertRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::VmExtensionPolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::zone_vm_extension_policies::InsertRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::VmExtensionPolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Insert {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ZoneVmExtensionPolicies::list][crate::client::ZoneVmExtensionPolicies::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::zone_vm_extension_policies::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::zone_vm_extension_policies::ListRequest>);

    impl List {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ZoneVmExtensionPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_vm_extension_policies::ListRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::VmExtensionPolicyList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::VmExtensionPolicyList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<
            crate::model::VmExtensionPolicyList,
            crate::Error,
        > {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::zone_vm_extension_policies::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::zone_vm_extension_policies::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::zone_vm_extension_policies::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::zone_vm_extension_policies::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::zone_vm_extension_policies::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::zone_vm_extension_policies::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::zone_vm_extension_policies::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::zone_vm_extension_policies::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::zone_vm_extension_policies::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::zone_vm_extension_policies::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::zone_vm_extension_policies::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }

        /// Sets the value of [zone][crate::model::zone_vm_extension_policies::ListRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ZoneVmExtensionPolicies::update][crate::client::ZoneVmExtensionPolicies::update] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::zone_vm_extension_policies::Update;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Update {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Update(RequestBuilder<crate::model::zone_vm_extension_policies::UpdateRequest>);

    impl Update {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ZoneVmExtensionPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_vm_extension_policies::UpdateRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .update(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Creates a [Poller][google_cloud_lro::Poller] to work with `update`.
        pub fn poller(
            self,
        ) -> impl google_cloud_lro::Poller<crate::model::Operation, crate::model::Operation>
        {
            let polling_error_policy = self.0.stub.get_polling_error_policy(&self.0.options);
            let polling_backoff_policy = self.0.stub.get_polling_backoff_policy(&self.0.options);
            let mut poller_options = self.0.stub.get_poller_options(&self.0.options);
            if let Some(ref mut details) = poller_options.tracing {
                details.method_name =
                    "google_cloud_compute_v1::client::ZoneVmExtensionPolicies::update::until_done";
            }

            let stub = self.0.stub.clone();
            let mut options = self.0.options.clone();
            options.set_retry_policy(google_cloud_gax::retry_policy::NeverRetry);
            let project = self.0.request.project.clone();
            let zone = self.0.request.zone.clone();
            let query = move |name| {
                let stub = stub.clone();
                let options = options.clone();
                let project = project.clone();
                let zone = zone.clone();
                async {
                    GetOperation::new(stub)
                        .set_project(project)
                        .set_zone(zone)
                        .set_operation(name)
                        .with_options(options)
                        .send()
                        .await
                }
            };

            let start = move || async { self.send().await };

            use google_cloud_lro::internal::PollerExt;
            google_cloud_lro::internal::new_discovery_poller(
                polling_error_policy,
                polling_backoff_policy,
                start,
                query,
            )
            .with_options(poller_options)
        }

        /// Sets the value of [project][crate::model::zone_vm_extension_policies::UpdateRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [request_id][crate::model::zone_vm_extension_policies::UpdateRequest::request_id].
        pub fn set_request_id<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [request_id][crate::model::zone_vm_extension_policies::UpdateRequest::request_id].
        pub fn set_or_clear_request_id<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.request_id = v.map(|x| x.into());
            self
        }

        /// Sets the value of [vm_extension_policy][crate::model::zone_vm_extension_policies::UpdateRequest::vm_extension_policy].
        pub fn set_vm_extension_policy<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.vm_extension_policy = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zone_vm_extension_policies::UpdateRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }

        /// Sets the value of [body][crate::model::zone_vm_extension_policies::UpdateRequest::body].
        pub fn set_body<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<crate::model::VmExtensionPolicy>,
        {
            self.0.request.body = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [body][crate::model::zone_vm_extension_policies::UpdateRequest::body].
        pub fn set_or_clear_body<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<crate::model::VmExtensionPolicy>,
        {
            self.0.request.body = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Update {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [ZoneVmExtensionPolicies::get_operation][crate::client::ZoneVmExtensionPolicies::get_operation] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::zone_vm_extension_policies::GetOperation;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> GetOperation {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct GetOperation(RequestBuilder<crate::model::zone_operations::GetRequest>);

    impl GetOperation {
        pub(crate) fn new(
            stub: std::sync::Arc<dyn super::super::stub::dynamic::ZoneVmExtensionPolicies>,
        ) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zone_operations::GetRequest>>(
            mut self,
            v: V,
        ) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Operation> {
            (*self.0.stub)
                .get_operation(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [operation][crate::model::zone_operations::GetRequest::operation].
        pub fn set_operation<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.operation = v.into();
            self
        }

        /// Sets the value of [project][crate::model::zone_operations::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zone_operations::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for GetOperation {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}

/// Request and client builders for [Zones][crate::client::Zones].
#[cfg(feature = "zones")]
#[cfg_attr(docsrs, doc(cfg(feature = "zones")))]
pub mod zones {
    use crate::Result;

    /// A builder for [Zones][crate::client::Zones].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_cloud_compute_v1::*;
    /// # use builder::zones::ClientBuilder;
    /// # use client::Zones;
    /// let builder : ClientBuilder = Zones::builder();
    /// let client = builder
    ///     .with_endpoint("https://compute.googleapis.com")
    ///     .build().await?;
    /// # Ok(()) }
    /// ```
    pub type ClientBuilder = crate::ClientBuilder<client::Factory, gaxi::options::Credentials>;

    pub(crate) mod client {
        use super::super::super::client::Zones;
        pub struct Factory;
        impl crate::ClientFactory for Factory {
            type Client = Zones;
            type Credentials = gaxi::options::Credentials;
            async fn build(
                self,
                config: gaxi::options::ClientConfig,
            ) -> crate::ClientBuilderResult<Self::Client> {
                Self::Client::new(config).await
            }
        }
    }

    /// Common implementation for [crate::client::Zones] request builders.
    #[derive(Clone, Debug)]
    pub(crate) struct RequestBuilder<R: std::default::Default> {
        stub: std::sync::Arc<dyn super::super::stub::dynamic::Zones>,
        request: R,
        options: crate::RequestOptions,
    }

    impl<R> RequestBuilder<R>
    where
        R: std::default::Default,
    {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Zones>) -> Self {
            Self {
                stub,
                request: R::default(),
                options: crate::RequestOptions::default(),
            }
        }
    }

    /// The request builder for [Zones::get][crate::client::Zones::get] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::zones::Get;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    ///
    /// let builder = prepare_request_builder();
    /// let response = builder.send().await?;
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> Get {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct Get(RequestBuilder<crate::model::zones::GetRequest>);

    impl Get {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Zones>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zones::GetRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::Zone> {
            (*self.0.stub)
                .get(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Sets the value of [project][crate::model::zones::GetRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [zone][crate::model::zones::GetRequest::zone].
        pub fn set_zone<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.zone = v.into();
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for Get {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }

    /// The request builder for [Zones::list][crate::client::Zones::list] calls.
    ///
    /// # Example
    /// ```
    /// # use google_cloud_compute_v1::builder::zones::List;
    /// # async fn sample() -> google_cloud_compute_v1::Result<()> {
    /// use google_cloud_gax::paginator::ItemPaginator;
    ///
    /// let builder = prepare_request_builder();
    /// let mut items = builder.by_item();
    /// while let Some(result) = items.next().await {
    ///   let item = result?;
    /// }
    /// # Ok(()) }
    ///
    /// fn prepare_request_builder() -> List {
    ///   # panic!();
    ///   // ... details omitted ...
    /// }
    /// ```
    #[derive(Clone, Debug)]
    pub struct List(RequestBuilder<crate::model::zones::ListRequest>);

    impl List {
        pub(crate) fn new(stub: std::sync::Arc<dyn super::super::stub::dynamic::Zones>) -> Self {
            Self(RequestBuilder::new(stub))
        }

        /// Sets the full request, replacing any prior values.
        pub fn with_request<V: Into<crate::model::zones::ListRequest>>(mut self, v: V) -> Self {
            self.0.request = v.into();
            self
        }

        /// Sets all the options, replacing any prior values.
        pub fn with_options<V: Into<crate::RequestOptions>>(mut self, v: V) -> Self {
            self.0.options = v.into();
            self
        }

        /// Sends the request.
        pub async fn send(self) -> Result<crate::model::ZoneList> {
            (*self.0.stub)
                .list(self.0.request, self.0.options)
                .await
                .map(crate::Response::into_body)
        }

        /// Streams each page in the collection.
        pub fn by_page(
            self,
        ) -> impl google_cloud_gax::paginator::Paginator<crate::model::ZoneList, crate::Error>
        {
            use std::clone::Clone;
            let token = self.0.request.page_token.clone().unwrap_or_default();
            let execute = move |token: String| {
                let mut builder = self.clone();
                builder.0.request = builder.0.request.set_page_token(token);
                builder.send()
            };
            google_cloud_gax::paginator::internal::new_paginator(token, execute)
        }

        /// Streams each item in the collection.
        pub fn by_item(
            self,
        ) -> impl google_cloud_gax::paginator::ItemPaginator<crate::model::ZoneList, crate::Error>
        {
            use google_cloud_gax::paginator::Paginator;
            self.by_page().items()
        }

        /// Sets the value of [filter][crate::model::zones::ListRequest::filter].
        pub fn set_filter<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [filter][crate::model::zones::ListRequest::filter].
        pub fn set_or_clear_filter<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.filter = v.map(|x| x.into());
            self
        }

        /// Sets the value of [max_results][crate::model::zones::ListRequest::max_results].
        pub fn set_max_results<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [max_results][crate::model::zones::ListRequest::max_results].
        pub fn set_or_clear_max_results<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<u32>,
        {
            self.0.request.max_results = v.map(|x| x.into());
            self
        }

        /// Sets the value of [order_by][crate::model::zones::ListRequest::order_by].
        pub fn set_order_by<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [order_by][crate::model::zones::ListRequest::order_by].
        pub fn set_or_clear_order_by<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.order_by = v.map(|x| x.into());
            self
        }

        /// Sets the value of [page_token][crate::model::zones::ListRequest::page_token].
        pub fn set_page_token<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [page_token][crate::model::zones::ListRequest::page_token].
        pub fn set_or_clear_page_token<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<std::string::String>,
        {
            self.0.request.page_token = v.map(|x| x.into());
            self
        }

        /// Sets the value of [project][crate::model::zones::ListRequest::project].
        pub fn set_project<T: Into<std::string::String>>(mut self, v: T) -> Self {
            self.0.request.project = v.into();
            self
        }

        /// Sets the value of [return_partial_success][crate::model::zones::ListRequest::return_partial_success].
        pub fn set_return_partial_success<T>(mut self, v: T) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = std::option::Option::Some(v.into());
            self
        }

        /// Sets or clears the value of [return_partial_success][crate::model::zones::ListRequest::return_partial_success].
        pub fn set_or_clear_return_partial_success<T>(mut self, v: std::option::Option<T>) -> Self
        where
            T: std::convert::Into<bool>,
        {
            self.0.request.return_partial_success = v.map(|x| x.into());
            self
        }
    }

    #[doc(hidden)]
    impl crate::RequestBuilder for List {
        fn request_options(&mut self) -> &mut crate::RequestOptions {
            &mut self.0.options
        }
    }
}
