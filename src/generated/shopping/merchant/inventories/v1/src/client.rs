// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// Code generated by sidekick. DO NOT EDIT.
#![allow(rustdoc::bare_urls)]
#![allow(rustdoc::broken_intra_doc_links)]
#![allow(rustdoc::invalid_html_tags)]
#![allow(rustdoc::redundant_explicit_links)]

/// Implements a client for the Merchant API.
///
/// # Example
/// ```
/// # use google_shopping_merchant_inventories_v1::client::LocalInventoryService;
/// use google_cloud_gax::paginator::ItemPaginator as _;
/// async fn sample(
///    account_id: &str,
///    product_id: &str,
/// ) -> anyhow::Result<()> {
///     let client = LocalInventoryService::builder().build().await?;
///     let mut list = client.list_local_inventories()
///         .set_parent(format!("accounts/{account_id}/products/{product_id}"))
///         .by_item();
///     while let Some(item) = list.next().await.transpose()? {
///         println!("{:?}", item);
///     }
///     Ok(())
/// }
/// ```
///
/// # Service Description
///
/// Service to manage local inventory for products
///
/// # Configuration
///
/// To configure `LocalInventoryService` use the `with_*` methods in the type returned
/// by [builder()][LocalInventoryService::builder]. The default configuration should
/// work for most applications. Common configuration changes include
///
/// * [with_endpoint()]: by default this client uses the global default endpoint
///   (`https://merchantapi.googleapis.com`). Applications using regional
///   endpoints or running in restricted networks (e.g. a network configured
///   with [Private Google Access with VPC Service Controls]) may want to
///   override this default.
/// * [with_credentials()]: by default this client uses
///   [Application Default Credentials]. Applications using custom
///   authentication may need to override this default.
///
/// [with_endpoint()]: super::builder::local_inventory_service::ClientBuilder::with_endpoint
/// [with_credentials()]: super::builder::local_inventory_service::ClientBuilder::with_credentials
/// [Private Google Access with VPC Service Controls]: https://cloud.google.com/vpc-service-controls/docs/private-connectivity
/// [Application Default Credentials]: https://cloud.google.com/docs/authentication#adc
///
/// # Pooling and Cloning
///
/// `LocalInventoryService` holds a connection pool internally, it is advised to
/// create one and reuse it. You do not need to wrap `LocalInventoryService` in
/// an [Rc](std::rc::Rc) or [Arc](std::sync::Arc) to reuse it, because it
/// already uses an `Arc` internally.
#[derive(Clone, Debug)]
pub struct LocalInventoryService {
    inner: std::sync::Arc<dyn super::stub::dynamic::LocalInventoryService>,
}

impl LocalInventoryService {
    /// Returns a builder for [LocalInventoryService].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_shopping_merchant_inventories_v1::client::LocalInventoryService;
    /// let client = LocalInventoryService::builder().build().await?;
    /// # Ok(()) }
    /// ```
    pub fn builder() -> super::builder::local_inventory_service::ClientBuilder {
        crate::new_client_builder(super::builder::local_inventory_service::client::Factory)
    }

    /// Creates a new client from the provided stub.
    ///
    /// The most common case for calling this function is in tests mocking the
    /// client's behavior.
    pub fn from_stub<T>(stub: impl Into<std::sync::Arc<T>>) -> Self
    where
        T: super::stub::LocalInventoryService + 'static,
    {
        Self { inner: stub.into() }
    }

    pub(crate) async fn new(
        config: gaxi::options::ClientConfig,
    ) -> crate::ClientBuilderResult<Self> {
        let inner = Self::build_inner(config).await?;
        Ok(Self { inner })
    }

    async fn build_inner(
        conf: gaxi::options::ClientConfig,
    ) -> crate::ClientBuilderResult<std::sync::Arc<dyn super::stub::dynamic::LocalInventoryService>>
    {
        if gaxi::options::tracing_enabled(&conf) {
            return Ok(std::sync::Arc::new(Self::build_with_tracing(conf).await?));
        }
        Ok(std::sync::Arc::new(Self::build_transport(conf).await?))
    }

    async fn build_transport(
        conf: gaxi::options::ClientConfig,
    ) -> crate::ClientBuilderResult<impl super::stub::LocalInventoryService> {
        super::transport::LocalInventoryService::new(conf).await
    }

    async fn build_with_tracing(
        conf: gaxi::options::ClientConfig,
    ) -> crate::ClientBuilderResult<impl super::stub::LocalInventoryService> {
        Self::build_transport(conf)
            .await
            .map(super::tracing::LocalInventoryService::new)
    }

    /// Lists the `LocalInventory` resources for the given product in your merchant
    /// account. The response might contain fewer items than specified by
    /// `pageSize`. If `pageToken` was returned in previous request, it can be used
    /// to obtain additional results.
    ///
    /// `LocalInventory` resources are listed per product for a given account.
    ///
    /// # Example
    /// ```
    /// # use google_shopping_merchant_inventories_v1::client::LocalInventoryService;
    /// use google_cloud_gax::paginator::ItemPaginator as _;
    /// use google_shopping_merchant_inventories_v1::Result;
    /// async fn sample(
    ///    client: &LocalInventoryService, account_id: &str, product_id: &str
    /// ) -> Result<()> {
    ///     let mut list = client.list_local_inventories()
    ///         .set_parent(format!("accounts/{account_id}/products/{product_id}"))
    ///         .by_item();
    ///     while let Some(item) = list.next().await.transpose()? {
    ///         println!("{:?}", item);
    ///     }
    ///     Ok(())
    /// }
    /// ```
    pub fn list_local_inventories(
        &self,
    ) -> super::builder::local_inventory_service::ListLocalInventories {
        super::builder::local_inventory_service::ListLocalInventories::new(self.inner.clone())
    }

    /// Inserts a `LocalInventory` resource to a product in your merchant
    /// account.
    ///
    /// Replaces the full `LocalInventory` resource if an entry with the same
    /// [`storeCode`][google.shopping.merchant.inventories.v1.LocalInventory.store_code]
    /// already exists for the product.
    ///
    /// It might take up to 30 minutes for the new or updated `LocalInventory`
    /// resource to appear in products.
    ///
    /// [google.shopping.merchant.inventories.v1.LocalInventory.store_code]: crate::model::LocalInventory::store_code
    ///
    /// # Example
    /// ```
    /// # use google_shopping_merchant_inventories_v1::client::LocalInventoryService;
    /// use google_shopping_merchant_inventories_v1::Result;
    /// async fn sample(
    ///    client: &LocalInventoryService
    /// ) -> Result<()> {
    ///     let response = client.insert_local_inventory()
    ///         /* set fields */
    ///         .send().await?;
    ///     println!("response {:?}", response);
    ///     Ok(())
    /// }
    /// ```
    pub fn insert_local_inventory(
        &self,
    ) -> super::builder::local_inventory_service::InsertLocalInventory {
        super::builder::local_inventory_service::InsertLocalInventory::new(self.inner.clone())
    }

    /// Deletes the specified `LocalInventory` from the given product in your
    /// merchant account. It might take a up to an hour for the
    /// `LocalInventory` to be deleted from the specific product.
    /// Once you have received a successful delete response, wait for that
    /// period before attempting a delete again.
    ///
    /// # Example
    /// ```
    /// # use google_shopping_merchant_inventories_v1::client::LocalInventoryService;
    /// use google_shopping_merchant_inventories_v1::Result;
    /// async fn sample(
    ///    client: &LocalInventoryService, account_id: &str, product_id: &str, store_code_id: &str
    /// ) -> Result<()> {
    ///     client.delete_local_inventory()
    ///         .set_name(format!("accounts/{account_id}/products/{product_id}/localInventories/{store_code_id}"))
    ///         .send().await?;
    ///     Ok(())
    /// }
    /// ```
    pub fn delete_local_inventory(
        &self,
    ) -> super::builder::local_inventory_service::DeleteLocalInventory {
        super::builder::local_inventory_service::DeleteLocalInventory::new(self.inner.clone())
    }
}

/// Implements a client for the Merchant API.
///
/// # Example
/// ```
/// # use google_shopping_merchant_inventories_v1::client::RegionalInventoryService;
/// use google_cloud_gax::paginator::ItemPaginator as _;
/// async fn sample(
///    account_id: &str,
///    product_id: &str,
/// ) -> anyhow::Result<()> {
///     let client = RegionalInventoryService::builder().build().await?;
///     let mut list = client.list_regional_inventories()
///         .set_parent(format!("accounts/{account_id}/products/{product_id}"))
///         .by_item();
///     while let Some(item) = list.next().await.transpose()? {
///         println!("{:?}", item);
///     }
///     Ok(())
/// }
/// ```
///
/// # Service Description
///
/// Service to manage regional inventory for products. There is also separate
/// `regions` resource and API to manage regions definitions.
///
/// # Configuration
///
/// To configure `RegionalInventoryService` use the `with_*` methods in the type returned
/// by [builder()][RegionalInventoryService::builder]. The default configuration should
/// work for most applications. Common configuration changes include
///
/// * [with_endpoint()]: by default this client uses the global default endpoint
///   (`https://merchantapi.googleapis.com`). Applications using regional
///   endpoints or running in restricted networks (e.g. a network configured
///   with [Private Google Access with VPC Service Controls]) may want to
///   override this default.
/// * [with_credentials()]: by default this client uses
///   [Application Default Credentials]. Applications using custom
///   authentication may need to override this default.
///
/// [with_endpoint()]: super::builder::regional_inventory_service::ClientBuilder::with_endpoint
/// [with_credentials()]: super::builder::regional_inventory_service::ClientBuilder::with_credentials
/// [Private Google Access with VPC Service Controls]: https://cloud.google.com/vpc-service-controls/docs/private-connectivity
/// [Application Default Credentials]: https://cloud.google.com/docs/authentication#adc
///
/// # Pooling and Cloning
///
/// `RegionalInventoryService` holds a connection pool internally, it is advised to
/// create one and reuse it. You do not need to wrap `RegionalInventoryService` in
/// an [Rc](std::rc::Rc) or [Arc](std::sync::Arc) to reuse it, because it
/// already uses an `Arc` internally.
#[derive(Clone, Debug)]
pub struct RegionalInventoryService {
    inner: std::sync::Arc<dyn super::stub::dynamic::RegionalInventoryService>,
}

impl RegionalInventoryService {
    /// Returns a builder for [RegionalInventoryService].
    ///
    /// ```
    /// # async fn sample() -> google_cloud_gax::client_builder::Result<()> {
    /// # use google_shopping_merchant_inventories_v1::client::RegionalInventoryService;
    /// let client = RegionalInventoryService::builder().build().await?;
    /// # Ok(()) }
    /// ```
    pub fn builder() -> super::builder::regional_inventory_service::ClientBuilder {
        crate::new_client_builder(super::builder::regional_inventory_service::client::Factory)
    }

    /// Creates a new client from the provided stub.
    ///
    /// The most common case for calling this function is in tests mocking the
    /// client's behavior.
    pub fn from_stub<T>(stub: impl Into<std::sync::Arc<T>>) -> Self
    where
        T: super::stub::RegionalInventoryService + 'static,
    {
        Self { inner: stub.into() }
    }

    pub(crate) async fn new(
        config: gaxi::options::ClientConfig,
    ) -> crate::ClientBuilderResult<Self> {
        let inner = Self::build_inner(config).await?;
        Ok(Self { inner })
    }

    async fn build_inner(
        conf: gaxi::options::ClientConfig,
    ) -> crate::ClientBuilderResult<
        std::sync::Arc<dyn super::stub::dynamic::RegionalInventoryService>,
    > {
        if gaxi::options::tracing_enabled(&conf) {
            return Ok(std::sync::Arc::new(Self::build_with_tracing(conf).await?));
        }
        Ok(std::sync::Arc::new(Self::build_transport(conf).await?))
    }

    async fn build_transport(
        conf: gaxi::options::ClientConfig,
    ) -> crate::ClientBuilderResult<impl super::stub::RegionalInventoryService> {
        super::transport::RegionalInventoryService::new(conf).await
    }

    async fn build_with_tracing(
        conf: gaxi::options::ClientConfig,
    ) -> crate::ClientBuilderResult<impl super::stub::RegionalInventoryService> {
        Self::build_transport(conf)
            .await
            .map(super::tracing::RegionalInventoryService::new)
    }

    /// Lists the `RegionalInventory` resources for the given product in your
    /// merchant account. The response might contain fewer items than specified by
    /// `pageSize`.  If `pageToken` was returned in previous request, it can be
    /// used to obtain additional results.
    ///
    /// `RegionalInventory` resources are listed per product for a given account.
    ///
    /// # Example
    /// ```
    /// # use google_shopping_merchant_inventories_v1::client::RegionalInventoryService;
    /// use google_cloud_gax::paginator::ItemPaginator as _;
    /// use google_shopping_merchant_inventories_v1::Result;
    /// async fn sample(
    ///    client: &RegionalInventoryService, account_id: &str, product_id: &str
    /// ) -> Result<()> {
    ///     let mut list = client.list_regional_inventories()
    ///         .set_parent(format!("accounts/{account_id}/products/{product_id}"))
    ///         .by_item();
    ///     while let Some(item) = list.next().await.transpose()? {
    ///         println!("{:?}", item);
    ///     }
    ///     Ok(())
    /// }
    /// ```
    pub fn list_regional_inventories(
        &self,
    ) -> super::builder::regional_inventory_service::ListRegionalInventories {
        super::builder::regional_inventory_service::ListRegionalInventories::new(self.inner.clone())
    }

    /// Inserts a `RegionalInventory` to a given product in your
    /// merchant account.
    ///
    /// Replaces the full `RegionalInventory` resource if an entry with the same
    /// [`region`][google.shopping.merchant.inventories.v1.RegionalInventory.region]
    /// already exists for the product.
    ///
    /// It might take up to 30 minutes for the new or updated `RegionalInventory`
    /// resource to appear in products.
    ///
    /// [google.shopping.merchant.inventories.v1.RegionalInventory.region]: crate::model::RegionalInventory::region
    ///
    /// # Example
    /// ```
    /// # use google_shopping_merchant_inventories_v1::client::RegionalInventoryService;
    /// use google_shopping_merchant_inventories_v1::Result;
    /// async fn sample(
    ///    client: &RegionalInventoryService
    /// ) -> Result<()> {
    ///     let response = client.insert_regional_inventory()
    ///         /* set fields */
    ///         .send().await?;
    ///     println!("response {:?}", response);
    ///     Ok(())
    /// }
    /// ```
    pub fn insert_regional_inventory(
        &self,
    ) -> super::builder::regional_inventory_service::InsertRegionalInventory {
        super::builder::regional_inventory_service::InsertRegionalInventory::new(self.inner.clone())
    }

    /// Deletes the specified `RegionalInventory` resource from the given product
    /// in your merchant account.  It might take up to an hour for the
    /// `RegionalInventory` to be deleted from the specific product.
    /// Once you have received a successful delete response, wait for that
    /// period before attempting a delete again.
    ///
    /// # Example
    /// ```
    /// # use google_shopping_merchant_inventories_v1::client::RegionalInventoryService;
    /// use google_shopping_merchant_inventories_v1::Result;
    /// async fn sample(
    ///    client: &RegionalInventoryService, account_id: &str, product_id: &str, region_id: &str
    /// ) -> Result<()> {
    ///     client.delete_regional_inventory()
    ///         .set_name(format!("accounts/{account_id}/products/{product_id}/regionalInventories/{region_id}"))
    ///         .send().await?;
    ///     Ok(())
    /// }
    /// ```
    pub fn delete_regional_inventory(
        &self,
    ) -> super::builder::regional_inventory_service::DeleteRegionalInventory {
        super::builder::regional_inventory_service::DeleteRegionalInventory::new(self.inner.clone())
    }
}
