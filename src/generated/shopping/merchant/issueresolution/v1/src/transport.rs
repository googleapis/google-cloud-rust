// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// Code generated by sidekick. DO NOT EDIT.

#[allow(unused_imports)]
use crate::Error;
use crate::Result;

/// Implements [AggregateProductStatusesService](super::stub::AggregateProductStatusesService) using a [gaxi::http::ReqwestClient].
#[derive(Clone)]
pub struct AggregateProductStatusesService {
    inner: gaxi::http::ReqwestClient,
}

impl std::fmt::Debug for AggregateProductStatusesService {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::result::Result<(), std::fmt::Error> {
        f.debug_struct("AggregateProductStatusesService")
            .field("inner", &self.inner)
            .finish()
    }
}

impl AggregateProductStatusesService {
    pub async fn new(config: gaxi::options::ClientConfig) -> crate::ClientBuilderResult<Self> {
        let tracing_is_enabled = gaxi::options::tracing_enabled(&config);
        let inner = gaxi::http::ReqwestClient::new(config, crate::DEFAULT_HOST).await?;
        let inner = if tracing_is_enabled {
            inner.with_instrumentation(&super::tracing::info::INSTRUMENTATION_CLIENT_INFO)
        } else {
            inner
        };
        Ok(Self { inner })
    }
}

impl super::stub::AggregateProductStatusesService for AggregateProductStatusesService {
    async fn list_aggregate_product_statuses(
        &self,
        req: crate::model::ListAggregateProductStatusesRequest,
        options: crate::RequestOptions,
    ) -> Result<crate::Response<crate::model::ListAggregateProductStatusesResponse>> {
        use gaxi::http::reqwest::{HeaderValue, Method};
        use gaxi::path_parameter::PathMismatchBuilder;
        use gaxi::path_parameter::try_match;
        use gaxi::routing_parameter::Segment;
        use google_cloud_gax::error::binding::BindingError;
        let (builder, method, _path_template, _resource_name) = None
            .or_else(|| {
                let var_parent = try_match(
                    Some(&req).map(|m| &m.parent).map(|s| s.as_str()),
                    &[Segment::Literal("accounts/"), Segment::SingleWildcard],
                )?;
                let path = format!(
                    "/issueresolution/v1/{}/aggregateProductStatuses",
                    var_parent,
                );
                let path_template = "/issueresolution/v1/{parent}/aggregateProductStatuses";

                let resource_name = format!(
                    "//merchantapi.googleapis.com/issueresolution/{}",
                    var_parent,
                );
                let builder = self.inner.builder(Method::GET, path);
                let builder = builder.query(&[("pageSize", &req.page_size)]);
                let builder = builder.query(&[("pageToken", &req.page_token)]);
                let builder = builder.query(&[("filter", &req.filter)]);
                let builder = Ok(builder);
                Some(builder.map(|b| (b, Method::GET, path_template, resource_name)))
            })
            .ok_or_else(|| {
                let mut paths = Vec::new();
                {
                    let builder = PathMismatchBuilder::default();
                    let builder = builder.maybe_add(
                        Some(&req).map(|m| &m.parent).map(|s| s.as_str()),
                        &[Segment::Literal("accounts/"), Segment::SingleWildcard],
                        "parent",
                        "accounts/*",
                    );
                    paths.push(builder.build());
                }
                google_cloud_gax::error::Error::binding(BindingError { paths })
            })??;
        if let Some(recorder) = gaxi::observability::RequestRecorder::current() {
            recorder.on_client_request(
                gaxi::observability::ClientRequestAttributes::default()
                    .set_rpc_method("google.shopping.merchant.issueresolution.v1.AggregateProductStatusesService/ListAggregateProductStatuses")
                    .set_url_template(_path_template)
                    .set_resource_name(_resource_name)
            );
        }
        let options = google_cloud_gax::options::internal::set_default_idempotency(
            options,
            gaxi::http::default_idempotency(&method),
        );
        let builder = builder.query(&[("$alt", "json;enum-encoding=int")]).header(
            "x-goog-api-client",
            HeaderValue::from_static(&crate::info::X_GOOG_API_CLIENT_HEADER),
        );
        let body = gaxi::http::handle_empty(None::<wkt::Empty>, &method);
        self.inner.execute(builder, body, options).await
    }
}

/// Implements [IssueResolutionService](super::stub::IssueResolutionService) using a [gaxi::http::ReqwestClient].
#[derive(Clone)]
pub struct IssueResolutionService {
    inner: gaxi::http::ReqwestClient,
}

impl std::fmt::Debug for IssueResolutionService {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::result::Result<(), std::fmt::Error> {
        f.debug_struct("IssueResolutionService")
            .field("inner", &self.inner)
            .finish()
    }
}

impl IssueResolutionService {
    pub async fn new(config: gaxi::options::ClientConfig) -> crate::ClientBuilderResult<Self> {
        let tracing_is_enabled = gaxi::options::tracing_enabled(&config);
        let inner = gaxi::http::ReqwestClient::new(config, crate::DEFAULT_HOST).await?;
        let inner = if tracing_is_enabled {
            inner.with_instrumentation(&super::tracing::info::INSTRUMENTATION_CLIENT_INFO)
        } else {
            inner
        };
        Ok(Self { inner })
    }
}

impl super::stub::IssueResolutionService for IssueResolutionService {
    async fn render_account_issues(
        &self,
        req: crate::model::RenderAccountIssuesRequest,
        options: crate::RequestOptions,
    ) -> Result<crate::Response<crate::model::RenderAccountIssuesResponse>> {
        use gaxi::http::reqwest::{HeaderValue, Method};
        use gaxi::path_parameter::PathMismatchBuilder;
        use gaxi::path_parameter::try_match;
        use gaxi::routing_parameter::Segment;
        use google_cloud_gax::error::binding::BindingError;
        let (builder, method, _path_template, _resource_name) = None
            .or_else(|| {
                let var_name = try_match(
                    Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                    &[Segment::Literal("accounts/"), Segment::SingleWildcard],
                )?;
                let path = format!("/issueresolution/v1/{}:renderaccountissues", var_name,);
                let path_template = "/issueresolution/v1/{name}:renderaccountissues";

                let resource_name =
                    format!("//merchantapi.googleapis.com/issueresolution/{}", var_name,);
                let builder = self.inner.builder(Method::POST, path);
                let builder = builder.query(&[("languageCode", &req.language_code)]);
                let builder = builder.query(&[("timeZone", &req.time_zone)]);
                let builder = Ok(builder);
                Some(builder.map(|b| (b, Method::POST, path_template, resource_name)))
            })
            .ok_or_else(|| {
                let mut paths = Vec::new();
                {
                    let builder = PathMismatchBuilder::default();
                    let builder = builder.maybe_add(
                        Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                        &[Segment::Literal("accounts/"), Segment::SingleWildcard],
                        "name",
                        "accounts/*",
                    );
                    paths.push(builder.build());
                }
                google_cloud_gax::error::Error::binding(BindingError { paths })
            })??;
        if let Some(recorder) = gaxi::observability::RequestRecorder::current() {
            recorder.on_client_request(
                gaxi::observability::ClientRequestAttributes::default()
                    .set_rpc_method("google.shopping.merchant.issueresolution.v1.IssueResolutionService/RenderAccountIssues")
                    .set_url_template(_path_template)
                    .set_resource_name(_resource_name)
            );
        }
        let options = google_cloud_gax::options::internal::set_default_idempotency(
            options,
            gaxi::http::default_idempotency(&method),
        );
        let builder = builder.query(&[("$alt", "json;enum-encoding=int")]).header(
            "x-goog-api-client",
            HeaderValue::from_static(&crate::info::X_GOOG_API_CLIENT_HEADER),
        );
        let body = gaxi::http::handle_empty(req.payload, &method);
        self.inner.execute(builder, body, options).await
    }

    async fn render_product_issues(
        &self,
        req: crate::model::RenderProductIssuesRequest,
        options: crate::RequestOptions,
    ) -> Result<crate::Response<crate::model::RenderProductIssuesResponse>> {
        use gaxi::http::reqwest::{HeaderValue, Method};
        use gaxi::path_parameter::PathMismatchBuilder;
        use gaxi::path_parameter::try_match;
        use gaxi::routing_parameter::Segment;
        use google_cloud_gax::error::binding::BindingError;
        let (builder, method, _path_template, _resource_name) = None
            .or_else(|| {
                let var_name = try_match(
                    Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                    &[
                        Segment::Literal("accounts/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/products/"),
                        Segment::SingleWildcard,
                    ],
                )?;
                let path = format!("/issueresolution/v1/{}:renderproductissues", var_name,);
                let path_template = "/issueresolution/v1/{name}:renderproductissues";

                let resource_name =
                    format!("//merchantapi.googleapis.com/issueresolution/{}", var_name,);
                let builder = self.inner.builder(Method::POST, path);
                let builder = builder.query(&[("languageCode", &req.language_code)]);
                let builder = builder.query(&[("timeZone", &req.time_zone)]);
                let builder = Ok(builder);
                Some(builder.map(|b| (b, Method::POST, path_template, resource_name)))
            })
            .ok_or_else(|| {
                let mut paths = Vec::new();
                {
                    let builder = PathMismatchBuilder::default();
                    let builder = builder.maybe_add(
                        Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                        &[
                            Segment::Literal("accounts/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/products/"),
                            Segment::SingleWildcard,
                        ],
                        "name",
                        "accounts/*/products/*",
                    );
                    paths.push(builder.build());
                }
                google_cloud_gax::error::Error::binding(BindingError { paths })
            })??;
        if let Some(recorder) = gaxi::observability::RequestRecorder::current() {
            recorder.on_client_request(
                gaxi::observability::ClientRequestAttributes::default()
                    .set_rpc_method("google.shopping.merchant.issueresolution.v1.IssueResolutionService/RenderProductIssues")
                    .set_url_template(_path_template)
                    .set_resource_name(_resource_name)
            );
        }
        let options = google_cloud_gax::options::internal::set_default_idempotency(
            options,
            gaxi::http::default_idempotency(&method),
        );
        let builder = builder.query(&[("$alt", "json;enum-encoding=int")]).header(
            "x-goog-api-client",
            HeaderValue::from_static(&crate::info::X_GOOG_API_CLIENT_HEADER),
        );
        let body = gaxi::http::handle_empty(req.payload, &method);
        self.inner.execute(builder, body, options).await
    }

    async fn trigger_action(
        &self,
        req: crate::model::TriggerActionRequest,
        options: crate::RequestOptions,
    ) -> Result<crate::Response<crate::model::TriggerActionResponse>> {
        use gaxi::http::reqwest::{HeaderValue, Method};
        use gaxi::path_parameter::PathMismatchBuilder;
        use gaxi::path_parameter::try_match;
        use gaxi::routing_parameter::Segment;
        use google_cloud_gax::error::binding::BindingError;
        let (builder, method, _path_template, _resource_name) = None
            .or_else(|| {
                let var_name = try_match(
                    Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                    &[Segment::Literal("accounts/"), Segment::SingleWildcard],
                )?;
                let path = format!("/issueresolution/v1/{}:triggeraction", var_name,);
                let path_template = "/issueresolution/v1/{name}:triggeraction";

                let resource_name =
                    format!("//merchantapi.googleapis.com/issueresolution/{}", var_name,);
                let builder = self.inner.builder(Method::POST, path);
                let builder = builder.query(&[("languageCode", &req.language_code)]);
                let builder = Ok(builder);
                Some(builder.map(|b| (b, Method::POST, path_template, resource_name)))
            })
            .ok_or_else(|| {
                let mut paths = Vec::new();
                {
                    let builder = PathMismatchBuilder::default();
                    let builder = builder.maybe_add(
                        Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                        &[Segment::Literal("accounts/"), Segment::SingleWildcard],
                        "name",
                        "accounts/*",
                    );
                    paths.push(builder.build());
                }
                google_cloud_gax::error::Error::binding(BindingError { paths })
            })??;
        if let Some(recorder) = gaxi::observability::RequestRecorder::current() {
            recorder.on_client_request(
                gaxi::observability::ClientRequestAttributes::default()
                    .set_rpc_method("google.shopping.merchant.issueresolution.v1.IssueResolutionService/TriggerAction")
                    .set_url_template(_path_template)
                    .set_resource_name(_resource_name)
            );
        }
        let options = google_cloud_gax::options::internal::set_default_idempotency(
            options,
            gaxi::http::default_idempotency(&method),
        );
        let builder = builder.query(&[("$alt", "json;enum-encoding=int")]).header(
            "x-goog-api-client",
            HeaderValue::from_static(&crate::info::X_GOOG_API_CLIENT_HEADER),
        );
        let body = gaxi::http::handle_empty(req.payload, &method);
        self.inner.execute(builder, body, options).await
    }
}
